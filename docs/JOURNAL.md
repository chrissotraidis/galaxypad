# GalaxyPad journal

## Latest blocker: dome Pull Star / galaxy selection

User reports right-stick aim plus A fails to activate the blue Pull Star, blocking
level entry. Original controls require hand-cursor acquisition then held A; Xbox
RB duplicates A, while RT is B and spends Star Bits. Exact dome reproduction and
held-input/coordinate audit are in progress. Do not infer playability from its
60.0 HUD screenshot. See [current handoff](PERFORMANCE-2026-09-12.md#newly-reported-level-entry-blocker).
The controller Menu event fix passes host regression and the full Simulator UIKit
fixture, but is not yet on the physical iPad. Release remains NO-GO.

## 2026-09-12 — installed performance/audio candidate; physical feedback and Start fix

Current summary: [performance and hardware handoff](PERFORMANCE-2026-09-12.md).
Logging-off matched-route gain is roughly 44→58 frame events/s. Separate diagnostic
control recorded 157 underruns; combined candidate recorded zero. Actual mixer PCM
has no sustained silence in its 60–120 second gameplay interval; listening and
physical audio acceptance remain open. These measurements must not be conflated.

Installed signed physical host:
`85d84c887cae4b3736b5304cbf757a0543d74e99b4e418e3618184c73b00d0ad`.
Signed nested module:
`aa7d6b6f38d0fae938dcc83509672e6d5ef822567eb0d5d005e5547b3c1e55d2`.
Evidence: `generated/runtime/ipad-iteration-1/physical-dual-core-audio-tempo-20260912/`.
In-place install and process inventory establish launch; the game save readback is
identical. Six configuration/history/preference files changed while the user
started playing; do not claim complete snapshot equality or restore older data.
Selected render scale became 2×; active physical resolution remains unverified.

User reports stable, promising gameplay with remaining slowdowns and intermittent
Xbox Start/Menu. New source fix uses event-time button state; exact old source
fails the queued quick-tap regression and current source passes. Evidence:
`generated/experiments/controller-menu-event-20260912/result.json`.
The installed hash above predates this follow-up. Next: validate the new controller
candidate, merge the documented work, then rebuild/retest before another hardware
promotion. Confirm physical Xbox behavior directly with the user. Compare known
active 1× and 2× physical settings before attributing slowdowns or extrapolating
to iPhone. Four-minute Simulator capture still needs review and a matched control.

The app runs native AOT ARM64 game code with a Dolphin-derived Wii runtime and
interpreter fallbacks; it is not emulator-free. No release acceptance is claimed.

## 2026-09-12 — repeated dual-core gain and real-game tempo continuity

Dual-core repeat logging-off HUD over120s:54.5,55.5,58,60,58,57,59,59,59,57.2,
60,59.1,58.2. Fresh unchangedcontrol60s:43,41.8,44,45,41.8,46.4,44.5. Two
candidate windows support the threading bottleneck fix; continuous frame tails
are not established by sparse HUD. Summary: generated/runtime/ipad-iteration-1/
dual-core-comparison-20260912/summary.json. Private candidate retained for gates.
Separate dual-core original-audio diagnostic:21new underruns/57.999946s,
97.1365%nonzero output,13946real enqueues. This improves supply but still fails
no-recurring-underrun target. Three-dot runtime/inputblock and nativepause/resume
checked through Simulator UI with counters; initial idle-disconnect dialog phase
explicitly excluded. Evidence: dual-core-lifecycle-20260912/review.json.

Full isolated tempo-v8 core rebuild passes1550compiler-policy entries,18core
Mixer layout consumers plushostconsumer,platform/signatureand45controlhashes.
Host bb8b49859d1573122ce5bcb8c001b7bc91f7827c47a838cb66c2c802a8aabba9.
Single-core logging-off audio candidate HUD41,42,44,44.6,43.6,45.5,45.5; no
performance win claimed. Actual separatediagnostic58.099106s:0newunderruns,
0newfull/backlogdrops,0shortcallbacks,2788864/2788864nonzero outputframes,
10580real128-frame enqueues (~23309inputframes/s). Evidence:
generated/runtime/ipad-iteration-1/audio-tempo-v8-diagnostic-20260912/candidate/audio-summary.json.
Cumulative26underruns precededthiswindow;321full-drop events also predatedit,
including200duringnavigation. Newdropunit is rejected8-frameinputcalls whereas
oldFIFOeventsrepresented128-framegranules; do notcomparethosecounts directly.
No resets/suppression. Soundquality/startup/pause andphysicalacceptance remain.

Combined private dual-core+tempo-v8 host built/signed with matchedMixerheaders:
ef9898603ebf4eddf7fb01f6406f862aa20e03ad808025cf48078b50bcc69953,
generated/build/ios-simulator-dual-core-audio-tempo-20260912/GalaxyPad.app.
Nowunderlogging-off120sroute, followedbyactualaudioandlifecyclegates. OldPGOmodule
90e24dfb4e96597686b8fccf09bb55efdcbd7d059dd3ff710a9f32fab26f353f remainsunchanged.
No physicalinstall. Physicalrecipeaudit foundseparateIOSsignedcontrolmodule919b…;
Simulator90e… isnotdevice-compatible. DevicefullaudioABI rebuildpreparedonly.


## 2026-09-12 — dual-core gain under repeat; audio tempo v8 source gate passed

The isolated session-only MAIN_CPU_THREAD=true host produced logging-off HUD
56.4,55.5,58.0,58.0,58.0,59.1,59.1 FPS over60s. All8scene/window images were
directly verified as active121-star Observatory gameplay. Live executable SHA
ad0add304f633bacae5bb97565038417208c268a31cec8e28efa19d025a17d86 and separate CPU
and Video threads confirmed after acceptance window. Original module remains
90e24dfb4e96597686b8fccf09bb55efdcbd7d059dd3ff710a9f32fab26f353f. This is a
promising provisional gain, not sustained60FPS or physical acceptance. Fresh
control and120srepeat are being run without build contention. Sparse HUD does
not establish frame tails, speed or audio. Evidence and exact launch manifests:
generated/runtime/ipad-iteration-1/dual-core-comparison-20260912/.

Audio tempo copied integration v8 passes30 exact48k Mixer cases plus sanitizers,
including slow67-100%production, bursts, steps and actual pause/restore paths.
Measured oldest sample age max118.625ms; unavailable windows zero in supported
producing cases,234 during real1sstarvation. Real enqueue counters remain real.
These are offline tests, not realgame acceptance. Full core+host dependents must
rebuild because Mixer layout changed. Frozen source under
generated/experiments/audio-tempo-integration-v8-20260912/; Mixer.cpp SHA
102a876d8efe784e2c37c4cadae46eadc4479f0776797a8388d16d31215565e1.
First configure stopped at a false source-set gate: duplicate sources collapsed
1550target entries to1539. The strict gate is repaired and1550commands match
control, with7regression checks. Fresh recipe ready under
generated/build/ios-simulator-audio-tempo-isolated-retry-20260912/; compilation
waits for logging-off windows. Normal startup DMA32k is supported; any other
DMA rate latches candidate failure and must be checked in actual diagnostics.
No120msbuffer increase, physical install or release selected.


## 2026-09-12 — actual audio replay rejection; tempo prototype underway

The user reiterated fixing audio underruns. Actual matched-scene diagnostic
intervals (not FPS acceptance) reject simple replay: candidate236 underruns over
~53.2s (4.436/s), control184 over~58.1s (3.167/s), approximately2359guestframes each.
Candidate nonzero output100%; control69.1624%. Producer23672.8/21665.2samples/s
tracks emulated44.342/40.602FPS, so sustained CPU slowdown explains the supply
shortfall. Both diagnostic image sets were verified in the same gameplay scene.
No counter suppression, physical promotion or audio-quality acceptance.
Exact logs, counters, hashes, scene reviews and decisions:
generated/runtime/ipad-iteration-1/audio-gap-fill-diagnostic-20260912/comparison-summary.json.
Logging-off audio candidate HUD42,43,45.5,46,45.5,45,44.6; no improvement selected.

A new isolated pitch-preserving tempo prototype is being tested in
experiments/audio-tempo/AudioTempo.h with tests/audio-tempo.cpp and
tests/test-audio-tempo.py. It has not been integrated into the app. Current work
covers actual32k→48k callback sizes,67%production/bursts, pitch/stereo/transients,
startup/stall recovery and measured source wall-age within120ms, not merely
input-frame capacity. Tests exposed spurious unity re-entry and stale recovery;
those are being corrected before integration. Reset must be quiescent; live
pause handling needs consumer-only flush. No new audio-buffer increase accepted.
If prototype passes, changing Mixer layout requires a complete isolated core
and dependent allocation rebuild, plus legacy-unity-output and realgame gates.

CPU scheduling audit excludes E-core starvation in the observed interval:
CPUthread99.27% walltime; at least97.44% execution on Pcores. Whole-chunk restrict
is unsafe through callback aliases. Evidence: generated/cpu-scheduling-audit-20260912/
and generated/cpu-state-alias-audit-20260912/. No priority/alias hack selected.

## 2026-09-12 — O3 rejected; audio gap-fill candidate running

O3+oldPGO module aa8760b88656aa05f9f3e0c6c6becaace47f3b2802ad5193682413d85a622d11
shows no gain versus unchanged O2+oldPGO. All14candidate/immediate-control images
were directly reviewed: same scene, no dialogs/crash. OCR means44.429/45.286;
manual review differs in three small candidate HUD readings, retained separately.
Either reading rejects a gain. Exact records/limits:
generated/runtime/ipad-iteration-1/o3-old-pgo-comparison-20260912/summary.json.
Earlier delayed control windows invalidated by guest idle disconnect are excluded.
Capture now supports --provisional immediately after launch, with mandatory later
visual review; stale scene checks remain. Do not insert model/build delays between
route and capture. No capture is accepted based solely on route success.

M3 tuning rejected before fullbuild: actual Apple backend emitted identical
native and linked instructions for both hotTUs. Evidence:
generated/target-tuning-source-audit-20260912/two-tu-screen/summary.json.

Next actual audio source candidate restores the configured generic gap filler
while Running; Apple DMA earlyreturn had bypassed AudioFillGaps. Offline exact
FIFO tests at44/60 production removed recurring~122ms zero-output spans; fullspeed,
disabledgapfill, stalls and pause/resume checks pass O2/ASan/UBSan. Musical replay
quality and runtime underruns remain unverified; do not claim sound accepted.
Host4e8d29281a536d35e9960e0a4adccbce937fef2f1c29e9903ec177842468bce8:
generated/build/ios-simulator-audio-gap-fill-app-20260912/GalaxyPad.app.
Normal controlmodule90e24 remains unchanged. OneMixerobject selected in linkmap;
baseline relink reproduced b3ed exactly, signatures pass. Patch/tests/buildrecipe:
generated/experiments/audio-gap-fill-20260912/. Immediate route/capture:
generated/runtime/ipad-iteration-1/audio-gap-fill-comparison-20260912/candidate.
No physical promotion. Next: evaluate logging-offFPS, then separate actualaudio
output/underrun diagnostics against unchangedcontrol; reject regressions honestly.

## 2026-09-12 — fresh LUT rejection; O3 with validated PGO building

Fresh LUT run stayed in gameplay:43.6,44.6,43.6,44.5,43,43.7,44 HUD FPS,
mean43.857 versus controls44.614/44.786. Reject as performancecandidate;
memoryreduction alone does not meet the selection rule. Exact retained evidence:
generated/runtime/ipad-iteration-1/coarse-lut-comparison-20260912/summary.json.

Next build command: python3 scripts/prepare-o3-old-pgo-experiment.py --output
generated/build/ios-simulator-o3-old-pgo-20260912 --execute --jobs 4.
This changes only final frontendO2→O3 while retaining validatedoldPGO, strictFP,
normalABI and unchangedThinLTOlinkpolicy. PreviousO3trialwasunprofiledand cannot
answerthiscomparison. Recipe checks source/input stamps, compile/linkparity,
profilediagnostics and module-table parity. Build log:
generated/runtime/ipad-iteration-1/o3-old-pgo-build-20260912.log.
Matched freshcontrol route: generated/runtime/ipad-iteration-1/o3-old-pgo-comparison-20260912/control.
Wait for compilers/linkers to end before timing. No candidate selected/deployed.

## 2026-09-12 — two CPU source screens rejected

MEM1-first precedence-preserving helper passed1.2M mapping cases plus callback,
overlap, journal and reservation checks at O2 and ASan/UBSan. Exact old-PGO
func805170A0 codegen grew23467→24116 instructions, addingregisterpreservation for
onlyoneinstruction saved on its firstMEM1 path. Reject beforefullbuild.
Evidence: generated/experiments/mem1-first-precedence-20260912/.

Shared ps_madds0/1 multiplier rounding passed960,016 completeCPU/host-FPflag cases.
Fair separate-TU ThinLTO+oldPGO microcomparison regressed madds0 +12.56%, madds1
+10.13%, losing everypair. Reject beforefullmodulelink; profile-free microgain
is superseded. Evidence: generated/shared-ps-rounding-20260912-final/policy-cost-summary.json.
These source/codegen/cost screens are not game FPS acceptance.

The invalid LUT-window Wii Remote disconnect OSD maps to guest HCI_CMD_DISCONNECT;
neutral reports continue and A activates the emulated remote. No hostkeepalive or
automaticinput hack is justified. Sourceaudit: generated/runtime/ipad-iteration-1/coarse-lut-comparison-20260912/idle-disconnect-source-audit.md.
Fresh LUT repeat is underway after external compiler jobs ended; no gainselected.

## 2026-09-12 — coarse LUT remains unselected; guest Back verified

Coarse LUT host3c63d8085e42fb45af142b3d7d6194f01ecc433ae3c5d1d9ad2cacceb9724ec8
with unchanged old PGO launched the correct scene. First window became invalid
when the emulated Wii Remote disconnected and a dialog reported60FPS. This is
not a gain. After A reconnect, pointer(0.5,0.45),2s aim then2s A selected Back and
returned to gameplay, confirming the original guest menu exit route.
Updated controller help source explains Back+A; native Xbox toggle remains separate.

Repeat HUD45,45.5,44,41.8,45,32.7,33.6; last dips coincided with CPU allocation
88.6/88.5% and numerous external compiler jobs appearing in host-after inventory.
Candidate remains unselected pending comparable host conditions. RSS about700864KiB
is lower than the roughly763000KiB control, but no FPS gain is established.
Exact commands, hashes, images, manifests and decision:
generated/runtime/ipad-iteration-1/coarse-lut-comparison-20260912/summary.json.
Next prepared source screens: safe MEM1-first address selection preserving live
metadata and MEM2 overlap precedence; shared exact multiplier rounding in
ps_madds0/1. No unsafe fixed-map contract or altered FP behavior accepted.

## 2026-09-12 — dispatch boundary measured and rejected

Both preserve_none variants completed the fixed logging-off Observatory window.
V1 mean43.043 HUD FPS; corrected inline v2 mean43.586; repeated unchanged control
44.786. Reject both for no improvement. V2 final linked assembly eliminated the
normal ABI spill bridge, but that source change did not improve this scene.
Exact seven-point readings, manifests and hashes:
generated/runtime/ipad-iteration-1/preserve-none-comparison-20260912/summary.json.
V2 module SHA42380a4cdc0f03458e6789fdbced98bce1c1395d9cdbe79a6194ccfa62e0e2fb;
host90fdd93b7f0f66d67ae2cb6f75c9839b4336d1675403b2b1870352b276a55546.
A separate post-window sample is diagnostic only. Sparse HUD windows do not prove
frame tails, audio or physical controls; no physical deployment.

Next small source candidate: validated32-byte eligibility lookup buckets reduce
88MiB to11MiB; generic unaligned modules retain4-byte fallback. Actual-source
ASan/UBSan fixture passed81scenarios/423,360address and invalidation comparisons.
Copied source/patch/buildrecipe: generated/coarse-chunk-lut-20260912-final/.
Build command: python3 generated/coarse-chunk-lut-20260912-final/build-host.py.
Uses normal ABI and unchanged old PGO independently of the rejected ABI variants.

## 2026-09-12 — measured PGO rejection; dispatch boundary next

Logging-off, visually verified 121-star Observatory route, scale 1, aspect 0,
same pause-toggle host b3edb1647b350bc35256f8fba8c2e63878b842b7b5111b8df0864c7eb2018d43:
old PGO control averaged 44.614 HUD FPS (44–45.5); fresh heavy-profile candidate
averaged 43.671 (42.7–44.6). Reject the fresh profile; retain control module
90e24dfb4e96597686b8fccf09bb55efdcbd7d059dd3ff710a9f32fab26f353f.
Exact observations/hashes: generated/runtime/ipad-iteration-1/heavy-profile-comparison-20260912/summary.json.
These are seven sparse HUD observations over 60 seconds, not continuous frame-tail
or timed audio acceptance.

Extended TSP module 01c45a1be01c7b603f933c48bb563c52497a448f081c403fc2dd8572638cbb64
had HUD 29.1,20,30.9,45,45,45.5,44 with matching CPU allocation dips and concurrent
external builds/emulators. First window inconclusive; unselected pending repeat.
Evidence: generated/runtime/ipad-iteration-1/ext-tsp-comparison-20260912/candidate.

Next source candidate carries preserve_none through the optional versioned module
boundary into Run, retaining the normal descriptor fallback. Build succeeded;
Simulator comparison starts at generated/runtime/ipad-iteration-1/preserve-none-comparison-20260912/candidate.
Build command: bash generated/experiments/preserve-none-boundary-20260912/build-candidate.sh.
Host SHA-256: 90fdd93b7f0f66d67ae2cb6f75c9839b4336d1675403b2b1870352b276a55546.
No physical install or acceptance yet.

Continuous profile output must include %c in LLVM_PROFILE_FILE; overriding the
embedded filename without it disabled live updates. The harness now rejects this
invalid training configuration. Explicit flushed interval above remains valid.
Touch guest Start+ repeat did not close the original guest menu in a separate
logical input run; B also did not close it. This remains open, distinct from the
software-tested native Xbox Menu/Options toggle fix. Evidence: heavy-profile-comparison-20260912/candidate/plus-*.png and plus-input-diagnostic.log.

## 2026-09-12 — renewed Simulator performance loop

Latest user instructions supersede the old single-Simulator and single-subagent
limits: multiple Simulators are allowed, with multiple secondary-account Astra
high agents for independent source work. Primary owns candidate selection/builds
and the current Simulator route. Other apps/data are preserved. Work continues;
no performance success or physical promotion is claimed.

New executable loop: `scripts/galaxypad-simulator-comparison-loop.py`.
For each one-change candidate, run `launch APP MODULE FRESH_OUTPUT`, visually
verify `scene.png` (Luigi,121 stars,2324 bits,Observatory map platform), repair
navigation if needed, then `capture OUTPUT --scene-verified --seconds 60`.
Repeat unchanged control/candidate under comparable host conditions, record the
decision, and move to the next measured hypothesis. The loop checks live host and
module hashes, refuses instrumented modules, verifies scale1/aspect0/loggingNO,
records concurrent Simulator/host inventory and sparse HUD/process observations.
The route stops before sampling and backs up Simulator Wii state before reseeding.
It does not provide continuous frame-tail, speed or timed audio proof; those
remaining acceptance limits must not be inferred from sparse screenshots.

Current PGO comparison uses identical host
`b3edb1647b350bc35256f8fba8c2e63878b842b7b5111b8df0864c7eb2018d43`,
control module `90e24dfb4e96597686b8fccf09bb55efdcbd7d059dd3ff710a9f32fab26f353f`,
and fresh candidate `54b117d28c18e27241c3241797e42505d00aab3492863db1b147f7773e70d1ec`
at `generated/build/ios-simulator-heavy-pgo-20260912/gRMGE01_recomp.dylib`.
Evidence: `generated/runtime/ipad-iteration-1/heavy-profile-comparison-20260912/`.
Matched windows completed: fresh profile rejected; see newer result above.

Heavy profile capture was repaired: continuous flags did not produce live file
updates. The first raw file had total count1. An explicit diagnostic LLDB call,
`expression -- ((int (*)())(void*)__llvm_profile_write_file)()`, appends a complete
32,388,648-byte record. Extracting the last record before/after a60-second
stationary Observatory interval, validating identical schemas/nonnegative deltas,
and merging only those deltas produced profile
`f46ec57792e9714c694d33998153addc88d6d90d6a015e5796df501bf5f0cd9c`.
All32,551 functions match;5,562 have positive interval counts. Files, debugger
commands and timing: `heavy-pgo-training-20260912/retry/flushed-window/` under the
same evidence root. Debugger/training observations are not FPS acceptance.
The build uses `GALAXYPAD_PGO_PROFILE=$PWD/generated/runtime/ipad-iteration-1/heavy-pgo-training-20260912/retry/flushed-window/observatory.profdata`
and `GALAXYPAD_SIMULATOR_MODULE_BUILD=$PWD/generated/build/ios-simulator-heavy-pgo-20260912`
with `GALAXYPAD_BUILD_JOBS=4 bash scripts/build-ios-simulator-fresh-module.sh`.
Build passed; no hash mismatch, only existing comment/unprofiled integer-file warnings.

Parallel source decisions: four-stage vertex dispatch unroll rejected after819
ASan/UBSan parity cases and16 slower paired timing rounds (+2.9–13.7%). Evidence
`generated/vertex-stage-unroll-20260912-screen/`. Simple per-chunk `preserve_none`
ABI also rejected: wrapper preservation offsets the savings;46,080 O2/UBSan
state/memory/callback cases pass but complete boundary code grows. Evidence
`generated/runtime/ipad-iteration-1/audio-generated-source-audit-20260912/REPORT.md`.
A versioned optional dispatch ABI reaching the actual host Run loop is now being
screened separately, with canonical ABI unchanged. No speculative audio-buffer
increase: existing audio sample mostly waits and does not establish mixer cost
as the CPU bottleneck. Zero short callbacks is not callback deadline proof.

## 2026-09-12 — Menu resume regression reproduced and fixed in Simulator

The production controller adapter previously stopped reading input whenever native
pause blocked gameplay. The second Menu press therefore could not resume. It now
observes Menu/Options release and press while the native pause panel is visible,
and toggles that panel; unrelated dialogs still block the shortcut. Pause re-arm
requires Menu/Options release, not perfectly centered sticks. Touch Start + remains
the guest Wii Plus input pending a separate live-game repeat-press check.

The isolated UIKit controller test reproduced the old gate failure, then passed
with the corrected adapter. Evidence:
`generated/runtime/ipad-iteration-1/pause-toggle-20260912/before-console.log`
(`FAIL: second Menu press resumes while gameplay is blocked`) and
`after-console.log` (`GALAXYPAD_UI_TEST_PASS`). These use software controller
snapshots on the sole existing Simulator, not physical Xbox acceptance. The added
held-stick regression also passes in `after-held-stick-console.log`; the final
host gameplay comparison remains in progress. Fresh Simulator host:
`generated/build/ios-simulator-pause-toggle-app/GalaxyPad.app/GalaxyPad`, SHA-256
`b3edb1647b350bc35256f8fba8c2e63878b842b7b5111b8df0864c7eb2018d43`.
Built with `cmake --build generated/build/ios-simulator-pause-toggle-app -j 4`,
then `codesign --force --sign -` on that app and strict deep verification passed.
Test app built with `bash tests/build-mobile-ui.sh`; executable SHA-256
`0cae6f819cb9c6dc9d8b58eb1e03b86d22f73ba032adcfa426c7fe66edc7f1c1`.

The module build script also now clears cached C and shared-linker profile flags
when switching back to unprofiled mode. A real CMake cache probe reproduced stale
flags and verified the fix; see `pgo-mode-reset-20260912/result.txt` under the same
runtime evidence root. This is build correctness, not an FPS claim.

The first Observatory training capture is rejected for profile generation: its
counter-before copy occurred after termination, so it cannot isolate the verified
60-second scene from lengthy title/navigation training. Preserve
`heavy-pgo-training-20260912/observatory-35979.profraw` as diagnostic evidence only.
Next: capture before/train/stop/after in one sequence after visual scene verification,
validate counter deltas, then build an isolated profile-use module. The unchanged
PGO control remains selected; physical app and saves are untouched. Goal stays active.

## Resumed on the existing Mac Simulator — 2026-09-12

### Decision: removing PGO rejected

The same handoff host with the preserved unprofiled module
`f988f9a78f875f78c2c9ae89f458c2d85785c0e62de4595ef12d1af7734c8b03`
reached the same scene but yielded 38.2/37.3/37.0/38.0 sparse HUD FPS,
versus 45.5/45.5/44.0/45.5 with PGO. CPU snapshots were 102.0–104.4%
and RSS 774,960–775,456 KiB. Reject removing PGO as an improvement.
Evidence: `generated/runtime/ipad-iteration-1/resume-20260912-existing-unprofiled/`.
Canonical PGO module and host artifacts remain unchanged. GalaxyPad was stopped
after the trial; the existing Simulator and MeleePad remain running.

Next performance hypothesis: train the existing instrumented module on this
verified 121-star Observatory route and compare one new profile-use build to
the unchanged PGO control. Prior training came from the wrong opening scene;
current PGO clearly helps but does not establish that its profile is optimal.
Use the same host/module inputs, exact advanced seed, camera, orientation,
resolution and logging-off capture. Never call training/profiling FPS acceptance.
The physical build/install gate remains unmet. The goal is active, not blocked.

### Correct-scene comparison and attribution

On the existing single Simulator, the unchanged control yielded sparse stationary
HUD readings 45.0/45.0/44.0/45.0 emulator FPS. The handoff-fix host with the same
PGO module yielded 45.5/45.5/44.0/45.5. This is no meaningful performance win;
the source guard remains a tested correctness candidate, not a promoted
performance candidate. Both visibly retained Luigi at the same 121-star
Observatory platform, scale 1 and aspect mode 0, with frame logging off.

A separate post-window 8-second CPU sample in
`resume-20260912-existing-candidate/cpu-sample-diagnostic.txt` was processed with
`python3 scripts/summarize-fallback-sample.py <sample> --output <cpu-self-summary.json>`.
Of 6,402 CPU-thread wall-stack samples, 3,397 belong to generated chunks (53.1%),
622 to the host Run symbol (9.7%), 269 to dispatcher (4.2%), and 304 to out-of-line
PPC helpers (4.7%). Largest individual generated function: func_804B60A0, 155
samples. These are conserved self counts, not CPU-time percentages or frame
acceptance. This favors investigating broad native execution/profile coverage
over another tiny vertex-conversion change.

The existing post-run local diagnostic report for control (exported without
sending it) records 778 cumulative DMA underruns, 7 backlog drops and 241.467 ms
maximum producer gap. It includes startup/navigation and is not an audio delta
for the stationary window. Thermal 0 means nominal or unsupported. Continuous
frame-window FPS, frame-tail statistics, emulation-speed percentage and timed
audio deltas remain unavailable in this logging-off capture; no acceptance is
claimed from their absence. Physical installation and saves remain untouched.

The user's continuation is being executed on the sole already-booted iPad
Simulator (`94BACEE0-DE7F-4D31-8097-4F3F0B02C7D1`, same M5 13-inch model).
GalaxyPad uses its own app container; MeleePad was not terminated or modified.
This resolves the prior access assumption without booting a second iPad.

The advanced file is now visually verified: `slot1-details.png` shows 121 stars,
2324 star bits, and Complete; `loaded-scene.png` shows Luigi at the Observatory
central map platform with those counts. Initial HUD reading is 39.0 emu FPS.
Evidence is `generated/runtime/ipad-iteration-1/resume-20260912-existing-control/`.
The logging-off stationary control retains screenshots/process snapshots and
is not instrumented or sampled; startup confirms scale 1, aspect mode 0,
frame_logging 0. Full performance/audio acceptance remains open.

The canonical unattended harness now defaults to the private
`generated/runtime/ipad-iteration-1/advanced-seed/Wii` copy and validates the
exact advanced save hash before any Simulator operation. A deliberate old-seed
attempt exited 1 with `save seed does not match the verified 121-star file`.
It also captures file selection/details and removes the obsolete opening-story
A pulses. The original backups and physical saves were not changed. The harness
still performs diagnostics after its route; it must not be mistaken for a
logging-off unsampled FPS acceptance tool.

## Resume audit and touch handoff fix — 2026-09-12

### Blocked audit — exclusive Simulator access required

On the third consecutive goal turn with this conflict, `xcrun simctl list
devices booted` still reports only MeleePad Netplay iPad booted. GalaxyPad remains
shut down. The handoff candidate and isolation guard have completed their
available offline/UIKit checks; no valid late-game control/candidate comparison
or physical promotion has occurred. `git diff --check` remains clean.
The goal is blocked pending the other task releasing Simulator access. Do not
terminate that unrelated task or bypass the one-iPad limit. Resume from the
prepared advanced-save file selection, verify 121 stars and Observatory, then
measure control before testing/promoting a performance candidate.

### Current stop point: Simulator isolation conflict

The next continuation revalidated that only MeleePad remains booted. The
canonical harness now rejects every unrelated booted Simulator, including when
GalaxyPad is also booted; `GALAXYPAD_ALLOW_OTHER_SIMULATORS` no longer bypasses
the check. The prepared file-select harness has the same guard. A live attempt
with the old override set exited 1 before boot/install/save mutation, as required.
Evidence: `generated/runtime/ipad-iteration-1/resume-20260912-isolation-guard/`.
`bash -n` and `git diff --check` pass. This fixes the harness isolation defect;
it does not establish gameplay performance or prevent another task from booting
a device later. Recheck isolation during any future capture.

The advanced-save control reached file selection, but another task booted
`MeleePad Netplay iPad` during the run. GalaxyPad's dedicated Simulator was
terminated and shut down immediately; only the unrelated MeleePad Simulator
remains booted. Do not restart GalaxyPad while another iPad is booted.
No heavy-scene or candidate/control result was produced, and the candidate
was not installed on the physical iPad.

Prepared seed: `generated/runtime/ipad-iteration-1/resume-20260912-heavy-control/seed-Wii`
contains a copy of the verified `cd8d1fa9…` save; original backups are untouched.
Evidence: `file-select-ready.png`, `save-seed.sha256`, `isolation-conflict.txt`,
and `shutdown.log` in that run directory. File selection shows occupied Mario
slot 1; the star count/Observatory still need direct visual verification.
Next: after the other Simulator is shut down by its owner, use the preserved
`file-select-harness.sh`, select slot 1, inspect its star count, then Play and
verify the Observatory. The initial 30-second A+B hold still showed the title;
a fresh 1-second A+B edge advanced to file selection. Do not reuse the old
opening-story pulse sequence blindly. Only then establish a logging-off
control and compare the isolated handoff candidate using the same PGO module.

The resumed logging-off control exposed an invalid scene assumption: the
unattended harness defaults to `preinstall/Wii`, whose GameData SHA-256 is
`717f7fb3e0749b404adf551332ea0ce457a182c64e17b750bab60b197476e36c`.
This is the original zero-star backup (see `IPAD-FEEDBACK-2026-09-11.md`),
not the later 121-star save. Current screenshots show Star Festival, not the
Observatory. Earlier unattended results using this seed must not be called
late-game heavy-scene acceptance. PGO format validation does not establish
representative late-game training.

Evidence: `generated/runtime/ipad-iteration-1/resume-20260912-baseline/`.
The generated-only acceptance harness disables frame/phase logging and sampling
and stops its startup console stream before the stationary window. Sparse HUD
readings at 0/10/20/30 seconds were 60.0/60.0/59.1/58.2 emulator frame events/s;
these are not a continuous frame-window or display-completion measurement.
Frame-time tails, emulation-speed percentage, guest thermal state, and audio
underrun deltas were unavailable with this logging-off path. Concurrent host
work qualifies all timings. No performance candidate is accepted from this run.

A separate concrete controller/touch defect was reproduced in the UIKit test:
after controller auto-hide clears touch aim, a new screen contact could reclaim
pointer ownership even while controls were hidden. `touchesBegan` now rejects
contacts while `_touchControlsHidden` is true. The regression checks clear,
rejection while hidden, and reacquisition when shown. Before-fix evidence is
`generated/tests/mobile-ui.1e2Lhv` (FAIL: hidden touch controls cannot reclaim
controller aim). The after-fix UIKit run passed all checks, including the new
regression (`generated/tests/mobile-ui.hlNXD7`; wrapper
`resume-20260912-baseline/mobile-ui-after.log`).
Input/controller/settings shell tests pass. This is a
correctness candidate, not a claimed FPS optimization or physical input pass.

Exact artifacts:
- Unchanged control executable: `650bc3b5e34d60ab58d4974d81f934327b4fdbf4d848b33478b3929e717359f8`.
- Selected PGO module: `90e24dfb4e96597686b8fccf09bb55efdcbd7d059dd3ff710a9f32fab26f353f`.
- Validated profile: `784f47e4904a36319b7805cd7807091d1d486ccc4cacc09f4785636d3949ca54`.
- Isolated handoff candidate executable: `2446fbd5534a6893f406909b385c6dd605abed525eda7c3e9ddc1b5c8e861954`.
- Preserved normalized advanced save: `cd8d1fa98c266aa321dbb018247e5df7a084e5330ffa55f2012df4971b1bc473`.

Build command: `cmake -S apple/ios -B generated/build/ios-simulator-handoff-app
-G Ninja -DCMAKE_TOOLCHAIN_FILE="$PWD/scripts/ios-simulator-toolchain.cmake"
-DCMAKE_BUILD_TYPE=Release`, then `cmake --build
generated/build/ios-simulator-handoff-app --parallel 4`. Ad-hoc signing and
`codesign --verify --deep --strict` passed. Build output is
`resume-20260912-baseline/handoff-build.log`. Control artifacts were not rebuilt.

The physical app was observed alive at PID 9161. The preserved staged bundle
`generated/device-stage.JOtjGn/GalaxyPad.app` passes strict signature verification;
its host/module hashes are `b62abff0c0401ddfd390dbb3efad9124c67a14f4e6873a3b7784caf9ee4da05d`
and `919b2382ebf37007bf86865f1d2f46ca048bec61b45574797483684a039a047b`.
Those are local staged-file hashes, not a readback hash of the installed bundle.
No physical install, save replacement, or physical input test occurred.

Next gate: verify the advanced file and Observatory visually in the sole
Simulator before a matched control/candidate capture. Keep the 120 ms audio
buffer, 4:3 default, module, resolution, and scene fixed. Do not promote this
candidate until the required heavy-scene/audio gate is available and passes.

## 2026-09-12 — self-audit follow-up: clean-up and PGO provenance

- The preceding work window did not land a performance fix or a commit. It
  produced rejected candidate comparisons, diagnostic logging, UI/default
  corrections, and documentation. That is activity, not resolution.
- A process audit found twelve stale, read-only `afcclient` jobs from earlier
  MeleePad log reads, each running for roughly 30–58 minutes at about 92–99%
  CPU under the Codex app server. I terminated only those explicit transfer
  jobs. Android QEMU, Spotlight/FileProvider, MacDroid, and other unrelated
  processes remain untouched, so the host is still not a clean benchmark host.
- A fresh logging-off control then reached the seeded late-game scene on the
  dedicated iPad Pro 13-inch M5 Simulator. The save seed remained
  `717f7fb3e0749b404adf551332ea0ce457a182c64e17b750bab60b197476e36c`;
  startup logged `render_scale=1 aspect_ratio_mode=0`, the screenshot was
  2064x2752, and the visible counter read 56.4 emulator FPS. An 8-second host
  sample contained 4,392 `StaticRecompCore::Run` samples, 4,088 nested
  `chassis_dispatch` samples, and 581 `VertexLoader::RunVertices` samples.
  The run was terminated cleanly and is retained at
  `generated/runtime/ipad-iteration-1/self-audit-control-after-afc-clean-20260912/`.
  It is current control evidence, not clean performance acceptance, because
  QEMU and system services still consumed substantial CPU.
- Build provenance is the higher-leverage next gate. The selected Simulator
  module is `generated/build/ios-simulator-fresh-module/gRMGE01_recomp.dylib`
  at SHA-256
  `f988f9a78f875f78c2c9ae89f458c2d85785c0e62de4595ef12d1af7734c8b03`.
  Its current build graph is effective `-O2` with ThinLTO and no
  `-fprofile-instr-use`; the historical profile hash recorded in `PERF.md`
  is absent from `generated`, as is the old active-module marker. The old
  iOS PGO builder therefore cannot reproduce the present artifact. The fresh
  Simulator builder now accepts optional `GALAXYPAD_PGO_PROFILE` while
  preserving the unprofiled default and failing closed for a missing file.
- Next loop rung: on a quiet/roomier host, build an isolated current-input
  instrumentation module, train it on the autonomous seeded route, merge and
  validate the profile, then compare one PGO `-O2` candidate against the
  current control. Do not make another micro-optimization or call the 56.4
  FPS result an iPad result.

## 2026-09-12 — self-audit and profiling reorientation

- The audit found no performance fix landed during the preceding work window.
  There were no commits; the tracked source changes are diagnostics, the 4:3
  default test, the unattended harness, and documentation. The generated
  `-O3`, ThinLTO loop-budget, lookup, vertex-reader, texture-coordinate, and
  dispatcher candidates were all measured and rejected or parked; the
  canonical generated module remains `-O2`.
- The strongest current evidence is bounded but incomplete: logging-off
  sampling still spends the CPU-GPU thread in `StaticRecompCore::Run` →
  `chassis_dispatch`, and audio DMA underruns coincide with guest-producer
  enqueue gaps while CoreAudio reports zero short callbacks. This identifies
  native-side starvation, not the winning source-level optimization.
- The host was not a clean benchmark: Android QEMU, multiple `afcclient`
  transfers, and compiler work were consuming CPU. I left those unrelated
  tasks alone. The GalaxyPad process I owned was terminated after each
  diagnostic run.
- I added an opt-in System Trace branch to the unattended harness, but the
  Simulator attach returned `Cannot find process for provided pid` (exit 21);
  an earlier `spindump` attempt correctly refused non-root live capture (exit
  77). Neither produced profiler evidence, so neither is used as a result.
- The loop is reoriented: first obtain two stationary captures with timestamped
  stacks and scheduler state on a quiet host (or a separately authorized
  physical Instruments capture), then change code only if one lane explains at
  least 60% of the combined underrun-gap time. No further code-size or logging
  experiment is considered progress without that attribution.

## 2026-09-11 — performance self-audit and O3 candidate rejected

- The performance problem remains unresolved. The loop did establish that
  logging is not the primary cause: logging-off profiling still led into
  `StaticRecompCore::Run` and `chassis_dispatch`, while DMA underruns tracked
  guest-producer gaps and EFB counters stayed at zero.
- Built a private generated-module `-O3` candidate. Its `__text` shrank by
  371,864 bytes versus canonical `-O2`, but the matched seeded route measured
  56.730 versus 56.987 frame events/sec, equal phase-trace DMA underruns (70)
  and backlog drops (6), and lower average process CPU (73.247% versus
  75.615%). This is not a material frame/audio win, so it was rejected.
- The host was concurrently running Android QEMU, `afcclient`, and MacDroid;
  both Simulator runs are qualified rather than clean performance acceptance.
  The next iteration must start from a clean matched baseline and exclusive
  cost attribution, or obtain a separately authorized physical heavy-scene
  capture. No physical iPad, unrelated Simulator, save, or canonical module
  was changed.

## 2026-09-11 — menu-inclusive diagnostics and canonical recheck

- Rebuilt the Simulator host so opt-in frame windows include the runtime while
  the three-dot menu is visible; explicit pause and modal UI remain excluded.
  Each window now records `native_menu`, `ui_blocked`, and `pause_requested`.
- The rebuilt canonical app completed the seeded late-game Simulator route
  with logging enabled for this diagnostic only. Eighteen windows averaged
  59.109 frame events/s, with a 49.058 lowest window; the route was qualified
  by host contention and is not a performance acceptance result.
- Audio output had zero short callbacks, while DMA underruns rose 1 → 14 and
  the maximum guest producer gap was 98.728 ms. The seed hash remained
  `717f7fb3…e36c`; no physical iPad or unrelated Simulator was changed.

## 2026-09-11 — packed texture-coordinate candidate rejected

- Secondary Astra Medium reviewed the measured native path and identified the
  u16-indexed, signed-16 two-component texture-coordinate converter as a
  bounded candidate (159 sampled leaf observations in the retained profile).
- The temporary ARM64 candidate combined its two adjacent four-byte component
  loads. Its target object shrank by 12 bytes of text with no calls or spills,
  and the candidate reached the normal seeded late-game Simulator route.
- Matched phase traces did not show an improvement: candidate 19 DMA
  underruns versus 18 for the canonical control, with lower terminal frame
  cadence (231 versus 253 frames in the final five seconds). The source was
  reverted and the canonical Simulator app/core were rebuilt. No physical iPad
  or unrelated Simulator was touched.

## 2026-09-11 — dispatcher duplicate-PC-store candidate rejected

- A generated-module candidate removed the second `ctx->pc = address` store
  from `dolrecomp_call_original`; the enclosing dispatcher already writes the
  same address and is the only caller path. The candidate module passed the
  static size gate with an 8-byte `__text` reduction.
- Matched five-second phase traces failed the runtime gate: 252 terminal
  frames and 19 DMA underruns for the candidate versus 256 frames and 17
  underruns for the canonical module. It was not promoted. The canonical app
  and module remain selected, with no physical iPad or unrelated Simulator
  touched.

## 2026-09-11 — end-of-session iPad candidate installed

- Reinstalled `generated/device-stage.JOtjGn/GalaxyPad.app`, the latest
  known-good strict-signed private iPhoneOS bundle, in place on the physical
  iPad and relaunched it successfully. The resulting device process was PID
  8171 at handoff.
- Verified the development signature and retained the byte-identical save
  backups (`cd8d1fa9…`). No uninstall, app-data reset, save replacement, or
  WBFS change was performed.
- Parked the two-range R916b experiment: its Simulator run crashed with a
  null instruction pointer before renderer readiness, so it was not promoted
  to the physical device. Resume details are in
  `docs/RESUME-2026-09-11-GALAXYPAD-IPAD.md`.

## 2026-09-11 — physical controller hot-plug sequence proven

- The exact live iPad console captured controller ownership and visibility for
  disconnected → connected → disconnected: `hidden=0` → `hidden=1` →
  `hidden=0`, with `auto_hide=1` throughout. This demonstrates the requested
  touch-control handoff on the Xbox connect/disconnect path, and the raw
  excerpt is retained at
  `generated/runtime/ipad-iteration-1/controller-hotplug-console-20260911.log`.
- Released the console attachment for the other private iPad task, verified
  the save backup hash remained `cd8d1fa9…`, and relaunched the same installed
  app without uninstalling or changing app data. Current app PID is 8160.
- The unattended R915 Simulator recheck reached the late-game plaza with
  logging off at 47.3 emu FPS; its sample still attributes the pressure to
  `StaticRecompCore::Run()`/`chassis_dispatch`. This remains diagnostic, not a
  physical-device performance claim.

## 2026-09-11 — final private host rebuild installed

- Rebuilt the iPhoneOS host with the gameplay counter labeled `emu FPS`,
  strict-signed the staged app, and installed it in place on the physical iPad
  as PID 8142. Startup confirms 4:3, `frame_logging=0`, and controller
  auto-hide while connected.
- The app database UUID is unchanged and the pre-install save backup remains
  byte-identical (`cd8d1fa9…`). This was a clarity/packaging pass, not a claim
  of improved performance; physical disconnect/reconnect and three-dot
  gameplay continuity still need direct user confirmation.

## 2026-09-11 — controller visibility, pointer response, and menu pause fix

- Physical logs showed the old generic `presentViewController:` override forced
  `pause_runtime=1` for about half a second during every three-dot menu open,
  even though reconciliation later cleared it. The override now blocks input
  first and defers pause selection until UIKit identifies the presentation;
  native UIMenu remains runtime-neutral.
- The controller-hide preference now defaults to on when unset. The fresh iPad
  launch logged `connected=1 auto_hide=1 hidden=1`; disconnect uses the same
  notification/reconciliation path to restore touch controls. Visibility is
  now persisted in the low-frequency runtime log.
- Increased right-stick cursor response from 0.8× to 1.2×. Controller,
  UIKit, diff-check, device build, strict signature, in-place install, and
  logging-off launch checks pass. Save backup remains `cd8d1fa9…`; no data was
  erased. Physical disconnect/reconnect and pointer comfort remain to be
  confirmed by the user.

## 2026-09-11 — iPad 4:3 default and menu pause correction

- Rebuilt the iPhoneOS host after making invalid or absent aspect preferences
  resolve to 4:3 (`aspect_ratio_mode=0`); 16:9 remains an explicit Display
  choice. The exact private bundle was strict-signed and installed in place.
- The physical launch log reports the iPad 4:3 viewport
  `{{0, 0.013671875}, {1, 0.97265625}}` with `frame_logging=0`. The app stayed
  alive at PID 8107, and the app database UUID remained unchanged.
- Made the three-dot menu input-only even if UIKit exposes it as presented UI:
  it clears held input but forces `pause_runtime=0`; dismissal clears the
  gameplay gate synchronously. Lowered the three-dot and top Pause controls by
  8 points. UIKit and controller regression tests pass.
- Refreshed the private save backup before install; its SHA-256 remains
  `cd8d1fa9…`, matching the prior backup. No save, WBFS, or app-data erase was
  performed. Physical menu/controller/touch acceptance remains pending because
  this Mac has no supported unattended hardware-input route.

## 2026-09-11 — R911 unattended normalization candidate parked

- The dedicated Simulator loop now operates without an operator: it installs
  the private app, seeds the advanced save, waits for renderer readiness,
  holds A+B, selects the file and Play by pointer, advances the opening story,
  and captures the same late-game plaza plus a bounded host sample. The
  unrelated booted iPhone Simulator was preserved and recorded as a
  host-pressure qualification.
- Whole-normalization correctness passed 261,120 complete-routine comparisons
  and 69,120 callback cases. The first matched late-scene pair was candidate
  50.0 FPS versus control 49.1 FPS, with effectively identical CPU/dispatch and
  EFB-wait samples. Reverse-order control completed; reverse-order candidate
  stopped before renderer readiness and produced no profile or screenshot.
- Decision: park the candidate and do not install it on the physical iPad.
  The physical app remains at the known-good logging-off baseline. The next
  iteration must use one measured shared execution/dispatch hypothesis, pass
  offline correctness/cost gates, then pass the unattended Simulator route
  before any device candidate is considered.

## 2026-09-11 — unattended Simulator loop reoriented and EFB trace captured

- Reoriented the active goal around the dedicated iPad Pro Simulator because
  QuickTime/CoreDevice can observe/install/profile the physical iPad but cannot
  inject unattended touch or simultaneous Xbox/Wii input. The physical app and
  save remain at the known-good logging-off baseline.
- Updated `scripts/galaxypad-unattended-simulator-loop.sh` to wait for renderer
  readiness, hold A+B for 30 seconds on the slow title path, select the seeded
  file and Play with pointer input, advance the opening story with 30 bounded A
  pulses, capture the late-game plaza, and take a bounded host `sample` profile.
  `tests/test-simulator-input.sh`, `bash -n`, and `git diff --check` pass.
- Clean replay reached the late-game plaza without operator input; output is
  `generated/runtime/ipad-iteration-1/simulator-reorientation-unattended-30s-plaza/`
  and the on-screen counter read51.0 FPS with frame logging off. The five-second
  title attempt remains a failed route-timing artifact, not a bridge failure:
  host accepted buttons3, the input device consumed3, and Wii desired buttons
  were3072 (A+B).
- Exposed the existing opt-in `GALAXYPAD_EFB_TRACE` through the unattended
  script. The trace-enabled run was terminated after capture to freeze output;
  it contains10,365 depth reads, mean1.242ms, max11.391ms,648 over5ms and6
  over10ms, concentrated at `(0,0)` and `(520,377)` with verified callers
  `0x80385be0`/`0x8029e6e4`. This is launch-to-profile diagnostic evidence,
  not a stationary FPS or physical-device claim.
- **Next:** run one stationary late-scene EFB/caller comparison with the opt-in
  trace, then evaluate exactly one semantics-preserving candidate if the wait
  cost remains material. Do not change resolution, audio buffers, or EFB
  semantics from stack evidence alone.

## 2026-09-10 — R914 suite passed; R915 local install UI and IPA

- Full suite61169 exited0, generated/check-r914.log. This predates installer edits;
  does not audit untracked source publication or prove packaged hardware behavior.
- User explicitly requested install UI/docs. Read SunPad deployment/install docs;
  reused signature-check then devicectl pattern, keeping bundled Galaxy module
  and private-only boundary (no SunPad public-release or tmp-module assumptions).
- Added double-click Install on iPad.command and native-dialog Python assistant:
  export exact hash-pinned private IPA, signed-app validation/install, local help.
  No automatic account/profile creation, credential prompt, upload, data deletion
  or automatic game launch. Apple validates final provisioning at install time.
- Initial export carried AppleDouble metadata; corrected ditto packaging flags.
  Final IPA generated/ipad-install.7z6b20r8/GalaxyPad-private-unsigned.ipa, adjacent
  SHA256.txt. Layout/CRC and host/module byte parity checked. Original stage intact.
- Native dialog inspected through accessibility; Cancel exits19531 with0.
  Four installer fixture tests pass; added to regression entry point. Export80814
  terminal0. Signed-install branch cannot be executed without device/signing.
- README and INSTALL_IPAD.md document UI, re-signing both binaries, import,
  limitations and first test. No claim that unsigned IPA is directly installable.

## 2026-09-10 — R914 broader regression running

- Prior turn progress: new physical candidate staged with unchanged game module.
  Current disk18Gi; no booted Simulator. Original dirty checkout preserved.
- Started full scripts/check-repository.sh, session61169, log
  generated/check-r914.log. Same handle verified live with advancing output
  through input/pointer/import/EFB/platform tests; do not restart it.
- This suite is source/fixture evidence, not packaged device gameplay or a
  release audit of untracked files. Next inspect terminal result, fix any causal
  failure, then resume actual product/runtime validation. Full PRD stays active.

## 2026-09-10 — R913 physical Release report candidate

- Prior turn progress: source report fields and dual-platform compile checks.
  Disk19Gi available; no game/compiler overlap at start.
- Device Release build11885 exited0; log generated/device-report-build-r913.log.
  Retained upstream unused-parameter warning, no suppression. Staging60036exit0
  produces device-stage.ZaTANc host27b37908 and unchanged device module48f455eb.
  Older E7pIDj preserved. Stage verifies IOS/min16, icons, module symbol/install name.
- Settings/import activation/SDK-selection regressions94677 pass. No full suite,
  signed install, visual report or device-performance claim. Next hardware/signing
  then same-scene first test per PHYSICAL-IPAD-FIRST-TEST.md. Full PRD active.

## 2026-09-10 — R912 useful mobile report context

- Previous turn progress: invalid capture recorded, original app/save restored,
  physical stage verified and first-test plan created. Shutdown71107 terminal0;
  current boot inventory empty. No runtime started this turn.
- Report audit found existing diagnosticContext omitted available audio counters
  and mislabeled after_frame_event as presentedFrames. Corrected source label,
  added cumulative DMA counters with validity flag, Simulator/device, OS,
  thermal/low-power state and explicit scope caveats. No identifiers/game data.
- Both physical and Simulator main.mm syntax checks pass Wall/Wextra/Werror.
  Existing mobile redaction, report generation and rotation test52708 passes.
  Not full suite or packaged UI proof. R884 device stage remains unchanged;
  next signed test candidate must incorporate this source, then verify the
  report preview/export on that exact artifact. Full PRD remains active.

## 2026-09-10 — R911 capture invalidated; physical baseline prepared

- Starting plaza visibly reached; process99973 and thermal84974 terminal0.
  Plaza guarded normalization delta62.5% accepted, not a frame-rate gain.
- SIGTERM ended runtime98501 but no VI/work export: timing attempt invalid.
  Future native Stop must precede exit and export verification. Documented failure,
  no unsupported metric retained as acceptance. Original app restored95059exit0,
  save99d432d5 unchanged; shutdown71107 initiated.
- Existing physical Release host/module hashes and IOS platform reverified.
  devicectl no devices; zero signing identities. PHYSICAL-IPAD-FIRST-TEST.md
  provides stable-artifact first-test sequence and device-versus-Simulator decision.
  Full PRD remains active; no public release or device installation performed.

## 2026-09-10 — R911 private iPad runtime and eligibility

- R910 link34260 terminal0, verified module d966417b. One iPad running existing
  bdd8633e measurement host with new private module; original85ffc100 backed up.
- Runtime19845/console98501 live; visible landscape file select and story reached.
  Save99d432d5 independently unchanged. First mixed-scene counter sample accepts
  43,442,348/rejects18,218,166; debugger detached. No gameplay timing gain yet.
- NORMALIZATION-IPAD-R911.md records exact artifacts, counters, restoration and
  next matched-scene gate. Full PRD active; do not mistake private host for new UI.

## 2026-09-10 — R909–R910 normalization candidate reaches Simulator build

- Implemented whole9-op guarded vector normalization in existing C backend.
  R90945446 initial261120 comparisons/69120 callback cases/456 accepts pass.
- R91047050 expanded raw-input/FPSCR and direct-entry expected-result/zero/tie
  tests pass with1804 accepts.35195/61669/15255 complete-routine cost runs pass;
  NI0/NI1 ordinary~40–44% faster, large/zero rejection slower. No game gain yet.
- Added private builder reusing verified R804 control/all1329 object hashes.
 34260 currently LIVE at Simulator link after verified inputs/compile. Log
 normalization-build-r910.log. Do not restart; poll exact handle to terminal.
  NORMALIZATION-VECTOR-R910.md records next one-iPad eligibility/scene gate.
- Normal app/module/save selection unchanged. Full original PRD active.

## 2026-09-10 — R908 vector-history audit and concrete next target

- R907 is verified progress: long-region cost rejected the scalar candidate.
  Audited pinned paired lowering and R428–432/R802–813 vector work; both older
  vector candidates already failed scene benefit. Neither is reopened.
- Read exact normalization9-op source, reference gated scalar semantics and
  retained coverage ranking. Recorded8 live input lanes, scalar estimate/type
  hazards, otherwise-dead upper-lane status effects, and original suffix/observer
  boundaries in VECTOR-REORIENTATION-R908.md.
- Next whole normalization paired-vector candidate, not compact-state inlining
  or scalar opcode expansion. Must pass full-context/varied-cost and eligibility
  before another app build. This audit adds no new performance evidence and
  changes no runtime/app/module/save. Full original PRD remains active.

## 2026-09-10 — R907 long-region cost gate, scalar candidate rejected

- Added lane-selected arithmetic, sums and scalar fmuls to cover13 actual ops
  at64E0–6510. Corrected a sum passthrough conversion/status mismatch using
  exact compiled-helper disassembly. Full-state/status comparisons then pass.
- Initial cost2775: all8 pairs slower. Removed demonstrably unnecessary per-op
  caller-saved scratch preservation in this FP-only region; outer ABI frame now
  owns scratch. Random/halfway63975 and final fixed/cost55294 both exit0.
- Final candidate3431–3436 instructions vs3494–3497 control, but203.080–211.970
  ns/routine vs187.050–195.136; every pair slower. Reject this scalar variant.
  No app/module build, no new FPS claim. RESIDENT-LONG-COST-R907.md has artifacts.
- Next materially different paired-vector arithmetic path, after checking prior
  vector/precision work; no more scalar opcode coverage or stack tuning here.
  Original PRD remains active. All experiment handles terminal; no runtime.

## 2026-09-10 — R906 resident fused arithmetic

- Extended static resident multiply emitter to fused arithmetic, exact single
  halfway correction, A/B/C NaN precedence, guest status, and result negation.
  Real candidate region6504–6510 retains three multiply operations plus nmsub.
-12071/15770/75603 terminal0: fixed/randomized operands+FPSCR/explicit-halfway
  tests each pass629760 full-routine state/memory/callback/FPSR comparisons,
  including172800 callback cases. Halfway fixtures have independent expected
  nearest-mode results, not only two-implementation agreement.
- Multiply/merge regression67663 exited0 with the same629760/172800 counts.
  No timing, app,
  module selection, save or runtime changes.19GiB available. Full PRD active.
  Next longer arithmetic span and full-routine cost, not tiny-fragment timing.

## 2026-09-10 — R905 first resident arithmetic lowering

- Continued R904 into exact paired multiplication in static ARM64: no runtime
  JIT, C arithmetic helper call or scratch CPUState. Real region6518–6528 now
  includes multiplication followed by resident merges.
- Randomized full-routine test caught raw FPSR underflow mismatch despite equal
  guest state. Exact control disassembly proved unconditional conversion before
  NI result selection; matched it rather than masking status.
- Final16928/8240 both terminal0: fixed and randomized operand/FPSCR tests each
  pass629760 full-state/memory/callback/FPSR comparisons,172800 callback cases.
  RESIDENT-MULTIPLY-R905.md records artifacts, failed run, and scope limits.
- No timing/promotion/runtime/save changes. Next extend to long-span fused and
  scalar/sum arithmetic, then full-routine cost before another iPad candidate.
  Full original goal remains active.19GiB available; no booted Simulators.

## 2026-09-10 — R904 actual guest entry integration

- Added hash-pinned DOL merge-region staging and static ARM64 suffix exporter.
  Preserved original whole-chunk dispatch/cycle accounting and per-entry FP
  availability, materialized PCs, and flushed before original stores/callbacks.
- Extended the existing whole-library routine oracle, not a second harness.
  Initial session46449 exited0:629760 full-state/status comparisons,172800
  callback cases. Final rerun72581 exited0 with the same totals and includes
  malformed-export input checks. Benchmark mode correctly rejects before build.
- No timing test or product installation. RESIDENT-REGION-ENTRY-R904.md records
  scope and remaining gates. Broader resident arithmetic is next; full original
  PRD remains active. No runtime or Simulator started, 18GiB available initially.

## 2026-09-10 — R903 paired record semantics verified

- R902 was private lowering progress. Added exact record-bit decoding and CR1
 update preserving other CR fields/scratch registers, without FP arithmetic.
- Added observer checkpoint between non-record and recorded sequences, flushing
 cached values before a normal C call and reloading afterward. Full-state check
 prevents later recorded writes from hiding unwanted earlier CR mutation.
-63181initial/55537final terminal0. Combined/plain/conversion regressions each
100k CPUState/FPSR/FPCR pass; decoder262144 valid combinations; stderr empty.
 PAIRED-MERGE-CR1-R903.md records bounds/artifacts. No app/module/save/runtime
 or Simulator change, no timing claim. Next explicit guest entry/cycle boundary
 integration before broader arithmetic and complete-routine performance gate.

## 2026-09-10 — R902 private resident paired merge lowering

- R901 was a verified negative cost result, not an app improvement. Continued
 existing offline FPR-cache path rather than reopening guard/mapping candidates.
- Added exact non-record paired merge decoder and register-resident value
 lowering; stages operands in reservedQ30, preserves adapter scratchQ31.
128-op alias/spill/flush sequence tested after callback mutations.
-98230/74756/53577 all terminal0. Final combined/plain/unchanged-conversion
 regressions each100k full CPUState/FPSR/FPCR passes; decoder131072 combinations
 plus invalid forms checked. ASan/UBSan driver, not emitted-code instrumentation.
- RESIDENT-PAIRED-MERGES-R902.md records scope/commands/artifacts. No full guest
 entry/cycle/CR/arithmetic backend or FPS claim; no game/Simulator/module/save
 change. Next broaden resident execution with those contracts, not a merge-only
 speed screen. Full original goal remains active.

## 2026-09-10 — R901 whole-chunk guard candidate rejected on cost

- R900 made implementation/correctness progress. Added private independent
 whole-chunk library mode to existing harness, with real helper objects and
 original dispatch; configuration synchronized outside benchmark interval.
-76821exit0: selected41-entry routine629760 state/memory/callback/FPSR cases
 pass.8alternating1M-call pairs all slower;~3% more instructions. Code text
288144→160860bytes and candidate PGO mismatch recorded, not hidden.
- FP-GUARD-COST-R901.md records scope/commands/artifacts: whole chunk compiled,
 not every chunk entry exercised; local cost is not gameplay/device proof.
 Close guard-relocation rollout, no app/module build or repeated tiny tuning.
 No live runtime/Simulator, save/default unchanged, full original goal active.

## 2026-09-09 — R900 private shared FP guard-chain implementation

- R899 was profiling progress. Source-matched native/Simulator code comparison
 17455exit0:577 chunk opcode sequences/counts equal,13,237,199/13,240,666 raw
 instruction words equal. This excludes static chunk-instruction bloat, not
 platform execution/cache/dynamic-path differences. Raw report retained.
- Implemented closed-grammar availability-chain relocation preserving suffix
 checks/precharge/PC and all arithmetic/status. Source-pinned helper contract;
 negative entry/observer/operand tests pass. Not selected by product builders.
- Actual-policy original-chunk versus transformed-routine oracle80219/64195
 passes wide314880/normalize130560 cases. Added lazy-FP off coverage; final
 wide35339exit0 passes629760 comparisons/172800 callback cases. No masks or
 precision relaxed. All commands/artifacts and PGO caveats in FP-GUARD-CHAINS-R900.md.
- Next whole-chunk dispatch-preserving correctness/cost experiment, not a new
 profile or isolated-helper benchmark. No app/module/save change, runtime,
 Simulator, game-level performance gain or full PRD acceptance.

## 2026-09-09 — R899 heavy native CPU profile captured and mapped

- R898 was evidence progress. Actual one-native-game run12287/63375 reached
  invaded plaza, same position before/after10s CPU Profiler capture22058exit0.
  UUID/loadbase verified; export33318/98842 and577-chunk mapping17063exit0.
- Existing mapper gains optional --profile, same module identity/default. Full
  mapping conservation and focused classifier/origin regressions pass. Loads
  remain~52.44% of generated sampled weights; not removable-cost/FPS proof.
- Separate counter window completed but overlaps offline analysis; explicitly
  not clean timing comparison. HEAVY-NATIVE-PROFILE-R899.md records exact
  identities, commands/artifacts, scene/config differences and next decision.
- SIGTERM/RequestStop clean63375exit0,save99d432unchanged,all tools terminal;
  no Simulator. Heavy-scene instruction evidence ready; no further same baseline
  profiling needed. Next broad source-backed implementation and iPad A/B.

## 2026-09-09 — R898 optimization scope reconciled

- Previous turn: no performance progress, only R897 status/shutdown verification.
- Reused R705 invaded-plaza sample with existing self-count parser; new ignored
  report conserves428 observations across131 generated chunks. Parser regression
  passes. Reconciled R822 weighted profile and R894/R895 narrow-candidate scope.
- PERFORMANCE-SCOPE-R898.md records commands, hashes, limits and exact unresolved
  attribution question. No new timing/FPS claim, runtime, build or product change.
- Updated stale STATUS entry with R897 performance and current queue. Next known
  working native heavy-scene instruction capture, not failed Simulator attach or
  another old microcandidate. Original full goal remains active.

## 2026-09-09 — R897 heavy iPad transition measured

- Existing private recorder host/module, one iPad, THPon and output counters.
  Actual movie and invaded-plaza windows complete in same process, zero drops.
  POSTMOVIE-IPAD-R897.md records identities, navigation, bounds and limits.
- Movie59.881VI/s/6.637msCPU; invaded plaza37.150VI/s/23.422msCPU and178.576M
  instructions/VI. Heavier workload than starting plaza; CPU alone exceeds budget.
  Audio interior plaza interval74underruns while callbacks remain~48kHz.
- Clean stop failed0,5591native frames/0fallback; restored85ffc100 and save99d432
  unchanged; Simulator shutdown. No code speedup or full audio/device acceptance.
  Next material whole-workload CPU reduction; no repeated identical movie proof.

## 2026-09-09 — R896 readback contract verified; no wait-removal change

- R895 completed negative transfer tests. Inspected actual cache population,
  staging Flush, encoder submission and sync notification; reconciled existing
  EFB-READBACK.md R595–604 before changing rendering policy.
- Synchronous population already clears needs_flush; cached reads do not repeat
  waits. Staging skips completed buffers, and FlushEncoders submits no absent
  render buffer. R604 measured tiny GPU execution versus delayed handler entry;
  no concrete removable synchronization or expensive handler body is established.
  This is not a new root-cause discovery. No stale depth or spin-polling change.
- Added test-metal-staging-flush.py executing the actual ObjC++ Flush method with
  command-buffer/tracker doubles. ASan/UBSan69231exit0: nil/completed/pending/error
  branches, ordered flush→notify→wait, flags/pointer clear and repeat-no-op pass.
  Registered in repository checks; full suite not run. No real GPU/error recovery
  or performance proof from doubles. No app/module/save/runtime changes.
- Next target the reported heavier post-movie iPad gameplay, not another neutral
  starting-plaza baseline. R89056VI/s before invasion does not represent R791's
 31.95VI/s invaded plaza. Use existing larger recorder and output telemetry to
 compare workload growth across that actual transition before choosing another
 optimization. Full PRD/perceptual audio/physical/product gates remain active.

## 2026-09-09 — R895 direct-memory transfer tested; broad rollout closed

- R894 was implementation/cost progress. Extended private generator/probe to
 complete17-op normalization and15-op cross-product routines, unchanged math,
 derived checked memory spans and exact suffix accounting.
-130560/115200 correctness cases pass; each23040 callback cases/96fast entries.
 Eight alternating cost pairs: normalization regresses, cross has negligible
 CPU gain despite fewer instructions. Wide314880-case regression also passes.
 All handles terminal0; details WIDE-FP-TRANSFER-R895.md.
- Close this broad rollout; no module build, another guard tweak or FPS claim.
 Next inspect actual CPU/GPU readback synchronization boundary using retained
 EFB timing, preserve correct depth and honor duplicate-peek closure. No runtime
 or Simulator launched, product/save unchanged, full original goal remains.

## 2026-09-09 — R894 guarded direct-memory candidate implemented

- R893 oracle was progress. Implemented private whole-entry MEM1 fast path:
 span/alias/FP/GQR/journal guards, original arithmetic and ordered stores,
 reservation clearing, original suffix/slow paths. No product module change.
-314880 comparisons pass, including57600 callback cases,96 actual fast entries.
 Eight alternating cost pairs pass state/memory/flags: about15% fewer instructions
 but only5–7% less CPU for this routine. Not game FPS or promotion evidence.
- Tightened undersized MEM2 rejection and added reservation cases after cost;
 final79600exit0 passes again. Earlier cost is not exact final-binary timing.
 WIDE-FP-CANDIDATE-R894.md retains artifacts/contracts/limits.
- Next evaluate transferability across different whole routines before any module
 build; no further small tuning of this routine. No game/Simulator/app/save change.
 Full original PRD and iPad acceptance remain active.

## 2026-09-09 — R893 complete wider-routine oracle implemented

- Previous turn R892 was tooling/selection progress. Added source-pinned complete
 41-instruction extracted routine and original whole-chunk comparison driver.
 Actual cached strict-FP/O2/ThinLTO/PGO flags retained; driver assertions separate.
- Final23196exit0:314880 CPUState/memory/ordered-callback/FPSR comparisons pass,
 57600 callback cases. Includes all suffixes, GQR type/scale, FP availability,
 NI/rounding, overlap/self-alias and mutating callbacks; independent cycle/return/
 SRR0 checks pass. See WIDE-FP-ORACLE-R893.md for limits and reproducibility.
- No optimized candidate, speedup, app/module/save change or runtime launch.
 Next guarded normal-RAM candidate against this oracle, then whole-routine cost;
 no further census or module build before that evidence. Full PRD remains open.

## 2026-09-09 — R892 broader memory-inclusive routine boundary

- R891 corrected next action; now extended rank-fp-islands.py with opt-in
  memory-inclusive spans and explicit observer/helper locations. Default mode
  preserved. Focused ranking tests and actual source-hashed coverage scan pass.
- FP-MEMORY-SPANS-R892.md records results. Highest spans still include rejected
  normalization/cross-product; they are not newly discovered optimizations.
  Selected larger41-instruction64A4–6544 routine has interleaved stores and11
  memory observers. Moving stores/keeping stale state across callbacks is invalid.
- Next complete-routine oracle and explicit normal-RAM eligibility contract,
  preserving suffix cycles, GQR/FP semantics, aliases and callback mutation, then
  actual-policy cost. No further baseline capture or full game build justified
  yet. No product FPS gain; full PRD/iPad acceptance remains open.

## 2026-09-09 — R891 correct cutscene diagnosis before implementation

- Previous turn progressed with complete iPad data, not a speedup. Revalidated
  no game/Simulator, then read movie implementation and R791 measured evidence.
- R890 next-lane wording was misleading: movie delivery already measured
 59.946events/s with zero new underruns/drops/short callbacks; post-movie
 gameplay31.953events/s produced104 new underruns. No separate decoder audio
 bug is established. Do not patch buffer size, callback rate or decoder timing
 speculatively. Perceptual sync/physical integration gates remain open.
- Re-read R828 compact-normalization closure and R825 restrict closure; neither
 is a candidate to revive. Inspected current StaticRecompCore_Run.cpp and actual
 DolRecomp dispatch.c: dispatch carries timing, exception, stop, SMC, replacement,
 host-call and physical-alias semantics. A direct function-table shortcut cannot
 silently bypass those contracts. Existing lookup design had only~2% gain.
- Next CPU implementation question is broader whole-routine register retention
 across proven observation boundaries, with actual-policy whole-routine cost
 before module build. Avoid another isolated compact FP region or dispatcher
 shortcut. This audit corrects the execution queue; no product performance or
 newly fixed code claim, no source/module/save changes or runtime launched.

## 2026-09-09 — R890 iPad performance capture and implementation plan

- R889 progressed authoritative platform-attribution evidence. User reasserted
  iPad Simulator priority; built private65536 recorder host, actual iPad run and
  matching plaza capture completed with synchronized host measurements.
- IPAD-PERFORMANCE-RESULT-R890.md records full bounds, results and limitations.
  56.114VI/s,15.660ms CPU/VI; zero paired drops; independent CPU clock agrees.
  Not a product speedup or reproduction of historical6–22FPS. CPU near budget.
- Host.csv completely brackets window, no swap traffic. Process-pressure log
  covers first19.33s only; no full-window process attribution. Unrelated apps
  untouched. Existing priority tweak and duplicate-EFB lanes stay closed.
- Clean menu stop failed0, app restored85ffc100, save99d432 unchanged, console0,
  sole iPad shut down. Next implementation targets CPU headroom and native-THP
  audio/transition gate; no more unchanged baseline loops. Full PRD remains open.

## 2026-09-09 — R889 complete native evidence; iPad priority renewed

- Previous turn R888 progressed bounded recorder/build/test state. R889 actual
  native run cleanly completes matching plaza capture; zero drops/full pairing,
  independent CPU-clock agreement, save unchanged, runtime terminal0.
- See THREAD-WORK-NATIVE-RESULT-R889.md for bounds, identities and results.
  Native53.192VI/s,142.990M CPU-thread instructions/VI,15.337ms CPU/VI.
  R836 Simulator has1.79% fewer instructions but16.98% more CPU time. No FPS gain.
- Native final5s itself falls32.2VI/s versus first5s60, similar instructions/VI,
  CPU14.382→20.867ms and P-core share98.45→83.15%. No synchronized pressure or
  frequency evidence; cannot name the cause. Do not dismiss Simulator as unfixable.
- User renews iPad Simulator acceptance priority. Next synchronized iPad slowdown
  capture and evidence-selected material intervention; gameplay/cutscene both
  required. Full original goal remains active; no running native/Simulator left.

## 2026-09-09 — R888 bounded diagnostic capacity fix

- Previous rejected capture changed the next action: increase private paired
  buffers, not weaken completeness checks. Builder now accepts 16384 or 65536
  samples; ordinary product recorder remains unchanged. Both private buffers
  use the same selected capacity.
- Focused tests pass, including 20,000 paired VI samples with zero drops and
  matching timestamps, invalid-capacity rejection and ordinary SPI exclusion.
  Native build completes with control/scope checks and strict ad-hoc signature.
  Candidate thread-work-native-r888 host SHA256
  2cf50e8a47399981a78dbc134e41ae23c89ed5987fd5347cceaab86294f014dc.
- This fixes measurement coverage, not game performance. Next capture uses
  working background-input navigation, fresh r889 outputs and unchanged module.

## 2026-09-09 — R887 native scene reached; measurement rejected for capacity

- R886 builder integration was progress. Candidate1ccee366 with explicit
  --background-input, original c0021b9f module, private R837 user profile and
  fresh r887 outputs; LC byte/pair0, no fallback JIT, Metal/Cubeb. PID4973,
  console79690; no Simulator. Background-input log present.
- Title/file72651 and aim86608 work; Play-lower then story5308 plus finish99139
  reach stationary plaza. No movement. Pre/post screenshots show matching
  life3/lives4/coins0/StarBits0 with lower-right pointer. Process capture63590
  completes after10s warmup/30s capture. SIGTERM handled as RequestStop;
  console79690exit0, original private NAND99d432d5 unchanged.
- REJECTED: VI and sidecar dropped7937 after16384 retained samples. Last VI
 595170865599583 precedes requested window595221323874125–595251330006708.
  summarize-thread-work.py with explicit verified prefix still correctly rejects
  Incomplete requested coverage. No native-thread comparison/FPS gain claimed.
  Whole-process snapshot alone cannot answer the pending thread-specific question.
- Navigation/setup overhead consumed capacity before capture. Next enlarge only
  private diagnostic recorder capacity (or arm after startup), verifying paired
  buffers and memory bound, then reuse now-working route. Do not relax completeness
  guards, edit CSVs or rerun the same16384-capacity capture unchanged.

## 2026-09-09 — R886 native counter runner gains explicit input option

- R885 isolated RuntimeConfig override; previous turn was progress. Extended
  build-thread-work-host.py with native-only --background-input-option, applying
  existing parser patch to private runner source. Default native/mobile paths
  unchanged. Only private runtime recorder and runner objects change; guest
  module, normal app, original archive/source/runner cache stay unchanged.
- Recompiled original and relocated unchanged runner: both byte-identical to
  cached runner object. Unchanged runtime/archive and metadata-normalized host
  control checks pass. Existing parser and LINKEDIT/UUID tests pass.
- Attempts r886/r886b stopped at missing-option assertion, no runtime launched.
  Initial VFS suspicion was wrong: git apply inside ignored generated directory
  silently skipped patch paths. Replaced with patch -p1 at explicit private root,
  plus source-content assertion before compile. Original vendor source untouched.
- Final r886c builder40170exit0, strict ad-hoc signature verified. Provenance has
  both changed-object commands/hashes and unchanged baseline identities.
  Candidate path generated/candidates/thread-work-native-r886c/candidate/GalaxyPad.app.
  Built executable --background-input --help confirms option is actually linked;
  no game run. No performance result claimed.
- Next use this candidate with --background-input for one native plaza thread
  measurement matching R836. Keep new output paths; prior rejected navigation
  records remain. No Simulator/game running and full PRD remains open.

## 2026-09-09 — R885 native measurement setup defect isolated

- R884 device integration was progress. Re-read independent review/disposition
  and R836/R837: direct-call design already closed; unresolved useful question
  is native CPU-thread work versus Simulator, not another arithmetic/QoS tweak.
- Verified existing native counter host9ee5404f and private NAND99d432d5. Changed
  private Dolphin.ini BackgroundInput False->True; launched unchanged host/module
  with R837 prescribed LC0/no-fallback-JIT/Metal/Cubeb and new r885 output paths.
  PID4064/console27058. Title/file fixture23571 reaches file details; subsequent
  pointer aim78640 does not remain visible with unfocused window. No Play/story
  or30s performance capture accepted. No module build or product change.
- Concrete source cause: dolphin_runtime.cpp sets MAIN_INPUT_BACKGROUND_INPUT
  from RuntimeConfig, overwriting file preference. Runner option exists only in
  isolated runner-background-input.patch; its parser regression already passes,
  but R837 counter runner reuses unpatched runner object. Binary has no option
  string. Saved Dolphin.ini after run is False again. Prior file-only fix invalid.
- Runner explicitly handles SIGTERM through RequestStop; sent only to4064,
  console27058exit0. NAND hash unchanged. Retained r885 runtime/vi/work logs are
  mixed startup/file-navigation data, NOT a plaza comparison. No FPS gain claim.
- Next extend isolated native counter builder with the existing tested runner
  option, verifying unchanged control and changed-object scope. Then one prompt
  plaza capture; do not repeat unpatched runner navigation or Simulator capture.

## 2026-09-09 — R884 current physical-iOS build integration

- R883 causal packaged B response was progress. Rebuilt existing Release
  iphoneos device-r852 tree against current host sources; handle92546exit0,
  generated/build-r884.log. Native THP remains OFF. vtool confirms IOS,
  min16.0/sdk26.5, not Simulator. Includes recent Audio/restart/layout/AX changes.
- Private stage generated/device-stage.E7pIDj/GalaxyPad.app passes staging
  architecture/platform/min-version/bundle/icon/module checks, handle95315exit0.
  Host70b573b177d56e30d1d8054e17b34bc0f2f626ead44b91fe947926c1eaed0958;
  unchanged module48f455ebc33f8eb2fc58151cd722ea756db77a5738a9573a0a49f0c656110041.
  No code-sign-for-device, installation, public release or runtime claim.
- Fresh devicectl59444exit0: No devices found. No Simulator booted; disk17GiB
  at start, no cleanup. Tracked whitespace check passes, not full untracked audit.
- Device compilation gap is closed for this candidate, not hardware acceptance.
  Next remaining Back/story layout issue and renewed bounded performance work;
  do not rebuild unchanged mobile modules or repeat file-screen B proof.

## 2026-09-09 — R883 packaged B activation has a causal game response

- Prior R882 full regression/status repair was progress. Fresh signed stage
  Zmi382 installed sole iPhone; host174c72e371bad471c2b32ec3f5213c9e72d320dc438f7c846d1bd0fbc8b7a7e8,
  unchanged module input3acdcddc. Existing data retained; container now
  0C8DE3B4-620E-4778-A367-95721FDDA999. PID3369, console81325.
- Leased A+B then file1 pointer(.38,.65)/A opens existing file panel. Sender
 64685 terminal0, final input snapshot neutral/expired. Simulator UI B click
  returns panel to planet selector. This is a causal packaged button response,
  unlike automatic title advancement. back-response.png and console.log under
  generated/runtime/product-r883. No save created/deleted/played.
- This does not establish which Simulator injection path invoked the control,
  full VoiceOver usability, simultaneous physical touches, Plus in opening
  gameplay or that R881 caused an improvement over a matched baseline.
- Native Stop21:48:13.860 failed=0; GameData.bin compares unchanged with R859.
  App terminated after runtime stop, console81325exit0; sole Simulator shutdown.
- Next bring physical-iOS host build current (still older R856-era source),
  then remaining Back/story overlap and performance acceptance. Do not keep
  repeating the now-proven file-screen B activation as the default loop.

## 2026-09-09 — R882 integrated regression and current-status repair

- Prior R881 implementation/tests were progress. Ran full current
  scripts/check-repository.sh, handle50968 completed0, log generated/check-r882.log.
  Settings/input/import, timing/codegen/audio/native diagnostics and remaining
  registered checks passed. No Simulator/runtime or concurrent product build.
- This script does not execute packaged UIKit, physical touch, gameplay or
  audio listening. Its git-based final checks cover tracked files, not the
  largely untracked product tree. Do not infer release readiness/reproducibility.
- Refreshed stale STATUS.md (still claimed Audio missing), distinguishing
  installed R880 eMKPkg from uninstalled R881 host5554b584 and bounded evidence.
  Older entries explicitly historical. Final R881 settings-cancellation test
  mobile-ui.mFco3t also passed; no fresh runtime acceptance this turn.
- Next one causal actual accessibility/input comparison; keep full PRD gates.
  Disk17GiB free at start, no cleanup needed or performed. Full goal remains open.

## 2026-09-09 — R881 explicit accessibility button activation

- R880 visual evidence was progress; Plus runtime input remained inconclusive.
  Source trace confirms Plus->Buttons/+ mapping intact; UIButton subclass had
  no accessibilityActivate override, only TouchDown/TouchUp/Cancel handlers.
  This is a missing explicit activation route, not proof of R880's root cause.
- Added accessibilityActivate returning success only for eligible active UI,
  publishing a150ms pulse on separate accessibility mask. Per-button generation
  and reset epoch discard obsolete releases. Physical-touch held bits remain
  independent; idle/menu/settings reject activations; reset clears pulses.
- Initial dispatch-after test ox67fb failed because main-queue delivery did not
  execute inside the nested test run loop. Changed to main-run-loop NSTimer;
  xmTm1l passes press/expiry/physical-held preservation/reset/idle tests and
  existing regressions. Additional settings cancellation test added afterward.
  Final host build65243 exits0; no vendor/module changes or product install.
- Candidate is host-only; next actual accessibility activation/game response
  with this build. Do not claim Plus gameplay behavior, physical multitouch,
  full VoiceOver playability, performance or device acceptance from unit tests.
  Sole test Simulator shut down; installed product remains R880 eMKPkg.

## 2026-09-09 — R880 phone pause visual check

- R879 was implementation progress. Signed stageeMKPkg host
  de7b3c4ff9e12eb807cd5c6e9a2f0262eadba0df3dcc54fbf00128c9e1202119,
  unchanged module; sole iPhone, existing data, DevInputFile only. PID99980,
  console98643; data container9554C050-E49E-47C6-B293-167C50834343.
- Leased A+B reached files; file2 pointer(.63,.65) opens confirmation, new Plus
  clears Yes/No and prompt. Cancelled No(.35,.79); no file creation. File1
  (.38,.65), Play(.76,.88), ten A1s/release3s pulses reached plaza. All senders
  terminal0/neutral. Screenshots product-r880/{confirmation,plaza}.png.
- Actual plaza: Plus is clear of central Mario/lower HUD. Story/Back still
  obstructed by movement stick; this change does not fix those. Simulator AX
  click on Plus yielded no visible pause screen: causal button behavior is NOT
  accepted. Need distinguish activation delivery/mapping/game phase, not move
  the button again based on lack of pause response. No physical multitouch proof.
- Native Stop clean21:34:59.415 failed=0, save compares unchanged with R859.
  Runtime stopped before app termination; console98643exit0, Simulator shutdown.
  No performance measurement/gain. Next input-path investigation or remaining
  story/Back layout issue, not a repeat of this file-opening visual route.

## 2026-09-09 — R879 phone pause-placement candidate

- R878 runtime restart verification was progress. Returned to phone center/HUD
  overlap. First proposed lower-right Plus (.73,.88) failed existing center
  regression Wobgy5; inspected R848/R849 rationale and actual confirmation.png.
  That lower-right placement would regress the observed Yes overlap; rejected.
- Candidate phone Plus now safe-normalized(.72,.62), inward beside the action
  cluster and above confirmation actions; same compact size, custom origins
  preserved, iPad untouched. Original SunPad layout inspected as reference, not
  copied blindly across Galaxy's different HUD/confirmation geometry.
- Replaced positional-center assertion with confirmation-action exclusion;
  added lower-central-content exclusion, safe-area/min44 and action/optional
  target separation checks. Sole-phone UIKit Erb9Fm passes; build67192 exits0.
  Geometry checks use a conservative rectangular approximation, not full game
  screenshot acceptance. Existing custom-origin/editor tests continue passing.
- Updated host-only candidate, not installed. Next visually test candidate in
  actual phone gameplay and save confirmation without creating/deleting saves;
  reject if another obstruction appears. No FPS or ergonomic acceptance claim.
  Simulator shut down; installed product still R878 OZjNpA.

## 2026-09-09 — R878 actual same-process restart

- Prior R877 implementation was progress. Fresh signed stageOZjNpA installed on
  sole iPhone17Pro; host f25a065dcb8b27ad1d5780c0b6a8c2f03624f9f495f11f59a6f2030c89aa0ece,
  unchanged accepted module. Existing private import retained; new data container
  EB92BCE6-E3C8-4065-BF9B-89F960A98221. No additional disc copy.
- First runtime visibly rendered strap; native Stop completed21:20:27.980.
  Stopped screenshot: only menu, Game stopped and clear Restart Game button;
  no gameplay controls. Clicked Restart at21:20:51 in same PID98619, second
  module load/RemoteIO initialization and fresh strap then title rendered.
  Second native Stop completed21:23:04.026. Both runtime exit failed=0.
- A was clicked after restart, but title transition is not sufficient causal
  input proof because startup also advances automatically. No gameplay control,
  multitouch, audio quality, performance gain or physical-device acceptance.
  Restart renderer/runtime recovery is bounded to this one pair of runs.
- Evidence generated/runtime/product-r878/console.log, stopped.png, restarted.png.
  GameData.bin compares unchanged with R859 backup. App terminated only after
  runtime shutdown; console85631 terminal0, sole Simulator shut down.
- Next return to phone layout overlaps and real gameplay input, not another
  startup restart pair. Full PRD/performance/audio/device gates remain open.

## 2026-09-09 — R877 stopped-state implementation

- R876 was progress: packaged Audio retention and concrete stopped-screen gap.
  Added Restart Game button after runtime exit, extracting the existing startup
  path into startGame. Busy/import/removal guards prevent duplicate starts;
  stop stays unavailable when idle. Reset frame-observation baselines on restart.
- Overlay gameplayAvailable separates idle visibility from controller/user
  preferences; clears input on idle, rejects pointer contact, hides gameplay
  controls while preserving menu access. Layout editing can still show controls
  intentionally. Startup enables controls; stop/exit disables them.
- Initial compile caught initialization inserted into the wrong local class;
  corrected before testing. Final Release host build37568 exits0. Sole-phone
  isolated UIKit67714 exits0, mobile-ui.9W3ot7: idle hides A, menu remains,
  controller refresh cannot reveal A, active restores A, existing tests pass.
- No product install or actual stop/restart renderer reuse proof this turn.
  Installed product remains R876 O35wDl; candidate host-only product-r841 updated.
  Next stage and visually verify stopped screen and same-process restart, then
  resume phone layout/performance work. Simulator shut down; no save mutation.

## 2026-09-09 — R876 packaged Audio menu lifecycle

- Prior turn progress: R875 implementation/build/UI regression. Fresh stage
  generated/simulator-stage.O35wDl/GalaxyPad.app, strict ad-hoc verified; signed
  host a03424edab02a47575421d05248f4008b5aa8d7107a6a7f171e359209fb389a1.
  Accepted unchanged module input3acdcddc; installed only iPhone17Pro. Existing
  private import/save retained; no extra disc copy or device build.
- Actual native UI: Audio submenu visible, Mute selected during startup; then
  Main Volume50% selected while muted. Title subsequently rendered. Normal Stop
  completed21:10:28.300 failed=0; runtime Dolphin.ini saved Muted=True/Volume50.
  Relaunch retained checked mute and50% label (audio-retained.png). Unmute via
  native menu, normal Stop completed21:13:49.225 failed=0; runtime ini now
  Muted=False/Volume50. This exercises persistence and host config application,
  not measured output gain, listening, held-input safety or physical-device audio.
- Logs generated/runtime/product-r876/{console,relaunch}.log. App data container
  now75993B16-A41B-47CF-B1CA-FB08FF37C73B. GameData.bin compares unchanged with
  R859 backup after both runs. Native game stop precedes app termination; sole
  Simulator shut down. No performance gain or broad gameplay acceptance.
- Found concrete stopped-state UX defect: gameplay controls remain visible over
  Game stopped with no obvious Restart action. Next fix idle/restart affordance
  while preserving data/menu access and preventing duplicate runtime startup.

## 2026-09-09 — R875 main audio controls

- Added persisted main volume/mute and native Audio menu. Five volume presets;
  mute retains level; handlers clear touch input. No action text on game buttons.
- Startup settings applied between Create/Run; live settings queued to runtime
  host from existing lifecycle timer, with session/state/stop checks. No vendor
  change, module regeneration, buffer tuning or gain-DSP workaround.
- Release host build68875 passed. Sole iPhone isolated UIKit23801 completed0,
  log generated/tests/mobile-ui.cEj0ZR, including new bounds/corrupt-pref/mute
  retention/checkmark tests. These do not execute the host audio adapter.
- Product remains the earlier installed R866 candidate. No save/disc mutation,
  performance claim, audible gain or physical-device acceptance. Next stage and
  test actual audio menu/lifecycle; full PRD, speaker gain and phone overlaps open.
- Final retry-state change rebuilt56065 and strict ad-hoc signature verified.
  Host SHA256 fab3897af0fc04c5d6c7b4111f24c6122616033deae4f77d2d0c1273a4d73b07;
  generated/candidates/product-r841/GalaxyPad.app remains host-only. Sole phone
  shut down; no booted Simulator. Tracked diff whitespace check passes, not a
  claim that the largely untracked checkout is reproducible from HEAD.

## 2026-09-09 — R874 live volume API capability

- R873 produced bounded recovery/route evidence but no progression. Parked
  repeated wall navigation; inspected missing PRD Audio menu and current backend.
  CoreAudioSound ignores SetParameter status; suspected HAL/RemoteIO mismatch.
- Added standalone tests/remoteio-volume-probe.mm. Creates/disposes RemoteIO,
  uses same output volume parameter/scope/element before Initialize, sets0 and
  reads it back. No stream start, no preferences, no product install. Build/sign/
  sole-phone boot/spawn16802 exits0: set=0 get=0 value=0. Hypothesis refuted for
  this Simulator, not physical output or whole lifecycle acceptance.
- Public Runtime lacks volume API; internal AudioCommon volume helpers exist.
  Next lifecycle-safe adapter/persistence before native menu, followed by actual
  output verification. Do not redesign backend or increase queues based on this.
  Sole phone shutdown. No game/FPS/save/product-source change this pass.

## 2026-09-09 — R873 route clarification and checkpoint recovery

- R872 was progress. Consulted walkthroughs to correct route ambiguity:
  https://www.gamerguides.com/super-mario-3d-all-stars/guide/super-mario-galaxy/good-egg-galaxy/dino-piranha
  describes tower-top Luma/Sling Star; community mission guide agrees. Sources
  are route guidance, not GalaxyPad behavior proof. GameFAQs direct fetch failed.
- Same isolated profile/normal671729c6/modulec0021b9f, console68047. Load3 visually
  restores rounded path end. Diagonal-forward-right2s then right8s returns to
  tower wall; short forward jump does not reveal ascent. Three input handles
  terminal0/neutral. No new launch/progression evidence and no diagnosed bug.
- Native Quit68047terminal0, NAND cmp passes; checkpoints not overwritten.
  runtime-r873.log retains counters. Next improve visual route understanding
  rather than repeat same wall inputs or call a navigation limit runtime failure.

## 2026-09-09 — R872 Good Egg underside progression

- R871 was progress. Same isolated progression-r871, normal runner671729c6/
  modulec0021b9f. No booted Simulator/other game before console49211 launch.
  Module-loaded log before CUA. Load2 reloads house-side checkpoint; enemy
  contact leaves life2, not a save-integrity failure.
- Short checked Pipe movements traverse planet edge to underside stairs.
  Added forward/right short jump and left/back short movement fixtures (commands
  only, not guaranteed routes). Ledge recovery visibly succeeds. Forward jump
  then Spin removes stair enemy with Star Bits appearing. Further jumps reach
  curved upper path, leftward moves reach its rounded end. Some forward/back
  attempts show no position change; obstacle/camera mapping not fully resolved.
  No launch-point or new-star claim. All input tool sessions terminal0/neutral.
- Saved isolated slot3 SHA256
  af5bfcfbb8c486d7ac5b23a8ac878ef5c3252f0c472f511ceeee665bdd4ebe44.
  Slot3 not reload-tested. Source checkpoint and NAND untouched; post-run NAND
  cmp passes. Native Quit49211terminal0, fallback0/smc_failed0. Runtime log
  generated/runtime/progression-r871/runtime-r872.log; screenshots viewed CUA.
-~782s DMA active time with74underruns/43backlogs; no quantified smoothness or
  audio acceptance from window labels. Next resolve launch route from slot3
  (or preserved slot2), not repeat opening or infer ride readiness.

## 2026-09-09 — R871 restore actual progression route before ride probe

- R870 was progress. Inspected save/progression records: Good Egg available,
  no verified ball/ray checkpoint found. Chose actual progression instead of
  adding a probe that cannot yet see its mechanic.18GiB available, no game or
  booted Simulator before launch. Copied R418 Config/Wii/StateSaves/config.ini
  into new generated/runtime/progression-r871; created independent Pipe FIFO.
- Normal runner671729c6, accepted modulec0021b9f, explicit extracted root/private
  profile/Metal/no-mods. Console31316/PID93784. Waited for module-loaded log;
  bundle-ID CUA selection ambiguous, exact generated/macos/GalaxyPad.app works.
- Native Load1 restores verified Good Egg landing; copied source slota30fd3ed.
  g6-walk-forward-short Pipe fixture3547 exits0, visibly moves Mario to house
  with Read/A prompt. Native Save2 creates isolated slot SHA256
  ec44edde0f8b1fdbd4e04f8193f0edc893f48902aec5056cfbca5249d49b0a69.
  This slot not yet reload-tested; source slots/saves untouched. Next progression
  from here, not another initial-file verification or invented ride fixture.
- Native Quit31316 terminal0. NAND cmp against source passes. Whole-run DMA
 45underruns/12backlogs and nonzero PCM, not perceptual audio/performance pass.
  Screenshots inspected via CUA; no new star or ride completion claimed.

## 2026-09-09 — R870 exact ball dispatch and output boundary

- R869 was progress. Inspected constructor, vtable construction/data, shared
  movement virtual call and axis prologue/output/epilogue in generated chunks,
  then independently asserted raw DOL words and branch targets.
- Extended audit-tilt-consumers.py checks Tamakoro allocation result storage,
  acceleration vtable entries, shared movement tail branch, vtable+0x20 call,
  r4/r5 output preservation and float stores. Audit passes. Detailed addresses
  in TILT-CONSUMER-AUDIT.md. Source labels remain qualified; checked operands
  establish only this bounded dispatch/output contract.
- Candidate probe should observe original axis outputs and live object identity
  before any mechanic-specific adapter. No global motion changes, runtime hook,
  game/Simulator launch or performance claim. Full gameplay acceptance remains.

## 2026-09-09 — R869 exact-DOL tilt call contracts

- R868 was progress. Located existing R546 pinned Bussun USA symbol leads;
  preserved its warning instead of treating names as authoritative. Located
  corresponding generated chunks0812/0814 and compared selected call operands
  directly against extracted RMGE01 DOL SHA2c680585.
- Added read-only scripts/audit-tilt-consumers.py: explicit identity rejection,
  section mapping, relative branch opcode/target checks and core/sub selector
  load check. Pass confirms eight branch relationships; details recorded in
  TILT-CONSUMER-AUDIT.md. No raw game bytes copied into public artifacts.
- Distinct sphere stick/acceleration paths and ray gate/shared getter now have
  exact-byte call-site evidence, not just Korean reference source. Full ABI,
  dispatch, live values and gameplay unproven. No build/game/Simulator or patch.
  Next narrow dispatch/return-boundary audit then actual mechanic observation.

## 2026-09-09 — R868 source-level tilt consumer audit

- R867 was progress. Inspected complete local sphere controller source, relevant
  Tamakoro constructor, ray steering/ride/tutorial gates, host device config,
  vendor acceleration transformations and WPad reference conversion.
- Found distinct consumers and pointer/acceleration coupling; no evidence that
  raw host tilt automatically satisfies both mechanics. Reference code is
  incomplete and symbols are RMGK01, so no guessed RMGE01 patch was applied.
  docs/TILT-CONSUMER-AUDIT.md records exact paths and next falsifiable sequence.
- Also found old R498 response controls belonged to prior overlay/routing lane;
  historical menu tests never accepted actual ball/ray behavior. Do not inherit
  them as gameplay evidence. No Simulator/game/build launched this pass.
  Next exact RMGE01 consumer resolution then mechanic-specific observations,
  not more response settings or a global Wiimote orientation change.

## 2026-09-09 — R867 touch tilt response and preference safety

- R866 was progress and closes bounded menu-copy check. Read PRD tilt/menu
  requirements and actual stickChanged path: previously raw axes/no response
  preferences. Added touch-only response submenu with three sensitivity choices,
  Y inversion and recenter via existing clearTouchInput. Default1× unchanged;
  movement and physical-controller paths not modified. Settings changes release
  current touch state before response changes; output saturates±1.
- New persisted sensitivity clamps0.5–1.5; nonnumeric/nonfinite fallback1×.
  Isolated UIKit tests exercise default axes,0.5 scaling,1.5 saturation/inversion,
  input clear on changes, movement unaffected and malformed preference fallback.
  These directly invoke production handlers, not physical gesture delivery or
  actual menu action clicks. Test96164/LTJKzM terminal0 with completion marker.
- Release/test build78244 terminal0; signed strict-verified host SHA256
  803c93a6b9b8f540bdd3051552c8bbba2ade0af25a83277cda37f8c420d40731.
  Not staged/installed. Existing fjpWgy app/import/save untouched; sole phone
  shut down after isolated tests. No performance or ball/ray acceptance claim.
  Next inspect/verify actual mandatory tilt consumers and progression route;
  do not substitute more settings work for gameplay proof.

## 2026-09-09 — R866 shortened native copy visually verified

- R865 was progress. Reused built42f9215b; fresh private fjpWgy stage signed
  and strict-verified. Installed host SHA256
  d3c3ffa4e4fb7f502b77a178d1d5449881ad87017326c5a68fefbc2df2d748f4,
  module32a92fdb unchanged. Sole phone launched without arguments, existing data.
  Console53660/PID92139; boot9114 terminal0.
- Actual native guide and data-status open/dismiss; CUA screenshots show all
  shortened text plus Done without scrolling at874×402/default text size.
  Private captures generated/runtime/product-r866/{guide,data-status}.png.
  Status shows existing image/save and full expected hash/disclaimer. This
  closes bounded compact-copy readability, not all Dynamic Type/device cases.
- Native Stop failed0 at20:09:37.710. Current data container relocated to
  F485E1AC-8AD0-4E22-BC93-A361A0F8D7E7; cmp against R865 backup passes.
  Idle host terminated and sole Simulator shut down. No game-state input,
  gameplay performance, held-input/audio acceptance or product source changes.
  Next unresolved gameplay mechanics/control overlaps rather than repeating
  startup/menu checks. Full original PRD stays active.

## 2026-09-09 — R865 actual native menu inspection

- R864 was progress. Opened Simulator explicitly with CurrentDeviceUDID for
  sole phone7B639924 after boot; CUA getApp now succeeds. Prior timeouts did not
  prove game failure. No Simulator service or unrelated process restart needed.
- Staged current host as generated/simulator-stage.ZSXvs9, signed/verified and
  installed. Final hostdecd823c9a4d90ad59832e0542253b5481888cee87f78d098838ce5868d02814,
  module32a92fdb. Plain app launch console62336/PID91355 uses preserved import.
  Guide opened during startup, dismissed; native UI unblocked20:02:22.096 then
  wrist-strap scene rendered. Status opened afterward detects image/save and
  displays expected hash. Native screenshots inspected through CUA.
- Actual compact402pt landscape reveals both alert footers need scrolling.
  Shortened guide by removing redundant customization paragraph/wordiness;
  shortened status disclaimer without claiming validation. Build77037 exit0;
  phone UIKit59071/NxFYwp passes. Signed strict-verified host42f9215bd0f94fc31a62fa563317c1d7e22cbe217d15e5d0fd9389a19096d053,
  not staged/installed; new copy still needs visual recheck.
- Native Stop exits failed0 at20:04:24.618, idle app terminate and console62336
  terminal0. Initial cmp used stale container and failed ENOENT; simctl resolves
  relocated container318157D7-DBFD-4A3A-AAA3-6BF3482287F1, then cmp confirms
  save50b7290a unchanged. Sole phone shutdown. No fresh gameplay/performance,
  held-input/audio or full lifecycle acceptance from this startup/menu run.

## 2026-09-09 — R864 PRD game-data status gap

- R863 staging was progress. Rechecked CUA getApp, again -10005 timeout. Moved
  to a concrete missing PRD menu requirement rather than retrying product boot.
- Added optional host delegate and read-only Game Data & Save Status action.
  Keeps destructive Remove last and disabled without host; status independently
  disables when handler absent. Host reports supported RMGE01 revision0,
  generated expected DOL SHA, installed-image and GameData.bin presence only.
  Wording explicitly excludes fresh integrity checks and saved-progress proof.
  No path disclosure, file-content read, rehash on main thread or save mutation.
- Release build plus isolated test build14559 exit0. Actual sole-phone test
 43487 exits0, generated/tests/mobile-ui.cGLZFl completion verified. Tests cover
  status action identity/non-destructive disabled state, not host filesystem
  status combinations or native screen appearance. Existing menu tests pass.
- Signed/strict-verified host SHA256
  6baab23bec58927e9975396129a2fda199ae0712de0e4b0ea3759a3bf78c4406.
  Not staged/installed; iGn9x1 remains R862 candidate, installed7BjbCR unchanged.
  Sole phone shutdown after tests. Next product visual/input/lifecycle checks
  once UI access recovers; no performance improvement or shipping acceptance.

## 2026-09-09 — R863 package current guide/import host for verification

- R862 was progress (source/tests/build). Created fresh private Simulator stage
  generated/simulator-stage.iGn9x1 from host69f23f7f and module3acdcddc; signed
  module then app and verified deep/strict signatures. Final host SHA256
  0445a3e155ced1e439eca5b069f6f00f180c8dbd1b059e9565d06464965ce836,
  module32a92fdbfde6921dcb72c72cc01eeb01de54969c596bff102538119b4e28a345.
- Boot41409 terminal0 for sole iPhone7B639924. CUA getApp by name twice and
  bundle ID once timed out -10005; getState inventory succeeded and reported
  Simulator running, corroborated by pgrep PID94843. No restart based solely
  on a timeout; no installation or game launch occurred. Existing import/save
  and installed7BjbCR untouched. Stage/sign18764 terminal0.
- Actual guide readability/dismissal and game pause/resume remain unverified.
  Shut down sole Simulator; next use staged candidate once UI access recovers.
  No performance claim, no full-goal blocker or completion declaration.

## 2026-09-09 — R862 discoverable touch mappings

- Reviewed R608/R624/R861 evidence before choosing next work: basic mobile
  file navigation and Star Bit collection/shot already have bounded evidence.
  Refreshed goal queue; no repeat opening or counter-census run this pass.
- Added Touch Control Guide under Controls, retaining root seven groups/actions
  and SunPad letter-only buttons. Native alert explains real face labels,
  independent pointer/actions, title A+B and optional non-device-motion tilt.
  Uses root host presentation path; no new runtime input routing or settings.
- Isolated tests assert submenu reachability, mapping/tilt wording and explicit
  dismissal. Initial test compile failed on UIMenuElement.attributes; corrected
  to checked UIAction cast. Phone log generated/tests/mobile-ui.OcRdCe passes.
  Release build20781 exits0; sign/strict verify63807 exits0. Host SHA256
  69f23f7f1096b53891e8e9e3c24b5eeed4e3a2af2b472231090b201223176140.
- Not installed/staged; no product visual, runtime pause/resume or FPS claim.
  Existing installed stage/import/saves untouched; sole phone shut down.
  Next visually check guide sizing/dismissal in product, then unresolved
  gameplay mechanics and control overlaps, not another opening-only run.

## 2026-09-09 — R861 packaged saved-file recovery

- R860 storage fix was progress. No rebuild/install/reimport; retained7BjbCR
  host3de9facf/module32a92fdb on sole phone7B639924. Console72939/PID88612,
  only DevInputFile argument, no external root/disc/module paths. Runtime log
  confirms bundle Frameworks module. Save50b7290a verified before and cmp after.
- File1 Mario timestamp18:50 visibly recovered; explicit file/Play inputs reach
  story.46s story fixture ends at Peach letter; observed before22s finish fixture.
  Plaza visible, moveY0.7/3s changes position. Recovered-file/plaza screenshots
  and runtime.log under generated/runtime/product-r861. All input handles end0.
- Native Stop clean, idle app terminate/console72939exit0, phone shutdown.
  This closes bounded packaged zero-star file recovery, not firstGrandStar,
  physical touch, forced termination/corruption, audio or performance acceptance.
  No need to repeat opening file verification as next default task.

## 2026-09-09 — R860 preflight import capacity before copy

- R859 native import/boot was progress. Source lacked capacity preflight despite
  PRD storage requirement. Added worker-thread filesystem available-space query
  before stage creation; unknown/error rejects. Conservative exact-RMGE01 budget
  9GiB covers3.51GBWBFS +4.70GBdisc-sized extraction +headroom; not reservation.
  No free-space assumption from deleting source, installed data or recovery data.
- Test subclass supplies boundary capacity without filling disk: threshold-minus1
  rejects without staging/extractor call, threshold allows synthetic transactions.
  Existing cancellation/failure cleanup/activation and separate activation tests
  pass31794. Test capacity is controlled so low-space CI can run tiny fixtures.
- README documents requirement and repairs first-launch paragraph continuity.
  Simulator host20642 rebuilds, ad-hoc signature verifies, SHA256
  d7965e0c00344335d5dd6a8799b411621464a5879ddc4744451567a75a7b67f3.
  Includes R859 success text; not installed or full-import-retested. Existing
  self-contained stage remains unchanged. No game/Simulator, no save changes.
- About19Gi free; retain existing phone import. Next packaged saved-file recovery
  and gameplay/control acceptance, no repeated data-copy or microbenchmark lane.

## 2026-09-09 — R859 native import to packaged boot

- R858 was progress. Disk25Gi free, existing extraction3.2Gi/image3.51GB.
  Backed up phone save into generated/runtime/product-r859/GameData.before.bin;
  APFS clone/no-overwrite copied source image into empty app Documents as
  Galaxy-RMGE01.wbfs. Original ref image untouched. PhoneC05D77B2 data retained.
- Actual native Game Data→Import from GalaxyPad Folder→image selection completes
  verification/extraction/activation. Savehash50b7290a05b5994115fac2476ed0c39ba60773899cbe14c0f441fce3e45722a4
  unchanged. Success text wrongly instructed Simulator development-module use;
  changed source to platform-neutral Relaunch GalaxyPad to play, not rebuilt.
- Initial console87835 terminal0. Plain relaunch9200/PID87874 loads actual
  app/Frameworks/gRMGE01_recomp.dylib, screenshotpackaged-boot.png shows strap.
  No development arguments or diagnostic input. This establishes import-to-boot,
  not actual file restore/first-play/performance/physical-device acceptance.
- Native Stop completed; after-stop save cmp passes. Console9200 terminal0,
  phone shut down. Logs runtime.log/relaunch.log in product-r859. About19Gi free
  after import; source clone and installed data retained for next test. Do not
  duplicate import unnecessarily. Next packaged saved-file/gameplay recovery.

## 2026-09-09 — R858 self-contained private Simulator startup

- R857 progressed host startup selection. Extended historical device-staging
  script with explicit SDK allowlist; default iphoneos unchanged, exact IOS vs
  IOSSIMULATOR checks retained. Stage28864 exits0 atsimulator-stage.7BjbCR.
  Wrong-platform device host in Simulator lane rejected; all15 unsupported SDK
  cases across5entrypoints pass, no configure/copy attempted by those cases.
- Copied module then app ad-hoc signed; deep/strict verification passes. Final
  host3de9facf2e97114e69b86efe633ec40a56b569cea4d996f75cdbd9e5c7c75f48,
  module32a92fdbfde6921dcb72c72cc01eeb01de54969c596bff102538119b4e28a345.
  Unsigned input identities retained in generated/stage-r858.log; signing changes
  bytes, not claimed byte-equality with accepted input after signing.
- Installed on sole phone7B639924, launched with no development arguments;
  actual UI says import supported image, native Game Data actions reachable.
  This proves bundle discovery/setup, not module loading or gameplay. README
  documents staging boundary; no public artifact or physical signing implied.
- No import started or save changed. Next actual Documents-folder import with
  storage preflight, then no-argument launch and existing file recovery. App
  console62448 terminated0 and phone shut down; no runtime remains.

## 2026-09-09 — R857 actual editor lifecycle and bundled startup

- Previous R856 broader suite/device staging was progress. Installed dae0296e
  in sole phone7B639924, console86066/PID86342, no development paths/game runtime.
  Actual menu→Controls→settings→Move→2 selection, Home, foregroundsamePID, Done
  observed. Selection/slider retained, editor ends and extra buttons hide.
  Does not exercise an active pan cancellation or running-game/audio interruption.
- Missing-module setup screen accurately reflects host-only app, but source also
  ignored bundled Simulator modules entirely. Moved packaged Frameworks lookup
  outside platform conditional; nonempty explicit override remains Simulator-only.
  Device imported data cannot select code; installed-data precedence unchanged.
- Build64576 exits0, signed/verified host
  857d6501a33ceb98707632f7334a0627a708f6745ce25f8a8e059c61b8f2da2c.
  This new host is not installed or bundled-module runtime-tested. Next package
  same known Simulator module privately, verify argument-free setup/launch and
  imported-data behavior rather than more CLI-only first-play runs.
- Console86066 terminated0, Simulator shut down. No gameplay/save changes.
  Logs generated/runtime/product-r857/runtime.log and product-r857-build.log.

## 2026-09-09 — R856 broader regressions and device editor parity

- R855 was progress. Ran entire scripts/check-repository.sh, handle94553,
  loggenerated/check-repository-r856.log. Terminal0/final Repository safety checks
  passed. Warnings retained, no failure suppressed. UIKit is separate; previous
  R855 phone/iPad passes cover its changed handler tests, not actual OS gestures.
- Explicit audit limit: git-based safety checks inspect tracked scope; current
  implementation mostly untracked, so no complete publication-safety claim.
- Device build/stage71522 exits0 with R854/R855 changes. LateststageRvOFtT host
  23284c78b44b41395fbfdb46309a3152b4c200502c9675e6d121057d662806cf,
  unchanged R853 core and48f455eb module. Release index updates both current host
  identities. No install signing, device test, game-run or performance acceptance.
- All handles terminal; no Simulator/game started. Next actual editor/lifecycle
  and gameplay/control coverage, not another unchanged full-suite invocation.

## 2026-09-09 — R855 transactional editor drag cancellation

- Prior R854 reset fix was progress. Current controlDragged saved both Ended
  and Cancelled states. Added isolated gesture-state test: default cancellation
  must not write; successful drag must write; later cancellation must retain
  both saved custom string and geometry. Redk7HOeF fails unwanted persistence.
- Cancelled/Failed now request relayout without preference write; only Ended
  persists. endLayoutEditing also requests relayout for unfinished geometry.
  Phone r4jVe9 and iPad SaYVdL PASS sequentially. Tests call real overlay handler
  with controlled gesture states, not proof of an actual OS interruption.
- Rebuilt Simulator candidate28336 exits0, signed/verified SHA256
  dae0296ec3a2be0ea5c765b222abe8758594de9e31ceb652280694a014584955.
  Includes R854/R855. No install/game test or device rebuild this turn; previous
  device-stage hashes remain valid for their older content, not new fixes.
- Both test handles terminal; both Simulators shut down. No game/save changes,
  no FPS claim. Next actual interruption/editor behavior and phone occlusion.

## 2026-09-09 — R854 reset-layout preference boundary

- Prior R853 refreshed device core. Inspected actual reset/settings/editor code
  and reference SunPad: inherited reset also removes opacity although confirmation
  promises positions/sizes. Kept SunPad editor structure, narrowed reset to that
  stated boundary; appearance and controller-visibility preferences retained.
- Added real UIKit test invoking reset after individual/global size, origins,
  D-pad origin/scale, opacity and visibility changes. Red phonehhdC5z explicitly
  fails opacity preservation; after change phonenQzRhf and iPad5qnbg8 explicitPASS.
  Phone shutdown before iPad boot, both test handles terminal0. No game launched.
- Fixed preview's stale cached per-control sizes by calling existing settings
  reset after clearing test persistent domain. This resolves R845 harness caveat;
  no actual preview visual claim this turn.
- README documents reset boundary. Product source change is not yet in existing
  Simulator/device stages; retain their recorded hashes until a justified rebuild.
  Both Simulators shut down; no game/save/settings data mutated outside test app.

## 2026-09-09 — R853 refresh stale device core

- R852 was progress. Provenance audit finds provisioned archive still exactR643
  f4730e66. Device module uses accepted generated path/PGO/O2; ABI header timestamp
  predates module, not sufficient semantic compatibility proof. Signing inventory
  reports0valid identities; no connected device from R852 inventory.
- CMake configure succeeds. Ninja dry-run only predicts regeneration, not a
  reliable pending build count; did not report its2steps as full rebuild scope.
  Actual four-job core37500 performs226steps, exits0. Logdevice-r853-core-build.log.
- Provision/relink/stage75995 exits0. Coreb1c9def7c0d270b2376de414bbad98793cd0448ca0ede9b309c6cd53e8deeeac;
  stagegenerated/device-stage.EI86W0 host8824315d07b47cbd21c2821a6645c89e2376e9db4edb5bd02e787a7eb67b1c2e;
  unchangedmodule48f455ebc33f8eb2fc58151cd722ea756db77a5738a9573a0a49f0c656110041.
  Separate provision/host/stage logs retained; exact platform/export/icon checks
  pass. Old stages untouched, no sign/install/upload or performance claim.
- Release index updated. Physical testing needs signing identity and device;
  full goal remains active with other product/control work available. No Simulator
  launch or save changes. Core source rebuild is not runtime acceptance.

## 2026-09-09 — R852 current device host and safe staging

- Previous R851 added required release index. Device inventory reports none;
  older device app datedSep8 lacks Assets.car. No Simulator booted.
- Configured isolated generated/candidates/device-r852 Release/THP OFF with
  ios-device-toolchain; build83438 exits0 against existing device core archives.
  Log generated/device-r852-build.log. Current overlay and icons compile/link;
  linkage is not core/module ABI or gameplay acceptance.
- stage-private-ios-device.sh now takes optional explicit app/module paths,
  rejects excess arguments and requires Assets.car plus both AppIcon metadata
  families before creating a stage. Defaults retained; no old output overwrite.
- Actual positive stage57108 exits0 at generated/device-stage.yXz4N2. Host
  b2665b801500cf83a8324839b040b8ce6e39f69dcc093b7b854c57fbb58c09b3;
  module48f455ebc33f8eb2fc58151cd722ea756db77a5738a9573a0a49f0c656110041.
  Existing Mach-O/platform/minOS/export/install-name and copied-byte checks pass.
  Negative old app reports Missing compiled app icons before staging; bash -n
  passes. No signing/install/upload/publication. Release index updated.
- Next device core/module provenance audit and signing prerequisites; no device
  or performance claim. Existing Simulator app/save data untouched.

## 2026-09-09 — R851 close repeated converter lead; release index

- Previous R850 evidence/README update was progress. Current RunVertices and
  indexed normal/texcoord source reviewed; R683/R684 already cover the normal
  cursor optimization. R684's mixed timings do not justify reopening it merely
  because the same routine appears in R849. No vendor change/build/benchmark.
- Added PRD-required RELEASE-READINESS.md, preliminary NO-GO, not completed audit.
  Rehashed current67fc872a host and3acdc module; root289a87a is not reproducible
  candidate identity because implementation remains dirty/untracked.
- Explicit D12/row36 retained despite Section12.5's older D1–D11/rows1–35 wording.
  All release gates remain unaccepted pending exact-candidate evidence; historical
  partial runtime passes are not erased. README linked; local linked documents
  exist. git diff --check passes only tracked scope, not untracked-content audit.
- Next product priority: remaining phone occlusion and physical-device candidate
  readiness. No publication, app mutation, runtime or performance gain this turn.

## 2026-09-09 — R850 sample interpretation and README parity

- R849 made actual gameplay/save-load evidence progress. Read its two-second
  sample:1315 stacks per thread; main1288 in run-loop wait. CPU-GPU thread826
  under Run+2516, including773 native-dispatch subtree and51 depth-readback waits.
  Separate vertex conversion branches108+34 samples; indexed normals32,
  texcoords28, colors18, float positions16, short positions10 within the108.
  Inclusive counts are not exclusive CPU time, lost FPS, or all host contention.
- R536 prior dual-core experiment found no clear win. Don't infer that merely
  enabling two threads fixes current single-thread configuration. Next inspect
  portable indexed vertex conversion for a concrete avoidable cost and existing
  experiments before coding; no runtime change justified by this sample alone.
- README now records actual iPhone Simulator persisted-file/plaza evidence,
  separates physical-device gates, explains editor route/custom-layout retention,
  and names remaining overlaps. No screenshots/game media published.
- No builds or runtimes launched; Simulator inventory empty. No performance gain.

## 2026-09-09 — R849 actual iPhone candidate progression

- R848 was progress (tested default placement change). Installed67fc872a host,
  unchanged3acdc module on sole phone7B639924. PID80581/console54226; artifacts
  generated/runtime/product-r849 including runtime.log, confirmation.png,
  plaza.png, plaza-hud.png, slowdown.sample.txt and copied input-sequence.py.
- Fresh file-select shows Mario file1, contradicting the earlier uncertainty
  about whether R848 saved icon selection. New containerA124045F retains save:
  GameData.bin SHA25650b7290a05b5994115fac2476ed0c39ba60773899cbe14c0f441fce3e45722a4
  measured during this run; not a before/after equality claim.
- Empty file2 opens confirmation: centered Plus clears Yes/No labels; cancelled
  with No, no second file created. File1 details→Play→story→plaza observed.
  Diagnostic moveY0.7/3s visibly advances Mario. Actual A click not captured
  airborne, so jump not independently proven. No physical/multitouch claim.
- Remaining layout: move stick over Back/story text, Plus near title text and
  overlapping Mario during introductory plaza camera. Gameplay HUD mostly clear
  in later view, not all-screen/ergonomic acceptance.
- Recent five-second gameplay windows40.55,46.42,33.84,31.32 after-frame events/s.
  Host snapshot JumpConnect214.1%CPU, game81.6%, duetexpertd42.5%, WindowServer37.4%.
  Unrelated apps untouched. Two-second sample31924 completes; generated native
  dispatch and Metal waits present, not a quantified exclusive cost or FPS gain.
- Native Stop failed=0 at19:00:34.330; console54226 exits0, sole phone shut down.
  Candidate remains installed on phone only. Next address documented layout
  tradeoffs and inspect slowdown sample; don't repeat proven startup/file route.

## 2026-09-09 — R848 iPhone file flow and pause placement

- Prior R847 evidence correction was progress. Launched existing79ebf127 with
  unchanged3acdc module and opt-in input file on sole iPhone7B639924, PID79737,
  console57313. Artifacts generated/runtime/product-r848/runtime.log/build.log.
- Observed strap, empty file planets, create-file Yes/No, then icon selection.
  Explicit leased A+B/pointer input is not touchscreen or first-play acceptance.
  Mario-icon selection attempt did not visibly complete; no saved-file claim.
- Plus overlays Yes in the observed confirmation screen. Changed only phone
  default Plus X to safe-area midpoint; Y retained0.88, custom positions preserved.
  Added center regression; UIKit AlruL3 explicit PASS. Build9965 exits0; ad-hoc
  signature verifies, new candidate SHA256
  67fc872a0c3f0256ef6ea6c30329881e12110cbe1c771374dfa94e4863531e2b.
  Not installed or actual-screen rechecked yet; no iPad geometry changed.
- Home then foreground returned same PID and icon screen. Limited lifecycle
  smoke only, not held-touch/audio/save acceptance. Stop failed=0 at18:52:19.901,
  runtime console exits0; phone shut down. No iPad saves copied or changed.
- Next install candidate on phone, confirm Plus clears Yes/No in real product,
  then complete icon selection/new file and extend actual gameplay.

## 2026-09-09 — R847 avoid repeating the fallback investigation

- Previous turn was progress: actual phone UI/startup evidence. Read R846 log,
  current StaticRecompCore_Run.cpp interpreter/JIT branches and
  StaticRecompCore_Hooks.cpp cache fast path; compared R841/R844 product logs.
- R841 fallback40635768/hook_fb89924502; R844 fallback59438987/hook_fb131680581.
  Nonzero totals therefore predate the iPhone run. They do not establish an
  iPhone-only regression, and hook_fb is not an interpreter instruction count.
- PERF R489/R553 already records counter semantics. JOURNAL R755 captured
  99.1379% low-memory fallback events in plaza; R758 verified vector bytes.
  Those phase-specific results do not identify every R846 startup event, nor
  turn event count into timing. Avoid another unchanged census as default work.
- Replaced mistaken R846 queue with actual iPhone progression/lifecycle. No
  source/build/runtime mutation, no performance claim; all Simulators remain off.

## 2026-09-09 — R846 clean iPhone product validation

- Private host79ebf127, unchanged module3acdc; first GalaxyPad installation on
  iPhone17Pro7B639924. Separate new data container, no iPad saves copied.
- Actual wrist-strap screen, menu→Controls→Touch Control Settings verified;
  Reset fully visible and menu reachable. Captured settings-phone.png in
  generated/runtime/product-r846. A UI tap recorded four button samples/two
  transitions, not simultaneous-input or gameplay proof.
- Confirmed native Stop, runtime failed=0 at18:45:37.720. Interpreter fallback
  count7843419 and hook_fb11230256 prohibit native-only acceptance; no timing
  comparison claimed. Next classify startup fallback source before progression.
- Console91133/PID79108 terminated cleanly; sole Simulator shut down. Candidate
  remains installed only on phone. No source/runtime optimizations this pass.

## 2026-09-09 — R845 compact editor and settings accessibility

- Prior R844 progress. Current phone UI reproduced partially clipped Reset and
  bottom editor covering extra Wii buttons. Changed compact panel placement and
  upper editor geometry, retaining mappings/custom positions; Done44pt.
- Added UIKit panel/reset/menu/auxiliary reachability checks. Phone qlRMQy and
  subsequent iPad H2Rh0i pass. Actual phone settings→Move→2 selection→Done works.
  Preview singleton retains prior size1.25 despite cleared preferences; documented
  as harness limit, not claimed new resize or product corruption.
- Rebuilt/signed private79ebf127 host in product-r841 directory, no install/game
  run. PHONE-EDITOR-R845.md records screenshots/limits and next actual iPhone
  product lane. Both Simulators shut down, no save/data changes.

## 2026-09-09 — R844 actual inset-layout/settings and icon evidence

- Previous R843 progress. Ran exact fdf7539e candidate, unchanged guest3acdc,
  one iPad, console97077/PID76635. Diagnostic route with observed intermediate
  letter state and finish sequence reaches real plaza.
- Both lower-corner counters now clear. Settings compact/touch-only and close
  verified in game; actual A UI click responds. Physical reach and file-menu
  crowding remain open. Native Stop failed=0; save99d432 unchanged.
- Home/Dock icon visually verified after app termination. Added macOS ICNS slot
  generator, package copy/plist key and PROVENANCE.md. Icon generation, plist/shell,
  macOS config and iOS icon tests pass; no Mac runtime or full-package promotion.
- PRODUCT-UI-R844.md records evidence/limits. Restored prior85ffc app and shut
  down Simulator. Continue full product/performance goal, not another same-plaza
  layout-only loop or a shipping claim from isolated acceptance.

## 2026-09-09 — R843 HUD-clearance layout candidate

- Previous R842 was progress. Inset iPad default movement/D-pad and right button
  cluster to avoid actual R841 lower-corner HUD; preserve per-control saved origins
  and phone defaults. Relocated optional tilt/Plus to keep targets separate.
- Extended actual UIKit pairwise checks to D-pad and conservative scene-specific
  HUD rectangles. Initial H5LdQy exposed1pt story-copy intrusion: iPad stick146→144.
  nugB3C/7A1Ap8 exposed Plus/Right overlap: adjusted default D-pad center43→41%.
  Final full iPad suite cnoWtk passes; tests not weakened.
- Inspected landscape preview, generated/ui-r843/layout-candidate.png. Inward
  clusters trade corner clearance for farther thumb reach; ergonomics remain
  unaccepted. No original SunPad mapping/appearance rewrite, no game HUD mutation.
- Product rebuilt in private product-r841 build directory and signed/verified:
  fdf7539e588943c4fbcfa5374bcebdaffe4c0bd66b72b38faa5104a5fb7be14e.
  Not installed/promoted. Preview terminated, sole Simulator shut down.
- Next real plaza/settings comparison of this candidate; reject/refine if reach
  or gameplay feels worse. Full gameplay, performance, device and branding gates
  remain open. Do not call conservative zone assertions full HUD acceptance.

## 2026-09-09 — R842 touch-only settings

- Prior R841 was progress: actual gameplay supplied visible defect evidence.
  Removed render selector from touch settings and its ivar/handler/update paths;
  unchanged Display actions retain render persistence and four choices.
- Added UIKit assertions for no segmented duplicate and retained Display choices.
  Hp0Ycg passes; live isolated settings preview confirms touch-only contents.
  Preview showed excess blank panel, so cap reduced430→320pt, height budget62→80%
  for compact screens; Close/Reset44pt and rows minimum44pt. Final suite eOJsHY
  passes; final compact geometry not visually rechecked yet.
- Rebuilt/re-signed private product in generated/candidates/product-r841 (path
  now contains revised source, not original R841 binary). Original installed app
  untouched. No game run, module or save change. Sole iPad Simulator shut down.
- Read current product preferences: no saved control origins/D-pad origin;
  saved per-control scales A/2 are1. R841 HUD intrusion is a default defect.
  Next targeted default HUD placement plus final settings geometry recheck in
  actual product. No performance, gameplay-layout or shipping acceptance claimed.

## 2026-09-09 — R841 product UI against real game

- Prior pass progress; built private complete host with new icon/menu/controls,
  signed/verified0a1fe67e, unchanged guest3acdc. No normal build overwrite.
- One iPad Simulator, console12025/PID73986. Exact local data/module, backed-up
  save; diagnostic navigation reaches opening plaza. Real screenshots identify
  HUD intrusion and touch-settings render-row contrast/duplication defects.
- Menu/settings open/close and confirmed Stop observed; runtime failed=0 and
  save99d432 unchanged. Restored prior85ffc app and shut down Simulator.
- PRODUCT-UI-R841.md records bounded claims and next fixes, not another preview
  loop. No sustained performance, audio, full touch, device or release acceptance.

## 2026-09-09 — R840 phone target size and separation

- Previous R839 was progress. Inspected phone preview and actual layout code:
  scaled36pt D-pad and40pt menu contradicted PRD minimum targets.
- Set44pt default D-pad/small-control floor and menu. Added actual UIKit phone
  minimum-size, safe-area and all-visible-pair overlap assertions; corrected
  exposed move/Down, A/B and Spin/C default collisions. Saved layouts untouched.
- Phone Qs1PUZ and subsequent iPad ZR0PDk suites pass, sequential Simulators.
  Initial cold-boot console attempt had no marker and is rejected; intermediate
  geometry failures retained. PHONE-CONTROLS-R840.md has paths and exact boundary.
- Both Simulators stopped. Next private product build/icon/menu/control check
  over real gameplay, not more isolated test-only acceptance. Full goal stays open.

## 2026-09-09 — R839 native menu refinement

- R838 changed authoritative product/docs state: progress. Continued product
  queue with source comparison to actual SunPad and live isolated overlay audit.
- Moved FPS under Display; removed empty experiment section; preserved confirmation
  while marking destructive actions. Stop now disables without runtime callback.
  Added callback setter refresh to prevent stale disabled action after host setup.
- Baseline UIKit pass8xf9g2; edited test initially failed variable collision, fixed;
  complete regression passzd646f. Actual preview menu/FPS toggle checked visually
  and AX-selected after reopen. No game runtime or save changes; Simulator stopped.
- MENU-REFINEMENT-R839.md records narrow evidence limits. Next controls/settings
  and private product gameplay HUD layout; no FPS gain or shipping claim.

## 2026-09-09 — R838 product experience reprioritization

- User requested continued optimization plus refined controls/menu, original
  iOS/iPadOS icon and SunPad-level README. Updated active loop without reducing
  original PRD acceptance gates or authorizing publication.
- Native 5947/69899 stopped at file details after a missed Play target. Retry
  84368/70270 remained at title; copied Dolphin.ini has BackgroundInput=False.
  Focus dependence is a diagnostic lead, not a tested fix. Both exited0 through
  native Quit. No 30-second thread-work capture was started or accepted.
- Added missing root README using actual SunPad structure and Galaxy source/docs.
  Explicitly records no release/device acceptance and local-artifact-pinned
  mobile build rather than advertising a clean-clone workflow that is not proven.
  No normal app/module promotion or Simulator launch in this product pass.
- Generated original orbital icon with built-in image generation; inspected full
  source and saved source/prompt provenance in apple/branding. Added opaque1024
  universal iOS catalog and CMake icon target that merges only icon metadata.
  Real actool compilation passes for Simulator and device in an isolated temp
  bundle. Focused test passes repeat builds, preservation of other plist keys and
  missing-family failure without writing the plist. Registered in repository
  checks; full suite/product rebuild not run. Home-screen/small-size acceptance,
  macOS packaging and editable layered/vector source remain open.
- Next: screenshot-led audit of active GalaxyPadGameOverlay and original SunPad
  UI, then a bounded menu/control change with regression and visible verification.
  Do not restart parked native performance navigation before this product work.

## 2026-09-09 — R837 native counter host and profile

- Prior turn progress: complete Simulator thread-work result. Extended private
  builder native lane; incomplete compile DB resolved through exact Ninja rule.
  Cached runtime object reproduced; normal dependency files/Sys copy untouched.
- Retained pre-candidate stops for build-rule shape and control UUID metadata.
  Native control differs only16UUID+2LINKEDIT bytes; all other contents equal.
  Explicit parsed UUID option tested; no general byte/code mask.
- Successful39453 creates signed9ee5404f host in thread-work-native-r837e.
  Private R830-derived Config/Wii/Pipe profile prepared; save99d432 matches.
- THREAD-WORK-NATIVE-HOST-R837.md records commands/provenance and next native
  neutral-plaza30s capture. No game/Simulator launched, speed or goal gate claim.

## 2026-09-09 — R836 complete CPU-thread plaza measurement

- Reused exact private host/module; prompt navigation, one Simulator.68587/37616,
  capture7485 and pointer45175 complete; CUA pre/post stationary plaza verifies.
- Zero drops, complete30s coverage:1482VI/49.379Hz,17.941ms CPU/VI,140.437M thread
  instructions/VI.98.333% CPU time on Performance cores. Raw Mach conversion
  agrees with existing clock within42ns over26.571s; no clock-unit explanation.
- No optimization: difference from R831 reflects unmatched run/context and
  instrumentation, not a proven speed gain. THREAD-WORK-RESULT-R836.md gives
  exact boundaries/counts and next native same-thread comparison gate.
- Clean menu stop/exports; original app restored and Simulator shutdown87371.
  Restored app85ffc100/save99d432 hashes verified under relocated containers.
  Full PRD remains active; no guest rebuild, product promotion or acceptance.

## 2026-09-09 — R835 first self-thread game capture

- Added strict sidecar summary and tests; pairing, topology, resets/errors,
  query uncertainty, Mach conversion and verified-prefix coverage pass.
- Private62865/63085 reaches visible plaza after CUA stale-menu recovery.
  Capture3090 and pointer47393 finish; menu stop exports both recorders cleanly.
-16384samples/1228drops; retained end is1.419151s short of requested30s window.
  Both summaries reject; no shortened substitute or performance claim.
- Original app restored and Simulator shut down87054; reinstall relocated data
  and bundle UUIDs. THREAD-WORK-CAPTURE-R835.md records paths/evidence and next
  same-build prompt-navigation retry. No normal module change or goal closure.

## 2026-09-09 — R834 isolated thread-work host build

- Prior turn progress: recorder overlay validated. Original runtime source/flags
  reproduce archived object exactly; no full module or build-graph rebuild.
- First73283 stops at unsigned relink difference. Exactly two bytes map to
  read-only LINKEDIT virtual reservation; all other bytes and UUID identical.
  Added parsed-field comparison and focused negative tests, no arbitrary mask.
- Retry62102 completes private runtime compilation/archive replacement/relink,
  signing and verification. Candidate24002e50, normal app/archive/module intact.
- THREAD-WORK-HOST-R834.md records provenance and next strict sidecar analysis,
  then one Simulator actual-thread plaza capture. No launch, save edit or speed
  claim; original full PRD remains active.

## 2026-09-09 — R833 private per-VI thread-work integration

- Previous turn progress: self-thread collection feasible on both targets.
- Added compile-guarded, opt-in fixed-buffer recorder and isolated header overlay
  preparer; normal vendor/header/module/app unchanged. Explicit query errors,
  raw reset evidence, bounded drops and exclusive post-join sidecar export.
- Fake-provider ASan/UBSan, ordinary/private VI lifetime, release exclusion and
  native worker-thread timestamp integration pass; existing wiring tests pass.
  Registered focused suite; full repository suite not rerun. Handles terminal0.
- THREAD-WORK-RECORDER-R833.md records identities and next private host-only
  build/include-precedence plus strict sidecar interval analysis gates. No game
  or Simulator launched, no speed gain or original PRD gate closed.

## 2026-09-09 — R832 self-thread counter feasibility

- Prior turn progress: R831 matched platform evidence changes next question.
- Added standalone scripts/probe-thread-work.c; Apple source-checked private
  wrapper, no raw syscall or permission changes. Native/Simulator O2 strict
  warning builds and execution pass; converted Mach CPU clock ratio1.000049/
  1.000053. Per-level counters available; not actual game-core attribution.
- Boot27680 and probe/shutdown51817 terminal0; one Simulator only, no game.
- THREAD-COUNTER-PROBE-R832.md retains measurements/commands/hashes/limits.
  Next opt-in private recorder with injected tests before host-only build;
  reuse guest module, preserve release API boundary and full original goal.

## 2026-09-09 — R831 Simulator plaza platform comparison

- One Simulator, existing app/module, LC0/THPoff/QoSoff. CUA pre/post neutral
  starting plaza; diagnostic pointer lease, no movement. PID60333/session94462.
- Work window921VI/30.010899s,30.688851Hz; CPU29.490ms/VI,218.988M process
  instructions/VI. Complete retained coverage with67.781s tail margin;2790 drops
  later outside window. No invalid CPU intervals or counter resets.
- Native counterpart59.949Hz/14.226ms,212.964M instructions/VI. Similar task work
  does not establish guest work or frequency; no QoS/cap/physical-device claim.
- Menu stop exports VI, runtime failed=0; terminate stopped app and shut down
  sole Simulator. Save99d432... unchanged. No product promotion or speed gain.
- PLAZA-PLATFORM-RESULT-R831.md contains reproducible commands and next bounded
  self-thread counter feasibility probe, not repeated whole-game/profile runs.

## 2026-09-09 — R830 macOS plaza work window

- Initial58953/48202 clean exit but VI coverage ends11.264s too early: rejected.
- Retry59437/50769 uses bounded story sequence; pre/post plaza screenshots,
  neutral capture6354, clean exit0.1799VI/30.008625s=59.949431Hz,CPU14.226ms/VI.
- Append-only recorder retry tail drops occur6.105s beyond window. Added explicit
  prefix-capacity opt-in with retained metadata and coverage regressions; first
  attempt still rejects. No trace editing or quiet normalization of drops.
- MAC-PLAZA-WORK-R830.md holds artifacts/boundaries. Save99d432... unchanged;
  next Simulator counterpart only, no repeated native run or game-speed claim.

## 2026-09-09 — R829 platform-cost reconciliation and measurement preflight

- Rechecked R639/R640 counters and R641/R642 closed QoS experiment; recent
  R813/R822 plaza artifacts lack comparable per-frame work windows.
- Private plaza-platform-r829 Config/Wii/Pipe prepared; save99d432... verified,
  current process-work probe and focused interval/recorder regressions pass.
- Device refresh empty. PLAZA-PLATFORM-PREFLIGHT-R829.md gives exact next native
  launch/capture, then Simulator sequentially. No live runtime or product change.

## 2026-09-09 — R828 compact normalization disposition

- Oracle without FENV pragma reproduces raw-status divergence; strict helper-only
  whole-chunk variant moves, but does not resolve, failure. No comparison masks.
- Expanded audit:8,704 full CPUState/memory passes,96host-flag failures, exit3.
- Separate all-checks-passing finite workload cost screen shows~27% slower CPU
  and~7% more instructions. Candidate parked, no more state-copy/flag/guard tuning.
  NORMALIZATION-DISPOSITION-R828.md records limits and platform-cost next gate.
  No runtime/Simulator/build remains; normal product/save untouched.

## 2026-09-09 — R827 compact FP state and actual-policy failure capture

- Implemented25-helper source-pinned compact closure; reversible-body/field tests
  pass. Both isolated sanitized and ThinLTO suites pass294,912 cases each.
- Work-path frame falls to192bytes, but whole-chunk normalized routine fails
  host flags0x15/0x1 at pattern3/RN0/NI0/entry3; full CPUState equal. No mask or
  timing. Diagnostic and retained reruns reproduce; all handles terminal.
- COMPACT-NORMALIZATION-R827.md records exact artifacts, PGO warnings and next
  failing-case investigation. No product changes, game launch or FPS claim.

## 2026-09-09 — R826 real normalization region state differential

- Source-pinned nine-instruction region and actual suffix charges extracted.
  Local FP-state candidate retains exact helper math and exception semantics.
- Sanitized O2 and unsanitized ThinLTO each pass294,912 CPUState/FPSR comparisons,
  including73,728 unavailable cases; all9suffixes/internal-external entries.
- Assembly shows large CPUState scratch remains, so no runtime timing or module
  build justified for this form. NORMALIZATION-STATE-R826.md records compact
  helper/state representation next, not further copies/attributes. No app/save
  changes, running game/Simulator/build, or claimed FPS improvement.

## 2026-09-09 — R825 whole-chunk alias-contract compile screen

- Previous turn progress: exact load attribution and callback checks. Compiled
  three control/hypothetical-restrict pairs with actual cached optimization flags.
- Fixed private harness header layout, LTO root retention and install-name layout;
  final artifact checks prove identical instruction encodings, not just counts.
- Simple restrict design closed, no runtime test or app rebuild warranted.
  STATE-ALIAS-CODEGEN-R825.md records the common PGO warning and all boundaries.
  No product/save changes or game/Simulator/build remains; full goal active.

## 2026-09-09 — R824 exact load-origin and ABI checks

- Corrected global attribution to require numeric address equality with exact
  module symbols; added regression cases and origin/base/opcode conservation.
- Actual ABI probe and ASan/UBSan callback-mapping test confirm mutable mappings
  and pre-journal store-pointer ordering. Blanket caching remains invalid.
- PLAZA-LOAD-ORIGINS-R824.md records local weights, unresolved origins and a
  bounded whole-chunk state-lifetime gate. No new profile, product change or FPS
  claim. No game, profiler, build or booted Simulator at checkpoint.

## 2026-09-09 — R823 broad exact-binary weighted instruction join

- Joined17,459 generated-chunk samples across574symbols to exact module assembly,
  preserving per-PC weights and nearby instructions without full disassembly dump.
- Loads dominate selected weights; mechanical x0/x19 grouping is not state-origin
  proof. Cycle-aware classifier tests pass and counts/weights conserve.
- PLAZA-INSTRUCTION-MAP-R823.md records next provenance/mutation-boundary gate,
  not another local arithmetic or disabled-flag variant. No runtime or app changes.

## 2026-09-09 — R822 actual plaza instruction-weight capture succeeds

- Continued10913/PID55020; existing copied file reached neutral starting plaza,
  confirmed before/after in CUA screenshots. No second game/Simulator or restart.
- CPU Profiler32426 and export19470 exit0.25,462 CPU-thread samples; module
  UUID/path verified against exact binary, per-PC cycle weights retained.
- Native Quit10913exit0, smc_failed0, copied save99d432... unchanged. Nonzero
  interpreter fallback and whole-run audio/frame counters are explicitly scoped.
- MAC-PLAZA-PROFILE-R822.md sets weighted disassembly join as next action. No
  runtime/profiler left and no game-speed or full-goal completion claim.

## 2026-09-09 — R821 module provenance and isolated macOS runtime

- Recomputed helper-source fingerprint3535c2dc0243bc7a over38files and PGO hash
  f39a3ced...; both match selected module manifest. Strict-FP/O2/ThinLTO sources
  shared with Simulator, host/platform differences explicitly retained.
- Created private profile with copied Simulator Wii save99d432..., desktop Config,
  background Pipe input and private FIFO. Normal save/app/module untouched.
- Started sole desktop runtime10913/PID55020; exact module loaded, native window
  resolves after startup. Continue same process, not restart. No plaza/profile yet.
  MAC-PROFILE-PROVENANCE-R821.md contains continuation and evidence boundaries.

## 2026-09-09 — R820 alternative profiler preflight resolves viable path

- Ordinary read-only Mach probe against own child denied task access; zero reads,
  child reaped, no permission changes. Stop this sampler lane under current policy.
- macOS CPU Profiler launches bounded test workload, saves promptly; both exports
  exit0, summary9,083samples with per-address cycle weights. Test child killed by
  profiling time limit (command54), explicitly not clean-exit or game evidence.
- PROFILE-PREFLIGHT-R820.md records next actual macOS attach/provenance gate.
  No test/game/profiler/Simulator remains; installed app/module/save unchanged.

## 2026-09-09 — R819 real plaza capture and bounded profiler failures

- Baseline host/module hashes verified; PID53643 reached existing-save neutral
  plaza with before/after visual evidence. No build or normal selection change.
- Host attach exit21; explicit Simulator CPU/Time Profiler each stalled beyond
  10-second request, watchdog stopped exact children after40seconds, exit1.
  spindump denied live sampling without root. No usable per-PC profile obtained.
- PLAZA-PROFILER-LIMIT-R819.md closes unchanged Instruments retry path and sets
  preflight of narrow ordinary-access sampler before any further game boot.
  Game/Simulator/profilers stopped; save99d432... unchanged. Full goal active.

## 2026-09-09 — R818 exclusive symbol accounting and per-PC evidence gap

- Extended existing sample parser with conserved exclusive symbol buckets:
  3,496 generated-chunk observations across 420 chunks, top ten 844 of CPU6,004.
  Collapsed offsets cannot support arithmetic/state/branch instruction attribution.
- Added per-address cycle weights to existing xctrace summary; focused parser
  tests pass. Verified CPU Profiler template availability, not capture success.
- PLAZA-SYMBOL-COST-R818.md defines one fresh baseline-plaza uncollapsed capture
  as next action. No rebuild or runtime this turn; all product requirements remain.

## 2026-09-09 — R817 quiet-loop cost screen closes promotion lane

- Added separate-TU callback/loop/driver cost screen with exact candidate source,
  timed entry gate, 27 workload/exit combinations and ABBAABBA ordering. Actual
  execution is host macOS with stub services/layout, not Dolphin/Simulator.
- Nontrivial callback savings negligible/variable in initial and same-binary
  repeat; empty-burst gains do not justify a game-wide claim or app rebuild.
  Park specialization. RUN-QUIET-COST-R817.md records ranges and next direction.
- Rerun 5,376-case exact-source differential passes sanitized and optimized.
  No app/module/reference source/save change and no running process left.

## 2026-09-09 — R816 full actual-policy Run object gate

- Added private builder using the cached Simulator host compile entry. Both
  whole translation units compile; unchanged input hashes verified. Candidate
  Run machine code grows 544 bytes, with no outlined template-call overhead.
- Added complete candidate-source identity check to the differential test;
  5,376 cases pass under sanitizers and -O2. This does not execute Dolphin itself.
- RUN-QUIET-OBJECT-R816.md records artifacts and pending runtime cost screen.
  No app link/install/Simulator or performance claim. No process left running.

## 2026-09-09 — R815 configuration audit and private native-burst differential

- Optional trace/sampling/REL/direct/lockstep configuration audited to lifecycle
  initialization; live chunk/hook/interrupt/CPU checks deliberately retained.
- New exact-source extraction test passes 5,376 burst cases in sanitized and
  optimized builds. Actual boundary/transfer/charge regression also passes.
  Runtime services are stubs, not full Dolphin integration or a cost benchmark.
- RUN-QUIET-AUDIT-R815.md defines actual-policy cost gate before any host build.
  No Simulator/runtime/build left running and no normal app/module/save change.

## 2026-09-09 — R814 thread-specific cost map and host source correlation

- R813 sample separated: CPU6004/video6001 observations. Video wait3374,
  vertex pipeline830; CPU Run self698. No cross-thread or inclusive-count
  CPU-share claims. Exact host atos/disassembly maps Run offsets to dispatch
  call, sampling flag, idlePC and cycle/timebase bookkeeping.
- Hook checks already bound static lookup and retain dynamic pending returns.
  No stale-cache or timing/interrupt bypass proposed. Next audit optional flag
  mutability before whole-loop production specialization equivalence/cost gate.
  See PLAZA-COST-MAP-R814.md. No rebuild/runtime/product policy change.

## 2026-09-09 — R813 reverse baseline closes the cross-product experiment

- Matching plaza capture1418 exit0:996events/36.801155s=27.06437events/sec.
  Order25.93A/26.63B/26.01B/27.06A fails repeatable material gain. Park region;
  no default promotion or further guard tuning from isolated benchmarks.
- Separate10s CPU sample2750 exit0 after speed measurement. Retained stacks
  identify broad host/dispatch/generated and vertex-decoding work; no global
  wait-stack percentages treated as CPU shares. Next source-correlated thread
  classification. CROSS-DISPOSITION-R813.md records exact sample/hash.
- Runtime61573 and shutdown75840 exit0; save99d432... unchanged. No runtime or
  build left. Original PRD/SunPad/audio/stability/device goal remains active.

## 2026-09-09 — R812 clean candidate measured in matched plaza

- Candidate captures61536/67399 exit0:26.63122 and26.00536 frame-events/sec.
  Baseline25.92818; initial2.71% difference, repeat approximately0.30% above
  baseline. Not material/repeatable gain. Host background variation retained.
- Same neutral starting plaza in before/after screenshots. No build/profiler.
  Candidate stopped10535 exit0; save99d432... unchanged. Reverse baseline now
  starts in sole Simulator, console61573, cross-baseline-r812-reverse.log.
- Next final reverse order capture then disposition; no expanding/promoting
  this optimization without end-to-end gain. CROSS-CANDIDATE-R812.md records
  artifacts and audio limitations. Full original objective remains active.

## 2026-09-09 — R811 first baseline scene measurement

- Existing baseline reached starting plaza, remained neutral, warmed15seconds.
  Capture83752 exit0:1089 events/42.000638seconds=25.92818frame-events/sec.
  Screenshots confirm unchanged scene. Host snapshots show variable Logitech
  updater load;8system swapins, no swapouts. No host-idle or physicalFPS claim.
- Baseline58553 terminated exit0, save99d432... unchanged. Candidate50305/
  console10535 launched sequentially, same host/settings/save/input file, same
  Simulator. Wrist-strap screen visible; next matched plaza capture. No A/B
  result or promotion. CROSS-BASELINE-R811.md retains paths and limitations.

## 2026-09-09 — R810 counter-free module complete, baseline launched

- Repeatedly polled same53784; linker remained active through8m37s, then exit0.
  Candidate59e9fd166068a10b41b942fe1f32e9f3cef89b621d6ea15bb5684690b3cd418e
  passes platform/signature checks. Normal module and host hashes unchanged.
- Established matched plaza capture protocol; existing capture tests3pass.
  No build left running when one Simulator booted. Save backup99d432... verified.
- Baseline PID49602/console58553 starting; keep this runtime. Initial screenshot
  shows Starting Galaxy, not gameplay. Log cross-baseline-r810.log; neutral
  input-r810.json. No comparative speed result. See CROSS-AB-PROTOCOL-R810.md.

## 2026-09-09 — R809 counter-free candidate build

- R808 measured eligibility changes next action to clean speed comparison.
  Added private --ni --uninstrumented builder mode, retaining exact control/
  object/flags checks and no normal selection changes.
- Exact-source test initially rejected an omitted generated comment in its
  expected boundary. Corrected harness, retained pre-link attempt. All262144
  state/status cases pass; NI0/NI1 each24129fast. No arithmetic change.
- Rerun53784 has passed source/compile/test gates and is linking. Last48934
  active655%CPU. Poll same handle; logs cross-build-r809b.log/.err. No Simulator
  booted or speed claim. CROSS-UNINSTRUMENTED-R809.md records continuation.

## 2026-09-09 — R808 real-scene eligibility and NI regression guard

- R807 link29185 completed and validated; exact-source NI coverage test passes
  262144 cases with24129 fast cases separately in NI0/NI1. Builder now requires
  NI coverage explicitly. Summary regression and Python syntax checks pass.
- One Simulator reached Star Festival plaza, with visible movement/jump.
  Window7753..8019:999424 calls,991598 fast,7623 range,203 ties,0 mode rejection.
  Diagnostic16.60..26.04 FPS still slow; no A/B or audible-output proof.
- Runtime70780 and shutdown35880 finished exit0; save99d432... and normal
  module3acdcd... unchanged. CROSS-SCENE-R808.md retains evidence and exact next
  step: uninstrumented candidate, matched sequential scene comparison.

## 2026-09-09 — R807 separate NI diagnostic and verified control reuse

- R806 mode discovery justified NI-capable diagnostic, not repeating old binary.
  Builder --ni uses newr807directory; checks old control,1329objects, compiler/
  linker flags and response inputs before reuse. All pass, normal files untouched.
- Exact generated source test262144pass,48258fast/256ties. Build29185active at
  private link; last46900parent46899~677%CPU,2m59s. Next poll SAME29185.
- Summarizer accepts unique r804/r807 label and inclusive scene line windows;
  focused mixed-version/bounds/empty/reset/totals tests pass. Synthetic counter
  summary not treated as gameplay. CROSS-NI-DIAGNOSTIC-R807.md records handoff.
- No Simulator or app/save changes; full objective active.

## 2026-09-09 — R806 actual floating-point mode defeats old fast-path guard

-5301finished exit0; control exact, candidate Simulator/signature checks pass.
  Booted one iPad;43849/PID46206 confirms private module. Save backup before run.
- Title→file-select visible after A+B.10.4M counters all mode-rejected;92764
  one-shot debugger read findsFPSCR86004004 NI1/RN0; detached normally.
- Terminated app/shutdown41212exit0,43849exit0; save hash unchanged, no simbooted.
- Bounded-input source guard now allows NI; no host/game mode setting altered.
 25357standalone262144pass;8994fullcontext12000+7680+512pass. NI ordinary~19%
  local benefit, fallback costs remain. Old binary not promoted/reused as new.
- CROSS-LIVE-MODE-R806.md records exact hashes/logs/next distinct diagnostic build.
  Full objective active; no gameplay-level/FPS benefit claimed.

## 2026-09-09 — R805 verified control and instrumented-source regression

- Continued5301 without restart. Control hash exactly equals normal Simulator
  module3acdcddc...1bac0; candidate chunk compiled, candidate link now running.
  Last linker45352(parent45351) active~637%CPU at4m48s.
- Added --diagnostic-source regression;41402exit0 passes262144CPU/FPSR cases,
 24129fast/128ties and64 real diagnostic log records. Counter parser validates;
  this synthetic eligibility ratio is NOT gameplay evidence.
- Rehashed existing Simulator save unchanged99d432d5...ab64f; no Simulator boot,
  app install or normal artifact changes. CROSS-DIAGNOSTIC-R805.md has exact
  source hash/logs and next poll5301 continuation. Full objective remains active.

## 2026-09-09 — R804 private eligibility diagnostic build started

- R803 showed promising accepted-input gains but material rejection costs.
  Verified current Simulator module SHA, one-Simulator inventory empty,32Gi free.
- Added builder with1329-object provenance and unchanged-control equality gate;
  candidate compile/link only follows a matching control. Session5301 running,
  linker44968 observed active~704%CPU at2m29s. Poll same session next.
- Added tested/registered counter-window summarizer rejecting reset/missing/
  inconsistent data. CROSS-DIAGNOSTIC-R804.md records continuation details.
  No candidate ready or gameplay measurement yet; normal artifacts/saves untouched.

## 2026-09-09 — R803 cross-island observation boundaries and rejection cost

- R802 positive local result warranted expanded tests. Added independent-library
  journal setters, callback mapping/register mutations, CPU/RAM alias cases,
  all suffixes and four varied workload groups. Harness include failure86634
  corrected;3269/84678 passed before final rejection-path refinement.
- Rejections now preserve state/status and fall through original generated code.
  Final66614exit0:12000 new context+7680 prior+256 workload comparisons pass.
  Standalone262144pass,24129fast/128ties, explicit false-return preservation.
- Ordinary/small~20% gain persists; large rejection~19% and tie~29% regressions
  remain. CROSS-CONTEXT-R803.md records limits; no speed-fix claim for rewrite.
- Next measured scene eligibility before promotion; no more speculative guard
  tuning. No normal module/app/save/Simulator change; full objective active.

## 2026-09-09 — R802 whole cross-product native vector region

- R801 conversion gate passed. Actual FMA inspection motivated whole-island
  vector lowering in the existing backend, retaining generic/tie fallbacks.
- Standalone262144 state/FPSR cases pass:24129fast,128ties. Registered test.
- Full-context83504first run positive; strict-status final72291exit0 includes
  standalone rerun plus7680 original-routine/suffix/RAM/flags comparisons.
  Final CPU43.608→35.095ns (~19.52%), instructions~865→778. Profile warnings kept.
- CROSS-ISLAND-R802.md records mechanism, evidence and remaining gates. Next
  varied input/callback/alias/rejection tests, then private module/scene gate.
  No current FPS claim or app/module/Simulator/save changes; full objective active.

## 2026-09-09 — R801 exact cache single-to-double conversion

- R800 fixed split-lane transport. Added private AOT_EXACT_FP integer-only
  widening with source capture before alias writes and saved scratch/NZCV.
  No runtime helper/live pointer; store-safe facts remain conservatively false.
- Final46372exit0:100000 complete-state/host-status comparisons,65536 explicit
  exponent/sign/mantissa-edge and rounding/FZ/DN cases. Exercises all three
  single cache representations and dirty-upper preservation.
- GPR34938 and FP1516 regressions each100000pass, stderr empty. ASanUBSan applies
  to driver only; NZCV preservation not independently measured. Recorded
  ARM64-FP-CONVERSION-R801.md. Next complete-region arithmetic, not an FPS claim.
  No app/module/Simulator/save changes; original goal remains active.

## 2026-09-09 — R800 reference FP cache split-lane state binding

- R799 qualified hot FP regions. Inspection found paired128-bit IO assumptions
  incompatible with CPUState's separate fpr/ps1 arrays; offsets alone are unsafe.
- Added private source-hash-pinned IO adaptation and static emitter/driver probe.
  Reference vendor untouched. ReservedQ31 scratch; dirty-upper preservation,
  duplicated values, spills and mutable callback reloads exercised.
- Final10516exit0:100000 complete callback-entry/final-state and seeded FPSR
  comparisons, empty stderr. ASanUBSan driver only; generated assembly uninstrumented.
- ARM64-FPR-LAYOUT-R800.md records scope, source hashes and exact conversion next
  gate. Conversion stubs still abort; no arithmetic or FPS claim. No app/module/
  save/Simulator mutation; original goal active.

## 2026-09-09 — R799 qualify sustained FP register-lifetime regions

- R798 made progress with correctness plus a negative local cost result.
- Added hash-checked FP-island ranker and focused regression, registered in
  repository checks. Tests pass; generated/fp-islands-r799.json ranks all spans.
- Nine-op normalization has203478012 retained site attempts; six-op cross-product
 115676400. Inspected actual helper status/rounding/gating and surrounding loads/
  stores. Entry attempts are not CPU share; no safe fusion inferred from counts.
- FP-ISLANDS-R799.md sets actual FP-cache ABI/paired-lane gate followed by whole
  region equivalence and full-policy cost; prior tiny/attribute/FPRF lanes retained.
  No game/Simulator/build/promotion; full objective active.

## 2026-09-09 — R798 direct RAM loads and negative local cost result

- Added private normal-RAM lwz lowering with original helper fallback and
  canonical PC/GPR visibility. Stores retain original journal helper.
- 61842 mixed fast-load and11069 helper-only each pass152320 matrix+2880 alias
  cases.6989 restore fast-load passes60928+1152. All exit0, empty stderr.
- Separate unsanitized alternating-order benchmark added. Late direct-load
  pairs ~9.7–10.0ns versus C ~7.4–7.6ns. Host warmup substantial; not actual
  module policy or FPS evidence. Helper-only is also slower than C.
- ARM64-DIRECT-LOAD-R798.md records reproducibility, limits and no-promotion
  decision. Goal priority updated to larger observed region/lifetime cost,
  not more tiny fixture tuning. No runtime, app/module, save or Simulator change.

## 2026-09-09 — R797 decoded mixed integer block and store callbacks

- Previous turn progress: real restore block/all suffixes. Revalidated latest
  source and found real mixed sequence80319AB8–ADC; source34534c0a...7b766b.
- Exporter now supports addi/li, plain add, lwz/stw and terminalblr. Cache flushes
  at memory/return, argument registers reserved. Original source supplies oracle
  and exact suffix charge; unsupported opcode/entry remain rejection gates.
- Driver compares fullCPUState and three reset RAM buffers plus callback trace.
  Added writes/journal callbacks that mutate regs/maps/LR/flags/cycles.94353exit0:
 152320mixed cases;67065direct/60862frame restore regressions60928each, emptystderr.
- ARM64-MIXED-BLOCK-R797.md records limits. No FPS/fullbackend/mobile claims.
  Next inline normalRAM lowering plus CPU alias contract, then meaningful block
  work comparison. No runtime/Simulator/app/module/save changes; full goal active.

## 2026-09-09 — R796 actual block decode, state binding and suffix dispatch

- Previous turn progress: offline GPR allocation/callback reload. Verified latest
  status, generated suffix cases and exact DOL/chunk hashes. Added private lwz/blr
  exporter, explicit frame/helper adapter and actual-source differential driver.
- Frame51861exit0 and direct CPUState-GPR binding39727exit0 each30,464 full-state/
  callback-order comparisons. Direct binding reuses reference cache with private
  GPR-offset macro; FP/CR unsupported. No product include path changes.
- Final dispatch adapter69376exit0, empty stderr: exact4/3/2/1 charge and five
  invalid-entry no-mutation tests. ASanUBSan on C++ driver/helpers only. RAM/MEM2,
  mirrors/boundaries/wrap/missing callbacks and callback remaps/LR/flags covered.
- ARM64-RESTORE-BLOCK-R796.md records scope. No benchmark/whole backend/speed
  claim; next mixed arithmetic/memory block and broader state contract before
  module integration. No game/Simulator/app/module/save change; goal remains active.

## 2026-09-09 — R795 offline reference register-cache execution

- Previous turn progress: static emitter transport verified. Rechecked current
  status/dirty tree, inspected actual reference cache/constant-propagation/state.
- Added private host shim plus cache exporter, callback driver and build harness.
  Uses unmodified reference cache; FP-dependent shim methods abort, not fabricated
  conversions. Generated code saves reference allocator's callee-saved registers.
-45271include failure;70607noncopyable PowerPCState/destructor failure corrected
  by compiler-offset aligned byte fixture, no object fabrication.95202passes;
  final44457exit0/empty stderr after pragma typo fix.100000 randomized callback
  observations/reloads and whole byte-layout comparisons pass. Evidence r795d.
- ARM64-GPR-CACHE-R795.md records scope. Actual architectural adapter/PPC block,
  memory slow paths/FP/cycles still outstanding; no speed claim or app promotion.
  Next connect decoded instructions and explicit fields into a complete block.
  No game or booted Simulator, no save/artifact/default change, full goal active.

## 2026-09-09 — R794 reference ARM64 static-link transport

- Previous turn progress: source boundaries and exporter requirements identified.
  Revalidated current status and dirty tree. Added offline reference emitter
  exporter, two helper bindings, C driver and private orchestration probe.
-45621exit0 first attempt; fixed macOS26assembler versus14link warning.95240exit0
  final target-correct version, empty stderr;200000 linked calls passed, each
  library has distinct helper result and loaded function address. Verified BR26
  object relocation and ordinary signatures. IOS/IOSSIMULATOR16 objects pass.
- Separate-process export determinism rerun59375exit0, empty stderr;
  generated/arm64-static-link-r794c.json retains final passing evidence.
  No executable allocation, mobile JIT or game data; temp artifacts self-clean.
- ARM64-STATIC-LINK-R794.md states scope: no PPC lowering, register-cache/state
  adapter or speed result yet. Next complete block/export adapter, not repeating
  this transport proof. Product sources/artifacts/saves unchanged; full goal active.

## 2026-09-09 — R793 mapping lifetime and broader backend boundary

- Previous turn progress: corrected coverage and ranked shared memory work.
  Rechecked current status/git. Selected Ninja include graph resolves actual
  GXRuntime core/cpu.h; audited Run, Sync, Hooks, Memmap, reset and mod ABI.
- Explicit mapping writes occur at Run entry; memory-region indirect pointer
  writes and mutable module/SyncIn callbacks prevent blanket invariance claims.
  Existing acquired-pointer journal behavior retained; no cache mutation.
- Inspected reference ARM64 register cache/block compiler. Added source-hashed
  audit-arm64-aot-boundaries.py;22 pointer,35 helper,13 cache/mutation,26 live-state
  textual sites. NATIVE-EXECUTION-R793.md documents exact boundaries/limitations.
- Next concrete implementation is a private offline complete-block exporter with
  static relocations, state adapter and controlled compile-time assumptions.
  It is not implemented/benchmarked yet. No claim that dumped JIT bytes form an
  AOT module. No product source/build/runtime/save change or FPS improvement.
  Full original goal remains active; avoid repeating prior narrow cache variants.

## 2026-09-09 — R792 rank shared work from corrected coverage

- Previous turn progress: native movie/audio gate measured, gameplay still slow.
  Verified no game/booted Simulator; dirty tree preserved.
- Added rank-covered-instructions.py with source-hash validation. Extended
  coverage probe to arbitrary chunk;805170A0 exposed outlined duplicate labels
  and comment-only entry locations. Fixed function scope and statement lookup.
  Initial output rejected helper wrapper, then provisional gaps; final38353exit0
  and first-chunk confirmation69192exit0. No execution or full module build.
- Retained known attempts1,034,646,642 in804B60A0;1,583,700,587 in805170A0.
  Latter lwz595,113,765/stw512,594,769; three saves/restores each~104.5million.
  First chunk fully mapped; outlined loop excluded in second. Frequency only.
- COVERED-WORK-R792.md preserves R451 profile-loss/scope versus R311 regression
  distinction. Next shared memory mapping writers/callback boundary audit;
  no repeated tiny range/predicate/page-table candidate. No FPS improvement claim.
  Original full PRD active, normal artifacts/saves unchanged, no runtime remains.

## 2026-09-09 — R791 movie/audio measurement completed

- Same29585/PID38079; visible route reached airships and returned to plaza.
  Capture9413exit0, movie-audio-r791.json:7windows36.899723s/2212events59.946Hz.
  Audio8snapshots36.899697s,1771520output/nonzero frames,0new underruns/drops/short.
- Post capture81027exit0:4windows21.500135s/687events31.953Hz;5audio snapshots
 21.500163s,1032192output/563402nonzero,104new underruns. Both output~48kHz.
  Capture metadata preserves host load; zero swapins/out, not controlled A/B.
- Added MOVIE-AUDIO-R791.md with commands/clock-domain distinction/limits.
  Focused summarizer tests pass after final uncertainty wording; R790 full suite
  passed before this small script/test addition, not rerun during measurement.
- Native stop09:19:19.375 failed0/smc_failed0,5591decoder accepted/0fallback.
  Save99d432d5...ab64f unchanged; idle app termination/shutdown99733exit0,
  console29585exit0; verified no GalaxyPad or booted Simulator remains.
  No decoder default promotion/audio perception or graphics-fault acceptance.
- Next material native execution cost from retained instruction coverage and
  exact-policy disassembly. Do not repeat movie/PCM proof or enlarge buffers to
  hide sustained game-speed deficit. All original PRD/SunPad/device goals persist.

## 2026-09-09 — R791 deploy output telemetry and begin phase validation

- Full regression94352 exits0, check-r790.log. Core build65936 and provision/
  private nativeTHP app33555 exit0. Core SHA f319c3ab21ce79da796afeab18734cc69372f105b4e5f9afd418dc19d670e986.
  Prior archive retained generated/candidates/r791-baseline/libGalaxyPadCore.a.
- Signed candidate85ffc10065d375062a0c445d87d0a501d4fc91e236986d6d55d74f0b8333dbcc
  has one shared outputCounters symbol, arm64 IOSSIMULATOR. Normal app e2f9c933
  unchanged. Module3acdcddcd47812c2e2a67b8cec0cf06c17009cf1ad7f2e9dc9abd83158a1bac0
  unchanged. Save99d432d5...ab64f verified before install. Root289a87a unchanged.
- Initial console87941 showed missing-module error: Simulator requires explicit
  DevGameRoot/DevDiscImage/DevModule arguments. No module packaging change needed.
  Terminated idle app and relaunched29585/PID38079 with those paths, DevNativeTHP
  YES, frame logs, AUDIO_OUTPUT_DIAGNOSTICS=1 and THP_TIMING=1. One M5 only.
- Live RemoteIO48000Hz, counter availability1 and nonzero PCM prove telemetry
  integration. DMA underruns remain during some slower scenes; not audio success.
  Added tested summarize-audio-window.py rejecting unavailable/reset/gap windows.
  Counter granularity and separate atomics remain explicit limitations.
- In progress: same runtime/input-r791.json, navigating opening to movie. Retain
  ios-runtime-r791b.log. Next visually bracket native movie and compute output/DMA
  deltas alongside frame cadence; no default decoder promotion/full PRD claim.

## 2026-09-09 — R790 RemoteIO measurements and reproducible patch

- Previous turn progress: hostDMAcounters exposed, output gap located. Implemented
  opt-in outputcounts/header and CoreAudio hook; no callback fileIO/clock/lock or
  playback-policy change. Host lognowincludes availability/requested/actual/nonzero/
  short/peak. ASanUBSan bounds/reset/concurrency and crossTU storage tests pass.
- Core/main actualflags syntax36110exit0; hostafterbootstrap64813exit0. Canonical
 0030SHA496f03aa...448514 reversechecks/headerparitypass. Bootstrap initiallyfailed
  older0026/27peel order + optionalfetchoverlay and plainunified filelists. Restored
  edits on each failure; final46754/repeat38174exit0 with fixes preserved.
- Full regression94352 ACTIVE,generated/check-r790.log. Pollsamehandle before
  build. No installedapp/save/runtimechange. Next private instrumentednativeTHP
  candidate plus targetedmovie/audio capture; fulloriginalgoal stillincomplete.

## 2026-09-09 — R789 mobile audio diagnostics gap

- Previous turn progress, range/type implementation closed. Audited retained
  R695/R696 logs and actual Mixer/RemoteIO/Cubeb sources: mobile only logs first
  callback, while mixer DMA counters exist. Do not carry macOS audio acceptance
  or infer continuous delivery from native movie59.9frameevents.
- Implemented lifecycle-locked host audio snapshot and optional bracketed DMA
  cumulative logs with output_counters_available=0. No callback/queue/decoder
  policy changed. Host95898exit0(Werror), main actual-policy syntax passes.
- Not installed or runtime-proven. Next bounded opt-in RemoteIO output telemetry
  and source-patch/tests, then specifically instrumented movie/audio measurement.
  Saves/app artifact unchanged; original full goal remains incomplete.

## 2026-09-09 — R788 shared memory/type prefix cost gate

- Previous turn progress, general multiply rejected. Private sixinstruction
  prefix shares two directRAM ranges and derives source precision from loads;
  original charge/interior/suffix/callback paths retained, no persistent cache.
-86334exit0:7680fullroutine comparisons, reference43.271–44.051ns vs candidate
 41.659–42.601ns,~3.4%mean improvement with~8%more instructions. Candidateprofile
  mismatch retained; guard-boundary integration proof incomplete. Close variant
  for promotion without more tuning or module build.
- Next inspect retained native movie timing/audio gaps, not another unchanged
  throughput/parity/movie replay. No app/runtime/save changes; gameplay CPU and
  full original PRD performance/stability/product/device scope remain open.

## 2026-09-09 — R787 general checked multiply cost disposition

- Previous turn progress, square guard stress. Added private four-lane checked
  multiply and real-helper oracle:29700exit0 ASan/UBSan,655360cases/106571fast,
  fivealiases, rawbits and hostflags. Registered; no fullsuite rerun.
- Fullchunk cross-checked mode replaces only804B6CCC in hot15instruction routine.
 75110exit0,7680state/RAM/hostflag cases. Reference43.286–43.949ns vs candidate
 42.878–43.180ns;~1.1%local benefit,~8%more instructions. Reject general helper
  for promotion; no attribute/predicate rescue tuning or fullmodule build.
- Keep square result separately scoped; broader typefacts remain unimplemented.
  No runtime/app/save changes; full original PRD remains active.

## 2026-09-09 — R786 checked-square guard stress

- Previous turn progress, bounded hot-normalization gain. AST-extracted exact
  candidate helper tested against actual runtime oracle with ASan/UBSan;
 53008exit0,262144 fullCPU/hostflag cases,43133fast calls. Raw64, widenedraw32,
  exponent sweep, boundaries/NaNs, allRN/NI and pre-existing flags included.
- Test registered, no full-suite rerun. Joined retained coverage to actual
  annotations: non-square804B6CCC19.3M,804B6CA07.1M and square804B6C5C4.3M calls.
  Next general checked-multiply correctness and full-routine cost, not automatic
  extrapolation or all-callsite deployment. No runtime/app/save change; fullgoal open.

## 2026-09-09 — R785 checked binary32 square in hot normalization

- Previous turn progress: corrected target via instruction coverage. Added private
  normalization-square mode, replacing one arithmetic call in full chunk only.
  Integer bit round-trip rejects non-single inputs without host flag side effects;
  original fallback, outputstatus, memory, all17entries and cycles preserved.
-28029exit0 initial cost run had108.9ns reference outlier. Extended inputmemory
  corpus and repeated:15227exit0,8704 comparisons; reference81.810–83.876ns vs
  candidate76.845–77.379ns. About7%routine CPU reduction, instructions slightlyup.
- No gameFPS extrapolation, sanitizer/fullmodule proof or deployment. Next guard
  stress and broader actual executed-site qualification. App/save untouched;
  full original goal remains active.

## 2026-09-09 — R784 retained instruction coverage, corrected target

- Previous turn progress, memory range correctness prerequisite. Added temporary
  one-chunk coverage-object probe against retained frontend instrumentation.
  Final91102exit0; generated/chunk-coverage-r784e.json. Exact source/profile
  hashes and expected timestamp warning recorded; other diagnostics reject.
- LLVM resolved segments show R783loads0, oldtransform9787, normalizationentry
 22608894 executions.1023 labels mapped, one gap null, not zero. This changes
  next action: no cost run of cold load span; inspect genuinely executed region
  and prior experiments before another candidate. Counts not CPU-time shares.
- No module/app/runtime/save changes. Full original objective still incomplete.

## 2026-09-09 — R783 range-sharing across memory instructions

- Previous turn progress, GPR-only scope census. Inspected actual mem_read32 and
  generated lwz bodies. Private four-load range-sharing candidate retains original
  callback/interior paths, excludes CPU alias, wrap and invalid range probes.
- test-ram-load-span.py exits0 ASan/UBSan:120000 fullstate/orderedcallback cases,
 17145 shared-range calls, explicit CPU alias fallback passes. Evidence
  generated/ram-load-span-r783.log. Not full-function cycles or timing proof.
- Initial exact-body scan68974lwz,1853 instructions in4+ same-base spans. No
  dynamic coverage or whole-game speed estimate; do not deploy on static counts.
  No module/runtime/app/save mutation. Full objective remains active.

## 2026-09-09 — R782 native scope and pure-GPR census

- Previous turn progress, rejected small typed span and full regression pass.
  Revalidated source/retained profiles. R762 exclusive leaves1070 generated,
  305Run/chassis,320other of1695; not CPU-time attribution. R764 phase implies
 1783ns/burst savings required for8%total CPU, not measured synchronization cost.
- Added read-only GPR-span census reusing established body recognizer plus focused
  boundary tests.40039exit0:1322chunks,169835 recognized instructions,671 in8+
  spans. Tests pass and registered; no new full-suite claim. Park pure GPR batch
  implementation for insufficient scope, document memory/helper-boundary next
  qualification. No runtime/module/app/save changes. Full original goal open.

## 2026-09-09 — R781 regression after precision experiments

- bash scripts/check-repository.sh, original session30339exit0; retained
  generated/check-r781.log ends Repository safety checks passed. R778 and R779
  precision tests now included in the full successful run. R780 cost fixture is
  separate. Existing compiler warnings remain visible, not suppressed.
- Disk32Gi available. No app launch/rebuild, save mutation or publication.
  Tracked-only final safety scans do not audit every untracked release artifact.
  Next broader native-cost/coverage qualification; narrow batch promotion closed.
  Full PRD performance, stability, audio, SunPad UI and device gates remain open.

## 2026-09-09 — R780 guarded precision propagation in complete chunk

- Continued original session23621 to exit0, without restart. Facts begin unknown;
  FP-on/VE-off/NI-off entry guard, four qualified binary32 paired multiplies,
  nonfinite original fallback and unchanged original interior-entry labels.
- 22,528 CPU/RAM/hostflag differential cases pass under actual full-chunk build
  flags. Eight alternating timing windows: reference190.836–191.501ns versus
  candidate185.167–187.579ns; about2.8% mean improvement, about4.6% more instructions.
  Missing candidate PGO warning retained; not sanitized integration or wholegame.
- Record insufficient benefit for promotion. No module/app/save changes and no
  further batch attributes/representation tuning. Full PRD remains active; run
  regression including new precision tests, then qualify broader native-cost
  coverage before another emitter change or expensive module build.

## 2026-09-09 — R779 precision-qualified paired multiplication oracle

- Previous turn progress: scalar type-fact exception counterexample. Implemented
  test-only typed paired multiply for known widened-binary32 finite sources with
  NIclear. Original fallback for NI/nonfinite, original destination/FPRF writes;
  no unsafe assumption that scalar opcode always established source precision.
-40165exit0 under ASan/UBSan:640000 fullCPU/hostflag comparisons,292605 actual
  vector calls; RN/NI, randomized status, fouraliases and special/raw32bits.
  Host flush disabled during input preparation to preserve subnormal fixtures.
  Widening can quiet sNaNs, so not exhaustive raw64 signaling-NaN coverage.
- Test registered; full suite not rerun. No microbenchmark or projected speed
  improvement, no module/app/core changes. FP-TYPE-FACTS records integration
  prerequisites. Next guarded facts in actual chunk and full-context cost before
  any broader build. Full performance/stability/SunPad/device PRD remains active.

## 2026-09-09 — R778 audit reference precision facts against C helper semantics

- Previous turn progress: scalar batch disproved copy-cost attribution. Read
  actual JitArm64 paired register-width/round-C decisions and PPCAnalyst single
  tracking; reverified retained R425 configuration FPRF/NaNsTrue, exceptionsFalse.
  Historical speed gap remains with those accuracy options; no new timing run.
- Added test-fp-single-facts.py with actual C scalar helpers.65217exit0 under
  ASan/UBSan:12invalid/VE/alias cases plus finite VE-enabled success. Invalid
  scalar operations can retain a demonstrably non-single destination, so naïve
  opcode-only tags can wrongly skip later operand rounding. Registered suite;
  full suite not rerun after this addition.
- FP-TYPE-FACTS.md records success-conditioned/guarded facts, unknown external
  entry and observer invalidation requirements. No proposal to disable accuracy
  defaults or deploy JIT. No product source/build/runtime/save changes. This is
  a correctness prerequisite for different typed lowering, not a speed gain or
  permission to reopen rejected state-copy/scalar batching. Full goal active.

## 2026-09-09 — R777 explicit scalar lanes test prior cost attribution

- Previous turn progress: local-array FP batch regressed. Added --scalar-span
  to same actual-policy whole-chunk harness. Expands fixed-register wrappers
  and write helpers, preserving single evaluation of value parameters;23 named
  lanes/four-byte FPSCR replace arrays. Helper closure limited to status fields.
- Initial source guard caught parameter ps1 incorrectly substituting member
  name cpu->ps1; excluded member identifiers, reran rather than weakening guard.
  Final51437exit0:22528 fullCPU/RAM/hostflags cases including unavailable FPU
  pass. Baseline2745–2746 vs candidate~2949instructions/call, CPU190.2–199.4ns
  versus206.0–206.8ns. generated/scalar-fp-span-r777c.log. Candidate PGO absent
  warning retained; no sanitizer/installed runtime claim.
- Nearly unchanged cost versus array version refutes the working array-copy
  explanation. Documented correction; close both batching variants, no further
  attribute/representation rescue. Prior claim that register-lifetime analysis
  was necessarily the next solution was not demonstrated. Full PRD remains
  active; no game/app/module/core selection, vendor or save change.

## 2026-09-09 — R776 pure FP span with explicit local state

- Previous turn progress: full registered regression refresh. Read old R428
  paired-vector result (isolated gain shrank to~4% kernel cost), actual helper
  fields and R769 whole-chunk failure before selecting a different state scope.
- Added --local-span to existing private probe: guarded19-op pure region,
  transitive source helper closure, local lanes/status, ten dirty destinations,
  original entry labels and unavailable-FPU fallthrough. No runtime source edit.
- First4144exit0 cost regressed. Added explicit MSR.FP-off pattern and reran:
  25795exit0,22528 wholeCPU/RAM/hostflag comparisons pass; finalblr works without
  FPU, other entries raise. Release full-chunk verification, no sanitizer claim.
- Baseline2743–2750 vs candidate2948–2951instructions/transform,~7% more work.
  ABBA timings drift but candidate slower in adjacent pairs. Candidate reports
  missing PGO; do not call this installed/matched-retrained-PGO evidence. Logs
  generated/local-fp-span-r776{,b}.log. Copy-in/out batching is not qualified for
  deployment or a full rebuild. No micro-attribute rescue. Future locality work
  needs actual live-value/dataflow lowering, not assumed free CPU-array copies.
- No game, app/module/core build selection, vendor mutation or save change.
  Full original performance/stability/SunPad/mobile/device objective remains active.

## 2026-09-09 — R775 full registered regression refresh after live run

- Previous turn progress: original movie-to-gameplay return and clean shutdown.
  Confirmed no GalaxyPad process. Ran complete scripts/check-repository.sh;
  session63723exit0, generated/check-r775.log final Repository safety checks passed.
- Registered FIFO33,153+2cases and gather32,768sequences pass in suite, along
  with nativeTHP config and prior lifecycle/input/render/native diagnostics.
  This does not rerun the separate R768/R769 experimental transform benchmark
  or prove visible gameplay, device audio or speed. Tracked-only final material/
  credential/path checks are not an audit of all untracked files or release proof.
- Fixed PRD companion link from missing docs/GOAL-LOOP.md to actual
  docs/GALAXYPAD-GOAL-LOOP.md, with no requirements change. No app/module/save
  mutation, simulator launch, source staging or publication. Full objective active.

## 2026-09-09 — R774 bound and close current live reproduction

- Previous turn progress: same-session town-to-movie transition. Revalidated
 28463/PID11247 at11m44s, no XF assertion; two bounded30s observations showed
  cutscene progression then return to invaded plaza. Directional input afterward
  visibly moved Mario to Toad dialogue, life3. Host automation only, no new
  touch/audio/cadence acceptance. Original decoder intentionally remained active.
- Read-only devicectl list devices5950exit0 reports no physical devices. Simulator
  evidence cannot settle actual iPad/iPhone performance; no device build attempted.
- Stopped via native menu and confirmation, runtime exit failed0 at06:32:55.762
  local, shutdown smc_failed0. Counters are whole-run including startup/movie/menu,
  not phase timing: native6089331043 fallback96549057 cycles115326154737.
  About16minutes host lifetime. No cmd2/source assertion observed.
- Post-stop save SHA99d432d517b92b19da0c403819b91288b10c7063c3ce61f5000347ef823ab64f
  unchanged. Terminated idle app, console28463exit0; no active game/launch handle.
  Retain log ios-runtime-r772.log. One M5 remains booted, no extra Simulator.
- This is a bounded unsuccessful crash reproduction, not proof the intermittent
  fault is fixed or a performance gain. Stop repeating this unchanged opening
  path; retain failure-origin diagnostics in future substantive builds. Full
  original PRD/performance/SunPad/device requirements remain unfinished.

## 2026-09-09 — R773 continue same runtime through town into first movie

- Previous turn progress: live plaza movement; session28463/PID11247 freshly
  polled and process verified, exactly one M5 still booted. No reset/relaunch.
- Leased directional inputs advanced Mario from overlook through town square;
  visible camera/position changes and life3/starbits0 retained. Entering square
  triggered airship movie. Subsequent30second observation shows Peach and
  burning town rather than initial ships. No new cutscene-speed/audio claim.
- At9m35s live process, no cmd2/XF/source assertion/runtime-exit in log. Optional
  Wii dynamic-banner HTTP404 appears; no demonstrated connection to graphics
  fault. Input scripts all terminal and final JSON contains only expired lease.
- Keep same live console28463/log ios-runtime-r772.log, original decoderNO-native
  and clocks/censusOFF. Next verify actual return to invaded plaza and continue
  bounded origin-qualified reproduction. No product edits or stability acceptance;
  no observed crash does not prove the earlier fault fixed. Full goal active.

## 2026-09-09 — R772 resume visible runtime for origin-qualified failure capture

- Previous turn progress: gather-width/burst screen. Verified exactly one M5
  booted, no existing GalaxyPad process,32Gi available. Installed appe2f9c933
  and save99d432d5 hashes match prior evidence. No build/install required.
- Launched28463/PID11247 with original decoder and run-cost/fallback censusOFF,
  own input-r772.json. Log ios-runtime-r772.log. Source-origin diagnostic exists
  in this retained R764 build; do not infer enabled nativeTHP from arguments.
- Visually navigated title/file1/PlayThisFile and opening cards to actual plaza.
  Leased A pulses and movement advanced Mario uphill to waterside overlook;
  screenshots show life3/starbits0 and changed position/scene. This is host
  automation, not new UIKit/control acceptance. Final short diagonal movement
  lease follows; input script clears automatically. No checkpoint invoked.
- At4m40s PID11247 remains live; no XF assertion in log. Missing optional VFF
  logged, not a malformed-command failure. No timing/profiler/build concurrent.
  This is ongoing reproduction, not a stability/performance pass. Preserve
  session28463; next revalidate it and visible state, then continue rather than
  repeat startup. Full original PRD remains open.

## 2026-09-09 — R771 check gather producer byte-width and burst path

- Previous turn progress: isolated FIFO compaction preserved captured boundary
  shape. Inspected actual HookExternalWrite gather-page switch and GPFifo
  FastWrite/UpdateGatherPipe. AOT8byte writes use eight Write8 calls, versus
  direct Write64 in the other route; tested rather than assuming byte loss.
- Added test-gather-write-widths.py using extracted actual switch and methods,
  memory/CP/state stubs and independent big-endian byte oracle.32,768 sequences
  of40writes cover all32residual counts, widths1/2/4/8 and256payload patterns.
  Byte stream, spill, callback count, alternating FIFO wrap addresses and guards
  all agree.78794exit0 under ASan/UBSan; registered repository checks.
- No cause demonstrated. Tests compare completed-write boundaries, not CPU/GPU
  concurrent observation or real MMU/CP/display-list effects. Do not replace
  eight byte writes or promote a write-path change from this result alone.
- Stop further isolated append/width probes. R742 lacks source origin, so next
  useful evidence is recurrence with existing origin-instrumented app. Only then
  choose FIFO versus display-list tracing. Last-N CPU writes may be ahead of
  GPU and cannot be asserted causal without FIFO-address/sequence correlation.
  No app/vendor/runtime edits, launch or save change. Full original PRD active.

## 2026-09-09 — R770 test actual FIFO residual-buffer compaction

- Prior turn progress: actual-policy FP inlining rejected. Re-read R742 failure
  bytes, actual OpcodeDecoding and FifoManager paths, existing partial-command
  and origin tests. Later retained short-run logs offer no fresh XF origin.
  R742 offset0/available34 is insufficient to name main FIFO versus display list.
- Added source-derived test-fifo-compaction.py, compiling unchanged actual
  ReadDataFromFifo method with bounded fake256byte backing and CopyFromEmu stub.
  Exhaustively33,153 consumed/pending combinations verify preserved bytes,
  append count, pointer position, capacity rejection and32byte boundary guards.
  Two residual10 00 +32byte BP-burst cases cover wrap and no-wrap. ASan/UBSan
  exits0, all pass. Registered repository suite; full suite not run this turn.
- This rejects the specific isolated compaction/append-byte-loss hypothesis,
  not producer/MMIO/DMA/concurrency errors or deterministic mode. Real
  MemoryManager::CopyFromEmu validates GetPointerForRange then memcpy; invalid
  range returns after PanicAlert, so logs/range evidence matter upstream.
- Next trace gather-pipe burst production/history if needed; existing failure
  window cannot recover already-consumed commands. No skipped bytes, masked
  assert, product runtime edits or Simulator launch. Full goal remains active.

## 2026-09-09 — R769 validate inlining under real whole-chunk compile policy

- Prior turn progress: strict standalone transform screening. Rechecked source,
  actual graph flags and32Gi available. Added --full-chunk mode to existing
  probe: complete original entry/return flow, two independent dylibs, actual
  ThinLTO/PGO/NDEBUG/strict flags, fresh CPU units, assertion-enabled driver.
- First64337 failed missing private FPRes declarations before clones. Corrected
  header order;70051exit0.22,528 fullCPU/RAM/hostflags comparisons pass. Warmed
  ABBA x2 baseline191.197–193.905ns/2744–2749instructions, candidate
  194.011–197.010ns/2708–2711instructions. ~1.4% instruction reduction and slight
  CPU regression reject this implementation. Earlier standalone16% not portable
  to actual build context. No full module link or game-speed claim.
- Retained warning evidence: integer unit unprofiled, one stale CPU profile
  function, existing comment warning. Same-policy single-chunk libraries are
  not exact installed full module. See TRANSFORM-INLINE-EXPERIMENT R769.
- No app/core/vendor/generated product source changed, no Simulator/game
  launched, no saves touched. Temporary build products auto-removed. Close
  blanket inlining without further attribute tuning; original PRD stays active.

## 2026-09-09 — R768 complete emitted transform strict-inlining screen

- Previous turn progress: additional sampled decoder fidelity. Returned to
  review's gameplay-native overhead lane; inspected prior finite, value-ABI,
  direct-call and FPRF results before selecting full44-instruction fixture.
- Added tests/probe-transform-inline.py: original generated body, all44entries,
  exact float clones with mandatory inline, real RAM/exception helpers, no FP
  policy/ABI change. First link lacked actual estimate-table/exception units;
  included them, without introducing stubs. Existing cpu.c comment warning only.
- Expanded initial352cases to22528 special/raw/status/RN/NI/interior-entry full
  CPU/memory/host-flag comparisons, pass both sanitizer and release. Warmed
  final2788exit0 ABBA x2:~16% fewer driver-inclusive instructions; reference
  170.750–178.007ns versus145.364–153.593ns. Static1570/5519 instructions and
  82/51call sites expose code-size cost. generated/transform-inline-r768d.log.
- No installed-code or game-speed claim: standalone reference differs from
  actual ThinLTO/PGO module. Review's2x instruction target not reached. Next
  bounded actual-build comparison, not full module/blanket inlining deployment.
  RAM callback/fault paths and wider kernels not proven. No app/core/vendor
  edits, runtime launch or save changes; full original goal remains active.

## 2026-09-09 — R767 bounded native-cutscene acceptance review

- Reviewed actual app gate and decoder transform, retained R664/R674/R680/R696
  evidence, and the external review. Normal app OFF/private app ON is intentional;
  request arguments cannot enable code omitted at compile time. No app rebuild.
- Compiled current original frame harness and native sparse/floor prototype
  against pinned FFmpeg8.0.1. Offline batch36909exit0 covers PrologueA frames
  1/500/2000/4000/5000/5590. Different bytes respectively0/0/1/0/2/1, maximum
  magnitude1; all U planes exact.2,119,680 compared bytes total. Original module
  returns0 with353280 DMA bytes, exact-once coverage and32-byte guards each.
  Evidence generated/thp-{original,native,compare}-r767-<frame>.*; raw media
  stays private in ignored generated/. Not all-frame or whole OS-state proof.
- No tolerance widened, decoder semantics changed, app installed or Simulator
  launched. Existing single M5 remains booted. Audio/duration/device gates open.
- This closes the bounded coverage check, not the product gate. Honor the
  review's warning against repeatedly revisiting movies: native feasibility is
  established. Next substantive lane remains gameplay-native execution, with
  source/code-shape evidence of a material effect before a module build; retain
  negative direct-call and FP-interface results rather than retrying them.

## 2026-09-09 — R766 narrower FP interface and native-decoder policy check

- Previous turn progress: deferred-TB arithmetic screened without promotion.
  Inspected actual chunk/helper calls, prior LLVM/chunk/inline decisions. Tested
  value-argument paired-FP interface rather than claiming helper-internal
  inlining proves absence of CPU-wide call boundaries.
- Prototype extracts exact add/sub/mul and status-only dependencies, rejecting
  new CPU fields/unknown CPU-pointer callees. Initial full scratch CPUState was
  replaced by4-byte FPStatus.192000 full CPU/host-flag cases pass each UBSan and
  release, all RN/NI, special/raw inputs, aliases and varied FPSCR.
- Initial sanitized/unmatched-inlining chain timings were misleading and NOT
  accepted. Final matched-internal-inlining34044exit0: original18.897/18.752ns,
  candidate19.442/19.531ns. Caller20/25 static instructions; add/sub207/199 and
  mul213/207. Reduced helper size fails net chain cost. Park this implementation;
  no module build or more small attribute variants. See VALUE-FP-ABI-EXPERIMENT.
- Actual app CMake caches: normal ios-simulator-app OFF, separate private
  ios-simulator-thp-app ON. NativeTHP is an APP setting, not core CMake policy.
  R680 already records native movie completion, with audio/device gates open.
  Added explicit host warning when YES is requested without compiled support;
  preprocessor0/1 tests pass/registered. No default enablement or decoder change.
  Next audit native candidate acceptance/build selection, keeping full original
  PRD and native execution performance requirements. No app installed/run or
  FPS benefit claimed this turn.

- Compile-only host check initially exposed stale test setup: missing generated
  identity/GXRuntime headers and ARM64 definitions, then vendor unused-parameter
  warnings. Matched actual CMake include/architecture configuration and marked
  vendor headers system-only, keeping host -Werror. Final81301exit0, empty
  generated/check-ios-host-r766.log. No app link/install/runtime proof implied.

## 2026-09-09 — R765 screen deferred TB without changing runtime semantics

- Previous turn progress: live CPU-time split parks vector acceleration.
  Audited per-dispatch TB update and actual module source paths. Simulator uses
  GXRuntime cpu.c with ThinLTO/PGO; DolRecomp's similarly named implementation
  is not the installed C runtime. Generated mftb six sites/two chunks, not
  dynamic frequency. SPR read/write helpers, SyncIn/Out, lockstep full snapshot,
  trace and LLVM direct-state reads prevent blind deferral/whitelisting.
- Implemented isolated pending-cycle accounting, preserving baseline uint64
  wrap, TB rollover, low/high writes and remainder. Tests62,208 boundary triples
  and1million mixed events pass ASan/UBSan; registered repository check. Not
  guest execution or complete observer equivalence proof.
- Benchmark52546exit0, deferred-timebase-r765.txt: read every8 charges eager
  2.76692/2.73214ns versus0.872319/0.877637ns; every64 eager2.70321/2.72582ns
  versus0.910225/0.909156ns. Every-charge gain fails repeatability. All checksums
  match. Register-local arithmetic only, no runtime aliasing/observer overhead.
- Decision: retain tested arithmetic, do not launch a full module rebuild or
  integrate a standalone TB tweak without a material whole-game benefit case.
  DEFERRED-TIMEBASE-EXPERIMENT.md records observer boundaries and limits. Next
  broader native state-materialization/shared execution work. No app/core edits,
  live run, FPS gain or original-PRD acceptance claimed this turn.

## 2026-09-09 — R764 measure native/non-native CPU-time split in live plaza

- Previous turn progress: sparse probe built/tested. Provision/app28430exit0;
  mergedcoreceda19099371d68ac82902fc77dabd7324d84b74cd3e2975a66fc0630781ed31,
  signedappe2f9c933f25647e87b0f23aa15a2ed06656e22894d6f8960137cf0c88b3031c8.
  Install89558exit0; new data container95D7A7EA-B26A-470F-A1B1-5D589012C758.
  Save99d432d5 unchanged across install. No other Simulator/game;32Gi available.
- Runtime53350/PID5860, RUN_COST1, fresh run-cost-start-r764.marker, histogram0,
  nativeTHP explicitlyNO matching compiledOFF. Visually navigated title/file1/
  story to same plaza/life3/starbits0. Created marker only there; capture-start
  pc804ab358 at approximately10:26:24UTC. No build/heavy profiler during capture.
  Same animated/correct scene afterward; native menu then clean Stop.
- First Run ends at menu pause05:27:51: CPU80601706708ns; native3615590 spans,
  selected/completed14145, sum319112859ns; non-native888108 spans,
  selected/completed3426, sum6551621ns. No clock errors. Stop confirmation resumes
  a second Run with668930958ns; initially strict parser correctly rejected two
  reports. Added explicit --run selection with tests, never silently merged.
- First report run-cost-r764.json: native raw101.3538% ±4.1402pp nominal;
  non-native2.08087% ±0.08724pp nominal (ratio estimate2.10709%). Raw native
  above100% shows sampling/clock overhead; no cap/renormalization or claimed
  exact shares. Host-only empty-pair probe10000reads mean389.457ns,min250,
  max12834,zero0. This is NOT Simulator CPU-thread calibration and was not
  subtracted. Enough coarse separation to park vectors, not fine-cost acceptance.
- Native Stop05:28:13 failed0/smc_failed0; idle app terminated, runtime53350exit0.
  Save99d432d517b92b19da0c403819b91288b10c7063c3ce61f5000347ef823ab64f unchanged.
  No game left, sole M5 booted. Parser/invalid-report tests pass and registered.
  Goal loop now returns to material native state/timing/shared-FP work. No FPS
  gain, gameplay-completion, audio, touch or physical-device acceptance claim.

## 2026-09-09 — R763 implement and compile sparse Run CPU-time attribution

- Previous turn progress: fresh current-build wall sample/offset evidence.
  Inspected complete native/non-native branches; chose inclusive branch CPU
  measurement instead of another unchanged sample or speculative vector rewrite.
- Added apple/experiments/run-cost/sampler.h, identical vendor RunCost.h,
  two scope boundaries in Run, per-Run report on CPU thread, mandatory opt-in
  GALAXYPAD_RUN_COST=1 plus fresh GALAXYPAD_RUN_COST_START_FILE marker. One-shot
  activation at1024-slice poll; no caller marker deletion or per-step timing.
  Native includes SyncIn/Out and exception checks. Other lane explicitly includes
  host-call routing and any fallback, not mislabeled pure interpreter time.
- Sparse1/256 pseudo-random spans retain totals/selected/completed CPU ns and
  sum-of-squares/max. Failed clock invalidates evidence, no wall fallback.
  Sanitizer tests1million spans, null opt-out, early continue, invalid clock;
  scope wiring and temporary0029 peel/0027 reverse-check/reapply byte equality
  pass. Prior fallback wiring/trigger tests and bootstrap shell syntax pass.
- Canonical0029 SHA8db2920cc54c923ff07bcda7b878308702db1e734023ea77e159fa6b72f5185c.
  Bootstrap hash/early peel/trap restore/final reapply registered. Baseline
  generated/run-cost-r763-baseline/StaticRecompCore_Run.cpp retained.
-32Gi available; incremental Simulator core build6078exit0, ios-core-r763.log.
  Only Run object/core archive needed; app unchanged, no game launched. Next
  package/install and bounded same-plaza capture, then sample uncertainty and
  clock-overhead qualification. No CPU split/FPS benefit/PRD completion claim.

## 2026-09-09 — R762 fresh plaza screening and exact Run offset inspection

- Prior turn progress: dispatch-only vector candidate rejected on measured
  isolated cost. Current checks: no game, sole M5 DE8E956F booted,32Gi available.
  Verified installed/app executable0078ae5d and save99d432d5 before launch.
- Runtime1197/PID4152 uses existing module, histogram0 and nativeTHP NO (compiled
  OFF). Navigated title/file1/story to visually verified Star Festival plaza,
  life3/starbits0. Input leases released.20s `sample 4152 20 10` session30053
  exits0; plaza still correct/animated after capture. No heavy xctrace attach.
- Added summarize-fallback-sample.py and synthetic tree-conservation tests;
  computes self samples and non-double-counted interpreter ancestry. Registered
  tests, pass. R705 reanalysis428total/2interpreter/5shared/49wait; freshR762
  1695total/9interpreter/4shared/96wait. Run236self/chassis69self. These are WALL
  stack samples, not CPU percentages; incomplete/tail-call unwinds and shared
  helper attribution prevent an exhaustive fallback bound.
- Disassembled exact Run, base10023dc58. Two shown collapsed self offsets1504
  and1300 are AOT-side pending-idle-PC load and dispatch function-pointer load.
  This does NOT attribute all236 samples: further offsets were collapsed.
  The initial Mach-O symbol option unexpectedly dumped broad text; stopped own
  recorder session7962/exit130 and regenerated only explicit address range,
  replacing oversized temporary output with34822-byte static-run-r762.asm.
- Clean native Stop05:10:31 failed0/smc_failed0; idle app terminated, runtime1197
  exit0. Save99d432d517b92b19da0c403819b91288b10c7063c3ce61f5000347ef823ab64f
  unchanged. No game/profiler remains, M5 alone booted. No performance improvement,
  stability soak, audio or touch acceptance claim. Fresh evidence deprioritizes
  vector execution; next split Run AOT-state/timing cost from fallback chassis
  before choosing a material change. Full original PRD remains unfinished.

## 2026-09-09 — R761 reject dispatch-only vector acceleration on cost

- Previous turn made progress: recognition code and exact-DOL tests. Added
  bounded dispatch.h selecting pre-resolved EXISTING interpreter operations.
  No executable memory, guest-byte cache, runtime integration or fetch bypass.
- Test compiles actual Interpreter_Tables.cpp and actual UGeckoInstruction
  definition with operation-body spies. All121 entries choose exactly the
  original operation and operand;3872 mutations reject. ASan/UBSan passes.
  This proves dispatch identity, not guest instruction semantics or chassis.
- Ran `python3 tests/test-vector-dispatch.py --benchmark`, optimized -O3,
  same121-entry sequence, 200000 repeats, ABBA. Baseline4.35133ns, candidate
  7.07893ns, candidate6.72917ns, baseline4.37446ns per dispatch. Every checksum
  47694417668000000. These exclude real operation bodies, fetch, HLE, metadata,
  exceptions and chassis, and are not weighted gameplay or game speed evidence.
- Decision: no product integration/build for this losing dispatch-only design.
  Do not spend more turns tuning its small lookup. Next quantify whole fallback
  CPU cost;99%fallback steps cannot justify a large execution rewrite by itself.
  Updated active loop; full PRD, stability, THP, SunPad/device gates unchanged.

## 2026-09-09 — R760 implement fixed vector recognition

- Added isolated constexpr instruction plans for the four verified vector
  regions. Every interior entry is supported only at its exact address/word;
  no high-address aliasing, cached RAM assumptions, fetching or guest mutation.
  Both generic rfi exits and syscall rfi are identified for later handoff.
- New test compares all121 positions against the SHA-pinned actual DOL and
  exception-number patches, checks every address below0x1000, rejects3872
  single-bit mutations plus zero fetch/high aliases, and checks later valid
  recognition after refusal. ASan/UBSan, warnings-as-errors pass after fixing
  C++17 constexpr initialization. Registered in check-repository.sh.
- This tests recognition, NOT actual cache behavior or execution equivalence.
  Product interpreter and app unchanged; no build/run/performance claim.
  Next integrate an opt-in execution experiment retaining SingleStepInner's
  authoritative fetch and exception chassis, then differential semantics/cost
  before a Simulator build. Full PRD and R759 stop gates remain unchanged.

## 2026-09-09 — R759 bound vector-execution design against actual contracts

- Prior turn progress: runtime vector RAM verified. Audited actual interpreter
  HLE/fetch/lookup/exception/PC/performance-monitor path and I-cache fetch.
  Identified existing nonexec CachedInterpreter callback-data design as useful
  reference, not a mobile machine-code JIT or drop-in fallback (owns Run loop,
  private block execution, missing static-core cache invalidation routing).
- Wrote VECTOR-EXECUTION-EXPERIMENT.md: four static plans, authoritative fetched
  words/operations, preserve exception/privilege/cache/HLE/debug/budget/PC/cycles,
  immediate rfi handoff. No physical-byte immutable assumption or global backend.
- Tests and material net-benefit gate defined; <8%unrepeatable closes this
  specific design. No implementation or performance claim. Next fixed-plan
  recognition plus actual-interpreter differential execution, not another census.

## 2026-09-09 — R758 installed vector bytes verified in live plaza

- Previous turn progress: snapshot/comparator compiled. Provision/app34188
  exit0, signedapp0078ae5d48ae9c4216c9db3cd17b439e854e0116a04f87e05f78c5a686730d35,
  core17f82b9a28c8862b3dc3f24506dc3bad3f1b2c2e9e3fe2431f1ee50a76571c05.
  Explicit nativeTHP NO with compiledOFF matches R755; not movie performance.
- Runtime72830/PID2283, sole M5. Title/file1/story input visibly reached plaza
  life3/starbits0; only then created fresh fallback-start-r758.marker. Snapshot
  comparator all_match=true for500/800/900152bytes, c0028bytes. No differences
  beyond allowed exception numbers. Artifactvector-comparison-r758.json.
- Native Menu Stop completed failed0; summarized censusfallback-pcs-r758.json,
  then terminated idleapp/session72830exit0. Save99d432d5 unchanged in new
  data5CAD340F-C1B9-4BA0-B3AC-0DB79BA37770. No second simulator or save edits.
- This establishes runtime RAM template identity, not I-cache fetch identity,
  later immutability, exclusive cost, or correctness of native replacement.
  Next implement bounded guarded execution experiment with explicit HID0/
  MMU/exception/cycle contracts and measured stop condition. No more address
  hunting needed for these four regions; no FPS or full stability claim.

## 2026-09-09 — R757 bounded physical vector snapshot and comparator

- Prior turn progress: exact DOL templates and vector role/path counts. Added
  CPU-thread snapshot at one-shot marker activation,256bytes×4vectors only.
  Null/output/undersized RAM guards, bounded hex lines, no per-step work.
- Histogram sanitizer validates every output byte and guards; actual init/poll
  test updated with empty memory stub; pass. New comparator pins exact DOL,
  checks whole syscall28/generic152 bytes allowing only exception-number
  immediate4/7/8 atoffset68; differences reported. Synthetic expected/mismatch/
  missing/duplicate cases pass; suite registered, live bytes still not captured.
- Canonical0027 SHA0d46c23073de589bf9fcb8e28c59e5dd3634301feab58197d6b8f7122f640cd4,
  bootstrap updated; reverse/syntax/diff checks pass. Core68814exit0, log
  ios-core-r757.log. No app package/install or source execution optimization.
- Physical RAM isn't instruction-cache contents or proof of later immutability.
  Next package and trigger at verified plaza; compare installed bytes before
  defining a semantics-preserving/cost-qualified fast path. No FPS gain claim.

## 2026-09-09 — R756 identify vector semantics from source and exact DOL

- Prior turn progress: zero-loss gameplay census. PowerPC_Exceptions routes
  syscall→c00,external→500,FPU-unavailable→800,decrementer→900. Petari SDK
  is semantic reference only; do not reuse RMGK01 addresses for RMGE01.
- Exact hash-pinned DOL scan uniquely locates seven-word synchronization
  syscall template804aab88 and38word generic template804a1ca0. Offline script
  audit-exception-templates.py validates observed offset sets/uniform counts:
  c00sevenwords438498entries; generic34executedwords at50031034,80020433,
 90042720. Outputgenerated/exception-templates-r756.json. Generic installer
  patches exception number and potentially debugger jump; runtime bytes needed.
- Current generated syscall code uses fallback for mfhid0/mthid0/mthid0;
  blindly aliasing its entry isn't a full native solution and may cost more
  sync/dispatch. Keep HID0/cache/MMU/MSR/rfi/cycle effects and every external
  entry. No runtime bytes, exclusive timing or improvement proven this turn.
- Next collect installed vector bytes at trigger on CPU thread, compare exact
  patched template; then evaluate bounded semantics-preserving execution cost.
  No app rebuild, simulator action, source behavior change or FPS claim.

## 2026-09-09 — R755 phase-separated gameplay fallback evidence

- Previous turn progress: trigger compiled. Provision/app44157exit0, signed
  app7adf2a6e2dd63c49306f5bd355963835fa96b52111a163b6aa33b47a42cb7c88,
  core61500bf19d53054861c1856d54c0f1397cf13f8ae0303da5370cf2759f2cfaec.
- Install/launch71606 PID451, same single M5; new data100F1285-EC89-46DE-B798-
 3BC0A9DD8ABB. Save99d432d5 unchanged before/after. Opt-in inputfile and absent
  marker. CUA Rotate fixed visible landscape; scripted A+B/file pointer/story
  A inputs visibly progressed to Star Festival plaza life3/starbits0.
- Only after visible plaza created fallback-start-r755.marker. Log confirms
  capture-start pc804ab358 native1483297877. Plaza visible again after capture;
  native Menu Stop at04:36:32 failed0, then idle app terminate/71606exit0.
  No XF assertion in short run; not soak or full stability proof.
- Capture6326386events/187keys,0drops.6271844low-memory (99.1379%),54431DOL
  hook and111outside. Blocks c00=3069486,900=1452480,500=1055156,800=694722.
  Filesfallback-pcs-r755.json/fallback-image-ranges-r755.json and runtime log.
  No matched VI timing or interpreter CPU share. High-address startup pattern
  not representative here. Next exact vector bytes/semantics and measured cost.
- Discovered cached GALAXYPAD_ENABLE_NATIVE_THP=OFF despite YES launch arg;
  do not claim native decoder active. Treat config explicitly on future builds,
  don't compare this route to THP-on performance. Range mapper caveat now phase
  neutral: external visual evidence determines phase, not hardcoded startup.

## 2026-09-09 — R754 opt-in scene-triggered census

- Prior turn progress: bounded table capacity/hash and replay. Added optional
  GALAXYPAD_FALLBACK_START_FILE gated by exact FALLBACK_PCS=1. Init waits with
  no histogram; CPU checks marker at1024timing-slice intervals and allocates
  once, logs capture-start PC/native count. No polling when disabled/started,
  no marker deletion or per-instruction I/O. Fresh absent path is caller contract.
- Actual extracted init/poll fragments pass ASan/UBSan: opt-out, absent marker,
  boundary activation, no repeat reset, marker retention, legacy immediate mode.
  Wiring/histogram/summary tests pass; suite registered, full suite not rerun.
- Canonical0027 SHA2c0a83806bf138b3414f212770a0b4291c975ecb42e64f62d4d4bc3288e00667,
  bootstrap updated, reverse-check/shell syntax pass. Incremental Simulator
  core53457 completes exit0. No app provision/install yet or live phase proof.
- Next package and new absent marker; visually confirm scene before triggering,
  confirm actual capture-start then native Stop. Phase label remains externally
  evidenced, never inferred from elapsed startup time or marker existence.

## 2026-09-09 — R753 bounded census capacity/hash correction

- Prior turn progress: initial image mapping excludes simple loader range.
  R751 lost22.29%; expanded optional table4096→32768slots and probes8→16,
  hash now mixes upper PC bits (old mask ignored those bits). Still no per-step
  allocation/I/O/guest reads and explicit dropped accounting.512KiB entries.
- Histogram sanitizer/wiring and summary tests pass; summary accepts up to
 32768keys while retaining conservation checks. Replay3916retainedkeys and
 5801917recordedevents has0drops. Replay order is grouped, absent keys unknown;
  cannot claim live complete coverage. No runtime performance assertion.
- Canonical0027 SHA3e65cd37f9e55b9f94fff075d8dd82119ce925b0209851f0ec2d19fb2ab65156,
  bootstrap hash updated, reverse-check passes. Initial attempt to replace
  patch with same-path delete/add rejected without mutation; full update worked.
- No new module/app build or install. Next phase separation before rebuilding
  diagnostic core so startup and gameplay aren't mixed in the next census.

## 2026-09-09 — R752 rule out naive initial-apploader attribution

- Prior turn progress: clean actual fallback census, saves intact. Read exact
  main.dol/apploader headers and Boot_BS2Emu RunApploader copy destination.
  Loader body+trailer initially81200000..8123b73c, entry81200294. Neither91f0
  nor921b PCs belongs there. Relocated copies remain possible, not established.
- Added map-fallback-image-ranges.py, hash-pinned output and focused boundary/
  truncated-image tests. Pass; registered suite (not full-suite rerun). R751
  recorded counts3449008outside,2315199low memory,37710DOL text. Drops1664068
  remain unclassified. Artifact generated/fallback-image-ranges-r752.json.
- Next separate post-startup gameplay census and reduce collision loss before
  claiming frequency; obtain bytes/relocation evidence before adding coverage.
  No source runtime/app changes or new simulator, no performance/PRD closure.

## 2026-09-09 — R751 user-authorized switch and successful clean census

- User says stop asking and switch. Shutdown08636791 then bootDE8E956F via
 3286exit0; never two booted devices. Installed signed2bdf90ee via1482exit0.
  Data migrated5D8104BD-10AF-4224-99F6-76AB1256EA22; save99d432d5 verified before
  and after. No game data deleted, no old-checkpoint identity bypass.
- Runtime77298/PID98808 with explicit game/disc/module/nativeTHP args and
  FALLBACK_PCS=1. CUA works; visible wrist-strap startup. Native Menu→Stop→
  confirmation at04:22:09 flushed census and failed0/smc_failed0. Idle app
  terminated afterward;77298exit0. M5 remains sole booted Simulator.
- Summary accepts total7465985=5801917recorded+1664068dropped,3916keys.
  Recorded ranges91=3144947,00=2315287,92=303973,80=37710; hook37710, forced0,
  uncovered5764207. Dropped22.28866%; frequencies not CPU time, startup not
  gameplay. This proves non-vector interpreted PCs exist, not their provenance
  or steady-state dominance. No XF assertion in short run, not a stability pass.
- Next map high-address runtime code and obtain phase-separated low-loss
  gameplay census before optimization. Preserve ios-runtime-r751.log and
  fallback-pcs-r751.json. Shared-Simulator blocker resolved by explicit authority.

## 2026-09-09 — R750 shared Simulator blocker confirmed

- Previous turn progress: integrated suite pass and patch-order repair. Current
  simctl again shows08636791/M4/iOS18.5 booted; GalaxyPad absent. Candidate
  strict bundle verification passes. No further preparation needed for next run.
- Same external lane constraint persisted R748/R749/R750, with release/switch
  question already sent. Stop unchanged polls and speculative changes; mark
  goal blocked pending Simulator availability or permission to switch. No
  other project's process, device, data or save altered. Full goal preserved.

## 2026-09-09 — R749 integrated checks finish; XF patch layering fixed

- Previous turn progress: signed diagnostic app and EFB oracle compatibility.
  Full rerun2589 continued, then terminal exit0: check-r748b.log ends Repository
  safety checks passed. Latest XF patch-order test added during run executed
  separately and passed; don't imply it was in already-running shell input.
- Read-only reverse check reproduced0024 unable to apply beneath0028. Added
  hash-checked early peel/exit restoration and normal reapply state reset for
 0028 in existing bootstrap framework. No live vendor peeling during suite.
- New temporary-tree round-trip test reverses0028/0024, reapplies0024/0028,
  asserts byte-exact final and untouched live source. Pass; suite registered.
  Shell syntax, git diff check and signed bundle verify pass. Full bootstrap
  chain not executed or claimed validated by this focused check.
- Other M4 still sole booted Simulator. No new game run/install, no saves
  changed. Candidate awaiting simulator availability/authorized switch; full
  PRD and live stability/performance evidence remain unfinished.

## 2026-09-09 — R748 diagnostic packaging and integrated checks

- Prior turn progress: XF origin core built. Provision/app87275 exits0. Core
  SHA38eab9cffd4ded4b22e7e4e1d7c7781c8de7174e0b3ba997dc3eb68fb7292fe8.
- Full suite26075 terminal exit1 at test-efb-read-context.py pinned hooks hash.
  New census block explains drift; remove only exact optional block for old
  oracle, retain original hashes/reconstruction and independent census tests.
  Focused EFB context passes; not a changed expected-product hash shortcut.
- codesign verification failed resource seal: app resource target runs after
  executable linking. Simulator packaging now signs completed bundle ad-hoc
  and verifies; device workflow untouched. Rebuild96322 exit0; strict deep
  verify passes. AppSHA2bdf90eecf95ca5268d5121dfc48d207aa640b7d78d8c0ea9e640cf3f140f07e.
- Full rerun2589 currently LIVE at direct-tap tests; logcheck-r748b.log. Poll
  same handle; no full-suite pass yet. Other M4 booted,34GiB disk available.
  Asked user to release simulator lane or approve switch; no app installed or
  other project's runtime disturbed, no new gameplay/performance evidence.

## 2026-09-09 — R747 failure-only decoder origin context

- Prior turn progress: actual XF partial-case oracle. Added optional templated
  DescribeCommandSource and runtime callback DescribeSource. Only failed assert
  formats source/preprocess/listaddress/listsize; existing m_in_display_list
  selects origin, two fields set on actual list entry. Inactive stale addresses
  report zero. Other callback clients explicitly unspecified, no invented origin.
- Source helper sanitizer tests pass5cases;624partial/refill and window bounds
  pass. No full suite/live provenance proof. Canonical0028 SHA
  4f6eaad058addedd2ed7dd55123a9a6863cc90e6d8a652681ecc2986677411ec,
  bootstrap/suite registered, reverse apply and shell syntax pass. Baseline
  generated/xf-origin-r747-baseline retained. No assertion/consumption change.
- Incremental Simulator core39624 completes exit0, ios-core-r747.log. No app
  provision/install/launch yet. Other M4/iOS18.5 still sole booted device, left
  untouched. Preceding-refill history not implemented; source tag is first
  discriminator, not a completed corruption diagnosis or performance fix.

## 2026-09-09 — R746 narrow partial-command hypothesis checked

- Prior turn progress: recovered actual malformed GX bytes. Inspected FIFO
  refill/compaction, RunFifo cursor update, Run loop and display-list paths.
  Both FIFO and display lists invoke Run; offset0 is ambiguous. Current local
  Fifo.cpp diff changes wakeup diagnostics, not copying/compaction statements.
- New test-xf-partial-command.py compiles actual extracted XF case with bounded
  callback/assert stubs. All624 truncation/refill cases for1..16words pass
  ASan/UBSan; no callbacks before complete input, proper count/address after.
  Captured00611600 header rejects before callback. Registered repository suite.
- Does not test producer/concurrency/all opcodes or prove a renderer fix. Next
  failure capture needs FIFO-versus-display-list provenance and preceding
  command/refill boundary. No assertion masking, speculative fix, module build
  or another simulator launch. Full goal/stability/performance gates remain.

## 2026-09-09 — R745 resumed: runtime ended, graphics failure evidence recovered

- Previous turn recorded external blocker. On resume PID89863 absent and
  runtime56672 now exits0; log has NO core shutdown/census. Do not classify the
  launcher's exit as graceful game completion. Save99d432d5 remains unchanged.
- UI access recovered. Foreground/sole booted device is M4/iOS18.5
  08636791-2675-4675-8335-EF72EF954DCF, not our shutdown DE8E956F. No other
  device touched, no app launch. Goal's external blocker has changed.
- R74203:12:09 GX assertion: cmd2=00611600, offset0 available34. Failure-only
  context from R694 finally supplies useful bytes:10 00 then six complete BP
  writes61/16..1b/000000. probe-xf-failure-r745.py verifies exact captured shape.
- XF high-length97 exceeds15.34=2+32 fits a leftover/burst boundary, but source
  also has display-list decoders; this alone identifies neither producer nor
  byte loss. Do not skip prefix or mask assertion. Next inspect FIFO ingress/
  decoder provenance; graphics stability precedes another fallback census.

## 2026-09-09 — R744 external UI blocker confirmed for third turn

- Previous turn progress: complete diagnostic parser/tests, user request for
  native Stop. Current PID89863 live03:29; R742 log has no shutdown/census.
- CUA getApp timeoutReached again. R742 reset/retries, R743 retry, R744 retry
  establish same external blocker across three consecutive turns. Canonical
  diagnostic patch still reverse-checks. No new build/restart is justified.
- Runtime evidence now requires native three-dot Stop Game or recovered UI
  access. Keep existing56672/session and saves; no force-kill or another device.
  Mark goal blocked pending this external change, not complete or reduced.

## 2026-09-09 — R743 validate census analysis while UI access is unavailable

- Prior turn progress: packaged and visibly booted diagnostic. Same PID89863
  live,02:03 elapsed; CUA Simulator getApp still timeoutReached. No restart.
- Added summary parser with total=recorded+dropped, exact PC/path uniqueness,
  path validation,4096entry bound, one-session and following shutdown checks.
  Counts remain whole-session frequencies, not cost or relocation proof.
- Focused valid/lossy/complete and nine rejection cases pass; registered suite.
  Current ios-runtime-r742.log correctly exits2 for absent shutdown census.
- Nonblocking user question requests native three-dot Stop Game if visible.
  Await clean-stop dump or recover UI access; no force-kill, new build, save
  mutation or gameplay/performance acceptance. Goal remains active.

## 2026-09-09 — R742 packaged diagnostic and visible startup

- Previous turn progress: actual Simulator core compiled. Provision/app87179
  exit0; app SHA c45c54fb9cf435ebe6556e2da678d5dde2758ad97187bfd605f359297bf75387,
  core725421349d14d2f32b72f622f0961b1f2dcf50a854ed698e2d212a2b2dbe4058.
- Preserved installed d34fe135 executable in r742-installed-baseline. Installed
  via65250exit0; save99d432d517b92b19da0c403819b91288b10c7063c3ce61f5000347ef823ab64f
  unchanged. Simulator migrated data to08E9491E-C8E3-46DC-A455-1A170D70E08C.
- Only DE8E956F booted. Explicit game/disc/currentSimulator module/nativeTHP
  args, SIMCTL_CHILD_GALAXYPAD_FALLBACK_PCS=1; runtime56672/PID89863 live.
  ios-runtime-r742.log; screen simulator-r742.png proves wrist-strap rendering,
  not gameplay or good performance. No checkpoint compatibility bypass.
- CUA Simulator getApp repeatedly timeoutReached, including after JS reset.
  No controls clicked or clean Stop yet; histogram dumps only at shutdown.
  Next recover native UI and clean-stop SAME run to inspect first census. Do
  not kill/restart solely for UI timeout. No completion/performance claim.

## 2026-09-09 — R741 fallback census wired and Simulator core compiled

- Prior turn progress: bounded counter/tests. Added optional ownership at Init,
  exact opt-in1, pre-step recording at three interpreter sites, post-stop dump
  and reset at Shutdown. No fast cache path/JIT/debugger attribution implied.
- Canonical0027 SHA7786a8edf1e15e101abe62f665ca29104da10432e44d21eafe2698ef56fa94b2,
  bootstrap registered, reverse apply check and shell syntax pass. Private
  pre-edit baseline retained in generated/fallback-r741-baseline.
- Histogram ASan/UBSan plus source-boundary wiring tests pass; suite registered,
  full suite not rerun. Incremental simulator core79368 terminal exit0, log
  generated/ios-core-r741.log. No generated game-module rebuild necessary.
-35GiB available, no booted Simulator or GalaxyPadRunner at pre-build check.
  No app/save installed or changed. Next provision/package and controlled
  opt-in live census; instrumentation timing is not an optimization result.

## 2026-09-09 — R740 bounded fallback-PC diagnostic

- Previous turn progress: R739 conversion screening closed without module build.
- Current source confirms forced/uncovered chassis and actual slow hook are
  distinct interpreter sites; existing hook_fb also counts native cache paths.
  R710b totals have no PC attribution, so exception-vector theory remains open.
- Added fixed-capacity8-probe histogram, exact alias/path keys and explicit
  dropped accounting. C++ ASan/UBSan test passes aliases/path/zero/repeat/full
  table conservation. No runtime integration or new live capture yet.
- Next opt-in lifecycle/wiring/canonical patch before mobile build. No game or
  Simulator launched, product untouched; full goal remains active.

## 2026-09-09 — R739 conversion hint fails the code-shape screening gate

- Inspected actual linked diagnostic ps_add: arithmetic, conversion and status
  helpers are already inlined. NI-off still computes flush candidates; tested
  one branch-likelihood hint in isolated source copies, not the product.
- tests/test-single-mode-hint.py completes:800000 cases across RN/NI modes
  match bits, host flags and unchanged FPSCR. Both ps_add variants have84 static
  instructions. Log: generated/single-mode-r739.log. No dynamic speed proof.
- Park hint without whole-module build or runtime replay. Remaining shared-state
  work must demonstrate a broader repeated-work reduction before a costly build;
  otherwise move to bounded identification of mobile fallback PCs. Original
  PRD remains active; no installed app change or performance gate closure.

## 2026-09-09 — R738 conservative integer-gap census closes narrow hypothesis

- Previous source audit identified prior optimization and a new gap question.
  Implemented read-only body-level recognizer with unknown/callback/state/exit/
  noncontiguous-entry barriers. Tests pass and are registered.
-1322chunk scan97973exit0:165totalregions, just1nonadjacent over old164. Gap has
  addi/li only; full hash/address records in generated/fprf-gaps-r738.json.
- Park this insufficient-scope extension without module build or runtime. Not
  proof every region/state idea fails. Next broader shared helper lowering,
  not opcode-by-opcode extension or repeated adjacent-site experiments.

## 2026-09-09 — R737 source/history audit for shared-state lane

- Previous turn measured reverse pair and closed direct-call promotion. Read
  actual float helpers/emitter and historical FPRF/provenance/entry-split results.
  Found119decoder deferrals already integrated; did not recreate their tests.
- Current-source adjacent audit164remaining sites/68chunks; largest37/23/16.
  Static count is not dynamic importance or savings. Documented new bounded
  nonadjacent straight-line status-liveness coverage question with explicit
  callback/exception/observer/control/entry barriers. Unknown operations stop it.
- No product edit/compiler/runtime or gain claimed. Next bounded broader region
  audit, not another unchanged adjacent-helper or entry-split experiment.

## 2026-09-09 — R736 reverse-order trial; reject promotion of this design

- Previous turn produced first enabled comparison. Prepared fresh matched
  reverse profiles. Enabled12581/87671 bound1 capture92913; unbound21919/87870
  capture19586; all finished0. Nativecheckpoint and pre/post visual checks pass
  scene smoke; nativeQuit/fallback0/smc_failed0, saves106e unchanged, PIDsgone.
- CPU enabled28.988783 vsunbound24.533556ms/VI; instructions298.073222M vs
 286.856603M/VI. First5.66% gain not repeated. Host/focus not controlled; inactive
  chrome/no pointer in enabledB2 limits attribution, not proof universal slowdown.
- No material reproducible gain or instruction reduction. Close this design for
  promotion, retain evidence/product bindingoff. Updated actual goal/performance
  queue to sharedFP/state semantics-preserving work; no new tinydirect-call loop.
  Full original PRD acceptance remains unfinished; no installed app changes.

## 2026-09-09 — R735 enabled direct-call measurement, modest first result

- Previous turn completed unbound baseline. No other runtime/compiler/simulator.
  Exactsame binaries/settings freshenabledR724,65580/PID87354, bound1. Restored
  slot2 via native menu and verified matching scene before/aftercapture42244.
- CleanQuit65580exit0, VI saved/fallback0/smc_failed0, save106e unchanged, PIDgone.
 1146VI/30.005s,CPU24.761164ms/VI (5.6633% less),294.600105Mprocessinstructions/VI
  (1.5985% MORE). Single order; no reproducibility/net-product gain claimed.
- Whole-run1.203Btransfers confirms actual path, not normalized-window dispatch
  savings. Next reverse-order repeat. Reproducible<8% closes design under existing
  gate; move sharedFP/state lane rather than endless small modifications.

## 2026-09-09 — R734 module linked and live unbound baseline measured

- Previous turn verified linker wait. Same37737 completed exit0. Module6871db5d
  exports setter and ABI getter; loader metadata/signature verifies90083exit0.
- No other game/linker or simulator booted. Launched signed R730 runner explicit
  unbound R724 profile, runtime84704/PID86966. Startup sample identified game
  hashing before UI; no restart. Native LoadState2 visually restored Good Egg.
- Capture95366exit0; pre/post scene remains hanging Mario/life3/StarBits4.
  Clean native Quit84704exit0, VI saved, fallback0/smc_failed0, save106e unchanged.
 1084VI/30.009s=36.122Hz; CPU26.247646ms/VI,289.964938Mprocessinstructions/VI.
  Zero invalid/reset intervals. Artifacts live in direct-unbound-r724 profile.
- Next enabled pair in untouched R724 enabled profile with exact same binaries.
  No performance gain inferred from baseline alone; full PRD remains unfinished.

## 2026-09-09 — R731 clean simulator shutdown during final module compilation

- Previous turn packaged runner. Same37737 waited twice50s, advancing past1007
  then1063 steps; no restart. Prepared transition to only one desktop game.
- Native menu Stop Game->confirmation clean-stopped paused75679. Runtime
 01:41:15.881 failed0/smc_failed0, save99d432d5 unchanged. Terminated idle app,
  shut down soleDE8E956F;8698exit0, PID absent/no booted devices. No deletion.
- Module now1105/1333 and still live; poll same37737 next. No launch/benchmark
  while compiler remains active. Original installed simulator app preserved.

## 2026-09-09 — R730 isolated runner bundle ready

- Previous turn linked actual guard runner. Same module37737 polled live,
  advancing to910/1333. Read Apple Sys path and avoided normal package script's
  module promotion/default-user bootstrap for this diagnostic-only experiment.
- Added distinct experiment plist; prepared direct-calls-r730 app with runner
  and Sys only. Plist/signature verification37936exit0; post-sign runnerSHA
 871f4f8726cf1e12c56022a61dfbd013cbfa6614ce68ee6873982e869a9f4faf.
- No launch or installed-app mutation. Next finish same module build, verify
  exports and clean-stop simulator before explicit-profile desktop diagnostic.

## 2026-09-09 — R729 guarded runtime runner linked

- Previous turn was verified wait. Continued same87774 until exit0, no restart.
  Runner linked successfully with nonfatal duplicate-library warnings. Hash
  b5151c2f38f6b76638e93f9c57e0a5fb156f165068828fa0e83cad953ef5c2c5.
- nm confirms actual guard in executable; strings confirms extension symbol,
  opt-in flag and diagnostics. No loaded module or runtime-performance proof.
- Module37737 remains live, last819/1333. Continue same handle; simulator stays
  paused until builds finish. No app installation/publication or goal completion.

## 2026-09-09 — R725 verified build wait and measurement path audit

- Previous turn prepared matching profiles. Re-polled module37737/runner87774;
  module advances to350/1333 during bounded50s wait, runner74/232. Both remain
  live, no restarting/benchmarking under compiler load. Simulator stays paused.
- Re-read actual VI recorder: thread CPU clock, validity flag, bounded in-memory
  samples, post-join file flush. Traced event from SystemTimers VICallback and
  VI end-field path. Existing wiring/summary tests exit0; no new instrumentation
  or assumptions that frame-event counters measure physical presentation.
- Continue SAME handles until terminal; no linked candidate or gain claimed.

## 2026-09-09 — R724 matched profiles prepared while builds advance

- Previous turn made runner build progress. Both37737/87774 polled live; latest
  logs221/1333 module and40/232 runner. No restarted build or runtime measurement.
- Read actual R462 profile, runner arguments and VI/counter capture setup.
  Prepared fresh unbound/enabled profiles with identical Config/Wii/slot2 and
  separate FIFOs, no old logs/caches. Verified matching74e453b5 checkpoint and
 106e8248 save hashes; config diff empty. Original profile untouched.
- Restore compatibility and visible scene remain unproven until new runner is
  linked. Keep simulator paused during build, clean-stop before desktop launch.
  No performance gain/PRD gate completion claimed.

## 2026-09-09 — R723 preserved runner and real relink underway

- Prior turn started actual module build. Poll37737 confirms live and log moves
  beyond initial chunks. Read desktop build/package entry points; existing guard
  objects alone cannot enable runtime testing, so started real runner target.
- Backed up desktop runner to candidates/r723-runner-baseline; source/copy hashes
  both13354ea966aeca76f893c769ee81c0b85ce260dcefa14c80debf8f3459c8f53a.
  Runner build87774, parallel1, log direct-call-runner-build-r723.log.232 steps;
  known nonfatal SCM revision diagnostic. Both37737/87774 re-polled live.
- Simulator remains paused; no benchmark or app installation during compilers.
  Next poll same handles, inspect final link/symbols and prepare matched run.

## 2026-09-09 — R722 full guarded diagnostic build in flight

- Previous turn completed broad source/configuration progress. Audited actual
  compile graph1321+1 chunks andbinding with no duplicates and strict-FP/O2/IPO.
 36GiB free; existing game75679 verified and native menu paused for compilation.
- Started full module build37737, parallel2, generated/direct-call-build-r722.log.
  Subsequent handle poll confirms still running, log advances through chunks.
  Poll SAME handle next and keep game paused during load; no linked result yet.
- Explicit build rationale/measurement limits recorded in DIRECT-CALL-EXPERIMENT.
  Historical evidence makes broad experiment worth testing but proves no numeric
  forecast. Need guard-enabled runtime link and matched controls, not comparison
  against old differently-profiled binaries. No installed app mutation/promotion.

## 2026-09-09 — R721 broad caller source overlay

- Previous turn's profile evidence changed coverage selection. Implemented
  isolated batch overlay using tested transformation and actual entry index.
  Preparation86148exit0:150753 sites across1321/1322 chunks,361MiB. Original
  source hashes retained; manifest written only after completed preparation.
- CMake overlay mode validates source tree, hashes and target membership, then
  replaces candidate sources in isolated graph. Configure63234 initially stayed
  live (cmakePID79971 confirmed), then completed exit0 after123.1s. No restart.
- Focused source-shape/differential tests pass again. No complete module build,
  installed app change or performance result. Next complete/verify configuration
  and diagnostic integration. Full PRD remains active.

## 2026-09-09 — R720 caller coverage decision

- Previous turn made actual build-integration progress. Read retained profile
  with llvm-profdata; all1322 chunk counts sum2,895,966,494. Single compiled
  diagnostic chunk0166 has247,493 entries; top20 destinations total45.9042%.
- Evidence changes next action: no one-chunk speed trial, nor callee-only hot
  selection. Rewrites occur at caller sites. Prepare broad eligible caller
  overlay before meaningful architectural comparison. Counts are historical,
  not dynamic BL coverage, exclusive CPU work or a speedup prediction.
- No code/app/runtime mutation or new benchmark this turn. Updated experiment
  decision and status; full original PRD goal remains active.

## 2026-09-09 — R719 isolated diagnostic build integration

- Previous turn made executable differential-test progress. Read actual vendor
  module CMake/cache. Added experiment wrapper preserving its flags, runtime
  sources and ABI generation, replacing only one selected chunk plus binding.
- Configure89506exit0, targeted actual chunk/binding object build65636exit0.
  Confirmed ThinLTO/O2/strict-FP arm64 flags in generated build graph. No full
  module link/rebuild, no runtime guard deployment or benchmark gain claimed.
- Revalidated sole DE8E956F/PID75679. Native menu pause during build, dismiss
  afterward. Installed source/module/app selections remain unchanged. Next
  coverage/materiality decision before costly complete diagnostic candidate.

## 2026-09-09 — R718 executable transformed fixture comparison

- Previous turn made nested helper/runtime-body test progress. Revalidated the
  current transformer and existing source-shape tests, then added executable
  transformed callers with nested BLs and suffix-charge continuation entries.
- Actual transformer/binding/transfer helper tested in original/transformed
  executables. Each passes36 modes, comparing five boundary register/charge
  records to original dispatch, final saved LR, depth0 and53 total cycles.
  ASan/UBSan compile/run exits0; registered in repository suite.
- Runtime and guest semantics remain explicit fixtures; actual module execution
  and measured gain remain unproven. Next macOS diagnostic integration rather
  than further isolated helper tests. No app/simulator mutation this turn.

## 2026-09-09 — R717 nested guard/helper execution

- Previous turn made generated wiring progress. Re-read boundary test and actual
  core guard; extended existing harness rather than creating another framework.
- Link actual C binding and transfer header with extracted runtime guard/outer
  charge bodies. Five nested scenarios pass ASan/UBSan, including matching outer
  continuation after inner rejection, paused/exception state and depth limit.
  Verify exact budget/timebase and resumed counts; compile/run exits0.
- Existing denial and257 charge cases remain. No full suite, app/module link or
  performance measurement this turn. Runtime services/callee bodies are stubs;
  generated differential fixture and diagnostic integration remain next.

## 2026-09-09 — R716 isolated generated call-site wiring

- Previous turn made code/test progress. Revalidated generated header and chunk
  source. Added explicit single-chunk transformation, keeping selected source
  immutable and refusing existing/in-tree output. Target membership is verified
  using real entry cases rather than inferred solely from chunk ranges.
- Continuation switch entries sometimes subtract suffix cycles. Preserve that
  exact subtraction before inline goto; reject unknown accounting shapes.
- Actual chunk0166 now has76 guarded sites in generated/direct-call-r716 copy;
  clang C11 syntax check with actual CPU/generated headers exits0. Focused
  transformation tests exit0. Initial0001/0004 attempts refused with no output;
  matched generated brace shape before successful0166 transformation.
- No full build/runtime mutation or measured gain. Next actual-guard nested
  differential execution, then materiality gate before a full diagnostic build.

## 2026-09-09 — R715 preserve module replacements in direct-transfer glue

- Previous goal response restated recorded progress without implementation;
  classified as no progress. Revalidated current source and took the next step.
- Generated dolrecomp_call performs module replacement dispatch before host
  interception/original code. Added isolated transfer.h preserving replacement
  execution at callee entry and caller continuation, with balanced depth and
  host boundary checks. Early or replaced returns preserve PC and yield.
- tests/test-direct-call-transfer.py compiles actual helper and binding under
  ASan/UBSan; eight ordering/denial/replacement/depth cases pass (exit0).
  Registered in check-repository.sh; full suite not rerun. No generated module
  build, simulator mutation or performance claim. Nested actual-guard tests and
  isolated generated call-site wiring remain next; full PRD goal stays active.

## 2026-09-09 — R714 runtime binding, guards and exact-once charge integration

- Prior turn implemented dynamically tested module extension. Preserved exact
  pre-edit core files under generated/direct-call-r714-baseline for scoped patch.
  Added default-off dynamic-module binding in core Init and detach in Shutdown.
  Refuses binding for REL/lockstep/dispatch trace or sampling modes; attached
  modules stay unchanged. Explicit env must equal1 and versioned symbol exist.
- Added actual HookDirectCallBoundary: validates CPUState ownership/native-call
  context; commits cycles/timebase; rejects inactive/REL/lockstep/mismatchedPC,
  stopped/exhausted/idle/unverified/forced/hooked/exception transitions. Failed
  check latches unwind until outer invocation exits. Accepted target resets
  segment commitment. Outer Run avoids duplicate minimum1 on committed unwind.
  Guest/host exception delivery remains with original outer path.
- tests/test-direct-call-boundary.py extracts production guard AND outer charge
  code into explicit runtime stubs.13 guard-denial cases,3 context cases, null
  state,257 chained charge cases, zero-charge handling, remainder carry and
  EE-disabled async exception case pass ASan/UBSan98604exit0. Tests registered.
  This is not actual SMC/host-mod execution or full generated-code integration.
- Canonical0026-guarded-direct-call-boundary.patch SHA
  f9c905b4e0f23e5a39989825bd583c678dd3c0a114315a56c25079c2ce34f408,
  bootstrap registered, reverse apply check and shell syntax pass. Corrected
  trace opt-out to actual STATICRECOMP_TRACE_FILE before recording patch.
- Paused75679 via native menu; actual macOS Core.cpp/Run.cpp object compile
 44167exit0, direct-boundary-compile-r714.log. Known nonfatal SCM '^master' noise.
  Resumed unchanged Simulator app. No full link/full suite/module rebuild or
  performance result. Next emitted-site wiring and integrated differential tests.

## 2026-09-09 — R713 implement optional direct-call extension plumbing

- Prior turn progressed exact-source census and transfer contract. Rechecked
  core Init/LoadModule/Shutdown and CPUState opaque tag, host membership includes
  pending returns, and existing timebase helper. Chose optional versioned module
  setter rather than ABI change or repurposing memory callback slots.
- Implemented experimental binding.h/.c with one module-global callback, explicit
  export under hidden visibility, fail-closed missing/state/version handling,
  explicit detach. Binding is outside CPU execution, not concurrent-safe by design.
  Not linked into product or enabled in generation pipeline.
- Added actual dylib/host lifecycle test: clang C11 Wall/Wextra/Werror +ASan/UBSan,
  dlopen/dlsym, argument forwarding, callback rejection, invalid-version clearing,
  detach and three load/unload cycles pass15290exit0. Registered in full suite,
  which was not rerun this turn. Diff check clean, live75679 unchanged.
- This is real module binding plumbing only. Runtime segment charging, target/
  continuation validity, interception and emitted call-path tests remain next.
  No unsafe flag or new module build, no claimed performance result.

## 2026-09-09 — R712 actual call census and direct-transfer contract

- Previous turn progressed explicit user review incorporation/reorientation.
  Inspected actual emitter, FastDispatchableAt/DispatchableAt, host membership/
  hook bridge, Run charge/exception checks, CPUState ABI and dynamic loader.
- Implemented scripts/audit-direct-call-sites.py plus synthetic shape/rejection
  test registered in check-repository.sh. Census48600exit0 over current1322files:
  165220BL,150916chassis returns,14304localgoto,0direct. Source manifest1a0cc009
  and caveats in generated/direct-call-census-r712.json. Optimized parser to avoid
  repeatedly copying remainder of chunk; classifications unchanged, tests pass.
- Documented concrete callback integration shape in DIRECT-CALL-EXPERIMENT.md:
  optional versioned dynamic-module binding, no CPUState ABI change, target and
  continuation validation, existing slow paths, exact-once timing, lifecycle.
  Notably early guard charge flush plus ordinary Run's minimum1 can double-charge
  glue-only unwind; this must be tested rather than relying on local256budget.
  No runtime/emitter implementation or speedup yet; no unchanged profiling run.

## 2026-09-09 — R710 candidate deployment, R711 user-supplied review reset

- R710 clean stopped71970 failed0, terminated idle host, installed dd96031a,
  verified appSHA and preserved NAND99d432d5. Launched75275/86294 with unchanged
  flags. Before restore, source audit found INFO release logging is filtered by
  warning/error-only embedder forwarding. Replaced only this diagnostic with a
  bounded stderr line; flow128cases and reverse patch check pass32841exit0.
- Paused and clean stopped75275 failed0 at00:31:09; core/provision/app89958exit0,
  installed diagnostic-corrected d34fe135 after oldPID absence. New75679/11407,
  ios-runtime-r710b.log. No own checkpoint save/restore yet. No measured saving.
- Chris then supplied independent review and requested recording/reorientation.
  Preserved it verbatim in INDEPENDENT-PERFORMANCE-REVIEW-2026-09-09.md, separately
  marked external analysis. Checked emitter direct calls/depth helper, Run loop,
  R424/R425/R463/R464 evidence, exact R705 tree, current chunk directory and
  R700 shutdown counters. Current1322chunks; named leaf groups89/428, not140;
  49waits; charged cycles/dispatch14.0237593 is a whole-run ratio. Additional
  hook/entry costs exist but no established one-third CPU lower bound.
- Adopted architectural critique and prioritized guarded direct-call experiment.
  Kept production unsafe flag refusal; rejected assertion that correctness risks
  cannot affect diagnostic measurements. FP policy remains explicit, not silently
  relaxed from JIT defaults. Recorded accepted/corrected/unverified claims,
  matched metrics and15%predicted/20%target/<8%stop thresholds in response doc.
  Updated actual goal loop and active performance queue. Full original PRD intact.
  Undo-memory live check parked explicitly to avoid returning to small-target loop.

## 2026-09-09 — R709 memory candidate fully linked and regression checked

- Prior turn implemented/tested the scoped undo-buffer release candidate. Paused
  current71970 through native menu before full repository suite and incremental
  core build. Core17086exit0; provision and app link12869exit0. Nine upstream
  unused-parameter warnings in app compile, no build failure. Actual linked
  local symbols include State::LoadAsWithoutRetainingUndo and LoadAsFromCore(bool).
- Candidate appdd96031a16c768efe9b2ab399dd3fdf18f994118368ad9907f8e1eb23af4fe75;
  merged coreb5f6a1c95129470aa7debd9df2f6fbdb16d7dbfeac412de66824b6d9095330a8.
  generated/checkpoint-core-r709.log,checkpoint-provision-r709.log,
  checkpoint-app-r709.log. No module regeneration or app installation.
- Full check-repository.sh86990 completedexit0, generated/check-r709.log ends
  Repository safety checks passed. Includes new undo-flow regression and prior
  checkpoint identity/path gates, native diagnostics, timing/input/render tests.
  Synthetic suite is not proof of live memory reduction or game performance.
- Preserved installed84f0fe05 with ditto into previously absent
  generated/candidates/r709-baseline/GalaxyPad.app; backupSHA matches installed.
  No game/save/checkpoint deleted or overwritten. Resumed71970 after tests.
  Next candidate install and candidate-owned checkpoint restore/release-byte
  verification; cross-build old checkpoint refusal must remain intact.

## 2026-09-09 — R708 release unneeded development-checkpoint undo storage

- Previous turn progressed restart/checkpoint verification. Inspected actual
  State.cpp LoadAsFromCore and UndoLoadState. Upstream keeps a full undo buffer
  after successful loads; dev UI has no Undo Load action. Added separate opt-in
  API without changing existing public LoadAs symbol/default behavior.
- Load still captures rollback before reading, invokes existing UndoLoadState
  on failed deserialization, preserves unreadable-file buffers, and retains undo
  for recording/playback/current or newly loaded DTM. Only successful eligible
  dev loads release allocation, on CPU thread before after-load callbacks can
  reenter. Logs exact bytes released. No render, clock, game data or input mapping
  changes. Host checkpoint identity/size/path guards remain intact.
- Canonical patches/ModernGekko-dolphin/0025-development-checkpoint-undo-release.patch
  SHA6f34f55fa96cca0035e408d49f1c98b457944cb7f1a4cef0cb65a0cd1d33492a;
  bootstrap hash/scope registration and reverse-apply check pass. Initial patch
  replacement command rejected duplicate targets; corrected with one update,
  no patch file lost. tests/check-repository registration added.
- tests/test-checkpoint-undo-release.py extracts actual load function into stubs:
  128 combinations of retention/read/success/DTM/movie flags, failed-load rollback,
  callback buffer preservation and existing default retention pass ASan/UBSan.
  This is flow coverage, not real savestate deserialization or allocator/RSS proof.
  tests/test-development-checkpoint.py passes updated API gates. Session72792exit0.
- Paused71970 through native menu; compiled actual Simulator State.cpp object
  via52604exit0, generated/checkpoint-undo-compile-r708.log. Nonfatal SCM bad
  revision '^master' remains known. Resumed unchanged running84f0fe05 afterward.
  No archive/app linked or deployed yet. Full suite and live byte-release/log/
  restore test pending; must not claim109MiB saving or frame-rate improvement.

## 2026-09-09 — R707 candidate checkpoint same-process and restart proof

- Prior turn made progress establishing presentation API/platform limits. Next
  tested saved plaza checkpoint for reproducible performance scenes, no rebuild.
  Verified66439 live and exactBFA5final44821637bytes. Native restore00:13:03
  returned to plaza. Leased forward2s65870 exit0 visibly changed Mario/camera;
  second restore returned to entry position. Screenshots in task tool results.
- Native Stop completed00:14:38.759 failed0. Final THP partial176/0 flushed;
  total5591accepted/0fallback confirms full PrologueA hook run on candidate0030.
  NANDGameData SHA99d432d5 unchanged. SMC failures0 in shutdown summary;
  this is not full stability/audio/first-play acceptance. Then simctl terminate
  idle app and ps confirmed old66439 absent before new launch.
- Relaunched same installed84f0fe05 with R700 flags, no build/settings changes.
  New71970/session84246, generated/ios-runtime-r707.log. Boot wrist-strap screen
  visually verified; restored BFA5 to invaded-plaza entry. Forward1s53593 exit0
  visibly moved Mario. Second new-process restore00:17:09 returned checkpoint
  scene, input cleared. New process has not decoded a movie; later native THP
  behavior after checkpoint restore remains unverified.
- AppSHA84f0fe0567897035fb86a52de9454b4e300b62933872822c22567a30e5937b4c;
  checkpointSHA50505369e0c328829927650cb39fb5814b6049e28a2422e4417615c8f004c24b.
  SoleSimulatorDE8E956F verified. Cross-build guard unchanged; only same exact
  app/module/NAND compatibility tested. Restore retains upstream undo buffer,
  so annotate memory baseline accordingly. No fresh controlled speedup evidence.

## 2026-09-09 — R705/R706 independent review handoff and measurement limits

- User requested a prompt for another bot, delivered a self-contained read-only
  review contract with actual paths, counter caveats, rejected-experiment history,
  three ranked opportunities and falsifiable experiments. No agent spawned or
  second runtime launched. No review result yet.
- Before that interruption, fresh visually confirmed plaza5s10ms sample38272
  completed0, artifact plaza-sample-r705.txt. CPU428 samples,37floatfuture waits,
  12BlockingLoop waits,15ModDispatch leaves. Broad generated chunk work remains;
  re-read prior SDK/compiler/restore-range rejection evidence before proposing
  native math replacements. No new hotspot proof from aggregate chunk names.
- Continued by auditing actual Metal presentation path and installed SDKs.
  Simulator MTLDrawable protocol declares present/presentAtTime only, despite
  a presentedTime reference in its typedef comment. Mac/device SDKs declare
  addPresentedHandler and presentedTime. No speculative selector use, no
  command-buffer-completion-as-display metric, no unnecessary renderer rebuild.
- devicectl list devices28425 completed0:No devices found. Codesigning identity
  query:0 valid identities. Hardware comparison unavailable today; no false
  conclusion that Simulator predicts physical iPad performance. Full goal active.
  Source/API evidence in PERF R706; current game66439 left intact.

## 2026-09-09 — R704 external CPU-time measurement and visual confounder

- Extended existing scripts/thread-state-probe.cpp, not game hot-path logging.
  Supports exact CPU/Video thread names and GalaxyPad Simulator executable,
  process birth identity before/after, raw user/system nanoseconds. Apple XNU
  fill_taskthreadinfo explicitly converts basic thread seconds/microseconds to ns.
  Controlled sleeping/busy worker self-test:1.400ms versus191.366ms CPU, pass.
- CPU/Video30s probes at100ms cadence and overlapping frame/VM capture ran without
  build/profiler. First result51.376846Hz, CPU82.24%, Video55.42%. Post-capture
  screenshot revealed controller-disconnection dialog; exact onset unknown.
  Reject normal-gameplay interpretation despite stale scene metadata in raw JSON.
- Bounded A input0.3s exited0/released; screenshot verified warning cleared.
  Repeat captures24269/28490/99004 all exit0. Before/after screenshots show Mario
  stationary in invaded plaza with scene animation advancing, no dialog.
  CPU291 valid rows25.943207s CPU/29.993576s wall=86.49588%; Video290 valid rows
  11.264737s/29.913406s=37.65782%. Both counters monotonic. Cadence997events/
  26.499987s=37.622660Hz; rolling speed0.613836–0.686546. Compression0,
  swapout0,swapin4,decompression2562. CPU/Video/frame intervals overlap but differ.
- This supports substantial CPU work, not exclusive functions/core type/host
  frequency or causal percentages. Non-CPU remainder includes blocking and
  descheduling. No application code/settings/build changes or proven speedup.
  Preserve current scene/PID66439 and full goal; prioritize CPU execution work
  over more dispatch microbenchmarks. Visual validation prevented false progress.
- Focused tests/test-thread-state-probe.py passed ASan/UBSan worker, bounds,
  malformed arguments and non-runner rejection (57969 exit0), after captures.
  git diff --check clean; simctl confirms exactly one booted Simulator. Full
  repository suite was not rerun for this external-tool-only change.

## 2026-09-08 — R703 candidate native movie and return regression check

- Previous turn progress: candidate festival entry/checkpoint. Revalidated66439
  and recent log; continued bounded leased movement up hill/down village. Camera
  rotations/obstacles required short corrections. All helper sessions completed
  exit0 and released input. No build/relaunch or source/settings change.
- Reached PrologueA trigger via plaza approach. Screenshots show movie progression
  and invaded-plaza return. Native timing starts with301calls/0fallback first5s;
  18 emitted windows total5415/0, weighted rounded mean1.654606ms wall. Final
  partial not flushed; no full5591 or audio/display parity claim from counters.
- Candidate movie45s capture16404 exit0:2548events/42.500653s=59.952020Hz,
  VI59.939–59.941,speed0.998027–1.002001. VMswapin/out0,compression8631,
  decompression10404. Quiet45s observation, no profiler/build or intermediate calls.
- Visibly returned to invaded plaza. Stationary30s capture95548 exit0:984events/
  26.199983s=37.557276Hz,speed0.576973–0.695790. VMswapin/out0,compression631,
  decompression2485. Earlier baseline35.51Hz is not controlled gain proof; main
  gameplay slowdown remains. Dispatch0030 preserves this native movie path.
- Native menu save created BFA5E5B8-9F1C-470E-AFC3-0C0F5AE029E2.sav (~43MiB)
  at plaza entry, final exists alongside candidatefestival44EC and oldDAF88.
  Restore of new file untested; latest metadata now points to BFA5. No existing
  checkpoint deleted/overwritten. Same66439/session56873 live at entry position.
- Evidence generated/candidate-movie-r703.json,candidate-postmovie-r703.json/.png,
  same ios-runtime-r700.log. No graphics assertion recurrence, not stability proof.
  Next retained gameplay work, not another opening restart/movie microbenchmark
  or small dispatch tweak. Full PRD and goal remain active, no performance gate.

## 2026-09-08 — R702 candidate gameplay and fresh checkpoint

- Previous turn progress: full suite and quiet-interval attribution. Revalidated
 66439/soleDE8E956F, then Play This File(.76,.89)+A76998. Advanced each visually
 observed story page with released A pulses; sessions7916/9636/16461/60100 and
 immediate helpers all exit0. No runtime/settings/source changes or rebuild.
- Reached visible Star Festival on84f0fe05. Story frames near60, gameplay~40–50
 events/sec; not measured display cadence, stutter-free or dispatch gain proof.
- Native menu Save Development Checkpoint created unique44EC9B1E-3DFB-447D-8AED-
 48A748DA0834.sav at festival entry. Final file present (~40MiB), oldDAF88 file
 retained. Latest checkpoint preference now belongs to this app identity. Restore
 of this new file not exercised; no undo-load buffer in this process so far.
- Forward4s20087 exit0 moved Mario visibly toward flower-lined hill, beyond
 starting circular flowerbed. Screenshot generated/candidate-festival-r702.png;
 same ios-runtime-r700.log. No malformed-XF recurrence or new movie callback yet.
 Runtime66439/session56873 remains live; next same-session route to plaza/movie,
 native THP hook test after0030 and return. No first-play/performance gate closed;
 full original goal remains active.

## 2026-09-08 — R701 dispatch comparison, full regressions, quiet observation

- Previous turn progress: candidate deployment and guarded state rejection. Same
  66439/84f0fe05 and selected-file screenshot revalidated. Sample11367 exit0,
  3s/10ms: CPU254 with10ModDispatch/20float-future; Video253 with20EFB completion
  and71condition waits. Old selected-fileR695:CPU254/12Dispatch/21future,
  Video254/18EFB/68condition. These sampled wall-stack counts do not establish
  statistically meaningful improvement, CPU percentages or dispatch as main fix.
- Native menu pause before full repository suite29506. Exit0, generated/check-r701.log
  ends Native diagnostic and hook-lookup regressions passed and Repository safety
  checks passed. Covers new checkpoint, bounded XF context and Dispatch changes.
  This is regression evidence, not game/device/performance acceptance.
- Resumed same game. Quiet45s capture29047 with clock sleep45s and no other calls
  during interval;7wholewindows1704/37.000734=46.053140Hz, speed0.573072–1.002498.
  VMdelta4swapins,0swapouts,0compressions,1447decompressions. No profiler/build or
  settings changes. Earlier candidate selected-file46.5Hz also inconclusive.
  Lack of intermediate screenshot/tool calls did not eliminate slowdown. Host
  scheduling still uncontrolled; do not conclude host is irrelevant.
- Artifacts generated/dispatch-selected-sample-r701.txt,quiet-selected-r701.json,
  check-r701.log. Still66439/session56873/soleDE8E956F, selected file. No source
  change this turn. Next gameplay/native movie hook exercise and candidate-owned
  checkpoint; do not repeat identical selected-file samples or tune small lookup
  paths as though they explain the whole deficit. Original goal remains active.

## 2026-09-08 — R700 dispatch candidate deployed and file-select tested

- Previous turn progress: same-build cross-process checkpoint proof. Revalidated
 65953/soleDE8E956F, candidate84f0fe05. Native pause; backup59472 exit0 preserved
  installed4942411a app, checkpoint and preferences in generated/candidates/
  r695-baseline. Verified backup apphash. No files deleted or overwritten there.
- Native stop65953 runtime exit failed0 at23:28:05.446; then simctl terminate and
  verified no GalaxyPad. Install3224 exit0, installed84f0fe05 verified. Bundle
  8711DD6A-1FD2-4D3A-B8EE-336B1A2BA7AC; data63F66D50-27D8-4C6B-9026-195F10A64B88.
  Save99d432d5 unchanged; checkpoint635bcb4979c223b1fb9dd5d41c8041c547f725536778
  c391503bd10df90f94d2 matches backup. AOT module unchanged3acdcddc.
- Launch56873/PID66439 with standard settings, correct GalaxyPadDevPointerPitch22
  NO flag. Core boots visibly. Native menu restore23:29:46 refused mismatched
  app identity, then resumed. This is intended cross-build guard, not checkpoint
  failure; no source guard bypass, state loaded or metadata rewritten.
- A+B66806 thenpointer(.38,.66)+A6166 reached selected Mario file, screenshot
  shows Play This File. Capture36551 exit0, fivewholewindows1251/26.899989s
  =46.505595Hz, VI~47–51/speed~0.76–0.85. Systemswapin/out/compressions0,
  decompressions1297. No profiler/build during window. No benefit established
  relative to prior54.4Hz; sequential variable-host runs are not controlled A/B.
- Artifacts generated/dispatch-file-selected-r700.json,ios-runtime-r700.log.
  Still66439/session56873/soleSimulator, selected file; no game/movie regression
  acceptance yet, no new candidatecheckpoint. Next representative gameplay and
  native hook exercise, then own same-build checkpoint and dispatch attribution.
  Old app/state retained for rollback. No performance gain claimed; full goal open.

## 2026-09-08 — R699 clean relaunch and same-build checkpoint restore

- Previous turn progress: CPU candidate implementation/tests/build plus host
  pressure evidence. Revalidated63074/soleDE8E956F and current log. Native Stop
  confirmed, runtime exit failed0 at23:22:51.333. Finalpartial176+earlier5415 gives
  nativeTHP5591/0; no audio/display parity claim. GameData.bin99d432d5 unchanged.
- Terminated idle host with simctl, verified no GalaxyPad, then launch12239/PID65953
  same INSTALLED4942411a, hash verified. Did not install built84f0fe05. Same flags
  except accidental redundant GalaxyPadDevNativePointerPitch22 NO spelling;
  actual previous GalaxyPadDevPointerPitch22 NO persists unchanged. Future launch
  should use correct flag. Input file lease expired, no held buttons.
- At visible safety screen/core running, native menu Restore23:24:13.538. Returned
  saved invaded plaza without opening route. Forward3s41564 exit0 visibly moved
  Mario to central ice block, input continues. Savehash99d432d5 unchanged. This
  proves this checkpoint on this same build across processes, not cross-build,
  later native movie lifecycle, audio or original NAND first-play persistence.
- Capture64560 exit0:30s, fivewholewindows1052events/26.599855s=39.549088Hz.
  Systemcompression0,swapins4,swapouts0,decompressions7161. No profiler/build.
  Same wall-time3s movement is not exact-position matching when speed differs;
  do not claim controlled A/B or dispatch optimization gain from OLD build.
- Artifacts generated/ios-runtime-r699.log,relaunch-plaza-r699.json/.png.
  Runtime65953/session12239 still live, same sole Simulator and data583E547C.
  New dispatch candidate84f0fe05 remains built/notinstalled; original exact-app
  identity guard intentionally rejects cross-build checkpoint use. Next deploy
  and measure candidate without losing original checkpoint/save. Full goal active.

## 2026-09-08 — R698 measured host churn; implemented dispatch rejection

- Previous turn progress: actual same-process checkpoint validation. Revalidated
  same63074/DE8E956F, stationary frozen-Toad plaza. Capture71581 exit0:26.886710Hz,
  system298743 compressions/124514 decompressions,zero swapin/out. Screenshot
  confirms stationary scene; no profiler/build during baseline. ChatGPT111%CPU,
  renderer63% in first snapshot; no unrelated process killed or changed.
- Separate sample15437 exit0,259 samples/thread. CPU11 future waits,32 ModDispatch
  under chassis+348; Video134 condition waits,9 depth/Metal waits,45 vertex-loader
  samples. Not exclusive CPU percentages. vmmap91240 exit0:521.3M footprint,
  peak841.1M,writable403.9M swapped category. 'corpse' is snapshot type; verified
  process/log still live. Artifacts plaza-r698.json,plaza-sample-r698.txt,
  plaza-memory-r698.txt. No thermal warning recorded, not no-throttling proof.
- Read checkpoint header/writer:114630753 uncompressed bytes (~109.3MiB). State
  restore retains s_undo_load_buffer, exact live size unmeasured. This is added
  diagnostic overhead, not explanation for pre-checkpoint slowdown. Left semantics
  unchanged rather than discard error recovery or silently lose undo behavior.
- Concrete source mismatch: earlier static min/max guard covered HandlesAddress
  only; Dispatch unconditionally probed hooks and patches. Added rejection AFTER
  runtime_start and pending-return processing so observable callbacks remain.
  Canonical0030-mod-dispatch-fast-reject.patch SHA bba8bb0eaab1664fd78230e979d7
  baa1d559d37606f99802b6da07b467de349e; bootstrap registered and reverse-check pass.
- Expanded actual ModManager sanitizer test: null/startup negative address,
  startup-once/state preservation, actual patch hit, holes/endpoints, pending return
  below/above static range with wrong/correct SP, reload. Test73128 exit0,
  dispatch-tests-r698.log. Existing negative LOOKUP microbench not dispatch/FPS proof.
- Paused native menu before tests/build; core47715 exit0 (SCM bad ^master message
  nonfatal), provision/app4124 exit0. Newcorea5a46e26,candidate84f0fe05, NOTinstalled.
  Same63074/4942411a resumed via native menu after build. No full suite rerun.
  Next justified same-build clean relaunch/checkpoint proof, then candidate
  deployment and measured comparison; exact-app checkpoint guard remains intact.
  Full goal active. No new in-game performance gain claimed.

## 2026-09-08 — R697 actual development checkpoint validation

- Previous turn progress: live same-session movie/gameplay attribution. Revalidated
  PID63074 and sole Simulator; no rebuild/relaunch. Menu paused/input-cleared.
  Re-read guards: unique filename,2GiB free,8entry limit,30s cooldown, exact
  app/module/NAND identity, Simulator-only opt-in. Volume38GiB available.
- Native Save Development Checkpoint requested23:09:04.441, final .sav exists,
  DAF88E8D-F375-4E39-8ED7-353C2CC9FA8A,43,875,609bytes under private data583E547C.
  No existing state or GameData.bin overwritten. Native menu dismissed/resumed.
- Saved-position screenshot, forward3s30619 exit0 visibly moved to central frozen
  Toad. Native Restore Last Development Checkpoint requested23:10:23.358; visible
  original Mario/camera position returned. Forward3s40425 exit0 moved again, so
  restored game accepts input. All leased holds released. Same process/build only;
  not cross-process/cross-build, touch/control, movie-state or audio validation.
- Savehash99d432d5 unchanged. Restore warning caused by upstream SDIOSlot0::DoState
  read→OpenInternal creating a missing SD image. Located app-private Load/WiiSD.raw
  using no-ignore file listing; normal rg omitted it. Do not claim restore has no
  filesystem side effects. No files deleted or unrelated settings changed.
- Artifacts generated/checkpoint-before-r697.png,checkpoint-restored-r697.png,
  checkpoint-input-after-r697.png, same ios-runtime-r695.log. No graphics assertion
  seen. Runtime63074/session23593 remains live near central frozen Toad; checkpoint
  preserves original invaded-plaza entry position for same-build repetition.
- Next gameplay CPU/graphics wait measurement at retained scene; do not replay
  opening for routine diagnostics. Cross-process checkpoint test remains pending
  until a justified clean relaunch. No new source change/performance gain, full
  original PRD and goal remain active.

## 2026-09-08 — R696 preserve session and separate movie/gameplay cadence

- Previous turn progress: deployed logging plus live file-select/capture evidence.
  Revalidated63074, same sole Simulator and recent log before input. Selected
  Play This File(.76,.89)+A88704, advanced visible story pages, then traversed
  festival via bounded leased input. Camera rotations/railings required route
  corrections; all holds completed/released. This is not UIKit control proof.
- Story screens~60 events/VI~59.94/speed~1; gameplay variable~35–50. File-select
  speed had already fallen on unchanged idle scene before leaving, so R695's
  faster result cannot be claimed as logging-caused performance improvement.
- Reached invasion movie after plaza left3s34640. Visually verified ships then
  Bowser, and subsequent return to invaded plaza. Native log first5s window ends
  epoch1788926648.377083; last emitted18th window ends1788926733.747110.
  Full18 emitted windows5415calls/0fallback; weighted rounded wall1.552835ms,
  CPU1.540969ms. Maxwall18.624ms pairedCPU2.679ms, not all active execution.
  Final partial window unflushed; do not promote5415 to full5591 acceptance.
- Movie capture30572 exit0:45s observed,7whole windows2241/37.400666=59.918719Hz,
  VI59.939–59.940/speed0.997942–1.000006. Post-movie capture17589 exit0:30s,
  4whole windows767/21.599933=35.509369Hz,VI35.017–36.584/speed0.570–0.636.
  VM movie/post swapins0/8,swapouts0/0,compressions3358/10625,decompressions217/4033.
  No profiler/build or unrelated-process mutation. Sequential workload comparison
  not controlled host A/B; host pressure and audio/display cadence remain open.
- Artifacts generated/movie-r696.json,post-movie-r696.json/.png and same live log.
  No new product source/settings. Still63074/session23593, no crash recurrence,
  same Simulator, stationary invaded plaza, no checkpoint action invoked.
  Updated PERF/priority queue: retain this scene, validate same-build checkpoint,
  then target gameplay CPU and graphics waits. Do not restart movie for routine
  diagnostics. Full goal active; this is stronger attribution, not performance
  acceptance or a graphics stability fix.

## 2026-09-08 — R695 deployed diagnostics and observed file-select slowdown

- Previous turn classified progress: tested, built and provenance-registered
  failure-only logging. Revalidated no game/booted Simulator, save99d432d5 and
  module3acdcddc. Provision/app build exited0; app4942411a, coreabc85567.
  Sole Simulator boot/install67327 exit0. Installed app hash matches candidate.
- Launch23593/PID63074, same R693 settings and checkpoint opt-in but no checkpoint
  action. New log generated/ios-runtime-r695.log. A+B reached file list; second
  screenshot confirmed it before bounded30s capture43632. 1567 events/26.500098s
  =59.131857Hz. VI/speed rolling estimates are not exact same-window counters.
- Pointer(.38,.66)+A action79839 exit0 selected Mario file, visibly showing
  Play This File. Capture14644:1436 events/26.399293s=54.395396Hz, rolling speed
  0.787319–0.913292. No malformed XF through22:53 local, well beyond previous
  ~24s-after-selection failure. This single non-reproduction is not a fix.
- Capture VM deltas respectively:swapins4/4,swapouts0/0,compressions0/0,
  decompressions4595/4507. Logitech updater still ~57–60% CPU in first capture.
  No unrelated process killed, no concurrent build/profiler in these captures.
  Cannot attribute inter-run improvement to logging, cold boot or host state.
- Separate sample46274 exit0,3s at10ms:254 samples/thread, CPU21 future<float>
  waits under chassis_dispatch, Video18 PeekEFBDepth→Flush→Metal completion,
  68 timed condition waits. These sampled wall stacks do not establish exclusive
  CPU percentages or the sole slowdown cause. Save same99d432d5; old container
  path disappeared because install relocated data to583E547C-ED32-4AC8-B7CC-
  067D9E9D4A58. Re-resolved through simctl before verification.
- Artifacts generated/file-select-r695.json, file-selected-r695.json,
  file-selected-r695.png, file-selected-sample-r695.txt and runtime/build logs.
  Simulator window rendered portrait while app logs landscape; no UI acceptance
  claim. Runtime63074 still live at selected-file screen, sole Simulator preserved.
  Next use this run for progression/useful checkpoint validation with failure
  context armed; no unchanged rebuild/reboot to chase a nonreproduced failure.
  No product performance or graphics stability gate closed; full goal active.

## 2026-09-08 — R694 failure-local graphics diagnostics

- Hypothesis: the R693 malformed XF header may be a command-boundary/producer
  defect. Existing log records only cmd2; it cannot locate the failed command in
  the decoder input or establish surrounding bytes. No cause assigned to THP,
  checkpoint code or host contention.
- Added failure-only DescribeCommandWindow and propagated each Run input start.
  Direct RunCommand callers default to their own span start; recursive display
  lists carry their own start. Reads are capped at32 preceding and32 available
  following bytes, no reads outside that input. Existing ASSERT_MSG evaluates
  formatting only on failure. No command-size, callback or recovery change.
- Canonical patches/ModernGekko-dolphin/0024-malformed-xf-context.patch registered
  with hash8ae3658143fc2f7cfaf3e2f35f48a1921d81b09088ebbf8e4607429902d94ab4.
  Reverse-apply verification and bootstrap/check script syntax passed.
- tests/test-xf-context.py exercises the extracted actual helper with formatting
  stubs, ASan/UBSan, all offsets in spans1–128. PASS, wired into repository checks.
  This verifies bounded accesses, not actual fmt output or live FIFO behavior.
- Simulator core build11559 failed because ArrayToString is global, not Common.
  Corrected source, canonical patch and test stub; rebuild96210 exit0, private
  generated/xf-context-build-r694b.log. No Simulator/game running during build.
  No core provisioning, app rebuild/install or live reproduction yet; installed
  R693 artifact/save unchanged. Full suite not rerun in this step.
- Updated ranked performance queue: graphics stability, then visible scene plus
  VI/speed/CPU-wait attribution; preserve completed native-movie evidence without
  claiming audio/display parity. Next provision/build/install exact diagnostic
  candidate and reproduce file-select failure with fresh private log. Full goal
  remains active; no frame-rate improvement claimed.

## 2026-09-08 — R693 deploy, identify simulation slowdown and graphics failure

- PriorR692progress guardtests/build. Revalidated52374/candidate,pausedandbackedup
  save togenerated/save-backups-r693/GameData.bin,99d432d5…ab64f. NativeStop
  clean22:34:43failed0; THPaccepted12667fallback0matchesA5591+B7076 framecounts.
  Not exactframeuniqueness/displaycadence/audio proof. Oldruntime terminatedbefore
  install98094exit0; installed43b624f6verified,NANDhashunchanged.
- SoleDE8E956F installedbundleD02C68A8-B335-471A-A952-826B9132D946,datacontainer
  7F79188A-A4E8-4844-8FCD-707B50FD74E5. Launch12825/PID61558 sameR679flagsplus
  -GalaxyPadDevCheckpoints YES, logios-runtime-r693.log. A+B81716exit0 reaches
  visiblefileselect; pointer(.38,.66)+A12747exit0. Nocheckpointactionselected.
- Cadenceloglive: startup~60frameevents/59.94VI/speed1; filelist~23–32frameevents,
  VI20–33,speed0.33–0.56. Simulationcadenceisactuallyslow; notmerelyhostFPSlabel.
  RollingVI/speed estimatesnotexact5swindowcountermatches; no profiler/build.
- At22:37:46.014 firstcausalvisiblelog: GX_LOAD_XF_REG assertsstream_size_temp<16,
  cmd2=0x61E30E70,OpcodeDecoding.h166. Sourceinterpretscmd2>>16 asXFcountfield.
  SpringBoardvisible,61558absent,launchsessionexit0withoutcleanruntimeexitmarker.
  NoGalaxyPadDiagnosticReport found. No savestateaction/THPmoviecallback preceding
  failure. Do not attributecausetocheckpointrestore or suppressassertion.
- Savehashstill99d432d5. ShutdownsoleSimulator; noapp/profilerleft. PriorR600
  opcodeFFincidentiscontext,notestablishedsamecause. Nexttarget graphicsstream
  reproduction/earliestinvalidproducerboundary beforecheckpointorperfacceptance.
  Fullgoalactive; no newproductsourcechange. FullsuiteR690precheckpoint remains.


## 2026-09-08 — R692 guarded same-build checkpoint restore

- R691progress saveactionbuild; revalidated52374. Added privatecheckpointhelper:
  bounded64KiBstreamSHA256(app,module,NANDGameData), UUIDbasenamevalidation and
  exactdictionaryidentity. Save storespendingmetadata inNSUserDefaults; no claim
  filecompleteatrequest. Restorechecksidentity,runtimestate,finalregularfile,size
  32..512MiB beforeupstreamStateLoadAs. Upstreamfinalrename distinguishespending
  temporarywrite; corestillvalidatesformat/errors. DefaultoffSimulator-only.
- Sameappbinary/module/NANDbytesrequired; doesnotpermitcross-buildrestores and
  doesnotreplacein-gamepersistenceproof. NewhelpertestscompileactualFoundation
  codewithASan/UBSan:abcSHA,missingfile,changedidentityfields,nil,UUID/traversal
  rejection. Sourcecontractchecks requestgates beforeStatecalls; allpass.
- PausedGatewayduringbuild74979exit0; candidate43b624f6…e63806notinstalled,
  checkpoint-restore-build-r692.log. Menu dismissed,same52374/c00098d3resumed.
  No checkpointcreated or restoredyet. LatestfullsuiteR690beforecheckpointchanges.
- Next cleanbackup/stop/install isjustifiedbycompletedcheckpoint/cadencecandidate,
  thenactualsave–move–restoreandVI/speedobservation, notanotherunusedloggingbuild.
  Originalfirstplay/GameDatasave/device/audio/performancegatesremainunchanged.


## 2026-09-08 — R691 private development checkpoint save request

- R690progress fullregressionpass; revalidated52374 andstatus. ReadStateSaveAs:
  schedulesCPUthread, capturesbuffer, asynchronouscompression, tempfilerename.
  StateLoadAs waitswriteworker; nativeDoState restoresregisters/cache/MMU,
  JitInterface ownsStaticRecomp and ClearCache invalidatesverifiedchunks; next
  nativeburstSyncIn copiesregs/GQR/FPSCR/timebase. No actualmobileloadproof yet.
- Added opt-inGalaxyPadDevCheckpoints Simulator-only menu action to existingmenu,
  delegate→hostrequestDevelopmentCheckpoint. Product/defaultmenuunchanged. Unique
  UUIDprivatepath, no existingcheckpointoverwrite, no automaticdeletion;2GiBfree,
  count<8directoryentries and30smonotoniccooldown gates. Running/paused/notstopping
  runtime required; inputcleared. SaveAs requestlog explicitlynotcompletion.
- Save-onlyfirststep; restoreandidentityvalidation stillpending. Nevercountthis
  as retailNANDsave/reload. Sourcecontracttest assertsgates/uniqueprivatepath/no
  load/delete/requestwording; passes, wiredintosuite. Notexecutedwriteproof.
- Pausedviamenu forSimulatorbuild3844exit0,checkpoint-build-r691.log; candidate
  2a50d777…cc09f6 built,notinstalled. LastfullsuiteR690beforethesechanges. Resumed
  samec00098d3/PID52374afterbuild. No actualcheckpoint,gameinput/defaultchange.
  Next guardedrestore/identitycontract thenjustifieddeployment;fullgoalactive.


## 2026-09-08 — R690 complete regression pass, preserve current gameplay

- Previous turnprogress:cadencelogging/labelcorrection build. Same52374revalidated,
  native menu pausedGateway while hostregressions ran. No newgameinstance orsim.
- bash scripts/check-repository.sh > generated/check-r690.log,1684exit0.
  Endmarkers Native diagnostic and hook-lookup regressions passed; Repository
  safety checks passed. Includesnewlivecaptureparsertests, input/import/save
  isolation,timing,memory/FP/resolver and nativeTHPtiming checks. These are not
  wholemovieparity, livecadencelogvalidation, playablemobile or physicaldeviceproof.
- Checkedcheckpointavailability: DolphinState.h exposesSave/Load, macOS journal
  hasemulatorstatefixtures; no exposedStateSave/Load foundin iOShost/runtimeAPI.
  A developmentcheckpoint path could preservecomparablescenes across necessary
  builds; it mustnotbe substitutedforin-gameNANDsave/reloadacceptance.
- Menu dismissedaftersuite; samec00098d3/PID52374Gatewayresumed.02711110candidate
  remainsnotinstalled; no code/settings/inputchange thisturn. Fullgoalactive,
  performance/stability/audio/firstGrandStar stillunaccepted.


## 2026-09-08 — R689 distinguish frame events, VI and simulation speed

- Prior turn observedtraversal but no newgate;52374live revalidated. Read diagnostic
  inputdevice/mixer/config: logicalA1,Z8,MoveY+up/−down consistent, timer0.1s.
  This does not prove each downstreammotion event; no inputrewiring performed.
- Importantcounter audit: runtime after_frame_event incrementsdiagnosticframecount,
  not after_present_event. HistoricalpresentedFPS labeloverstates evidence.
  Added counter_source=after_frame_event to5slogs, retainedlegacy presentedkey for
  parsercompatibility; AX now saysframeeventspersecond. Prior numericalmeasurements
  remain recorded but are engineframecadence, not physical/displaycompletionproof.
- Existing PerformanceMetrics GetVPS/GetSpeed areany-thread getters (atomicrolling
  values), so added smallhost cadenceEstimate method and5sopt-in logfields
  vi_rate_estimate/emulation_speed_estimate. No coreABI/perframeinstrumentation.
  They are upstreamrollingestimates, not countsalignedto5swindow. MissinghostNAN.
- Capture helper retains optionalmetrics;3parser tests pass old/newlogs and prior
  weighted/boundarychecks. Menu paused forbuild64826exit0, thenlabelcorrection
  build99183exit0; final02711110…70d879,notinstalled. Logs cadence-build-r689{,b}.log.
  Fullsuite notrerun; latestR678. Same52374/c00098d3 resumed via menudismiss.
- Next deployment can distinguish slowVI/speed from downstreamframe deficit;
  not restartingGateway solelyforlogs. Fullgoal/performance/audio/save remainopen.


## 2026-09-08 — R688 landscape Gateway traversal and tunnel transition

- Prior turn yieldednegativeprofiler/limitednativeevidence; parked unchanged
  attachment retries. Verified52374,soleDE8E956F. Rotated using Simulator toolbar,
  gameviewport/overlay resize visibly; no rebuild/runtime restart.
- Short leasedinputs: left2s17302,down+A2s53011,right/up+A1s62609 barely clearroof;
  Z+down1s68363 then showsgrass/rocks,up1.2s48512 showsdarkhole/bluebubble. All
  helpersexit0andneutral. SubsequentCUA showsMario back onrockysurface byhole.
  Capture generated/ios-gateway-tunnel-r688.png. This is observedtraversal, not
  rabbitcatch, GrandStar, reliablecamera/jump or UIKit inputacceptance.
- Read GalaxyPadSimulatorInput.h and main timer: fullsnapshots,expiry/lifecycle
  notBefore invalidateoldholds. No mappingchange; sourceinspection doesn't prove
  downstreamrelease/collisioncorrectness. Navigation remainsawkward;16–38FPS
  momentaryreadings still unacceptable. Avoid labeling everyblockedmove a freeze
  or assuming collisionexplainsallbehavior. Same runtime retainednearhole.
- No source/build/profiler/defaultchange, fullsuite remainsR678. FullPRDactive;
  continue actualfirstplay checkpoint, not anotherunchangedmovieboot.


## 2026-09-08 — R687 bounded profiler alternative fails; native matrix subset checked

- R686 progress: exactentrymapping. Verified52374,38GiBavailable and CUA stationary
  Gatewayroof/hole scene. Attempted5s CPUProfiler defaulthost target58651exit21:
  cannotfindPID. Runtimeps stilllive. This repeats knownR534defaulttarget limitation;
  read journal before another attempt. Do not restart game on profiler lookupfailure.
- Tried different TimeProfiler explicitsoleSimulator,5s requested.39942 attached
  but stalled beyondlimit. ps57482 showed41s; INTthenTERM stopped onlyrecorder.
  Exit1,no usabletrace; pgrep xctraceempty,52374stilllive. Incomplete ignoredtrace
  generated/gateway-time-r687.trace retained. No unchangedretry or acceptanceclaim.
- Verified historicalnative moduleSHA1fb635f7 matchesR423profile; its retained
  disassembly independently shows matrixprimarybody5f66ee0..5f676cc (laststore
  tails toshared5f637d0). Read exactspan and matched alignedPCs in
  generated/fprf-site-r423.json:23/28822CPUleafsamples,~0.08%. This excludes shared
  and outlinedhelpers, so is not routine total/upperbound or currentSimulatorcost.
- Decision: no evidence yet for matrix-only replacement as majorperformancefix.
  Park further narrowaudit/attachmentchurn; preserve diagnostics for futurebetter
  measurements. SameGatewayruntimeposition, no app/core/source/settings change.
  Fullgoalactive, latestfullsuiteR678; no audio/save/performance gatepromotion.


## 2026-09-08 — R686 exact matrix code and entry-address audit

- Prior turn progress: unique exactDOLcandidate, continued Gateway traversal.
  Same52374live revalidated. No game input/settings, build, install or restart.
- Confirmed active Simulator CMake GENERATED_DIR is modules-scale-r387; read
  generated804b683c..688c source:21cycle entry, PSQGQR0 loads/stores, FPavailability,
  exception exits, pairedmultiply/madd/sum and LR dispatch. Full21words match
  Petari reference operand sequence; strengthened audit-matrix-boundary.py.
- Disassembled exact active3acdcddc modulechunk, generated/matrix-chunk-r686.asm.
  New hash-gated map-matrix-native-entries.py parses MachOsegments, checks six
  table-dispatch machinewords, maps21guestentries through unsigned16offsettable.
  Primaryentry5f66ef0 charges21; nextguest entry5f844d4 charges20 then branches
  to5f67054. Interior-entry stubs invalidate a simplistic native min/max range.
  Artifacts matrix-boundary-r686.json and matrix-native-entries-r686.json; audit
  session47419exit0. No hook or performance gain claimed.
- R676 sample has10aggregated parentchunk observations with ellipsized PCs, not
  enough to assign routine cost. Next build precise body/controlflow ownership
  before matching detailed profiler PCs; no replacement on staticcallcount alone.
  Whole matrix FP/GQR/FPSCR/exception/alias ABI remains required. No full suite
  this read-only runtime turn; latestR678. Gateway position unchanged, fullgoalactive.


## 2026-09-08 — R685 retain progression, locate larger math boundary

- Previous turn progress: mixed benchmark prevents unsupported promotion.
  Same52374 live and lateststate confirmed. Up4s29790 did not clear rock;
  down-left4s25699 moved around underside, disproving a total input freeze.
  Right5s55102 returned toward rabbit tutorial; left+A2s96922 moved to rocky
  side. C0.3s and diagonal0.7s completedneutral. Final visible location small
  roof/platform beside darkhole/coin.18–30FPS; no catch/jump acceptance claimed.
- Stopped extended navigation and investigated larger math boundary. No prior
  PSMTX native replacement found in searched app/scripts/config/runtime sources.
  Petari mtxvec.c provides paired-single PSMTXMultVec instruction reference;
  Korean symbol804b8cc0 is not our region's address.
- Added read-only scripts/audit-matrix-boundary.py: exactDOLSHA required, scans
  executable sections for threeinstructionregister/offset prefix,84bytes andblr,
  requiresunique candidate. FoundRMGE804b683c,hash0e86b496…392e78,85static direct
  calls. generated/matrix-boundary-r685.json includes21words/callsites. Prefix
  correspondence is not full semantic/ABI proof. Parentchunk804B60A0 is sampled
  historically but this does not attribute cost to matrix routine specifically.
- Next map emitted code/sample PCs and establish function cost before native
  replacement; preserve pairedfloat rounding/GQR/FPSCR/memory/alias semantics.
  No hook or source/runtime optimization installed. Same game retained; fullgoal
  active. No fullsuite this turn; lastR678, new audit completedexit0.


## 2026-09-08 — R684 benchmark normal candidate, retain stable runtime

- R683 was progress (parity plus actual codegen). Revalidated52374, paused via
  native menu without restart. Added --benchmark to extracted-reader probe:
  volatile function dispatch prevents inline callsite specialization, original
  and candidate use same deterministic input and last-vertex cache frequency.
- Initial8x2x5M runs sampled always-zero lowfloatbyte checksum; superseded by
  full3word nonzero checksum and50Mcalls/run. All500000 sanitizer cases stillpass.
  Final77132exit0, generated/normal-output-benchmark-r684b.csv: checksums identical
  323841151863871488 for everyrun; candidate faster6/8pairs, slower2. Original
  wall4.54–7.30ns/call versuscandidate4.06–6.47; significant host/order variation.
  This is isolated native conversion, not full indexed-loader, Simulator or FPS.
- Decision: keep candidate isolated, no default/vendor/app integration or routine
  runtime rebuild justified. Fewer staticinstructions does not guarantee overall
  frame-time improvement. Avoid further tiny converter loop churn as main queue.
- Menu dismissed, same Gateway resumed. Diagnostic A+moveY0.8 held2s thenneutral,
  helper39629exit0. Fresh screen shows overhead camera/Mario at rock, tutorial
  prompt gone; not frame-perfect jump or UIKit-touch acceptance. Observed16FPS.
  No save/reload or performance gate promotion; continue current first-play state.


## 2026-09-08 — R683 narrow software-normal codegen candidate

- Previous turn progress: scene-correlated host capture.52374live reconfirmed.
  Read EFB inventory: R595 already rejects duplicate Flush;R604 waits sum10.9%
  of its file-select capture, insufficient to explain full cadence deficit.
- Reused R676 profile rather than collecting another generic sample:34 loader
  child samples include13 Normal_ReadIndex<u16,s16,1>,10TexCoord_ReadIndex;
  another8 in loader loop. Samples are wall observations, not exclusive CPU cost.
- Inspected real Normal reader and ARM64 object: global output cursor reload and
  store repeated per component. VertexManagerBase owns separate CPU vertexbuffer;
  candidate's contract excludes output aliasing loader/caches/input. Prototype
  writes components via local pointer and commits final cursor once, preserving
  conversion/cache order. No vendor source or installed candidate modified.
- Native menu blocked1/inputcleared at21:47:19; preserved Gateway during compilation.
  tests/probe-normal-output.py extracts current actual reader, compares isolated
  candidate500000times underASan/UBSan across u8/s8/u16/s16/float, normal/NTB and
  offsets0/3/6, remaining0/1/2/3/100, unalignedoutput, full buffer guards and caches.
  All pass90467exit0; actual Simulator compile11814exit0 using original compileDB
  command and private staged translation unit. Normal-s16/index16 static code
  shrinks63→55instructions, redundant cursor updates visibly removed. No runtime
  FPS or throughput claim. Source hash6e611de1…c5b5d8 unchanged.
- Evidence generated/normal-output-r683/{VertexLoader_Normal.cpp.o,
  normal-s16-index16.asm}. No full suite this turn; latestR678. Menu dismissed
  after compilation; same Gateway runtime resumes. Next benchmark isolated actual
  normal reader and expand integration proof only if savings justify it; retain
  current gameplay route. Full PRD/audio/save/performance gates remain open.


## 2026-09-08 — R682 correlate visible gameplay slowdown with host deltas

- Prior turn progressed to Gateway. Same52374, soleDE8E956F confirmed; CUA shows
  stationary jump tutorial, not a freeze or active movie. No gameplay input change.
- Hypothesis: growing swap/compression might explain this stationary slowdown.
  Added scripts/capture-live-slowdown.py, a10–45s profiler-free recorder with PID
  identity check, private exclusive output, dated scene label, bounded host
  snapshots and complete-window frame aggregation. Reuses existing app logging;
  no extra per-frame instrumentation/build/install. Local log timezone assumption
  and ps CPU averaging limitations explicitly recorded.
- python3 tests/test-live-slowdown.py passes2tests: VM parser, weighted FPS,
  boundary-window exclusion and empty result. Wired into check-repository.sh.
  Full suite not run concurrently with game; R678 remains latest full result.
- Capture62589exit0: generated/ios-gateway-slowdown-r682.json, four fullwindows,
  663/21.400632=30.980393FPS. Swapins/out/compressions delta0; decompress+288pages.
  Growing swap traffic does not explain this interval. This does not exonerate
  scheduling/host load: updater and rendering processes are active. No unrelated
  process mutation. CUA before/after confirms unchanged scene; screenshot call
  itself is not claimed zero overhead. No FPS improvement claimed from tooling.
- Same live runtime retained at jump tutorial for continuation. Prior measured
  CPU work and synchronous depth-readback costs remain the gameplay optimization
  priorities, with correctness required; no fake depth or JIT vertex loader.


## 2026-09-08 — R681 castle-to-Gateway transition observed

- Previous turn made progress: paired CPU/wall outlier and movie return. Same
  PID52374/session42232 revalidated. Moved away from house wall, through plaza,
  along castle approach and bridge; camera/obstacle corrections required. Actual
  mapping source is MoveX+/− right/left and MoveY+/− up/down; no mapping bug proven.
- Same candidate triggers castle-lifting cutscene at entrance. Native decoder
  windows continue with zero fallback; sampled presented windows59.81/60.00FPS.
  Source PrologueB metadata640x368,7076frames,118.051387s from pinned ffprobe;
  visible sequence consistent with it, not runtime filename/duration proof.
- After fade, CUA shows Mario and rabbit introduction at Gateway. Captures
  generated/ios-castle-movie-r681.png and ios-gateway-arrival-r681.png. One leased
  A pulse advances dialogue and releases neutral. Same game remains live.
- No build, source/settings change, profiler or restart. Ground gameplay still
  slow; no Grand Star/save/reload/audio/full-input acceptance. Continue Gateway
  first-play route rather than restarting the opening. Full original goal active.

## 2026-09-08 — R680 live outlier is mostly non-CPU elapsed time

- Prior turn progress: verified install and gameplay. Revalidated52374 and sole
  DE8E956F; continued leased-input route from dock through village/plaza. Native
  movie visibly advances at~60FPS and returns to post-invasion gameplay.
- Eighteen emitted five-second windows5409calls, zero fallback; weighted rounded
  window means2.7913605ms wall/2.7674348ms CPU. Maximum25.990ms paired with2.091ms
  CPU at epoch1788920046.422117 window. Difference23.899ms is outside active CPU
  execution; cannot distinguish scheduler, blocking or another host wait from
  these counters. R677's63ms incident itself remains unattributed.
- Last partial window not flushed: do not assert5591/0 from this still-running
  session. No profiler/build, code/settings changes, unrelated-process mutation,
  or promise of stutter-free/audio-correct playback. Screenshot
  generated/ios-cpu-timing-return-r680.png; log generated/ios-runtime-r679.log.
- Kept game running after movie; forward10s32149exit0 visibly moved Mario to
  opposite plaza edge. Gameplay still14–29FPS in observed windows. Continue route
  to castle/Gateway rather than another timing-only restart; full goal remains.

## 2026-09-08 — R679 deploy verified CPU-time diagnostics

- Previous turn made progress: timing regression and full repository suite passed.
  Revalidated no game/booted Simulator, candidate c00098d3 hash and suite exit0.
- Boot/install30368exit0 on sole DE8E956F; installed binary hash matches. Save
  unchanged99d432d5 in new data CC387840-B2E8-4E08-BCFC-0535FF946ACE.
- Launch42232/PID52374 with prior flags and THP_TIMING1; runtime log
  generated/ios-runtime-r679.log. Visually verified files/story/Star Festival.
  Additional A pulses were needed as this run's story cadence was slower; both
  bounded helpers42178/60176 exited0 and released input. No profiler or build.
- CPU-time instrumentation awaits actual movie calls. Continue same run through
  movie, then retain progression into first-play rather than stop for another
  routine instrumentation change. No performance/audio gate promotion; full goal
  remains intact. No unrelated apps modified despite observed competing CPU use.

## 2026-09-08 — R678 validate attribution logger and rerun regressions

- Prior turn progress: movie latency outlier and CPU-time build. Revalidated no
  running48962 or bootedSimulator. Timing source showed unload span included idle;
  use window_end updated by dispatch and report idle_since_last_call separately.
- tests/test-thp-timing.c includes actual mod with deterministic clock/decoder
  stubs. ASan/UBSan:paired63mswall/3msCPU, separate12msCPU aggregate, failed clock,
  fallback, reset, periodic flush and disabled zero-clock-overhead checks pass.
  Output verifies0.074sdecode span vs60s idle. No movie timing inferred from fixture.
- tests/test-native-diagnostics.sh builds sanitized timing and mod-lookup tests,
  validates emitted fields, wired into check-repository.sh.56122exit0 plus candidate
  rebuild, appc00098d3…831a4; generated/thp-timing-build-r678.log. Not installed.
- Full suite40073exit0, generated/check-r678.log ends Repository safety checks
  passed. No game or extra build during suite. This does not replace native-THP
  image parity, audio, runtime or physical gates. Next live CPU/wall spike evidence.

## 2026-09-08 — R677 timestamped movie slowdown captured

- Prior turn progress: live candidate/diagnostics and gameplay sample. Same48962
  revalidated; software VertexLoader is intentional runtime no-JIT setting.
  Read priorR458–R470: generic lookup/layout/helper probes have negative history;
  do not repeat them from coarse new samples. No vertex source change.
- Host snapshot showed ChatGPT113%,Logitech updater64%,WindowServer42%CPU;
  pmset had no recorded warnings (not proof against throttling). No apps killed.
  Continued existing route, bounded leased actions, movie visible with new timings.
- Nineteen windows total5591calls/0fallback at unload. Means2.333–3.884ms, rare
  max63.209ms in epoch1788918573.546146 window; overlapping presented54.423FPS,
  min43.636 then60.004 recovery. Screenshot generated/ios-native-timing-r677.png;
  full log ios-runtime-r676.log. Movie→gameplay confirmed; latter again~25FPS.
- These are dispatch wall times, not CPU-only. Added CLOCK_THREAD_CPUTIME_ID
  opt-in count/mean and cpu_at_max_wall_ms, with-1forunavailable. Build10937exit0,
  candidateb8bf6d05…fa1077, not installed. Logs thp-cpu-timing-build-r677.log.
- Clean stop20:51:32.974failed0;5591/0, terminated shell and soleSimulatoroff,
  save99d432d5 unchanged. Exact movie/audio timing not proven: unload window69s
  includes202calls plus post-movie idle. Cross-logger mono origins differ; use
  epoch/date for correlation. Next CPU-vs-wall spike classification using new
  diagnostics at next justified launch; no default or device promotion.

## 2026-09-08 — R676 live candidate and five-second slowdown evidence

- Prior turn progress: fast-rejection runtime build. No live game/Simulator on
  recheck. Preserved old installed R672 app in generated/candidates/r672-baseline;
  copied hash7d14ec65 verified. SoleSimulator boot/install41892exit0. Installed
  candidatef55578e4 verified, module3acdcddc unchanged, save99d432d5 unchanged in
  relocated dataF0BF4B9E-A33B-4015-8102-9A1FC368FAEC.
- Launch27479/PID48962 with same flags plus THP_TIMING1. R673five-second logging
  active; title/files→existingMario→story→spawn. Input63699exit0 ends neutral.
  File windows42–55FPS and minima35; gameplay31.66–37.27 and minima24.55–25.
  Timer observation intervals1.1s. No same-scene controlled gain claim.
- Sample73706exit0 generated/ios-gameplay-r676.sample.txt:250samples/thread,
  CPU18futurewait/video18depth-Metalwait; hooklookup6, videovertexloader42.
  Still distributed translated work and dispatch; do not spend more turns on
  lookup microbenchmarks. No movie acceptance yet in this run.
- Host snapshots01:33:59→01:36:19UTC:swapins539364→539364,swapouts923488→923488,
  compressions143535153→same; decompressions82469601→82484806 (+15205pages).
  This interval does not support blaming growing swap for the lag, but it is not
  per-process memory/scheduling proof. No unrelated apps changed or killed.
- Leave sole game running at Star Festival spawn for continuation, no rebuild
  during measurements. Next same run movie diagnostics plus larger CPU/vertex/
  readback attribution. Native movie success fromR674 remains, audio gate open.

## 2026-09-08 — R675 remove avoidable hook-lookup work

- Prior turn progress: full native movie5591/0 and60FPS windows; gameplay bottleneck
  sampled. Revalidated no bootedSimulator, dirty state preserved, ModernGekko pin
  0514d9f unchanged. Source shows HandlesAddress hash checks every dispatch.
- Inclusive min/max for static patch/hook addresses eliminates impossible hash
  lookups; pending returns still checked everywhere. Reset bounds on unload.
  Canonical patches/ModernGekko/0029-mod-address-fast-reject.patch SHA53a3823a…e6c1;
  reverse application check passes, bootstrap hash/application/scope registered,
  bash syntax passes. Bootstrap itself not run (avoids unrelated graph changes).
- New tests/test-mod-address-lookup.cpp exercises exact boundaries, misses inside
  range, returns below/above range including0/fffffffC, and unload/reload. Initial
  compile fixed array assignment; sanitizer exposed test descriptor lifetime after
  manager destruction, fixed declaration ordering. Final34422exit0 ASan/UBSan.
  Release negative-lookup benchmark29149/82977exit0:20M0.143550→0.035385seconds.
  Not an FPS claim; this cost was only a fraction of sampled gameplay work.
- Core37330exit0 (SCM generator prints bad revision master but build completes);
  provisioning and candidate relink74558exit0. Logs generated/mod-lookup-{core,
  provision,app}-r675.log. Corefc37721c…8eb59, appf55578e4…6b9bf.
  No installation/runtime/default THP promotion or device changes. Next live
  same-scene comparison; full PRD and movie audio/timing gates remain open.

## 2026-09-08 — R674 native movie live success; gameplay remains slow

- Prior turn progress: bounded logging build. Revalidated same46228, soleDE8E956F;
  preserved installedR672 binary. Leased A pulses advance story, inspected route
  through hill/dock/village/plaza; final left5seconds triggers invasion.
- Movie first native frame accepted; complete windows1827/30.599504=59.706850FPS,
  1847/30.800243=59.967060FPS. Visible movie progression then return to gameplay.
  Unload after clean stop reports accepted5591/fallback0, matching source5591frames.
  Runtime exit20:22:47.440failed0. Not exact-duration or audio-sync acceptance.
- Three-second10ms CPU samples: gameplay8361exit0 vs native movie51805exit0,
  generated/ios-gameplay-r674.sample.txt and ios-native-movie-r674.sample.txt.
  Gameplay CPU29/250floatfuture wait, video28/250EFBdepth→Metalcompletion;
  distributed generated work and dispatch costs. Movie native decoder67/256CPU
  stacks and47/256PrecisionTimer. Sample percentages are not exclusive CPU cost.
- Screenshots generated/ios-native-movie-r674.png and return counterpart. Return
  window24.935638FPS means gameplay bottleneck persists independently. Attempted
  brief post-return move/A; screenshot alone does not prove jump behavior.
- Stopped via menu/confirm, terminated app shell and shut downSimulator. No build
  or other runtime competed in measurement. Next dispatch-path investigation and
  bounded R673 timing at next launch, preserving full PRD and audio/device gates.

## 2026-09-08 — R673 bounded diagnostics and live slowdown observation

- Same sole Simulator/PID46228, no restart. Cleared communication prompt with A,
  A+B to files, selected existing Mario save and Play This File via leased input.
  CUA confirms story pages; no native movie acceptance yet. R672 log windows
  ~57–59FPS at title,49–52 near file selection; no claim these isolate host cause.
- Added five-second frame summaries and UI observation-delay maximum; native
  timing opt-in records bounded mean/max dispatch and rejection counts with epoch
  and monotonic stamps. Rejected dispatch excludes later original decoder cost.
  Updated performance plan to correlate visible playback and source duration,
  audio and timed host deltas rather than trust average FPS alone.
- Paused game via menu before incremental build; CPU1.9% during build. Build61521
  exit0, generated/thp-logging-build-r673.log; candidateaca244d2…2d099. Resumed
  existing run afterward. New binary NOT installed yet; preserving route progress.
  No default decoder activation, physical candidate, save or reference edits.
- Next same live run through story to native movie; install diagnostic candidate
  at next justified restart with SIMCTL_CHILD_GALAXYPAD_THP_TIMING=1. Full goal
  active. Logging build success is not runtime validation or performance acceptance.

## 2026-09-08 — R672 candidate links and launches on sole Simulator

- Prior turn progress: dependency/wiring, link diagnostic. Replaced inaccessible
  module-journal helper with actual chassis external-write/MMU callback and exact
  reservation invalidation. Native disabled for configured lockstep. Retested
  forced fallback60014exit0: identical pixels and5130879generatedcycles. App link
  succeeds; IOSSIMULATOR16/sdk26.5 candidate7d14ec65…926e6 verified.
- Boot/install16275exit0 on sole DE8E956F. Installed candidate hash matches;
  data container relocated2F933ECE…, save99d432d5 unchanged. Launch13248/PID46228
  with GalaxyPadDevNativeTHP YES; same prior diagnostic inputs/settings, pair0,
  no fallback JIT, no lockstep. Exact DOL gate, mod initialized1/loaded logged.
- CUA screenshot shows ready title (60FPS snapshot). Native movie acceptance not
  yet reached; mod registration reports chunk fallback804520a0–804530a0, so record
  its real effect. No title FPS performance claim. Next same live run to movie,
  native frame log and complete cadence/audio/return test. Log ios-runtime-r672.log;
  no competing build/runtime or second Simulator. Full goal active.

## 2026-09-08 — R671 target dependency built, opt-in app integration reaches linker

- Prior turn progress: patch/fallback proof. No runtime/Simulator;40Gi free.
  Added pinned separate-SDK video-only FFmpeg build script;30007exit0. ARM64 object
  vtool confirms IOSSIMULATOR16/sdk26.5, configure LGPL2.1+. Static archive hashes
  recorded in generated/thp-simulator-dependency-r671.log.
- Added defaultOFF CMake candidate option, Simulator-only restriction, explicit
  runtime flag and exact DOL hash gate before mod registration. Shared candidate
  transform moved out of test implementation; tests retain include wrapper.
- Separate app34799 failed because FFmpeg root version file shadowed C++ header;
  C-only include scope fixes it.86222 then reaches linker and fails on unavailable
  g_mem_write_journal/_user: those generated-core helpers cannot be assumed to
  exist in chassis archive. No dummy globals added. Inspected actual external-write
  callback as next alternative; fallback semantic fix/retest required before link.
- No app install/runtime/default or device change; next real chassis fallback
  integration and relink, not another dependency build. Full goal stays active.

## 2026-09-08 — R670 explicit patch callback and exact rejection continuation

- Prior turn progress: compiled wrapper fixture. Added candidate THPPatch callback:
  bounded native success returnsLR; failure uses core mem_write32 to execute exact
  first stwu, advances8045239c, charges1. Existing external-entry suffix accounts
  remaining26; active write journal disables native bypass. No mod registered.
- THP_REAL_PATCH fixture invokes actual callback inside compiled player wrapper.
  Forced rejection83467exit0: original pixels cmp equal R657, inner generated
  charge5130879 matches baseline; no interception loop. Native success exits0 and
  output cmp equals R669 native wrapper output. Logs thp-patch-*-r670.log.
- Next isolated Simulator FFmpeg dependency/build and opt-in app wiring, then
  actual player cadence/audio evaluation. Successful native timing remains unproven;
  rejection equivalence is not native timing proof. No app/package/save/runtime
  changes; full performance-first PRD goal active.

## 2026-09-08 — R669 original compiled player wrapper accepts native frame

- Prior turn progress: bounded guest resolver. Extended offline fixture to execute
  actual8038d2e4wrapper with real local THP header/packet and bounded readbuffer/
  player metadata. Resolver derives native packet from original saved stack/player,
  not hardcoded host span. Wrapper returns1/101dispatches and writes frameNumber42
  through original instructions. Output/guard checks pass; no app/runtime launch.
- Added work-global/alignment/dimensions/entropy-cursor consistency and protected
  stack/player/readbuffer/work nonoverlap checks. Updated synthetic fixture;
  ASan/UBSan checks and guarded compiled-wrapper execution pass. Evidence
  generated/thp-player-r669.log/thp-player-guarded-r669.log.
- Offline constructed fixture does not establish live player/cadence/audio safety.
  Next explicit mod-patch fallback and opt-in native dependency/target integration.
  Installed apps/defaults unchanged; full performance-first PRD goal active.

## 2026-09-08 — R668 bounded guest packet eligibility resolver

- Previous turn progress: inner state/cycle attribution. Inspected actual sole
  outer call8038d30c, wrapper savedr31, texture offsets472/476/480, work176 and
  return-path readindex200/readbuffer fields; source map agrees at these sites.
- Added read-only GalaxyPadTHPGuest resolver with exact caller guards, THP1.1
  two-component header, valid index/buffer, declared capacity and payload span,
  original packet identity, texture arguments/dimensions and MEM1/MEM2-only maps.
  Corrected pointer arithmetic to subtract guest base before host pointer add.
- Synthetic test positive plus caller/index/span/validity/pointer/MMIO rejection
  passes ASan/UBSan; CPUState unchanged. No exact live-wrapper eligibility claim.
  Next work/header consistency and original-wrapper fixture/patch fallback before
  live candidate build. No game/Simulator/app target change; full goal active.

## 2026-09-08 — R667 inner cycle/state effects measured

- Prior turn progress: inner handoff/rejection fallback. Instrumented exact module
  downcount deltas at decompression entry/return, GQR/FPSCR and work tail. Frame0
  generatedcharge5130879; frame1000=6281568. Records generated instructions only,
  not complete chassis/console cycles, no conversion to wall delay claimed.
- Sampled GQR/FPSCR unchanged; entropy/destination cursors advance inside decoder
  scratch. Source search finds no THPInfo/WorkArea references outside decoder and
  player only allocates/passes mTHPWork. This is source ownership evidence, not an
  exact-DOL whole-program memory proof. Logs generated/thp-state-r667*.log.
- No guessed constant cycle charge, sleep or guestclock modification. Next live
  packet eligibility and disabled native patch with real buffering/retrace/audio
  cadence verification. No app/package/runtime/Simulator changes; goal active.

## 2026-09-08 — R666 inner guest-call handoff and original fallback

- Prior turn progress: bounded native adapter. Read ModManager Dispatch: entry
  hooks save/restore CPUState, cannot redirect execution. Replacement needs patch
  or equivalent explicit native path. Reference player obtains component sizes
  from THP frame header; live packet eligibility is not inferred from pointer alone.
- Diagnostic THP_NATIVE_INNER intercepts80452398 in offline harness with known
  local packet bounds, preserves original header parsing/outer return. One native
  call, outer success0/99dispatches, guards intact, output exactly R664 prototype.
  THP_NATIVE_REJECT supplies host length3 without changing guest packet: rejects,
  original decode resumes and output cmp equals R657 baseline exactly. Both exit0.
- Logs generated/thp-inner-r666.log/thp-inner-reject-r666.log. No guest cycle/work
  equivalence or audio/player/runtime claim. Next live bounds/eligibility and
  state/timing adaptation; neither app target changed. Full PRD goal active.

## 2026-09-08 — R665 bounded native decode interface and recovery tests

- Previous turn progress: sparse-compatible near-parity and whole-movie speed.
  Reviewed actual PRD correctness/cadence requirements and existing mod hooks.
  Added unwired shared C decoder interface, bounded padded packet copy, dimensions/
  pixel-format/range checks, nonoverlap/capacity checks and validate-before-write
  GX I8 tiling. No guest/audio/timing behavior or app target change.
- Initial adapter test exposed FFmpeg resetting idct_put on first8bit discovery.
  Explicit8bit initialization, recovery reinitialization and callback identity
  check fix silent substitution. Sanitized test16decode/error/recovery iterations
  passes, exact R664 output, boundary guards; malformedpacket/shortcapacity/
  wrongexpectedwidth/overlap rejection leaves buffers untouched. FFmpeg static
  library itself is not sanitizer-built. Log generated/thp-adapter-r665.log.
- Candidate uses pinned FFmpeg private ABI and diagnostic transform object; not
  yet a shipping dependency. Next guest-state/cadence integration and broader
  output checks. No game/Simulator/save/package changes; full PRD goal active.

## 2026-09-08 — R664 sparse compatibility substantially closes native output gap

- Previous turn progress: isolated SDK quarter-path behavior. Added native FAAN
  sparse-first-pass case (only AC1nonzero with higher row terms0): swap middle
  intermediate outputs before column pass, final floor conversion retained.
  No post-render global swap or stock FFmpeg source edit.
- Frames0/1000/3000 compare1/11/3differing bytes respectively, all absolute1;
  all U planes exact. Compared to prior max5–11errors, targeted compatibility
  explains the large discrepancies.68472/69203exit0. Private outputs/logs named
  generated/thp-native-sparse-r664*. No product integration or parity promotion.
- Added THP_BENCH build mode disabling coefficient tracing, decoding all5591
  packets/frames to no output, checks EOF/framecount, one decoder thread. Native
  compatibility prototype finishes5.847835s/956.08FPS/1.04594ms average per frame,
 30862320blocks;10445exit0. Evidence thp-native-sparse-bench-r664.log. Build ended
  before timing, no game or Simulator. No upload/audio/frame-tail measurement.
- Next residual rounding coverage and safe integration/memory/player cadence;
  three sampled frames are not full-movie pixel validation. Full PRD goal active.

## 2026-09-08 — R663 sparse transform case localized to reference quarter path

- Prior turn progress: independent IDCT outlier evidence. Diagnostic-only compile
  flag injects first-block AC(1,0)=16, all other coefficients0. Original module
  still completes/guards pass; row136,135,132,126,129,123,120,119 versus cosine
 136,135,132,129,126,123,120,119. thp-sparse-r663.log/gx stay ignored/local.
- check-thp-sparse-case.py scalar reference quarter-path algebra predicts actual
  row exactly. Generated804527cc ps_nmsub corresponds to reference operation;
  this supports SDK sparse-path compatibility behavior rather than proof of a
  recompiler regression. No physical console or all-path arithmetic proof claimed.
- Next native sparse-first-pass compatibility treatment followed by full-frame
  comparison and throughput. Do not globally swap pixels or change normal oracle
  inputs. Product/runtime/Simulator unchanged; full performance-first goal active.

## 2026-09-08 — R662 independent transform check of largest remaining errors

- Prior turn progress: quantization parity/final-rounding attribution. Rechecked
  status and original store/native FAAN arithmetic. Added bounded mathematical
  double cosine IDCT outlier probe using actual raw coefficients/DQT tables.
- At12largest residual pixel differences (four per Y/U/V), native floor output
  matches floor of independent cosine sum exactly; original differs5–7levels.
  generated/thp-idct-outliers-r662.log. Affected blocks include sparse row patterns;
  inspected reference quarter/half IDCT paths. No live/game/source-hook change.
- Interpretation: remaining differences must not automatically be treated as
  native-decoder faults. Mathematical reference does not establish Wii semantics
  or identify SDK versus port defect. Next isolated sparse-transform cases using
  exact instructions, then choose compatibility/correction based on evidence.
  No parity/performance acceptance or product promotion; goal remains active.

## 2026-09-08 — R661 quantization parity and final-rounding attribution

- Previous turn progress: full first-frame raw coefficient parity. Captured first
  six scaled quant tables at original transform entry and native DQT values;
  compare-thp-quant.py applies reference f64 products of f32 AAN constants and
  final f32 rounding:384/384 entries match exact bits.7794exit0.
- Added diagnostic source-including FAAN wrapper, final lrintf→floorf only;
  arithmetic/source library otherwise unchanged. THP_FLOOR_IDCT explicitly requires
  FAAN option.67272exit0: first-frame meanabsolute Y/U/V0.08398/0.05994/0.04276,
  signed errors nearzero, max7/7/5. Residual mismatches16286/2853/2292 remain.
  Evidence thp-quant-original-r661.log, thp-quant-native-r661.log and private
  thp-native-floor-r661.yuv/log. No stock library or product patch applied.
- Decision: conversion rounding explains most bias, not all differences. Next
  remaining transform arithmetic against original, preserving the exact-decoder
  acceptance boundary. No brightness offset, tolerance promotion, runtime or
  Simulator launch. Full performance-first PRD goal remains active.

## 2026-09-08 — R660 full-frame entropy/coefficient parity established

- Previous turn progress: transfer/guard checks. Added bounded original inverse-
  transform entry coefficient trace and native private-ABI idct_put wrapper.
  Native wrapper reverses quantization/scan permutation and1024DC bias with exact
  divisibility assertions, invokes unchanged original transform. First six blocks
  matched, then extended to complete5520blocks/353280raw coefficients: zero
  differences. Native output cmp equals pre-trace R657 bytes.2268exit0.
- Evidence generated/thp-all-blocks-r660.log, thp-native-all-blocks-r660.log;
  logs contain protected coefficients and stay ignored/local. No FFmpeg library
  source modification or shipped dependency added. Private ABI is diagnostic only.
- Decision: raw entropy/coefficient decoder is not the first-frame mismatch
  source. Focus next on dequantization/AAN scaling/transform/quantized-store
  conversion, not Huffman rewrites or ad hoc brightness fixes. No product/runtime/
  Simulator changes; full performance-first goal remains active.

## 2026-09-08 — R659 offline DMA and plane validity checks

- Previous turn progress: IDCT-option hypothesis rejected, later-frame differences
  confirmed. Read current status/harness/tree. Added test-thp-frame-dma.c against
  actual callback:256cases cover1–128blocks in both directions, encoded-zero length,
  no-trigger write, trigger clearing, PC advance and byte guards. ASan/UBSan pass.
- Harness now pre-fills destination planes and guards, checks exact-once DMA
  coverage and intact32-byte boundaries. Sanitized harness using existing original
  module returns0; guarded first-frame output cmp matches R657 byte-for-byte.
  Log generated/thp-frame-guarded-r659.log. Module code itself is not sanitized;
  this is not global memory/timing/OS-state proof. Explicit unsupported ops remain.
- Tested DMA and unwritten-buffer causes are ruled out. Next isolate coefficient/
  dequantization versus transform/quantized-store difference. No integration or
  package/game/save/Simulator changes; full performance-first goal remains open.

## 2026-09-08 — R658 stock-IDCT hypothesis rejected on bounded frame comparisons

- Prior turn progress: original/native first-frame comparison. Rechecked current
  tree/status and no booted Simulator. Native helper gains explicit IDCT selection;
  inspected FFmpeg config and ARM64 dispatch to verify float-AAN/integer options
  do not silently select the auto NEON path. Neither removes first-frame mismatch.
- Added bounded frame-index support to both probes. Original frames1000/3000
  return0 with25025/24433dispatches and353280DMA bytes. Native auto differences
  max11/10/9 and8/9/10 respectively, meanabsolute~0.48–0.59. Private outputs remain
  ignored; generated/thp-compare-r658.log records comparisons. No output overwrite.
- Decision: stop stock-transform-option experiments. Native format support alone
  is insufficient for current output parity. Next coefficient/dequantization versus
  transform/quantized-store attribution, plus transfer/guard checks on the offline
  harness. Do not apply a brightness offset or widen tolerance to conceal errors.
  No runtime/package changes or performance acceptance; full goal remains active.

## 2026-09-08 — R657 complete-frame reference and native mismatch measured

- Prior turn progress: original-module harness reached DMAU. Read current status
  and Dolphin DMA register fields/interpreter/MMUDebug transfers. Added bounded
  synchronous RAM/LC DMAU/DMAL support to diagnostic fallback only; unsupported
  instructions still fail. Full original decoder returns0,21463dispatches,
 353280transferred bytes. No game or Simulator launched.
- Added optional exclusive-create raw GX plane output, native FFmpeg first-frame
  helper and explicit8x4 I8 untile comparator. Both helpers compile/run exit0.
  Same PrologueA first frame: Y111472/235520,U27673/58880,V17268/58880 differ;
  maxerrors8/8/6, meanabsolute0.49354/0.48414/0.30003. Not byte parity. Evidence
  generated/thp-compare-r657.jsonl and thp-frame-r657-output.log. Private plane
  data only in ignored generated/. Existing package/module unchanged.
- Next harness DMA/guard validation, additional representative frames and IDCT/
  quantized-store rounding investigation. Do not hide mismatches with a tolerance
  or promote a fast decoder solely from throughput. Offline OSRegisterVersion
  omission and synchronous DMA mean this is not a full OS/timing-state oracle.

## 2026-09-08 — R656 executable standalone original-decoder harness

- Previous turn progress: exact boundary audit. Current status inspected. Added
  tests/probe-thp-frame.c using dlopen of actual existing module, no regeneration.
  ABI/CPU-size/game checks; DOL loaded into isolated24MB RAM, exact startup r2/r13,
  bounded dispatcher, first local movie packet, explicit unsupported-op failure.
- Initial OSRegisterVersion touches EXI; explicitly omitted version-registration
  side effects in offline fixture only. Original THPInit then returns1. Missing
  MSR FP/HID2 paired-load prerequisites produced real exceptions and were corrected
  using core constants, not ignored. Decoder now reaches unsupported DMAU mtspr
  7cdae3a6 at804a3194; latest harness exits2, generated/thp-frame-r656.log.
- Next implement bounded locked-cache DMA semantics from existing Dolphin core;
  no output/pixel-parity or completed decoder claimed. Harness is not part of
  green repository suite yet. Product, save and installed app untouched; no
  Simulator/game launched. Full performance-first PRD goal remains active.

## 2026-09-08 — R655 exact whole-decoder boundary and contract

- Previous turn was progress: native whole-movie throughput established. Rechecked
  current status, dirty tree and no booted Simulator. No runtime/build launched.
- Extended exact-DOL boundary audit through outer video decode/header parsing,
  inner decompression/init/audio; assertions pass, output thp-contract-r655.json.
  Verified generated outer instructions and direct call8045174c→80452398.
  Reference symbols are RMGK01; do not blindly hook their addresses on RMGE01.
- Reference contract reveals validation/work-state/locked-cache and planar-layout
  differences. Candidate inner boundary keeps guest header/player/audio logic;
  no product hook or FFmpeg integration yet. Recorded detailed contract in
  CUTSCENE-PERFORMANCE-PLAN.md. Next exact full-frame oracle/capture and layout/
  rounding comparison; not another navigation or native throughput repeat.

## 2026-09-08 — R654 whole-movie native decoder feasibility

- Hypothesis: whole-frame native THP video decode has far more headroom than
  recompiled PPC decoder execution. No game/booted Simulator, no concurrent build
  during measurement. No installed ffmpeg found; downloaded official FFmpeg8.0.1
  public source only, recorded archive/binary hashes and LGPL2.1+ configuration.
- Built minimal ARM64 CLI (72076exit0), added bounded benchmark-native-thp.py;
  actual private PrologueA decoded all5591frames three times,56854exit0. Total
  wall4.2196/3.9718/3.9850s,0.7547/0.7104/0.7128ms/frame, one decoder thread.
  Source640x368/59.94FPS/~93.277s. Evidence generated/native-thp-r654.log.
- Decision: pursue whole-decoder compatibility/integration feasibility. This
  establishes average native video throughput, not pixel parity, native player
  contract, audio, upload or game/Simulator performance. No product dependency
  integrated and no game/save/package changes. Next exact full-frame oracle and
  tiled-output/rounding/guest-state audit; full PRD goal still open.

## 2026-09-08 — R653 explicit performance-first reorientation

- Prior turn progress: deployed default activation. Same31065 initiallylive;
  movie windows9.12/12.21/6.41FPS expose unresolved severe stalls. Host16GB,
  ~7–7.7GB compression, advancing swap counters; current app footprint393MB,
 316MB compressed. Snapshots do not isolate cause. No unrelated processes killed.
- Chris requested top priorities and a slight goal-loop reorientation. Inspected
  prior R406 attribution, actual THP policy and Petari decoder/player contracts;
  read FFmpeg primary decoder reference. Actual local PrologueA header verifies
  59.94FPS/5591frames/~93.277s. ffmpeg/ffprobe not found on current PATH; no download
  or native benchmark claimed this turn.
- Updated goal loop and added CUTSCENE-PERFORMANCE-PLAN.md: whole-frame native
  decoder feasibility before more tiny generated-code tweaks; bounded playback/
  audio stage attribution; host pressure deltas; measured graphics costs. Preserve
  full PRD/no-JIT/rights/correctness and real movie completion acceptance.
- Stopped diagnostic runtime cleanly19:00:31.868failed0,27754exit0; Simulatoroff,
  save99d432d5 unchanged. Next isolated direct-frame decoder benchmark, not another
  title-to-movie loop or UI verification. Product/default artifacts unchanged.

## 2026-09-08 — R652 installed default-path activation verified

- Prior turn progress: suite passed, candidate installed and title rendered.
  Revalidated same31065/session27754, soleDE8E956F, app6f41c070/module3acdcddc.
  No relaunch, override or source changes. A+B23905/fileA99624/PlayA1224 enter
  story; eightA helper86955exit0 reaches gameplay. Route28189/72726/56952exit0
  reaches invasion; all leased input releases neutral.
- Actual log [galaxypad-lc-byte-fast] active pc80452a2c/eae0000000 without byte
  environment override proves app-default path. Screenshot
  generated/ios-default-byte-r652-movie.png confirms intended movie visually.
  No detailed VI recorder or renewed timing comparison; this is activation proof.
- Keep runtime alive through movie and onward toward first Grand Star/save loop.
  No stop/restart at benchmark boundary. Same log generated/ios-runtime-r651.log;
  movie currently running. Performance still sub60, audio/lifecycle/save and full
  gameplay acceptance open. No hardware or full PRD completion claim.

## 2026-09-08 — R651 regression pass and deployment of byte default

- Continuation: runtime31065/27754 visibly renders ready title. Next existing file
  and continued gameplay; default decoder-path activation not reached yet.
- Prior turn progress: repeated movie result, source default and both app builds.
  Revalidated same suite30113 live then exit0/Repository safety checks passed.
  Includes compiled default/opt-out test. No source edits needed this turn.
- Private staging29980exit0 creates generated/device-stage.d5VpYL/GalaxyPad.app
  with app936265bd5744518f51530b82a80c5507d50403cc46f899bb4ac3b3e473ca7cfd
  and module48f455eb; platform/export/install-name/copy checks pass. Unsigned and
  uninstalled. Prior qAV3Pi preserved; no publication or device claim.
- Boot/install sole DE8E956F26271exit0. Installed app6f41c070 matches build;
  save99d432d5…ab64f unchanged. Bundle71C4F911-984E-4AF5-9EEB-F813D0BD6B6F,
  dataDC42BFFE-C73A-43D7-9D4A-D0E971DCE3A4. Matching module3acdcddc unchanged.
- Launch27754/log generated/ios-runtime-r651.log without byte environment override
  or finite VI recorder; pair0/QoSNO and prior pointer/geometry flags unchanged.
  Next ready scene, default-path decoder activation, then continue gameplay beyond
  movie instead of repeating timing comparisons. Runtime-start is not acceptance.

## 2026-09-08 — R650 byte-cache confirmation and mobile default

- Prior turn progress: enabled confirmation reached overlook. Revalidated same
  28264/session65315, soleSimulator. Route98264/9728/13364/22063 reaches movie;
  activationpc80452a2c/eae0000000 repeats. Screenshot lc-byte-confirm-r650-movie.png.
- Complete movie windows1264/30.699525=41.173276FPS and1247/30.500521=
  40.884548FPS, consistent with R64740.195 and above R64835.387/36.053.
  Exact phases/hostconditions differ; not a precise causal percentage or60Hz fix.
  VI dropped17321; no late CPU-timing claim. Stop native menu/confirmation;
  exit18:38:37.069failed0/session65315exit0, Simulatoroff, save99d432d5 unchanged.
- Set byte default in mobile main before UIApplicationMain using setenv overwrite0.
  No pair default or runtime/guest changes. New test compiles actual assignment,
  proves unset→1 and preserves0/1/empty/other values, repeated invocation and pair
  isolation. Added to suite. Both Simulator10794/device83123 app builds exit0.
  Simulator executable6f41c070723857ae685046b9c94b377c83a12986ccf79f99f7f4ac6a5335588b.
- Candidate NOT installed; old installed54786759 and private device stage untouched.
  Full suite started generated/check-r650.log. Next suite result and candidate
  default-path activation without byte override, not unchanged timing repetition.
  Full PRD remains active; no hardware/audio/story/stability acceptance claimed.

## 2026-09-08 — R649 enabled confirmation underway

- Continuation:5612exit0; visually reached village overlook dock, neutral input.
  Runtime remains28264/session65315. Next off dock and through plaza to movie.
- Prior turn progress: disabled movie35.387/36.053 versus enabled40.195FPS,
  clean exit/save unchanged. Revalidated no runtime or bootedSimulator; exact
  app54786759/module3acdcddc/save99d432d5…ab64f before launch. No rebuild.
- Sole DE8E956F, runtime28264/session65315; byte1/pair0/QoSNO with previous
  pointer/geometry/diagnostic settings. Log generated/ios-runtime-r649.log;
  VI runtime/lc-byte-confirm-r649-vi.csv. Detailed buffer not suitable for long
  navigation; use full movie presented windows for this confirmation.
- Title inspected, A+B60147 then pointer/A12386 selects existingMario; pointer
  Play/A42375 starts story. Eight bounded pulses53181exit0, gameplay inspected.
  Route5612 currently7s forward/2s neutral/8s forward; poll and inspect before
  further navigation. Inputs release neutral; no touch-verification claim.
- Next movie byte activation and comparable full window. If gain repeats, then
  consider guarded mobile default with explicit opt-out preserved; no default
  changed yet. No physical-device/performance/full-PRD acceptance claim.

## 2026-09-08 — R648 disabled control underway

- FINAL: movie-only windows ending18:21:28.096 and18:21:58.497 give
  1097/31.000029=35.387064FPS and1096/30.400043=36.052580FPS. Visually movie
  before/after, screenshot lc-byte-control-r648-movie-window-end.png. Explicit
  byte0 has no activation log. R647enabled40.195464 is11–14% higher but windows
  have unequal exact movie phases and host conditions; no causal precision claim.
  Next enabled confirmation before default decision, not another general sample.
- Native-menu cleanstop18:23:34.642failed0, session76274exit0; soleSimulatoroff.
  Save99d432d5…ab64f unchanged; VI dropped26209, unusable for late CPU timings.
  No package or default changes; full PRD and60Hz/performance/audio gates open.
- UPDATE: story25305exit0. Route replay14891/63092 plus bounded corrections,
  final diagonal27781exit0, reaches invasion movie visibly. Screenshot
  generated/lc-byte-control-r648-movie.png; same runtime live, neutral input.
  Navigation differs in duration/StarBits, not a matched gameplay-timing claim.
  Next full movie-only frame window; current18:20:26 window mixes earlier scene.
- Prior turn progress: byte path activation plus complete40.195FPS movie window;
  stopped cleanly with unchanged save. Revalidated no game or bootedSimulator,
  app54786759/module3acdcddc/save99d432d5…ab64f. No source/package changes.
- Launched same sole DE8E956F with byte0/pair0/QoSNO, runtime26686/session76274,
  log generated/ios-runtime-r648.log and VI runtime/lc-byte-control-r648-vi.csv.
  Title inspected before A+B27867; actual pointer plus diagnosticA38263 selects
  existingMario; pointerPlay/A36309 reaches openingstory. Bounded eightA pulses
  helper25305 underway, six-second spacing, finally neutral after every hold.
- Next gameplay inspection and R647 route, then full movie presented windows.
  Do not rely on late VI rows if16384samplecapacity fills. Host variation and
  unequal exact movie phases limit causal precision; no default promotion.

## 2026-09-08 — R647 mobile byte-cache activation in the intended workload

- FINAL: full movie window ending18:03:53.137 gives1234frames/30.699982s=
  40.195464FPS, visually movie both sides (movie-window-end screenshot). This
  remains below target; disabled comparison required. Stop via native menu and
  confirmation; runtime exit18:05:15.662failed0, session6596exit0, Simulatoroff.
  Save99d432d5…ab64f unchanged. VI dropped23207; no late CPU-timing claim.
  Whole-run fallback127824147/hook_fb310566910/smc_failed0 are not exclusive CPU
  cost; R553 already explains this boundary. Do not reopen generic totals loop.
- Prior turn progress: suite passed, device access unavailable, mobile trial
  started. Revalidated same25126/session6596 and sole DE8E956F. No rebuild/relaunch.
- Leased diagnostic A pulses advance story and invitation into Star Festival.
  Bounded move actions route through hill/dock/village/plaza; no guest-memory or
  savestate shortcut. Final forward5s12820 triggers invasion movie; screenshot
  generated/lc-byte-r647-movie.png confirms it visually. All holds release neutral.
- Activation log proves byte path at pc80452a2c/ea e0000000 on unchanged mobile
  module; pair stays0 and CPU QoS experimentNO. Village windows45–52FPS before
  activation cannot be attributed to this optimization. Full movie window and
  same-artifact disabled comparison needed; no default promotion or speed claim.
- Measurement limitation caught: ViTimingBuffer capacity16384 drops tail after
  roughly273s at60Hz. This run's prolonged navigation exhausted it before movie.
  Movie CPU timing is unavailable; use complete presented-frame windows only.
  Runtime remains live; next movie observation/window and clean stop. Full PRD open.

## 2026-09-08 — R646 local performance continuation after hardware preflight

- Prior turn progress: device module linked and privately staged. Revalidated
  suite3418 exit0, Repository safety checks passed. security find-identity reports
  zero valid code-signing identities; devicectl no devices. No signing attempted.
- Next hypothesis: iOS currently omits the guarded locked-cache optimizations
  enabled in macOS wrapper. Historical macOS movie gains justify a mobile test,
  not silent default promotion. Start byte-only1/pair0, prove actual activation
  then compare same scene with byte0. QoS experiment remains off.
- Verified installed app54786759, module3acdcddc, save99d432d5…ab64f and no game.
  Booted only DE8E956F iPad; runtime25126/session6596 started with diagnostic input,
  pointer experiments off, LC_BYTE_FAST1/LC_PAIR_FAST0, VI trace
  generated/runtime/lc-byte-simulator-r646-vi.csv; log ios-runtime-r646.log.
  Startup visually inspected. No compile/test load overlaps runtime. Next scene
  readiness and movie activation, not touch layout work or performance acceptance.
- Continuation: title inspected, A+B83477, actual pointer selects Mario file,
  A13728 confirms; pointer selects Play and A48842 starts opening story page1.
  Input helpers release to neutral. Runtime remains25126/session6596; byte-path
  activation not yet seen. Story FPS is not the intended movie measurement.

## 2026-09-08 — R645 device module completed and staged

- Previous turn progress: physical startup changed and compiled; linker was
  verified live. Revalidated original38249 this turn: exit0, linker no longer
  running. No restart. Module48f455ebc33f8eb2fc58151cd722ea756db77a5738a9573a0a49f0c656110041.
- Ran private staging helper78238 exit0: arm64/IOS/min16, module export and
  @rpath install name accepted; copy identical. Stage
  generated/device-stage.qAV3Pi/GalaxyPad.app retains app2e73e706…40d. otool
  dependencies show only system libraries/frameworks. codesign reports unsigned.
  No device installation, launch, signing, publication or speedfix claim.
- Inspected selected runtime: iOS preprocessor excludes ARM64 fallback-JIT
  construction; MODERNGEKKO_HAVE_IOS selects software vertex loading and disables
  large JIT entry map. Host allow_interpreter=false rejects missing/incompatible
  modules; this does not prove absence of instruction fallback during gameplay.
  Physical no-executable-generation acceptance still requires actual execution.
- Full repository suite3418 running with generated/check-r645.log; poll same
  handle. No game or Simulator launched. Next suite completion and device
  signing/access preparation. Real-device speed and full original PRD unproven.

## 2026-09-08 — R644 physical startup and private staging preparation

- Hypothesis: a separate real-iOS build enables measuring device execution rather
  than extrapolating Simulator overhead. This is benchmark infrastructure, not a
  demonstrated speed fix. Priority experiment remains parked and default-off.
- Found startup entirely Simulator-only. Physical host now selects bundled
  Frameworks/gRMGE01_recomp.dylib and private imported GameData. Simulator keeps
  its development defaults. Import still requires relaunch; no automatic-start
  or full import-UX acceptance claim. Runtime validates module ABI/game ID;
  exact DOL verification occurs in the import path, not module requirements.
- Device main65960 and Simulator main98397 compile successfully; object strings
  confirm device excludes three Simulator development paths. Full device app
  51942 exit0, SHA2e73e706f21684bb69bb0f1740348261237d1fe801d0a866a5ad0aa2e64eb40d.
  Logs generated/device-main-r644.log, simulator-main-r644.log and
  build-device-app-r644.log. No physical launch or runtime-loading proof.
- Added scripts/stage-private-ios-device.sh: checks arm64/IOS/min16, bundle ID,
  module export/install name, then copies into a fresh private staging directory.
  It does not sign, install, publish or overwrite prior stages. Syntax passes;
  not run until module links. SDK-selection regressions pass again.
- Same module38249 reaches1330/1332; original linker23142 still active at5:48 and
  ~685%CPU. Do not restart. No booted Simulator; devicectl reports no devices.
  Next poll same build, audit and stage; full suite after heavy link completes.
  Signing, hardware performance, full gameplay and original PRD remain open.

## 2026-09-08 — R643 device build isolation and physical host compile

- FINAL UPDATE: originalcore85533exit0, actualruntimeobjectverifiedIOS/min16/sdk26.5.
  Provisioningexit0, combinedarm64archivef4730e664825bd931afd76b3d2614cbdfcde2813a192af73349e53a58a751435.
  Full deviceapp33663exit0, executable96f44f1a16533f28cdb01d50756f1895ca7151b9d7a34d439362bd5ef4c5f85d,
  vtoolIOS/min16/sdk26.5. DeviceAOTmodule nowbuilding38249 at
  generated/build-ios-device-module-r643.log; exactacceptedDOL/module/profile
  guards retained. Poll samehandle. No signing/package/playability/performance
  claim; next moduleplatform/ABI/noJIT/privatepackageaudit then deviceaccess.

- Prior turn progress; revalidated same85533 live and862/1480, no Simulator.
  Continued original build, now1311/1480. No restart or second core/module build.
- App/module/provision entry points now allowlistedSDKselection likecorebuilder;
  defaults unchanged. Device archive pathgenerated/ios/iphoneos/libs and headers
  ios-device-core isolated fromSimulator. Provisioning checks exact IOS versus
  IOSSIMULATOR line, not prefix, before combining archives. SDKmoduleguard now
  precedes privategeneratedfilelookup. All12invalidSDK cases pass; addedtest to
  fullsuite. bashsyntax/diffcheckpass; fullsuite not overlapped withcorebuild.
- Configured deviceapp79458exit0 andSimulatorappexit0; inspectedgeneratedNinja
  for correctSDK/archive/headerpaths and absenceofoppositeSDKarchive. No app
  link/run claimed. PhysicalCoreHost object11442exit0, vtoolIOS/min16/sdk26.5.
- Samecorebuild85533/loggenerated/build-ios-device-core-r642.log remainslive.
  Next finish, auditruntimeobjectplatform, provisionSDKiphoneos, buildmatching
  deviceAOTmodule/app. Neither signing nor connectedhardware resolved; fullPRD
  goal staysactive. No gameorSimulator;40Gi free. ExistingmacOS/Simartifactssafe.

## 2026-09-08 — R642 control closes QoS lane; real-device build preparation

- Prior turn progress: intervention verified, enabled timing captured. Revalidated
  same54786759/no games or bootedSimulator. Launched8598/session27787 CPUflagNO;
  requested21→21/setResult-1. A+B helper16696 thenneutral andactualcenterpointer.
  File-select capture69514exit0:51.108439VI/s,15.778782msCPU/VI; detailsPERF.md.
- Same-build enabled52.610632/15.718705, not meaningful proof against variability.
  Smaller outerSimulatorwindow noted; no claim perfectlycontrolled. Parkpriority
  tuning, defaultOFF/no promotion. Cleanstop17:22:45.067failed0/export1,27787exit0,
  soleSimulatoroff. No additional touchverification work or unrelatedprocesskills.
- Devicequery59558 no devices. Asked user optional USB-connected unlocked iPad.
  Read actual mobileCMake/build/provision/module scripts: Simulatorhardcoding
  blocks real-device benchmark. Added separate unsignediphoneostoolchain and
  allowlistedSDKselection to existingcorebuilder; existingdefaultpathsunchanged.
  bashsyntaxpasses; invalidSDKfails2 before configuring.41Gi availablepreflight.
- Started GALAXYPAD_IOS_SDK=iphoneos core build85533/log
  generated/build-ios-device-core-r642.log; cmake9135 underbuilder9128 confirmed
  live. No runtime overlap. Poll samehandle; then platformaudit/provisioning/module/
  app devicepaths. No signing/deviceexecution/performance claim. Fullsuite latest
  R639; not rerun concurrently with build. Full originalPRD/goal remainsactive.

## 2026-09-08 — R641 effective intervention selected, enabled measurement complete

- Previous turn progress; revalidated candidate d5871ba7/no runninggames.
  Installed; queueNO7066/session49342 andYES7288/session5004 both worker25/CPU21.
  Both cleanexit0/failed0/export1. Rejected queue setting, no contrived FPS test.
- SDK qos_class_self returns REQUESTED class, not effective priority or core;
  corrected labels and interpretation. Replaced ineffective queue flag with
  Simulator-only default-off GalaxyPadDevCPUUserInitiatedQoS on first CPU VI.
  No guestclock changes. Build49965exit0,547867590b1388dbbfe9a70b17e82a3700435818e04206cc97bf3ffc7457ed9b.
- Installed/started7767/session98543 withCPUflagYES. Verified21→25/result0 on
  CPU thread at17:12:02.343. A+B helpers9016/17285 thenneutral, actualcenterpointer,
  file-select screenshot. Work2574exit0:1579VI/30.0129447s=52.610632Hz,
  CPU15.718705ms/VI,164.763575Minstructions/51.751098Mcycles process-wide perVI.
  No drops. Later35FPSAX observation; no stable-speed claim. Compared with older
  R64053.014Hz no obvious large improvement, but not same-binary causal comparison.
- Cleanstop17:16:44.989failed0/export1;98543exit0, soleSimulatoroff. Savehash
  99d432d5…ab64f after installs. DataC54A766E-6E23-4A16-8B92-9E3C212C15CE.
  Next same54786759 explicitCPUflagNO, same visible-pointer scene/counters, then
  park unless repeatable gain. Fullsuite latestR639predatescandidate; actual
  CMakebuild and cleanlaunch/teardown verified. Full PRD/goal active, no speedfix.

## 2026-09-08 — R640 measured work divergence; scheduling experiment built

- Prior turn progress; confirmed64026 live then exit0/fullsuitepassed. Launched
  soleSimulator5921/session32327 only after tests completed. Normalbf6da365 and
  module3acdcddc, LC0. DiagnosticA+B/neutral reached file select; actual center
  pointer retained. Work82246exit0,30.010771s/1591VI53.0143Hz.
- Simulator178.935Minstructions/55.409Mcycles perVI (whole process), lower than
  native192.777M/58.244M; guestCPU15.208ms versus11.101ms. No gross instruction
  explosion found. ExternalSimMetalHost is excluded from process totals; CPU
  core/frequency/host contention not isolated. EFB2.210ms versus0.318ms separate.
- Cleanstop17:01:09.523failed0, recorderexport1/dropped0;32327exit0, deviceoff.
  GameData remains99d432d5…ab64f. No other app killed; no gameplay acceptance.
- Read SunPad QoS-only launch experiment. Added Simulator-only default-off
  GalaxyPadDevUserInitiatedQoS queue attribute plus once-per-run worker/CPU class
  logs. No guestclock/timing changes. First build87509 failed APIarity, corrected
  to SDK-declared qos_class_self;12379 built/linked exit0. Not installed.
  Standalone syntaxchecker lacks generated header include; actualCMakebuildpassed.
- Next same candidate explicitNO/YES, matched file-selection pointer/work/VI
  capture and actualCPUQoS logs, reverse repeat if promising. Do not call queue
  configuration proof of inheritance or promote without repeatable performance.
  R639fullsuite predates this candidate. Full originalgoal active, no speedfix.

## 2026-09-08 — R639 native file-selection benchmark and portable work capture

- Previous turn progress: platform comparison disproved fixed35FPS ceiling,
  established extra CPU time but not cause. Revalidated no games/bootedSimulators.
- Ran same native runner/module/settings, explicit LC0, copied private profile
  with configured Pipe. New fixture perf-title-to-file-select.json performs only
  held A+B and release. First execution during strap screen; second after visible
  title reached file selection. Pre/post screenshots retained,59.9FPS.
- Helper31266 captures1795VI in30.0051s=59.823VI/s;11.101ms CPU/VI.
  192.777M process instructions/VI,58.244Mcycles/VI. Exact paths/caveats in PERF.md.
  Native pointer visible unlike previousSimulator: match for next comparison.
  Runtime4315/session5785 clean exit0, traceexport1/no drops. No game/Simulatorleft.
- Portable capture helper accepts exact Simulator executable identity; removed
  osascript focus mutation. Native/mobile/no-focus/mismatch tests pass (initial
  mock failed on /var symlink canonicalization; corrected fixture path, not gate).
  Added test to full suite; process-summary regressions and fixture dry run pass.
- Full suite launched session64026, generated/check-r639.log. Continue same
  handle; do not launch competing game while tests run. Then Simulator work
  capture with matched pointer visibility, avoiding claim that gross process
  counters are guest-only. No performance fix/full-game acceptance claimed.

## 2026-09-08 — R638 sequential native/Simulator performance comparison

- Performance only; no control edits. Stopped previous99791 cleanly, shut sole
  Simulator down. Native3003/session40402 used copied disposable Config/Wii,
  runner671729c6/currentmodulec0021b9f, LC byte/pair0. Visible title59.9FPS;
  retained60–120s trace59.933VI/s,10.126ms CPU/VI. Clean native exit0.
- Sole Simulator3504/session96470, unchanged appbf6da365/module3acdcddc,
  VI recorder enabled and LC0. Title60–120s53.350VI/s,15.598ms CPU/VI.
  One-second diagnostic A+B then neutral (helper53012exit0); visually file select,
 150–210s50.717VI/s,16.194ms CPU/VI. Not touch evidence or full-game performance.
- Fresh50–60FPS without code changes invalidates treating older~35FPS as a fixed
  baseline. CPU and EFB overhead both higher, exact cause still unproven. Build
  driver probes both apple-m1: no default-A7 bug. Native input/audio/surface and
  host load confounders preserved in PERF.md; no unsupported causal conclusion.
- Focused VI summary and buffer/recorder tests pass69575exit0. Native trace
  dropped tail4247 but selected complete window retained; Simulator dropped0.
  No full suite rerun this measurement-only turn (latest full R633pass).
- Stop Game clean failed=0 at16:48:46.409; export_result1. Terminated shell app,
  session96470exit0; sole Simulator shut down. devicectl no connected devices.
  Next native file-select comparison and matched instructions/cycles/core residency
  to choose a meaningful CPU optimization. LC mobile movie parity is a separate
  bounded candidate. Original PRD/goal remains active; no speedfix claimed.

## 2026-09-08 — R637 user redirects performance; pathological dip captured

- Before redirection: same99791 advanced hill→village via helpers35925,55181,
  51333,44036,50773,81225,96094, all exit0/release. Actual touch B showed1→0 while
  stationary; collection during moving not isolated proof. Current bits3/coins0/
  life3 near lakeside village path. User explicitly demands stop touch verification
  and address underlying slowdown. That is now the priority.
- Captured fresh catastrophic scene rather than generic file-select profile:
  sample99791 5 10 → generated/ios-r637-village-slow.sample.txt, helper68492exit0.
  top3/5s → generated/host-village-r637.txt, helper68490exit0. No other app killed.
- Runtime window7.21FPS coincides with JumpConnect284.4%CPU/game47.1%; later
  Jump8.6%/game137.3%, unchanged scene recovers27.73FPS. Correlation supports host
  contention contributing to collapse, not baseline deficit resolved. Remote
  access is user infrastructure; do not suspend without explicit permission.
- CPU sample399 observations mostly compiled execution;27nestedfloatfuturewait,
 44Run body, not dominant readback wait. Read exact Run loop; preserve per-block
  timebase/downcount/interrupt semantics, no speculative batching. No speedpatch.
- Game remains RUNNING, not paused at end. Next matched native/Simulator workload
  comparison then materially significant AOT CPU work. Do not return to cosmetic
  or touch-verification loop while claiming to solve performance.


## 2026-09-08 — R636 installed candidate reaches gameplay and touch jump

- R635 progress: installed story clearance. Revalidated99791 and sole iPad,
  resumed native menu. Actual UIKit A advanced remaining pages and Peach's
  invitation. Text clear across these observed pages; no automation input there.
- Star Festival rendered with latestbf6da365 controls. Actual touchA visibly
  changed Mario to airborne jump pose, subsequent screenshot shows standing.
  Capture generated/ui-r636-opening-native.png is later opening frame; CUA
  conversation includes immediate jump and landing observations. No repeated
  stuck-A jumping observed during wait.
- Current A leaves inventory clear. Left stick partially overlays lives icon;
  this remains layout limitation, not full contextual HUD/ergonomic acceptance.
  Gameplay30s window35.625755FPS after story~60FPS confirms scene-dependent
  deficit remains. No UI speedup claim.
- Diagnostic forward5s helper16919 exit0 advanced to hill base and released.
  Movement is diagnostic controller input, not sustained UIKit joystick proof.
  Native menu paused at hill base, life3/coins0/bits0; same session2532 continues.
  Next continue first-play/pointer mechanics with this build. Save, Gateway,
  performance, motion, phone/physical and full original goals remain open.


## 2026-09-08 — R635 deploy and verify story-text clearance in game

- Previous pass progress: failing-then-passing UIKit layout test, candidatebuild.
  Verified old95966/sole iPad and appbf6da365/save99d432d5 hashes. Native Stop Game
  exited16:01:42.344 failed=0; terminated shell/session5906exit0. Unsaved story
  test position discarded, existing save retained; no game-state injection.
- Installed latest app; new data containerB15813C8-2128-40B3-B01C-A1D13300D5E6.
  GameDataSHA99d432d517b92b19da0c403819b91288b10c7063c3ce61f5000347ef823ab64f
  unchanged. Same normal module/arguments/trace0/pointer experimentsNO/.55opacity
  asR629; log generated/ios-runtime-r635.log, PID99791/session2532.
- Diagnostic titleA+B helper74432 exited0. Actual touch pointer/A selected Mario1,
  Play This File and story; actual A advanced to comet/shooting-stars page2.
  Visual comparison toR633 now shows last line clear of both left stick/D-pad.
  Evidence generated/ui-r635-story-clear-native.png. This proves observed story
  clearance, not every scene or sustained touch-stick/ergonomic acceptance.
- Sizing/stick-routing sanitizer tests rerun pass. Story30s windows57.27/59.05FPS
  are scene-specific; do not compare with35FPSfile details as an optimization.
  No performancefix. Native menu paused page2; latest installed candidate ready
  for continued first-play/control testing, full goal still incomplete.


## 2026-09-08 — R634 left story clearance and completed full regression

- Revalidated R633 handle98404: exit0, generated/check-r633.log ends Repository
  safety checks passed. This full run predates current layout edits; not a clean
  clone, gameplay, publication or full-goal acceptance.
- Added real UIKit observed story rectangle and checks for move/D-pad. Old
  layout fails (ui-r634-before.log → mobile-ui.KcEKz7). Full bottom anchoring
  alone still fails (Nk1y3a). Premature preview of that candidate also failed;
  stopped guessing and added exact overlap diagnostics. FKKiB9 identifies move
  bounds85.56,651,146,146 versus text ending652.01:1.01pt overlap.
- Kept full-bottom shift, moved default iPad stick to safe-left+8pt. All saved
  origins/mappings/phone layout untouched. Final UIKit runner exits0:
  generated/ui-r634-final.log → mobile-ui.c97X1T. All-control non-overlap and
  input/editor checks remain green. Real preview inspected, screenshot
  generated/ui-r634-layout-native.png. Not installed gameplay clearance proof.
- Exact preview bundle terminated, product foreground returns95966 with prior
  paused story. Release build exit0, generated/build-ios-app-r634.log;
  candidatebf6da365927800e0365a030219caf7c39e76bca8670e7ceaaf1d5763f46855b2
  NOT installed. Next deploy candidate and inspect story; unsaved test position
  is not a reason to defer user-requested UI improvements indefinitely.


## 2026-09-08 — R633 installed file-to-story touch and full-suite launch

- R632 progress was load-provenance tooling, not speed. Current candidate95966
  revalidated. Started full scripts/check-repository.sh into generated/check-r633.log,
  live exec session98404. Polls still running, log advances through native tests
  including640000 CPUState/host-flags cases; active compiler children confirmed.
  Preserve handle across continuation; completion NOT yet established.
- Resumed native menu; actual touch pointer600,575→635,585 plus A selected Play
  This File. Opening story visible; actual relocated A advanced first page to
  comet/shooting-stars page. Candidate0037f222 input route works this far, not
  sustained touch movement or completed first-play/save loop.
- Screenshot generated/ui-r633-story-layout-native.png: left stick and D-pad
  still encroach on last line of story copy. Concrete remaining UI defect; no
  whole-layout acceptance. Paused game on page2 while suite continues. No FPS
  benchmark claim because suite ran concurrently. Next poll98404, then refine
  default left-edge/bottom placement against this evidence, preserving customorigins.


## 2026-09-08 — R632 exact global-load provenance before optimization

- R631 host evidence did not isolate sole cause. Reviewed existing top20 exact
  macOS audit and prior fallback accounting before considering shared AOT work.
  Chose unresolved load0x679a580: objdump annotation suggests lazyFP but numeric
  address plus8 and nm prove memory-write journal pointer. No policy patch.
- Extended load_origin with optional exact-symbol map, adjacency/base/offset
  requirements. Audit still verifies module hash and derives symbols from same
  binary. Tests cover no symbols, wrong address/name/register and misleading
  annotation. Classifier tests pass; audit helper18264 exit0.
- generated/shared-aot-work-r632.json:214journal samples,3590remainingunknown,
  totals conserved4776loads/7782selected/28822CPU. 0.7425% leaves is not removable
  CPU time. Does not justify semantics change or compiler rebuild. Retained macOS
  evidence only, not current Simulator result. Product untouched and paused.
- Next still requires a materially larger, semantically understood CPU cost;
  do not promote a journal omission or another microbenchmark as performancefix.


## 2026-09-08 — R631 refresh host-contention evidence

- R630 progress was reliable UIKit failure propagation. Revalidated95966 and
  resumed same file detail15:47:31.316, no rebuild/restart. Question: is current
  slowdown consistent with whole-host saturation or active swapping?
- Captured top3samples/5s to generated/host-perf-r631.txt (helper74907 exit0),
  sysctl model/memory/CPU, memory_pressure, pmset therm. Later samples134–159%game,
  67–75%Logitech, aggregateidle30–42%; swap deltas0. 16GiB/8logical/M1Air.
  Current window1078/30.800160=34.999818FPS. No unrelated apps terminated.
- Supports actual competing load, not whole-host saturation/swap sole-cause
  claim. No thermal warning does not disprove throttling. No per-core allocation
  evidence or causal isolation, so no priority or QoS change justified.
- Read current default serial host queue and earlier R331 policy capture park.
  Avoid repeating generic policy sampling; next optimization needs a concrete
  CPU-work hypothesis. Native menu paused again. No speed, gameplay or save claim.


## 2026-09-08 — R630 reject false-green UIKit launcher results

- Previous turn progress: installed candidate and actual touch file selection.
  Revalidated paused95966. Bounded verification defect: simctl had returned0 for
  R627 UIKit exit1, so launcher success cannot establish a passed control test.
- Added explicit GALAXYPAD_UI_TEST_PASS only after all UIKit checks. New
  tests/check-mobile-ui-result.py requires exactly one marker and no FAIL lines.
  Missing/duplicate markers and failures reject. Three unit tests cover these
  cases; integrated parser tests into scripts/check-repository.sh.
- tests/run-mobile-ui.sh accepts one already-booted UUID, rejects any differing
  booted-device set, builds/installs only isolated harness, keeps unique log and
  validates completion. No boot, product replacement, save mutation or forced
  product termination. UI tests remain separate from headless repository suite.
- python3 tests/check-mobile-ui-result.py generated/ui-r627-before.log exits1,
  correctly rejecting real recorded failure. Fresh runner on sole iPad exits0;
  generated/ui-runner-r630.log points to generated/tests/mobile-ui.EdussX with
  explicit completion. Parser tests and bash syntax pass. Full suite not rerun.
- Foreground product returned same95966; app0037f222 unchanged, existing pause
  retained. No FPS/catch/save acceptance. Documented reproducible UIKit command
  in INPUT-AND-POINTER.md. Continue candidate input/performance work; full goalopen.


## 2026-09-08 — R629 deploy revised UI instead of preserving a stalled test

- Prior pass yielded narrow resume evidence but no progression. Reoriented:
  preserving assistant-owned unsaved tutorial position was delaying the user's
  requested UI iteration. Explicitly announced intentional loss of that unsaved
  test position; preserved existing saves, no borrowed state or injection.
- Copied exact title524d4745/data to ignored generated/save-backup-r629.
  GameDataSHA99d432d517b92b19da0c403819b91288b10c7063c3ce61f5000347ef823ab64f,
  bannerSHAbb36d744eb39166f7d8c4c0d182ef7893075034e92473ecbb6aee8c4482e1ef3.
  Actual native Stop Game confirmed; runtime87155 exited15:40:06.682 failed=0;
  terminated shell, session24676 exited0. Old in-memory checkpoint no longerexists.
- Installed candidate0037f222. Simulator relocated data container to1667B463-
  8717-4DD3-8E95-141BDFEF9E80; initial old-path hash failed, resolved fresh container
  through simctl and confirmed GameData exact hash unchanged. No save restoration
  needed. Launch same R619 arguments/module, trace0/pointer experimentsNO/.55opacity,
  output generated/ios-runtime-r629.log, PID95966/session5906. Only same iPadbooted.
- Title rendered with revised controls; diagnostic A+B helper26290 exit0 reached
  file select. Actual UIKit drag345,430→375,465 plus relocated A selected retained
  Mariofile1 dated09/08/2026 07:02, zeroStars. Screenshot
  generated/ui-r629-installed-file-native.png. Native menu paused at file detail.
- Sizing and stick-routing sanitizers rerun pass. Current title/file windows
  34.589789/36.797005FPS; transition screenshot briefly6.4FPS. No speed improvement
  or gameplay/HUD/GrandStar/save-loop acceptance. New layout is now installed,
  unlike prior previews. Next work against this candidate, not obsolete87155.


## 2026-09-08 — R628 live resume after isolated UI tests

- R627 classified progress: failing-then-passing real UIKit geometry regression.
  Current87155/24676 verified live, one booted iPad. Dismissed existing native
  menu15:35:14.043; blocked=0/input cleared. Live gameplay rendered and movement
  responded after foregrounding from isolated harness. Actual touchY responded.
  Narrow lifecycle evidence only: not background audio, memory warning, renderer
  recreation or full interruption matrix acceptance.
- Diagnostic helpers44492 forward4s,39922 forward+A3s,88563 right4s exited0 and
  released input. Rabbit disappeared over crest, approach reached crater then
  grass/dirt edge beside large white/yellow flower patch. No catch/dialogue/save.
  This route is not progression evidence and must not be repeated unchanged.
- Screenshot generated/ui-r628-resumed-gameplay-native.png; continued R619log
  windows32.73–43.14FPS, scene-dependent not speedup. Native menu reopened safely,
  preserving same process and checkpoint. Candidate0037f222 remains uninstalled.
- Next requires reliable rabbit target/interception rather than more blind
  camera-relative pulses. Original first-play/save, touch, performance and full
  PRD gates remain open; no claim of completed game or successful persistence.


## 2026-09-08 — R627 focused A/HUD clearance regression

- R626 was progress: real UIKit exposed remaining A/HUD overlap. Revalidated
  live87155/sole iPad. Hypothesis: lower A alone42pt while preserving other
  action positions, saved layouts and mappings; avoid another whole-cluster shift.
- Added tests/mobile-ui/main.mm observed Gateway inventory rectangle relative
  to viewport (x.85,y.83,w.10,h.09). Before source change, isolated UIKit output
  generated/ui-r627-before.log explicitly FAILs A/HUD clearance. simctl console
  itself returned0 despite test exit1: inspect test output, not launcher status.
- Changed default iPad A bottom anchor202→160. Rebuilt harness and reran:
  generated/ui-r627-after.log reports suite pass including new HUD assertion,
  all-control non-overlap, A+B/release/cancel, editor and pointer handler checks.
  Preview uses real production overlay; visually inspected and captured
  generated/ui-r627-layout-preview-native.png. A lower edge remains safe and
  A/B/Y separated. Not gameplay screenshot or full HUD/context acceptance.
- Terminated exact preview bundle; simctl foreground returned existing87155.
  Original blocked=1 log unchanged, process2.3%CPU; no game restart/secondgame.
  Product Release build exit0, generated/build-ios-app-r627.log, candidateSHA
  0037f222c4a9846b59b4ff45d72de1ee54ed2df7b218fe7b97394ed2fd9708cd, NOT installed.
- Next substantive lane: preserved first-play/save progression before replacing
  product, then live candidate layout check. Pointer offset, sustained touch,
  performance, physical acceptance and original full goal remain incomplete.


## 2026-09-08 — R626 real UIKit letterbox coverage and preview

- Previous turn progress: implemented/built placement candidate, no visual claim.
  Revalidated live87155. Found existing tests/build-mobile-ui.sh isolated UIKit
  host, avoiding replacement of unsaved game. Only same iPad used, no secondgame.
- Extended test viewport to recorded normalized letterbox; updated obsolete
  Plus/Minus paired-position assertion to inward primary Plus expectation.
  Source audit found optional tilt would collide with lowered actions; moved its
  default iPad center inward (right-400,bottom92). No saved positions changed.
- Built and installed org.galaxypad.overlay-tests only. Console test exit0:
  generated/ui-r626.log; minimum44pt/all-control non-overlap, glyph-only labels,
  A+B independent release/cancel, pointer handler, disable/editor/size tests pass.
  These handler tests are not real multitouch delivery or gameplay acceptance.
- Added explicit --preview to existing harness: after tests it resets only its
  own preference domain, shows actual production overlay on neutral letterboxed
  field, stays live for inspection. Screenshot generated/ui-r626-layout-preview-native.png.
  Visible lower arrangement improves playfield clearance, but A top near565px
  can still overlap gameplay Star Bit row near574px. Partial defect remains.
  Do not accept full HUD clearance or ergonomic playability from this preview.
- Preview terminated by exact bundle; foregrounding product returned existing
  PID87155, screenshot verified original native menu/rocky-grass checkpoint intact.
  Product rebuild exit0, generated/build-ios-app-r626.log; candidateSHA
  bc8e47e19dce2826cfd86e7b2adbedcd83279d7732f192b5420b654ec1e86296, NOT installed.
  No performance change. Next refine actual remaining A/HUD geometry; full
  gameplay, save/reload, pointer calibration and original goal remain incomplete.


## 2026-09-08 — R625 default iPad action cluster placement candidate

- Previous turn classified progress: actual touch collection/shot evidence changed
  next action to measured HUD overlap. G10/G12 continuation, full earlier gates
  remain unmet; no completion redefinition. Sole iPad and live87155 revalidated.
- Hypothesis: edge-anchor A/B/X/Y/Z as one unchanged arrangement in the available
  lower space to uncover inventory HUD; move small Plus inward beside it. Source
  change only for default iPad geometry; saved origins and phone geometry remain.
  No mapping, gesture, pointer-depth or gameplay semantics changed.
- GalaxyPadGameOverlay.mm computes group bound from default frames plus actual
  configured sizes, never saved positions. bottomActionShift uses safe bottom
  minus group bottom minus12pt, only if a lower viewport gutter exists. A shift
  capped at gutter height would still partially cover the Star Bit HUD, so the
  action cluster uses full remaining safe-area room. Custom origins are skipped.
- tests/test-mobile-control-size.sh and tests/test-mobile-stick-routing.sh pass
  sanitizer checks. Initial wrong scripts/test-mobile-sticks.sh path failed127;
  corrected to actual test path, not a product failure. Release app build via
  scripts/build-ios-simulator-app.sh exit0, generated/build-ios-app-r625.log.
  Candidate appSHA157e229cb2361207bd162fe897a441ef55b26ef50a4dc67f826a4bdd11161e33.
- Candidate NOT installed: current paused runtime87155 still oldb853f060 and
  R624 unsaved checkpoint, CPU2.1%. Source/tests/build do not prove visible
  layout or touch acceptance. Next validate candidate when checkpoint can safely
  be replaced; current-game progression/persistence and pointer offset still open.


## 2026-09-08 — R624 actual touch Star Bit collection and shooting

- Preserved R619 runtime87155/session24676, only iPadDE8E956F; no rebuild/restart.
  Actual Y recentered camera, revealing roof Star Bits. Initial touch aim clipped
  at right edge; correction798,300→798,310 collected roof bit with Mario stationary
  (counter0→1, roof bit disappeared). This is actual UIKit pointer collection,
  not movement-proximity inference. Full-viewport alignment still fails.
- Aim305,280→305,290 placed cursor near left-rim rabbit. Actual B spent1→0 with
  visible impact. No clear stun/catch dialogue; do not accept either. Diagnostic
  movement helpers16261,98667,62208,31048 exited0 and neutralized. Approaches ended
  on rocky grass near fleeing rabbit; sustained touch movement remains unverified.
- Dolphin Cursor defaults offset10cm/yaw25/pitch20 feed EmulatePoint simulated
  pose rather than direct pixel mapping. Already-known evidence, not newly proven
  sole root cause; experimental pitch/calibration paths remain disabled.
- Artifact generated/ui-r624-pointer-shot-approach-native.png and continued
  generated/ios-runtime-r619.log. Recent windows33.92–40.85FPS are scene evidence,
  not optimization. Right controls overlap coin/Star Bit HUD; UI remains unfinished.
- Native pause15:24:59.085 blocked=1/input cleared; live process1.4%CPU and session
  running, sole booted iPad confirmed. Preserve checkpoint. No rabbit catch, save,
  physical controller/device, audio, sustained-touch, or full-goal acceptance.
  Next prioritize concrete pointer/layout defects; avoid repeating blind chase.


## 2026-09-08 — R623 pipe and crater interactions, catches still open

- Previous turn progress: hide-and-seek active. Verified same live87155/24676 and
  sole iPad, resumed native menu. Diagnostic movement crossed flower/pond to
  tree-ring bush. Actual touch pointer/B spent lastbit1→0, no visible immediate
  reveal. Subsequent rabbit on rim is not enough to identify shot/reveal cause.
- Navigated to green pipe; diagnostic jump/left1.5s entered, emerged by green-roof
  house. Jump/forward1.5s returned to grassy pipe side. Both direction transitions
  visually verified. No separate hidden-rabbit release/catch claim supported.
- Followed visible rabbit toward crater rim, then entered crater: scene emerged
  by blue-roof house, HUDcoins7/life3/bits0. Screenshot
  generated/ui-r623-crater-exit-native.png; continuing ios-runtime-r619.log.
- Helpers54488,93149,16626,20089,38738,82114,39561,34117,37577,61759,20814,
  99622,27647,76366,24767,54279 exited0 with neutral release; one0.5s jump also
  exited0. Movement remains diagnostic controller, not actual sustained touch.
- No catch dialogue observed, no catch count accepted. Native pause15:13:48.874
  blocked=1/input cleared; process87155 live2.5%CPU/session24676 running.
  Preserve state. Next targeting/ammunition/catch approach should change rather
  than repeating blind chase; full goal, performance and touch layout remainopen.

## 2026-09-08 — R622 actual touch shot and three-rabbit search begins

- Previous turn progress: Gateway arrival. Verified PID87155/session24676 alive;
  read reference TrickRabbit chase/stop/contact code and older macOS journal.
  Important boundary: that source does not establish initial actor/state identity;
  do not turn earlier catch hypothesis into claimed current-runtime evidence.
- Resumed existing native menu. Diagnostic move33686 exit0 found rabbit near
  horizon. Actual touch drag470,280→470,290 produced pointer~469,247; corrected
 470,307→470,317 placed pointer~469,276 near rabbit. Thus visible aim still has
  vertical offset; usable local correction is not full-viewport calibration.
- Actual touchB consumed one Star Bit2→1; diagnostic move+jump31425 exit0 and
  forward97117 exit0 approached group. No independently observed stun/catch.
  Game visibly entered Let's-play-hide-and-seek, then catch-all-of-us dialogue.
  Actual touchA advanced both; three rabbits dispersed and gameplay resumed.
- Evidence generated/ui-r622-hide-seek-native.png and continued R619log. Accepted
  milestone is three-rabbit search active, not individual catch or GrandStar.
  Movement still diagnostic source, no sustained touch-stick acceptance.
- Native pause14:52:58.378 blocked=1/input cleared. PID87155 live1.9%CPU,
  session24676 confirmed running, elapsed1hour includes pauses and is NOT a
  60minute active gameplay soak. No restart/build/save-state mutation. Next
  resume at flower patch and locate crater/pipe/grass hiding places; full goal
  remains open, including performance, accurate pointer and touch layout.

## 2026-09-08 — R621 live Gateway arrival and curved-surface movement

- Previous turn progress: castle trigger. Verified PID87155/session24676 live,
  sole iPad, then native menu dismiss. Waited on same handle while visible
  cinematic advanced through castle lift/Mario fall/Gateway planet. No restart.
- Mario/rabbit arrival dialogue visibly rendered. Actual touchA advances
  Finally-you-are-awake dialogue into playable Jump-with-A tutorial. ActualA
  attempted jump; strongest new accepted evidence is dialogue and gameplay
  transition, not a separately captured full jump arc.
- Diagnostic axes94254,1802,3452,89273(move+jump),78961 exited0/released; Mario
  visibly moved from landing flowers across curved grassy/rocky surface with
  changing camera. Rabbit remains in initial tutorial; no capture/three-rabbit
  completion claim. No touch-stick or physical-controller acceptance.
- Screenshot generated/ui-r621-gateway-native.png, log continues R619.
  Gateway windows35.737710/37.287533/37.175992FPS, later35.064846; different scene
  from invasion, not an optimization gain. Audio/save/reload/GrandStar stillopen.
- Native pause14:39:52.658 blocked=1/input cleared. PID87155 live3.3%CPU,
  session24676 confirmed running. Preserve and dismiss menu next; no rebuild or
  relaunch. Full PRD goal active; next initial rabbit tutorial/first-play route.

## 2026-09-08 — R620 live continuation through castle arrival

- R619 progress, verified PID87155/session24676 alive and sole iPad. Native menu
  dismiss resumed existing scene; invasion cinematic advanced and returned to
  controllable attacked plaza. No restart, module change or state injection.
- Leased diagnostic axes navigated around crystals/fountain, up princess route,
  over bridge and to castle doors. All helper sessions86697,73997,59613,31501,
  37890,54726,78049,53074,4559,39776,71418,81068,60384,99339,60436,10689,77925
  exited0 and automatically released. Several direction corrections followed
  actual screenshots as camera changed; no sustained-touch evidence implied.
- Castle arrival triggered lift cinematic, visually verified. Evidence
  generated/ui-r620-castle-lift-native.png and continuing ios-runtime-r619.log.
  Late approach windows35.13/36.41FPS, cinematic25.53/24.83; differing scenes
  not comparative speedup. HUD life3/bits2; no save or pointer collection claim.
- Native pause14:25:44.194 logs blocked=1/input cleared; PID87155 at2%CPU,
  session24676 confirmed running. Preserve live checkpoint; next inspect/dismiss
  menu and continue toward Gateway. No production edits/build, no new Simulator.
  Full goal still open, especially FPS, touch layout, sustained touch, audio,
  GrandStar/save/reload and physical-device gates.

## 2026-09-08 — R619 normal iPad gameplay reaches invasion; live paused

- Prior turn ruled out repeating old compiler/guard experiment. Verified no game
  and sole iPad. Same appb853f060/module3acdcddc, R617 flags, trace0. Launch24676
  PID87155. Diagnostic title A+B74358 exit0. Actual CUA pointer/A selects existing
  zero-star file and Play; actualA advances five story pages and invitation.
- StarFestival: leased diagnostic moveY1 for3s visibly moves Mario onto flowerbed.
  Actual touchA click captured Mario airborne in CUA screenshot. Further bounded
  axes up/left/diagonal move over hill through village/plaza and trigger Bowser
  airship invasion. Helpers66374,41713,2784,96046,32272,93536,35151,47021,96078,
  41882,43767 exited0 with neutral finally release. HUD changes bits0→1 while
  moving; not evidence of pointer collection. No savestate/game-memory shortcut.
- UI defects visible: controls still overlap story text and right coin/bit HUD.
  Do not claim layout acceptance. Movement was diagnostic controller source;
  actual touchA jump and file/story interactions are distinct touch evidence.
- Gameplay windows29.51/30.56/30.10FPS; invasion27.16 then22.79. No speed gain.
  Logs generated/ios-runtime-r619.log; screenshots ui-r619-before-move-native.png
  and ui-r619-invasion-native.png. No audio/save/reload/GrandStar acceptance.
- Native menu pause14:07:34.137 logged blocked=1/input cleared. PID87155 remains
  live at2.1%CPU; polled session24676 still running. **Do not kill/relaunch this
  checkpoint.** Next inspect state and dismiss native context menu (last AX46)
  to resume invasion. One Simulator only. Live continuation avoids replaying the
  introduction; full goal remains active.

## 2026-09-08 — R618 current module provenance and PC resolution

- Prior turn progress supplied normal-module baseline. Read current status and
  build script/CMakeCache/Ninja, confirmed no GalaxyPad process. Effective flags
  are PGO,ThinLTO,finalO2,strictFP. No debug-build hypothesis supported.
- dwarfdump UUID EFDD0591-6DA6-3B40-B32D-505977B60142 matches R617 sample.
  nm plus bounded llvm-objdump resolves explicit804B60A0 PCs0x5f62ba8 and
  0x5f66404 to lazy-FP-global loads; aggregate13sample count cannot be split
  between the abbreviated PC list.803A30A0 PCs are prologue/CPU pointer copy.
- Crosschecked PERF R449: narrow availability-guard experiment already rejected.
  Do not repeat it based on this small sample. No production/build/runtime
  changes or speed gain. PERF updated with exact current-module boundary.
  Next prioritize actual first-play progression; current suite green does not
  satisfy gameplay, touch hold, save/reload or physical-device requirements.

## 2026-09-08 — R617 normal-module visible performance baseline

- Prior turn progress: full regression pass. Revalidated no game, sole iPad,
  normal module SHA3acdcddcd47812c2e2a67b8cec0cf06c17009cf1ad7f2e9dc9abd83158a1bac0.
  Installed app unchanged b853f060; launch74469 PID86550, same paths/flags asR615
  except movement trace0. Diagnostic A+B lease1773 exit0 reached file select.
- CUA visually verified file list; native screenshot generated/ui-r617-file-select-native.png.
  Pre-profile window ending13:49:16.491:30.500377s,1148frames,37.638879FPS.
  Earlier36.312758 window may include transition; initial54.771216 includes boot.
  Later37.082077 window overlaps sampling and is not independent baseline.
- sample86550 3 10, handle96403 exit0, generated/ios-r617-file-select.sample.txt:
  246CPU observations;205underRun+1072,200nestedchassis incl23float-future waits.
  Largest individual chunk shown13observations; work distributed. Video shows
  23PeekEFBDepth and48RunGpuLoop leaf observations among other work. Nested counts
  are not additive CPU cost, and sampling is not a physical GPU comparison.
- No trace records; normal module still slow, not merely pointer diagnostic
  overhead. Clean native exit13:50:15.174 failed=0 then shellterminated. No save
  mutation or actual gameplay progression accepted. Next investigate current
  module's distributed CPU workload rather than reusing old exact-PC attribution
  from a different macOS binary; do not extrapolate this to device performance.

## 2026-09-08 — R616 full regression after UI/input changes

- Prior turn progress: actual UIKit contact reached device and release cleared.
  Revalidated sole iPad booted, no GalaxyPad process, preserved dirty tree.
- bash scripts/check-repository.sh > generated/check-r616.log 2>&1,
  session80809 exited0 with Repository safety checks passed. Includes added
  movement trace, settings default and layout/input tests; generated-code tests
  also pass. Existing nested-comment compiler warning remains nonfatal.
- Read retained R606 top load sites and bounded exact module disassembly.
  func_804B60A0 entry copies x0 to x19; sampled0x5f7e330 loads offset0xd98
  followed by cycle-budget comparison. Existing R470 already identifies this
  offset as downcount; no new optimization or removable-cost claim follows.
  Avoid repackaging old audit as a performance gain.
- No runtime/build/module changes this turn. Full suite is regression evidence,
  not G6/G10 acceptance or clean-clone/publication audit. Next substantive lane:
  current normal-module gameplay/performance measurement, keeping the trace off;
  actual sustained touch and complete first Grand Star loop remain open.

## 2026-09-08 — R615 live touch-to-device trace

- Previous turn progress: boundary tests. Revalidated no game and sole authorized
  iPad booted. Added MovementTrace at accepted overlay callback and actual device
  UpdateInput. Exact environment1 opt-in; default no clocks/output; logs changed
  axes only, maximum128 records per instance, poll indices and steady-clock ns.
  No input behavior changed. Test verifies opt-in, unchanged suppression, cap;
  added to repository runner. Focused trace/input tests and diff guard pass.
- Release build6822 exit0, generated/build-ios-app-r615.log. Install38992 exit0;
  app b853f060933607ed356754485c572e79f8ea0ddcc4b892f1032dfea63548f227.
  Runtime84935/session40601 normal module3acdcddc, prior flags plus
  SIMCTL_CHILD_GALAXYPAD_MOVEMENT_TRACE=1; generated/ios-runtime-r615.log.
- Actual CUA center drags153,595 to198,595 twice publish only near-neutral
  (-.007642,.021738) then zero after.756ms/9.337ms, no device nonzero record.
  Off-center190,595 to190,565 publishes(.697838,.021738) at478403811976083ns;
  device identical at478403817827500ns poll16119, touch zero478403823719166,
  device zero478403830301541 poll16123. Thus real contact reaches device;
  prior short center drags do not establish broken sustained movement.
- Startup/title input test only, not gameplay movement or FPS acceptance. No
  minimum hold workaround. Clean native stop13:43:09.611 failed=0, shell
  terminated. Next resume substantive gameplay/performance work; sustained touch
  usability remains unproven and cannot be inferred from this boundary trace.

## 2026-09-08 — R614 held movement versus transient drag boundary

- Hypothesis: R613 short automated drags may release before device polling;
  neutral diagnostic controller ownership might alternatively mask touch.
- Inspected stickChanged/publishInput, InputMixer, GalaxyPadInputDevice and
  mobileWiimoteConfig. Move axes map to Nunchuk with matching signs. Mixer uses
  strongest axes independently of connection ownership; only buttons latch.
- Added focused test-mobile-input.cpp cases:120 held polls with neutral connected
  controller, immediate release, and nonlatched pre-poll analog contact. Both
  bash tests/test-mobile-input.sh and bash tests/test-mobile-stick-routing.sh
  pass under existing sanitizers; git diff --check passes.
- No behavior change or live input proof. Short-drag sampling remains hypothesis,
  not confirmed cause; no additional simulator or game launched. Next bounded
  publication/consumption trace of real UIKit drag, without pulse stretching.

## 2026-09-08 — R613 viewport refresh and visible layout validation

- R612 progress fixed missing viewport invalidation. Revalidated no game and
  sole iPad Sim, candidate8eb804084c386d25a53fcc8395fa18c7745aa41b3afa95874ea7ab27d2e859f9.
  Installed40788 exit0; runtime83333/session41746. Same normal module/R612 flags,
  temporary opacity0.55, all pointer experiments disabled.
- At13:19:14.798 log viewport{{0,0.1031175},{1,0.793765}}; actual title screenshot
  shows left stick/D-pad shifted downward. Thus R612 cause and correction now
  have live evidence. Actual file1 hover+A and Play hover+A reached story.
  Screenshot ui-r613-shifted-story-native.png. Repeated native A advanced story
  pages and invitation to visible Star Festival starting area. Diagnostic A+B
 42529 only at title, exited0/reset neutral. No new file/copy/erase action.
- Text is substantially less obstructed, but longer paragraphs still meet upper
  edges of left controls. Not complete layout/readability acceptance. First area
  screenshot ui-r613-before-move-native.png. Up drag153,595→153,550 and three
  rightward drags153,595→198,595 did not show clear Mario displacement. Do not
  infer successful movement from attempted input or background animation.
- Reloaded native UI API documentation: drag has only endpoints, no sustained
  touch/duration API. No alternative event-synthesis bypass used. Source mixer
  uses strongest touch/controller axis, so neutral diagnostic controller does not
  obviously override touch. Need axis publication/consumption evidence to
  distinguish short pulses from actual routing failure. No product workaround
  or minimum-hold behavior added. FPS remained visibly poor/variable.
- Native Stop clean exit13:33:13.203 failed=0; terminated idle shell to discard
  preview argument. New app installed/stopped, sole Sim. Full original goal
  active; movement, full first-play/save, phone/device and performance open.

## 2026-09-08 — R612 live placement failure and viewport invalidation

- R611 progress built bounded letterbox positioning. Rechecked no game process,
  sole iPad Simulator, no saved GalaxyPadControlOrigins in existing prefs. Installed
  R611 d51f48bb…a0f8f1d9695 exit0. Preview runtime82906/session77229, normal module
  and R610 settings with temporary opacity0.55, all pointer experimentsNO.
- Title visibly reached but left controls stayed at previous positions. No A+B,
  file selection or save action. Source inspection: normalizedViewport initially
  zero, later published by after_present hook. Overlay's original layout skips
  empty viewport; no existing invalidation notified it of the published viewport.
  Thus geometry test alone did not prove installed behavior. No layout pass.
- Native Stop clean exit13:16:45.888 failed=0 in ios-runtime-r612.log; terminated
  idle preview shell. Added cached CGRect to main controller; existing0.1s UI
  timer compares current normalizedViewport and calls setNeedsLayout only when
  changed. Log on change captures actual rectangle for next validation. No new
  timer or per-frame layout, no renderer/input mapping changes.
- Release build83057 exit0, generated/build-app-r612.log. git diff --check passes.
  New candidate not installed/live tested. Next verify viewport publication and
  actual shifted bounds, then story readability/touch. Full goal active; no FPS
  improvement or control-layout acceptance from compilation alone.

## 2026-09-08 — R611 default left cluster uses bottom letterboxing

- R610 progress verified opacity and remaining word overlap. Inspected actual
  default geometry, saved-origin placement and grouped D-pad path. Added bounded
  bottomLetterboxShift helper: min(available bottom letterbox, safe bottom minus
  scaled default cluster bottom minus12pt), nonnegative/finite. No gameplay
  viewport or image scaling changes.
- After default placement, shift unsaved iPad move stick and unsaved grouped
  D-pad by same default-derived amount before laying out D-pad buttons. Saved
  move/D-pad origins remain untouched independently. Calculation uses default
  positions so moving one control does not shift the other; enlarged controls
  reduce available shift. Phone layouts, right buttons, control sizes and input
  mapping unchanged. Empty viewport skips adjustment until real geometry exists.
- Expanded control-size tests for no letterbox, tight bounds, finite values and
  range grid. ASan/UBSan control-size plus movement/tilt routing tests pass.
  Release build16088 exit0, generated/build-app-r611.log. AppSHA
  d51f48bb963354071e630b55f0e2971a0c096b54be80c937d549539c0a0f8f1d.
  git diff --check passes. Not installed; R610 remains installed/stopped. Next
  actual story screenshot/touch and saved-position validation; no visual or
  gameplay/performance acceptance claimed from geometry tests alone.

## 2026-09-08 — R610 installed opacity and touch preview

- R609 progress built/tested0.55 absent-key default. Verified R608 clean runtime
  exit, sole iPad Simulator, R609 hash206091cb…406260. Terminated idle shell,
  install29721 exit0. Initial runtime81906/session38199 reached title but controls
  remained opaque. Read app preference: GalaxyPadControlOpacity1.0. Thus actual
  R608 appearance used explicit100%, not inherited82% default. Preserved it.
- Native Stop clean exit13:01:42.651 failed=0. Terminated shell then launched same
  candidate/module/settings with temporary argument-domain opacity0.55 only.
  Preview82101/session49665; no persistent setting write. All pointer experiments
  remainNO; normal module3acdcddc. No Metal trace or FPS A/B inference.
- Title diagnostic A+B62796 exit0/reset neutral. Actual touch file1 drag+A,
  Play drag+A reached story. Visually lower opacity; text visible through controls
  but left stick/D-pad still overlap words. Captured native screenshot
  ui-r610-story-opacity55-native.png. Explicit touch A advanced to next illustrated
  story page, visibly confirmed. This validates these touch actions, not complete
  controls/layout/readability/performance. No copy/erase/new save action.
- Native clean exit13:08:07.140 failed=0. Read persisted opacity still1.0, then
  terminated idle preview shell to discard launch override. New app remains
  installed/stopped, sole iPad Sim. Logs ios-runtime-r610.log and
  ios-runtime-r610-preview.log. Next use available letterbox space for default
  control positioning while preserving saved layouts; lower alpha is partial.

## 2026-09-08 — R609 lower default overlay opacity

- R608 progress proved default touch file-to-story route and visible dialogue
  obstruction. Compared SunPad reference getter/styling: both default0.82 and
  opaque tinted fills. Kept shapes, colors, labels, hit targets and layout;
  changed Galaxy absent-key opacity to0.55. No saved preference migration or
  forced reset. Editor still full opacity; user slider range unchanged.
- Extended existing read-only settings test with absent-key0.55 assertion;
  existing explicit0.82 and clamp tests prove overrides retained. Discovered
  this test lacked a suite invocation; added Foundation/ARC ASan/UBSan runner
  tests/test-mobile-settings.sh and check-repository entry. Focused35985 exit0.
  Full suite last passedR607; not rerun for this change. git diff --check passes.
- Release build95330 exit0, generated/build-app-r609.log, appSHA
  206091cbd7b1d9b60ffa3b926fb831dd5ff58b261cd56ee63fc7bb158d406260.
  Not installed; R608 installed old shell80907 still idle after confirmed clean
  runtime exit. No new game/Simulator. Next live story comparison and touch
  input verification; lower opacity alone does not prove text is sufficiently
  readable or solve physical overlap. No FPS claim, full goal remains active.

## 2026-09-08 — R608 default touch file-to-story route

- R607 progress restored green full regression suite. Audited normal touch path
  versus experimental R585/R586 claims: calibrated mapper requires Simulator
  pitch22/context/calibrated flags and is neutral/file-menu limited. It must not
  be mistaken for default product behavior. Selected normal baseline validation,
  not another performance A/B comparison.
- Revalidated no runtime, sole DE8E956F iPad Simulator, normal app SHA
  ff855d69067bc1cac94d53ce2770f807d760ee3510f18ce0aea0a243b9cb34e4 and normal module
  3acdcddcd47812c2e2a67b8cec0cf06c17009cf1ad7f2e9dc9abd83158a1bac0. Runtime80907/
  session37828, all pointer flagsNO including pitch22/context/calibrated/native
  observer/hooks; no Metal trace. This changes multiple settings from profiling,
  deliberately establishing default-path behavior, not attributing FPS changes.
- Title visible then diagnostic Controller A+B53022 exit0/reset neutral. Actual
  UIKit drag345,430→375,465 hovered existing Mario file1; explicit touch A opened
  detail. Drag600,575→635,585 highlighted Play This File; explicit A reached
  opening story. Native screenshot ui-r608-default-story-native.png. No Copy,
  Icon, Erase or new-file action; no save hash comparison. Actual default aim
  works for these broad targets despite visible coordinate offset; full viewport
  accuracy and gameplay pointer mechanics remain unproven. No Direct Touch pass.
- Yellow P2-labelled star appeared near right edge in detail. Read config showed
  only Mobile device under Wiimote1; other Wiimote sections empty before stop
  and absent afterward. Default sources1emulated/othersnone; this does not prove
  runtime connection state or explain icon. Record observation, no speculative
  controller disable/config mutation. Controls still obstruct story text.
- Native Stop clean exit12:53:48.805 failed=0 in ios-runtime-r608.log. Sole Sim
  remains, normal app shell idle; no candidate restore needed. No new source or
  build, no performance/full-gameplay/physical/save acceptance. Next address
  default control usability beyond broad file-menu targets while keeping this
  evidence separate from calibrated mapping experiments.

## 2026-09-08 — R607 full regression sweep and stale fixture repair

- R606 progress added tested load classifier. Revalidated no GalaxyPad process
  and sole DE8E956F iPad Simulator. Reviewed full check-repository script before
  running43555; failed at test-macos-reference-jit.py expected source053fa674.
  Original failure preserved generated/check-r607.log. No blind hash update.
- Compared retained R423 diagnostic source and current dolphin_runtime.cpp:
  only canonical R557 built-in descriptor loading differs aside from deliberately
  unapplied JIT selector. Current SHA4d6a94340dce60254990d9ec7010fe267ba9ff3f366b5e7eef49499f8026e72d.
  Updated test to require this hash, reverse only runtime.cpp hunk of canonical
  0028-builtin-mod-descriptors.patch in temporary copy, require original053fa674
  hash, reapply and require byte equality, then exercise/reverse JIT selector.
  Checkout/ref source remains untouched. Focused88448 exit0: default AOT, exact
  opt-in and mobile/other-target exclusions pass ASan/UBSan.
- Full changed-input rerun53750 exits0, generated/check-r607-fixed.log ends
  Repository safety checks passed. Includes recent pointer/control, EFB flush,
  Metal timing, load-origin, runtime arithmetic and patch regressions. Tracked
  material checks cover tracked scope only; this is not a public package audit,
  clean-clone proof, physical-device test or gameplay/performance acceptance.
  Existing compiler comment warnings remain nonfatal. No runtime/build launch,
  normal app unchanged/stopped. Full goal remains active; next CPU investigation
  starts from a green repository suite, without speculative optimization.

## 2026-09-08 — R606 conservative branch-table load classification

- R605 progress established build flags and conservative load origins. Read
  current report, selected highest sampled loads, then narrow exact retained
  macOS disassembly.805170A0 entry0x6798c94 reads CPUState.pc at x0+0x280;
  no x0 mutation before it.0x6798cc4 reads halfword table whose loaded value is
  scaled/added to ADR code base then branched.804AB0A0 at0x5e61d00 loads EXRAM
  base x0+0xda0; source/prologue path and retained CPUState offsets agree.
  These are code/CPU-state work, not automatically guest-data accesses.
- Extracted existing stack/lazy-global rules into load_origin helper and added
  strict six-instruction adjacent PC-relative table pattern. Other forms remain
  unresolved; this is a local pattern proof, not general CFG/register analysis.
  Tests reject missing dependencies, wrong base/destination/shift/branch target,
  and refuse to classify arbitrary x19 loads as CPUState. Added check-repository
  entry. Existing classifier tests and new focused regression pass; full suite
  not rerun this turn.
- Re-ran exact-module audit53156 exit0, generated/shared-aot-work-r606.json:
  selected7782/28822 CPU leaves;4776loads split147table,640lazy-global,185stack,
  3804unresolved. Table~0.51% CPU leaves, not dynamic instructions/removable time.
  No dispatch rewrite or compiler rebuild justified from this subset. Retained
  macOS profile remains separate from current mobile sample. No game/runtime,
  normal app unchanged/stopped. Next remaining high-weight base provenance.

## 2026-09-08 — R605 effective CPU flags and retained load provenance

- R604 progress narrowed staging impact and restored broader CPU priority.
  Rechecked STATUS and R562 graph: actual Ninja flags end inO2 after ReleaseO3,
  with ThinLTO, existing PGO rmge01.profdata, ffp-contract=off/fno-fast-math.
  CacheO3 alone is misleading. Graph settings are not independent reconstruction
  of binary provenance; no debug/unoptimized build explanation supported.
- Reviewed PERF R470/R469/R460/R449/R451/R592: prior compiler, footprint,
  tiny-helper and mapping-cache candidates lack repeatable gains. No repeated
  whole-module rebuild or unchanged performance experiment.
- Existing audit-shared-aot-work.py93510 exit0 verifies exact retained macOS
  module1fb635f7…01dc7a and profile before per-chunk disassembly. New report
  generated/shared-aot-work-r605.json:7782 selected of28822 CPU leaves,4776loads.
  Origins640 adjacent lazy-FP-global loads,185 stack,3951 unresolved. Adds origin
  counts missing from R470 final, not fresh sampling or R562 mobile attribution.
-640 cases are~2.22% of retained CPU leaves, not removable time or all flag-load
  coverage. Unresolved does not imply redundant mapping or guest-data access.
  Next resolve remaining high-weight bases with actual register provenance.
  No runtime, product change, Simulator change or FPS gain. Full goal active.

## 2026-09-08 — R604 common-clock GPU completion comparison

- R603 was progress: matched waits localize delay before handler entry. Checked
  status/process state. Rechecked Apple GPUEndTime contract (relative to system
  mach time, read after completion). Probe now explicitly uses mach_absolute_time
  converted with mach_timebase_info and wide integer arithmetic; CSV records
  clock domain and GPU absolute endpoints. Non-Apple steady clock never receives
  GPU/host cross-clock attribution. Invalid order remains counted, not clamped.
- Tests cover absolute timestamps, unknown domains, NaN, unavailable clocks,
  reversed GPU/handler order and prior cap/arming/concurrency cases. ASan/UBSan
  pass13532. Staged compile and candidate relink pass; logs
  metal-staging-compile-r604.log, metal-probe-build-r604.log. Candidate SHA
  9c59d87a267eda9b0db42b19d5872c1005906f8b942e2736edc46141a91a4ab0.
  Normal ff855d69…cb34e4 unchanged. git diff --check passes before run.
- Sole iPad Sim, no prior runtime. Installed81227 exit0; PID77475/session75790,
  same R603 flags/module. Title visible, A+B47311 exit0/reset neutral, file list
  visibly settled. Screenshot ui-r604-before-arm-native.png; zero trace rows
  before unique marker. Armed12:31:13local.4096 records over5.699802s.
-198/198 waits matched with valid GPU/host ordering. GPU-end→handler median
  0.731249485/max4.325499991ms. Wait-start→handler median1.2398125ms;
  handler-end→wait-end median0.0263125/max0.84125ms. Wait total619.03521ms,
  median1.306229/max17.24775ms. GPU median0.069666683/max0.279000029ms.
  Handler3304,total1.59727ms; copy396,total9.230208ms;submit198,total0.05424ms.
  Full summary metal-staging-summary-r604.json. Simulator reporting/logging
  perturbation remains; these intervals do not prove driver root cause.
- Zoom-out check: summed staging wait is ~10.9% of captured wall span, not an
  additive cross-thread CPU percentage or predicted FPS gain. Removing this
  measured wait alone is not a sufficient explanation for roughly half cadence.
  Took sample77475 3 10 after cap (8094 exit0), ios-r604-file-select.sample.txt:
  CPU247 samples,210 Run+1072,199 nested dispatch including35 float-future waits;
  video35 PeekEFBDepth and49 RunGpuLoop. Nested counts not additive. Native
  execution remains substantial; next revisit whole CPU/render balance rather
  than endlessly refining callback timing. Do not assign ellipsized offsets
  whole aggregate counts.
- Native Stop clean exit12:32:54.247 failed=0. No Unknown Opcode match in bounded
  log, not resolution of R600 incident. Idle shell terminated; normal app restored
  98312 exit0, not launched. Full original goal active, no gameplay/FPS acceptance.

## 2026-09-08 — R603 command-correlated completion trace

- Previous turn progress: handler-body measurement rejected costly mutex/body
  as likely wait explanation. Current state checked: no game process, sole iPad
  Simulator; separate candidate only. Added optional buffer token to trace CSV,
  taken from actual retained wait buffer and completion callback buffer. Token
  is private diagnostic identity, not a global command serial number.
- Summarizer matches only unique same-token handler wholly contained in wait;
  pointer reuse outside interval ignored, ambiguous/missing/old-schema explicit.
  Tests add reused pointer, reversed row order, ambiguity, missing identity,
  invalid token and concurrent token output cases. ASan/UBSan passes11818/90471/
  74147; staged compile and candidate relink pass; git diff --check passes.
  Logs metal-staging-compile-r603.log, metal-probe-build-r603.log. Candidate SHA
  e3f426707d9267119fbae747bf608db9ef131166db497316f3bcd17b96a28c5b.
- Installed23711 exit0; runtime76911/session58488, same R602 settings/R562 module.
  Title verified then A+B39137 exit0/reset neutral. File list visually settled;
  screenshot ui-r603-before-arm-native.png; trace0lines before marker. Unique
  metal-staging-r603.arm created12:24:19local.4096-record cap reached.
- All190 waits matched,0 unmatched/ambiguous/unrecorded. Wait-begin→handler-begin
  median1.642437ms; handler-end→wait-end median0.0341665/max0.142458ms. Wait median
  1.677187ms,total710.834625ms; GPU190valid median0.083895837/max0.485625002ms.
  Handler3335,total1.593148ms,median0.000375/max0.074084ms. Copy381,total11.553418;
  submit190,total0.065165ms. Summary metal-staging-summary-r603.json.
- Evidence shifts next action toward queued work/completion delivery before
  callback entry, not handler-body rewrite or post-handler wakeup. Does not
  isolate GPU queue time from callback delivery. Handler logging perturbs wait;
  aggregate medians are not algebraically additive. No FPS improvement claimed.
  Frame windows56.0595/28.3979/27.4184 include startup/transition/capture exposure.
- Native Stop clean exit12:25:23.817 failed=0. Idle shell terminated and normal
  app restored12740 exit0, not launched. No Unknown Opcode match in bounded log,
  not a fix for earlier incident. No gameplay/stability gate passed.

## 2026-09-08 — R602 completion-handler file-select measurement

- Prior turn progress: built/tested isolated tracker probe. Revalidated no game
  process, sole DE8E956F iPad Simulator, candidate6140e4be…149faed and R562 module
  c1c06e0a…521421 hashes. Installed76053 exited0; runtime76296/session84132.
  Same launch settings as R600, unique trace/marker metal-staging-r602 paths.
- Title visually confirmed; A+B helper51419 completed0 and neutralized input.
  File list visibly settled, native screenshot ui-r602-before-arm-native.png;
  trace0lines before marker. Armed12:18:06local.4096 rows across6.347338s;
  summary metal-staging-summary-r602.json validates IDs and timing fields.
- Handler3232 spans total1.340432ms,median0.000375/max0.024041. Host wait216
  total732.696535ms,median1.4488955/max13.885709. GPU216 valid median0.073770847/
  max0.387499982ms. Copy432,total10.224385ms;submit216,total0.030377ms.
  Handler body does not appear expensive; do not rewrite its mutex. Measurements
  do not distinguish callback scheduling, command queue delay or waiter wakeup.
  Logging after handler body can inflate outer wait; spans overlap, not additive.
- File list visually intact at stop. Frame windows55.6053/38.3117/34.0396FPS
  include differing startup/title/transition/capture exposure, not an A/B gain.
  No Unknown Opcode match in this bounded log, not proof R600 defect resolved.
  Native clean exit12:18:57.898 failed=0. Idle shell terminated; normal app
  restored3607 exited0, not launched. git diff --check passes. Full goal active.
- Next isolate submission-to-completion/waiter-wakeup timing with command
  correlation only if API clock/ordering contracts support it. No blind wait
  removal, spin-polling, fake depth or peripheral UI optimization justified.

## 2026-09-08 — R601 bounded render completion-handler probe

- Previous turn made progress: scene-specific host/GPU trace changed next action.
  Rechecked status, rights/private boundary, booted devices and process state:
  sole DE8E956F iPad Simulator, no GalaxyPad process. Original full goal retained.
- Hypothesis: original render completion handler might contribute to host waits.
  Extended prepare-metal-staging-probe.py with optional --completion-handler,
  SHA-gated MTLStateTracker source79f8f187…d44040. Wraps entire original handler;
  original lock dies before trace output. No lock/wait removal, no ref mutation.
  CMake optional separate tracker object defaults empty. Summarizer accepts new
  completion_handler phase; GPU fields still only valid for wait rows.
- Tests cover tracker identity rejection, unchanged lock/handler call counts,
  recording after lock release, two concurrent writers sharing4096-record cap,
  parser acceptance plus existing clock/arming/GPU cases. ASan/UBSan pass12084/
  13707. Staged compile --completion-handler --compile and separate CMake build
  exit0; logs metal-staging-compile-r601.log, metal-probe-configure-r601.log,
  metal-probe-build-r601.log. Link map proves FlushEncoders belongs to object2,
  the staged MTLStateTracker.mm.o. git diff --check passes.
- Candidate SHA6140e4be5fc80fd7b56c968e09bd4b95de53f1d0d6dc41cab09c1f735149faed.
  Not installed/run. Normal app SHAff855d69…cb34e4 unchanged/stopped. Next use a
  fresh marker after visibly settled file select. Handler wall span includes lock
  wait/work but cannot separate them; trace I/O after handler body still extends
  actual completion return and can inflate outer host waits. No speed claim.

## 2026-09-08 — R600 explicitly armed file-select capture

- Added StagingCaptureGate to diagnostic header: optional ARM_FILE marker must
  begin with1; checks every64 calls until latched, never polls after. Operations
  still execute before arming but clocks/GPU readers/sample IDs do not. Existing
  no-marker behavior preserved. Gate polling and delayed CSV integration tests
  added; ASan/UBSan tests, staged compile and candidate build98555 pass.
- Candidate SHA efcb2fb8c856762c7241c09e46b91bf7a0f97b6b8eb4c891ce2b410382f754ea,
  runtime75316/session5835. Same R599 settings/module. Actual file list visually
  confirmed after diagnostic A+B lease reset; marker metal-staging-r600.arm
  created12:05:32local. Unique marker must not be reused for another delayed run.
  Before-arm screenshot ui-r600-before-arm-native.png; no held pointer input.
- metal-staging-summary-r600.json validates4096 records: copy2050,total42.165969ms;
  submit1023,total0.192304ms; wait1023,total3081.201533ms,median1.710834ms.
  All1023 GPU intervals valid,median0.066875014/max0.324958353ms. Different
  boundaries and Simulator semantics prevent subtracting these as scheduler
  time. Per-record fflush perturbs timing; cap may split stage sequence.
- File list still visibly intact before clean Stop. Runtime log includes FIFO
  Unknown Opcode0xff at12:08:27.966; not a stability pass despite clean exit.
  Native exit failed=0 at12:11:05.229. Terminated idle shell, restored normal app
  ff855d69…cb34e4 (67724 exited0), not launched. No saves/gameplay acceptance.
- Checked Apple waitUntilCompleted contract and actual FlushEncoders handler:
  wait includes completion handlers; handler takes backref mutex and optionally
  returns perf-query results. Next bounded probe should distinguish handler/lock
  time from delivery/wakeup; no evidence yet to remove waits or spin-poll.

## 2026-09-08 — R599 Simulator timestamp availability

- Previous turn built GPU extension. Verified no game process, sole iPad Sim,
  candidated5e0855f…b8f20 hash before install15039. Runtime74816/session59524
  same R597 launch flags/module, trace path metal-staging-r599.csv.
- Title visibly rendered;4096 cap reached with no A+B/file-select action.
  All1024 waits valid GPU timestamps, median.054687465ms/max2.174416673ms;
  host median.779208/max10.6555/total1568.959681ms. Summarizer validated records
  and wrote metal-staging-summary-r599.json; full caveats in EFB-READBACK.md.
- Availability proven only on this Simulator/run. Different interval boundaries
  prevent subtracting GPU duration from host wait as scheduler cost. Title-only
  capture identifies need for explicit post-scene arming; R597 not settled-file
  evidence. No source or performance optimization this turn.
- Native Stop clean exit12:00:16.841 failed=0; shell terminated and normal app
  restored (90000 exited0), not launched. Next arm only
  after actual file-select view before exhausting trace cap. Full goal active.

## 2026-09-08 — R598 GPU interval contract and probe extension

- Previous turn captured valid host staging trace, wait dominating measured
  spans. Reviewed Apple official GPUStartTime/GPUEndTime documentation and local
  Simulator26.5 SDK header. Contract/source link recorded in EFB-READBACK.md.
- Extended staged-only wait wrapper with post-completion GPU interval provider.
  No extra wait, no provider/clocks when disabled or capped. Completed status,
  finite positive start and ordered end required; otherwise unavailable. Added
  separate CSV fields; old traces explicitly unrecorded. Whole command-buffer
  duration must not be treated as copy-only or subtracted from host wait.
- python3 tests/test-metal-staging-timing.py passes ASan/UBSan including invalid
  timestamp and parser cases. prepare-metal-staging-probe.py --compile and
  candidate cmake build pass (56207/45940); logs metal-staging-compile-r598.log,
  metal-probe-build-r598.log. App SHA
  d5e0855f3ed65ddb5f1fe418f3caf780f651be4705c39648e44aa4227e0b8f20.
  git diff --check passes. Normal app untouched/installed/stopped; no live run.
- Next install isolated candidate and determine whether Simulator reports usable
  GPU intervals. Missing values remain missing, not a hardware-performance
  conclusion. Full original goal remains active and unaccepted.

## 2026-09-08 — R597 isolated candidate and first staging trace

- Previous turn staged/compiled probe. Added optional diagnostic Metal object to
  CMake (default empty), separate build directory and linker map. Candidate
  configured/built session61472 exited0; map shows Flush from probe object1.
  SHA982e944795fbbe36d4c6a60f9a289a3e9c1c2ea1052438826d46276680128d2f.
- R594 stop verified before terminating shell/install. Sole iPad Sim; normal
  app ff855d69…cb34e4 unchanged. Candidate runtime74135/session45565 uses same
  R594 flags/module plus trace env path. Visually title, diagnostic A+B, file
  select. No file selection or intentional save action.
-4096 valid records cap reached, generic staging over~17.225s. Setup32.114775ms,
  submit14.617679ms,wait2225.350730ms totals; full summary in EFB-READBACK.md.
  Wait is CPU wall, not GPU execution; no scene markers, whole-run coverage or
  precise depth equivalence. Enabled logging can perturb timing.
- Added validated CSV summarizer and malformed-input tests; sanitizer tests
  session46936 pass. git diff --check passes. Clean native stop11:52:30.616
  failed=0. Candidate shell terminated and normal app restored (install30734
  exited0), not relaunched. Next inspect GPU timing API guarantees and Simulator availability before
  deriving a more specific optimization. Full PRD/performance goal remains open.

## 2026-09-08 — R596 staged Metal service timing

- Previous turn added actual depth-peek caller regression. Added default-off
  patches/experiments/metal-staging-timing.h and hash-gated staging generator;
  pinned source and stable app untouched. Goal: distinguish copy setup, encoder
  submission and completion waiting rather than attributing all service to GPU.
- Tests compile wrapper with ASan/UBSan: operation exactly once/order preserved,
  disabled path no clocks/records, shared4096 cap across lambda instantiations,
  CSV ordering, and rejection of unknown Metal source. session72952 exited0;
  test added to repository suite. Full suite not rerun.
- python3 scripts/prepare-metal-staging-probe.py --compile reused exact Simulator
  compilation command with isolated source/output. session77432 exited0; log
  generated/metal-staging-compile-r596.log. Object SHA
  f6935f3fa231f654f0f8ec6100198d114b9e674ca2fbf3eadca15b9f4123adc0.
- No link/install/runtime or optimization acceptance. Generic staging spans may
  include non-EFB copies/waits; logging perturbation and CPU-wall-vs-GPU limits
  explicit in EFB-READBACK.md. Next isolated candidate link and trace. R594
  remains stopped; full performance/PRD goal still active.

## 2026-09-08 — R595 EFB flush ownership regression

- Previous turn excluded context diagnostics as sufficient cure. Inspected
  actual Simulator compile_commands source graph plus FramebufferManager,
  MTLTexture and MTLStateTracker source; reread R403/R405 prior submission trials.
- Hypothesis: avoidable duplicate depth wait could explain sample waits.
  Refuted at caller level: synchronous population clears needs_flush, async
  first consumer clears it, later cached reads do not flush; completed Metal
  command buffers skip wait. No semantics-changing shortcut implemented.
- Added tests/test-efb-peek-flush.py extracting actual PeekEFBDepth, compiled with
  ASan/UBSan and bounded texture/cache doubles. Tests miss/cached/async-once,
  returned value and lower-left forwarding; run session34205 exited0. Added to
  check-repository.sh. Full suite not rerun. git diff --check passed.
  Scope excludes actual PopulateEFBCache/GPU execution; no fake depth in product.
- No product source/build/runtime/profile changes. Next targeted evidence:
  split actual Simulator staging setup and completion service to avoid conflating
  graphics CPU cost with GPU wait. Prior submission/default candidates stay parked.

## 2026-09-08 — R594 context-reader exclusion

- Previous turn measured observer-off. Verified its clean exit before terminating
  shell; sole iPad Sim. Rehashed unchanged app ff855d69…cb34e4/module c1c06e0a…d521421.
  Changed only DevPointerContext NO; same launch arguments otherwise as R593.
  CoreHost gate also prevents experimental mapper setup. Both native/legacy
  observers remain off. Runtime72759/session21376, ios-runtime-r594.log.
- Title visually checked; diagnostic A+B one-second lease then no input. CUA
  confirmed same file list. Four later windows28.49/37.44/34.94/34.87FPS,
  counts/times in PERF.md. Brief59FPS display does not override low averages.
  Captured ui-r594-context-off-native.png. No profiling/build during measurement.
- Result: no consistent large gain; separate-run host/animation variability
  precludes exact cost claim. Context/native diagnostics not a sufficient cause
  of the sustained deficit. Stop peripheral flag trials; next native execution/
  readback work must use exact-site correctness evidence and bounded timing.
- Native menu Stop confirmation used; clean exit11:39:11.386 failed=0. No file selection or save proof, no new
  source/build or speed acceptance. Full original goal remains unmet and active.

## 2026-09-08 — R593 observer-off comparison

- Previous turn attributed exact native PCs. Verified R591 stopped log before
  terminating idle shell; sole iPad Sim. Rechecked app ff855d69…cb34e4 and module
  c1c06e0a…d521421. No rebuild. Changed only DevNativePointerObserver NO versus
  R591; CoreHost attachment branch skipped, no attachment log. Legacy hooks NO,
  pitch22/context/calibrated YES retained. Runtime72333/session21546.
- Title visually reached; diagnostic A+B then no input. Stable file-list nerve
  0114 at11:30:21.012. Three clean windows37.245642/33.974711/35.310680FPS;
  omit earlier transitional sample. No profiler or build during measurement.
  CUA screenshot verifies same stationary file list; native capture retained.
- Interpretation: slowdown remains detached; results overlap prior attached
  range, so no precise overhead/gain claim. Null-check remains in same module
  and context reader remains active; not a wholly diagnostic-free baseline.
  Next: disable only context flag, which also prevents dependent mapper setup.
- Native menu Stop confirmation clean exit11:33:15.626 failed=0. No selection,
  save write proof or gameplay acceptance. Evidence generated/ios-runtime-r593.log,
  ui-r593-observer-off-native.png; PERF.md records counts/durations and caveats.

## 2026-09-08 — R592 exact sample-site audit

- Previous turn supplied live controls/performance evidence. Read current status,
  R591 CPU sample, exact R562 module and prior R427/R470 rejected hypotheses.
- Verified dylib SHA c1c06e0a…d521421 and UUID72DA0235-496F-375D-A9BA-B76DD1D28111
  against sample Binary Images. nm + bounded llvm-objdump resolved eight explicit
  native offsets across four chunks; six generated/r592-*.asm files retained.
  PERF.md records exact sites and corrected distinction between the sampled
  lazy-FP load and adjacent classification code. Generated chunk names are not
  individual Galaxy function identities.
- Result: mixed checked mapping, conversion, exception checks and dispatch;
  aggregated sample ellipses prevent weighting individual PCs. No sufficient
  basis for another broad FP/memory rewrite, especially given prior rejected
  candidates. No product source/build/runtime/settings/game-data change.
- Next falsifiable step: unchanged binary, disable native observer attachment
  only, matched file-select windows; diagnostic overhead not yet ruled out by
  R591's visibility experiment. Preserve all other experimental/context settings.
  Goal remains active, no performance acceptance or invented exact guest mapping.

## 2026-09-08 — R591 live visibility and bounded performance comparison

- Previous turn verified installed shell persistence. Rechecked no game process,
  sole iPad Sim and ff855d69…cb34e4 app. Launched same R562 observer and experimental
  pointer flags, runtime71366/session32084. No build or profile change.
- Title visually reached; diagnostic A+B then stationary file list. Kept pointer
  and buttons neutral through visible→hidden→visible comparison. Existing frame
  windows exclude native menu pause; details/counts/timestamps in PERF.md.
  Visible31.0/36.1FPS,hidden33.5/31.9,restored34.8: hiding controls is not a cure.
  This is not deterministic replay, nor removal of all host polling/observer work.
- Real Controls toggle hid all views while Menu stayed accessible. CUA verified
  unobstructed file select, native ui-r591-hidden-native.png captured. Restored
  views, then after comparison and sampling actual Touch drag345,430→375,465
  plus UI A selected existing file1, visibly checked. No new save/start action.
- Separate sample71366 1 5 -file generated/ios-r591-file-select.sample.txt
  completed0 via82716.142samples/thread,CPU120 Run+1072/110nested dispatch incl19
  future waits;video19depth flush waits. Counts overlap, no percentage speedup
  inference. Sampling transient excluded from comparison. Fresh native hotspots
  can now be mapped to exact generated functions before optimizing.
- Native Stop confirmation clean exit11:23:54.574 failed=0. Touch left on,
  auxiliary off, sole Simulator shell. Evidence ios-runtime-r591.log and PERF.md.
  Full performance/G6/mobile/gameplay/device/PRD goal remains active and unmet.

## 2026-09-08 — R590 installed manual visibility persistence

- Previous turn implemented/tested/built manual touch switch. Rechecked app
  ff855d69067bc1cac94d53ce2770f807d760ee3510f18ce0aea0a243b9cb34e4,
  no GalaxyPad process and sole booted iPad. No rebuild needed.
- Launched org.galaxypad.GalaxyPad without developer arguments; shell explicitly
  reported game-data setup pending. This intentionally tests shell/menu and
  persisted preferences without running or writing game data. Logs
  generated/ios-shell-r590.log and ios-shell-r590-relaunch.log.
- Actual CUA Menu→Controls showed Touch Controls selected. Toggled off:
  sticks/buttons disappeared and only menu/status remained, visually and in AX.
  Saved ui-r590-hidden-native.png. Terminated idle shell and relaunched;
  off persisted, menu remained reachable, action unselected. Enabled through
  menu; primary controls restored visibly and in AX; saved restored-native.png.
  Preferences left touch-on, auxiliary-off; idle shell terminated after test.
- No game boot, A/B gameplay, pointer targeting, speed, or save acceptance from
  this run. R589 isolated UIKit tests cover input release/reacquisition. Live
  game verification remains next, then performance work; full goal stays active.
  git diff --check passes. No source changes this turn.

## 2026-09-08 — R589 manual touch visibility

- Previous turn provided installed menu/input evidence and clean shutdown.
  Rechecked R588 failed=0 and sole iPad Simulator before terminating its shell.
- Implemented PRD Controls→Touch Controls on/off using existing visibility path.
  Default remains on, persistent disabled preference separate from controller
  auto-hide. Always clear touch input when toggling. Off hides buttons/sticks and
  rejects new pointer contacts but leaves three-dot menu available. Editing
  temporarily exposes controls; exit/refresh restores manual preference. No
  geometry, saved layout, controller mapping, or guest/cutscene inference changed.
- bash tests/build-mobile-ui.sh; install and launch isolated overlay-tests on
  DE8E956F-6B29-4FF3-AF4A-77034CE8588A. PID70873/session26635 exited0.
  Tests cover held A+pointer clear, hidden buttons, accessible menu, rejected
  new pointer, controller refresh, edit entry/exit, settings persistence, enable
  and pointer reacquisition, plus prior key/layout regressions.
  Evidence generated/ui-regression-r589.log. Product preferences not changed by
  tests. bash scripts/build-ios-simulator-app.sh session48733 exited0; evidence
  generated/build-app-r589.log. App SHA256
  ff855d69067bc1cac94d53ce2770f807d760ee3510f18ce0aea0a243b9cb34e4.
- Installed app successfully (session40239 exited0), not launched. git diff --check passes. Product interaction/visual proof remains
  next; no full gameplay, physical controller/device, or speed acceptance.

## 2026-09-08 — R588 installed auxiliary menu regression

- Previous turn made source/test/build progress. Rechecked installed-build SHA
  d49995b631e7b5f315c25233161dfba51b4bed9e0256e45ea63d5d39ffd9aea0,
  no GalaxyPad process, sole booted DE8E956F-6B29-4FF3-AF4A-77034CE8588A.
  SunPad remained clean at fcdc1411e483a86ca80ec82e7cd53839c51ff865.
- Launched same R562 native observer and pitch22/context/calibrated flags;
  runtime70273/session7663, generated/ios-runtime-r588.log. Slow startup remained
  live (~100%CPU) and completed without restart. No source/build changes.
- Real native Menu→Controls→Show Extra Wii Buttons exposed1/2/Minus; reopening
  showed selected state. Toggled off again. Native UI cleared input and resumed
  game both times. Viewed generated/ui-r588-extra.png and ui-r588-primary.png
  from native captures: optional keys present/absent, main layout unchanged.
- Diagnostic Controller A+B one-second lease reached file list. Actual UIKit
  drag345,430→375,465 then UI A selected existing file1; CUA visually confirmed
  detail entrance. No Copy/Erase/new-file/start-story action or save proof.
- Native Menu→Stop Game confirmed; runtime exit11:10:27.891 failed=0. One Sim
  remains, game stopped. Extra-button preference left off. Screenshots27/40FPS,
  file-list32.7FPS: no speed acceptance or attributed toggle performance effect.
- Next: implement PRD manual Touch Controls on/off using existing SunPad-derived
  visibility/reset path, keeping three-dot menu reachable. This is distinct from
  physical-controller auto-hide and avoids unverified automatic story detection.
  Original G6/performance/full PRD scope remains unaccepted and active.

## 2026-09-08 — R587 optional auxiliary keys

- Previous turn was progress: real touch→file detail→Play→story and clean exit.
  Rechecked stopped R586 log and sole booted iPad before terminating its shell.
- Hypothesis: use the PRD's advanced layout for 1/2/Minus to reduce permanent
  obstruction without changing Galaxy's primary actions or SunPad layout editor.
  Added persistent Show Extra Wii Buttons action under Controls; these three
  buttons default hidden/non-interactive and remain visible during editing.
  Plus/D-pad and primary actions remain; no automatic context guesses, geometry
  resets, remapping, game-data edits, or reference changes.
- Clear touch input before toggling visibility so hidden held keys cannot stick.
  Kept controller mapping first in the menu to preserve existing delegate gating.
- bash tests/build-mobile-ui.sh; install/launch org.galaxypad.overlay-tests on
  DE8E956F-6B29-4FF3-AF4A-77034CE8588A. Runtime70038/session63970 exited0.
  Tests cover default hidden hit targets, explicit availability, Two mapping,
  held-input clear, editor exposure, ungated menu action, existing A+B/pointer/
  cancellation and saved-size behavior. Test preferences are isolated from app.
  Evidence generated/ui-regression-r587.log. git diff --check passes.
- bash scripts/build-ios-simulator-app.sh succeeded (session21192), evidence
  generated/build-app-r587.log. Installed SHA256
  d49995b631e7b5f315c25233161dfba51b4bed9e0256e45ea63d5d39ffd9aea0
  (session24706 exited0), not launched. Product visual/gameplay acceptance still needed.
  Primary controls can still obscure dialogue; no performance improvement claim.
  G6/performance and full original PRD remain open, with mobile work continuing
  under the user's explicit platform/UI instruction.

## 2026-09-08 — R586 calibrated file-detail route

- Extended simulator-only experimental readiness from stable FileSelect0114 to
  also stable FileConfirm0128, based on pinned USA symbols and prior visible
  detail-state observations. Pending transitions remain denied. Added acceptance
  and pending/back-transition rejection tests; pointer-camera ASan/UBSan passes.
- Release build/install pass; app SHA256
  813fef1b6a0fe0db3729bb27bf03a7ada1000b9843177eefa32400d9af8ce95b.
  Runtime68993/session42562 retained the R562 native observer module and opt-in
  pitch22/context/calibrated flags. One Simulator/runtime only.
- Diagnostic Controller A+B used at title only. Actual UIKit drag345,430→375,465
  hovered existing file1; explicit A opened detail. Touch drag600,575→635,585
  highlighted Play This File; explicit A started opening story. No automatic
  action, Copy/Icon/Erase use, or deliberate new save. Save bytes not compared.
- Detail nerve0128 at10:55:17.159; pending012c at10:56:49.312; active012c
  at10:56:49.552; unknown context then story mode12. Experimental file-only
  mapping does not authorize story mapping or auto-confirmation.
- Viewed generated/ui-r586-play.png and ui-r586-started.png; latter proves
  opening story but also shows controls obstructing text. File detail24–28FPS,
  story screenshot48.2FPS and later30-second windows45–47FPS are not acceptance.
- Native Menu → Stop Game confirmation cleanly exited11:01:28.092 failed=0.
  Evidence generated/ios-runtime-r586.log, build-app-r586.log and
  pointer-readiness-r586.log. Full original goal remains active; this is bounded
  touch-route evidence, not finished UI, performance, gameplay, or device proof.

## 2026-09-08 — R585 actual touch-channel validation

- Previous turn progress: consumption-time mapper built/tests passed. Verified
  R581 stopped runtime, one Simulator, and R584 binarySHA2a2314c0…68b94 before
  terminating shell/installing. Same module; no rebuild or source change.
- Runtime68048/session47340 enabled22/context/calibrated-touch flags. Title
  visually ready before diagnostic Controller A+B. CUA then performed actual
  UIKit Touch drag345,430→375,465, giving file1 hit/visible hover with processed
  (321.355,298.066). This exercises consumer, not Controller bypass.
- Observed retained aim after release; checked source: intentional Classic
  touchesEnded retention, unlike touchesCancelled/reset. Kept correct behavior.
  Explicit UI A selected detail, B returned, both visibly verified.
- Native menu open/dismiss logged input cleared; no pointer/hover visible after
  return. Fresh Touch drag565,425→585,465 hovered file2, selector81192d08. No A on
  empty file, no automatic select/shoot, no save creation. Approx25–38FPS persists.
- Evidence generated/ios-runtime-r585.log,ui-r585-touch.png,
  ui-r585-cleared.png,ui-r585-retouch.png. Clean menu stop10:49:57.000 failed=0.
  Sole Simulator shell, no game runtime active. No full-context/mobile/perf claim.
- Next expand mapped-menu context coverage and action readiness without removing
  Classic aim retention or button ownership; full PRD remains active. Real touch
  coordinates from CUA screenshot are not subpixel accuracy measurements.

## 2026-09-08 — R584 consumption-time touch mapping

- Previous turn progress: runtime readiness bridge compiled. Rechecked source,
  mixer/lifecycle paths and stopped R581 run before implementing consumer.
- Added optional pointer-only mapping callback at InputMixer consumption. It
  preserves buttons/axes/controller routing, cannot resurrect released touch,
  re-evaluates held input and rejects nonfinite/out-of-range output.
- Simulator opt-in calibrated-touch flag requires22/context observer. Weak
  Session reference avoids ownership cycle; independent-lock snapshot copied,
  monotonic250ms freshness required. Spin/tilt from either source including
  latches invalidates neutral model for session; no guessed restoration delay.
- Input regressions pass mapping, held expiry, buttons/axes, controller fallback,
  hidden Spin and clear. Camera/readiness regressions pass. Build79599 SHA
  2a2314c091ba07845a63abf1de908eace83f9bf9b33ecfe736e2b03aa7e68b94 passes.
  Evidence generated/mobile-input-r584.log,pointer-readiness-r584.log,
  build-app-r584.log. No install/launch; R581 remains stopped.
- Identified validation boundary: main.mm diagnostic JSON uses Controller, not
  Touch, so it bypasses mapper. Next real touch-channel live test and lifecycle
  checks, not another falsely equivalent controller-input run. Full PRD and
  performance remain open; no default promotion or automatic action injection.

## 2026-09-08 — R583 runtime configuration bridge

- Prior turn progress: shared model with live fixtures. Inspected authoritative
  input publication, reset, VI observer and stopped R581 runtime evidence.
- Added MenuPointerReadiness for the explicit22-profile/menu waiting state,
  expected calibration/filter and neutral reference/acceleration horizons.
  It returns model configuration only, never action permission.
- Exact-DOL-gated CPU observer publishes optional configuration/time before its
  logging early return, protected by separate Session mutex. Input reset clears
  it. No consumer or control mapping changed; no guest memory writes.
- Sanitizer cases reject absent/profile/context/transition/calibration/orientation
  mismatches and accept recovery; camera grids/four live fixtures still pass.
  Final app build76830 SHA
  059d23c6f67f3c7d9574654705f487f63dc1fecc9a2be06c44ac237d7fc4701b passed.
  Evidence generated/pointer-readiness-r583.log,build-app-r583.log.
- No installation/runtime launch. R581 installed/stopped, sole Simulator shell.
  Next source/lifecycle/freshness-gated touch consumer and live verification.
  Full PRD/performance/device/story/action-context gates remain open.

## 2026-09-08 — R582 reusable forward model

- Previous turn progress: live edge coverage. Revalidated current status/source
  and R581 clean shutdown; no runtime or Simulator change this turn.
- Extracted neutral menu forward path into GalaxyPadPointerModel.h with explicit
  configuration, default-false neutral verification, actual-camera callback,
  finite/domain checks and missing/degenerate sensor rejection. No input wiring.
- Refactored camera/inverse test to consume shared model. Preserves420/441 at20
  and441/441 at22/24; four live fixtures agree within.002 logical pixels. Added
  unknown-orientation/no-camera-call, invalid scale/pitch, hidden and identical
  LED tests. ASan/UBSan passes; git diff --check passes.
- Evidence generated/pointer-model-r582.log. No app build/install/launch; R581
  remains installed/stopped. No UI/action change or performance claim.
- Next verified runtime configuration/context bridge, then touch pointer routing
  while preserving Classic Pointer and explicit A/B. Full original PRD remains
  open; model fixtures do not establish live touch, motion or context readiness.

## 2026-09-08 — R581 live edge coverage

- Previous turn progress: one inverse target verified live. Rechecked current
  source/status and R580 clean exit; one Simulator and stopped shell only.
- Added32-event changed-position/validity diagnostic independent of target hits.
  Missing/unchanged samples and hard-cap cases pass actual-hook sanitizer test.
  Inverse test now outputs bottom/left/right host candidates; coverage still
  passes. Build24370 passed; installed app SHA
  418d3477202e96ef1f229f86cb150ea1d11e1135c2c2ef675b8e217c6c11710c.
- Same observer module and22-degree candidate, runtime66373/session31652. Fresh
  title screenshot before A+B; then bottom15s,left12s,right12s pointer-only
  nonoverlapping leases. Captures taken after5s within each lease.
- Bottom target(.5,.999) yields(416,455.818), left(.02,.5) yields(17.164,228.026),
  right(.98,.5) yields(814.836,228.026); both validity flags1, no-target result0.
  Max component error.524 logical pixels. Screenshots show clipped bottom tip
  and side pointers. No select/shoot/new save. FPS23.6/30.0/32.7, not acceptance.
- Evidence generated/pointer-hook-r581.log,pointer-inverse-r581.log,
  build-app-r581.log,ios-runtime-r581.log,ui-r581-bottom/left/right.png.
  Clean menu stop10:28:09.851 failed=0; sole Simulator shell, no game active.
- Next configuration-aware touch mapping integration; retain classic controls,
  cancellation and A/B ownership. Full PRD, context actions, physical-device,
  story and performance gates remain open.

## 2026-09-08 — R580 live inverse-coordinate candidate

- Previous turn progress: bounded inverse/coverage test. Revalidated status,
  clean R578 exit and single Simulator before build/relaunch.
- Added Simulator-only GalaxyPadDevPointerPitch22 profile experiment, leaving
  normal/device template unchanged. App build90940 passed and installed SHA
  c90ab8764d798e99834d33113c400932e8e1fb6cb4925fc1e3cc350e76181e5c.
  Terminated prior stopped shell before installation; same observer module.
- Runtime65791/session57265 launched with22 flag; generated configuration and
  launch log confirm22. Reinstall moved data container; resolved fresh path
  through simctl instead of treating old path failure as missing data.
- Title visually verified before A+B. Pointer-only inverse input
  (.411593,.685748) for intended(.38,.64) produced file1 hit and valid past/current
  (315.896,291.972). Error(-.264,+.132) logical pixels against832x456 expectation.
  First screenshot missed lease; corrected by a fresh nonoverlapping15s lease
  with capture immediately in the same tool sequence. ui-r580-hover.png verified.
- Evidence generated/build-app-r580.log, ios-runtime-r580.log,
  ui-r580-hover.png; displayed27.3FPS, no performance claim. Input regression
  generated/mobile-input-r580.log passes. No automatic A/save creation.
- Clean native-menu stop10:21:15.821 failed=0. No game active, sole Simulator
  shell. Next bottom/edge live checks then configuration-aware mapping; one
  settled target does not prove Direct Touch, full range or original PRD.

## 2026-09-08 — R579 bounded inverse and coverage test

- Prior turn progress: reconciled live orientation and WPAD Y convention.
  Rechecked current STATUS and R578 clean exit before implementation. No runtime
  started; same sole Simulator shell and installed R578 app remain unchanged.
- Implemented shared normalized inverse with caller-supplied forward model,
  finite/domain/singular checks,10 Newton iterations and fixed25-point camera-
  quantization neighborhood. Maximum75 forward evaluations; returns only a
  measured residual within0.0015, never guessed/clamped success.
- Actual-camera test uses verified R578 neutral horizons/offset/scale and WPAD
  convention. Initial tight tolerance rejected57/441; diagnostics distinguished
  pixel quantization from genuinely unreachable bottom targets. Chosen0.0015
  reflects ~0.00222 camera-coordinate steps, not a full-screen acceptance bypass.
- After fixed neighborhood: pitch20 solves420/441, bottom row rejects with
  residual0.03724–0.04250.22 and24 degrees solve441/441 at unchanged threshold.
  Added exact counts/residual assertions, key targets, malformed/missing/singular
  model cases and evaluation bound. ASan/UBSan and retained R578 fixture pass.
- Evidence generated/pointer-inverse-r579.log and tight initial trial log.
  No product configuration or touch wiring changed, no build/install/launch.
- Next live-test22-degree configuration and inverse coordinates. Still need
  processed-position/target readiness, context-safe direct actions and all full
  PRD/performance/story/physical-device gates. Goal remains active.

## 2026-09-08 — R578 live orientation and WPAD sensor convention

- Previous turn was progress: live filter mode identified. Rechecked stopped
  R577 runtime, current status/source and sole Simulator before continuing.
- Hypothesis: active orientation/regular points should explain the observed
  filtered pointer through the audited conversion formula. Added CPU-thread
  read-only snapshot and finite/missing/retention tests, raw field-load checks.
  Context/hook tests and build93086 pass. Installed app SHA
  2e209383c58d1aa94b2ed1c6d2e65703a62ae3d85249c007c82770e8f1e9133d.
- Replaced stopped app shell only; same observer module, single SimulatorDE8.
  Runtime64595/session98723: an early A+B before title was ineffective; fresh
  title screenshot justified another pulse, which reached file select.
  Pointer-only (.38,.64)20s yielded file1 hover with detailed conversion input.
  Screenshot ui-r578.png captured during lease, visibly hovered,26FPS.
- Six-decimal live conversion inputs reproduce filtered position within1.1e-6.
  Comparing earlier projected sensor coordinates exposed omitted parser Y flip.
  Inspected reference WPADHIDParser and exact USA __parse_dpd_data: subfic767
  at804DF030/804DF1B4/804DF1F4. Added raw checks and camera fixture; both projected
  LEDs, after parser inversion/normalization, match live points within1e-6.
  No runtime axis change, no filter bypass, no automatic tap/save creation.
- Evidence generated/build-app-r578.log, pointer-hook-r578.log,
  pointer-contract-r578.log, pointer-camera-r578.log, ios-runtime-r578.log,
  ui-r578.png. Clean menu stop10:08:42.213 failed=0; no game runtime left active.
- Next implement/test bounded inverse mapping against reconciled forward chain.
  Tracking, touch context readiness, performance, full story/device gates remain
  open; no claim that a matching fixture closes the original PRD.

## 2026-09-08 — R577 position filter and active mode

- Hypothesis: retail position filtering can explain residual pointer displacement;
  identify both finite branch behavior and actual mode before inverse mapping.
- Added tests/probe-kpad-filter.py using the unchanged generated filter block,
  bounded memory and explicit finite FP/sqrt doubles. 84 radius/distance/mode/
  sensitivity cases pass sanitizers. Conversion's 1000 cases still pass after
  main-guard refactor. This is not tracking/FPSCR/full-runtime equivalence.
- Added read-only filterMode to calibration; exact raw instruction load audited.
  Reader and actual CPUState hook tests pass missing/invalid mode and entry-time
  snapshot retention. No input mapping or guest state mutation.
- App build38512 passed; installed SHA
  8aef69364b8f809e635d42b3bf21db6983c67424360e432213b05cf2df9f2745.
  Previous stopped shell terminated before installation. Single Simulator stayed
  DE8E956F-6B29-4FF3-AF4A-77034CE8588A; same observer module, no module rebuild.
- Runtime63970/session30170 attached native observer. A+B reached file select;
  pointer-only (.38,.64)15s produced file1 actor81144cc8 hit and visible hover.
  Title and file1 calibration mode0, radius0.03/sensitivity0.5, center(0,-0.2),
  scale2.272727. Verified ui-r577.png reads34.5FPS; not performance acceptance.
- Evidence: generated/build-app-r577.log, kpad-filter-r577.log,
  pointer-hook-r577.log, pointer-contract-r577.log, ios-runtime-r577.log,
  ui-r577.png. Clean menu stop10:02:16.246 failed=0; no new save/game active.
- Next active orientation evidence then bounded inverse mapping; Direct Touch,
  performance, physical-device validation and full original PRD remain open.

## 2026-09-08 — R576 emitted KPAD conversion check

- Prior turn progress: live calibration/logical width. Current status/source and
  clean R575 exit revalidated; sole Simulator unchanged, no game launched.
- Added tests/probe-kpad-conversion.py: actual accepted generated block
  8044FA6C–8044FB2C, bounded memory, finite arithmetic doubles, independent
  midpoint/rotation/scale oracle.1000 varied orientation/center/scale cases pass
  ASan/UBSan within2e-6 and expected final PC. Source hash and result recorded
  in generated/kpad-conversion-r576.log.
- This is finite coordinate-body evidence only: no object tracking/filter/FPSCR
  or full CPU equivalence. No module regeneration, product input/settings change
  or calibrated touch/performance claim. Next filter plus active orientation
  inputs, then bounded inverse validation. Full original goal remains active.

## 2026-09-08 — R575 active calibration capture

- Prior turn progress: calibration reader/tests/build. Verified prior clean exit,
  sole Simulator and app SHA; installed R574 host and ran existing observer module,
  PID62707/session91906, ios-runtime-r575.log. A+B reached file select.
- Active calibration center(0,-0.2), scale2.272727, radius0.03, sensitivity0.5 is
  consistent across title and two pointer-only targets. File2 processed position
  (539.691,260.502) and actor81192d08 match previous run; screenshot
  generated/ui-r575-right.png verified. No mod-fallback/pairing-invalid logs.
- Exact getScreenWidth audit now confirms608/832 logical units by aspect predicate,
  not640 EFB assumption; pointer-contract-r575.json passes. Located retail KPAD
  conversion/filter instructions for next execution proof; no SDK equivalence or
  inverse calibration claimed. Reference filter has unresolved comment.
- Native Stop09:48:20.013 failed=0. No file selection/new saves, no gameplay/FPS
  acceptance. Same sole Simulator shell retained, no active game. Full goal open.

## 2026-09-08 — R574 retail KPAD calibration observation

- Prior turn progress: executable camera-stage probe. Read current status and
  retail/Petari KPAD paths. Traced real WPadPointer reset call to8044E33C, decoded
  channel stride/base/parameter stores; sensor-height setter identifies center and
  scale fields. Added exact instruction checks, pointer-contract-r574.json.
- Added optional finite/positive-scale calibration reader and query-entry snapshot
  logging. Tests cover valid decoding, missing/zero/negative/nonfinite scale;
  context and native-probe sanitizer tests pass. App76531 Release build exit0,
  generated/build-app-r574.log, SHA
  f6a3caacdc1719b6d735d11958c9b4dc2dba754f862a0bdc211b88ee965a8d33.
- No install/runtime/module change this turn. Existing app shell stopped. Next
  active calibration capture, then exact conversion/filter correspondence before
  inverse mapping. No complete SDK equivalence, automatic taps or FPS improvement.

## 2026-09-08 — R573 camera-stage executable probe

- Prior turn progress: live processed positions. Revalidated R572 clean exit and
  sole Simulator. Inspected actual camera perspective/LED projection and neutral
  remote transform; no product settings/source changes or runtime launched.
- Added actual CameraLogic body/point constants plus real Matrix.cpp probe with
  explicit settled yaw25/pitch20/top-offset10cm, neutral other motion assumptions.
  Center, offset, hidden and two-target assertions under ASan/UBSan pass43014.
  Initial center384 test failed because actual truncated float result383; captured
  exact outputs and bounded expectation to383/384 with both LEDs agreeing.
- generated/pointer-camera-r573.log reports sensor pairs for the R572 coordinates;
  details in DIRECT-TOUCH-CONTRACT.md. Added focused test to check-repository.sh,
  full suite not rerun. KPAD unmodeled, no touch calibration or performance claim.
- Next exact retail KPAD LED-to-position conversion/parameters. Keep automatic
  taps disabled and preserve full original PRD scope; no game active.

## 2026-09-08 — R572 live processed-pointer correlation

- Prior turn progress: new reader built/tests passed. Revalidated prior clean exit,
  sole Simulator and app SHA c0c1236f312cbfbad789dfcd63f1f712cfb9c0ec7d25829f396c5a1c50c7c160;
  installed and ran PID61434/session2095 with same R562 module. Attach successful.
- Title invalid sample then A+B/file select. Pointer-only .38,.64 for0.5s followed
  by .61,.64 for20s changes result actor81144cc8 to81192d08 and past positions
  (281.229,260.458) to(539.691,260.502). Screenshot generated/ui-r572-right.png
  visibly highlights2. At09:33:35.335 hit retains pastValid1/currentValid0.
- Coordinates cannot be treated as direct host screen scaling. Inspected actual
  Cursor defaults and EmulatePoint yaw/pitch/offset/2m/acceleration path. Next
  trace Camera/KPAD projection before choosing mapping or tolerance. Two points
  and bounded hit logs are not input revision identity or latency measurement.
- Native clean Stop09:35:29.363 failed=0. No file selected/new save created, no
  automatic tap or performance claim. Game stopped, one Simulator shell retained.

## 2026-09-08 — R571 query-entry processed position reader

- Prior turn progress: exact pointer-buffer contract. Current stopped shell and
  sole Simulator unchanged; no runtime launched. Extended exact-DOL audit for
  director chain, controller array and P1 channel, pointer-contract-r571.json.
- Added ReadProcessedPointer with mapped/aligned word, channel0, finite XY and
  boolean-validity rejection. Captures both past and current guest coordinates,
  no assumed host scale/revision. Native observer frames snapshot at query entry
  and log alongside bounded return results; guest state remains readonly.
- Context sanitizer33513 and native probe tests pass; extended actual CPUState RAM
  test verifies endian decoding and old snapshot retained after memory changes.
  Release app65401 exit0, generated/build-app-r571.log. No module rebuild needed.
- Next install new host with existing diagnostic module and observe coordinate
  changes across separated targets. Latest host revision and DirectTouch readiness
  remain unproven; no input injection/performance claim/full goal completion.

## 2026-09-08 — R570 exact retail pointer freshness boundary

- Prior turn progress: native observer live proof. Revalidated clean runtime exit
  and sole Simulator; no restart or expensive build. Traced Petari pointer query,
  then disassembled exact hashed USA DOL using installed Capstone5.0.7.
- Query target predicate receives past XY/controller+4, not current+0x18; retail
  checks past validity+0xc directly, unlike reference inline out-screen helper.
  Audited movement order and current-to-past copy followed by current refresh;
  exact two-float copy body and independent screen-position getter also checked.
- scripts/audit-menu-pointer-contract.py expanded and passes; saved output
  generated/pointer-contract-r570.json. No claim of complete function equivalence,
  host revision consumption, latency magnitude, or completed Direct Touch.
- Next observer correlation must use past XY/validity, tracing KPAD sampling and
  preserving scene/gesture epoch rejection. No game active; original goal open.

## 2026-09-08 — R569 native observer live proof

- Prior turn progress: installed compact controls. Revalidated process59521 and
  sole Simulator. Baseline pointer+preaim0.5s+A selects existing Mario file;
  clean stop09:17:18.196 failed=0. Same app, candidate module and diagnostic flag
  only changed for new PID59957/session29427. No saves intentionally written.
- Binding/probe sanitizer tests45265 pass; full candidate manifest verifies.
  Native observer attaches09:18:21.149. Misses at title, file1 actor81144cc8/result1
  09:19:42.117 matching visible hover. A select and B back remain functional.
  Verified saved hover generated/ui-r569-hover.png; earlier hit.png had expired
  lease and was rejected as hit evidence. No mod fallback/pairing-invalid logs.
- Performance still poor; no matched timing claim. Process snapshot showed
  Simulator chronod85% and ManagedSettingsAgent65% alongside game152%; pmset
  reports no recorded thermal/performance warnings. This is confounding load,
  not proof the machine explains the defect. No unrelated processes killed.
- Clean native Stop completed09:22:46.427 failed=0. Source resets observer binding
  after runtime destruction and before exit delivery; no live detach counter.
  App shell remains, no active game, sole Simulator retained for continuation.
- Next establish actual guest processed-pointer
  freshness before queued tap integration. Full goal remains active.

## 2026-09-08 — R568 touch audit and completed candidate build audit

- Reoriented to user's SunPad control feedback. Live capture shows letter faces
  but oversized stacked pause pills; reference screenshot/source inspected and
  original Nintendo Wii manual researched. Compact auxiliary geometry only,
  no input rewiring or saved-layout overwrite. TOUCH-UI-R568.md records evidence.
- UIKit test96374 exit0; app build98288 exit0. R559 clean native Stop exit failed=0
  09:11:41.692 before testing/installing. R568 PID59521/session66921 launching
  accepted module with native observer NO; one DE8E956F Simulator only. Startup
  observed then title and A+B transition to file select visibly verified. Accepted
  final screenshot generated/ui-r568-file.png; transition-flash after.png rejected
  as final audit image. Instantaneous27.3FPS and prior30.5s35.705FPS window do not
  meet performance goal. Current game remains on file select; no save writes made
  by test actions. Next pointer and actual gameplay checks, not another UI rewrite.
- Prior module link did finish but shell reported EOF afterward. Current script
  syntax passes; rerun73281 exit0, manifest verifies, Ninja no work, platform
  IOSSIMULATOR min16/sdk26.5, native getter and module getter exported. SHA
  c1c06e0a03ab0822120f855291abf195139299b924e9363eb71408a15d521421.
  No candidate observer runtime or performance claim; separate pending step.

## 2026-09-08 — R567 verified original-linker wait

- Previous turn progress: caller boundary regression and app build. Current turn
  verified wait: original48689 repeatedly reports running, linker58198 advances
  3:04→4:49 elapsed,32:33 accumulated CPU,688%CPU final sample. No restart or
  competing build; partial linker output is not accepted as a finished module.
- Sole booted DE8E956F Simulator remains paused. Observer C source hash
  251a847ec6bb63096b2c3ded80079c89e777a6116d46c51cb04f7a55cb772f76;
  interface hash10bb3242eaa4b1d4a8e39bcdd74e236e3e422d80ca3cb2bd6dae4c4634a29576.
  Next same-handle completion, platform/export/hash audit, host installation and
  live native observer validation. Full goal stays active, no performance claim.

## 2026-09-08 — R566 caller coverage and verified link wait

- Previous turn progress: ABI/source checks. Same48689 verified live,1082→1331
  compilation, then linker58198 under clang58197/ninja54354,42s elapsed680%CPU.
  No restart. Observer source compiled with expected new-file PGO-unprofiled
  warning; not suppressed. Final binary not ready for audit yet.
- Exact-DOL audit scans all7 text sections for direct relative BL to803FB0EC;
  only80178EC4 found. Source reference also has one direct caller. Neither proves
  absence of indirect calls. Native observer now filters entry LR80178EC8 so an
  unsupported caller cannot leave an unpaired frame. Generic hook path unchanged.
- Extracted filter into actual diagnostic header for testing; added unsupported
  LR/no frame and supported entry/return/one hit assertions. Sanitizer/app36983
  exit0, test-pointer-r566.log and build-app-r566.log. Prior audit/app60311 also
  exited0 before replacement build; no overlapping app builds.
- Continue same module48689 through link, audit exported extension/platform and
  identities, then install updated host. R559 stays paused in native menu, one
  Simulator. No native callback or performance acceptance until live run.

## 2026-09-08 — R565 candidate source and ABI verification

- Previous turn progress: host integration builds. Same build48689 revalidated
  live,814→959/1333 and continuing; no restart.1328-file candidate inventory/hash
  verification passes. Source PGO out-of-date/unprofiled and nested-comment warnings
  remain visible; no warning suppression or performance claim.
- Added test-observer-cpu-layout.py compiling existing offset probe against both
  real app moderngekko/cpu_state.h and module core/cpu.h. CPUState size3528;
  gpr0/128bytes, pc280/4, lr284/4 and all additional probed FP/memory/callback/
  downcount/reservation fields match. This verifies actual callback-read layout,
  not merely advertised ABI version. Test added to repository suite.
- No new app/module installation. R55953265 paused in native menu during build,
  one Simulator. Continue same48689 through link, then export/platform/source
  audit and host/native callback delivery with generic hooks disabled. Full goal
  and live lifecycle/performance/Direct Touch acceptance remain open.

## 2026-09-08 — R564 native observer host integration

- Previous turn progress: binding checks plus live build. Re-polled48689 live,
 481→702/1333. No replacement build. R559 remains paused during compilation.
- Added Simulator-only GalaxyPadDevNativePointerObserver. Exact DOL check and
  optional symbol/API validation before stopped-CPU attach. Uses existing bounded
  read-only pairing observer on CPU thread; callbacks now take const CPUState,
  generic ModManager adapter remains compatible. Selecting native path suppresses
  generic hooks, avoiding their range fallback and duplicate observation.
- Observer library declared before binding and Runtime creation. Runtime::Run
  explicitly stops/shuts down core; host also destroys Runtime before explicit
  binding reset, covering failure cleanup. Library remains held until detached.
  No API calls or callback lifetime claims depend on UIKit being the CPU thread.
- Pointer probe and binding ASan/UBSan tests pass; app build6167 exits0,
  generated/build-app-r563.log (filename retained). Candidate not installed yet;
  no real native callback delivery claim. Module build48689 still live702/1333.
  Next finish/audit same module, install host and prove no mod fallback plus live
  hit-test result/input preservation. Full original goal remains open.

## 2026-09-08 — R563 host registration ownership

- Previous turn progress: candidate preparation and build start. Re-polled exact
  build48689 live,182→350/1333 in build-native-observer-r562.log; no restart.
- Added GalaxyPadNativeObserverBinding.h: compatibility check before attach,
  exact ordered two-site list, explicit trusted-module boundary, noncopyable RAII
  registration. Invalid replacement first detaches previous observer; reset is
  idempotent; destructor detaches while caller still owns library. All operations
  require CPU stopped; no claim this helper synchronizes live callbacks.
- test-native-observer-binding.sh handle34593 passes ASan/UBSan for malformed
  metadata, duplicate sites, failed attach, replacement and cleanup. Added suite
  entry; full suite not run. Helper not connected to CoreHost yet.
- Next scoped library lifetime/attach before Run and detach after CPU stop, exact
  DOL/module validation, then final candidate audit and live callback comparison.
  R559 remains paused at native menu during compilation, no second Simulator.

## 2026-09-08 — R562 exact-site candidate build

- Previous turn progress: observer bridge. Revalidated R55953265; free43Gi.
  Located exact labels in accepted346MiB generated tree. Query chunk hash50133c19…
  22b7b6, return chunk c01249ed…ea937. Both already materialize PC. Outlined helper
  in return chunk covers80178190, not the selected return80178EC8.
- prepare-native-observer.py verifies accepted module/DOL/chunk hashes, refuses
  overwrite, copies to generated/native-observer-r562/RMGE01_generated. Inserts
  only declaration and one call per selected chunk. Reversing additions restores
  source; manifest audits1328 files, exactly2 changed. Preparation test passes;
  --verify checks full inventory/hashes and passed during build, read-only.
- Candidate-only CMake DEFER adds observer source with core/cpu.h and actual
  GXRUNTIME_CPU_ABI_VERSION; no upstream template or existing ABI/guard edits.
  Separate build script uses native simulator/O2/ThinLTO/pinnedPGO,2 jobs.
  Build48689 verified live, generated/build-native-observer-r562.log, last95/1333.
  Poll same handle through link; do not replace due to observation timeout.
- R559 paused via native menu08:46:25.017/input clear while compilation runs;
  sole Simulator visible, no game save changes. Accepted module remains unchanged.
  Next module identity/export audit and host attachment, then real callback proof;
  source-site coverage alone is not Direct Touch readiness or performance proof.

## 2026-09-08 — R561 optional module observer interface

- Prior turn progress: executable native-label prototype. Revalidated PID53265.
  Inspected module ABI/template and selected a separate optional export rather
  than misrepresenting general ModManager patches as native observers. Existing
  CPUState and module descriptor ABI remain unchanged.
- Added GalaxyPadNativeObserver.h/.c: exact-DOL/CPU ABI/size/site metadata,
  read-only callback with user state, version/size-gated attach, rejection clears
  old callback, null detach, unknown-site/null-state filtering. Registration only
  while CPU stopped, detach after stop before module unload. Own trusted callback
  must respect read-only contract; const is not a sandbox.
- test-native-observer.sh passes ASan/UBSan with actual CPUState header; added to
  suite. No module build/host attachment/guard changes. Metadata currently lists
  intended two sites, not proof of emitted coverage: build integration must audit
  those exact labels before exposing this export in a runnable candidate.
- Next candidate generated-site instrumentation using this separate callback,
  coverage/source identity checks and compiled execution tests, then module build.
  R559 remains sole Simulator with generic hooks disabled. Full goal stays open.

## 2026-09-08 — R560 executable native-site prototype

- Prior turn progress: diagnostic removed, native bypass routes identified.
  R559 PID53265 revalidated. Added isolated probe-native-observer.py using current
  actual C emitter/CFG/decoder and CPU runtime in a disposable temporary directory.
  Production files/module and guard untouched; no rebuild of installed app.
- Prototype inserts observer callback at selected instruction labels, temporarily
  materializes/restores PC for observer, retains original instruction execution
  and cycle accounting. Disables outlined counted-loop helpers in containing
  functions so internal sites cannot be silently skipped; ordinary loops remain.
- First handle3602 passes entries/fallthrough/back-edge/null callback/cycles.
  Added generated local-return fixture; handle76352 passes under ASan/UBSan,
  including return into selected label and subsequent guest instructions/charges.
- This is a prototype with two synthetic sites, not production hook support.
  Callback is observational; ignoring its return value must NOT be advertised
  as support for arbitrary ModManager patches. Need explicit native coverage,
  observer-only routing without duplicate dispatcher callbacks, generation/cache
  identity and actual query-site validation before changing fallback guard.
  Full Direct Touch/performance goal remains open; R559 stays at title.

## 2026-09-08 — R559 remove diagnostic fallback from active run

- Previous turn progress: live query result and discovered whole-chunk fallback.
  Native Stop R558 exits08:33:00.255 failed0. Terminated stopped shell then same
  binary relaunched with -GalaxyPadDevPointerHooks NO, same other dev arguments.
  PID53265/session4980, generated/ios-runtime-r559.log. No mod fallback/loading/
  pointer-hook messages; one GalaxyPad process. Fresh CUA title screenshot proves
  render resumed. Input neutral, no save change, sole Simulator remains visible.
- Inspected actual C emitter1910–1976 and dispatch.c405/410. Hook callback is in
  external dispatch while native label fallthrough/gotos/local return routing can
  bypass it. Counted-loop helpers are emitted separately and must also be covered
  or disallowed for selected sites. Chunk guard is required by current codegen;
  do not suppress it merely to recover speed.
- Next isolated native observer emission tests for entry, fallthrough, local
  branch/return and counted-loop interaction; only advertise native hook coverage
  after those paths are proven. No emitter/guard change or performance comparison
  this turn. Full Direct Touch and original PRD goal stay active.

## 2026-09-08 — R558 bounded hit-test observer live

- Prior turn progress: runtime built-in API. Revalidated R555 PID51592. Added
  Simulator-only GalaxyPadPointerHooks.h:16-frame pairing stack, bounded4 entry/
  4 miss/16 hit logs, fail-closed pairing/overflow, reset on load. No guest writes.
  Exact DOL size/hash checked before config receives descriptor via explicit
  GalaxyPadDevPointerHooks flag. Default/device path has no observer.
- Actual-header/ModManager ASan/UBSan test31972 passes state preservation,
  pairing/overflow/reset. App61201 exits0. Clean R555 Stop08:28:32.733 failed0;
  installed executable58d41d658e9d7de59358360cd9d6dcfcb07b4f9269c1ea07483d58338aaf52d2.
  R558 PID52918/session72476, generated/ios-runtime-r558.log, same module/noJIT.
- Startup reports mod fallback chunk[803FB0A0,803FC0A0). Source confirms whole
  chunk fallback for hook-containing ranges. Diagnostic only; no FPS promotion.
  Entry actor81144cc8/return80178ec8 and misses seen even at title; A+B reaches
  file select. Pointer-only(.38,.64)12s visibly highlights file1 and reports
  actor81144cc8/result1 at08:30:45.890. No pairing error observed. Writer5552 exits
  neutral. Exact-DOL audit now verifies BL80178EC4→803FB0EC and passes.
- Next eliminate whole-chunk fallback for product hook delivery; do not bypass
  its correctness guard. Disable diagnostic flag before future performance runs.
  Input-revision freshness and action integration remain open. One Simulator
  left at file select; no selection/save creation during hover test.

## 2026-09-08 — R557 app-linked runtime descriptors

- Previous turn progress: actual ModManager sanitizer tests. Revalidated R555
  PID51592 live. Added RuntimeConfig builtin_mods of const descriptors with explicit
  lifetime contract. Nonempty configuration uses AttachedDescriptor/Load; mixed
  directories reject, descriptor validation failures return ModuleRejected before
  UICommon/core startup. Default empty list retains previous LoadDirectories path.
- Canonical patches/ModernGekko/0028-builtin-mod-descriptors.patch SHA256
  d198a24f168fd5b589552bcc9474468c70225ec2ec83a93bfdabe58f5eab37c9.
  Initial hand-written patch had malformed/minimal context; corrected it and
  verified reverse applicability. Bootstrap includes hash check, peel/restore and
  scope list, applies last before disabling restore trap. Syntax passes; full
  bootstrap not run against active dirty graph.
- Static-hook ASan/UBSan tests pass. Core handle57945 exits0 (build-core-r557.log);
  provision/app59191 exits0 (provision-r557.log/build-app-r557.log). Rebuilt core and
  app together for changed C++ config layout. No app installation, no hook supplied,
  no dynamic module/JIT addition. Existing R555 remains sole running Simulator.
- Next bounded exact-DOL-gated hit-test entry/return observation using built-in
  descriptor. Config rejection integration and actual AOT hook delivery require
  runtime validation; unit manager coverage alone is not that proof. Goal open.

## 2026-09-08 — R556 statically attached pointer observers

- Prior turn progress: About live/input isolation proof. Revalidated PID51592.
  Traced MR file-select query to its core hit test and existing ModManager host-call
  integration. Static AttachedDescriptor is supported by manager, but RuntimeConfig
  only exposes mod_directories and Create uses LoadDirectories.
- Added test-static-pointer-hooks.cpp/.sh compiling actual mod_loader.cpp without
  dynamic loading; handle34424 exits0 with ASan/UBSan. Wrong game/CPU ABI reject;
  entry/return preserve CPUState despite deliberate observer mutations; matching
  LR/stack consumes return once; unload removes pending return. Added to suite.
- Observers cannot change returned r3 (manager restores it). Suitable for bounded
  readiness observation, not an unconditional input predicate replacement.
  Actual module internal-call delivery and processed-pointer revision unproven.
- Next explicit built-in descriptor RuntimeConfig plumbing through existing manager,
  then exact-DOL opt-in live hook test. No dynamic executable download or device
  JIT workaround. App/module unchanged, sole R555 Simulator remains at file select.

## 2026-09-08 — R555 About live validation

- Previous turn progress: About implementation/build. Native Stop R552 completes
 08:13:13.051 failed0 before app-shell termination. Installed isolated UIKit test
 host; handle67480 exits0, generated/ui-tests-r555.log PASS. Includes About
 read-only/Dynamic Type/private-boundary/Done assertions plus control regressions.
- Installed candidate executable0e1c5bc68e4961a91cad3872f0511dabceceebbe8d7cd5b2dcfbd0dd729c3c3a;
 PID51592/session67312, generated/ios-runtime-r555.log. Same module/dev arguments.
 One booted DE8E956F Simulator, no second runtime. Test host exited before launch.
- About at startup visibly fits iPad sheet with all text readable and Done.
 Native block08:14:22.508 clears08:14:42.533; boot then reaches title visibly.
 Open again during title: blocked08:15:37.054. Publish A+B1s while sheet open,
 writer exits neutral; Done then unblock08:16:08.936. Resumed animated title
 remains at title (no leaked A+B). Fresh A+B afterward reaches file select
 mode5/nerve806a0114 field5052, confirmed visually. Final input neutral.
- No physical multitouch, large accessibility text, iPhone, audio-route or full
 notice audit acceptance. Scroll not needed at this default iPad text size.
 No performance claim (file-select snapshot42.7FPS). Next Direct Touch input
 freshness/runtime integration; full original goal remains active.

## 2026-09-08 — R554 About and rights screen

- Previous turn progress: bounded performance attribution. Advanced another
  explicit PRD menu item instead of repeating unchanged samples. Read current
  rights status and SunPad notices; SunPad has no About method in its overlay,
  so retained its native menu/delegate presentation pattern, not a claimed clone.
- Added GalaxyPadAboutViewController and optional overlay delegate action. Native
  scrollable text, Dynamic Type, safe-area constraints, selectable/read-only copy,
  version/build, credits, private distribution boundary, explicit unfinished full
  notice audit. Host presentation uses existing menu blocking/input release gate.
- Release app build54947 exits0, generated/build-r554.log. Added UIKit host
  assertions for read-only/Dynamic Type/private wording/Done. Test execution and
  visual/lifecycle validation pending; no product install yet. R552 PID50620 remains
  live, one Simulator. No license-completeness or gameplay/performance acceptance.
- Next install candidate and inspect About, scroll/dismiss and resume; then return
  to Direct Touch freshness/runtime integration. Full original goal remains open.

## 2026-09-08 — R553 bounded fallback-cost follow-up

- Prior turn progress: live transition evidence. Revalidated R552 PID50620 live.
  Read existing R489 counter warning and current Run/HookInstructionFallback:
  macOS JIT branch not counted; hook_fb increments before native cache fast path.
  Nonzero hook counts therefore do not prove interpreter work or a regression.
- One-second5ms sample, handle16840 exits0, generated/ios-r553-file-select.sample.txt.
 144 CPU samples:117 native-dispatch subtree,12 other Run offsets, two explicit
 SingleStepInner subtrees.21 CPU float-future waits correspond to21 video
 depth-peek/Metal completion waits; do not add them as independent time savings.
- Evidence argues against interpreter dominance in this sampled view, not against
 all fallback cost. Exact PC/reason attribution remains open. No new profiler
 remains, no app/settings/input changes, same sole Simulator. PERF.md documents
 the distinction and evidence. Next Direct Touch input freshness; any performance
 candidate still needs native-work/readback evidence and controlled comparison.

## 2026-09-08 — R552 live file-selector state transitions

- Previous turn progress: tested/built state reader. Native Stop on R549 exits
 08:03:50.460 failed0, smc_failed0; then terminate app shell/install candidate.
 Shutdown native7266088207/fallback102102386/hook_fb260286803; these nonzero
 counters must not be described as zero-fallback proof or independent time cost.
- Candidate executable c24ffa5a2d9bd0d688591e6c6fbc3a73c03658dcc8ce958b76b8a5c4bc879f2a;
 R552 PID50620/session87943, generated/ios-runtime-r552.log. Same external dev
 assets/module, no fallback JIT, opt-in probes. One booted DE8E956F Simulator.
- Title visually verified before A+B. File select current806a0114/selector81111800
 atfield2766. Preaim0.2s+A file1: field3738 item81144cc8 flags1; field3744
 current0114/pending0118 while flags still1; field3750 current0118 flags2;
 field3810 current0128 flags2. Mode remains5 throughout; confirmation screenshot
 verifies Play This File/Copy/Icon/Erase, old item remains cached but invalid.
- B only returns: field4746 current0110/item0 then4806 current0114/item0.
 Fresh screenshot verifies file select, no copy/erase/save creation attempted.
 Input writers completed neutral. UI snapshots44.0/31.8/43.6FPS and mixed30s
 window37.922 do not constitute a speedup or matched performance comparison.
- Next establish guest hit-test input freshness before DirectTapQueue wiring;
 current/pending state checks are necessary but not a complete epoch. Classify
 fallback paths from existing diagnostics as a performance follow-up. Full goal open.

## 2026-09-08 — R551 distinguish file-selector substates

- Previous turn progress: tested queue policy. Revalidated PID49469 and sole
  booted DE8E956F Simulator. Traced Petari FileSelectItem::updatePointing and
  FileSelector::exeFileSelect/exeCopySelect/control: cached hover is separate
  from per-item pointing queries, and copy selection shares pointer mode5.
- LiveActor::isNerve resolves Spine current/pending rather than mode5. Exact
  DOL audit verifies spine load80165834, call80165844→8016FBA4 and complete
  seven-instruction getter, which prefers pending+8 over current+4.
- Added bounded ReadActorPointerState with missing/invalid/state-transition
  tests. Extended Simulator-only diagnostic to log current/pending Nerve changes,
  no per-step spam or action injection. Semantic Nerve names remain symbol/source
  candidates until live comparison; sampled state is not a complete scene epoch.
- ASan/UBSan reader test, DOL audit and Release build pass; original build handle
  39858 exits0, generated/build-r551.log. Not installed yet; R549 unchanged/live.
  Next native clean Stop/install and visible file-select→confirmation comparison.
  Input freshness and complete Direct Touch/performance gates remain open.

## 2026-09-08 — R550 queued-tap cancellation/freshness policy

- Previous turn classified progress: distinct live file targets and release
  evidence. Revalidated R549 PID49469 live; inspected current context reader,
  CoreHost input publication/mixer and PRD pointer-mode requirements.
- Implemented CPU-owned DirectTapQueue with explicit gesture/scene epoch/input
  revision/target/deadline contract. One-shot acceptance requires fresh matching
  target, readiness and A released. Tests exercise stale observations, duplicate
  gestures, held A, absent/disabled target, newer cursor revision, scene change,
  unknown context, different target, deadline, cancellation and replacement.
- New sanitizer test passes and is in repository suite; existing mobile-input
  regression passes. Full repository suite not rerun. No runtime integration or
  FPS improvement claimed; same installed app and sole Simulator remain live.
- Next must establish guest-side pointer-processing/hit-test revision and scene
  epoch before queue wiring. Cached hover or an elapsed VI is insufficient.
  Full Direct Touch/menu/performance/device/story requirements stay open.

## 2026-09-08 — R549 distinct file targets observed

- Progress: extended bounded pointer context reader with mode5 request-owner
  lookup (16 records), cached FileSelector hover and item pointing/invalid flags.
  Rejects ambiguous owners, invalid addresses/modes/flag values; no RAM scan,
  guest writes or automatic A/B. Exact-DOL audit now checks table bounds,
  owner/mode stores, mode5 request call and cached-hover load.
- ASan/UBSan reader tests and raw-DOL audit pass; Release app build passes.
  R548 clean Stop07:47:28.921 failed=0 before install. R549 PID49469/session18901,
  ios-runtime-r549.log; executable cd843cdc6c4f47e9ff6c9042316ed5475e3642ff58881800c236f32aef225abf.
  Same module, no JIT, one Simulator, opt-in diagnostic only.
- Visible file1 hover matches item81144cc8, file2 matches81192d08 under selector
  81111800. Both flags1 (pointing, not invalid), then item0/flags0 after pointer
  expiry. File2 screenshot generated/ui-r549.png captured during repeated hover;
  final writer79704 completed neutral and log clears at07:57:12.753.
- No save selection/creation during hover checks. Cached hover is not enough
  to authorize a queued tap: freshness, cancellation and submenu context remain
  required. Next implement that bounded path, preserving ordinary A/B.
  File-select30s windows approximately42–47 FPS; screenshot35.9 is instantaneous.
  No performance gain or complete Direct Touch claim. Full PRD goal stays open.

## 2026-09-08 — R548 live context observation

- Prior turn progress: bounded reader. PID47040 verified alive. Wired scoped
  end-VI-field CPU-thread event in Simulator host, exact DOL hash/size checked
  before enabling via GalaxyPadDevPointerContext. Every6fields, log changes
  only; bounds-checked big-endian memory reads. No guest writes/action injection.
  Default/device builds do not register callback; lifetime ends with Run.
- Initial app compile exposed PowerPC header dependency on GXRuntime include
  and ARM architecture defines. Matched source-specific CoreHost _ARCH_64 and
  _M_ARM_64 to existing core build; no guessed struct layout. Release build passes.
  Pointer reader sanitizer test/exact-DOL audit rerun pass.
- R545 native Stop07:39:00.458 failed=0; savehash unchanged99d432d5…ab64f and
  earlier R545 backup still available. Installed R548, sole Simulator PID48778/
  session44678, ios-runtime-r548.log. Same external devpaths/noJIT/FPS flags plus
  -GalaxyPadDevPointerContext YES. No second Simulator or profiler.
- Logs enabled with exact DOL proof07:40:10.906. CPU-thread log initialunknown
  field6, titlemode4/object8091f3d8 field1398, unknown1530 thenmode4 at1536.
  Visible title verified. Actual A+B→file select, mode5/sameobjectfield2946;
  screenshot verifies saved Mario file1. Identity stays same across these modes;
  cannot use pointer object alone as a scene epoch. Unknowns must cancel safely.
- Last input writer completed and published neutral. Left at file select for
  next target-readiness probe. No Direct Touch feature/performance acceptance
  claim; only context detection now live. Current runtime R548 as above.

## 2026-09-08 — R547 bounded pointer-context reader

- Prior turn progress: exact-USA readiness call-site audit. PID47040 verified
  live; runtime left unchanged. Inspected core loop for observation cost; avoided
  adding every-native-dispatch/instruction instrumentation to lagging runtime.
- Exact DOL audit extended: getStarPointerOnOffController803FA3E4 loads singleton
  via r13−14968 then offsets12/4/8; mode offsetD0 independently observed in
 803FCC74. Raw lwz/branch assertions pass against supported DOL hash.
- Added GalaxyPadPointerContext.h read-only injectable reader. Cached MEM1/MEM2
  aligned bounds checked before each read; null/missing/overflow/invalid mode
  returns unknown. Includes controller identity; not a ready target or tap grant.
  Requires exact identity gate and CPU-thread caller before runtime integration.
- Mixed MEM1/MEM2 chain, missing nodes, bad pointers and modes tested under
  ASan/UBSan; pass. Added reproducible test script/repository suite entry.
  Not integrated/built into app yet; no claimed live mode or Direct Touch action.
  Next bounded CPU-thread observation and visible transitions. R545 remains live
  PID47040/session79921, no input writers, same sole Simulator.

## 2026-09-08 — R546 exact-USA menu readiness contract

- Prior turn progress: candidate installed, file reload and delayed selection
  validated. Confirmed PID47040 live; no restart, other Simulator or input writer.
- Read Petari FileSelectItem, ButtonPaneController and GamePadUtil: selection
  checks target pointing before an A edge; held A cannot regenerate an edge
  after readiness. Kept Korean addresses out of runtime changes.
- Searched primary community sources and downloaded only Bussun USA symbol text
  at pinned78f4a04ce85964440442101c606ba49fcf1c634e. Source warns non-Korean
  symbols may be wrong; treated names as leads, not proof.
- Added audit-menu-pointer-contract.py; exact DOL SHA guard, section mapping,
  raw lbz/li and relative-bl operand assertions pass. Confirms selected USA
  pointing flags and call relationships independently of generated C comments.
  Details/provenance/next safe hook boundary in DIRECT-TOUCH-CONTRACT.md.
- No guest hook or automatic A/B added. Target object/context lifetime remains
  unverified. Next readiness/identity probe, not a universal delay or bypass.
  Live R545/PID47040/session79921 unchanged at opening; no new performance claim.

## 2026-09-08 — R545 candidate install, reload and acquisition check

- Prior turn progress: isolated/fixed/built pointer reacquisition. Verified
  R542/PID45436 alive; native Stop completed07:18:31.126 failed=0. Backed up
  Wii/Config ios-r545-nand-before.tgz SHA256
  730eec1f33655d5ab56d5e6f0ca82a20b0297dcaf445e8d81e579900519e4f34.
- Installed R544 candidate executable a70d10c4fea5d94391ccdafeb96eed8e0456f8b9d476eb1c081054236b34ff43.
  Module SHA unchanged3acdcddcd47812c2e2a67b8cec0cf06c17009cf1ad7f2e9dc9abd83158a1bac0.
  Sole Simulator PID47040/session79921, ios-runtime-r545.log, same external
  dev input/game/disc/module args and local CPUThread experiment. Regressions
  mixer/controller/lease/exact-pointer all rerun and pass.
- Live A+B enters file select showing persisted Mario file1. Immediate
  pointer(.38,.64)+A0.5s still misses; no blind-repeat. Pre-aim0.2s then same
  A0.5s selects. Play(.68,.92) with pre-aim0.2s+A0.5s loads opening story.
  Corrected branch is not sufficient for zero-delay selection. No measured
  minimum delay, guest filter diagnosis, touch calibration or FPS improvement.
- GameData hash remains99d432d517b92b19da0c403819b91288b10c7063c3ce61f5000347ef823ab64f.
  Current data container AB107E56-DBEE-4C75-8B52-327549435198. Basic newly-created
  file reload now game-visible; not progressed save/reload/Grand Star acceptance.
  Native ui-r545-reload.png rotated and inspected, matches opening story.
- All diagnostic writers complete; last JSON neutral/expired. Left same app
  visible at first opening page. Next derive menu context/acquisition state
  before implementing Direct Touch; retain explicit Classic Pointer fallback.

## 2026-09-08 — R544 pointer reacquisition branch fix

- Prior turn progress: live Festival movement/jump and sample. Confirmed same
  PID45436 live; left it untouched while doing source/test/build work.
- Created exact-function extraction test with narrow dependency doubles.
  Original EmulatePoint fails hidden→visible immediate-reset assertion: positive
  position overwritten before checking old negative sentinel. Captured was_hidden
  first; corrected test passes repeated reacquisition, visible smoothing and
  sensor-bar offsets. No fake EFB, constant depth, guest override or motion bypass.
- Canonical Dolphin0023 patch hash8310e45ac65c4743924f1edf902dc056da4d54fc8eee925a474d157c1443233d;
  initial hand-authored hunk count corrected, reverse-check then passes. Applied
  source previously clean for Dynamics.cpp; unrelated nested edits preserved.
  Bootstrap hash/peel/restore/apply/scope and repository test integrated; bash -n
  passes, full bootstrap intentionally not run against active runtime graph.
- Incremental core build-r544-core.log passes (existing ScmRevGen bad ^master
  warning did not fail build); provision-r544.log and build-r544-app.log pass.
  Candidate is NOT installed. No live acquisition/performance gain claimed.
- Next install after clean Stop, exercise fresh-file pointer hide/reacquire and
  compare immediate A vs prior pre-aim requirement. Do not assume this removes
  guest acquisition/filter latency or fixes pixel alignment. Current live build
  remains R542/PID45436/session75150 at Festival hill foot with inputs neutral.

## 2026-09-08 — R543 Star Festival movement and gameplay sample

- Previous turn progress: actual file creation/opening. Verified same PID45436
  live; continued without restart. Touch A through CUA advanced six visible
  opening pages/invitation into Star Festival. Thus actual UI button route,
  not just injected snapshots, now has game-visible action evidence.
- Diagnostic moveY1 for2s moved from entrance into central flowerbed. Touch A
  produced jump. moveY1 for3s plus touch A moved/jumped toward hill; screenshot
  showed airborne Mario above shadow. Writers ended and neutral released; final
  native screenshot ui-r543-festival.png verified after landing at hill foot.
  Not physical multitouch, full gameplay, audio, or save/reload acceptance.
- Same runtime/no build/profiler running. Gameplay30s windows1273/30.699932=
  41.465890FPS and1278/30.800011=41.493492FPS. Opening static pages had near60;
  these different scenes are not before/after optimization evidence.
- One bounded sample1s/5ms completed, ios-r543-festival.sample.txt.141samples
  per thread. Main136sleepingmach_msg. CPU118underRun+1072/native dispatch,
  including22future waits;16exclusiveRun across collapsed offsets. Video21
  PeekEFBDepth→Metal StagingTexture Flush→waitUntilCompleted;71timed waits,
  20VertexLoader inclusive/exclusive branches. Supports native+readback cost,
  not overlay-dominated lag; no claim all waits are independently recoverable.
- Pointer investigation: active profile has GalaxyPad bindings and no IR angle
  overrides. Cursor defaults vertical offset10cm/yaw25deg/pitch20deg. Dynamics
  EmulatePoint maps axes to angles, not final screen coordinates. Source defect
  candidate: writes state.position.y=NEUTRAL_DISTANCE(+2) before testing y<0
  for hidden→visible immediate angle reset, making that branch unreachable.
  Existing hidden case sets y=-1000. This plausibly contributes acquisition
  delay but is NOT yet experimentally established as sole failed-tap cause.
  No core/settings change made; next isolate/reproduce this branch contract
  before changing dynamics. Preserve native motion behavior and exact profile.
- Sole Simulator/current R542 PID45436/session75150 left visible in Festival,
  fresh Mario file1 near hill foot; no held diagnostic command. Full goal open.

## 2026-09-08 — R542 actual mobile file-select route

- Prior turn progress: controller alias built/tested. R540 PID44540 confirmed
  live; no second Simulator. Implemented opt-in GalaxyPadDevInputFile diagnostic
  full-state JSON bridge on existing10Hz UI tick, using Controller mixer source
  while physical controller callbacks suppressed. Touch remains independent.
  Physical-device build excludes bridge. No guest changes or runtime JIT.
- Short leases, strict numeric bounds/size/type validation, automatic neutral,
  reset epoch and inactive gating prevent stale holds. Atomic Python writer
  refreshes0.5s lease and releases finally. Foundation sanitizer tests cover
  A+B/axes, expiry/reset/future/malformed; test added to repository suite.
  Release build-r542.log passes. Python compilation passes.
- R540 clean Stop06:59:17.534. Wii+Config backup ios-r542-nand-before.tgz SHA256
  65a685b72d8ca16fa54dcefcd33017c4e6189d3f1f9189d7cdd255aa981c9db4.
  R542 installed/launched PID45436/session75150 in same sole DE8E956F Simulator.
  Log ios-runtime-r542.log; external game/module args unchanged, dev input path
  generated/ios-dev-input.json (absolute workspace path at runtime).
- Actual A+B1s advances title. Pointer(.38,.64) visibly highlights file1.
  Simultaneous pointer+A0.5s did not select; --aim-first1 then A1s selects.
  Confirm create(.64,.80), Mario(.28,.32), confirm(.64,.80), each A0.5s after
  pre-aim1s. Play(.72,.85) missed upper target edge; inspected cursor, then
  (.68,.92) with same pre-aim selects and reaches opening story.
  Do not infer exact acquisition latency or calibration constants from this.
- Fresh Mario file1/0stars visible. GameData.bin SHA256
  99d432d517b92b19da0c403819b91288b10c7063c3ce61f5000347ef823ab64f.
  Current data container D8AEEDA2-3833-4F2B-9E94-191D177413BC. Save reload not
  tested. Native screenshot ui-r542-opening-native.png rotated−90 to
  ui-r542-opening.png and inspected: valid opening-story evidence.
- All writers ended; final dev JSON neutral/expired. Game remains visible at
  first story page, no held inputs. Menu snapshots31–51FPS, no performance gain
  claim. Next opening→actual movement/jump, pointer calibration then named
  context Direct Touch. This is live host-input evidence, not touch ergonomics,
  physical-controller acceptance, G10 first Grand Star, or complete PRD.

## 2026-09-08 — R541 preserve aiming while using A/B

- Previous turn: progress, R540 installed and visibly verified. PID44540 confirmed
  live this turn; title window1031/30.700008=33.583053FPS, lag remains.
- Investigated simultaneous input: current iOS host has no pipe/test-input route
  (macOS wii-pipe.py exists; SunPad's pipe is its core bridge). Do not claim
  handler tests exercise real UIKit multitouch or substitute them for gameplay.
- Found physical mapping gave right shoulder redundant C, leaving no A with
  right thumb on aim stick. Rechecked first-person GameCube adaptation post28;
  secondary shoulder A/B explicitly addresses this. Changed right shoulder to A,
  kept trigger B, Y/C, other mappings and touch layout intact; menu text updated.
- Added test: aim+A fails with original mapping, passes after fix; aim+A+B,
  independent release and Y/C pass. Sanitized mobile-controller and mobile-input
  tests pass; Release build generated/build-r541.log passes.
- R541 NOT installed; visible runtime is R540/PID44540/session35150. No extra
  Simulator, no profiler, no physical-controller/gameplay/performance acceptance.
  Next interaction work remains mobile repeatable input and real file select,
  then context-specific Direct Touch; do not repeat title-only acceptance loops.

## 2026-09-08 — R540 reject letterbox pointer ownership

- Previous turn is progress (requested UI corrections built/installed/verified).
  Current PID43791 confirmed live. No layout/button remapping this turn.
- Found real pointer-handler defect: touchesBegan accepted any contact, including
  a finger in letterboxing. Its retained _pointerContact prevented a second,
  valid playfield touch from acquiring pointer ownership.
- Added synthetic UITouch subclass in isolated UIKit test host; direct handler
  sequence border-down then viewport-down fails on unchanged product with
  "letterbox contact must not steal the gameplay pointer". After fix it passes:
  only in-viewport contacts acquire ownership; native-UI blocking also respected.
  Move while A held verifies normalized viewport coordinates and retained A;
  pointer cancellation hides IR without releasing A. These are handler tests,
  not physical multitouch delivery or live-game interaction acceptance.
- Test app briefly foregrounded in sole Simulator; existing game remained
  PID43791, fell to0.9%CPU in background, and resumed same PID when brought back.
  Test host removed after passing. Release build build-r540.log passes.
  Clean native Stop of R539 follows before product update. No second Simulator,
  no new game while first was active, no profiler, no performance gain claim.
- R539 clean Stop confirmed runtime exit failed=0 at06:47:43.645. Replaced only
  stopped product app; R540 installed and launched in same sole iPad Simulator,
  PID44540/session35150, generated/ios-runtime-r540.log. Fresh CUA screenshot
  verifies title and unchanged letter-only/lower-cluster UI. Counter35.5FPS;
  no gameplay/performance acceptance. Left visible for continued interaction.

## 2026-09-08 — R539 restore SunPad visual language after user rejection

- User explicitly rejected visible action subtitles and top utility strip.
  Opened actual SunPad docs/readme/sunpad-delfino-plaza.jpg and read its full
  layout method before revising. SunPad is the visual authority, not R538.
- Removed attributed/multiline button titles. Displayed face buttons A/B/X/Y/Z;
  X retains Spin input mask/identifier, Y retains C mask/identifier. Assistive
  labels explain actions, visible faces do not. Saved preferences not wiped.
- Restored lower-left stick/D-pad arrangement and asymmetric lower-right face
  cluster, with1/2 above movement and +/- pills above right cluster. Maintained
  nonoverlapping default rectangles, safe margins, optional tilt/menu/editor.
  This is reference-derived spacing with Galaxy additions, not pixel identity.
- Read first-person GameCube adaptation at BitBuilt post28 and actual SN30 Pro
  profile (fabito gist051f7c95041e41a993c8c016e6cd5402): common need is aim plus
  A/B concurrently, often enabled by duplicate shoulders; spin has face bindings.
  Mapping variants exist. No external game patch downloaded or installed.
  Research notes/sources in GALAXY-TOUCH-DESIGN.md; R538 decisions superseded.
- Release build passes. Expanded UIKit test rejects multiline/attributed action
  text and upper-half auxiliaries, alongside44pt/nonoverlap, A+B, tilt toggle and
  editor regressions; all pass, test host removed. R538 Stop exit failed=0.
- Installed R539, same sole iPad, PID43791/session37622, ios-runtime-r539.log.
  Upright live title and restored letter-only/lower-cluster layout inspected.
  Native-panel screenshot rotated only for orientation into ui-r539-verified.png,
  then opened and accepted; CUA same title/layout corroborates. Counter37FPS is
  not a benchmark. Keep current runtime visible; no second Simulator.
  Root/disc/module/no-JIT and experimental CPUThread=True unchanged. No FPS win
  claimed. Native Direct Touch and pointer-capable right-stick adaptation remain
  separate implementation work; optional yellow stick still tilt, never mislabeled.
- Initial read-only warning lead: Petari WPadHolder selects5-minute WPAD auto
  sleep except15-minute EpilogueDemoStage. This plausibly explains long idle
  title warnings; exact RMGE01 tracing needed before changing disconnect policy.

## 2026-09-08 — R538 research-led Galaxy touch hierarchy

- User steering replaced planned measurement-only rollout with touch research
  and iteration. Nintendo original Galaxy manual controls/actions is primary;
  Switch handheld touch-pointer precedent is secondary, explicitly distinguished.
  Sources and design inference documented in GALAXY-TOUCH-DESIGN.md.
- SunPad menu/editor and identifiers preserved. iPad A96/Spin80pt become primary
  right-thumb actions; B/Z64pt secondary, with role labels alongside A/B/C/Z.
  Camera C and D-pad sit above movement; Wii1/2/minus/plus in upper utility row.
  Saved positions/scales/opacity untouched. Phone positions unchanged/unverified.
- Existing virtual tilt is available through actual Controls menu with a saved
  toggle; default hidden, editor always exposes it. Toggle clears all held input,
  hidden tilt disables hit testing. No automatic stage detection or accepted
  ball/ray behavior claimed. Classic Pointer unchanged; Direct Touch still open.
- Expanded isolated UIKit checks pass: default geometry44pt/nonoverlap, A+B
  callbacks/releases, tilt visibility/hit testing, editor exposure and resize.
  Build-r538.log passes. Test app uninstalled. Clean R536 Stop exit failed=0;
  R538 installed/launched in same sole Simulator, PID42791/session33924.
- Live Controls action toggles tilt on, checkmarked action verified, toggles off;
  default layout restored. Title screenshot matches inspected CUA layout, labels
  readable and center unobstructed. Saved CUA buffers ui-r538-controls/platforming
  omit dynamic UI despite inline output; rejected as screenshot evidence. Native
  ui-r538-native.png captured same Simulator/title, rotated90deg for presentation
  only into ui-r538-verified.png and inspected; this is accepted visual evidence.
- R536 Wii Remote communication warning recurred without sustained profiler,
  so instrumentation is not its sole explanation. Remains open alongside FPS.
- R537 measurement now live: startup/title mixed window53.099824FPS excluded;
  menu blocks06:29:56.347→06:30:51.323, no window emitted while blocked; first
  resumed full window06:31:21.747 gives1073 frames/30.000012s=35.766652FPS.
  Later screenshot53.6 is instantaneous, not a gain. No performance acceptance.
- Full control ergonomics, physical multitouch, gameplay beyond title, Direct
  Touch contexts and tilt mechanics are unproven. Leave current game visible;
  no second Simulator or profiler. Goal remains the entire PRD, not UI completion.

## 2026-09-08 — R537 native-loop attribution and bounded FPS windows

- Previous turn classified progress: real overlay changes plus live thread
  comparison. Revalidated live PID41431; left single Simulator/game untouched.
- Exact app nm Run address100233514; disassembly retained in
  generated/ios-r537-run-disassembly.txt. R536 +1072 maps100233944, immediately
  after blr native module dispatch; inclusive child work is not interpreter work.
  Exclusive aggregated offsets +988/1488 map dispatch-sampling flag load and
  lookup metadata load respectively. Sample collapses offsets, so cannot assign
  the21 exclusive samples individually or claim those loads cost15% themselves.
- Read R469/R470 findings before choosing a candidate: lookup metadata snapshot
  already failed reversed-order macOS repeatability; do not recycle unchanged.
  iOS module already uses ThinLTO, pinned PGO, effective O2 and strict FP. No
  unfounded fast-math or metadata rewrite introduced.
- Added opt-in -GalaxyPadLogFrameRateWindows YES: existing1Hz observation emits
  actual frame delta/time every>=30s. Window resets on UI presentation/change,
  app activity changes and Stop; inactive/paused/not-running observations excluded.
  This measures presented-counter cadence, not VI, GPU scanout or gameplay.
  Release build generated/build-r537.log passes; not installed/runtime-tested.
  No benchmark or FPS improvement claim. Next validate a complete window and
  pause/reset before matched comparisons; keep title phase and settings explicit.
- Host read-only snapshot observed Logitech updater69.2%CPU, WindowServer44.8,
  SimMetalHost29.8 alongside game170.3 (percent of one core). Confound, not causal
  attribution; unrelated processes untouched. No heavy profiler started.

## 2026-09-08 — R536 local dual-core comparison, no clear win

- R535 clean Stop exit failed=0, terminated only that stopped app. Added just
  CPUThread=True to existing Simulator profile Dolphin.ini under data container
  81B4D48A-7E6A-4480-8B16-34004F2B62CF. Same app/module/dev paths/native resolution;
  no JIT. No product default change. R536 PID41431/session5578 remains visible.
- Fresh upright title counter39.1 then31.8FPS, compared with R53529–41 snapshots.
  These are observer snapshots at differing title-animation phases, not a matched
  benchmark; insufficient gain to promote. No communication warning in R536 so far.
- One-second sample at5ms confirms distinct CPU thread (versus CPU-GPU baseline).
  106/140 CPU samples inclusive under native chassis_dispatch, including8 waits
  for an EFB result;21 exclusive StaticRecompCore::Run. Main thread136/140 in run
  loop mach_msg wait. This points toward native execution/dispatch, not touch UI,
  as the main next profiling target. Do not attribute all106 to dispatch overhead
  or all fallback step counts to measured time. Module already uses pinned PGO.
- check-ios-host.sh and offline CPU summary tests pass. Only one Simulator and
  one GalaxyPad; no xctrace process remains. Next compare synchronized title
  intervals and resolve sampled native PCs/Run offsets before a targeted change.

## 2026-09-08 — R534/R535 profiler interruption and iPad overlay iteration

- R534 CPU Profiler attach with explicit Simulator device stalled beyond its
  10-second limit (xctrace PID40322). User observed ~6 FPS; screenshot12.7.
  Stopped that exact profiler with INT then TERM, leaving game alive. Subsequent
  counter snapshots43.6/41.8, but Wii Remote interruption dialog also appeared.
  Correlation supports profiler overhead, not a proven sole cause or stability.
  First attach without device failed to find PID; neither trace is acceptance.
- Offline CPU summary now supports explicit CPU-GPU thread prefix; synthetic
  tests pass. No usable cycle profile obtained from this interrupted capture.
- R535 retains SunPad menu/editor/input, replaces crowded iPad default positions
  with separated edge anchors, smaller actions/sticks, round auxiliary buttons,
  and thinner borders. Saved user layouts/opacity remain untouched. Status bar
  hidden; FPS label compact with descriptive accessibility text. Phone defaults
  unchanged. No image cropping/stretching or render-quality changes.
- UIKit test adds default iPad nonoverlap and >=44pt target checks. Test passes
  with existing A+B/release/editor slider checks on sole iPad; test app removed.
  Production Release app builds and is installed. R533 Stop clean exit failed=0.
- R535 fresh unprofiled title visibly verifies new layout. Counter33.6 then41.0;
  startup safety screen60 is not gameplay performance. Before/after native-panel
  captures generated/ui-r535-{before,after}.png are saved in Simulator native
  panel orientation; CUA window captures show upright UI. No physical touch or
  accessibility-compliance claim. Black bars preserve the 16:9 game aspect.
- New concrete performance lead: iOS profile has no Core/CPUThread override;
  upstream non-Android default is false, matching CPU-GPU sample thread. macOS
  explicitly uses True. Next compare same iPad app/module at native resolution
  with True, without profiler/build overlap, before promoting a product default.
  R533 shutdown also records59,980,411 interpreter fallback steps; quantify cost
  and identify their PCs, rather than infer cost directly from instruction count.

## 2026-09-08 — R533 visible runtime and title performance sample

- User asked how game runs and why it wasn't visible. Prior status-only reply was
  no implementation progress; reoriented from menu work to visible runtime.
  Confirmed none booted, booted only DE8E956F-6B29-4FF3-AF4A-77034CE8588A, installed
  R532 and launched full development root/disc/module paths (private installed
  assets removed in R531). STATICRECOMP_NO_FALLBACK_JIT=1, FPS counter YES.
- Raised Simulator and selected Landscape Left. Upright Galaxy title visible;
  observed AX51.8FPS/screenshot44.0FPS, later41.0FPS. These short observer-affected
  snapshots are not a benchmark or evidence of stable60, nor gameplay beyond title.
- Three-second sample PID39834 completed, generated/ios-r533-title.sample.txt.
  CPU-GPU thread1967samples; StaticRecompCore::Run inclusive1354 branch and leaf183
  among hot sites, native function/dispatch/MMIO work also present. Sleeping
  platform/audio/worker threads must not be mistaken for CPU bottlenecks. Need
  controlled frame-time/dispatch profiling to attribute slowdown, not a claim
  that the menu or machine alone explains it.
- LEAVING sole Simulator/game visibly running for user as announced. PID39834,
  console tool session7078, generated/ios-runtime-r533.log. No Stop/shutdown; next
  turn must inspect existing process/session rather than boot another. Original
  goal open, real simultaneous A+B/file select still unverified. Resume runtime
  advancement/profiling rather than repeating menu-only acceptance work.

## 2026-09-08 — R532 standalone diagnostic-log menu action

- Previous turn progressed with real idle removal/NAND preservation. Re-read
  PRD three-dot hierarchy/diagnostics requirements against current menu; standalone
  Share Diagnostic Log missing despite guided Report a Problem implementation.
- Added optional delegate/action to existing SunPad native menu, enabled only
  with implementation. Generates bounded/redacted local report without questions,
  presents existing review controller; only its explicit Share opens system sheet.
  No automatic upload/public issue tracker. Reused shared preview/error presenter
  for guided report to avoid divergent behavior.
- Shared diagnostic context now includes configured RMGE01 revision0 and image/
  DOL hashes from generated canonical identity header. Labels these configured,
  not proof of loaded-module verification. Runtime busy/paused/frame count/render
  selection/classic pointer retained; missing signature/audio/dispatch marked open.
- App build passes (build-ios-app-r532.log); native Foundation diagnostic redaction
  and bounded-formatting tests pass. No Simulator launched, log exported or data
  deleted. New action UI, shared preview regression, share cancellation and runtime
  pause/resume still need live evidence. Other PRD menu and gameplay/device gates
  remain incomplete; original goal unchanged.

## 2026-09-08 — R531 live removal, cancellation and NAND preservation

- Previous turn progressed with removal code and safety tests. Confirmed zero
  booted Simulators; booted sole DE8E956F-6B29-4FF3-AF4A-77034CE8588A, installed
  R530 and launched idle without module/game. Data container now
  E7B1BAC7-E115-4D22-BA18-D3743496CE6A. Installed GameData measured6.4GiB.
- Captured sorted relative-path SHA256 manifest of all19 files in private Wii
  NAND before removal. SunPad menu→Game Data & Saves→Remove opens disclosure.
  Cancel returns normally; installed image and DOL existence checks pass.
- Reopened same flow and confirmed Remove. UI reports installed data removed.
  GameData no longer exists and support root has no GameData.* staging entries.
  After manifest byte-identical to before: generated/nand-r531-before.sha256 and
  nand-r531-after.sha256. Screenshot ios-r531-removal-success.png. No control
  preference hash comparison performed, so that remains implementation-based.
- Free space39→46GiB. Deleted only Simulator-installed regenerable image/extracted
  data via product feature; original ref/supermariogalaxy.wbfs, known-good run1 and
  actual NAND files preserved. Reimport needed before next private-data boot.
- Terminated idle host and shut down sole Simulator. No gameplay during removal;
  running-game stop-before-delete and import-after-removal remain unverified.
  Full original PRD/menu/gameplay/device/performance objective remains active.

## 2026-09-08 — R530 SunPad installed-game-data removal

- Previous turn progressed with isolated UIKit resize/chord callback evidence.
  Continued remaining menu feature: read existing SunPad-derived removal dialog
  and current atomic import boundary before wiring its delegate.
- Added detachInstalledData: exact GameData.removal-UUID sibling, absolute private
  directory root, real-directory active target, confirmed stopped runtime and
  renamex_np EXCL. No traversal, overwriting, save/NAND or recovery-stage scan.
  Caller owns detached tree for off-main deletion; failures stay explicit.
- Menu confirmation now discloses Stop first, installed assets only, saves/settings
  preserved and originals/recovery copies retained. Host blocks concurrent imports
  and gameplay, waits onExit when busy, then utility queue detaches/deletes exact
  tree. Menu enabled only with implemented delegate. Failed cleanup is reported;
  crash-leftover and prior recovery-copy cleanup remain separate unfinished work.
- ASan/UBSan filesystem tests reject active runtime, invalid basename, collision
  and symlink GameData; successful detach/delete preserves Wii save sentinel and
  previous recovery marker. Existing transaction tests also pass. Simulator app
  build passes (`generated/build-ios-app-r530.log`), no compiler warnings/errors.
- No Simulator launched or real user/game data removed this turn. Only disposable
  synthetic test trees deleted. Live Stop→removal, confirmation cancellation,
  failure UI, file-space recovery and import-after-removal still need testing.
  Full PRD and gameplay/performance/device goal remain active.

## 2026-09-08 — R529 isolated UIKit overlay regression host

- Previous turn progressed with Wii2 visual/editor selection but resize drag
  unchanged. Inspected pass-through hitTest, editor constraints and size callback.
  Added standalone tests/mobile-ui host compiling the actual overlay/settings,
  separate org.galaxypad.overlay-tests bundle and preferences, no game core.
- Initial harness compile caught incorrect ObjC call syntax, then missing explicit
  CoreGraphics link; fixed both. Final build script tests/build-mobile-ui.sh builds
  generated/tests/OverlayTests.app with Simulator SDK, not a product test bypass.
- On sole iPad, test app displays actual overlay, locates/selects Wii2, proves
  slider hit-test target, sends ValueChanged1.25 and asserts persisted getter and
  width ratio1.25. It also dispatches A down/B down/A up/B cancel and checks
  A|B→B→neutral. Both runs completed; final log generated/ui-tests-r529.log,
  build log generated/build-ui-tests-r529.log. Runtime logs sizeChanged2=1.250.
- These are real UIKit objects/callbacks but programmatic events, not synthetic
  UITouch creation or physical multi-touch. They narrow the earlier drag failure
  without proving gesture delivery, across-relaunch persistence or game input.
  No product behavior was rewritten merely to make automation pass.
- Test uses only its own isolated defaults and exits after checks. Uninstalled
  test app/preferences (rebuildable), shut down sole Simulator, no game launched
  and product data/settings preserved. Full PRD goal remains active.

## 2026-09-08 — R528 Wii2 visual/editor integration check

- Previous turn progressed with missing Wii2 addition and input tests. Confirmed
  no booted Simulators, installed R527 on sole DE8E956F-6B29-4FF3-AF4A-77034CE8588A
  and launched idle app, no development module/game. Explicit Landscape Left.
- Wii2 visibly placed above1 with a gap; not overlapping other controls in this
  current iPad layout. Opened actual SunPad Controls→Touch Control Settings→Move
  controls. Coordinate selection of2 highlighted its border and changed editor
  label/slider accessibility name to “2 size.” Done removed editor normally.
  Screenshot generated/ios-r528-two-editor-selection.png.
- A coordinate slider drag425,643→451,643 did not change value/geometry. Source
  handler has expected ValueChanged target, per-control persistence and relayout,
  but this observation does not distinguish event-delivery from product defect.
  Do not claim size editing or persistence works from selection alone.
- No controls intentionally moved/reset, no game/save access, no data deletion.
  Terminated idle app and shut down only Simulator; full original goal remains
  open. Multi-touch title advancement, resize validation and compact iPhone
  layout remain outstanding; don't substitute policy tests for those checks.

## 2026-09-08 — R527 touch chord audit and missing Wii 2 control

- Prior turn progressed with upright title and separate A/B input transport.
  Inspected SunPad-derived down/up handlers, overlay multitouch setting and stick
  contact ownership. Buttons OR their own bit and clear only their own bit;
  overlay multipleTouchEnabled YES. No evidence justifies rewriting chord logic.
  Available tool discovery exposed no dedicated multitouch gesture tool.
- Found Wii Two runtime binding present but no overlay control. Added 2 through
  existing addButton/placeControl/identifier path, above1 in default layout; it
  inherits reference styling, settings, editing, scaling and persistence. Existing
  controls/identifiers were not repurposed. Layout still requires visual checks.
- Input mixer regression adds A→A|B|Two→B|Two→neutral sequence; passes alongside
  existing latch/clear/axes/pointer tests. This is policy proof, not physical
  multitouch or title advancement. App build passes (build-ios-app-r527.log).
- No Simulator/game launched or data removed. Next verify 2 layout/input and
  pursue genuine simultaneous input without adding a fake one-button title bypass.
  Full PRD/menu/device/gameplay/performance goals remain unfinished.

## 2026-09-08 — R526 upright live title and touch-input transport

- Previous turn progressed with targeted scene-lock correction and idle rotation
  proof. Confirmed no booted Simulators, used only DE8E956F-6B29-4FF3-AF4A-77034CE8588A.
  Launched installed R525 with development module only, imported private data,
  STATICRECOMP_NO_FALLBACK_JIT=1. Explicit Landscape Left, no modal workaround.
- Startup UI, safety output and Galaxy title all visibly upright. Screenshot
  generated/ios-r526-upright-title.png. Runtime startup took roughly77s before
  input registration; a one-second sample collected while checking the same live
  PID had already reached runtime work, not a diagnosed startup bottleneck.
  Sample generated/ios-r526-startup.sample.txt; no restart on observation delay.
- Coordinate clicks on visible A and B separately. Title correctly remains at
  simultaneous A+B prompt. Stop diagnostics: samples11454,buttonSamples2,
  transitions4,last0,pointerSamples0. This supports button transport/release,
  not button-chord, pointer, file-select or gameplay acceptance.
- Normal SunPad Stop: shutdown native514442717,fallback13505938,
  native_exc218910,hook_fb27838186,smc_failed0; runtime exit failed=0 and visible
  Game stopped. Log generated/ios-runtime-r526.log. No FPS/audio quality claim.
- Terminated stopped host, console exited0, shut down sole Simulator and verified
  none booted. Installed game data retained. Next needs a genuine simultaneous
  input test (or supported controller/keyboard route) to advance past title;
  do not replace separate clicks with a claim of multi-touch gameplay proof.

## 2026-09-08 — R525 targeted scene-orientation lock correction

- Previous turn progressed with imported-data title and clean Stop, but showed
  sideways presentation until native modal. Compared SunPad controller/app
  orientation masks and installed UIKit UIViewController.h lines727–740: scene
  orientation lock defaults false and is separate from supported orientations.
  SunPad uses landscape masks without Galaxy's prefersInterfaceOrientationLocked
  YES override. Native alerts could change the governing lock preference.
- Removed only that override; retained landscape masks, preferred landscape-left,
  requestGeometry and renderer. Build passes (build-ios-app-r525.log).
- Confirmed no booted Simulators, booted only DE8E956F-6B29-4FF3-AF4A-77034CE8588A,
  installed and launched idle app without module. Explicit Simulator Landscape
  Left yielded upright native overlay without any app modal. Screenshot
  ios-r525-unlocked-landscape.png. Terminated idle app, relaunched in landscape:
  upright again without modal (ios-r525-landscape-relaunch.png).
- This supports the lock mismatch as cause of the reproduced presentation issue;
  not acceptance of both landscape sides, foreground rotation, runtime rendering
  or physical iPad/iPhone. No game launched, no installed data removed. Terminated
  idle app and shut down sole Simulator. Next verify imported-data gameplay view
  with this correction, then advance through title using actual Wii controls.

## 2026-09-08 — R524 boot from imported private data and clean Stop

- Previous turn made real import/extraction comparison progress. Confirmed no
  booted Simulators, then booted only DE8E956F-6B29-4FF3-AF4A-77034CE8588A.
  Existing R522 app launched with only GalaxyPadDevModule pointing to the accepted
  simulator dylib, STATICRECOMP_NO_FALLBACK_JIT=1. No development root/disc args.
  Current startup source selects installed private GameData when its image exists.
- Native input device registered, visible safety screen followed by Galaxy title
  with A+B prompt. Screenshot generated/ios-r524-imported-title.png. This proves
  imported-data boot, not file select, gameplay or performance. No FPS benchmark.
- Explicit Simulator Landscape Left still left app sideways until native Stop
  alert; final stopped state upright. Host geometry initially1210x834 landscape
  right even while Simulator rendering sideways. Needs targeted orientation
  investigation before touch acceptance, not speculative renderer changes.
- SunPad menu Stop confirmation completed normal shutdown: native352092796,
  fallback11247630, native_exc152785, hook_fb21380174, smc_failed0. Runtime exit
  failed=0 and visible Game stopped both verified before shell termination.
  Log generated/ios-runtime-r524.log. Interpreter/hook fallback remains substantial;
  no native-only or real-device performance claim. No touch button samples used.
- Terminated idle host, console session exited0, shut down sole Simulator and
  verified no booted devices. Kept installed data for continued testing. Full
  original PRD goal open. Next fix/verify startup orientation then proceed past
  title with actual controls; import reactivation/cleanup and device package remain.

## 2026-09-08 — R523 real mobile import and extraction comparison

- Previous turn progressed with verified-stage discard fix and sanitizer/build
  pass. Confirmed 46 GiB available and zero booted Simulators. Booted sole iPad
  DE8E956F-6B29-4FF3-AF4A-77034CE8588A, installed R522 and launched without game
  arguments. No runtime or guest save access was started in this test.
- Live app data container: 9A9C53EC-C845-4F4C-BFDB-923AD68DCFF6 under that device's
  data/Containers/Data/Application. Created a cp -cn copy-on-write selection copy
  of original ref/supermariogalaxy.wbfs at Documents/GalaxyPad-R523-Import.wbfs.
  Original preserved. Selected through SunPad Game Data & Saves → Import from
  GalaxyPad Folder, confirmed disclosure. UI reports Game data imported.
- Installed Library/Application Support/GalaxyPad/GameData/RMGE01.wbfs SHA256
  bd0d3d4bc1376a8614fd8f4fee5df86be6bc676e9d617944918fda79ed9e3589;
  RMGE01/sys/main.dol SHA256
  2c680585a8f58e1cc9c5521b579057f12b124ff0ef409e470a57606c50a93c09.
  Both match exact supported identities. Separate diff -qr comparisons of sys/
  and files/ against generated/extracted/run1 both exit0, no differences.
  First comparison log generated/import-r523-files-comparison.log is empty;
  screenshot generated/ios-r523-real-import-success.png records native result.
- No GameData.import-* remains at support root. Installed GameData is 6.4 GiB;
  free space now 40 GiB. Deleted only the explicitly created 3.5 GB logical
  Documents selection copy (regenerable from preserved original); kept installed
  data for next runtime test. Terminated idle app and shut down sole Simulator.
- This proves real extraction/first activation, not imported-data boot, live
  replacement while running, save hash preservation, file-provider picker access,
  background import or power-loss durability. Full reference manifest including
  exporter-specific metadata was not compared; sys/ and game files were. Next
  launch with development module only and verify it uses installed private data.

## 2026-09-08 — R522 unactivated-stage discard ownership

- Previous turn progressed with transaction regression coverage and full repository
  pass. Current transaction could leave a verified stage behind after activation
  failure or cancellation following verification.
- Added explicit owned-unactivated-stage state and asynchronous discard. Refuses
  workers and activated stages, validates the exact UUID basename, refuses a
  symlink/replaced root, invalidates verification and retains exclusive ownership
  through utility-queue cleanup. Successful activation relinquishes ownership so
  the same API cannot delete old GameData left by an atomic exchange.
- Host activation failure now waits for discard completion before ending import;
  cleanup failure remains visible. No new automatic scanning/deletion of unknown
  or prior-install directories. Those recovery policies remain unfinished.
- Expanded standalone ASan/UBSan tests: worker-time discard rejected; activated
  stage discard rejected; separately verified then cancelled stage discarded;
  installed marker, source bytes and save sentinel retained. Tests pass, and
  simulator app build passes (`generated/build-ios-app-r522.log`). No Simulator
  launched or real game data removed. Synthetic fixture trees removed by tests.
- Real activation failure UI, cleanup failure, crashed-import recovery, retained
  prior-install cleanup and actual Wii extraction remain unverified. Full goal
  remains active; this is storage ownership work, not gameplay acceptance.

## 2026-09-08 — R521 transaction failure-path regression coverage

- Prior turn progressed with SunPad folder import and visible invalid-image
  rejection. Inspected current transaction and earlier negative-only tests.
- Added standalone test-only 32 MiB identity include, avoiding multi-gigabyte
  copyrighted fixtures without modifying the app's configured exact identity.
  Controlled extractor models failure after partial output and successful
  verification; this tests transaction ownership, not DiscIO correctness.
- ASan/UBSan/ARC/Wall/Wextra/Werror tests pass cancellation requested by copy
  progress (extractor never called), extraction failure cleanup, successful-stage
  runtime-stop rejection then activation, and repeated activation rejection.
  Every mode checks no leftover staging, unchanged save sentinel and every byte
  of the all-zero synthetic source. Copy may have finished by progress delivery;
  this does not establish a precise mid-copy cancellation latency bound.
- Added wrapper to aggregate check. Full repository suite exits0, including new
  tests and existing runtime/repository safety regressions (check-r521.log).
  All temporary fixture trees removed by tests; no user data or Simulator touched.
- Real extractor verification, disk-full/cleanup-failure handling, stale stage
  recovery, verified-stage discard and live activation remain open. Next address
  staged-data recovery/cleanup before repeated large imports, then real extraction
  and gameplay. Original PRD/goal loop remains active.

## 2026-09-08 — R520 SunPad folder import and visible rejection

- Previous turn progressed with picker cancellation and idle Stop fix. Read
  SunPad folder-selection flow and Files plist flags before adapting them.
- Enabled Import from GalaxyPad Folder. Enumerates top-level regular non-symlink
  WBFS files, sorts names, offers selection plus cancellation/disclosure, then
  dismisses before invoking the same private transaction. Errors/empty folders
  explain the expected Files location. beginImport rejects concurrent ownership.
  Documents sharing/open-in-place flags match reference; private runtime data
  remains in Application Support. Stored-data removal still disabled.
- Build passes (`generated/build-ios-app-r520.log`). Confirmed no booted device,
  then used only DE8E956F-6B29-4FF3-AF4A-77034CE8588A without game launch args.
  Added a tiny synthetic invalid WBFS to app Documents. Native menu→folder list
  displayed it; selecting it returned “Select the supported exact Galaxy image.”
  Menu reopened. File readback unchanged; support-directory listing had neither
  GameData nor GameData.import stages. Screenshot ios-r520-folder-rejection.png.
- Removed only the synthetic fixture after inspection, terminated idle app and
  shut down sole Simulator. No image import or save deletion. Empty/multiple-file
  cases, Files provider visibility/security scope, actual verification/extraction,
  mid-operation cancellation and activation/relaunch remain unverified. iPad was
  in portrait Simulator presentation for this rejection test; orientation gate
  remains open. Full PRD goal unchanged.

## 2026-09-08 — R519 picker cancellation and idle Stop regression

- Previous turn progressed with menu/transaction wiring and build. Confirmed zero
  booted Simulators, booted only DE8E956F-6B29-4FF3-AF4A-77034CE8588A, installed
  R518 and launched without development paths (no game runtime).
- SunPad cold-start menu opens, Game Data & Saves exposes Import/Reimport enabled
  while folder/removal stay disabled. Disclosure opens Files picker. Its visible
  close button cancels selection and menu opens again. Screenshot retained at
  generated/ios-r519-menu-after-picker-cancel.png. No file selected/copied.
- Explicit Simulator Landscape Left initially left app sideways; presenting Files
  produced upright landscape which persisted after cancellation. This is not a
  reliable cold-start orientation pass. No orientation-policy rewrite attempted.
- Source audit found idle Stop disabled interaction but no runtime existed to send
  onExit. Added !busy early return. App build passes (build-ios-app-r519.log);
  installed rebuilt app, confirmed Stop dialog → Game stopped → reopened menu.
- Terminated idle app and shut down the only Simulator. No runtime/save/image
  mutation or generated game-data cleanup. Real picker rejection, import success,
  cancellation during copy/extraction, verified activation and mobile gameplay
  remain untested. Files visibility/support-folder setup needs next attention for
  a synthetic picker fixture before any multi-gigabyte real import.

## 2026-09-08 — R518 SunPad import menu connection

- Prior turn progressed with transaction compilation and synthetic negative tests.
  Inspected current main/overlay and SunPad picker implementation. CoreHost sets
  busy false and calls onExit only after worker-owned runtime destruction.
- Connected existing SunPad Import/Reimport menu delegate to disclosure, document
  picker, cancellable transaction/progress and activation. Picker opens in place
  to avoid an extra system copy; transaction retains scoped access through copy.
  Import presence blocks gameplay even after cancel dismisses the progress alert.
  Verified completion disables Cancel, requests Stop if needed, and activation
  happens only after observed onExit/busy false. No stop-then-immediate-swap path.
- Menu now remains available before setup and after Stop. Only the implemented
  import item is enabled; folder import/removal remain disabled. Simulator startup
  prefers privately installed GameData while retaining the development module
  requirement. Device result explicitly says device module startup is unfinished.
- App build passes (`generated/build-ios-app-r518.log`) and check-ios-host passes.
  Device listing confirmed no booted Simulators; none launched, no game image
  copied or data deleted. UI presentation/dismissal, real extraction, mid-import
  cancellation, shutdown activation and relaunch still require runtime evidence.
  Recovery-stage cleanup/verified-stage discard and full supported import formats
  remain incomplete. Next run one-iPad UI rejection/cancel flow before real import.

## 2026-09-08 — R517 private import transaction owner

- Previous turn made progress: extractor cancellation and successful app build.
  Current checkout preserves moved root docs and existing untracked work.
- Added GalaxyPadImportTransaction to mobile target. A single-use instance owns
  UUID sibling staging, security-scoped source access during a bounded 64 KiB
  copy, exact-size rejection and private copied-image extractor verification.
  Source image is read only. Success alone enables activation, additionally gated
  by caller-confirmed runtime stopped. Successful activation cannot repeat;
  prior GameData is retained by the atomic helper and saves remain separate.
- Cancellation polls during copy; failed owned stages are removed only after
  worker completion, on a utility queue so large cleanup does not block UIKit.
  Cleanup failure is reported. Caller must serialize imports and retain owner.
- App build passes (`generated/build-ios-app-r517.log`). New Foundation test
  compiled with ARC/C++23/ASan/UBSan/Wall/Wextra/Werror and generated identity
  include; wrong-size, pre-cancel, duplicate preparation, premature activation,
  unchanged source and no-stage-on-rejection pass. Stub extractor aborts if these
  negative paths accidentally reach it. Tiny isolated test tree removed.
  Existing atomic activation/rollback/save sentinel sanitizer test also passes.
- No real image copy, Simulator launch or game-data deletion. Tests do not prove
  successful extraction, mid-copy cancellation, disk-full or cleanup-failure
  behavior. Menu wiring, confirmed host-exit activation, verified-stage discard,
  retained prior-stage cleanup and real import acceptance remain next. Full PRD
  and goal loop remain open; SunPad UI remains the required implementation.

## 2026-09-08 — R516 extractor cancellation boundary

- Reaffirmed user requirement: actual SunPad menu and editable touch controls,
  not a replacement design. Existing direct adaptation remains the UI baseline.
- Added optional thread-safe cancellation callback to the checked extractor.
  Hashing polls per 64 KiB buffer; recursive exports poll between files. Current
  DiscIO file export is not interruptible. Final main-queue completion checks
  cancellation before reporting success. Existing API remains compatible.
- Simulator app build passes (`generated/build-ios-app-r516.log`). Direct script
  invocation lacked execute permission; invoked the same script with bash.
  No Simulator or game launched, no image copied or data deleted.
- This is preparation, not a finished import transaction: private stage copy,
  cancellation behavior tests, verified ownership, activation after confirmed
  runtime exit, cleanup and import UI integration remain next. Full goal open.

## 2026-09-08 — R515 atomic staged activation and rollback tests

- Prior turn progressed with checked Wii extractor compilation; no real import
  yet. Implemented SunPad sibling-stage activation boundary with Apple's public
  renamex_np EXCL/SWAP (installed SDK declares iOS10+ availability).
- Helper accepts only an absolute owned root and exact UUID stage basename;
  rejects active-runtime requests, missing/non-directory/symlink stages and
  non-directory active targets. First install cannot overwrite an existing target;
  replacement atomically exchanges directories, retaining old GameData at stage.
  Exchanging again restores prior data. Saves/NAND outside GameData untouched.
- Actual filesystem ASan/UBSan tests pass initial activation, replacement,
  restoration, runtime gate, malformed names, missing/symlink stage and save
  sentinel preservation. iOS16 Simulator syntax passes. Test's tiny mkdtemp tree
  removed after success; no user/game data deleted. Added script to repo check.
- This is not yet wired to verified-stage ownership or stopped-runtime completion
  in the app. No power-loss/durability, live import, extraction-manifest or disk-full
  acceptance claimed. Next implement preparation/verification/cancellation owner
  and call activation only after verified stage plus observed runtime exit.
  No Simulator/game launch; original full goal remains active.

## 2026-09-08 — R514 checked Wii extractor from SunPad staging mechanism

- Prior turn progressed with mapping UI/store/reset and repository regression
  proof. Read pinned SunPadDiscExtractor completely plus import staging/activation
  flow and current Dolphin extraction/file/volume APIs. Reference ExportDirectory
  returns void and only logs individual failures: unsuitable as success proof.
- Added GalaxyPadDiscExtractor compiled into app, not yet called by import UI.
  Reads exact WBFS image size/hash and DOL hash from CMake-generated header sourced
  from config/galaxypad-disc.json. Verifies image before creation, Wii RMGE01 rev0
  and valid data partition; rejects existing destination; checks system export,
  DOL hash, each recursive file export and size, entry path components/depth.
  Progress is bounded by batches and does not disclose extracted filenames.
- Uses fresh staging only; failure leaves owned partial stage for the future
  import transaction. No activation/removal or save changes implemented here.
  Alternate container representations are deliberately not claimed supported.
- App build passes generated/build-ios-app-r514.log.20 upstream unused-parameter
  header warnings; no extractor warning observed. No extraction/runtime tests yet,
  no image copy, no new game-data artifacts.46Gi available before actual extraction.
- Next implement transaction ownership/security-scoped copy/cancellation, verify
  extraction manifest/negative cases, await stopped runtime before activation,
  preserve prior data and saves on failure, then wire SunPad import/reimport UI.
  Full goal active; Simulator stayed off.

## 2026-09-08 — R513 mapping swap/reset UI and repository regression pass

- Prior turn progressed with configurable mapping integration. Full repository
  check25717 exits0, generated/check-r513.log (includes controller sanitizer test;
  separate Foundation settings/diagnostic tests are not part of this aggregate).
- Sole iPad DE8E956F-6B29-4FF3-AF4A-77034CE8588A installed executable
  eb8a65ad341fff28824beffe74550b15eb86343151f6b1b64877da474668e339;
  run28556 console81735. Explicit landscape, Menu → Controls → Mapping → A → X.
  UI returns A—X and Spin—A; on-disk mapping array [2,1,0,3,4] verified.
- Reset returns defaults A—A/Spin—X and removes mapping preference. Done resumes
  runtime. No physical controller was tested; this is UI/store evidence only,
  not across-relaunch persistence, device callbacks, held-input or gameplay proof.
- Native Stop produces shutdown log, runtime exit failed=0 and visible Game
  stopped. Shell terminated and sole Simulator shutdown requested. Runtime log
  generated/ios-runtime-r513.log. No source edits, save changes or publication.
- Next finish required input validation/first-play and remaining menu adapters;
  no completion claim from title/menus/tests. Queued resize/held-touch and report
  share-cancel checks remain explicitly unverified.

## 2026-09-08 — R512 configurable controller mapping uses SunPad assignment flow

- Prior turn progressed with Apple controller adapter integration. Read complete
  pinned SunPadControllerMapping.mm and reference assignment/choice UI methods.
- Added pure Galaxy bijective mapping and versioned Foundation persistence:
  A/B/Spin/C/Z map to A/B/X/Y/Left Trigger. Selecting an occupied physical input
  swaps assignments, preserving action reachability; malformed stores fall back
  to defaults. Nonfinite/out-of-range persisted values rejected.
- Controller input consumes the mapping; change/reset reloads policy, clears host
  input and requires neutral release. Reference menu mapping action now enabled,
  using SunPad's action-list/physical-choice alerts adapted to Galaxy labels and
  fixed pointer/tilt/secondary mappings. No FLUDD or GameCube actions imported.
- Controller sanitizer tests pass all120 permutations and all25 reassignments
  each, invalid fallback, and neutral gating after remap. App build passes;
  final finite-value guard rebuild requested (build-ios-app-r512-final.log).
- UI assignment/reset/persistence/relaunch and actual Apple-controller gameplay
  are not yet tested. No Simulator booted. Next single-iPad test these mappings
  and controller state, while preserving queued resize/held-touch and full goals.

## 2026-09-08 — R511 Apple controller adapter wired to Wii input

- Prior turn progressed with SunPad ownership/Galaxy policy tests. Added
  GalaxyPadControllers main-thread adapter using reference authoritative
  GCController enumeration, stable P1 slots, weak callbacks, stale-device checks
  and handler detachment. Button callbacks publish complete snapshots immediately;
  60Hz timer integrates pointer with bounded elapsed time, not event-count speed.
- Every callback/tick rejects a no-longer-enumerated owner. Periodic reconciliation
  also recovers missed connection notifications. Ownership changes clear shared
  input and refresh reference touch visibility. Main host onInputReset now resets
  controller policy AND touch; active/menu/paused gates enforce neutral release.
- App build and controller ASan/UBSan tests pass; final stale-tick guard rebuild
  requested in build-ios-app-r511-final.log. Added controller test to repository
  check script. No physical/virtual Apple controller or runtime acceptance yet.
- Mapping UI remains disabled until configurable mapping is delivered; no claim
  that connecting this fixed default completes PRD controller/settings/haptics.
  Next test actual enumeration/connect/disconnect and implement reference mapping
  UI with Galaxy actions. No Simulator booted; original full goal unchanged.

## 2026-09-08 — R510 SunPad controller ownership and Galaxy input policy

- Prior turn progressed by runtime-verifying local report preview/redaction/
  dismissal. Controller input remained absent. Read pinned SunPadControllerSlots
  and controller enumeration/callback/reconciliation implementation; also inspected
  mapping interface to exclude FLUDD/GameCube assumptions.
- Added GalaxyPadControllerSlots direct reference adaptation with one active Wii
  player. Stable P1 cannot be stolen by enumeration reorder; queued controller
  can take over only after owner disappears. Apple enumeration integration next.
- Added pure GalaxyPadControllerInput complete-snapshot policy: A/B, X=Spin,
  Y/RB=C, LT=Z, RT=B, Menu=Plus, Options=Minus, D-pad; left movement stick, right
  pointer with deadzone/bounded timestep, right-stick click recenter, hold LB
  for right-stick ball/ray tilt. Reset hides pointer and requires neutral buttons
  AND axes before reactivation. These defaults still require usability testing.
- ASan/UBSan tests/test-mobile-controller.cpp passes ownership/reorder/handoff,
  simultaneous actions, reset held-input blocking, pointer, tilt/release/recenter,
  NaN/timestep bounds. No UIKit/controller hardware or gameplay claim.
- Not wired to GCController or menu yet. Next adapt reference main-thread
  callbacks/stale-device checks and runtime lifecycle reset, expose clear default
  mappings/settings, then test connect/disconnect and actual game interactions.
  No Simulator booted or user data modified. Full original goal remains active.

## 2026-09-08 — R509 local report generation/preview and resume verified

- Prior turn progressed with menu report integration and filesystem privacy tests.
  Installed executablefc4485f73a01adc098d902c21091d926915ff37c38408f8a48329d0548d16ed4;
  sole iPad/run26716 console22595. Explicit landscape orientation settles upright.
- Opened reference Report a Problem, entered synthetic token/path, generated
  actual local819-byte report. Preview visibly replaces both with redacted markers;
  file check finds neither SYNTHETIC509/private.wbfs nor user-home/container prefixes or
  github.com. These sentinel checks are scoped, not universal privacy acceptance.
- Coordinate Share/Done attempts not reliably effective; native bar items missing
  from AX tree. Accessible dismiss-popup action closes preview; runtime resumes
  to visible title, log native UI blocked=0/input cleared. Share-sheet cancellation
  NOT verified and no destination selected/upload performed.
- Added stable preview Close/Share labels+IDs for follow-up. Reworded curated
  missing-diagnostics text with commas: slash-separated names were over-redacted
  as paths. Rebuild requested after test shutdown; new UI IDs untested.
- Native Stop: shutdown log, runtime exit failed=0, visible Game stopped.
  Shell terminated and sole Simulator shutdown requested. Evidence runtime-r509
  log plus local report in Simulator Documents/Diagnostics. Full diagnostic fields,
  touch-resize/held-input, first-play and other menu adapters remain incomplete.

## 2026-09-08 — R508 guided local report wired into reference menu

- Prior turn progressed with SunPad diagnostics adaptation/redaction tests.
  CMake now links diagnostics and a local report preview controller. Reference
  Report a Problem is enabled after host delegate installation; other unfinished
  adapters stay disabled. SunPad three-question prompt generates a local report,
  then readonly preview/Done/user-initiated native Share. No public URL/upload.
- Context is curated busy/paused/frame count/selected-next-launch scale and
  Classic Pointer; missing detailed identities/audio/dispatch explicitly labeled.
  Session and native-UI/exit breadcrumbs use owned low-frequency messages only.
  No raw runtime callback messages, disc/saves/screenshots/inputs included.
- App build passes generated/build-ios-app-r508.log. Extended diagnostics tests
  redirect filesystem roots only in the test translation unit to mktemp path:
  real report generated, raw-runtime/path/credential sentinels absent, safe context
  present; forced1MiB log boundary rotates correctly. Pure redaction tests pass.
- UI prompt → preview/dismissal/share-cancel and lifecycle pause have NOT yet
  been exercised in this build. Full PRD diagnostic field coverage still missing;
  these bounded checks do not establish universal credential filtering/privacy.
  No Simulator booted, no report shared. Next install once and verify preview
  flow plus pending reference resize/input checks; retain full original goal.

## 2026-09-08 — R507 SunPad diagnostics adaptation and privacy tests

- Prior turn progressed with stick ownership fix and editor event instrumentation.
  Runtime checks remain queued. Moved required Report a Problem backend forward:
  read complete pinned SunPadDiagnostics.mm and PRD privacy boundary.
- Added Galaxy-owned diagnostics h/mm with reference provenance. Preserves
  local session/event/report structure; removes SunPad public issue destination.
  Reference header promised 1MiB rotation but implementation lacked enforcement:
  added size-triggered rotation, bounded session reads and low-frequency line
  limits. File-handle errors cannot intentionally propagate through log writes.
- Redaction covers non-container absolute paths, URLs and credential-like fields.
  Runtime-event payload text is omitted entirely; severity/category are bounded
  allowlists. No raw guest messages should be copied into export. Reporter text
  and technical context still require controlled callers and review before sharing.
- tests/test-mobile-diagnostics.mm runs pure formatting/redaction helpers without
  writing user logs. Host compile/tests pass; initial Simulator syntax passes.
  Not in CMake or menu yet. Rotation/disk failure/report file and final export
  privacy acceptance need end-to-end tests; regex tests are not full privacy proof.
- No Simulator booted or data shared. Next integrate local report generation and
  preview into the reference menu, with curated host context and no public upload.

## 2026-09-08 — R506 stick contact ownership after native input reset

- Previous turn progressed with actual reference editor selection/FPS/Stop
  evidence, but resize remained unproven. Inspected current reference-derived
  stick: touchesMoved unconditionally called handleTouch after reset, allowing
  a pre-menu/interruption contact to reassert movement when UI reopened gameplay.
- Added retained UITouch ownership to the existing SunPad-derived stick; reset
  invalidates ownership, moved/ended/cancelled act only on the owned contact,
  and a fresh touchesBegan is required to resume input. Hiding touch controls
  also explicitly clears input on the visibility transition. Appearance unchanged.
- Added editor-only selectedSizeChanged log with control ID/value/tracking, no
  gameplay coordinates, to separate actual value-change delivery from CUA's
  displayed AX value. Do not infer slider persistence from AX value alone.
- App build passes (generated/build-ios-app-r506.log), input mixer and stick-mode
  routing sanitizer tests pass. These do NOT exercise UIKit held-touch events;
  actual reset/re-touch and resize remain runtime gates. No Simulator booted.
- Next install this new build in the sole landscape iPad; test slider event log,
  actual size/persistence and held-contact clearing. Full actions/first-play and
  performance acceptance remain open. Diagnostics reference header located for
  later full report port, implementation not yet read or adapted.

## 2026-09-08 — R505 reference editor selection and FPS runtime smoke test

- Previous turn progressed by proving explicit landscape shell presentation.
  Same installed executable2b23576cedf463cba8ebf03b2e5571110678ee18e135c53c3acd719f2c3d4783,
  sole iPad/runtime25174 console18768. Stale CUA native-menu IDs recovered through
  connection reset and exposed Cancel action; runtime was not restarted.
- Explicit Device → Orientation → Landscape Left settles to upright game/title
  and reference controls. ios-title-reference-r505.png and ios-runtime-r505.log.
  FPS toggle works: Paused during editor, presented-rate label after Done
  (24.0 transitional, later36.4). These snapshots are NOT sustained benchmarks.
- Native menu → touch settings → edit; coordinate tap A selects cyan outline,
  A size label and enabled slider. Drag slider did not change value. AX setValue
  1.2 changes slider text but NOT actual A geometry/persisted size (plist A=1).
  Therefore resizing/persistence NOT accepted. Do not add another substitute
  editor; next distinguish CUA drag delivery from UISlider value-change handling.
- Done resumes; A AX press invoked. Simultaneous A+B/title-pass, held-input clear,
  pointer mechanics and full mobile first-play remain unproven. FPS restored off.
  Native Stop produces shutdown log plus visible Game stopped; shell terminated
  and sole Simulator shutdown requested. No source changes or gameplay claims.

## 2026-09-08 — R504 explicit Simulator orientation resolves shell presentation

- Previous turn progressed: reference UI integrated/build/visible transitions and
  Stop verified. Compared full pinned SunPad AppDelegate, view orientation methods
  and plist with Galaxy. Same landscape/full-screen declarations; SunPad uses
  legacy window ownership whereas Galaxy has scene lifecycle. No policy rewrite.
- Read Apple TN3192 current markdown and installed SDK geometry declarations.
  Added window/screen/native/device/effective-geometry diagnostics. Build passes
  generated/build-ios-app-r504.log. Sole same iPad booted, installed, shell-only
  launch24724/console12688 with NO game paths/runtime.
- Log generated/ios-geometry-r504.log proves window, scene and screen1210x834,
  effective landscapeRight, native1668x2420. CUA Device → Orientation → Landscape
  Left explicitly selected (not repeated Rotate toolbar). Settled screenshot
  now landscape with upright status/text and centered full-window shell:
  generated/ios-geometry-landscape-r504.png. No orientation mask/plist change.
- This resolves the observed shell presentation in this Simulator state, not
  physical device/resizing/gameplay acceptance or a proven automatic-rotation
  root cause. Next use explicit device orientation for actual overlay/gameplay
  drag/resize/input checks; do not rewrite host based on old portrait captures.
  Shell terminated and sole Simulator shutdown requested after capture.

## 2026-09-08 — R503 reference UI integrated and visibly exercised

- Prior turn progressed by porting the reference overlay. CMake/main now use
  GalaxyPadGameOverlay + shared settings instead of custom table/overlay/stick.
  Prototype files retained but no longer compiled into the app.
- Added documented UIControl context-menu callbacks (calling super), native UI
  state reconciliation, prompt pause/input-clear, editor/settings transitions,
  and confirmed Stop. Host snapshots persisted render scale on launch; UI labels
  specify next launch. FPS preference drives a presented-frame-rate label.
- Build passes: generated/build-ios-app-r503.log; app executable SHA256
  9ccbccdf31a7f021161a30dab530d3b2661ad6afc620e72d7d4d768f2fc7b767.
  Sole iPad DE8E956F-6B29-4FF3-AF4A-77034CE8588A, process24034/console96927:
  visibly opened native Menu → Controls → Touch Control Settings → Move controls,
  observed reference grouped D-pad/editor bar, then Done. No drag/resize proof.
  Opened Stop confirmation and confirmed: shutdown log plus visible Game stopped.
  Shell termination and Simulator shutdown requested after runtime stopped.
- Evidence: generated/ios-runtime-r503.log and ios-reference-overlay-r503.png.
  This run exercised UI during startup, not first-play or performance. Existing
  landscape scene centered within portrait Simulator remains visibly defective.
  FPS/scale UI changes require runtime tests; no active-input pause/soak proof.
- Import/remove, controller mapping, diagnostics and aspect adapters are not
  integrated: corresponding actions disabled, not claimed working. Empty
  experiments group is omitted by UIKit; full hierarchy still unfinished.
  Next fix scene presentation using SunPad host reference, validate touch/editor
  interactions against visible gameplay, then complete the required host actions.

## 2026-09-08 — R502 actual reference overlay adaptation

- Previous turn progressed by adding/test-compiling SunPad-derived settings.
  Read remaining reference layout/settings/editor source through EOF this turn.
- Added apple/ios/GalaxyPadGameOverlay.h/.mm directly from the pinned SunPad
  overlay with provenance. Retains native primary-action UIMenu/circular ellipsis,
  reference button/stick styling, responsive placement, grouped D-pad, settings
  panel, drag/select/size editor bar, persistence/reset and controller visibility.
- Adapted input to existing normalized Wii state: A/B/C/Z/Spin/Plus/Minus/1 and
  D-pad; left movement/right tilt stick, existing viewport absolute-pointer
  contract. Removed FLUDD pressure trigger and Sunshine CPU/30-to-60FPS patches.
  Corrected positive-up stick thumb on relayout. Reports delegate to the host
  instead of opening SunPad's public issue tracker.
- Strict iOS16 arm64 Simulator syntax check passes. This source is NOT yet in
  CMake/main: no visible UI or runtime acceptance. Next wire native-menu lifetime
  to host pause/input clearing (including dismissal), preserve confirmed Stop,
  connect settings/display/FPS and required data/controller/report actions, then
  replace prototype. Empty experiments section is unfinished, not a feature.
  Secondary 2 button, tilt options and Direct Touch remain to be completed.
- No Simulator or game launched; no macOS/module/save changes or publication.

## 2026-09-08 — R501 direct SunPad settings port

- Previous turn was no implementation progress: correction acknowledged and
  reference inspected only. Revalidated read-only SunPad pin fcdc1411e483a86ca80ec82e7cd53839c51ff865.
- Added Galaxy-owned shared/GalaxyPadSettings.h/.mm directly from SunPad's
  settings implementation with SPDX/source provenance. Preserved reference
  opacity/global size/per-control size ranges, editor and controller-visibility
  preferences, data paths and persistence. Removed Sunshine timing experiments
  and GameCube C-stick reversal. Galaxy aspect default/fallback is native 16:9.
- iOS16 arm64 Simulator ObjC++23 syntax check with warnings-as-errors passes.
  tests/test-mobile-settings.mm compiles/runs against Foundation: getter/range
  checks pass using volatile argument preferences, without writing user data.
  Setter/relaunch persistence and UI integration are not yet tested.
- Settings are deliberately not wired to the prototype. Actual SunPad overlay
  read through line1020; remaining layout/editor code must be read before port.
  Next adapt full reference overlay to existing Wii input/host boundaries, then
  wire settings and replace the custom sheet. No UI parity or gameplay claim.
- No Simulator booted; no game launched or artifacts published. Full goal active.

## 2026-09-08 — R500 user correction: actual SunPad UI, not substitutes

- Prior turn progress: stable AX IDs and startup-stop retry. Tested app
  bb2d06b976c010e1ae86df57af6f1017f53c49d9bcda8bff6fbc7282000d8c66,
  sole iPad DE8E956F-6B29-4FF3-AF4A-77034CE8588A, app22364/console63324.
  Tilt/invert switches now respond; recenter action invoked, Tilt label visible,
  saved mode/invert true, gain1. Source routing test8653 passes. No mechanic/input
  axis or recenter-with-held-contact acceptance. Restored mode/invert off.
- User explicitly reiterated use of SunPad's actual menu and touch controls.
  Current custom table sheet and incremental overlay diverge from that request.
  Stop extending substitutes; next directly port pinned SunPadGameOverlay and
  its settings/editor machinery with Galaxy-only adaptations. Recorded binding
  direction in MOBILE-IMPLEMENTATION.md; no prototype UI parity claim.
- Confirmed native Stop reaches Game stopped; log generated/ios-runtime-r500.log,
  screenshot generated/ios-tilt-r500.png. No shutdown grace-expired line: retry
  path is still unexercised. Shell then terminated, console63324 exit0, sole
  Simulator shut down. Preserve tested host/input mechanisms, private module/
  image/save boundary, and original full goal. Next read complete reference UI
  implementation before the direct adaptation; no new custom-control increments.

## 2026-09-08 — R499 tilt accessibility / early-startup Stop boundary

- Prior turn progress: tilt mode implementation/tests. Full R498 regression3759
  completes0 in generated/check-r498.log. Sole iPad boot71246 exit0,
  app21388/console87264, SHA1a6b559ee07471931e672f15ad7508c5f6716b1511f9a1e76ccd2a020a05eae5.
  Menu exposes tilt/sensitivity/inversion/recenter. CUA toggle fails with duplicate
  AX criteria, not a guest input finding. Added stable IDs to tilt/invert/gain/
  recenter; build9669 passes, generated/ios-tilt-ax-r499.log. Retest pending.
- Confirmed Stop during early paused startup does NOT finish: inputSamples0 at
  request03:32:15, then guest continues booting (03:32:38 title services) with
  CPU~106%. Unlike R493 ready-title stop, early STM event is not completing the
  shutdown contract. Log generated/ios-runtime-r499.log. Stuck test terminated;
  console87264 exit0 is external termination, not clean runtime teardown.
- Source Platform::UpdateRunningFlag already defines second request as leaving
  MainLoop for normal Core::Stop/Shutdown. Added single10s retry in shared
  RequestSessionStop helper (explicit Stop/dealloc), guarded session/runtime
  lifetime and stopping flag. Retains resume-before-STM and cleared input;
  no process kill/guest save injection. Host syntax/app build79455 pass in
  generated/ios-startup-stop-r499.log. Runtime retry test still mandatory.
- Sole Simulator shut down. Next test early-startup Stop with corrected build,
  then tilt UI using stable IDs. Full tilt mechanics, advanced controls/menu,
  gameplay and performance remain open. macOS untouched.

## 2026-09-08 — R498 selectable non-motion tilt-stick foundation

- Prior turn progress: size/relaunch/reset interaction proved, full R496 suite
  passed. Added manual Use Stick for Tilt, sensitivity0.25–2, Y inversion and
  recenter controls to native menu. Settings persist; host input clears before
  changes. Stick visibly labels Move/Tilt and accessibility label tracks mode.
  No automatic mechanic detection or motion permission required.
- Shared routeStick clears movement AND tilt channels before routing selected
  mode, clamps finite axes/gain, preserves unrelated buttons/pointer. Dedicated
  ASan/UBSan test covers switch/release, sensitivity/inversion and invalid input;
  added to repository suite. Routing and mixer tests plus app build55743 pass,
  generated/ios-tilt-r498.log; host syntax2380 passes. No Simulator launched.
- Runtime menu interaction and actual Wii tilt/ball/ray mechanics unverified;
  source config already maps normalized tilt axes but that is not gameplay proof.
  Full regression started in generated/check-r498.log, session3759; preserve that handle.
- Next one-iPad menu mode/label/recenter validation, then advanced D-pad/1/2/Home
  controls and remaining native menu. Raw drag/pinch, simultaneous A+B/file select,
  full gameplay and mobile performance remain open. macOS unchanged.

## 2026-09-08 — R497 size / relaunch / reset interaction passes

- Prior turn progress: sizing implemented/tested. Full regression97470 finishes0
  in generated/check-r496.log. Sole iPad DE8E956F-6B29-4FF3-AF4A-77034CE8588A
  boot98842 exit0, app19632/console7564. App SHA
  604971f02fd8be2d1d14b923e5b5496ed80ed5568327150cf4794c44248951d0.
- CUA verifies Reset Layout now exposed as button. Editor A Make Larger three
  times visibly grows A; defaults size1.331, normalized center preserved.
  Screenshot generated/ios-size-r497.png. Done then native confirmed Stop reaches
  Game stopped; shell terminated, console7564 exit0 before any replacement.
- Same app relaunched without install as app19868/console12728. Larger A visibly
  persists. Reset Layout confirmation clicked: positions and sizes keys removed,
  opacity1 and touch-enabledtrue retained. Done shows default A size and runtime
  advances to Wii screen. Confirmed Stop again reaches Game stopped with final
  input0 and smc_failed0. Logs generated/ios-runtime-r497.log and
  generated/ios-runtime-r497-relaunch.log. Shell then terminated, console12728
  exit0, sole Simulator shut down. No product source change this turn.
- This proves accessible sizing, size persistence/relaunch, reset and native
  exit, NOT raw pinch/drag, simultaneous A+B/gameplay or performance. Next
  advanced Galaxy control surface and tilt-stick, then remaining native menu
  sections; preserve input-delivery issue as a separate unverified test boundary.

## 2026-09-08 — R496 per-control size persistence and bounds

- Prior turn progress: accessible movement proved shared position path; raw drag
  still never reaches recognizer. Added edit-only pinch resizing and accessible
  Make Smaller/Make Larger actions using common finite scale clamp0.7–1.8.
  Positions remain centered/clamped, button minimum44pt/stick84pt where viewport
  permits, oversized controls capped to layout area. Saved sizes are distinct
  from normalized positions; reset removes both after existing confirmation.
- Added shared GalaxyPadControlSize.h and sanitizer test with73,200 bounded
  scale/viewport combinations plus invalid/edge values. Added test to repository
  suite. Size test, input mixer and app build12110 pass in
  generated/ios-size-r496.log; host syntax62176 passes. No Simulator launched.
- Menu selectable rows explicitly expose button accessibility labels/traits,
  addressing the missing Reset Layout AX row for next runtime check. Runtime
  pinch, accessible sizing, persistence on relaunch and confirmed reset still
  unverified; no editor acceptance claim. Full regression started in
  generated/check-r496.log, session97470; preserve that handle for next turn.
- Next single-iPad sizing/reset interaction and saved-layout relaunch, then
  advanced Galaxy controls/full menu. Drag-delivery defect remains separately
  open. No macOS changes; full original PRD goal active.

## 2026-09-08 — R495 accessible movement proves positioning/persistence path

- Prior turn progress: editor implemented, drag delivery unresolved. Added
  shared remember/persist helpers and edit-only VoiceOver directional actions,
  removed on Done. Weak captures avoid cycles. Drag diagnostic logs only gesture
  begin/end/control ID, no coordinate stream. Build67279 and mixer tests pass.
- Sole iPad DE8E956F-6B29-4FF3-AF4A-77034CE8588A boot1844 exit0, app17836,
  console67799, SHA2a5591487549259adb81638c0af848224c1a1d2e59ede279cdbe1028a9a56660.
  CUA sees all four directional actions; Move Up then three Move Left actions
  visibly move A. Preferences store A normalized position
  {0.90201005025125625,0.90267983074753178}. Screenshot
  generated/ios-editor-move-r495.png, log generated/ios-runtime-r495.log.
- One instrumented drag produces NO begin/end log and no visual movement.
  Positioning/persistence works through accessibility; recognizer delivery and
  coordinate targeting remain unresolved. Do not repeat unchanged drags or call
  drag/relaunch/resize acceptance passed. Restored A via inverse directional
  actions, exited editor; custom actions disappear in gameplay as intended.
- Confirmed Stop again reaches Game stopped; button/pointer samples zero during
  this editor-only run, finalbuttons0. App then terminated, console67799 exit0,
  sole Simulator shut down. macOS unchanged. Next complete control size/advanced
  actions and reset accessibility; preserve drag issue for meaningful input
  delivery test rather than rewriting shared position code. Full PRD remains open.

## 2026-09-08 — R494 first layout editor; drag delivery unresolved

- Prior turn progress: paused-menu Stop fixed and verified. Read SunPad normalized
  position/clamp/reset/editor code. Added Galaxy-owned per-control pan gestures,
  normalized safe-area positions persisted to defaults, top menu-row exclusion,
  edit highlighting/Done and confirmed position reset. Editor keeps host paused,
  suppresses touch/pointer publication and clears state on entry/exit. Individual
  sizes, grouped advanced controls and full editor remain unfinished.
- App build15537 and mixer sanitizers pass; generated/ios-editor-r494.log.
  Tested SHA c5772e202c72727f5472bd4fcc4b5fb0f25dd594482f9a0c7ed392adc8956614
  on sole iPad DE8E956F-6B29-4FF3-AF4A-77034CE8588A, boot13386 exit0,
  app16944/console42041, log generated/ios-runtime-r494.log.
- CUA Edit Layout opens highlighted controls/Done and Done restores Menu.
  Two bounded drags (screenshot coordinates, then toolbar-offset hypothesis)
  do not move controls; defaults contain no positions. Do NOT claim editor
  movement/persistence works. Next instrument recognizer delivery versus native
  coordinate targeting before repeating. Opacity AX setValue reports invalid ID;
  slider appearance shifts but persisted value remains1, so opacity still unproven.
- Confirmed native Stop after editor exit reaches Game stopped, runtime shutdown
  counters emitted, finalbuttons0, buttonSamples0/pointerSamples0. Screenshot
  generated/ios-editor-done-r494.png. App then terminated, console42041 exit0,
  sole Simulator shut down. No macOS changes. Full PRD goal remains active.

## 2026-09-08 — R493 paused-menu shutdown regression passes

- Prior turn progress: native menu and causal paused-stop correction. Revalidated
  corrected build87926 success, app SHA0f6d3a60adaa338f0a4bcea290c089faddcd1fd3974de130968707d03bf321ea.
  Sole iPad DE8E956F-6B29-4FF3-AF4A-77034CE8588A boot97147 exits0;
  app16036/console17520. Inspected real title before menu/Stop/confirmation.
- Result: request02:58:16.600, guest shutdown counters emitted and teardown
  proceeds by02:58:18.925. CUA explicitly verifies Game stopped in same app.
  Log generated/ios-runtime-r493.log; screenshot generated/ios-menu-stop-r493.png.
  Native587408685/fallback14493238/hook_fb30662084/smc_failed0; input final0.
  This closes the reproduced paused-menu Stop hang, not lifecycle/save/soak or
  performance acceptance. Shell subsequently terminated, console17520 exit0;
  Simulator shutdown99055 completes0. No game/Simulator remains.
- Applied same clear-input/resume-before-STM-shutdown safeguard in host dealloc.
  Host syntax, mixer sanitizers and app rebuild99093 pass in
  generated/ios-teardown-r493.log. Deallocation while paused still requires a
  dedicated runtime test; explicit Stop evidence belongs to preceding hash.
- Next full editable controls/advanced Galaxy actions and menu progression,
  with actual simultaneous A+B/file-select pointer route still required. Opacity
  interaction, physical-controller ownership, fullscreen/resizing behavior and
  original PRD menu sections remain open. macOS unchanged; full goal active.

## 2026-09-08 — R492 first native menu / paused-stop defect isolated

- Prior turn progress: viewport pointer transport. Full R491 regression57927
  completes0 (generated/check-r491.log). Added native three-dot menu, Controls
  switch/opacity persistence and confirmed Stop action; clear input/pause on open,
  clear/restore on Done or system dismissal. Reference SunPad overlay hierarchy
  inspected at fcdc1411; no Sunshine timing/aspect actions copied. Full PRD menu
  is not complete. Also honor failed host start before exposing controls/timer.
- Build67449 and mixer46091 pass. App SHA bf68f091801b6e5b3c5700b528adb7190bee48273d5c858adc1c795866227a38
  tested on sole iPad DE8E956F-6B29-4FF3-AF4A-77034CE8588A, app15094/console33711.
  CUA opens menu during startup, toggles touch off, Done returns to Wii screen
  with controls hidden, reopens/toggles on, cancels Stop, system-dismisses back
  to title/controls. Opacity drag did not change AX value; interaction unproven.
  Screenshots generated/ios-menu-r492.png and ios-menu-dismiss-r492.png; runtime
  log generated/ios-runtime-r492.log. No full pause/audio/stuck-input acceptance.
- Confirmed Stop hangs from paused menu. Sample72053 completes0 in
  generated/ios-stop-menu-r492.sample.txt: worker remains PlatformIOS::MainLoop,
  CPU waiting. Platform::UpdateRunningFlag sends STM power-button event on first
  request, but paused CPU cannot process it. Host stop now resumes paused CPU
  before RequestStop, with stopping flag rejecting input. This is a source-backed
  correction pending runtime retest, not a shutdown pass.
- Stuck app terminated, console33711 exit0 is termination NOT clean runtime
  shutdown, sole Simulator shut down. Corrected build87926 in
  generated/ios-menu-stop-r492.log. Next revalidate build result then repeat
  menu-paused Stop on one iPad; cover deallocation while paused as well. Full
  menu/editor, simultaneous A+B/file select and performance remain open.

## 2026-09-08 — R491 viewport-backed Classic Pointer transport

- Added renderer-thread after-present viewport snapshot with a separate mutex;
  UIKit maps touches into the actual target rectangle, not the entire surface.
  Controls win hit testing; black-bar touches do not acquire the pointer.
  Classic Pointer retains aim after release for separate A/B actions; cancelled
  contacts and host reset clear pointer ownership. Direct Touch remains separate.
- First compile exposed renderer headers requiring std::to_underlying. Aligned
  native host/check script with pinned Dolphin C++23 (no runtime source change).
  Host syntax and full app link8080 pass; generated/ios-pointer-r491.log records
  arm64 IOSSIMULATOR/min16/SDK26.5. Mixer sanitizer71917 passes.
- Sole iPad DE8E956F-6B29-4FF3-AF4A-77034CE8588A boot9000 passed. App13466,
  console47076, generated/ios-runtime-r491.log. CUA inspected Wii screen then
  title, clicked A, touched viewport and dragged within it. Input summary:
  samples13056, buttonSamples9, transitions2, last0, pointerSamples6434.
  This proves pointer delivery, NOT visible targeting/depth, simultaneous A+B,
  file-select/gameplay, or mobile performance. Native Stop reached Game stopped;
  shell terminated, console exit0, sole Simulator shut down.
- Full regression session57927 running in generated/check-r491.log. Next poll
  that handle, then reference-derived native menu/lifecycle integration and an
  actual simultaneous A+B/file-select pointer test. Full editable controls,
  device/controller work and original PRD acceptance remain open. macOS untouched.

## 2026-09-08 — R490 touch input reaches emulated Wii samples

- Prior turn progress: visible overlay/contrast and runtime input summary.
  Sole iPad boot24444 exits0; console76486 launches12510, registration=1.
  Inspected darker controls over bright startup screen. CUA clicked A then B
  separately, then native Stop Game. No synthetic runtime state injection.
- Runtime summary: samples10541, buttonSamples21, transitions4, last0,
  pointerSamples0. Two press/release pairs reached emulated controller sampling
  and ended released. This does not prove simultaneous A+B or file-select input.
- Log generated/ios-runtime-r490.log. Native stop reaches Game stopped; shell
  then terminated, console76486 exits0 and sole Simulator shut down. Mixer
  sanitizer test34325 passes again. No product source edit this turn.
- Next simultaneous input route/test and viewport-correct Classic Pointer,
  then editable controls/full native menu. Keep fallback attribution and mobile
  performance acceptance open; startup counters are not gameplay timing.

## 2026-09-08 — R489 visible touch layout / fallback counter semantics

- Prior turn progress: touch overlay connected/build passed. Sole iPad boot23877
  completed0, console54463 launched11912. Actual title shows stick and seven
  buttons; registration=1. CUA A click completed but title requires simultaneous
  A+B, so no button-delivery claim from unchanged title. Native Stop again reaches
  Game stopped; app then terminated, console exits0, sole Simulator shut down.
- Bright startup content makes white translucent buttons faint. Changed button
  background to dark translucent; added stop-time runtime input sample/transition
  summary for next test. Build73769 passes, generated/ios-touch-r489.log.
- Source proves counter caveat: StaticRecompCore_Run increments fallback_steps
  for interpreter SingleStepInner paths, NOT fallback_jit->Run. Therefore macOS
  fallback=0 alone never established zero fallback execution. hook_fb counts
  calls to HookInstructionFallback separately. Next distinguish source/settings
  and execution paths before comparing mobile vs macOS totals or performance.
- Touch movement, simultaneous buttons, pointer/editing/full menu still open.
  Next conclusive runtime input samples and touch A+B interaction test.

## 2026-09-08 — R488 first touch overlay wiring

- Prior turn progress: device registered, real title, first-frame and native
  stop verified. Added GalaxyPadTouchOverlay using reference-derived joystick
  and button events: A/B/Spin/C/Z/plus/minus, safe-area-relative initial positions,
  transparent hit testing and host input callback. Host reset clears contact,
  button tracking and held state; old joystick moves cannot reassert after reset.
- Native overlay visible only during runtime, hidden on exit. Initial compile
  caught weak-pointer dereference; corrected strong-local promotion. Final
  build86316 passes, generated/ios-touch-r488-fixed.log. No Simulator launched;
  touch delivery and layout not yet visually verified. Editing/pointer/menu are
  still required, not replaced by this first-control increment.
- Fallback audit began: macOS wrapper explicitly enables LC_BYTE_FAST and
  LC_PAIR_FAST, mobile does not yet. Source counters distinguish Run interpreter
  steps from hook fallback instructions. This is a configuration difference,
  not yet proven cause of the observed fallback totals; preserve next test.
- Next single-iPad touch A+B/title/file-select and native stop, then accurate
  viewport pointer/menu integration and classified fallback evidence.

## 2026-09-08 — R487 real input registration / native stop

- Prior turn progress: host transport/lifecycle compiled. Sole iPad boot60137
  completed0, console6700 launched10914. Log generated/ios-runtime-r487.log shows
  mobile input device registered=1. Visible title A+B prompt in ios-runtime-r487.png;
  startup label dismissed on frames. No touch button delivery tested yet.
- Native Stop Game clicked through CUA; observed Stopping game then Game stopped.
  Runtime shutdown completed while app remained alive; shell then terminated,
  console6700 exits0 and sole Simulator shut down. Not save/reload acceptance.
- IMPORTANT shutdown counters: native747432253, fallback16724349,
  native_exc312972, hook_fb37043952, smc_failed0. No-fallback-JIT env set, but this
  is NOT zero-fallback AOT acceptance. Need classify fallback/source/config before
  treating Simulator timing as comparable to macOS. No FPS success claim.
- Adapted reference joystick with radial clamp, positive-up Y, explicit contact
  ownership and reset so pre-interruption touch moves cannot reassert controls.
  Source added to app; build83661 passes, generated/ios-stick-r487.log. Overlay
  placement and touch runtime integration remain next, along with fallback audit.

## 2026-09-08 — R486 host input registration and release wiring

- Prior turn progress: virtual device compiled; mixer test passed. Connected
  host-owned per-session mixer/device, dedicated WiimoteNew.ini mapping and
  publish/reset APIs. Reject input while inactive/menu/interrupted/stopping.
  Lifecycle/menu/stop clears held AND pending edges plus cached device snapshot;
  UI reset callback is available for overlay-owned contacts.
- Device update now serializes snapshot consumption and assignment to prevent
  an older poll overwriting a synchronous clear. No FIFO or UI writes into
  unsynchronized touch-override globals.
- Source audit caught Core::Init draining pre-start jobs while Uninitialized;
  moved registration enqueue into active/paused lifecycle reconciliation so it
  occurs after initialization, executed on runtime host thread. Runtime shutdown
  owns normal device removal. Hotplug refresh recovery still requires testing.
- Build36507,6357 and final38241 pass; final log
  generated/ios-input-host-r486-queue.log. No Simulator/game run this turn.
  Next real registration/stop test and touch overlay wiring. Compilation is not
  input delivery or lifecycle/save acceptance; full PRD remains active.

## 2026-09-08 — R485 Galaxy mobile virtual input device

- Prior turn progress: startup regression and first-frame/stop UI; final app
  build84395 passed. Inspected runtime input override and Pipe implementations:
  override has no shake/tilt channels, Pipe fixed buttons. Chose normal virtual
  Device boundary instead of altering Wii reports or adding unsafe UI overrides.
- Added GalaxyPadInputDevice (GalaxyPad/0/Mobile), consumes shared locked mixer
  on device update; locked read snapshot exposes Wii/Nunchuk buttons, movement,
  pointer directions/hide, dedicated Spin and signed tilt axes. Matching config
  uses standard emulated Shake/Tilt/Nunchuk groups. HOME deliberately stays native.
- Actual Simulator C++ device compilation and app link32947 pass, log
  generated/ios-input-device-r485.log. Existing mixer sanitizer test85271 passes.
  Device registration, controller reload, lifetime cleanup and touch overlay not
  wired yet; no runtime input/Spin/tilt behavior claim. Next host registration
  plus clear-on-menu/lifecycle/stop and real touch/button tests.
- No game or Simulator launched. Full native menu/editing/controller/Direct Touch
  scope preserved; first working transport is not completion acceptance.

## 2026-09-08 — R484 Simulator rendering correction startup regression

- Prior turn progress: Simulator fetch capability correction compiled; canonical
  patch retained. Checked no game/Simulator; sole iPad boot62149 exits0, console
  26657 launches9750 with same module/disc/root and no-fallback-JIT environment.
- Observed shader boot through animated Wii wrist-strap screen; capture
  generated/ios-runtime-r484.png, log ios-runtime-r484.log. Neither previous
  render-target-read pipeline failure nor missing post-process shader warning
  appears in this bounded startup. Not a full rendering/gameplay regression pass.
- Added next-build renderedFrames host accessor, first-frame startup-label
  dismissal and native Stop Game action. Stop remains disabled while teardown
  pending; host exit ends startup polling. Host SDK check72487 passes. Runtime
  test used previous installed UI; new UI actions still need visible verification.
- Terminated previous app with simctl, console26657 exits0; sole Simulator
  shut down. Next build/native stop test and Galaxy input/overlay integration.
  No save/soak/FPS/mobile gameplay acceptance claimed.

## 2026-09-08 — R483 Simulator framebuffer-fetch capability correction

- Prior turn progress: native module loaded, Wii startup visible; missing Sys
  resources fixed in bundle. No game/booted Simulator at start of this turn.
- Inspected actual bad_pipeline_Metal_81.txt: fragment main accepts color(0)
  input and pipeline rejects render-target reading. Backend sets fetch from
  reported Apple1 family. Added TARGET_OS_SIMULATOR-only false override at the
  final capability assignment, leaving macOS/device branch unchanged; selects
  existing non-fetch shader/render path, not skipping EFB depth/readback.
- Retained canonical ios-simulator-framebuffer-fetch.patch; corrected patch
  context after initial reverse-check failure, final reverse-check passes.
  Pinned Simulator core build script applies idempotently after revision checks.
- Core rebuild/provision/app chain9531 exits0; logs ios-core-fetch-r483.log,
  ios-provision-r483.log, ios-app-build-r483.log. Actual MTLUtil object rebuilt.
  No runtime rerun yet: next same iPad startup verifies pipeline error removal
  and Sys resources before input/native menu work. No performance claim.
- Reference consulted: https://developer.apple.com/documentation/metal/developing-metal-apps-that-run-in-simulator
  Runtime pipeline failure is the direct evidence; family similarity alone is
  insufficient to infer support. Full PRD/mobile gameplay acceptance remains open.

## 2026-09-08 — R482 first mobile Galaxy rendering

- Prior turn progress: proved actual landscape scene, added pre-start surface
  layout; final build11433 passed. Revalidated module3acdcddd, DOL2c680585 and
  original ref/supermariogalaxy.wbfs bd0d3d4b identities before this launch.
- Boot58926 exits0; sole same iPad. Install/console54657 launches8915 with
  explicit dev root/disc/module arguments and STATICRECOMP_NO_FALLBACK_JIT=1.
  Native worker loads module entry8000403C, RemoteIO initializes48000Hz/callback.
  Visible shader compilation then Wii wrist-strap screen captured in
  generated/ios-runtime-r482-{start,later}.png. Not title/gameplay/audio acceptance.
- Exact log generated/ios-runtime-r482.log: missing bundle Sys post-process
  shader and Metal pipeline81 error reading from render target unsupported.
  Added public Dolphin Data/Sys resource copy to app CMake. Build67512 passes;
  expected shader exists. Runtime rerun not yet done; Metal issue still open.
- Terminated app via simctl (not graceful save acceptance); console54657 exits0,
  then sole Simulator shut down. Sandbox NAND isolated from macOS. Next verify
  bundled resources, inspect Simulator framebuffer-fetch capability and connect
  Galaxy input/native startup-ready and stop UI. Full PRD remains active.

## 2026-09-08 — R481 geometry corrects portrait diagnosis

- Prior turn progress: native scene ownership and separate orientation tests.
  Added change-only geometry diagnostic; build/boot79138 exits0, install/launch
  71129 exits0 PID8401 on sole same iPad. Opened Simulator UI for this device.
- Actual geometry log: view and scene1210x834, interfaceOrientation3 (landscape).
  Screenshot remaining1668x2420 does NOT prove app scene is portrait. Simulator
  Rotate toolbar action changes display ID but settled capture remains portrait;
  compatibility/display presentation remains to audit, not scene mask failure.
- Evidence generated/ios-shell-r481-{rotated,settled}.png. No runtime/game started.
  Added explicit surface layout before runtime request because diagnostic runs
  before child layout and initially sees zero drawable. Check build next turn.
- Stop treating orientation as a blocker to all mobile work: next runtime boot
  and actual Galaxy overlay, keeping responsive/presentation acceptance open.
  Terminate shell/shut down this sole Simulator before next launch.

## 2026-09-08 — R480 scene lifecycle integration / orientation still open

- Prior turn progress: first shell launch and reproduced portrait presentation.
  Consulted Apple TN3192 and installed SDK geometry/lock declarations. Added
  single-scene manifest and UIWindowSceneDelegate window/activity ownership,
  supported landscape geometry request on appearance. Multiple scenes disabled.
- Rebuild/boot10966 passes; install/launch88229 passes PID7810. Actual shell
  screenshot ios-shell-r480.png still portrait. Tested separate orientation-lock
  preference; terminated before rebuild/install/launch37643, passes PID7954,
  screenshot ios-shell-r480-lock.png still portrait. No fix claim.
- Apple reference: https://developer.apple.com/documentation/technotes/tn3192-migrating-your-app-from-the-deprecated-uirequiresfullscreen-key
  UIKit logs show scene orientation request delivery; next inspect effective
  scene geometry/device orientation versus compatibility scaling, not another
  blind plist change. Native scene setup remains useful for lifecycle ownership.
- Both tests no game data/runtime. Terminated app and shut down sole iPad.
  Full native controls/menu and mobile gameplay/performance remain open.

## 2026-09-08 — R479 first visible iPad native shell

- Prior turn progress: real app and module linked. Checked no game/Simulator,
  44Gi available; booted only iPad Pro11 M5 DE8E956F-6B29-4FF3-AF4A-77034CE8588A.
  Boot1550 completed0 after first-boot migration; install43318 completed0.
- Launch8744 exits0, app7154 visibly shows development setup label on black
  surface, generated/ios-shell-r479.png. No game data passed, no runtime started.
- Observed portrait despite landscape configuration. Added explicit iPad plist
  orientations and delegate mask/preferred presentation orientation. Terminated
  app before rebuild/install/launch34204 (exit0, PID7341); repeat screenshot
  generated/ios-shell-r479-landscape.png STILL PORTRAIT. Do not mark fixed.
- Terminated shell and shut down this Simulator. Next inspect actual iOS26
  scene/orientation behavior; then runtime/input/control/menu integration.
  Native shell launch proven only; all gameplay/mobile acceptance remains open.

## 2026-09-08 — R478 first native Simulator app link

- Prior turn progress: native host implementation with real SDK syntax check.
  Same module build61005 remains live, no restart. No Simulator booted.
- Added SunPad-derived CAMetalLayer view using one drawable pixel per point,
  independent of internal EFB scale; native UIKit entry/lifecycle forwarding,
  iPad/iPhone landscape plist, isolated CMake app and reproducible build script.
- Simulator-only explicit development path arguments can request runtime boot
  with sandbox-owned user directory. Device import is intentionally not replaced
  with host paths. Startup message remains until frame telemetry is connected.
- build-ios-simulator-app.sh52638 exits0: all3 ObjC++ sources compile and app
  links against provisioned core. vtool proves IOSSIMULATOR/minos16/SDK26.5.
  Log generated/ios-app-build-r478.log. This is a development shell, not touch/
  menu/import acceptance or a gameplay/performance claim.
- Next single-iPad native launch check then Galaxy input/UI integration; preserve
  full editable-controls/three-dot-menu scope and existing macOS artifacts.
- Completion update: module61005 exits0, actual IOSSIMULATOR/minos16 confirmed.
  Module SHA3acdcddcd47812c2e2a67b8cec0cf06c17009cf1ad7f2e9dc9abd83158a1bac0;
  app SHA478560009dda283921c8a884fb2f8b22a1a42424859845b66f9bad0b4c2012af.
  App plist lint passes. No live build handles remain.

## 2026-09-08 — R477 native runtime host

- Prior turn progress: assembled/validated Simulator archive. Same module build
  61005 remains live; output1330/1332, no restart on silent poll.
- Added GalaxyPadCoreHost adapted from SunPad Metal/runtime/lifecycle pattern.
  Dedicated worker owns creation/Run/destruction; stop requests do not join on
  UIKit. Busy remains set until teardown, weak completion avoids host/thread
  retain cycle. Runtime access is mutex guarded. No Sunshine idle address,
  experimental clock/frame-rate/widescreen code or GC FIFO mapping copied.
- Menu, application activity and audio interruption feed pause reconciliation;
  actual Core state checked rather than trusting early Pause success. Retrying
  during startup preserves lifecycle intent. Full input-clear wiring remains.
- Actual arm64 iOS16 Simulator Objective-C++ syntax check passes Werror; reusable
  scripts/check-ios-host.sh records command. Not linked/launched; runtime and
  lifecycle behavior still require visible tests. Next app surface/input wiring.

## 2026-09-08 — R476 native core provisioning

- Prior turn classified progress: isolated AOT build and its identity checks.
  Revalidated same live61005, now1317/1332; no restart. No booted Simulator.
- Added SunPad-derived provision-ios-simulator-core.sh with explicit archive
  dependencies, actual IOSSIMULATOR runtime-object check and arm64 validation.
  Writes only generated/ios/iphonesimulator/libs; no data/module/save provisioning.
- Initial merge succeeded but lipo argument order failed. Corrected input-first
  order and reran script successfully (exit0), log
  generated/ios-simulator-provision-r476-fixed.log. Final archive SHA
  ed0111a48e4bf4028e605a02cbd7c9f6f8641f9ce6bd57444a7a861eeef9e68b.
  Archive merge is not executable link or launch acceptance.
- Reviewed reference host runtime startup and input override source. Galaxy
  motion channels still need wiring; do not copy reference GMSE01 idle/clock/
  widescreen behavior. Next native host/app integration and module link audit.

## 2026-09-08 — R475 matching Simulator AOT module

- Simulator core completed successfully in R474. Added isolated module build
  script using the accepted RMGE01 generated C, original PGO and iOS toolchain;
  asserts accepted module/DOL/profile identities before configuration. Bash
  syntax passes. No macOS marker, installed app or generated C mutation.
- Build61005 remains active, log generated/ios-simulator-module-r475.log;
  observed850/1332 compilation steps. Continue this handle without restart.
- Read reference provisioning/static archive list and more host startup code.
  Do not carry over GMSE01 scheduler address, 30/60Hz patches, CPU clock tweaks
  or GC FIFO mapping. Galaxy input transport and native host wiring remain next.
- No Simulator or game launched; mobile app execution, controls/menu and device
  performance remain unverified. Full PRD active, macOS performance gate open.

## 2026-09-08 — R474 Galaxy mobile input foundation

- Prior turn started real Simulator core build and recorded user-authorized
  mobile scheduling. Polled82535; compilation continues, not restarted.
- Read SunPad mixer/encoder/overlay interfaces and current Wii Pipe mapping;
  implemented GalaxyPad-owned C++ logical action/mixer layer with provenance.
  No Sunshine GC bits, FLUDD triggers or stick mapping copied into Galaxy state.
- Tests cover tap/Spin edges, merged held buttons/axes, touch pointer precedence,
  source disconnect clearing, clear-all, clamping/nonfinite coordinates. ASan/
  UBSan run19978 passes; arm64 iOS16 Simulator syntax check passes. Added focused
  test to check-repository; full suite not rerun during active core compilation.
- Runtime source guards fallback JIT on iOS and selects software vertex loading;
  these are source checks, not mobile runtime acceptance. No Simulator launched.
- Next core completion/fix first causal compiler error if any, then matching AOT
  module/native host and visible Galaxy touch/menu integration. Full PRD active.
- Completion:82535 exits0, libmoderngekko.a linked; actual runtime object vtool
  records IOSSIMULATOR. Next matching module/native host; no executable app yet.

## 2026-09-08 — R473 user authorizes mobile implementation now

- User explicitly changes macOS-first scheduling: proceed with iPad/iOS shell,
  reference implementation, controls/menu while performance remains unresolved.
  EXECUTION-PLAN/STATUS now record override without marking G6–G9 passed.
- Added SunPad-derived Simulator toolchain and separate pinned-core build script;
  bash syntax passes. Actual configure/build launched as82535, log
  generated/ios-simulator-core-r473.log; continue polling this exact handle.
  No Simulator booted and no app or mobile gameplay success claimed yet.
- Read reference host/lifecycle/mixer interfaces and build/issue context. Mobile
  queue/provenance recorded in MOBILE-IMPLEMENTATION.md. macOS app preserved.
- Closed interrupted performance probes: R471 exact adjacent lazy-FP global
  load idioms640/7782 selected samples, stack185, unresolved3951 (not savings).
  R472 availability-order oracle800000 full-state cases passes in both builds;
  isolated compiler still loads the global unconditionally, so no demonstrated
  codegen win or module build.3925 exits0; logs fp-availability-order-r472.log and
  shared-load-origins-r471.json retained. Performance experiments paused by user
  direction; next mobile core completion and native app/module wiring.

## 2026-09-08 — R470 broadens retained sample attribution

- Prior reverse-order gameplay test rejected snapshot promotion and full suite
  passed. Reused retained profile/exact installed module, no new runtime capture.
- Twenty-chunk instruction join accounts for7782samples; added explicit FP
  move/conversion diagnostic category, focused classifier test passes. Compiled
  actual CPUState offsetof probe prevents confusing downcount with RAM metadata.
- Evidence and caveats in PERF R470; no instruction-location percentage is
  claimed as removable time. No product patch/build/launch/save mutation.
- Next identify actual bases of high-weight remaining loads before proposing a
  shared transformation. Parked experiments stay parked. No game or Simulator
  launched; offline commands exit0; full suite R469 remains latest session pass.

## 2026-09-08 — R469 reversed gameplay comparison rejects candidate

- Previous turn produced a preliminary positive pair; tested reverse order on
  fresh copies of exact slot/save/config using unchanged isolated binaries.
- Candidate55.7842 vs control55.8897Hz; process instructions/VI258.078m vs258.017m.
  No repeatable gain, so parked candidate with no product promotion. Detailed
  frame tails and artifacts in PERF. No unchanged third timing pair authorized.
- Both scenes visually advance, exact game handles56693/43454 exit0, capture
  handles18348/62576 exit0, saves106e unchanged, no runtime or Simulator remains.
- Full suite28233 exits0, generated/check-r469.log ends Repository safety checks passed.
  Next shared AOT helper work attribution across retained sampled chunks,
  preserving semantic boundaries and the full PRD/macOS-first goal.

## 2026-09-08 — R468 real-game screening pair completed

- Previous turn delivered tested/compiled isolated candidate. Linked same-graph
  control/candidate via copied core archives; normal link inputs and app unchanged.
- Launched one game at a time, exact slot2/normal1x settings. Verified same visible
  hanging-Mario scene before/after30s low-overhead work/VI captures. Control55.2931
  vs candidate56.4910Hz, work260.562m vs254.784m instructions/VI. Detailed pacing,
  artifacts, caveats and hashes in PERF R468. Preliminary gain, not promotion.
- Both exact game/capture handles exit0, fallback0/smc_failed0, saves106e unchanged.
  No game/Simulator remains. Whole-session audio totals do not establish acceptance.
- Process-work summary regression passes; full suite deferred until candidate
  decision. Next reversed candidate/control run on new disposable profiles to
  test repeatability before broader controls/lifecycle/audio regression.

## 2026-09-08 — R467 scoped lookup candidate, not installed

- Previous turn made progress by parking the narrow precision lane and mapping
  actual shared loop samples. Audited metadata writers and live SMC state before
  implementing a run-scoped immutable-metadata snapshot experiment.
- Differential exact-lambda eligibility tests pass4787968cases twice (sanitized
  and optimized), including invalidation after snapshot and slow-path routing.
- Isolated full Run control/candidate objects compile with cached runner flags,
  build76883 exit0. Source/cached object/installed runner hashes unchanged.
  Candidate stack increases48bytes, so no static speedup claim. Evidence in PERF.
- Next link isolated one-object runner and compare actual same-scene pacing/work;
  no module rebuild, additional synthetic benchmark or installed promotion yet.
- No game/Simulator or save changes. Focused tests/build ran; full suite and
  gameplay smoke deferred until a runnable candidate exists. Full PRD active.

## 2026-09-08 — R466 closes precision scope and attributes shared loop

- Previous turn refined authoritative execution policy but delivered no product
  change. This turn executes its pending go/no-go check rather than restating it.
- Exact-source44-entry audit passes: four path-local eligible rounding sites,
  no producer fact at each site's external entry. Full containing chunk is only
  1143/28822 retained CPU samples; site cost unproven. Parked, no module build.
- Verified installed runner SHA671729c6; reused retained R422 XML with existing
  summarizer and exact-symbol disassembly. Eight disjoint source-mapped regions
  explain3702/3711 Run leaves. Details and private artifact paths in PERF R466.
- CPU profile/footprint tests pass. No runtime, Simulator, package, save or product
  change; no FPS claim. Full regression/smoke not repeated for read-only evidence.
- Next: audit writers of execution-loop mode/lookup metadata and callback
  invalidation; consider one shared-loop specialization only if semantics hold.

## 2026-09-08 — User-directed goal refinement after R465

- Request: reorient the running goal using findings without reducing the original
  PRD or expected iOS/iPadOS product. Verified the original goal remains active;
  retained it rather than marking it complete or creating a replacement goal.
- Updated EXECUTION-PLAN and STATUS: startup side task closed, ~3.81× process
  work/VI evidence incorporated with limitations, plausible whole-game impact
  required before another optimization build, current precision audit must end
  in a go/no-go decision. No recent diagnostic is represented as an FPS gain.
- Preserved macOS G6–G9 before mobile, complete SunPad-derived visual and
  interaction acceptance, one Simulator, private data and publication boundaries.
- Documentation-only change; no product/build/save mutation or runtime claim.
  Regression/runtime suite not rerun because executable behavior is unchanged.
  Next action: conclude current precision-provenance safety and impact scope.

## 2026-09-08 — R465 precision identity oracle and safety boundary

- Prior installed-code work fixture supplied progress. Read actual rounding
  helpers and historical R195; did not repeat its rejected guarded25bit shortcut.
- New strict O2/UBSan actual-helper oracle passes800000 canonical-single
  idempotency checks over rounding/NI/random/special values and unchanged host
  flags. Explicit NI0→NI1 subnormal counterexample passes. Output
  generated/single-provenance-r465.log. No speed measurement or product patch.
- Source inspection identifies entry joins, conditional scalar destination
  updates and callback/FPSCR mutation barriers. Next quantify valid producer facts
  across actual path entries before proposing conversion specialization. Do not
  generalize finite full-entry fixtures to arbitrary external suffix state.
- No game/Simulator/build/save/package changes; focused oracle passed, no full
  suite repeated for isolated diagnostic. Full PRD/macOS-first G6 remains active.

## 2026-09-08 — R464 actual installed-code instruction-work probe

- Previous turn quantified3.81x process instructions perVI. Inspected existing
  exact-chunk harness; avoided its parked LLVM candidate and standalone-object
  policy mismatch by calling installed module's public dispatch descriptor.
- New private C driver uses known bounded synthetic44instruction arithmetic
  fixture, checks all44interior entries' PC/charge/exception. ABI size/game/version
  checked; installed module SHA1fb635f7 unchanged.92473 exit0;~2753–2763instructions
  per call, with driver/assertion/dispatch overhead explicitly retained.
- Source comparison found reference IsSingle/FixSinglePrecision tracking versus
  current double-lane/general conversion helpers. Next precision-provenance and
  observation-boundary audit; no arithmetic shortcut or compiler/module change
  justified yet. No full-state/new gameplay acceptance inferred from this probe.
- No runtime/Simulator/save/package changes. Focused driver passed; full suite
  not rerun for isolated diagnostic driver. Full PRD/macOS-first G6 remain active.

## 2026-09-08 — R463 reference process-work baseline completes comparison

- R462 established AOT baseline. Verified no game/Simulator, reference binary,
  same slot2/save106e and matching GFX; fresh work-reference-r463 profile. Explicit
  diagnosticJIT log, actual LoadState2 visible scene before/after, no build/profiler.
- SinglePID87388/helper93621 exit0;30.008733s,1799VI,69.046633m process instructions
  per VI,27.818520m cycles per VI. About3.81x fewer instructions than AOT's263.149m
  per VI, with whole-process/single-order/host context limits retained in PERF.
- Native Command-Q runtime86600 exit0/save unchanged, no runtime/Simulator left.
  Existing tested capture/summarizer reused, git diff-check passes. No product
  edits, so no full suite repeated. No performance fix or acceptance gate claimed.
- Next actual-chunk expansion attribution, not more aggregate profiling or parked
  microbenchmark repeats. Full PRD and macOS-first gate order remain active.

## 2026-09-08 — R462 AOT work-per-VI baseline

- Prior R461 added validated low-overhead process snapshot. Verified raw-clock
  compatibility against C++ steady_clock1000brackets, then inspected actual VI
  recorder clock. Extended focused tests pass; no performance compiler activity.
- Fresh profile work-aot-r462, accepted runner/module hashes, same slot2/save.
  SinglePID87099, actual native LoadState2; scene visually verified before/after.
 10s warm-up/30s snapshot helper76704 exit0; native Command-Q runtime72610 exit0,
  save106e unchanged. No Instruments/Simulator or concurrent game.
- Added bounded capture and tested per-VI normalization preserving query-time
  uncertainty.1635VI/30.006842s;430.249billion whole-process instructions,263.149m
  per VI. This establishes AOT baseline only, not excess-work comparison or gain.
- Full suite not repeated for diagnostic-only scripts; targeted snapshot/summary
  tests pass. Next matched diagnostic reference with same VI/snapshot method.
  No build/package mutation, publication, save change or G6/mobile promotion.

## 2026-09-08 — R461 measurement semantics and process work snapshots

- Previous comparison advanced evidence. Exported existing raw timer counter table
 95246 exit0; array event mapping not established. Stopped short of guessing it.
- Installed Apple bottleneck definition specifies time-weighted aggregation;
  added it alongside explicitly labelled cycle weighting and recomputed both
  retained captures (21743 exit0). P-core delivery38.24% AOT versus44.70% reference
  preserves conclusion. Tests prove distinct weighting and excluded durations.
- SDK/source-supported proc_pid_rusage probe compiles strict O2; real API self-
  work/external test-process queries advance and preserve identity/clock order.
  Added invalid-PID parsing tests and wired both focused tests into full suite.
  No full suite/game smoke repeated for diagnostic-only change; no game/Simulator
  launched, source/package/save unchanged. No publication.
- Next prospective matched late-scene process-counter/VI windows, explicitly
  whole-process work not guest-thread work. This is a lower-overhead measurement
  alternative, not an implemented speedup or G6/mobile promotion.

## 2026-09-08 — R460 counter validation and reference comparison

- R459 supplied new hardware evidence. Validated chronological non-overlap
  including core migration; added XML ref/sentinel/filter and invalid-value tests.
  Strict fraction-sum assumption failed on actual exported metrics; replaced
  with explicit nonunit-count/cycle/error reporting without altering values.
  Synthetic checks pass and R459 aggregate remains unchanged.
- Fresh reference profile preserves exact slot2/save and uses prior R425 accuracy
  config. Diagnostic runner34e96be1 logs explicitJIT. SinglePID86223 actual load
  observed asynchronously, same scene; CPU Counters10s90687 exit0, game89063
  exit0/save106e unchanged, final59.9 title not a formal throughput measurement.
- Export54181 and validated aggregate pass. Reference delivery46.06% versus
  AOT38.10% changes decision: no evidence for worse relative instruction delivery
  explaining AOT slowdown. Do not promote speculative layout/size change.
- Product/build unchanged; targeted parser tests pass, no full suite rerun for
  offline analysis-only edits. No game/Simulator left, no publication. Next read
  retained raw counter definitions for actual retired-work evidence/normalization,
  not another identical capture. Full PRD and macOS-first G6 hold remain intact.

## 2026-09-08 — R459 actual CPU hardware-counter capture

- Prior R458 supplied new footprint/schema evidence. Inspected installed template
  and local package docs; capability31466 saved actual bottleneck metrics (exit54,
  not game evidence). CPU Counters is usable without inventing raw event names.
- Fresh counters-r459 copied isolated config/save106e/slot2 74e453b5; unchanged
  accepted runner/module. One game85638/69265 only, actual Load State2 and visible
  same late scene before/after. Capture5332 exit0,10s; post title24FPS underscores
  observer contamination, not normal throughput. Native quit runtime exit0 and
  save unchanged. No game/Simulator/Instruments left; no deletion/publication.
- XML export69483 exit0. New counter summarizer initially encountered sentinel
  averages; fixed to explicitly exclude/report unavailable cycles rather than
  treating them as zero. Focused weighting/duplicate/incomplete/missing tests pass.
  P-core delivery38.10% versus processing6.28%, detailed interpretation in PERF.
- No performance fix claimed. Full suite not repeated for offline parser-only
  changes; R457 remains last full suite. Next validate interval aggregation and
  collect matched reference counters before selecting a source change. Preserve
  full original PRD and macOS-first gate order.

## 2026-09-08 — R458 sampled footprint and counter-evidence audit

- Prior turn made product progress through startup-feedback integration; returned
  to CPU/audio work. Reused R422 evidence and read earlier dispatcher/state/PGO
  findings to avoid repeating rejected work. Working tree preserved.
- xctrace retained-capture schema confirms cycle-only sampling; installed CPU
  Counters template exists, but event configuration/usability not yet verified.
  Module UUID matches trace. No game/Simulator running or started.
- Added sampled-code-footprint summarizer and focused synthetic test (pass),
  wired into repository check. Output generated/code-footprint-r458.json:
  22822module samples across14885 aligned PCs;90% covers1794 4KiB address bins.
  Not a cache-miss, dynamic-instruction or speedup measurement. No rebuild.
- Full suite/smoke not repeated for offline-only analysis; R457 full suite is
  the last completed product regression. Next inspect CPU counter event support
  before one bounded late-scene capture. No G6/mobile promotion or publication.

## 2026-09-08 — R457 canonical startup-feedback integration and promotion

- Previous turn was planning only, not product progress. Revalidated source,
  bootstrap layering, existing tests and process/Simulator absence before edits.
- Added hash-pinned ModernGekko0027 overlay with peel-before/restore-after0013,
  apply and scope registration. Initial62903/repeated28898 bootstrap exit0;
  source cmp against runtime-tested R454/R456 source passes after both.
- Expanded extracted monitor regression to success, child exit7, monitor error,
  quit, matching-window close and unrelated-window close. Duplicate close events
  request graceful termination only once. Focused pass; full suite73685 exit0,
  generated/check-r457.log ends Repository safety checks passed.
- Candidate audit29665 and normal-app audit25289 exit0. Copied only tested
  frontend into normal app and re-signed outer bundle; old frontend retained at
  generated/frontend-before-r457/GalaxyPadFrontend. Installed frontend SHA
  c283de0c3dee9e3f0b01d85f961eeb7515060fd57019aeefd875674ccd4956fc;
  runner671729c6/module1fb635f7 remain exact accepted hashes. No AOT regeneration,
  runner rebuild, game launch, save mutation or Simulator this turn. No publication.
- Runtime smoke not repeated because R456 exercised this exact frontend/runner/
  module combination; this turn establishes canonical integration and packaging,
  not fresh gameplay acceptance. Bounded log-tail readiness and unchecked failed
  termination remain limitations, not guarantees. Startup UX side task closed;
  next reconcile retained CPU/audio evidence and choose one causal performance
  change under EXECUTION-PLAN step2. No G6/mobile promotion or FPS gain claimed.

## 2026-09-08 — R456 failure and real-game startup-progress acceptance

- Prior turn linked and tested progress/cancel on controlled child. Separate
  failure profile used: frontend81482 shows Session failed/code7/log location
  after intentional child failure; Return dismisses, parent11577 exits1 as expected.
  No orphan. Failure capture frontend-failure-r456.png. No game/save in dummy test.
- prepare-progress-game.py copies existing app/profile into fresh ignored paths,
  replaces only frontend, signs outer app, verifies runner/module equal accepted
  hashes and save106e.75156 exit0. Candidate frontend SHA
  c283de0c3dee9e3f0b01d85f961eeb7515060fd57019aeefd875674ccd4956fc.
- Actual Play frontend81590→child81634: visible elapsed4s status, module-ready
  hides parent (AX window no longer exists; parent0.0%CPU snapshot), game window
  visible. Title→normal real-save Observatory1107 exit0→movement59723 exit0→
  Command-Q parent5936 exit0/no children. Save106e unchanged. Captures
  generated/progress-game-{start,ready,title,load,movement}-r456.png; full fresh log
  retained in frontend-progress-game-r456/GalaxyPad/Logs/ModernGekko.log.
- Native1796811156/fallback0/smc_failed0. Whole131.6s1underrun/4backlogs and
  max162msgraphics gap are not performance/audio acceptance. This proves new
  launch-feedback/handoff smoke, not faster initialization or heavy-scene speed.
  Normal app unchanged. Next integrate canonical frontend patch with focused
  monitor/readiness/cancellation tests and frontend-only package audit. No simulator.

## 2026-09-07 — R455 isolated launcher build and controlled-child UI check

- Prior turn compiled staged source only. build-launch-progress.py compiled one
  object/relinked from cached archives, preserved normal object/executable hashes;
 82861 exit0. No runner/module rebuild or normal package mutation.
- Controlled launch-progress-child.c accepted actual frontend Play. Captured
  elapsed18s startup screen (frontend-progress-visible2-r455.png). First close
  raced30s readiness and AppleEvent failed; do not call cancellation proven there.
  Changed cancel fixture to120s. Second frontend81312 close button triggers child
  graceful marker and parent67215 exit0; no remaining child/frontend. No game code.
- Evidence handling mistake: reused R447 XDG profile, so normal launcher log
  truncation overwrote original R448 raw log. Updated acceptance doc to invalidate
  that path as original evidence. Existing screenshots/transcribed counters stay;
  real save106e reverified unchanged. Added separate no-Wii frontend-progress-r455
  profile for every subsequent dummy test. Do not repeat gameplay just to conceal
  lost log, and do not present test logs as game evidence.
- Next controlled failure dialog then actual-game readiness/handoff in a fresh
  backed-up profile before canonical patch/package integration. Full G6/PRD and
  macOS performance-first hold remain; installed app unchanged.

## 2026-09-07 — R454 isolated visible-startup frontend prototype

- Prior turn parked page-table design with measured evidence. Inspected actual
  frontend launch/wait/render path, existing nonblocking-wait regression and
  canonical patch0013. Immediate SDL_HideWindow explains R448's invisible wait.
- Added experimental frontend-launch-progress.inc and stage-launch-progress.py.
  Stages current launcher without source mutation; draws4Hz elapsed status with
  no Play controls, reads bounded8KiB log tail for module-ready marker, then hides
  and ceases drawing. SDL events permit close→graceful child termination request;
  existing nonblocking wait remains. No second launch action or game-module build.
- Actual cached frontend flags syntax-check92595 exit0. Evidence generated/
  frontend-progress-r454/{moderngekko_launcher.cpp,compiler.log,report.json}.
  This is compilation only, no runtime/UI/cancellation acceptance. Source hash
  unchanged; installed package untouched. Next isolated frontend object/link and
  one-child launch/cancel/failure/readiness checks before canonical integration.
  Full PRD/performance/G6/mobile hold unchanged.

## 2026-09-07 — R453 page-table boundary model and stop decision

- Prior turn measured per-access coherence regression. Inspected actual runtime
  callback sites; extended isolated resolver with nested-callback bypass and
  outermost refresh model.635520 pointer cases plus nesting/acquired-store-pointer
  checks pass ASan/UBSan. Not production callback/alias coverage.
- Stable-map optimistic2M ABBA then changed10M BAAB show inconsistent timing
  advantage (PERF exact values), before actual callback integration costs. Park
  design rather than launch a full build or present fastest sample as a win.
- Next bounded product step: R448 startup lacked visible feedback for~40s after
  Play. Inspect frontend hide/wait/render path for a small responsive loading
  presentation change, not another unchanged performance experiment. Throughput
  and full original PRD remain open; no runtime/module/save change this turn.

## 2026-09-07 — R452 AOT/reference memory-contract discriminator

- Previous full-chunk audit changed candidate decision; no delivered speed gain.
  Read current status, pinned reference non-arena loads, AOT mapping and prior
  callback-cache/two-range history. Distinguished code lookup from data mapping.
- Implemented isolated data-page oracle retaining live metadata validation and
  fallback.635520 pointer/journal-offset cases pass ASan/UBSan; existing actual
  header callback/journal/field-offset test passes. Separate unsanitized ABBA
  makes table slower (PERF exact timings). No build/game/Simulator overlap.
- Reject per-access-table route without full module. Next audit actual mutation/
  reentrancy boundaries before moving refresh; do not remove coherence checks
  based on this oracle. No product/module/save change, full PRD/G6/mobile hold.

## 2026-09-07 — R451 full-chunk profile compatibility decision

- Previous turn provided isolated correctness evidence, not FPS progress. Read
  current status/cache compile-audit machinery and preserved dirty state.
- New audit-restore-profile.py compiles full original/candidate with actual cache
  flags and original profile.4002 exit0. Original entry count238389378 disappears
  in candidate despite quiet profile warnings. Saved full report/IR/logs; protected
  hashes unchanged. Added explicit profile-compatibility/promotion result fields
  for future runs; existing report's counts already establish failure.
- No unfair timing, fabricated profile or full module. Park narrow candidate on
  small measured scope versus retraining cost, not a false regression claim.
  Next broader generated memory-contract/reference source comparison. No runtime,
  build process or Simulator left; diff check passes. No product source changed,
  so no unrelated full-suite repeat. Full original goal remains active.

## 2026-09-07 — R450 bounded register-restore prototype

- Previous turn made evidence progress by rejecting narrow FP guards. Current
  status/dirty state and retained samples checked; no active game/compiler found.
  Disassembled exact installed805170A0, mapped dispatch versus restore sites, and
  re-read existing suffix-accounting fix to avoid regressing it.
- Added product-immutable actual-source restore-range probe; source SHA pinned.
  Whole12-byte direct-RAM guard falls back to original three loads at callbacks
  and boundaries.7616 full-state/callback-count cases pass ASan/UBSan (exit0).
  No dependency/source/module/save mutation or simulator/game launch.
- This is a correctness prototype, no user-visible speedup. Next full-block
  profiled code shape/quiet comparison before any expensive build. Do not broaden
  to stores or bypass cycle/return/SMC contracts. Full goal remains active.

## 2026-09-07 — R449 measured rejection of narrow availability-check lead

- Previous turn made progress: current frontend-visible gameplay and narrow
  pause/resume/exit proven. Read current state/PERF and retained R422–R427 evidence;
  no runtime/build active. Avoided fresh profile or expensive rebuild.
- Added read-only audit-fp-guard-samples.py joining exact retained assembly/sample
  addresses with current module hash guard; synthetic parser rejection tests pass.
  Run generated/fp-guard-samples-r449.json exit0;238 classified samples/28822CPU
  across two chunks. No whole-module/removable-time inference. Runtime FP helper
  and memory-callback boundaries inspected; no unsafe guard removal.
- Rejected candidate as insufficient support for the substantial gap. Installed
  game unchanged. No full suite needed for product-immutable audit; focused parser
  tests and diff check pass. Next inspect805170A0 sampled sites/behavior with exact
  source identity. Full PRD and macOS-first/G6 hold remain active.

## 2026-09-07 — R448 current frontend-visible-gameplay check executed

- Previous turn was planning, not runtime progress. Revalidated processes,
  Simulator state, prepared profile and runner hash. Created only isolated Pipe
  directory/FIFO. Launched current packaged frontend with isolated XDG and LC
  defaults; actual Play started sibling runner. CUA timeout created duplicate
  frontend79100, immediately terminated before game launch; exact-PID System
  Events controlled remaining frontend79088/child79154.
- Module loaded after roughly40s startup; one-second sample40156 exit0 captured
  subsequent responsive event loop, not startup cause. Title→real-save load74090
  exit0→visible Observatory movement80321 exit0→native pause/resume→movement84443
  exit0→Command-Q. Parent24515 exit0, no orphan game. Save106e unchanged.
- Captures and exact limits in MACOS-ACCEPTANCE.md. Narrow frontend/Pipe path
  complete, not audio/performance/physical-control/G6 acceptance. Whole run has
  fallback0/smc_failed0,3 audio underruns/5 backlogs; deliberate pause contaminates
  max-gap statistic. No build/source change; prior repository suite already green.
- Next step2 performance/audio: inspect existing slow-scene source/profile evidence
  for a new causal boundary. Do not rerun finished frontend test or parked compiler
  probes. Full original PRD/mobile hold retained; no game/Simulator remains.

## 2026-09-07 — user-directed goal reorientation

- Re-read the full original PRD and goal loop, current status and acceptance
  checkpoint. Kept the existing active goal unchanged; no completion or mobile
  promotion claim. Added EXECUTION-PLAN.md as the authoritative current queue.
- Preserved proven Grand Star/save progress, named the real AOT CPU-cost evidence
  and unresolved severe-lag/audio boundary, and parked non-delivering experiments.
  Explicitly retained all story/completion, mobile, full shell/visual/control,
  physical-device and release gates. Defined step results in user-visible terms.
- Next: one prepared current frontend-to-visible-gameplay stability check, then
  measured macOS performance/audio work. No new runtime/build or Simulator launched
  for this planning update. Existing check-r447.log ends in Repository safety
  checks passed; this is not a gameplay or performance result.

## 2026-09-07 — R447 accepted-product audit and isolated frontend preparation

- Prior turn progress: quiet profiled confirmation rejected selector; no delivered FPS gain. Audited current acceptance records instead of replaying compiler work. Fresh private app audit79408 exits0 and runner671729c6/module1fb635f7 match R390. Original import/Nunchuk/monitor fixes R77–R80 rediscovered and current source checked; old pending notes must not restart completed work.
- Added MACOS-ACCEPTANCE.md distinguishing historical route/import proof, current artifact smoke, unresolved cadence/audio and missing current frontend-visible-child evidence. No goal promotion or narrower objective. First Grand Star/persistence remains proven; full PRD/mobile hold retained.
- prepare-frontend-acceptance.py created ignored frontend-acceptance-r447/GalaxyPad from R390 Config/Wii/config.ini, verified original and copied106esave, no emulator states, default-game points to existing exact extraction. Uses frontend's XDG isolation; normal wrapper overrides XDG, so do not call this a Finder wrapper test. Normal user profile/product untouched. No game launched.
- Next review Pipe setup, launch one packaged frontend in prepared profile, use actual Play and visible child real-save/input/pause/resume/clean exit; no reimport/rebuild or new profiler. Full repository suite started separately; poll returned handle.

## 2026-09-07 — R446 quiet profiled comparison; return selector parked

- Prior turn progress: selector preservesPGO and passes semantics. Checked current checkout/status and no game/compiler/ninja at start. Built isolated native objects from audited profiledIR, renaming control after profile application; product hashes retained.
- Quiet1M ABBA passes55k state cases but arithmetic gain variable. One5M BAAB confirmation26823 passes same corpora and does not reproduce gain; merge slightly slower both orders. Exact metrics/boundaries in PERF. No performance timing overlapped repository suite.
- PARK candidate, no module build or promotion. LLVM remains parked. Next audit remaining original macOS G6 gameplay/stability evidence and accepted runtime, not another unchanged compiler/dispatch microbenchmark. Full PRD/G0–G15 and expected iPad/iPhone product retained.
- Full repository suite6080 started after timing and completed exit0 (generated/check-r446.log). No game or booted Simulator found; no live work remains. Installed app unchanged. Rechecked SAVE-AND-NAND: preserve R60/R61 Grand Star/save/relaunch and R363 persistence evidence; remaining current-artifact timing/audio/packaged-entry acceptance is not waived.

## 2026-09-07 — R445 return guard PGO compatibility and selector proof

- Prior turn progress: narrow C guard isolated tests passed. Read exact cached compile graph/source/profile, preserved all protected artifacts. Direct guard loses65,272,501entry profile and emits missing-profile warning; rejected that form.
- Added equivalent pure inline selector leaving caller switch CFG intact. Control/candidate both retain65272501entry count without missing-profile warning (52955 exit0). No fabricated profile data.55k exact-chunk state tests and327680 target checks pass (93183 exit0).
- Test-generated incidental timings overlapped repository suite and are explicitly not accepted as performance evidence. Next quiet profiled-settings comparison before any module build; current app unchanged, no FPS claim. Full original PRD/G6/mobile hold, parked LLVM/old dispatcher experiments preserved.
- Full repository suite90645 completed exit0 in generated/check-r445.log. No game or booted Simulator found; no live work remains. Next quiet profiled-object comparison uses return-selector-profile-r445 artifacts, not the rejected direct guard.

## 2026-09-07 — R444 C local-return guard proof and isolated timings

- Prior turn progress: LLVM parked after two regressing execution fixtures. Audited current state/history before new trace. R139/R140/R420 already cover generic host-loop/frequency lane; no repeat capture. Sparse switch at local returns provides narrower source hypothesis.
- Simplified initial label-table idea to unsigned envelope guard before unchanged switch, retaining original cycle check/default/targets and instruction text.327680 target comparisons,11k merge/store and44k arithmetic full-state/RAM/flag cases pass. Candidate compiler run/execution26561 exit0. No product mutation.
- Isolated ABBA favors guard (PERF exact numbers), but no installedPGO/LTO parity or FPS assertion. Next current generated-source/cached-profile graph audit before a module build; do not lose prior rejected-entry experiments or funding limits. Full PRD/G6/mobile hold unchanged; LLVM remains parked.
- Full repository suite20240 completed exit0 (generated/check-r444.log). No game or booted Simulator found; no live work remains. Existing generated/audit-vector-build-r429.py demonstrates compdb -x response decoding and profile function-entry metadata checks for the next scoped audit; do not rerun its parked vector build.

## 2026-09-07 — R443 arithmetic result and LLVM stop decision

- Prior turn progress: direct-context candidate compiles, small corpora pass, merge/store regresses. Current checkout and exact arithmetic source inspected; reused cached full objects without generator/compiler rebuild.
- Added arithmetic fixture: broad selected-entry comparison exposes state mismatch atentry23, retained failing log. Separate explicit normal-entry1000cases pass and ABBA timing completes93212 exit0: IR307–324ns vs C228–242ns. No masking, no broad correctness or FPS assertion. Fixed future test output to distinguish normal from selected-entry scope.
- PARK LLVM candidate. Both tested paths slower; no full module or continued unfocused backend expansion. Next investigate C dispatch-entry distribution and shape with bounded trace, retaining SMC/cycle semantics and prior rejected experiments. Full original PRD/G6/macOS-first objective preserved; product unchanged.
- Full repository suite53709 completed exit0 (generated/check-r443.log). No game or booted Simulator found, no live work remains. Broad arithmetic discrepancy is deliberately outside the green-suite claim. Next inspect existing sparse dispatch traces, with deterministic-sampling caveat, before new instrumentation or runtime.

## 2026-09-07 — R442 direct-context experiment and first exact-chunk execution

- Prior turn progress: cache-promotion experiment isolated stack pathology. Current checkout/status inspected; new staged direct-context alternative, no product changes. Full combined build/synthetic checks/exact chunk19795 exit0. Code187112bytes versus C134452, stack2960bytes; matching C hash.
- New hash-checked cached-object execution harness. Fixed missing real helper-table link, then explicit HID2 paired-load/store fixture setup (initial exception was not a backend regression).11000 whole-state/RAM/flag/return/cycle checks pass. ABBAmerge/store path IR~110ns versus C36–37ns: no speedup, no representative gameplay assertion.
- Next reuse cached objects for one arithmetic-heavy path and check semantics before timing. No full module or unchanged rebuild. Full original PRD/G6/mobile hold retained. Product/module/save unchanged, no game/Simulator launched.
- Full repository suite67481 completed exit0 in generated/check-r442.log. No game or booted Simulator found; no live build/test remains. Next reuses game-chunk-r442 cached objects, not a recompile.

## 2026-09-07 — R441 cached-state promotion experiment

- Prior turn progress: real-chunk emission exposed17.89× code/92.3KiB stack. Current checkout/status inspected. Added explicit staged-only memory-state diagnostic, preserving all instructions/entries and runtime boundaries; no pinned/product changes.
- Fresh stage/build/test/emission44041 exit0.88k memory,32k exception policy and30k baseline pass. Same exact chunk C object byte-identical. IR stack94528→2944bytes; __TEXT2405424→1243140bytes, still9.25× C. No FPS or full-module acceptance.
- Next: bounded removal of redundant cached/context state traffic, against same exact chunk and regression corpus. materialize's function-wide dirty-slot writes are the next inspected boundary. Do not expand to a full module or pretend this diagnostic memory traffic is the final optimization. Full original PRD/G6/mobile hold unchanged, installed app untouched.
- Full repository suite60076 completed exit0 in generated/check-r441.log. No game or booted Simulator found, no live work remains. No installed gameplay or speed claim.

## 2026-09-07 — R440 exact gameplay chunk code-generation checkpoint

- Prior turn progress: staged reservation fix passes matched corpus. Inspected current checkout/status, existing R422 profile and matched assembly. Identified shared dispatch among hot sample sites; did not equate a convenient tiny helper with representative gameplay.
- Added private exact-DOL chunk emission audit; source contains no game words.1024decoded/unknown0, C and IR objects/assembly complete (24283 exit0). LLVM emission observed live then polled same handle; no restart. Exact identities/profile attribution in generated/game-chunk-r440/report.json.
- Result: IR __TEXT17.89× C, very large stack reservation. This fails the practical full-module checkpoint even before a defensible runtime fixture is available. No FPS benchmark claimed. Next inspect all-entry state-merge lowering and test one scoped structural correction against same chunk, retaining R439 correctness tests. Installed app/module/save unchanged; full PRD/G6/mobile hold unchanged.
- Full repository suite20864 completed exit0 (generated/check-r440.log). No game or booted Simulator found, no live work remains. Generated private case retained for reproduction, no publication. Source entry point for next experiment: FunctionEmitter::emitEntry creates cached slots and a switch into every instruction block, making state-merge pressure a falsifiable hypothesis rather than a measured cause yet.

## 2026-09-07 — R439 staged reservation fix and matched regression

- Prior turn progress: source-correlated memory defects. Current checkout/status inspected; reused public pinned source and LLVM20, no bootstrap or production source changes. Added explicit opt-in stage correction with hashes/diffs and original-file rehash checks.
- First stage stopped on ambiguous anchor, not a compile failure; scoped transformation to stores and used fresh r439-fixed path. Build31891 exit0. Added journal reservation mutation to corpus. Fixed88k memory cases pass; unfixed R433 same corpus still fails. Baseline30k, FP-policy32k and ARM64 guard/ABI/execution checks all pass (9585 exit0).
- Next bounded decision: select one actual R422-profiled gameplay block, construct defensible input fixture, compare C/IR generated work and execution cost before any full module. Fixed synthetic checks are not gameplay/chassis acceptance. Full PRD/G6/mobile hold preserved; normal app/module/save unchanged, no game or booted Simulator found.
- Full repository suite84455 completed exit0 in generated/check-r439.log. Correct probe root generated/llvm-arm64-probe-r439-fixed; R433 remains unfixed control (34k failures on same88k corpus). Initial r439/staged is unused partial public-source staging, not the candidate. No live work remains; next is measured-block selection, not an unchanged rebuild.

## 2026-09-07 — R438 memory/callback reservation defects isolated

- Prior turn progress: exception-policy oracle/source decision. Inspected active status and dirty checkout; reused staged emitter/cache. No tool install, bootstrap, product build or runtime launch.
- New memory corpus first ran64k cases,10k external-store reservation failures. Added diagnostic by mode/entry, journal reservation observation and opposite-mirror reservations; expanded80k run3416 exits1 with30k failures. Source explains external clear placement, missing mirror normalization, and unsynchronized journal observation. Full state/RAM/events/flags retained; no masks or downcount-only failures.
- Baseline actual-runtime30k passes. Next staged-only reservation correction plus journal-mutation regression, then existing corpus/FP oracle before considering measured gameplay block. No evidence that these prototype defects explain installed-game lag. Original PRD/G6/mobile hold unchanged.
- Full repository suite44758 completed exit0 (generated/check-r438.log). Standalone baseline30k and existing ASan/UBSan memory callback contract test also pass (40119 exit0; differential-standalone-r438.log). The new opt-in memory probe remains deliberately failing and is not included in that green-suite claim. Installed app/source/save unchanged; no game or booted Simulator found, no live work remains.

## 2026-09-07 — R437 exception policy oracle and reference-source decision

- Prior turn progress: new FP-boundary test exposed a reproducible32k cycle mismatch, not a speedup. Current checkout inspected; no source/bootstrap/module rebuild. Traced actual runtime charge consumption to scheduling/timebase, then pinned Dolphin interpreter and ARM64 JIT fault-exit accounting.
- Source supports charging through the faulting instruction, not unexecuted suffix. Added a separate, explicitly non-equivalence policy oracle using actual helper exception state and independently stated cycle expectations.32000 full-state/RAM/host-flag cases pass; original30000 differential baseline remains green. The broad --fp-boundaries discrepancy is deliberately not masked or made green. Evidence and exact source boundary in PERF R437.
- Decision: do not alter IR to imitate C precharge, and do not change installed C timing without its own scoped regression/integration. No evidence that this rare exception policy explains sustained lag. Next memory/callback boundary corpus, then measured gameplay-block experiment under the full original PRD/G6 priority.
- Full repository suite91262 completed exit0, generated/check-r437.log. No game or booted Simulator found; no product or save changes, no gate promotion. Next memory corpus can reuse actual-header test-memory-mapping-callback.py contracts (callback remap, journal pointer ordering, reservation); no live process remains from this step.

## 2026-09-07 — R436 FP exception timing mismatch reproduced

- Prior turn: progress, user-directed authoritative plan refinement plus verification of R434 suite completion. Inspected current checkout, STATUS/JOURNAL and existing differential harness; no repeated build/tool install or game launch.
- Added opt-in FP boundary corpus using actual runtime FPSCR mode update, all rounding/NI/FP-availability modes and special/random double bits. First run89842 exits1 at disabled-FP exception: same PC0x800, C charges3, IR charges1. Changed diagnostic to enumerate scope without masking any failed field; run66343 exits1 with480000cases/32000failures, all downcount-only. Source explains C basic-block precharge versus IR per-instruction charge/materialization. No assertion that either policy is the required chassis contract.
- Baseline actual-runtime30000cases still passes. PERF records source boundaries/logs and next decision: inspect actual exception/dispatcher timing contract and construct expected-cycle oracle before changing accounting or integrating backend. No speedup, G6 pass, mobile promotion or production mutation. No GalaxyPadRunner or booted Simulator found.
- Full repository regression62530 completed exit0, generated/check-r436.log. Experimental FP-boundary probe deliberately remains failing and separate from the repository green suite; do not hide the discrepancy. No live work remains from this step; next is the timing-contract oracle, not a repeated unchanged probe.

## 2026-09-07 — R435 full-product goal refinement

- User explicitly requested refining the running goal without losing the original PRD or expected iOS/iPadOS appearance. Reviewed current status, PRD objectives/delivery/product requirements and original goal-loop gates. Kept the existing active goal; did not create a competing goal or alter original acceptance criteria.
- Verified R434 fullsuite session18146 terminal exit0, ending `Repository safety checks passed` in generated/check-r434.log. No GalaxyPadRunner or booted Simulator found. No product source, installed app, save or dependency changed; no runtime smoke was needed for this documentation-only refinement, and no new gameplay claim is made.
- STATUS now has one authoritative top plan: bounded execution-subsystem experiment with correctness and measured-block go/no-go checkpoint; actual gameplay improvement required before promotion; original macOS G6–G9 before iPad G10, iPhone G11 and complete shell G12. All original G13–G15 requirements retained. Older pending experiment notes explicitly historical.
- Next technical action: extend the existing real-runtime C/IR differential harness across FP exceptions/modes/special values and memory/callback boundaries, then assess one measured gameplay block before any full-module investment. Park failed helper candidates; no compiler-only scope drift, no premature mobile build or gate promotion.

## 2026-09-07 — R434 C/IR differential execution with actual runtime helpers

- Completion verified R435: full repository suite18146 exited0; generated/check-r434.log ends with repository safety checks passed. This replaces the pending-suite note below.

- Prior turn progress: offline staged ARM64 emitter and standalone synthetic checks passed. Current git/status inspected; pinned/reference files retained. New compiler-derived audit extracts30emitter-referenced fields; all offsets/sizes match standalone and GXRuntime. Full structs differ7608/3528 and cache_control7600/3520, so no whole-struct interchangeability claim. Evidence llvm-layout-r434.json.
- Built only cached dr_backend target, then new probe-llvm-c-differential.py uses actual C and staged IR emitters on the same4public synthetic blocks. Integer/branch/MEM1 load-store/paired arithmetic, every instruction entry ×2000seeded states =30000cases. Full CPUState/PC/downcount/RAM/hostFPflags compared without masks; standalone passes. --runtime links actual GXRuntime CPU/float/exception source and actual3528byte CPUState; same30000cases pass. Only existing nested-comment warning. Logs differential-llvm-r434.log and differential-runtime-r434.log. No Galaxy input, emitted full module or production change.
- Boundaries: finite FP/default mode, FP-enabled, mapped128byteMEM1, fixed LR; not comprehensive exceptions/MEM2/callbacks/SMC/chassis integration or performance proof. Next extend this harness along those boundaries, then a measured gameplay block. Fullsuite started separately; poll returned handle, no rebuild of R433 tools required. Full original PRD/G6 unchanged; no game/Simulator launched.

## 2026-09-07 — R433 staged ARM64 LLVM probe and supported toolchain

- Tail validation: full repository suite92677 completed exit0/check-r433.log; git diff-check passes. Located upstream test_codegen_emit/test_c_execute as the existing C-emission fixture path for the next real C/IR differential harness. No build/test/runtime process remains from this step.

- Prior turn progress: vector hypothesis rejected with actual gameplay evidence. Current status/git reviewed, no game. Pinned CMake requires LLVM19/20, machine only22; Homebrew metadata inspected and versioned llvm@20 20.1.8 installed without default compiler/PATH/product changes.84236 exit0,1.5GB. Verified public bottle SHA b95ac9e58b54a35797d89b3ac9ec2d56411b47d3942851567a6eb169e5dc9c09, Apache-2.0 WITH LLVM-exception; source/version/hash/license in PERF. No game download.
- Added stage-llvm-arm64-probe.py copying public files only to ignored staging. Experimental exact opt-in macARM target; reject nonzero direct-call range tables; unsigned narrow declaration/direct/indirect-call ABI attrs; original sources untouched. Initial stage stopped on duplicated verifier anchor; fixed first-only insertion and fresh staged directory, provenance complete. Existing nested c_cfg/emitter overlays retained; no bootstrap/production pin update.
- Configure33052/build42000 exit0; strict-FP reconfigure/rebuild25590 exit0.16synthetic block generation and standalone expected-value CPU tests pass, including revised cross-chunk return checks and86iteration bounded loop. New check-llvm-arm64-probe.py verifies default rejection/no artifacts, range-table rejection, ARM64MachO, own-body-only16wrappers, mfspr ABI and executable exit0. No cross-chunk body calls. Evidence check-llvm-r433.log, strict build logs and staged/probe-provenance.json.
- Important boundary: standalone DolRecomp CPU, not real Galaxy chassis layout or C/IR differential equivalence. No gameplay/performance/mobile promotion. Next true C-versus-IR block differential harness and runtime CPU/helper ABI audit. Full repository suite started separately; poll returned handle to terminal, no unchanged rerun. Normal app unchanged and no game/Simulator launched.

## 2026-09-07 — R432 vector gameplay result; candidate parked

- Previous turn completed control evidence; completed cooldown handles were not repolled. Fresh thermal0/0 and no game/booted Simulator, candidate signed hash220f6aed verified. Launch33740/PID59889 normal runner671729c6, actual candidate module loaded, no JIT or profiling/compiler interference. LoadState2 via actual menu and initial/final screenshots show same hanging scene/life3/Star Bits4/ambient animation.
- Helper40735 exit0 after10+30s method. Window422173567377541..422203567377541:1653VI55.1Hz/CPU16.660554ms/wallp9920.127209ms/EFB1.232572ms. Compare control1652VI55.066667Hz/CPU16.651327ms: no meaningful gain. Worstcandidate27.099292ms includes9.321871ms idle wait, not smoothness acceptance. Both31thermal0/0, complete/no drops/invalid/reset. Finaltitle60FPS is not the anchored-window metric.
- ExactPID validated/SIGTERM/runtimeexit0/native2249106054/fallback0/smc_failed0/save106e unchanged. No live game/Simulator left. Park vector helper; do not promote its offline4% gain or expand/rebenchmark unchanged. Source remains as R428 tested; installed module/app unchanged, no publication. Full PRD/G6/mobile hold unchanged.
- Next: scoped feasibility of dispatcher-preserving ARM64 LLVM-IR emission on small guest blocks, explicitly solving R426 ABI/target/validation barriers and differential-testing before any fullmodule attempt. No unsafe direct calls or accuracy cuts; no production upstream/toolchain change. This is a structural execution investigation after the helper hypothesis failed gameplay, not a reduced final goal.

## 2026-09-07 — R431 module pair packaged; pre-measurement cooldown

- Tail checkpoint: between-run probe82247 completed exit0 with finalFair readings. No live runtime/cooldown remains. Candidate is ready, pending a fresh thermal check; do not keep polling the completed probe or redo control/build.

- Continuation: cooldown4062 and follow-up69926 exit0, initially Fair; later actual probe returned0/0 before launch. Control runtime30466/PID59555 loaded exact packaged module; CUA only after load log. LoadState2 initial/final views verify matching hanging scene/life3/Star Bits4/ambient movement. Helper89761 exit0, window421879666701833..421909666701833:1652VI55.066667Hz, CPU16.651327ms, wallp9920.181458ms, EFB1.241117ms, dropped/invalid/reset0 and31thermal0/0. Worst34.577625ms includes16.615841ms idle wait; not smoothness acceptance. ExactPID SIGTERM/runtimeexit0/native1810930177/fallback0/smc_failed0/save106e unchanged. No game left. Candidate not launched: post-run thermalFair,60s between-run probe82247 live. Next inspect that probe and thermal recovery, then candidate once, same method. Source regression R428 unchanged. Full PRD/G6 remain active.

- Previous turn was concrete control validation/profile preparation plus verified wait. Re-polled9634, confirmed candidate linker59004 CPU-active, then terminal exit0. Both links complete; original objects/source/profile/module/active marker hash assertions passed. Control unsigned c308b0b1e5229c3be8632bf114de7e4078985ba2290f7add93a7f4c685ad2bc5; candidate unsigned859cd69475691d1f668fcd8a426d9dce647af3b8acc631075e841044a34becde.
- package-vector-pair-r429.py ran63457 exit0. Both module audits verify1322chunks/19SMC/ABI/target/header; isolated app signing/dependency/private-data checks pass. Both runners exactly normal671729c6bb32e29ec44e95dedae0044c9c3d7e64f99736653f3146d806d3d762. Signed controlaa23fc0eb958a5f7658f9e6166dd268f889f8f96c715737c6e7cd7a3404328c0, candidate220f6aed42c6f36d3346e6b00ad1ed812c86b39925af936c9c305f8a92cc414a. Package hashes in vector-build-r429/packages.json; normal app untouched. This is diagnostic package validation, not original active-module app audit or gameplay acceptance.
- No game/Simulator running. Thermal state Fair after ThinLTO, so measurement deliberately not started immediately.60s cooldown probe4062 running to vector-build-r429/cooldown.csv. Next inspect its terminal result/last thermal readings, then run control app using prepared vector-control-r430 profile once recovered. Candidate follows only after control closes. Do not rebuild completed artifacts. Full PRD/G6/mobile hold unchanged; no FPS improvement claimed.

## 2026-09-07 — R430 control link verified, candidate link running

- Prior turn was progress plus a verified build wait. Re-polled SAME session9634; no restart. Control linker58706 was still CPU-active, then exited and the script advanced to candidate. Control unsigned module SHA c308b0b1e5229c3be8632bf114de7e4078985ba2290f7add93a7f4c685ad2bc5. Actual module-info verifies RMGE01/ABI3/CPUABI3/CPUState3528/entry8000403c/two code ranges/19SMC/1322chunks; only libSystem dependency. Candidate object compiled without diagnostics; candidate link remains live in9634. Final input-hash assertions have not run yet.
- Prepared separate ignored vector-control-r430 and vector-candidate-r430 profiles from normal late-good-egg-r422 (Core6, normal accuracy config, Metal), each with its own Pipe. Both slot2 SHA74e453b5 and real save106e rehashed equal. Neither launched. New ignored measure-vector-r430.py validates exact variant/PID/app/profile/slot/save, fronts window, waits10s and records30s prospective VI/thermal anchor. It does not close the game: inspect final scene and promptly SIGTERM exactPID afterward.
- No game or booted Simulator; no original module/app/marker changed. Pending: poll9634 until terminal, verify candidate/build-provenance and original-input hash assertions, run prepared package-vector-pair-r429.py, allow thermal recovery, then one app at a time with LoadState2 visible proof. No gameplay or performance claim from module-info or profile preparation. Full original PRD/G6 remains active; R428 fullsuite latest source validation.

## 2026-09-07 — R429 matched-profile module pair in progress

- Previous turn progress: R428 vector candidate/oracles/fullsuite passed; current dirty tree preserved and no game runtime. Cached accepted R387 module graph inspected with read-only ninja -t compdb -x; actual AppleClang21/O2last/ThinLTO/strictFP/original PGO f39a3ced verified.55GiB free. No regeneration/bootstrap or source mutation.
- Ignored audit-vector-build-r429.py compiles baseline, wrapped control and vector candidate to IR using cached flags/actual includes, stage-local outputs and original float source SHA554149a2.48profiled functions retained in each; control/candidate all function-entry metadata identical. Six helpers' optimized entry counts differ from baseline due to wrapper/inlining structure; this is explicitly a matched diagnostic pair, not automatic installed-build parity. Candidate IR contains one double-lane fadd and fsub; no stale-profile diagnostic (only -c unused with textualIR output).
- Ignored build-vector-pair-r429.py compiles only staged float objects, reuses every original chunk/object/archive and links via isolated response files. Hashes every original link input plus protected source/profile/module/active marker and asserts unchanged at completion. Session9634 is LIVE; last verified linker PID58706 running control at3m15s/~740%CPU/~1.1GBRSS. Script will link candidate sequentially. No error output; do not restart on silence. Current evidence generated/build-vector-r429.log and vector-build-r429/{audit.json,build-provenance.json,control/link.log}.
- Prepared but NOT RUN package-vector-pair-r429.py: makes isolated copies of normal app with each new module, validates module tables/target/header, signs/checks dependencies/private-data exclusion, asserts both runners equal original and original package unchanged. No normal marker change. Next poll9634 to terminal; inspect hashes/logs, then run packaging only after both module hashes exist. Do not launch gameplay while linking; use matching copied late-scene profiles and measure only after host recovers. R428 fullsuite remains applicable; new code is ignored diagnostic tooling only. Full original PRD/G6 and mobile hold remain intact; no promotion or FPS claim.

## 2026-09-07 — R428 paired-vector arithmetic experiment

- Previous turn progress: matched BAT-table reference measurement narrowed the performance hypothesis. Current git/status/journal show preserved dirty tree, no initial runtime. Read reference paired SIMD/single-state/liveness logic and actual AOT paired helpers. Chose an isolated two-lane arithmetic experiment, not speculative state caching or relaxing FP.
- Added unapplied patches/experiments/ps-vector-addsub.inc. Finite inputs use double-lane NEON add/sub; nonfinite values use original functions; force_single/result/classification unchanged. No reference source or installed module edits. New actual-source oracle expanded from320000 to640000 cases (all13^4special combinations plus randombits, fourRN/twoNI modes, aliases) and passes full CPUState/host flags. Source float SHA554149a2 verified. Test added to fullsuite.
- Isolated helper ABBA ~3.06–3.49ns candidate versus6.51–6.88ns warmed controls, not gameplay. Added mutually isolated --vector-add-sub to existing full-kernel oracle with identical input kernel for both sides and staged floatTU only.1440cases/fourbuilds exact digest882be09b1f38e9ad, including faults/yields/interruptions/callbacks/journals. Session71452 exit0/oracle-vector-r428.log. Fullsuite34575 exit0/check-r428.log includes latest640000cases.
- Matched offline kernel CPU-time ABBA33423 exit0:24900.817/24228.860/23827.126/25159.357ns summed means, ratio.959964 (~4% lower). Stronger than helper-only evidence, but THP workload not current Good Egg acceptance or full lag fix. PERF records bounds. No game/module promotion/publication; no game/Simulator left. Next audit isolated module cache/link/PGO provenance for valid same-graph control/candidate, then gameplay measurement if defensible. Do not repeat unchanged helper timings or regenerate everything.

## 2026-09-07 — R427 state-traffic audit and arena-memory isolation

- Previous turn progress: concrete alternate-backend safety audit and actual-header ABI probe. Current dirty work preserved; no initial game/Simulator. Inspected current generated hotspot C, R422 sample addresses and exact saved disassembly. Only16/2273 selected leaves are stack memory; mapping/status traffic is prominent but is not removable-cost proof. R217 callback remap/journal test rerun passes; reviewed R311 rejected cache to avoid repeating it.
- Fresh ignored reference-nofast-r427 profile copies R425 Config/Wii/StateSaves/config.ini, own Pipe. Only Fastmem=False added; measurement helper allowlist extended. Actual source shows BAT-table fallback, not all-fast-path disabling. Runner SHA34e96be1 and slot2 SHA74e453b5 verified. No normal config/app/source/module changed.
- Runtime31495/PID56843 explicitJITreference; platform log before CUA. LoadState2 initial/final screenshots retain hanging scene/life3/Star Bits4, ambient animation.10s warm-up/30s quiet measure helper51945 exit0. Exact window420061904541333..420091904541333 yields59.933333Hz, CPU6.827720ms, wallp9916.709458ms, EFB1.603460ms; dropped/invalid/reset0.31thermal rows0/0. Accuracy-enabled arena reference was6.257124ms; AOT16.683192ms. Large gap persists; not exact cost or memory-subsystem exclusion.
- ExactPID command validated, SIGTERM, runtime31495 exit0; save106e unchanged. No game/Simulator remains. Wholeaudio2underruns/3backlogs includes startup/restore/shutdown. Normal module1fb635f7 rehashed. No gate promotion or shipping speedup; full PRD/mobile hold remains. Next inspect reference/AOT block work and state liveness/helper boundaries, not blind arena port, register cache, or unchanged mapping experiment. Focused regression and visible diagnostic run performed; full product suite not rerun because product source unchanged.

## 2026-09-07 — R426 alternate-backend safety audit

- Previous turn made progress: finished R425 pending runtime, measured accuracy-enabled reference and refined priorities. Current git/process/Simulator checks show preserved dirty work and no game/booted Simulator. Full original goal/G6 unchanged.
- Located actual nested pinned DolRecomp after initial assumed ref/DolRecomp path was absent. Verified fa0cf619. Audited LLVM target gate, helper declarations, CPUState offsets, direct cross-chunk calls and job range wiring. Found concrete Apple narrow-argument ABI mismatch and unconditional LLVM cross-chunk call path; cycle guard is not code validation. PERF records exact source locations and scope. No claim of an observed runtime failure or speedup.
- Added read-only scripts/audit-apple-helper-abi.py: actual CPU header, Apple Clang21, macOS ARM64 and iOS ARM64 Simulator LLVM IR; zeroext assertions for representative u16/u8 helpers passed exit0. Also inspected Homebrew22.1.8, not used for accepted build. No source pins, generated modules or product code mutated; no full regression rebuild needed for this isolated diagnostic.
- Decision: reject simple LLVM target-guard removal/full-module experiment. Next inspect structural state materialization/helper boundaries in existing C-backend gameplay hotspots while retaining mutable-code validation. No game/Simulator left, no publication or promotion. Original PRD/mobile appearance and shell remain terminal requirements.

## 2026-09-07 — R425 precision reference and user-directed goal refinement

- User requested reorientation without reducing the original PRD or expected iOS/iPadOS product. Active goal remains unchanged; STATUS now has one superseding execution plan: substantial AOT execution improvement, macOS G6–G9 acceptance, sequential mobile targets and full shell, then remaining original gates. Historical pending actions are explicitly superseded. Grand Star/save evidence already exists; remaining G6 stability is not a claim that persistence never worked.
- Completed pending reference-fp-r425 measurement: same diagnostic runner34e96be1, slot2, FPRF=True/AccurateNaNs=True, FP/divide exceptions=False, Core4. Previously inspected initial/final views show hanging Mario/life3/Star Bits4. Helper41609 exit0. Window419263739438583..419293739438583:1798 VI/59.933333Hz, CPUmean6.257124ms, wallp9916.712042ms, EFBmean1.563234ms; invalid/reset0. Accuracy options do not remove the large gap versus R424 AOT16.683192ms. This is not full FP equivalence, AOT coverage, all-scene proof, or a product speedup.
- Runtime was left open after the bounded measurement; VI buffer reports5741 dropped later events. Retained requested window remains analyzable, but no dropped0 or whole-run acceptance claim. Exact PID55947 command validated, SIGTERM, runtime27460 exit0. Real save SHA106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90 unchanged. No booted Simulator. Whole-run audio1underrun/2backlogs includes startup/restore/shutdown, not matched audio acceptance.
- Documentation-only refinement; no product code, dependency, normal app, module, source pin or goal gate changed. No rebuild/regression rerun warranted for prose; R423 suite remains latest applicable source validation. Next: bounded AOT execution/code-generation audit, choose one high-impact hypothesis with semantic proof before costly builds. No speculative LLVM ARM64 switch, JIT shipping workaround, or fractional experiment loop.

## 2026-09-07 — R424 matched AOT/JIT reference and precision-boundary audit

- Prior turn progress: reference selector tested, isolated runner built/audited and profiles prepared. Current state/git/no-game/no-Simulator and exact signed34e96be1/slot2 SHA74e453b5 checked; previous fullsuiteR423 terminal0. Full PRD/G6 remains active. No build/source/bootstrap/profiler during measurements.
- AOT runtime75083/PID55244: same diagnostic runner, reference-aot-r423 profile, explicit trace/module overrides unset, LC1, VI-only, no JIT flag. LogStaticRecomp/module loaded before CUA. States→Load2 visibly restored darker-side hanging scene/life3/Star Bits4: first reload proof of new fixture. Helper70485 validatesPID/profile/frontmost,10s warm-up then30s thermal/VI anchor. Final screenshot same scene/ambient advancement, no control-route acceptance. ExactPID SIGTERM/exit0/native2340568939/fallback0/smc_failed0/save106e unchanged.
- AOT window418462854253750..418492854253750 gives1652VI55.066667Hz, CPU16.683192ms, p99wall20.068375ms, EFB1.222187ms; worst23.555292wall/22.156834CPU. Complete/dropped0/invalid0/reset0. No concurrent game before reference launch.
- JIT runtime91196/PID55427: identical diagnostic runner/profile clone with GALAXYPAD_MACOS_REFERENCE_JIT=1. Log explicitlyJITreference, existing window inspected only after platform/CPU selection (no StaticRecomp module-load log expected in this mode). Same Load2 visually restores scene. Helper40245 exit0, same10+30s method. Final screenshot retains hanging scene/life3/Star Bits4. ExactPID SIGTERM/exit0/save106e unchanged/persistedCPUCore4. This is actual JIT execution, never AOT coverage or mobile acceptance.
- JIT window418671414513958..418701414513958 gives1798VI59.933333Hz, CPU6.048078ms, p99wall16.735625ms, EFB1.543843ms; worst16.918542wall/5.321958CPU, mean throttle10.347942ms. Complete/dropped0/invalid0/reset0.31thermal samples each nominal-or-unsupported/lowpower0; not frequency/load equivalence. Large CPU-subsystem gap supported in this stable scene; one-order/no-motion test does not explain every reported slowdown or prove whole-game correctness.
- Follow-up source audit: default FPRF=False/AccurateNaNs=False versus stricter AOT helper contract; AccurateFmaddsTrue. Packaged RMG.ini has noCore override. Do not call entire10.64ms CPU difference compiler overhead. Prepared separate reference-fp-r425 with FPRF/AccurateNaNsTrue and matching slot/save/Pipe. Initial plan included FP/divide exceptions; actual ARM64 emitters force interpreter fallback for those settings, so reverted only those disposable-profile options to explicitFalse before any run. Next precision-enabled reference window, not a rebuild/normal config change; not full semantic parity.
- Evidence reference-aot-r423/reference-jit-r423 logs/VI/summaries/anchors/thermal and ignored measure-reference-r424.py. Whole audio95/6versus2/2 has unequal runtime/startup/restore scope, not audio acceptance. R423 suite applies to unchanged product source; git diff-check passes. No game/Simulator remains; no deletion/publication/promotion and full PRD/mobile hold intact.

## 2026-09-07 — R423 FPRF coverage screening and isolated CPU-reference build

- Previous turn progress: late profile, checkpoint and unsafe-codegen guard completed. Current journal/status/git/process/Simulator state checked; no game/Simulator. Memory quick search found no GalaxyPad entry. Full original PRD remains active/G6 unmet.
- Ignored read-only fprf-coverage-r423.py joins current strict-site audit to R422 CPU leaves:164sites/68chunks/5065whole-chunk samples (17.573%); not site cost. Exact signed-module asm:80018C04 classification main5samples, zero/subnormal0; fourteen-site804B6128..615C chain0inclusive CPU-stack samples including helper callers. Detailed addresses/PC materialization in PERF. Park extension: no evidence for high-impact savings, no module rebuild or claim that zero samples means zero execution.
- New bounded diagnostic hypothesis: compare AOT and Dolphin ARM64 JIT in the same isolated macOS runner/settings/scene to distinguish shared runtime/host headroom from AOT execution cost. This is reference evidence only, never mobile/product acceptance. Inspected runtime's hardcodedCPUCore, actual enum/JitInterface and state-cache clearing. Cross-core checkpoint compatibility remains runtime-unverified.
- Added unapplied patches/experiments/macos-reference-jit.patch against source053fa6748d38014b068cb0c097878221d722f226d9cb864d57d1d734f4db5cdb. Exact flag1 choosesJITARM64 only AppleARM64 nonIOS; default/other targets retainStaticRecomp. Logs diagnostic identity, no clocks/EFB/timing policy modified. Initial minimal-context patch failed check; proper surrounding context fixed it. No reference source mutation.
- Actual-selection extraction test temp-applies/reverses pinned patch and compiles actual CPUCore enum plus selector under ASan/UBSan.24platform/flag cases pass. Initial simulated nonApple compile undefining __APPLE__ before libc++ headers failed SDK availability checks; moved test-only undef after system includes. This emulates selector condition, not a cross-platform build. Test97309 exit0 added to suite.
- Ignored build-reference-jit-r423.py extracts cached Ninja compile/link commands, compiles only isolated dolphin_runtime.cpp, replaces its unique member in copied libmoderngekko.a, links new runner and verifies all original input hashes. Build50456 exit0; unsigned7d84ed9c...257b. Copied normal app to isolated reference-jit-r423, replaced runner and ad-hoc signed: signed34e96be1...e09df/module1fb635f7. Audit68742 exit0; fullsuite7572 exit0/check-r423.log; normal runner671729c6 and reference source053fa674 rehashed unchanged.
- Prepared separate reference-aot-r423 and reference-jit-r423 Config/Wii/config.ini/Pipe/StateSaves-slot2, no original data changes. No launch yet. Next AOT-off control on this diagnostic binary, visibly reload slot2 and measure stable scene; terminate before explicitJIT-on comparison. Verify selected core, warm-up, scene/input/save and render state; JIT result cannot close AOT/mobile gates. No game/Simulator remains, no publication or goal promotion.

## 2026-09-07 — R422 late-route CPU profile, build safety guard and bounded pivot

- Prior turn progress: normal-app gameplay measurement established57.73Hz, late CPU deficit and real57ms hitch. Read current journal/state; no initial game/Simulator,56GiB free. Fresh late-good-egg-r422 Config/Wii/StateSaves/config.ini and own Pipe; normal runner671729c6/module1fb635f7, exact arrival slota30fd3ed. Full PRD/G6 stays active.
- Runtime95008/PID51789 normal native1x/dual core/VI-only with LC byte/pair1 and no diagnostic background-input flag. Module loaded before CUA. Load1 visibly verified; exactPID frontmost plus six-repeat42.8s input39945 exit0. Post-input screenshot shows darker-side rim, Mario hanging/life3/Star Bits4. This is scene verification, not deterministic trajectory or benchmark.
- Saved unused slot2 via observed States→Save→Save State2. Immediate hash was premature; later file confirmed SHA74e453b5fd20b6a603a9811dfb363c9b66903662bda83971c558f57493adfe01, no duplicate save. Reload pending. CPU Profiler36955 requested10s, exit0; xctrace export24332 exit0; summary28822CPU-thread samples/Run3711/dispatch1256/804B60A01143/800180A01130. Save request precedes capture but no claim of exact activity isolation. Same broad hotspot mix as R419; stop unchanged profiles.
- ExactPID graceful close95008 exit0/native3216200603/fallback0/smc_failed0/save106e unchanged/VI dropped0. Whole111underruns/6backlogs not audio acceptance. Profile artifacts in late-good-egg-r422, no game/Simulator left.
- Broader build audit: O3/Oz/blanket-inline/Gateway-PGO already tested/rejected; top guest chunks have nonzero large original PGO counts. Primary upstream discovery led back to pinned pipeline/emitter: DOLRECOMP_UNSAFE_DIRECT_CALLS=1 bypasses chassis code-validation boundaries. Pinned LLVM implementation explicitly rejects non-x86-64 Linux/Windows, so no speculative ARM64 backend switch or dependency change.
- Found supported generation/build preflight inherited unsafe option without rejection and codegen cache does not identify it. Added simple early rejection in generate-aot.sh (also reached by build-module.sh preflight), not a runtime optimization. Actual-entrypoint tests cover generate/preflight/module × unset/empty/0/1 with deliberately absent tool and verify no output creation. Focused test and fullsuite41653 exit0/check-r422.log; git diff-check passes. Accepted app/reference sources unchanged.
- Current read-only audit-fprf-regions.py21819 exit0:164remaining strict adjacent sites/68chunks, including23 in top guest804B60A0 and16 in804B50A0. Next quantify profile coverage before considering extension of existing accepted first-writer policy; do not infer gain from static eligibility or relax observer/FP/exception/entry contracts. No speedup, mobile promotion or goal-completion claim.

## 2026-09-07 — R421 normal-app foreground gameplay and late-route CPU deficit

- Tail check: worst interval57.276458ms at+1.162s, CPU38.383125ms/EFB2.270958ms. This actual hitch remains open independently of the sustained late-route deficit;57.73Hz average is not smoothness acceptance.

- Prior turn progress: offline binary attribution/tests completed and duplicate host-loop lane rejected. Current status/journal/git state and no game/Simulator verified; full PRD/G6 unchanged. Fresh ignored normal-good-egg-r421 copied only R418 Config/Wii/StateSaves/config.ini, own Pipe. Normal runner671729c6/module1fb635f7 and slota30fd3ed rehashed. No build/bootstrap/profiler.
- Runtime98500/PID51282 normal runner --game generated/extracted/run1 --user-dir normal-good-egg-r421 --graphics Metal --no-mods, LC byte/pair1/VI-only, module/phase/EFB trace overrides unset. Module loaded after~45s startup, before CUA. States→Load1 visibly restored arrival. Exact-PID System Events frontmost then short walk50429 exit0 visibly moved Mario toward house (damage3→1). This establishes working default foreground input, not a background-input fix.
- Restored Load1 through observed native menu; screenshot verified life3/arrival/Star Bits1 again. Ignored measure.py validates exact PID/profile, activates frontmost, anchors C++ steady clock, runs established42.8s six-repeat movement/jump/Spin sequence, then announces60s. Helper5353 exit0. No UI during window; only brief read-only thermal/process checks. Final screenshot before closing showed Mario hanging on darker-side rim, life3/Star Bits2. Same fixture but not deterministic route equivalence to R419 Star Bits6.
- Exact window416115332054833..416175332054833:3464VI57.733333Hz, meanCPU16.022547ms/wall17.323149ms, wallp9518.842ms/p9920.41575ms, EFBmean.571624ms; dropped0/invalid0/reset0. Five-second bins55.8–60.4Hz; final15s56.0–56.4Hz with CPU17.037–17.200ms. No sustained20–40FPS reproduced, and no evidence normal app is dramatically worse than diagnostic. Different trajectories/thermal context prevent causal A/B. Two thermal snapshot rows nominal/unsupported and low-power0 are not continuous thermal/frequency proof.
- Validated exactPID then SIGTERM; runtime98500 exit0/native3681150059/fallback0/smc_failed0. Save SHA106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90 unchanged. Persisted BackgroundInputFalse; whole input296button samples/12transitions/final0; whole76underruns/13backlogs not window audio acceptance. No runtime/Simulator left.
- Evidence normal-good-egg-r421/{runtime.log,vi.csv,summary.json,bins.jsonl,measure.py,start-ns.txt,input.log,thermal-snapshot.csv}. Previous R420 fullsuite applies to unchanged source; no redundant build/suite. Next short CPU profile of normal app's late darker-side workload, not the earlier transient21–31s profile, to identify scene-dependent CPU cost. Full PRD/mobile hold preserved, no performance-fix or acceptance claim.

## 2026-09-07 — R420 exact runtime attribution and duplicate-lane rejection

- Completion: suite31955 exited0; generated/check-r420.log ends Repository safety checks passed. Focused binary-selection/provenance test and git diff --check also pass. No game/Simulator left. Next remains normal-app Good Egg comparison, not another build.

- Previous turn: progress through completed R419 profile interpretation and recorded goal refinement, not a speedup. Current authoritative source, profile, runner hash and no-game/no-Simulator state checked. Full PRD remains active, lowest unmet G6.
- Extended scripts/summarize-cpu-profile.py with explicit --binary and address_binary provenance; default game-module filtering retained. Synthetic tests verify runner-relative offsets/ranges, wrong-binary exclusion, raw-PC alignment and unchanged CPU leaf totals. Focused test passes. No runtime/product source or normal package change.
- Disassembled exact diagnostic Run at0x100239da4; CLI runner-relative ranges yield3527/3549 Run leaves across trace/lockstep234, sampling guard202, dispatch/REL/counter528, cycle/timebase289, idle check529, exceptions269, backedge policy774, chunk validation702. Saved runner-run.asm, runner-summary.json and runner-ranges.json under existing good-egg-profile-r419. Sample skid and nonexclusive microarchitectural effects prevent interpreting these as removable instruction costs. Timebase /12 already uses multiply-high, not variable divide.
- Exact module offsets also inspected: chassis_dispatch table/load/epilogue and entry-switch/FP checks within top generated guest chunks. First named objdump request omitted Mach-O underscore and returned no symbol; bounded address disassembly succeeded. No rebuild warranted by that tool naming error.
- Historical R139/R140 already covered equivalent host-loop/PC-frequency hypotheses. Therefore reject another unchanged dispatcher micro-optimization loop. Next normal installed app on same Good Egg checkpoint with verified foreground input, matching settings and bounded unprofiled window, to reconcile diagnostic library-graph differences before choosing a fix for user20–40FPS. Do not infer normal parity from similar Run prologues or infer a cause from one instruction sample.
- Full repository suite running as31955, log generated/check-r420.log; no bootstrap or game while it runs. Focused changed test passes. No new gameplay smoke this offline-only step; R419 remains last runtime evidence and is not normal-app performance acceptance. No deletion/publication/promotion.

## 2026-09-07 — R419 gameplay CPU profile and user-directed goal refinement

- Goal remains all original PRD/G0–G15, lowest unmet G6; macOS performance and stability precede mobile. Re-read original PRD/goal loop and current evidence. Refined STATUS without weakening acceptance or changing the original requirements. No product edit, build, publication or gate promotion.
- Fresh good-egg-profile-r419 uses diagnostic runnerb4df2afa/module1fb635f7, exact arrival slota30fd3ed and save106e, Metal1x/dual core/explicit --background-input and VI telemetry. Runtime20657/PID49717; visually verified arrival reload, then established42.8s movement/jump/Spin fixture30978. CPU Profiler35881 attached for requested10s about21s into input. Input and capture exit0; post-input screenshot showed Mario on darker side of planet and Star Bits1→6, confirming gameplay beyond arrival, not continuous-route or performance acceptance.
- Exact-PID graceful runtime exit0; native3052269158/fallback0/smc_failed0; VI export saved/dropped0. Whole-run79underruns/6backlogs are not window-scoped audio acceptance. Save SHA256106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90 unchanged. No game/Simulator remains.
- CPU capture saved and exported with xcrun xctrace export cpu-profile schema; scripts/summarize-cpu-profile.py produced summary.json. Evidence generated/runtime/good-egg-profile-r419/{cpu.trace,cpu.xml,summary.json,capture.log,input.log,runtime.log,vi.csv}. 29,266 CPU-thread leaf samples: StaticRecompCore::Run3549 (12.1%), chassis_dispatch1285 (4.4%), func_804B60A01115, func_800180A01076.119 samples on E cores, remainder P cores. Leaf shares do not prove exclusive costs or attainable speedups; profiler overhead means no FPS A/B claim.
- Next inspect exact sampled runtime/dispatch offsets and call paths, then select one falsifiable gameplay hypothesis. R418's56.25Hz is a real diagnostic deficit but not sustained user20–40FPS reproduction. Diagnostic library graph differs from normal app; require normal-app reconciliation and matched unprofiled gameplay before promotion. Keep fractional movie candidates parked.
- Refinement explicitly preserves full iPad/iPhone visual, touch, pointer, Spin/tilt and complete three-dot menu acceptance after macOS gates, plus physical/clean-clone/rights requirements. Documentation-only check: git diff --check; R416 full regression applies to unchanged product code. No additional boot/build for this planning-only update; R419 supplies latest gameplay evidence, not a new acceptance claim.

## 2026-09-07 — R418 bounded Good Egg gameplay window

- R417 made functional progression/input-policy progress, not performance acceptance. Fresh good-egg-r418 copied isolated background-r416 Config/Wii/StateSaves/config.ini, regenerated independent Pipe. Exact slota30fd3ed/save106e verified; diagnostic signed runnerb4df2afa/module1fb635f7 rehashed. No initial game/Simulator. Runtime89103/PID49349 explicit --background-input, Metal1x/dual core/LC paths1, VI-only/no module or trace overrides. Actual module load before CUA; slot1 visibly reloads Good Egg arrival, first reload evidence for fixture.
- Ignored measure.py validated exact PID/profile, created prospective C++ anchor then ran established42.8s movement/jump/Spin fixture, announced60s window end and automatically sent graceful SIGTERM at75s. Helper23885 and thermal16593 exit0, runtime89103 exit0. No UI during window/no profiler/compiler. Post-window screenshot was not obtained before automatic close: initial scene/reload is visible, input completion285button samples/12transitions and finalbuttons0 are confirmed, but actual final movement/camera/health is unverified in this run. Do not describe as complete visual gameplay-route proof.
- Window414851447214375..414911447214375:3375VI56.25Hz; meanwall17.780783ms/CPU16.127178ms; wallp9519.787375/p9920.885375; EFBmean.883000ms/p991.703496ms. Worst26.886708wall/23.516458CPU/1.031375EFB. No dropped/invalid/reset samples and complete boundaries.61thermal samples Fair; not frequencies. Whole-run81939color reads/14354depth reads identify broader readback behavior than Observatory, but not their exact-window counts or caller attribution. Timers overlap and are not a disjoint CPU/wait accounting.
- Native2878817799/fallback0/smc_failed0/save106e unchanged. Whole81underruns/5backlogs include startup/restore/close, not exact-window audio acceptance. Evidence good-egg-r418/{vi.csv,runtime.log,start-ns.txt,input.log,summary.json,thermal.csv,measure.py}. New scene shows sustained below60Hz in this diagnostic run, not reported20–40FPS reproduction or speedup. Diagnostic library graph differs from normal runner, so no normal-app comparison claim.
- Next10s bounded CPU profile on same Good Egg fixture during control input, using existing small CPU Profiler path and exact module attribution; identify dominant non-movie gameplay cost before selecting a change. No heavy SystemTrace or unchanged helper candidates. FullsuiteR416 applies to unchanged product code; measure helper syntax/git diff-check pass. No runtime/Simulator left, full original PRD/mobile hold intact.

## 2026-09-07 — R417 explicit input policy works; Good Egg entry and movement

- R416 made implementation/build/test progress. Verified no game/Simulator, signed diagnostic runnerb4df2afa and slot5f3368150; reused prepared background-r416 with --background-input explicitly. Runtime45157/PID48574 logged opt-in then actual module1fb635f7 loaded before CUA. Normal app/reference source unchanged; no profiler/compiler. Initial INI stillFalse while running is not effective-state proof because SetBase is persisted on shutdown.
- Restored Terrace slot5 and advanced tutorial via Pipe. Three-tap fixture did not finish every dialog; extra observed A completed it. Pointer remained present across selection; first spatial selection hit an unknown/locked target, which correctly reported unavailable/not enough Stars. Bounded changed aim points eventually unlocked and selected Good Egg; confirmed Fly to This Galaxy at(.37,-.76), then mission star at(0,-.20). Visibly launched Dino Piranha and arrived at its starting planet. This is actual new interaction/scene-boundary evidence, not fresh-save story completion or all-depth acceptance. Input handles65261/1175/36302/69944/35366/57133/9581 terminal0.
- Saved new slot1 using exact-PID native accessibility menu after CUA's changing menu indices selected the wrong app menu. System Events menu inventory verified States→Save→Save State1 before clicking, no blind keyboard save. Private checkpoint49899232bytes/SHAa30fd3ed3f39e1978e2b726f9f09a9d0d723f67bc8edb3845844ff857198669b; reload NOT tested. Then forward fixture85992 exit0 visibly moved Mario near house/enemy; life fell3→1, so not damage-free route or complete control proof. Checkpoint precedes movement, real NAND save106e remains unchanged.
- Exact-PID graceful45157 exit0/native11086903584/fallback0/smc_failed0. Whole-run750button samples/64transitions; IR visible136246/136246 samples; finalbuttons0. Persisted BackgroundInputTrue verifies explicit host option survives runtime configuration, unlike INI-only previous attempts. Task screenshots show continued pointer and real selections; this is functional evidence for diagnostic input policy, not proof of every focus/lifecycle scenario or normal background-input behavior.
- Functional session~759s; VI buffer dropped27294, whole309underruns/23backlogs. No full-session FPS or audio acceptance; screenshot59–60FPS titles are individual frames, other title snapshots dipped. Preserve original logs/VI/input scripts in generated/runtime/background-r416. Next fresh short Good Egg movement window with prospective anchor/automatic end, verify checkpoint reload; investigate slow frames there if reproduced. No more template tracing or micro-optimizations without slow scene evidence. FullsuiteR416 applies to unchanged code; no game/Simulator left; full PRD/mobile hold unchanged.

## 2026-09-07 — R416 explicit runner background-input diagnostic

- Completion checkpoint: fullsuite23060 exit0/Repository safety checks passed. All build/audit/test handles terminal; no game/Simulator. Next actual diagnostic run, not another build or test-only loop.

- Prior turn progressed by finding host override and preparing opt-in; current source967accdf/normal runner671729c6 rehashed unchanged, no game. Added tests/test-runner-background-input.py: exact source pin, patch apply/reverse in temp, actual complete runner option parser compiled with real RuntimeConfig under ASan/UBSan. Default-off, exact/repeated opt-in, option-as-value boundary, malformed flag/missing value, help and interpreter independence all pass. Added test to repository suite. No prepared reference or normal product source edited.
- Isolated build recipe generated/build-background-runner-r416.py reads Ninja's real commands, copies/patches only runner source, substitutes one object/output, retains current cached desktop library graph and hashes every linked archive/object before/after. Initial guard failed before compile because object occurs both -MT and -o; preserved first directory/log, corrected exact two positions, new r416b output. Build25819 exit0, unsignedd62f65272510ee34a80e8766318d0b60c4f4c588fbc0e80ba9e9ebc06470bd62. Existing duplicate-library warnings retained. No bootstrap, canonical source change or normal app overwrite.
- Copied normal app to generated/macos/background-input-r416/GalaxyPad.app, replaced only runner, signed/audited package61696 exit0. Signed runnerb4df2afa27efeaf964e92d24b6d9c5d916d9b920d50316e62d5a74f8646bbb21; module1fb635f71b8ca6afab01fccb1b06a0a5e2b6ddd5a1aec4bb8ef629af3201dc7a unchanged. Real executable --background-input --help exits0, logs explicit enable and advertises default-off. This uses cached diagnostic runtime libraries, so it is not identical to normal runner and not an FPS A/B candidate.
- Fullsuite23060 running generated/check-r416.log. Prepared independent background-r416 profile with same Config/Wii/slot5 and regenerated Pipe; no live game. Next suite terminal, then actual opt-in runtime: verify host-config log, load checkpoint, advance tutorial and pointer selection while automation focus changes, release input/cleanexit/check save and persisted policy. No performance claim, normal app/default/source untouched; full original PRD/macOS-first mobile hold intact.

## 2026-09-07 — R415 Terrace continuation and host input-policy override found

- R414 progressed by reaching real-save gameplay, not fixing sustained lag. Read authoritative status/journal/fixtures and no game/Simulator. Existing R414 whole trace5s bins (relative firstVI, excluding incomplete tail) lowest57.6Hz at+85s/CPU15.916ms/EFB.456ms; no5s20–40Hz reproduction, no precise scene attribution. No new profiler or benchmark.
- New terrace-r415 profile copied R414 Config/Wii/config.ini, independent Pipe and exact R361slot5f3368150bfc401ed2758ebf39a86995b93d055aac6bbcfb8fc2c6fcbef1e244d. Real save106e differs from R361's5040 fixture; savestate continuation is not fresh-save progression/persistence evidence. Runtime60494/PID46841 normal installed module, Metal1x/dual core/VI-only. Actual module load before CUA; slot5 reload visibly restores galaxy-selection tutorial, first reload evidence for that fixture. Three A prompts advanced tutorial; New galaxy screen appeared. Later A/aim input did not preserve pointer visibility and screen requested pointing; do not claim galaxy selected or entered.
- Found current profile BackgroundInput=False despite copying previous purportedTrue profile. Source-level cause: RuntimeConfig::Input background_input defaultsfalse (runtime.hpp); moderngekko_run never sets it; dolphin_runtime.cpp explicitly SetBase(MAIN_INPUT_BACKGROUND_INPUT, impl->config.input.background_input). INI edits alone are not an authoritative persistent policy. Correct R414's causal conclusion: successful route/input counters are real, but changed INI did not establish sole-cause control independent of focus/timing. No App Nap/scheduler/FPS cause proven.
- Prepared UNAPPLIED patches/experiments/runner-background-input.patch: explicit --background-input sets existing RuntimeConfig flag and logs opt-in; help documents default-off. Exact current runner source SHA967accdf84004447f7bafb53821ac0f2d86ddfa7e6971df15668b7dcef9735a7; git apply --check and diff-check pass. No normal source/binary/default change. Next focused actual-parser/config regression then isolated runner integration/build and live button/pointer checks. Do not change normal background-input policy or launch another INI-only test.
- Exact-PID clean stop60494 exit0, vi dropped0, save106e8248 unchanged; whole-run49underruns/9backlogs not window or audio acceptance. Runtime and input evidence generated/runtime/terrace-r415, screenshot progression in task. No game/Simulator/recorder remains. Full original PRD/macOS-performance-first mobile hold intact.

## 2026-09-07 — R414 input gate comparison; moving Observatory baseline

- R413 reoriented priorities at user request; this turn executed its input-confound check. Normal runner671729c6/module1fb635f7 rehashed unchanged, no initial game/Simulator. Reused disposable scheduler-r413 Config/Wii; save106e8248 unchanged. Runtime93351/PID45991, VI-only, no module override. Waited module-load, AX-clicked window and verified actual title before input64430. Still title afterward; cleanexit0/native1725943902/fallback0/smc_failed0, zero button samples/transitions. Window-raise alone is not sufficient proof of actual input focus. Prospective title window412305599312625..412365599312625 gives59.7VIHz/CPU10.4311ms/p99wall22.35375; not gameplay or sustained20–40FPS reproduction. Early thermal probe nominal-or-unsupported, not no-throttling proof.
- Source: PlatformMacos.mm ProcessEvents derives focus from isMainWindow; VideoInterface.cpp calls UpdateInputGate with !BackgroundInput; Core.cpp and ControlReference.cpp gate actual expression values. Changed exactly BackgroundInput False→True in this disposable profile only. Normal user profile/defaults remain unchanged. Runtime71722/PID46269 with same app/module/settings/save; title readiness visually verified before identical full input84569. Real-save Observatory visibly reached; input counter389button samples/18transitions over whole run. This supports input-gate interference in automated testing, not a universal lag fix.
- Prospective gameplay anchor412600874427708; six-repeat existing control fixture23789 exit0 (~42.8s), thermal87650 exit0 (60s), no profiler/compiler/UI during60s. Subsequent screenshot shows Mario moved to railing. Window ends412660874427708:3597VI/59.95Hz, CPUmean15.1955166ms, p95wall17.323583/p9918.362583ms, EFBmean.533296ms; complete/no invalid/reset/dropped0. Thermal all61samples Fair; no frequency inference. Individual jump/Spin outcomes not separately captured, and final railing position means this is not60s of continuous locomotion.
- Graceful runtime71722 exit0/native3340802548/fallback0/smc_failed0/save106e unchanged. Whole-run7underruns/7backlogs include load/close and are not window audio acceptance. Evidence in scheduler-r413: runtime-background-r414.log, vi-background-r414.csv, gameplay-start-r414.txt, gameplay-summary-r414.json, gameplay-thermal-r414.csv, prior false-setting run retained. Focused Pipe/macOS-config tests and git diff --check pass; fullsuite9724 from R412 applies to unchanged product source. No game/recorder/Simulator left.
- Read normal Application Support profile: Metal,1x,CPUThreadTrue,CPUCore6,640x528,BackgroundInputFalse, so no obvious higher-resolution explanation for user slowdown. Next advance beyond short Observatory route and target user's actual slow scene/window context; optional async question sent, not blocking. Do not infer a performance fix from background-input setting, replace normal defaults, or resume large scheduler captures without a slow reproduction. Full original PRD/mobile hold intact.

## 2026-09-07 — R413 short loading-transition scheduler capture

- User explicitly requested reorientation while preserving the entire original PRD and expected iOS/iPadOS product. Added authoritative active priorities at top of STATUS: sustained user-visible20–40FPS first, dominant-cost diagnosis, materially useful fix and same-route verification, complete original macOS gates, then full mobile/shell/device/release gates. Fractional CPU experiments and rare hitch tracing no longer default priorities. Original goal stack and acceptance criteria unchanged.
- Runtime14224/PID45654 loaded normal module; two select-prefix sequences56772/50863 exit0 but screenshots remain title37.4/43.2FPS, not selected save. First readiness explanation was unproven and second attempt refuted it as sufficient. Actual Config/Dolphin.ini has BackgroundInput=False; screenshot window inactive. Input ownership/focus is next concrete confound to resolve, not a claim that App Nap or background state caused all lag. No trace/clock capture began. User reorientation interrupted the planned capture; exact-PID graceful shutdown14224 exit0. Preserve runtime/input evidence; no app/source/default change.

- R412 made diagnostic/tooling progress, not game-performance improvement. Revalidated no game/recorder/booted Simulator,58GiB free, unchanged normal runner671729c6/module1fb635f7 and real save106e8248. New scheduler-r413 profile copies only Config/Wii/config.ini, independent pipe; no savestate restore.
- Split the existing g6-observatory-save-load fixture immediately before its final A confirmation. Select file first, visually verify confirmation, then begin90s C++clock bridge and10s standalone Thread State Trace with existing storage watchdog; confirm load once recorder reports started. Capture is intrusive and may itself create stalls. Actual retained metadata/clock coverage/VI bounds and exact CPU thread identity are required before classification. No cadence/correctness/scheduling changes or concurrent compilation.

## 2026-09-07 — R412 scheduler capture failed safely; narrow instrument preflight

- Preflight85054 terminal54 (launched-process recording), log explicitly confirms saved output. Trace29MiB, retained1.353857s/no discarded prefix; thread-state export67171 exit0/684001bytes. Includes actual rows, but TOC still contains syscall/VM dependencies and rows include other processes despite target SINGLE. Do NOT claim minimal-event isolation, game-scale storage savings, or performance improvement. Added explicit --thread-only selection to existing capture watchdog, preserving previous default/guard behavior; focused command/clock/storage and retention tests pass. Next10s clock-bracketed loading-transition game capture with this selection and existing watchdog; no unchanged30s full-template retry. All processes terminal; original goal remains open.

- Previous status-only answer was no progress; resumed authoritative process/artifact checks. Runtime89277/PID43903 cleanly exited0 before replacement. Correction: initial title run actually exported dropped0 (16071 lines), not a proven recorder overflow; it had no prospective measurement anchor and is not performance evidence.
- Normal runner671729c6/module1fb635f7 rehashed unchanged; exact slot1 4a421726 and save106e8248 verified. Replacement57208/PID44238 used scheduler-r411 profile, VI-only vi-r412.csv, no module override. Actual module-loaded preceded CUA; slot1 visibly restored central Observatory. Full suite9724 exited0 before recording/input began; no compiler overlap with capture.
- Clock bridge97050 (90s), recorder83740 (30s explicit retention), active-input99969 all terminal. Input exited0 and post-input screenshot visibly shows movement. Recorder reached time limit but finalization exceeded observed-volume-growth guard: initial65730301952/min59418517504 free bytes, child exit-9 after93.652s, wrapper exit1. Trace lacked form.template; retention audit fails closed. No scheduler/VI correlation or unprofiled performance claim is possible. This is failed diagnostic evidence, not a performance improvement.
- Runtime57208 cleanexit0/native2790288629/fallback0/smc_failed0, save106e unchanged, vi-r412 dropped0. Clock/input/log/watchdog/VI artifacts retained in generated/runtime/scheduler-r411. Verified recorder/game terminal before deleting exactly its incomplete system.trace (3.3GiB logical, not recoverable from this cleanup); filesystem reclamation may lag. No other temp directories or evidence deleted.
- Pivot: local xctrace help permits --instrument; installed Apple SystemTrace package documentation names standalone Thread State Trace, distinct from System Call Trace and Virtual Memory Trace. One-second disposable /bin/sleep preflight85054 tests standalone recording/export feasibility before any game rerun. Do not repeat full System Trace or change scheduling policy based on these failures. Full PRD/macOS-performance-first mobile hold remains; no app/source/default changes.

## 2026-09-07 — R411 scheduler alignment and capture safeguards

- Completion checkpoint: fullsuite62416 exit0/Repository safety checks passed. All handles terminal; prepared diagnostic is next, not another suite/build. No game/Simulator left running.

- Prior turn progressed by rejecting sub-percent paired-read candidate. Read current STATUS/no game,61GiB available and R230–232/R305–307 evidence. Old scheduler relative timestamps require retained_start_unix, not recording start; prior polling RUNNING does not distinguish runnable/on-core and pauses with the game. A new diagnostic must fix alignment/retention/storage, not infer a scheduling policy fix.
- Added scripts/clock-bridge.cpp: same C++steady clock as VI brackets system_clock Unix samples,10Hz/bounded1..120s/preallocated rows/export only after capture. ASanUBSan actual-probe argument/epoch/order/duration tests pass. Built generated/clock-bridge; actual1s generated/clock-bridge-r411.csv calibrates to observed37,167ns offset envelope. Added scheduler_clock.py with strict ordered coverage/no extrapolation and default1ms maximum observed envelope. Bounds are observed, not proof against unsampled wall-clock steps.
- Inspected local xctrace record help: time-limit/window controls exist, no byte quota exposed. Added capture_scheduler.py: only GalaxyPadRunner PID/private new generated/runtime .trace target, no append/overwrite;20GiB preflight, explicit matching1..60s time-limit/window,1Hz free-space watchdog stops own recorder on <16GiB free, >4GiB observed volume growth, or seconds+90 timeout. SIGINT then30s grace before killing only its own recorder child. Metadata records stop reason/exit/free-space/elapsed; no auto deletion. This is an observed-space watchdog, not a hard quota; unrelated disk activity can conservatively stop it.
- Clock/storage policy tests pass, added to fullsuite62416 running generated/check-r411.log. Runtime sources/app/defaults unchanged; no bootstrap. Prepared scheduler-r411 profile from R403100 Config/Wii/config.ini with exact R374slot1SHA4a421726 and save106e8248, independent pipe, Metal1x/dual core. No game/Simulator launched.
- Next finish62416, then one bounded30s scheduler diagnostic on normal installed module with VI-only, continuous clock bridge spanning trace before/after and active-input fixture. Audit retained metadata and clock coverage before correlating thread-state intervals. Heavy tracing is intrusive: it cannot establish unprofiled FPS or sole cause of historical lag. Stop/inspect any watchdog abort and do not auto-repeat an unchanged capture. Full original PRD/performance/audio/mobile hold remains active.

## 2026-09-07 — R410 checked-pair movie runtime, candidate parked

- Prior turn progressed: isolated control identity/candidate build/audit passed. Rechecked current STATUS/no game/booted Simulator. Prepared R408 profiles and atomic helper reused, normal671729c6runner explicit unsignedc002 control/56616345candidate, Metal1x/dual core/interval100/LC byte-pair1/VI-only. Actual module-loaded verified before CUA on both; matching pre-attack slot3 visibly restored before helper trigger. No midwindow UI or compiler/other game/Simulator.
- Control93283/PID42144/helper52351 all exit0. Window410085083911291..410145083911291:3411VI56.85Hz, CPUmean17.412765ms, p99wall18.9525ms, EFB0. Candidate2166/PID42315/helper28098 all exit0. Window410270699859833..410330699859833:3430VI57.166667Hz, CPUmean17.313438ms, p99wall19.108917ms, EFB0. Both complete windows/dropped0/validCPU/no counter resets. Thermal before/after allstate1(Fair), lowpower0; snapshots do not prove frequencies, continuous state or host equivalence.
- Native control1032387112/candidate875142940, fallback0/smc_failed0, both savea5743199 unchanged. Final projection190cf1d3666f1e56/4draws/12primitives on both, not exact movie-frame equality or complete visual movie acceptance. Worst window control22.717ms wall/21.713CPU versus candidate27.453/26.283; long80–90ms historical hitches did NOT reproduce in these two windows. Whole startup/restore/close counters are not window audio evidence.
- Decision: PARK566candidate, no promotion/reverse pair or gameplay runs for this sub-percent result. About+.557% cadence/-.570%CPU but p99 worsens; offline2.95% does not establish material game improvement. Prepared R409 gameplay fixtures retained unrun, not acceptance. All runtime/helper handles terminal; normal app/marker/default unchanged. FullsuiteR408 applies to unchanged product source; no game/Simulator left.
- Next distinct problem: historical low-CPU long hitches (R305/R307/R403/R405), not another decoder micro-trim or unchanged thread-policy snapshot. Re-read R230–232 and R305–307: default SystemTrace retained only last10s; corrected60s attempt failed disk-full before cleanup. Polling RUNNING includes runnable and observer itself pauses; host counters may be cached. Before a new bounded scheduler capture, inspect existing clock-pair/window-audit tools and verify contemporary wall/raw/VI alignment plus explicit retention/disk bound. Now-reclaimed space changes the prior storage blocker, but heavy tracing remains diagnostic/intrusive and cannot establish unprofiled performance. Do not alter scheduling policy on speculation; full original PRD/performance/audio/mobile hold remains.

## 2026-09-07 — R409 control identity proven, candidate link active

- Final checkpoint: same29035 exited0 after candidate link, module audit passedRMGE01/arm64macOS14/1322chunks/1024indexed/19SMCranges. Candidate5661634558290b6b2889805ab1c49966a12ce22f9b4228f92d949f2a511479e7 independently rehashed; provenance controlc002/candidate566/identical flags/selectedfalse verified. Normalrunner671729c6 and signedmodule1fb635f7 rehashed unchanged. No active builder/game/Simulator. Next launch prepared R408 movie control with explicit unsignedc002 override then candidate566, same normal runner/VI-only/atomic trigger helper; account for post-build host thermal/order confounds, no promotion from a single favorable window. FixedR409 railing gameplay profiles are ready for regression checks after movie evidence. All prior full-PRD/mobile holds remain.

- Previous turn progressed with isolated builder/suite and verified control-link wait. Revalidated same29035/40732 live at5m56/693.5%CPU, no restart. Control then passed exactc0021b9f hash gate; independently rehashed fresh control and provenance. Only changedchunk1102 compiled with unchanged flags, one expected mismatched profile of34functions recorded. Same build29035 now links candidate through41745 (1m17/718.9%CPU). Finish this handle before runtime/audit claims.
- Prepared s16-pair-gameplay-{control,candidate}-r409 from exact R352Config/Wii/config.ini and only verified slot4SHA406a1cd20209e00852ad3d9a3450e6869c33cdd9a9b8e9dda2abb3d8a1f30e09; independent pipes. Save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6, Metal1x, original fixed railing scene (not easier spawn). No runtime or Simulator launched. Prepared R408 atomic movie profiles remain next after link/audit; gameplay gate follows any promising movie result. Fullsuite5283 already passed, no product source changes here. Normal app/marker/default unchanged; original full PRD/performance/audio/mobile hold remains.

## 2026-09-07 — R408 isolated checked-pair builder

- Latest checkpoint: suite5283 exit0/Repository safety checks passed. Same control ld40732 remains active at4m46/497.6%CPU under build29035; no candidate compile yet and no restart. Finish29035 control hash gate, compilation/link and audit before launching prepared profiles.

- Preparation checkpoint: separate s16-pair-{control,candidate}-r408 runtime profiles copied identical R405100Config/Wii/config.ini/slot3, independent pipes; slot3aea2b7a3/savea574 hashes match. Prepared syntax-checked generated/run-s16-pair-movie-r408.sh with control/candidate allowlist, validated exact PID/profile, existing-anchor refusal and atomic trigger→clock→60s→stop. No runtime launched. Fullsuite handle5283, build29035; control ld40732 verified active at2m19/475.6%CPU. Continue same handles, no repeated link/build.

- Prior turn progressed with full correctness/suite and~2.95% offline kernel gain, not runtime acceptance. Current STATUS, marker, dirty checkout and no game/booted Simulator verified. Added mutually exclusive --s16-pair to existing isolated builder, pinned acceptedc0021b9f/currentheaderda2def/currentCPU05e221 and originalchunkfa455. Other experiment policies unchanged. No bootstrap/global helper/app/default/marker mutation.
- Builder29035/Python40726 live, generated/build-s16-pair-r408.log; new generated/s16-pair-r408 only. Control link40732 verified704.8%CPU/44s under40731. Do not restart: unchanged full-graph relink must matchc0021b9f before candidate compilation/link. Recorded1329object hashes, compiler21/strictFP/O2/savedPGO/ThinLTO settings. Independent response comparison confirms only chunk1102 object differs; compile flags identical. Candidate source SHA f818c07e1dbb96afffda50169d49dd946d28662a439a3ea3130fdbad71970362. Build-provenance.json retains exact inputs/commands.
- Graph parser regression passes; fullsuite started generated/check-r408.log separately (no source-changing bootstrap). Finish same build and suite handles before runtime. Next control identity→single-chunk compile→candidate link/audit→atomic movie/fixed-state gameplay tests, no promotion from offline result. Installed671729c6runner/1fb635f7module remains selected; original full PRD/performance/audio/mobile hold intact.

## 2026-09-07 — R407 checked signed16 pair prototype and correctness

- Final benchmark32926 exit0. Saved-PGO strictFP/ThinLTO CPU-time ABBA summed12pattern means: control24130.576/24163.039ns, candidate23553.004/23314.597ns; means24146.8075/23433.8005 (~2.9528% less CPU). Text229376→212992bytes. Candidate one mismatched profile of34 functions recorded (not hidden); fixture/current chunk identity verified above. This is offline whole-kernel evidence, not real-frame weighting, gameplay nonregression or FPS improvement. Next add isolated builder option for this exact pair transform against currentc0021b9f module/current graph, verify one-chunk-only replacement and flags/source/object identities; retain normal app/defaults. Then corrected atomic movie and fixed-state gameplay checks before any promotion. All test/benchmark handles terminal; no game/Simulator.

- Prior turn progressed with current-module CPU attribution. Rechecked STATUS/no runner/booted Simulator. Read actual pinned cpu.h get_ram_ptr/mem_read16 and old scoped S16/type0 pair contracts. New scripts/s16_pair_psq_load.py alters only the same16 kernel coefficient sites, not global ppc_psq_load. Requires two lanes/type7/scale0/LSQE-or-indexed, bounded mapping sizes and one valid four-byte mapping. Lane0 conversion/assignment precedes lane1 read; every guard failure delegates unchanged original ppc_psq_load_inline, not the parked per-lane fast path. No metadata cached across callbacks, no guest timing/FP relaxation.
- New test-s16-pair-psq-load.py pins current cpu.h7fc14481 and CPU05e221c0, exhaustive65536values/complements×4rounding modes plus GQR/type/scale/W/indexed/LSQE/fallback cases. Expanded mixed boundaries/mirrors/sizes1–65/oversized declarations, external-read RAM remap plus GQR/HID2/FPSCR/register mutation, and RAM/EXRAM aliasing destination FPR. Original/candidate fullCPU, callback ordering/digest, return and host FP flags agree. BothASanUBSan/O2 pass278906cases with262234mapped hits. Undersized/oversized metadata uses only physically backed offsets; not proof that arbitrary invalid mappings are safe. Logs s16-pair-{r407,expanded-r407,final-r407}.log.
- Added --s16-pair subordinate to existing --s16-load in whole-kernel oracle; other historical modes unchanged. Both sides use current accepted CPU05e221c0. Revalidated oracle fixture equals current generated chunkfa455a7a except include path. Full oracle92504 exit0: all four sanitized/O2 original/candidate runs agree1440cases/digest882be09b1f38e9ad,624yields/480FPfaults/480illegal/192interruptions/96callerreturns/18816callbacks/34688journals/1440reservationclears; candidate pair_hits4840 each. No game-speed inference.
- New helper test added to repository suite; fullsuite84886 exit0/generated/check-r407.log. No bootstrap overlap. No app/module/default or runtime source changes. Next benchmark started with --kernels --s16-load --s16-pair --benchmark --cpu-time --module-pgo, generated/s16-pair-bench-r407.log; finish same live handle before deciding whether any isolated build is warranted. Preserves saved PGO/strictFP/ThinLTO and current helper on both sides. No game/Simulator; full original PRD/performance/audio/mobile hold remains active.

## 2026-09-07 — R406 current-module decoder profile

- Prior turn progressed: atomically timed movie comparison parked submission50. Rechecked STATUS/JOURNAL/no runner/booted Simulator. Fresh decoder-profile-r406 copied R405100Config/Wii/config.ini/slot3, independent pipe. Rehashed signed module1fb635f71b8ca6afab01fccb1b06a0a5e2b6ddd5a1aec4bb8ef629af3201dc7a, slot3aea2b7a3 and savea574 unchanged. Same normal app/Metal1x/dual core/LC byte-pair1/VI-only, no module override.
- Runtime53384/PID38856, actual module loaded before CUA. Visually verified exact pre-attack slot3; command47041 executes existing trigger then clock anchor then xctrace CPU Profiler --attach38856 --time-limit10s, exit0. No intervening UI delay before profiler invocation; command anchor is not exact sample-window boundary. Capture saved successfully, movie advancement visually confirmed after capture. Exact-PID SIGTERM runtime53384 exit0/native1060410429/fallback0/smc_failed0/savea574 unchanged. Export26557 exit0. No SystemTrace/large capture, no normal app/source change.
- Current exact-module profile31807CPU samples,31772P-core/35E-core. Leaves func804530A05752,kernel05223,kernel12946 (13921/31807=43.77% combined), convert_to_double1519,ps_add1328,ps_sub1165,ps_madd1085,psq_store1060,ni_add904,Run824,TryGetLockedCachePair724. Statistical attribution, not per-function elapsed cost, performance gain or device acceptance.
- Exact signed-module llvm-objdump and classifier join all selected offsets. Kernel loads6302/8169samples (~77.15%); func804530A0 has2600load/928store/2122integer/102branch. Hot57afef4 materializes guestPC before coefficient load;57afe5c stores result after CLZ cascade. These are still the previously investigated coefficient tail, not a new CLZ/lookup-order finding. Existing MEM1-first R218, cached-map R311, scoped signed16 R344–353, local-column R343 and helper/inlining rejections read; none revived unchanged.
- Next distinct bounded hypothesis: within the 16 scoped signed16/scale-zero coefficient loads, obtain one checked four-byte mapping for two lanes, rather than repeat mapping per16-bit lane; retain original path for boundaries/invalid sizes/callbacks and preserve lane0 assignment before lane1 read when RAM aliases CPUState. Previous s16_psq_load.py already scoped these calls, so this is not a newly scoped version of that rejected candidate. Existing type-zero eight-byte paired-load guard/oracle provides contract guidance, not signed16 proof. Inspect actual helpers and add exact boundary/remap/alias/LSQE/GQR/fault oracle before an offline kernel benchmark; no runtime gain assumed and no global helper replacement.
- Evidence generated/runtime/decoder-profile-r406/{cpu.trace,cpu.xml,toc.xml,summary.json,hot-functions.asm,instruction-families.json,capture.log,runtime.log,vi.csv}. Focused CPU-summary/classifier and repository safety checks pass. R400 fullsuite still applies to unchanged product source; no runtime/Simulator remains. Full original PRD/performance/audio/mobile hold remains active.

## 2026-09-07 — R405 atomic movie timing; submission candidate parked

- R404 was diagnostic progress but invalid A/B: manual post-trigger UI delay mixed movie/gameplay. New ignored run-submit-movie-r405.sh atomically executes trigger→C++ anchor→60s→verified exact-PID stop. Bash syntax passes and unknown setting/non-numeric PID/missing process are refused. No extra public framework/product changes. Fresh independent100/50 profiles retain identical slot3aea2b7a3 and savea574; GFX diff exactly100/50. Same normal671729c6runner/bundled1fb635f7module, Metal1x/dual core/LC byte-pair1/VI-only.
- Control51344/PID38286/helper8460 all exit0. Actual module-loaded before CUA, exact pre-attack scene visually verified, then no UI between trigger and close. Anchor407490459602416..407550459602416.3416VI=56.933333Hz, CPUmean17.390578ms, p99wall18.831834ms, EFB0 throughout complete window. Worst wall21.054834ms/CPU20.593708ms. Native712833907/fallback0/smc_failed0/savea574 unchanged.
- Candidate6197/PID38453/helper48348 all exit0, same observed restored scene/atomic protocol. Anchor407683867876666..407743867876666.3409VI=56.816667Hz, CPUmean17.397787ms, p99wall19.2825ms, EFB0 throughout complete window. Worst wall80.563292ms/CPU21.837417ms with no EFB/gather and negligible DVD/idle/wakeup; not explained by submission. Native725590558/fallback0/smc_failed0/savea574 unchanged. Both final projection190cf1d3666f1e56 with4draws/12primitives matches movie composition, not frame-exact equality or complete visual movie acceptance. Original VI/log/anchor/helper output in each profile; summaries enforce complete bounds/no invalid/reset, dropped0.
- Decision: park interval50, normal default100 remains. Candidate cadence~.205% lower/CPU~.041% higher is negligible/host-order-sensitive, not an improvement. Both movie windows have zero EFB elapsed. Actual VertexManagerBase::OnEndFrame returns without scheduling readback-driven kicks when no CPU accesses; the knob does not address sustained decoder CPU deficit. R403 real-depth/service result retained but no overall cadence evidence justifies promotion. No unchanged further pairs or scheduling-policy-only probes.
- Next focused step: refresh CPU instruction attribution against current1fb635f7 module (R310 trace used older5c2101, predating accepted two-range dispatch/store-scale), using existing profiler/export tooling on the corrected atomic movie fixture in a separate diagnostic run. Compare remaining decoder/state/mapping traffic versus arithmetic before selecting one new transformation; do not recycle parked cached-map/local-column/CLZ/entry-split/FP experiments unchanged. No new build/normal app change, no runtime/Simulator left, full PRD/performance/audio/mobile hold remains. R400 fullsuite applies to unchanged product source; focused timing/safety checks rerun.

## 2026-09-07 — R404 submission setting in CPU-heavy movie

- Prior turn is progress: fixed-scene depth equality and service reduction established, without overall stutter acceptance. Current STATUS/JOURNAL, dirty checkout and no game/booted Simulator checked. Full original goal and mobile hold remain intact.
- Hypothesis: existing interval50 may reduce readback service but add CPU/submission overhead in the known heavy movie. Compare explicit100/50 with normal installed671729c6runner/bundled1fb635f7module, native1x/dual core/LC byte-pair1, VI-only (no phase/EFB/dispatch/completion traces), no module override. No source change/rebuild/promotion.
- Prepared isolated submit-movie100-r404 and submit-movie50-r404 from identical R381 candidate Config/Wii/config.ini/slot3; independent pipes, only interval differs. Slot3 SHAaea2b7a3fe295b77e409753a216abf8fe6f2f1ec23ee7a8832fc9ac1a27b7ab8 and real savea5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 verified. Control33531 launched; wait module-loaded before CUA, restore3 and visually verify before exact g6-r85-trigger-attack fixture. Persist prospective60s anchor and auto-stop; no UI during window. Candidate stays unlaunched until control terminal.
- Control33531/PID37817/trigger19899/timer97083 and candidate95553/PID37991/trigger29013/timer61235 all exit0. Both visibly restored exact pre-attack scene and triggered movie; savea574 unchanged/fallback0/smc_failed0/dropped0. Control60s406972957005208..407032957005208=57.116667VIHz/CPU17.257751ms/p99wall19.44325ms/zero EFB. Candidate407213240184125..407273240184125=58.35VIHz/CPU16.013187ms/p99wall19.082541ms/EFB881.306178ms.
- INVALID A/B: screenshot before candidate anchor shows later Peach shot, control earlier ship shot; candidate window reaches subsequent EFB-active gameplay. The delay between fixture completion and manual anchor varied. Do not claim a speedup/nonregression. Persisted complete windows prove timing coverage, not scene equivalence. The zero-EFB control movie independently supports CPU-heavy bottleneck, but not blanket zero-EFB for whole run.
- Unblocking: eliminate coordination delay structurally. Prepared generated/run-submit-movie-r405.sh executes existing trigger then C++ clock anchor then60s wait then validated exact-PID SIGTERM in one process, no intervening UI/tool work. Refuses unknown setting/non-numeric PID/wrong profile process/existing anchor. Only ignored artifacts, no product source change. Next fresh100/50 profiles with this atomic sequence; screenshots only before trigger. No unchanged retry or promotion. All R404 runtimes terminal; full PRD/mobile hold unchanged.

## 2026-09-07 — R403 fixed-state submission comparison

- Changed the confounded R402 movement fixture to verified profile-r374 slot1 SHA4a4217264a1968fc6b93fce8fb213eb9e50af1aaebf378cd2f14be9c28f15edb, no movement. Separate submit-fixed100-r403 and submit-fixed50-r403 profiles preserve R401 save/config and explicit100/50 intervals; same R400 diagnostic app/bundled module. No normal app or default change.
- First control90361/PID36882 restored the verified central Observatory scene visibly, but coordination overran the VI recorder: dropped5165, no persisted prospective window anchor. Exclude this run from the fixed-window A/B, rather than selecting a favorable retrospective interval. Exact-PID SIGTERM exited0, native5889870926/fallback0/smc_failed0. All original traces retained. Next measurement must persist its anchor and automatically terminate after60 seconds to prevent a repeat. Candidate88849/PID37167 launched; wait actual module load before selecting UI. No Simulator.
- Completed candidate88849/PID37167 and replacement control83508/PID37390, both exit0 after exact-PID automatic SIGTERM; timer handles61666/13084 exit0. Saved anchors406382428595708 and406574685590166, each end=start+60000000000; both summaries prove complete window coverage, VI dropped0/no invalid CPU/reset intervals. Actual bundled module-loaded preceded CUA selection; States Load1 visibly restored identical central Observatory camera/Mario sleeping position. No movement or UI during either window. Replacement submit-fixed100b-r403 copied candidate Config/Wii/config.ini/StateSaves after clean exit, reconfigured independent pipe, changed only explicit interval50→100; exact slot1 and real save106e hashes verified. Original failed run retained.
- Candidate50:3596VI/59.933333Hz, CPUmean15.464545ms, p99wall17.568958ms.10788 reads, service1421.080537ms total/.131727896mean/1.476875max; dispatch35.834832ms/resume95.724178ms. Control100:3594VI/59.9Hz, CPUmean15.412025ms, p99wall17.845666ms.10785reads, service1761.063297ms/.163288206mean/2.357958max; dispatch37.941836ms/resume98.286789ms. Every dispatch row matches EFB coordinate/PC/LR and stage ordering. Both windows have exactly the same three depth outcomes:320,194=f919a9;0,0=fa49df;0,2=fa82e9, each repeated3596(candidate)/3595(control). Read/frame boundaries differ slightly; not a missing-depth claim.
- Interpretation: approximately19.3% lower mean read service with50, approximately0.341% higher CPU; no material cadence gain in this near-60Hz scene. Candidate zero exact-window audio events; control one underrun at+1.338s and backlogs+1.658/+2.330s. Control's92.532ms wall hitch has22.966ms CPU and only.423ms EFB; candidate also has56.051ms wall/27.506ms CPU/.410ms EFB hitch. Unequal post-load settling and host scheduling remain possible; do not attribute those hitches or audio difference to submission interval. Not sustained stutter/audio acceptance and not GPU execution-time measurement.
- Evidence generated/runtime/submit-comparison-r403.json, reproducible local analyzer generated/analyze-submit-r403.py, both original profile traces/runtime logs/window-start-ns.txt/window-summary.json. Saves106e unchanged both sides; fallback0/smc_failed0. Normal runner671729c6/module1fb635f7 rehashed unchanged; no runtime/booted Simulator,62GiB available. Existing R400 fullsuite applies because no product source changed; focused analysis and safety checks run below. Next bound candidate50 CPU-overhead risk using existing pre-attack slot3/trigger movie fixture with VI-only diagnostics and normal installed app, compared with explicit100. No rebuild or default promotion. Original full PRD/performance-first mobile hold remains.

## 2026-09-07 — R402 first submission-interval pair, scene confound found

- Prior turn progressed by localizing service cost. Actual source/default100 and background execution true inspected; installed RMG.ini contains no override (no RMGE01.ini exists). Isolated submit100-r402/submit50-r402 profiles copied identical R401Config/Wii/config.ini, independent pipes, GFX differs only explicit100/50. Same52104227runner/bundled1fb635f7module/Metal1x/CPUThreadtrue/Cubeb/LC byte-pair1/dispatch+EFB+phase+VI; no module/completion override or concurrent build/game/Simulator. Normal settings untouched.
- Control35417/PID36213 ready title→load36770exit0→visible Observatory→controls1971exit0→C++ anchor405227309285583..405287309285583. No UI in60s; exact-PID graceful stop35417exit0/native3699054134/fallback0/smc0/save106e unchanged. Controls were dispatched but post-control visual state was not captured on this side; earlier commentary that movement worked was stronger than this run's visual evidence. VI dropped0;59.95Hz/CPU15.607956ms/p99wall18.073958ms.10788complete depth reads/service1716.441397ms total/.159107ms mean/2.526958ms max; zero window underrun/backlog/full events.
- Candidate51161/PID36440 ready title→load13869exit0→visible Observatory→controls89287exit0→anchor405543609649250..405603609649250. No UI in60s; postwindow screenshot shows Mario at different side/camera from prior route expectation. Exact-PID stop51161exit0/native4495945747/fallback0/smc0/save106e unchanged. VI dropped0;59.933333Hz/CPU15.754312ms/p99wall17.932167ms.10788complete reads/service1352.356478ms total/.125357ms mean/1.691583ms max; zero window audio events. Whole-run counts/durations include startup/UI/close and are not compared.
- Service total is~21.21% lower but CPU~.938% higher/no cadence gain. More importantly, matched dispatch/EFB rows reveal different depth states: control dominant pointer320,194 depth00f93c51 and origin00fffd43/45; candidate00f7f992 and origins00fffd46/49. Inputs were same wall-clock sequence, but movement/camera was not identical. Do not attribute these differences to submission interval or claim depth equivalence; no promotion and no unchanged third run.
- Evidence generated/runtime/submit-comparison-r402.json plus original per-profile logs/window summaries. Next lock scene using existing verified generated/runtime/profile-r374/StateSaves/RMGE01.s01 (R374 SHA4a4217264a1968fc6b93fce8fb213eb9e50af1aaebf378cd2f14be9c28f15edb,39042803bytes), no movement before fixed window, verify restored camera/coordinate/depth state. This changes fixture/control quality, not another identical pair. Need runtime effective-setting evidence if ambiguity remains; source/config inventory alone is not a changed-command-buffer-count measurement. Default100 and normal app unchanged; no live processes/Simulator. Full original PRD/performance-first mobile hold intact.

## 2026-09-07 — R401 readback service localized, submission experiment identified

- Previous turn progressed with integrated stage diagnostic/build/suite. Current STATUS/JOURNAL/no runner/booted Simulator verified. New disposable efb-dispatch-r401 copied R398Config/Wii/config.ini, independent pipe, save106e before/after unchanged. R400 runner52104227 hashverified; provenance copied. No rebuild, concurrent game or normal app modification.
- Runtime50873/PID35688 launched bundled1fb635f7module/Metal1x/CPUThreadtrue/Cubeb/LC byte-pair1 with dispatch/EFB/phase/VI; module/completion overrides unset. Waited actual module-loaded before CUA, visible ready title→load56582exit0→Observatory→controls37219exit0→visible moved Mario/camera→exact-PID graceful stop50873exit0/native2498311807/fallback0/smc_failed0/export1. No runtime/Simulator remains. Whole167s3underruns5backlogs is not fixed-window/audio acceptance; no individual jump/Spin proof.
- All21519dispatch rows match depth rows by coordinates/PC/LR; every queued<=entered<=finished<=returned. Totals dispatch80.959184ms/service3481.350603ms/resume192.951139ms (3755.260926ms total), so service~92.71%. Slowest pointer482,378/readPC804ba23c/LR80385be0 has11.078375ms total: .001333dispatch/11.070917service/.006125resume. Other top reads also service-dominated. Previous40ms outlier not reproduced. This rules out request queue/resumption as dominant for this run's measured reads, not all historic stalls. stage-summary.json beside original logs.
- Actual PopulateEFBCache flushes pokes, records OnCPUEFBAccess, resolves/copies cache tile, then flushes readback. Metal supports depth readback and partial depth copies; do not toggle those flags or add fake-depth shortcuts. Staging copy encodes download into current render command buffer; Flush submits/waits as required.
- Important existing mechanism: VertexManagerBase::OnCPUEFBAccess stores read draw counts; OnEndFrame schedules future command-buffer submissions; OnDraw submits at scheduled counters with background execution allowed by default. Actual GraphicsSettings default CommandBufferExecuteInterval=100 (old OnEndFrame comment says250). No recorded experiment on this knob found in PERF. Next isolated explicit100 control versus50 candidate on same stage diagnostic/profile/route, compare readback service, CPU/cadence and depth outcome/call counts. This adjusts existing early GPU submission, not emulated time or correctness. Keep default100/normal app unchanged until matched evidence and regression checks; extra submissions may worsen CPU-heavy movie. No new stage instrumentation needed for this hypothesis.
- R400 suite applicable/no product code changed; stage runtime smoke supplies missing package check. Full original PRD/performance-first mobile hold remains.

## 2026-09-07 — R400 depth stage timing integration and build

- Final checkpoint: same suite15593 exited0/Repository safety checks passed. All build/test handles terminal, no runtime. Next execute the new stage-resolved diagnostic, not another bootstrap/build.

- Previous turn progressed with tested diagnostic prototype. Current bootstrap/test sources and no runner verified. Integrated exact dispatch overlay as outermost pin/peel/restore/apply/scope; initial95671 and repeat bootstrap pass. New tests/efb_source.py reverses only exact pinned dispatch patch in temp and requires known9164b11a base; original/context/stage layered tests preserve raw checkout and reject unknown/partial sources. Unknown source negative cases and open-failed trace path added.
- Postintegration focused context/stage tests pass. Actual EFBInterface a9caaf2ec38b064acacb3eb36ab0d62810da51aafd85cb721f93a47a8c54a010 pinned in test. No bootstrap/source-read race. Separate package36535 exited0 (199 build steps), private app audit passed. Output generated/macos/efb-dispatch-r400/GalaxyPad.app runner5210422767c9750e3416ba35b670e62abc805abc551524de78c883070cb0528b; signedmodule1fb635f7...1dc7a unchanged. Adjacent provenance.json records required env/capabilities/timing caveats. Normal installed runner671729c6 rehashed unchanged.
- Full suite15593 running generated/check-r400.log; finish same handle before runtime. No game/Simulator. Next one real-save loading/movement diagnostic with EFB_DISPATCH_TRACE plus EFB_TRACE/phase/VI, compare queued→entered→finished→returned durations for exact callers and relate to VI/audio events. Do not infer GPU execution from service wall time. No normal-app promotion, buffering/renderer policy change or mobile work; full original PRD remains active.

## 2026-09-07 — R399 depth-dispatch stage timing preparation

- Final checkpoint: same suite66819 exited0; Repository safety checks passed. No active runtime/test/build; staged diagnostic patch remains unapplied, next integration/build rather than repeat prototype tests.

- Previous turn progressed by verifying live read PCs/callers. Revalidated actual depth path and no runner. Existing R398 max read ordinal10826/frame6284/Talk-like caller8029e6e4 at0,2 took39.980625ms; nearby enclosing VI91.728ms wall/18.554ms CPU/40.210ms EFB. Two pointer reads at482,378 take10.243458/10.047875ms in frames with roughly25.8ms wall/15ms CPU. Presented-frame end brackets are approximate; current data cannot split queue/pipeline, readback service and CPU resumption.
- Prepared unapplied experiments/efb-dispatch-timing.patch SHA5226881b8ce0f00b0b2673b68f40040fa4490771d170aade1289db00ae19b638; header fbee4f13c98e89f0a9e0fd89584bfeda6f135019ca6eebc7553c262a71afaa84. Depth only: wrap existing PushBlockingEvent and exact existing read callback with opt-in queued/handler-enter/read-finished/CPU-returned steady timestamps, plus coordinates/live context. Env GALAXYPAD_EFB_DISPATCH_TRACE. Unset/empty/open-failed path invokes original dispatch/read without timing. Existing EFB_TRACE must also be enabled for nonzero live-context PC/LR. No pixel conversion/readback/future/sync/queue policy changes.
- Dispatch duration includes queue and vertex-pipeline work before handler starts; service includes framebuffer cache/Metal waits, not GPU execution alone; resume includes promise/scheduling overhead. Stage timestamps exclude final CSV write, but outer EFB elapsed includes that write, so do not claim direct timing parity with old traces. Future completion synchronizes handler timestamp writes; no detached work or unsynchronized shared reads.
- New tests/test-efb-dispatch-timing.py applies patch to pinned real EFBInterface9164b11a source in temp, compiles actual patched header under ASan/UBSan/O2, checks sync/future callback order, exact finite/negative-zero/infinity/NaN bits, single invocation, opt-out, monotonic stage timestamps/metadata and exact reversal/unchanged checkout. Focused tests pass; full suite66819 running generated/check-r399.log. No runtime overlay/build/app changes this turn.
- Next finish same suite then integrate outer overlay reproducibly, preserving exact context-test normalization, separate build and one stage-resolved run. No unchanged per-read replay, no speculative priority/cache/depth/buffer change. Normal installed app/full original PRD/performance-first mobile hold intact.

## 2026-09-07 — R398 corrected EFB caller attribution verified live

- Previous turn progressed with integrated source/repeat bootstrap/build/audit/full suite. Current no runner/booted Simulator and separate runner09353ce1 SHA verified. Disposable efb-context-r398 Config/Wii/config.ini copied from R395, independent pipe, save106e before/after unchanged. Exact provenance copied beside traces. No build, second game or normal app change.
- Runtime39668/PID30113 uses bundled signed1fb635f7module/Metal1x/CPUThreadtrue/Cubeb/LC byte-pair1, phase/VI/EFB enabled, module/completion overrides unset. Waited for module-loaded before CUA; visible ready title→load95120 exit0→one-star Observatory→controls82693 exit0→visible changed Mario/camera→exact-PID graceful termination39668 exit0/native2029805462/fallback0/smc_failed0/export1. No runtime/Simulator remains. Individual jump/Spin effects not isolated; whole140s4underruns7backlogs includes startup/loading/UI/close, not performance or audio acceptance.
- EFB trace16896 contiguous rows matches shutdown depth count; every read PC804ba23c, zero unattributed. Exact selected generated chunk1206 line2295 is lwz r0,0(r6), preceded by EFB uncached-address coordinate/type assembly at804ba228. Caller LR80385be0 (10262reads/2513.990454ms/max10.243458ms) follows exact80385bdc bl804ba228; LR8029e6e4 (6634reads/595.306540ms/max39.980625ms) follows8029e6e0 bl804ba228. Corrected trace no longer mislabels mfmsr804a3c9c as a read. Evidence caller-verification.json and original logs in generated/runtime/efb-context-r398.
- Petari source offers behavioral matches, not imported region addresses: StarPointerPeekZ::drawSyncCallback loops in-range channels, converts screen position, stores depth at DpdInfo+12; TalkPeekZ converts position, reads into object+8 then inverse-projects. Exact generated80385bdc destination+12 and8029e6e0 destination+8 support those associations. Both callsites perform meaningful depth-dependent work; no skip, constant-depth or P2 suppression proposed. Same-frame duplicate suppression was already disproven as useful; do not revive it from new names.
- This closes live attribution verification, not the intermittent stall or sustained movie deficit. R397 full suite remains applicable (no code changes this turn). Next use trustworthy per-caller evidence to assess whether the longest read is queue/video/Metal wait versus CPU execution; avoid another unchanged per-read reproduction. Normal installed671729c6 app unchanged, full original PRD/performance-first mobile hold remains.

## 2026-09-07 — R397 live-context source integration and diagnostic build

- Final checkpoint: same full suite81870 exited0/Repository safety checks passed. All build/test handles terminal; no booted Simulator. Next bounded diagnostic runtime/caller verification, not another build or unchanged trace.

- Previous turn progressed with prototype/complete regression suite. Current dirty checkout and no runner verified. Added exact efb_context outer overlay pin/peel-first/restore-last/apply/scope to bootstrap; initial83096 and repeat bootstrap pass. Tests normalize only exact original or fully patched real files, require consistent overlay/header presence and byte-identical patched outputs; unknown/partial/mixed overlays reject. Added bootstrap order/scope checks. No bootstrap ran concurrently with source-reading suite.
- Integrated Hooks SHA4a6d2d01290bf5d74319a799eec473f1780adafabb2cd9995afeb50dfb27fc30 and EFBInterface9164b11a36e898f4e2ee479b975c751deb2752f2b27047d610feb0290197405f verified. Focused original/patched context tests pass. Header/patch identities unchanged from R396. Separate package34215 exited0, generated/build-efb-context-r397.log; private app audit passed.
- Diagnostic output generated/macos/efb-context-r397/GalaxyPad.app runner09353ce1a5f3df198ee015ef957276d7c2c707033e081cc17744982aad93f74b, signedmodule1fb635f7...1dc7a. Exact capability/source/build identities in adjacent provenance.json. Normal installed runner671729c6 rehashed unchanged. No runtime smoke yet; no app promotion, game/Simulator or mobile work.64GiB free.
- Full suite81870 running generated/check-r397.log; finish same handle before runtime. Next real-save EFB diagnostic route, verify reported nonzero PCs are actual memory-read instructions in exact generated code, and retain zero/unattributed rows explicitly. Correct attribution is a prerequisite for targeted performance work, not itself an FPS improvement. Full original PRD remains active.

## 2026-09-07 — R396 scoped live-AOT EFB diagnostic preparation

- Final checkpoint: same full suite53488 exited0/Repository safety checks passed. Unapplied patch still passes git apply --check against live vendor source. Normal runner671729c6bb32e29ec44e95dedae0044c9c3d7e64f99736653f3146d806d3d762 rehashed unchanged. No live test/build/runtime handle remains; runtime smoke will follow actual integration, not claimed from this prototype.

- Prior turn progressed by exposing stale caller provenance. Revalidated actual HookExternalRead/HookExternalRead32, EFBInterface color/depth call sites, dirty checkout and no game/booted Simulator. Hooks original SHAd31ca92693854811b7da47c5d40efb9bdb2347e50a09337ca9be1f40223a63a2, EFBInterface661ffe6c2b64b12090d0d181c279ec7d12e53cce309822e481f22796dc132a77.
- Prepared unapplied experiments/efb-live-read-context.patch SHA4d5782faa59f0dc77bdc31c3bef75c2e8215088b427a308337bbfe21d410b775. New header efb-read-context.h SHAf40aac5c0eaf792e4615b7dfac2c281fb9d96b45b41731182fe8c7c607151a2b carries opt-in thread-local PC/LR with RAII nesting/restoration; disabled scope does not touch context or open files. Both AOT external-read hooks establish scope from live CPUState; both EFB trace calls use scoped values. Absent scope returns0/0 (unattributed), never mirrored PPC guesses. No emulated register synchronization, MMU/queue/readback/depth/audio policy changes.
- tests/test-efb-read-context.py applies exact patch to pinned temporary real sources, verifies new header and both hook/color-depth wiring, compiles header across two TUs under ASan/UBSan and O2, verifies unset/empty/enabled env, nested calls, early return, exception unwind, noncopyability and thread isolation, then reverses patch byte-exactly and confirms real checkout unchanged. Focused tests pass; added to repository checks. Full suite53488 running generated/check-r396.log; poll same handle. Product overlay not yet applied; no build/runtime.
- Next finish suite, wire exact new outer overlay into bootstrap peel/restore/apply/scope and normalize test inputs only for known original/patched identities, then separate diagnostic build and verify real read PCs against exact generated instructions. R392 normal diagnostic and installed app unchanged. Full original PRD/performance-first mobile hold intact; diagnostic correctness is not a performance fix or acceptance.

## 2026-09-07 — R395 per-read reproduction and stale attribution defect

- Previous turn progressed via event-cluster localization. Revalidated no runner/frontend/Simulator, R392 diagnostic runnerfa9adfa5 hash, isolated new efb-loading-r395 Config/Wii/config.ini/profile pipe from R393; real-save106e before/after unchanged. Added existing EFB_TRACE only; phase/VI and bundled1fb635f7module/Metal1x/CPUThreadtrue/Cubeb/LC byte-pair1 unchanged. No rebuild or normal app mutation.
- Runtime30022/PID24610 starts; waited for actual module-loaded log before CUA getApp (no stray frontend). Visible ready title→load95565 exit0→one-star Observatory arrival. Exact-PID SIGTERM clean exit0/export1/fallback0/smc_failed0. No runtime/Simulator remains. Whole134s8045presentedframes/13512depthreads/1underrun/3backlogs/0full, save unchanged.
- Phase input confirmation402705086258208; matching relative R393 selection offset2.282035292s gives15s window402707368293500..402722368293500. This run60.0VIHz/CPU14.964574ms/EFB.628935ms/oneunderrun/twobacklogs, NOT reproduction of prior45.4Hz/39underruns. Elevated loading stall is intermittent; do not claim a deterministic expensive transition or infer cache/scheduler cause.
- Complete phase/EFB ordinal sets verified. Phase frame_end timestamps monotonic; presented-frame counters5017..5914 select2529reads (843 each at482,378;0,0;0,2). Pointer coordinate482,378 accounts544.399ms of565.412ms (~96.3%); max12.245625ms. Other coordinates0,2 total16.521ms and0,0 total4.492ms. Frame mapping is approximate at concurrent boundaries, not exact per-read wall timestamp. Shutdown count matches13512trace rows.
- Critical diagnostic finding: most trace rows report pc804a3c9c/lr80397884 or8029e630. Exact selected generated chunk1183 identifies804a3c9c as mfmsr r5, not a memory read. EFBInterfaceBase::PeekDepth supplies Core::System.GetPPCState pc/LR; StaticRecompCore::HookExternalRead receives live CPUState but only PropagateGuestMSR before mmu.Read. It does not synchronize mirrored PC/LR. Therefore existing EFB trace caller fields can be stale in AOT mode; cannot label those addresses as exact read callers. Coordinates/value/elapsed remain usable.
- Next fix diagnostic provenance narrowly: opt-in scoped live AOT read PC/LR carried from HookExternalRead to RecordEFB, restored across nested calls, without synchronizing/mutating emulated registers or altering readback/queue semantics. Actual-header tests must prove nesting/opt-out/restoration before separate build. Do not patch a guest caller based on stale fields; do not repeat this unchanged trace. Original PRD/performance-first mobile hold remains open.

## 2026-09-07 — R394 underrun cluster localization

- Previous turn progressed via complete exact-event runtime evidence. Current STATUS/no runner/no booted Simulator revalidated. Analyzed existing R393 phase and VI traces only; no new run/build/product edits.
- Input trace locates file-load confirmation A at anchor-42.282035292s. Subsequent [-40,-25)s window (402097536543208..402112536543208) has39underruns/1backlog,45.4VIHz,24217.6guestframes/s vs48025.6hostframes/s. Mean VI wall22.0339ms/CPU14.714802ms/EFB6.2676ms; EFB4.262s total across680complete intervals, idle.309ms/DVD.0065ms/wakeup.0193ms mean. This is a sustained production deficit coincident with elevated readback time, unlike quiet Observatory CPU15.611ms/EFB.478ms/zeroevents. Input timing does not prove the precise scene of every frame.
- Separate [-20,-10)s interval has59.5VIHz/oneunderrun, meanCPU15.413353ms/EFB.4453ms. Underrun-containing VI56.273542wall/20.862208CPU/.300750EFB/.013709wakeup is preceded by71.214416wall/19.879167CPU/41.146124wakeup. These are different patterns; don't classify all underruns as one CPU or GPU bottleneck. An initial requested late window extended beyond retained data and correctly refused; corrected covered window [79,83.5)s analyzed instead, no trace loss.
- Source audit: EFBInterfaceBase::PeekDepth timer surrounds PeekDepthInternal; hardware implementation uses AsyncRequests::PushBlockingEvent and future.get on CPU thread. Video PullEvents flushes vertex work, then FramebufferManager::PeekEFBDepth populates tile cache and flushes readback as needed. Metal::StagingTexture::Flush can FlushEncoders/NotifyOfCPUGPUSync/waitUntilCompleted. Thus EFB elapsed spans queue/wakeup/video/Metal waits and some CPU work, not exclusive GPU execution. Existing per-read EFB trace already supplies coordinate/value/guestPC/LR/frame; no new instrumentation needed to identify exact callers.
- Evidence cluster-analysis-r394.json and transition-window-r394.json beside R393 traces. Next bounded same real-save loading route with existing EFB_TRACE added to diagnostic profile, correlate presented-frame ordinal to phase frame_end, identify slow coordinates/callers before choosing any semantics-preserving change. This changes measured detail; do not repeat unchanged quiet windows or introduce cache/skip/priority/buffer changes from aggregate timing. Normal app unchanged; original full PRD/performance-first mobile hold remains.

## 2026-09-07 — R393 event-capable real-save recording

- Previous turn was progress: diagnostic source integration, separate app build/audit and full suite passed. Revalidated current STATUS, no runner/booted Simulator, exact profile/config and module resolution. Copied R390 Config/Wii/config.ini to ignored audio-events-r393, no states, independent pipe; save106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90 before/after unchanged. Normal app untouched.
- Runtime24713/PID23958 used separate R392 app with bundled signed1fb635f7 module, phase/VI enabled, completion/module overrides unset, LC byte/pair1, Metal1x/CPUThreadtrue/Cubeb. An early CUA getApp before module loaded timed out and opened extra frontend23999; exact path verified and frontend terminated before title/input/window. No second game ran. Do not call getApp until module-loaded log exists.
- Visible ready title → deterministic real-save load2406 exit0 → one-star Observatory → controls93698 exit0. C++ steady anchor402137536543208, end402197536543208; no UI in60s window. Postwindow capture shows moved Mario/camera and idle animation, not individual jump/Spin proof. Exact-PID graceful SIGTERM; runtime24713 exit0/native2881716034/fallback0/smc_failed0/export_result1. No runner/frontend/Simulator remains.
- generated/runtime/audio-events-r393 contains capability provenance, original phase/VI/runtime/input logs, window-summary.json and event-summary.json. Phase ordinal set is contiguous; sorted timestamps span window. Actual event totals47underruns/10backlogs/0full match shutdown counters. VI dropped0/invalidCPU0/reset0. Window3596VI/60s=59.933333Hz, meanCPU15.611282ms, p99wall18.554708ms, maxwall24.364250ms with21.831500ms CPU. Window14999DMAenqueues=31997.8667guestframes/s; host48000frames/s, maxDMAgap17.425416ms, maxcallbackgap11.223459ms.
- Exact window events: zero underruns/backlogs/full drops. All47underruns lie outside (44before/3after). Clusters relative window start:2 at-105.280s,2 at-79.925s,39 from-38.794 to-26.912s,1 at-15.754s,2 at+80.022s,1 at+83.179s. Outside-window events include startup/loading/UI/close periods but timing alone does not assign cause. This supplies missing exact event evidence; it does not invalidate earlier active-gameplay starvation or accept G6/audio/soak. No buffering/priority/renderer adjustment.
- Next use recorded event clusters to distinguish sustained producer deficit from isolated stalls by queue/callback/VI CPU deltas; do not repeat an unchanged quiet-window probe. Sustained opening movie CPU deficit remains (~56.9Hz in accepted scale candidate); full original PRD and performance-first mobile hold remain intact. R392 full suite is applicable (no product/test code changed this turn); this recording supplies isolated diagnostic package smoke.

## 2026-09-07 — R392 reproducible audio-event diagnostics

- Final checkpoint: full suite80674 exited0, Repository safety checks passed. Build92997 already exit0/audited; neither handle remains live. Diagnostic runtime/gameplay smoke deferred to the next bounded recording, not claimed from build/tests. No runtime/Simulator launched this step. Continue with separate diagnostic app and disposable106e real-save profile, explicit phase/VI traces, no UI inside measured window, clean close/export, then correlate exact audio events and VI CPU/wall intervals. No new acceptance claim or mobile promotion.

- Build92997 completed exit0 (199 build steps); private app audit passes. Diagnostic runner fa9adfa572bec3417b5f53d76af340c6de8d0fa32be681c1b3bdea47a2714102; signed module remains1fb635f71b8ca6afab01fccb1b06a0a5e2b6ddd5a1aec4bb8ef629af3201dc7a. Normal runner rehashed671729c6 unchanged. Ignored generated/macos/audio-events-r392/provenance.json records exact capabilities, identities and timing-order caveats; diagnostic runtime not yet tested. Full suite80674 running generated/check-r392.log; finish before runtime to avoid benchmark contention.

- R391 full suite20987 completed successfully (generated/check-r391.log). Cleanup already completed R283: live Devices492MiB, none booted,64GiB available; no repeated erasure. Source/reference/local saves and normal app preserved; previous simulator-container saves were permanently erased. Root289a87a1499c3d0da23c21c472fd16525742208a and dirty work preserved.
- Hypothesis: exact running-state underrun/backlog event timestamps will distinguish starvation inside a fixed VI window from startup/pause events; aggregate first/last enqueue counters cannot do this. No buffering or audio algorithm change. Existing opt-in phase tracing can perturb timings, so this remains diagnostic evidence, not audio acceptance.
- Completed bootstrap pin/peel/restore/apply/scope for audio-phase-events.patch SHA d0f1d941e552e4baa85b9fe623922dcf8ded804d393708d3f4e321276d99bc89. Initial and repeated bootstrap pass; actual resulting diagnostic header SHA9c11e94e0b7c22abe93b319e92f32030e436d9eefb30c487c09aee27c64f772b. Test now accepts only original or complete exact overlay, rejects partial/unknown source, and still checks ASan/UBSan/O2 counters, repeated events and reversal. Focused checks pass before and after bootstrap.
- Separate diagnostic package build92997 running, generated/build-audio-events-r392.log, output generated/macos/audio-events-r392/GalaxyPad.app. Normal installed app is not targeted. No game or Simulator. Next finish same build, record runner/header provenance, run suite after bootstrap, then bounded event-resolved real-save correlation. Never interpret an old event-less trace as zero underruns. Full original PRD/performance-first mobile hold unchanged.

## 2026-09-07 — R391 exact audio-event diagnostic preparation

- Checkpoint: fullsuite20987 confirmed live; poll same handle. No runtime/build/Simulator, diagnostic overlay remains unapplied.

- Prior turn progress: installed no-override smoke passed. No game/Simulator/build. Revisited existing R372phase.csv and actual Mixer/diagnostics source: phase trace records enqueue queue depth/audio callbacks, but RecordDmaUnderrun/BacklogDrop only retain aggregate first/last enqueue positions. Thus old traces cannot establish exact interior-event counts; low queue-depth snapshots are not equivalent to dequeue underruns. No new runtime probe or buffer-policy change.
- Prepared unapplied patches/experiments/audio-phase-events.patch adding only three calls to existing opt-in RecordPhase at actual underrun/backlog/full-drop counters, value=current enqueue ordinal. Existing opt-out returns before clock/lock/I/O; enabled phase tracing retains known observer overhead. No audio algorithm, reserve, pitch, synchronization or cadence change. Header pinned6f9c2dda1739eec70b137060ea8f12e00694639a9b4475fa0f8e6e9c795256d0.
- New tests/test-audio-phase-events.py compiles actual temporarily patched header under ASan/UBSan and O2; verifies unset trace yields no event ordinals, enabled exact6event order including repeated same-enqueue underruns, nondecreasing timestamps, unchanged counts/first-last positions and exact patch reversal. Initial hand-written patch hunk count was malformed; corrected count, focused95138 exit0. Product header untouched. Added test to repository suite generated/check-r391.log, running.
- Next isolated diagnostic runtime with exact event-enabled header provenance, then bounded no-pause real-save window to count events and correlate VI boundaries. Old logs lacking event capability must not be interpreted as zero-event windows. This changes the measurement variable, not another unchanged endpoint-only wait probe. Keep trace-induced overhead explicit and do not call a trace-only quiet result audio acceptance. Normal installed1fb635f7module/671729c6runner unchanged; full PRD/performance-first mobile hold intact.

## 2026-09-07 — R390 installed no-override smoke passes

- Source follow-up: loads use psq_dequant with an existing scale==0 early return; nonzero scales use double ldexp(value,-scale), not the store's ldexpf(1,scale). THP coefficient GQR5 scale0 therefore already avoids this libm call. Do not blindly port the store optimization or assume material movie benefit; inspect nonzero-scale coverage before choosing it as the next workload.

- Prior turn progress: canonical module parity/cache/promotion. Revalidated same postpromotion suite45970 to terminal0/repository safety passed, generated/check-promoted-scale-r389.log. Prepared scale-installed-r390 from R374Config/Wii/config.ini/no states/independent pipe; save106e hashverified and installed signedmodule1fb635f7...1dc7a rehashed.
- Normal app2377 launched with STATICRECOMP_MODULE and phase/VI/completion diagnostics explicitly unset. Runtime log confirms bundled Contents/MacOS/gRMGE01_recomp.dylib loaded, not override/cache candidate. Visible titleA+B→real-save load39935exit0→one-star Observatory→controls29025exit0 visibly changes Mario/camera→clean UI close2377exit0/native2510768137/fallback0/smc_failed0. Save106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90 unchanged. Individual jump/Spin effects not isolated visually. No game/Simulator/build remains.
- Whole168s9underruns/10backlogs/21gaps>=33ms includes startup/load/UI and confirms no stutter-free/audio acceptance; instantaneous63.3FPS capture is not a stable cadence claim. This closes package smoke for modest scale-store optimization, not full PRD/G6/performance/mobile gate. No source changes this turn.
- Next inspect separate psq_load_value scale construction for the analogous exact-factor opportunity; store patch touched only quantizer and left load semantics unchanged. Require exact source/range/callback/FP tests and larger workload timing, not assuming the store result transfers. Preserve full goal and performance-first mobile hold.

## 2026-09-07 — R389 canonical parity, cache reuse and module promotion

- Checkpoint: postpromotion suite45970 running; poll same handle. The first audit invocation was given a relative path and failed its path guard, not module validation; the corrected absolute-path invocation passed.

- Prior turn verified link wait. Revalidated SAME53203/ld16924 actively through5m39 without restart; build53203 then exit0. Canonical c4cbfba1bd04990b module SHA c0021b9f2fad3acc3746b6a582b46935e2f7b60bc38434ad7d485564957c7939 is byte-identical (cmp exit0) to isolated candidate tested R382–385. Initial audit passed relative path and correctly refused absolute-path requirement; reran once with exact absolute path, audit passes1322chunks/1024indexed/19SMCranges/arm64macOS14.
- Final manifest module_sources_fnv1a3535c2dc0243bc7a, unchanged FPRF/THP/two-range policies/strictFP/ThinLTO/savedPGO f39a3ced. Repeated canonical build34932 exit0/cache hit without compilation; normal marker still old before explicit selection.
- Backed up previous marker to generated/scale-integration-r389/active-module-before.txt, then selected canonical scale module via apply_patch. Module-only packaging22590 exit0/private audit passes; byte-equal launcher/runner/frontend checks built into packaging. Normal runner remains671729c6bb32e29ec44e95dedae0044c9c3d7e64f99736653f3146d806d3d762; newly signed module1fb635f71b8ca6afab01fccb1b06a0a5e2b6ddd5a1aec4bb8ef629af3201dc7a. Previous package retained generated/macos/GalaxyPad.app.previous.20260907T210556Z. No rebuild of runtime/frontend, no deletion/publication.
- Post-selection suite running generated/check-promoted-scale-r389.log; continue same handle. No game/Simulator. Next installed normal-package smoke without module override, then return to remaining CPU/pacing/audio/performance gate. Approx1%movie gain is incorporated, not stable60Hz/full G6/soak/audio or mobile acceptance; full original PRD preserved.

## 2026-09-07 — R388 canonical compilation complete, link active

- Prior turn progress: integrated oracle passed/canonical generation identical. Revalidated SAME53203 live; compile log advances853→1330/1332. Final linker16924 verified713.6%CPU at3s; no restart. Warnings1of8module-export,1of51CPU and1of33kernel profile mismatches recorded; no fatal build error. Continue same handle through final link/manifest creation/audit.
- Normal runner671729c6bb32e29ec44e95dedae0044c9c3d7e64f99736653f3146d806d3d762 rehashed unchanged, dirty checkout preserved. No game/Simulator. Existing --module-only packaging path audits selected module, stages copy/signs/audits, compares unchanged launcher/runner/frontend, retains previous package before swap; use only after canonical artifact equality/validation and marker selection. No promotion now.
- Next when53203 terminal: audit canonical c4cbfba1bd04990b artifact, compare to tested c0021b9f candidate, inspect final source fingerprint/policies, verify cache hit, then decide exact module-only promotion. R386 tests and R387oracle remain applicable. Full PRD/performance/audio/mobile hold stays intact.

## 2026-09-07 — R387 integrated oracle pass and canonical build

- Prior turn progress: source integration/repeat bootstrap/fullsuite passed. Revalidated same25874 to terminal0, all four sanitized/optimized reference/candidate full-kernel outputs match882be09b1f38e9ad (1440cases/624yields/480FPfaults/480illegal/192interruptions/96callerreturns/18816callbacks/34688journals/1440reservations). FullsuiteR386 remains applicable; no product/source change this turn. Dirty checkout preserved,66GiBavailable, no game/Simulator.
- Launched canonical separate-output build53203: GALAXYPAD_MODULE_OUTPUT=$PWD/generated/modules-scale-r387 GALAXYPAD_PGO_PROFILE=$PWD/generated/pgo/rmge01.profdata bash scripts/build-module.sh, log generated/build-scale-canonical-r387.log. ExactDOL/Broadway preflight passes. New cache suffixc4cbfba1bd04990b rather than oldede0bf4e2d66a29f; existing tool includes module-source fingerprint before lookup. Active normal marker remains oldcanonical549988, no selection/package overwrite.
- Generated entire RMGE01_generated tree compares byte-identical to accepted R366 tree (diff -qr exit0), headerda2def2...9336/chunk1102fa455a7...6bae verified. Only runtime CPU-helper source changed to05e221c0 testedcandidate; no codegen policy change. Manifest is not written yet during compilation; an early glob inspection found no manifest, not a build failure.
- Same53203 verified running, log advances162→426/1332. Continue same handle through compile/link/audit. Once terminal, compare canonical binary to isolatedc0021b9f...c7939 and inspect final manifest/cache identity; any mismatch needs explanation/validation before app promotion. No concurrent test/runtime. Full PRD/performance/audio/mobile hold intact.

## 2026-09-07 — R386 canonical source integration and oracle preservation

- Checkpoint: suite53768 exit0/repository safety passed. Same oracle25874 remains live; poll through final parity result before any bootstrap/build. No runtime/Simulator.

- Prior turn progress: gameplay control and prepared exact overlay. Same suite63994 finished0; old run applies to pre-integration state only. No runtime/build/Simulator. Added psq_scale.reference_source accepting only original1350d119 or exactcandidate05e221c0; known candidate reversed to byte-exact original, all other identities refused. Focused scale/patch tests verify recovery and unknown-source rejection.
- Updated historical whole-kernel, first-pass, dead-PC-entry, signed16-load and two-range dispatch tests to compile a temporary exact original cpu.c, preserving original oracle semantics rather than silently swapping both sides. Full-kernel --psq-scale compiles recovered original against factor-transformed candidate; source/helper/header pins remain strict. Historical isolated build scripts remain pinned/fail-closed and are not reused as canonical integration.
- Wired0022 patch in bootstrap: hash declaration74ca7e8...7486, peel first before oldercpu overlays, restore last on failure, apply after original1350d119 identity check, final05e221c0 identity check and Dolphin scope inclusion. Bootstrap11518 and repeat79734 exit0; final source exact05e221c01bead6801c51217f84398bd10f22e1b810f6d06977e04d8a801436ea. Focused overlay test confirms original→testedcandidate→original and rejects reapply, plus wiring assertions. Normal installed app/active module unchanged.
- Post-bootstrap suite53768 running generated/check-r386.log; whole-kernel integrated-source oracle25874 running generated/psq-scale-integrated-oracle-r386.log. Poll SAME handles, no bootstrap/build while source-reading tests run. Next both terminal then canonical separate-output build using existing source-identity cache mechanism, confirm source/header/flags and candidate binary equality before selection/package. Full PRD/performance/audio/mobile hold intact.

## 2026-09-07 — R385 gameplay control and prepared reproducible scale overlay

- Checkpoint: suite63994 confirmed live via same-handle poll; continue generated/check-r385.log to terminal. No game/build/Simulator. Bootstrap has not been changed or run for the new overlay.

- Prior turn progress: candidate real-save/lifecycle pass and retained gameplay window. Rechecked status/processes/save106e before launch. Control runtime84607 using same671729c6runner/explicit549988module/Metal1x/VI-only and prepared R384profile. Visible title→real-save load89418→one-star Observatory→controls52670 changed Mario/camera to matching platform-edge view, then anchored60s399115807471083..399175807471083 without UI. Both input handles exit0.
- Control3597VI59.95Hz, meanCPU15.659909912ms, wallp9917.69525ms, dropped0/invalid0/reset0. CandidateR384 same59.95Hz/15.669609589ms/p9918.299375ms: meanCPU~0.062%higher (effectively unchanged in this pair), candidatep99~0.604ms higher. No material mean/cadence gameplay regression observed, but no improved tail-pacing claim and only one gameplay pair. Movie gain confirmed separately both orders. Cleanclose84607 exit0/native4217240844/fallback0/smc_failed0/save106e...2c90 unchanged. Whole4underruns/6backlogs are not window/audio acceptance. No game/Simulator/build remains.
- Prepared unapplied patches/ModernGekko-dolphin/0022-psq-store-scale.patch SHA74ca7e8c82bf32d25af12bde4cf1b4318ceb7a34ad7e0925540e415227fd7486. New test-psq-scale-patch.py applies it to temporary exact pinned CPU source, requires byte-exact tested candidate05e221c0...36ea, rejects second application and verifies exact reverse. Focused pass; ref source untouched. Added to repository suite, generated/check-r385.log running.
- Next bootstrap canonical integration: declare/hash-pin new overlay, peel before older cpu.c overlays and restore/apply after them, include in Dolphin scope, retain pre-overlay1350d119 CPU identity check and verify final05e221c0 identity. Update source-pinned test oracles by exact reversal rather than blessing arbitrary source; ensure module source identity changes cache key. Bootstrap/repeat separately from source-reading tests, canonical build isolated, exact candidate comparison/audit before marker/package promotion. No normalapp/module change yet; full PRD/performance/audio/mobile hold remains.

## 2026-09-07 — R384 candidate real-save gameplay and lifecycle

- Prior turn progress: reverse movie gain confirmed. Rechecked status/processes/save106e8248...2c90 before launch. Normal671729c6runner with explicit c0021b9fcandidate, prepared psq-scale-gameplay-r383 profile/no states, Metal1x/VI-only. Runtime66186 loaded exact module, titleA+B visually ready, real-save load51022 exit0 and one-star Observatory visibly confirmed.
- Controls32469 exit0, Mario position/camera visibly changed. Fixture sends jump/Spin but individual effects were not captured, so no separate visual jump/Spin claim. Anchored60s398648943739125..398708943739125 after controls and scene confirmation, no UI until after interval. Window3597VI59.95Hz, CPUmean15.669609589ms, wallp9918.299375ms, invalidCPU0/reset0. Candidate-only interval is not a paired gameplay-performance gain claim.
- NativeMisc Togglepause explicitly set Paused title; resume cleared it. Resumed controls7021 exit0 and Mario visibly moved to other platform edge. Clean UI close66186 exit0/native4729714832/fallback0/smc_failed0/save106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90 unchanged.
- Whole302s run includes23.4s deliberatepause,7underruns/11backlogs: not audio or soak acceptance. VI buffer dropped328 late samples. Verified append-only first16384 retention in vi-timing-buffer.h; last retained timestamp398801281769708 is92.338s AFTER fixedwindow end, and summary checks full coverage/monotonic timestamps. Thus earlier60s window retained, but do not claim complete whole-run trace or repeat runtime solely for late overflow.
- No game/Simulator/build remains; normal app/module untouched and R381suite applies (no source changes). Prepared psq-scale-gameplay-control-r384 from identical R374Config/Wii/config.ini/no states/independent pipe, save106e hashverified. Next fresh control same real-save/load/control route and60s interval; avoid unnecessary lifecycle pauses on control and close promptly. If gameplay timing does not regress, integrate canonical reproducible scale overlay then audit/rebuild exactcandidate identity; full PRD/performance/audio/mobile hold remains.

## 2026-09-07 — R383 reversed movie pair confirms modest scale gain

- Prior turn progress: audited candidate and positive first movie pair. Rechecked dirty checkout, exact549988control/c0021b9fcandidate hashes and no game/build/booted Simulator. Reused verified private R381 profiles with new reverse-r383 evidence subdirectories; no source/config/module changes. Candidate first, control second; same normal671729c6runner, explicit module overrides, Metal1x/CPUThreadtrue/VI-only, exact visibly verified slot3 and trigger, full60s without UI.
- Candidate16203/trigger85662 exit0; window398049305038041..398109305038041:3411VI56.85Hz, CPUmean17.390125526ms, p99wall19.3165ms. Control84818/trigger46620 exit0; window398277102376541..398337102376541:3385VI56.416667Hz, CPUmean17.542845597ms, p99wall19.405333ms. Reverse gain approximately0.7681%cadence/0.8706%CPU; both orders favor candidate. Combined equal-window means56.908333candidate versus56.325control (~1.036%cadence), approximately1.005%CPU reduction. No third unchanged movie pair.
- Both visually advancing movies, dropped0/invalidCPU0/reset0, clean native-only exits (candidate883265792/control1327630256, fallback0/smc_failed0), both savea5743199...afa6 unchanged. Whole audio46/61underruns and5/3backlogs span unequal140.8/184s runs/startup/state-load/UI; not matched audio acceptance. Final captured movie frames differ with elapsed UI/close timing, not an exact end-frame comparison. Fixed-window summaries remain authoritative.
- No game/Simulator/build remains. Prepared psq-scale-gameplay-r383 from R374Config/Wii/config.ini only, independent pipe/no savestates, real save106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90 verified. Next candidate real-save title→Observatory→movement/Spin/pause/resume→clean close, and gameplay timing regression before any canonical integration. Existing suiteR381 applies; normal app and marker unchanged. Full PRD/performance/audio/mobile hold intact.

## 2026-09-07 — R382 scale candidate audited and first movie pair

- Previous turn progress and verified link wait. Revalidated SAME35607/ld7796 live through6m03, no restart; then35607 exit0/module audit passes. Candidate c0021b9f2fad3acc3746b6a582b46935e2f7b60bc38434ad7d485564957c7939 remains unselected. R381 fullsuite applies; no source changes this turn.
- Sequential control18728/trigger88358 and candidate47851/trigger83226 all exit0, same normal runner671729c6...d762 and explicit verified module overrides549988/c0021b. Prepared R381 private profiles, exact slot3 visibly loaded before each identical trigger, Metal1x/CPUThreadtrue, only buffered VI timing. No builder/profiler/Simulator overlap. Full60s clock-anchored windows with no UI; both final images visibly show advancing Peach movie, then clean UI close.
- Control window397577952129416..397637952129416:3374VI=56.233333Hz, CPUmean17.578963163ms, wallp9919.214042ms. Candidate397780014040208..397840014040208:3418VI=56.966667Hz, CPUmean17.378905656ms, wallp9919.088209ms. Approximately1.3041%cadence improvement/1.1381%less CPU in one pair. Build heat/order/host differences remain possible confounds; require reverse-order confirmation before selection, then real-save gameplay/lifecycle regression.
- Both traces dropped0/invalidCPU0/counterreset0. Native control833091163/candidate936756152, fallback0/smc_failed0, both savea5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 unchanged. Whole-run audio50/49underruns and4/4backlogs include startup/stateload/UI/unequal durations and are not matched audio acceptance. Different shutdown projection follows different close timing, not an exact scene-equivalence assertion. Evidence generated/runtime/psq-scale-{control,candidate}-r381.
- No game/Simulator/build remains. Normal app/module marker untouched; no promotion/publication or G6/performance/audio acceptance. Next reverse candidate→control pair with same protocol, avoiding another full build. Full PRD and performance-first mobile hold remain.

## 2026-09-07 — R381 kernel timing and isolated scale module

- Checkpoint: suite80439 terminal0/repository safety passes. Same35607 remains live; ld7796 verified688.6%CPU at2m51. Continue build handle to terminal audit before runtime; no restart or further source edits while builder executes.

- Prior turn progress: quantizer/kernel correctness and suite passed. Revalidated same19629: terminal0. SavedPGO/strictFP/ThinLTO CPU ABBA sums12pattern means control25023.774/25113.937ns, candidate24531.101/24532.800ns; means25068.8555 versus24531.9505, approximately2.1417%less candidate CPU. Both text229376bytes; same1of33kernel profile mismatch/unprofiled harness recorded. This is full-kernel evidence, not FPS or whole-module proof.
- Extended isolated builder with mutually exclusive --psq-scale against current549988module/da2defheader. Source transform changes only exact quantizer factor; all1329object identities, original/candidate compile flags, savedPGO, compiler and link response verified. Reused R376 unchanged-control relink only after graph/object/compiler/command/response/hash equality gates; control remains5499889a558e44197bb740b3b3c5cc96187bcd80e3a8df097b814cb2703692e1. Candidate source05e221c01bead6801c51217f84398bd10f22e1b810f6d06977e04d8a801436ea, original CPU source untouched.
- Build35607 live in generated/build-psq-scale-r381.log; candidate cpu.c compiled, expected1of51profile mismatch recorded, linking via ld7796 (verified637%CPU at58s). No restart or app selection. Fullsuite80439 running generated/check-r381.log. Continue same handles; no runtime measurement until both terminal and module audit passes.
- Prepared separate psq-scale-{control,candidate}-r381 Config/Wii/config.ini, only slot3aea2b7a3...b7ab8 and savea5743199...afa6 verified equal; independent neutral pipes, Metal1x/CPUThreadtrue. No game/Simulator. Next matched60s movie comparison with current runner/explicit audited module override, no phase trace/build/profiler overlap; finish well within VI recorder capacity. Full PRD/performance-first mobile hold remains, no promotion/publication.

## 2026-09-07 — R380 exact quantized-store scale experiment

- Prior turn made progress: completed movie comparison and parked negligible dead-PC change. Current dirty checkout preserved; no game/Simulator/build at entry. Revisited original PRD reference-cadence and performance-first order plus R310 profile: ldexpf157 and its dyld stub86 of31256 CPU samples. These samples are attribution, not an estimate of removable total cost.
- Hypothesis: quantized pixel stores need exact2^scale for a signed six-bit GQR scale, but currently call ldexpf. scripts/psq_scale.py pins actual CPU source1350d119...edb69 and replaces only quantizer factor construction with exact IEEE float exponent bits for[-32,31], retaining libm fallback outside that range. Original conversion/multiply/clamp/order remain. Candidate is offline; no ref/app/module change.
- tests/test-psq-scale.py verifies exact source refusal/reversal, extracts actual quantizers and compares672816cases per ASan/UBSan and O2 build:321scales including outside-range fallback,4rounding modes/4integer types/random double bits/special values/saturation edges/preexisting flags/errno. Both pass generated/psq-scale-r380.log. Isolated10M-call CPU ABBA control0.078401958/0.077767125s, candidate0.049246209/0.045098333s, matching digest650342513; finite signed values/GQRscale-3 only, not kernel or FPS evidence.
- Extended existing whole-kernel oracle with mutually exclusive --psq-scale, same extracted source on both sides and candidate-only cpu.c factor change. Oracle86386 exit0: all sanitized/optimized1440cases match882be09b1f38e9ad,624yields/480FPfaults/480illegal/192interruptions/96callerreturns/18816callbacks/34688journals/1440reservations. Fullsuite29380 exit0/repository safety passes generated/check-r380.log; new focused test included.
- Started savedPGO/strictFP/ThinLTO whole-kernel CPU ABBA19629 in generated/psq-scale-kernel-bench-r380.log; same-handle poll confirms live. No concurrent tests/runtime while timing. Continue same live handle; only whole-kernel result can justify considering an isolated full-module experiment. Full PRD/performance/audio/mobile hold unchanged.

## 2026-09-07 — R379 cleanup verification and bounded candidate result

- User-authorized other-project simulator erasure already completed R283; live Devices492MiB/all shutdown/filesystem66GiB available. No new deletion or simulator launch. Original R283 report distinguishes59GiB device-folder reduction from approximately57GiB eventual physical reclamation. Installed runtimes/device definitions, project inputs and local saves retained; erased simulator-installed apps/saves cannot be recovered from those containers.
- Resumed existing candidate17940/PID6039, but elapsed runtime already exceeded16384-sample recorder capacity before trigger. Closed UI cleanly and polled same17940 to terminal; shutdown native3431584402/fallback0/smc_failed0/save unchanged. Trace dropped3606; no performance measurement claimed. Preserved original files rather than overwriting them.
- Fresh candidate runtime28080 reused identical private profile/module/runner with new retry-r379 evidence directory. Visually verified exact slot3 castle checkpoint, trigger84968 exited0, steady-clock window396436413054041..396496413054041. No UI during full60s; final image shows advancing Peach movie. Clean UI close28080 exit0/native758414368/fallback0/smc_failed0. Savea5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 unchanged. No game/Simulator/build remains.
- Fixed window3387VI=56.45Hz, meanCPU17.513327451ms, wallp9919.45325ms; dropped0/invalidCPU0/counterreset0. Compared R378 control56.366667Hz/17.560407621ms/19.919458ms p99: approximately0.148% cadence and0.268% CPU edge. One sequential pair with host/order differences is not material improvement or promotion evidence. Whole-run50underruns/3backlogs include startup/state-load/UI and are not fixed-window audio acceptance.
- Park dead-PC candidate unselected; skip unchanged reverse timing for this sub-percent result. Normal runner671729c6/module75db remain unchanged, R376 fullsuite applies (no source changes this turn). Next choose a materially larger measured guest-CPU hypothesis from existing profiles, preserving strict arithmetic/exception/memory semantics. Full PRD, G6 performance/audio and mobile hold remain open; no publication.

## 2026-09-07 — R378 candidate audit and first movie pair

- Previous turn progress/control relink exact; revalidated SAME63125/ld5439 through active link without restart. Build63125 exit0/module audit passed, candidate03ec7901b3a4ee8fe5916cd0fad890caa500b49004f6b1a1c71ca090f13153ed remains unselected. FullsuiteR376 applies; no source changes this turn. No game/Simulator before measurement.
- Control runtime46270 using current normal runner671729c6...d762 with explicit verified549988control override, only VI recorder, Metal1x/private prepared R377 profile. Visually loaded exact slot3pre-attack checkpoint, trigger49625 exit0 anchored window395793645976000..395853645976000. Movie visibly advanced, clean UI close46270 exit0/native1481710904/fallback0/smc_failed0/savea574...afa6 unchanged. Fixed60s56.366667VIHz/CPUmean17.560407621ms/wallp9919.919458ms; evidence dead-pc-movie-control-r377. Whole audio counters include startup/state load/UI, not acceptance.
- Candidate runtime17940 started same runner/settings with explicit03ec...53ed override and separate identical save/checkpoint/pipe. Poll same handle/load checkpoint/trigger/fixed60s, then clean close and compare; no promotion from control-only result. No builders/profilers/Simulator overlap. Installed module remains75db...323c/full PRD/performance-mobile hold intact.

## 2026-09-07 — R377 control relink verification and runtime preparation

- Previous turn progress: all95site oracle and fullsuite passed; build verified live. Revalidated same63125/ld4515 live4m27~696%CPU, did not restart quiet link. Later same-handle poll shows control byte-identical and candidate compilation complete, now candidate linking. Exact control SHA5499889a558e44197bb740b3b3c5cc96187bcd80e3a8df097b814cb2703692e1;1329object identities/currentda2defheader/unchangedstrictFP savedPGOflags verified in provenance. Expected1of33function profile mismatch in changed chunk recorded, not suppressed. Candidate not audited/selected yet; continue63125.
- Prepared separate ignored dead-pc-movie-control-r377 and dead-pc-movie-candidate-r377 profiles from R362control Config/Wii/config.ini and only slot3. Verified42084638byte slot3aea2b7a3fe295b77e409753a216abf8fe6f2f1ec23ee7a8832fc9ac1a27b7ab8 and savea5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6; independent neutral pipes/Metal1x/CPUThreadtrue. No game while linking; no Simulator. After terminal candidate audit, matched sequential movie test with same runner and explicit module override, fixed60s/known trigger, no phase trace/other builders. No gain or full-goal claim. Normal671729c6app/75dbmodule unchanged; no new code tests needed for preparation-only turn, R376suite applies.

## 2026-09-07 — R376 every changed PC site and isolated module build

- Checkpoint: fullsuite18256 exit0/repository safety passes generated/check-r376.log. Build63125 remains live at control relink; ld4515 verified610%CPU at1m27 earlier, subsequent same-handle poll still running. Do not restart on quiet log. No candidate/normal-app change yet; next poll same63125 through exact-control gate and candidate build.

- Previous turn progress: whole-kernel parity and ~0.64%CPU microbenchmark edge, fullsuite green. Current status/git/process/Simulator checks preserve dirty checkout/no runtime. Added tests/test-thp-dead-pc-entries.py extracting actual reference AND transformed bodies at all95sites, invoking real pinned FP/exception helpers. Tests random incomingPC/FPSCR/MSR/state, all lazy-FP/FP-availability combinations, four host rounding modes, special/random operand bits, overlapping actual destination/source registers, fault returns and a capturing/mutating observer after next-PC materialization.
- Focused26201 exits0 in ASan/UBSan and optimized strictFP builds:194560cases each,48640unavailable faults/145920next-PC observers, completeCPU snapshots/return/hostFPflags agree. Observer snapshots include PC before observer mutation. No guest arithmetic/helper/exception implementation replaced by a stub. Added to fullsuite.
- Extended existing isolated one-object builder with mutually exclusive --dead-pc against current unsigned549988...692e1 and headerda2def...9336. Historical experiment pins remain old/default; new branch uses current canonical graph, hashes all1329objects/1322chunks, preserves strictFP/ThinLTO/savedPGO flags, and must relink unchanged control to exact current module before candidate compile. Only chunk1102 transformed; fresh generated/dead-pc-r376, no marker or installed app changes. Build63125 running generated/build-dead-pc-r376.log; suite18256 running generated/check-r376.log. No runtime timing during build/tests. Poll same handles; after exact control/candidate audit, assess runtime comparison, not promotion from unit tests. Full PRD/performance/mobile hold unchanged.

## 2026-09-07 — R375 offline unobserved arithmetic PC stores

- Suite56374 exit0/repository safety passed (generated/check-r375.log). All handles terminal/no runtime/Simulator. Existing tests/test-ps-fprf-chain.py provides exact helper/full-state/host-FP scaffolding for next95site direct-entry oracle; do not substitute whole-kernel aggregate parity for that missing coverage.

- Timing56789 exit0: savedPGO CPU-time ABBA sums of12pattern means control25094.334/25051.182ns, candidate24934.183/24890.437ns; means25072.758 versus24912.310 (~0.640%lower candidate). This is a small whole-kernel result, not full-module/FPS proof. Both sides report same1of33profile mismatch and unprofiled harness; strictFP retained. No unchanged third timing run. Before module build, add direct-entry coverage for all95changed sites, including arbitrary PC initialization/unavailable-FP and observers at the following instruction. Whole-kernel1440cases do not exhaust all entry states. Suite56374 running generated/check-r375.log after timing (no contention); poll same handle. Normal app unchanged.

- Oracle68724 exit0, all sanitized/optimized reference/candidate digests882be09b1f38e9ad match:1440cases/624yields/480FPfaults/480illegal/192interruptions/96caller returns/18816callbacks/34688journals/1440reservations. Tightened recognizer to exactly four observed ppc_ps add/sub/mul/madd helper names and exact95site count after oracle completed; resulting transformed bytes unchanged. Added cheap structural regression covering byte-exact reversal, unknown/reapplied-source refusal and unknown helper/return/observer rejection; passes and included in full suite.
- Started savedPGO whole-kernel strictFP CPU-time ABBA56789, generated/dead-pc-bench-r375.log. No concurrent suite/build/runtime while timing. Poll same handle; correctness is not speed proof. Candidate remains isolated/unselected and normal671729c6 runtime/75db module unchanged. Run repository suite after timing finishes.

- Previous turn progress: fresh-profile slot write/reload verified and candidate promoted. Current status/git/process/Simulator checks preserve dirty checkout/no runtime. Reviewed R310 actual CPU profile and parked experiments; no repeated S16/layout/FP-fast-math/unchanged wait capture. New hypothesis: 95 pure arithmetic instruction PC stores inside exact THP kernels are overwritten at adjacent next-PC materialization with no intervening observer, but external arithmetic helper calls prevent compiler elimination. Preserve normal FP arithmetic and unavailable-FP PC exactly.
- Added isolated scripts/thp_dead_pc.py pinned to exact98d986...9891 kernel fixture. Recognizes only exact annotated arithmetic/availability/helper bodies followed by adjacent next label with explicit PC store. Replaces unconditional PC assignment with assignment only on lazy-FP-unavailable path before original availability call; no instruction/return/charge/helper/memory ordering changes. Pinned float source's paired add/sub/mul/madd paths modify FP state but neither read PC nor call host observers. No selected generated source/runtime/app change.
- Extended existing full-chunk oracle with mutually exclusive --dead-pc option; both sides use same extracted kernels, candidate alone transformed. Oracle68724 running generated/dead-pc-oracle-r375.log, tests full-state/memory/callback/exception/yield behavior under sanitized/optimized builds. Poll same handle; if parity passes, time whole kernels with savedPGO/strictFP and CPU-time ABBA before any module build. No runtime/FPS benefit claim. Full PRD/performance/mobile hold remains.

## 2026-09-07 — R374 fresh-profile slot persistence and promotion

- Previous turn progress: canonical directory fix built/audited and fullsuite passed. Initial git/process/Simulator checks preserve dirty checkout/no runtime. Prepared generated/runtime/profile-r374 from prior Config/Wii/config.ini with independent pipe; explicitly verified StateSaves absent and save106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90 before launch. Candidate runner671729c6bb32e29ec44e95dedae0044c9c3d7e64f99736653f3146d806d3d762, bundledmodule/no override/no diagnostics, Metal1x.
- Runtime24399/PID2664 created StateSaves automatically. Visible title→fixture46743→one-star Observatory. States Save1 wrote39042803byteRMGE01.s01 SHA4a4217264a1968fc6b93fce8fb213eb9e50af1aaebf378cd2f14be9c28f15edb. Controls71221 visibly moved Mario/camera; Load1 restored original platform-center position and original camera. Restored-controls73555 visibly moved again; all input handles exit0. Clean UI close24399 exit0/native4056751512/fallback0/smc_failed0. Slot/NAND hashes unchanged afterward; no game/Simulator. This proves fresh-directory slot1 write and same-process restoration, not every slot/corruption/cross-process or in-game new-save acceptance.
- Copied exact tested candidate to separate staging, private audit and runner cmp passed, then moved normal app to generated/macos/GalaxyPad.app.previous.r374 and activated tested copy. Promotion88449 exit0; normal runner now671729c6...d762, signed module remains75db...323c. Original candidate and rollback remain intact; no rebuild during promotion. Audit evidence generated/audit-promotion-r374.log, runtime evidence profile-r374. No code changes this turn; R373 fullsuite applies. Whole-run counters include UI/state serialization and44underruns, so no performance/audio acceptance. Next return to established heavy movie CPU hotspot work with a new concrete semantics-preserving hypothesis; no unchanged tail-probe loop. Full PRD/mobile hold unchanged.

## 2026-09-07 — R373 standard fresh-profile directory setup

- Completion: build77238 exit0, candidate audit90150 exit0/private app audit passed (generated/audit-profile-r373.log), suite9513 exit0/repository safety passed. Candidate runner671729c6bb32e29ec44e95dedae0044c9c3d7e64f99736653f3146d806d3d762; signed module remains75db109bdfb1a1b096eed361c20e0e36443b0e31d5296600a305ce25a2e2323c. All handles terminal/no game/Simulator. Normal app unchanged. Candidate functional fresh-profile save/load still required; do not claim file-write/persistence success from source guard or build.

- Previous turn progress: no-UI trace refuted capture as necessary for tails; unchanged observation branch stopped. Current dirty checkout preserved, no game/Simulator. Source Runtime::Create sets user path then calls UICommon::Init without standard UICommon::CreateDirectories; upstream UICommon.cpp296 creates StateSaves. Hypothesis: invoking standard creation between path selection and Init fixes fresh-profile slot writes, without wrapper path guesses or changing external UI ownership.
- Added canonical0026-runtime-user-directories.patch SHAeae4e6f3b8476354b5b05e9e006646cbca8ce1e4fe17597d2b6628f498aeb6a2: one call inside !s_external_ui_common. Bootstrap pin/peel-before-overlays/restore-last/apply-last/scope wired; bootstrap68699 and repeat78871 exit0. Structural regression test-runtime-directories.py failed on original missing call, passes patched order/external guard/upstreamStateSaves/pin/peel/restore/scope. This is structural coverage, not runtime file-write proof.
- Separate candidate build77238 running generated/build-profile-r373.log, output generated/macos/profile-r373/GalaxyPad.app; normal app/module selection unchanged. Current canonical source rebuilds199 runtime objects after bootstrap timestamps; no module regeneration. Initial suite raced package bootstrap's temporary overlay peel and failed missing-call assertion; confirmed bootstrap completed, actual source test and reverse-check pass, then launched suite9513 generated/check-after-bootstrap-r373.log. Do not run source-reading suite concurrently with bootstrap again. Poll same handles; next candidate fresh-profile save-state write→movement→reload/clean exit before selection. No game/Simulator/publication; performance/mobile hold unchanged.

## 2026-09-07 — R372 remove late UI observation from audio diagnostic

- Completion: load81888/controls82589/runtime78769 exit0. Last UI capture confirmed one-star Observatory BEFORE clock anchor; no UI queries/capture afterward. Exact PID97598 revalidated at end then SIGTERM invoked existing flag/watcher/RequestStop path, no forced kill. Window392973275282125..393093275282125 (120s), continued until signal after full window; VI export saved/dropped0, valid clocks/no resets. Save106e...2c90 unchanged/native4540124950/fallback0/smc_failed0. No game/Simulator. No product changes; R368 suite still applicable.
- correlation.json validates DMA trace67869=shutdown count and full window. Window59.941667VIHz/CPUmean15.633516639ms/wallp9917.739959ms; exactly32000DMA guestframes/s and48000callbackframes/s. At+35.488..35.579s:90.357459msVI/19.776334msCPU/3.519709msEFB/0.379283msidle/0DVD/gather/0.027874mswakeup. Thus no UI capture is required for a large tail. Trace mutex/IO and host conditions remain confounds; unaccounted wall time is not proof of scheduler or driver fault. This reproduces R303-class small-named-wait tails, not justification for new synchronization policy.
- Whole-run5underruns/7backlog corrections. First underrun precedes window91.4s, last+143.72s around stop sequence; endpoint-only counters cannot classify every interior event, so no active-window-zero claim. Stop unchanged observation loops; no evidence to enlarge buffer or alter waits. Next bounded correctness work: fresh-profile StateSaves creation defect previously reproduced R352. Source search locates standard UICommon.cpp296 creation and StateFile.cpp87 path, while modern runtime initialization must be checked for missing setup. Fix/test canonical path without changing timing policy, then return to heavy movie CPU bottleneck. Full PRD/performance-first mobile hold unchanged.

- Previous turn progress: correlated wait-heavy audio endpoint, outside fixed60s interval near UI observation. This turn changes observation procedure only: same diagnostic runner856b2e...2b0e, selected module, graphics/save/input, phase and VI recorders; no UI calls after active window begins and existing SIGTERM handler for graceful shutdown. Source moderngekko_run.cpp confirms signal flag→watcher→RequestStop, not forced kill. A quiet result would not prove all prior hitches were observer-caused; a repeat would refute UI capture as necessary for the hitch.
- Fresh audio-no-ui-r372 Config/Wii/config.ini copied from R371, independent pipe/no savestates. Initial game/Simulator inventory empty, dirty checkout preserved. Runtime78769 started; continue same handle through visually confirmed real-save Observatory, then clock-anchored120s with controls only at start, no UI queries/capture, exact-PID checked graceful stop. No product/app/audio-policy changes. Full PRD/G6/mobile hold remains.

## 2026-09-07 — R371 paired audio/VI CPU-clock diagnostic

- Completion: title/one-star Observatory and changed position after controls visually verified; load47655/controls59770/runtime64924 exit0. VI export saved/dropped0, all clocks valid/counters monotonic. Save106e...2c90 unchanged, native3402627344/fallback0/smc_failed0; no game/Simulator. Normal package/product source untouched; R368 tests remain applicable.
- Fixed60s392486715612083..392546715612083 contains3597VI59.95Hz/3596whole intervals, CPUmean15.67476577ms, wallp9918.207709ms. Largest23.442208ms wall/21.444042ms CPU is execution-heavy. Phase DMA53677 exactly matches shutdown. First underrun precedes fixed window by56.42s; last ordinal51275 brackets+78.09984..78.16925s, AFTER fixed window. It overlaps84.773125ms VI with20.070834ms threadCPU,48.486245ms idleFlushGpu,0.613083msEFB,0DVD/gather,0.031417mswakeup. Subsequent39.50/30.91ms frames also recover slowly; last backlog+80.66s. Endpoints cannot count interior events, so do not claim zero active-window underruns.
- This links a wait-heavy tail to audio starvation but reproduces the already known R247/R248 FlushGpu boundary rather than discovering a new synchronization bug. Source CoreTiming.cpp582 times the existing FlushGpu call, not sleep or GPU execution specifically; elapsed counters overlap. Large tail occurred near end-of-run UI observation, outside the promised untouched60s window, so capture activity remains a confound. Do not shorten wait/disableSyncOnSkipIdle/change audio reserve or claim macOS scheduling. Next different-variable experiment: no UI queries/captures after starting measurement, signal-based graceful close with exact PID/handle verified; inspect resulting full trace and endpoints. No unchanged third wait-only run. Analysis corrected an initial hyphenated-module import error with runpy, producing valid correlation.json from original CSV/log.

- Previous turn progress: trace localized active-play underrun with near-target mean production but69ms DMA/89ms VI stall. No initial game/Simulator, dirty checkout preserved. Hypothesis: existing per-VI thread CPU clock plus named wait counters distinguish execution-heavy tails from off-CPU/wait-heavy tails when correlated with DMA events. Neither trace nor CPU-clock timing is acceptance performance evidence.
- Verified diagnostic runner856b2e...2b0e and existing source recorder CLOCK_THREAD_CPUTIME_ID, cumulative EFB/throttle/idle/DVD/gather/wakeup counters, post-join export. No rebuild or product modification. Fresh audio-cpu-r371 profile from R370 Config/Wii/config.ini, independent pipe/no states, save106e...2c90 verified. Launched diagnostic app64924/PID97117 with explicit selected canonical module549988...692e1, phase.csv and vi.csv enabled, completion diagnostics unset. Existing normal package remains untouched. Continue same handles through title/save→defined active window→clean close, then correlate audio ordinal endpoints with VI CPU/wall deltas. At most one runtime/no Simulator.

## 2026-09-07 — R370 audio-event localization

- Completion: title and one-star Observatory visually verified, load39486/controls62098 exit0. Marked exact60s steady window392072117502541..392132117502541; input sequence in first~7s then neutral, no menus/pause. End view remains Observatory. Runtime4592 cleanexit0/native2778999045/fallback0/smc_failed0; save106e...2c90 unchanged. No game/Simulator. No product changes; R368 suite remains applicable.
- CSV DMA count44888 exactly matches shutdown enqueues; unique trace ordinals and complete window validated. Window3596VI/14998DMA granules/5625callbacks at512frames →59.933Hz/~31995.7guestframes/s/48000hostframes/s. Whole-run8underruns/7backlog corrections: last underrun ordinal28957 brackets+19.3988..19.4185s, last backlog29298 brackets+20.6837..20.6933s. First events precede gameplay; endpoints do not reveal all event times. Concurrent record-before-counter increment makes brackets approximate at enqueue boundaries, not exact callback timestamps.
- Largest active DMA gap69.35575ms at+19.2980..19.3674s drains queue24→6, then15–20ms production gaps drain to2. VI gap89.06375ms overlaps+19.2946..19.3836s; frame_end88.936ms. Audio callback max11.384ms over window, showing demand continues while guest/video production stalls. Evidence window-summary.json/stall-summary.txt beside original trace/log. This disproves all-underruns-are-startup/pause for this diagnostic run, but trace locking/IO overhead and lack of CPU-time fields prevent host scheduling versus guest-work attribution. Do not enlarge reserve/change servo from this alone. Next existing diagnostic VI CPU/wall recorder plus audio-event localization; keep selected module and settings fixed. Heavy movie deficit remains separate/unresolved.

- Previous turn progress: installed-package real-save/lifecycle smoke passed. No initial game or booted Simulator, dirty checkout preserved. Source inspection shows audio underrun/backlog counters retain first/last DMA enqueue ordinal only; aggregate pause-inclusive counters cannot isolate active play. Existing default-off phase trace records every DMA enqueue and timestamp, permitting ordinal correlation without changing audio policy or rebuilding.
- Hypothesis: a no-pause real-save Observatory run distinguishes active-play underrun endpoints from startup/transition events. Trace's mutex/line-buffered writes add observer overhead, so this is diagnostic localization, never acceptance FPS. Fresh audio-active-r370 profile copied Config/Wii/config.ini from R369, no savestates; save106e...2c90 verified and independent neutral pipe configured. Normal packaged runner with bundled module and only GALAXYPAD_PHASE_TRACE enabled; runtime4592/PID96577, module-loaded path confirmed. Continue same runtime, visually load save, mark steady-clock active window, run controls, close cleanly, correlate first/last event ordinals with DMA trace. No app/source/audio setting change.

## 2026-09-07 — R369 installed-package real-save smoke

- Completion: load37662, controls54622 and resumed-controls80968 exit0. Visually verified one-star Observatory, changed Mario position/camera after each control sequence; native Toggle pause adds Paused subtitle and resume removes it. Individual jump/Spin effects were not separately captured. One stale AX menu index failed before activation; refreshed tree and correct index used. Private screenshot saved via runtime menu at generated/runtime/package-r369/ScreenShots/RMGE01/RMGE01_2026-09-07_13-39-27.png. Normal close14607 exit0, native2498717729/fallback0/smc_failed0; save106e...2c90 unchanged, no remaining game/Simulator. No source/app changes this turn; R368 full suite remains applicable.
- Aggregate182s runtime includes approximately18s native pause plus startup/transitions, so9831 frames, four underruns and seven backlog corrections cannot establish uninterrupted cadence/audio acceptance. Cubeb errors0/queue-full0, input ends buttons0. Next measure uninterrupted gameplay/audio on selected normal package, with a defined active interval and no pause; do not repeat unchanged dispatcher/movie A/B. Heavy movie56Hz and full G6/audio/soak/story/mobile goals remain open.

- Previous turn progress: selected-header regression repaired and full suite53489 passed. Current state preserves dirty root289a87a, no initial game/booted Simulator, source pins ModernGekko0514d9/Dolphin13e492 and DOL2c680585 verified. Hypothesis: module-only promotion preserves normal-package real-save gameplay and clean lifecycle; diagnostic-runner evidence alone is insufficient.
- Private app audit19483 exits0 (generated/audit-app-r369.log). Packaged runner e86f2e09d27f69a41d1f10c2513cde14898557bc379fa787be013b5f0122b689 and signed module75db109bdfb1a1b096eed361c20e0e36443b0e31d5296600a305ce25a2e2323c verified. Prepared fresh generated/runtime/package-r369 from R363 Config/Wii/config.ini, no savestates; configured independent neutral pipe. Save106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90 before launch; source save untouched.
- Launched normal packaged wrapper with STATICRECOMP_MODULE and optional diagnostic environment unset, Metal1x/extracted run1/private user-dir/allow-interpreter/no-mods. Runtime14607 PID96117 loaded bundled module (runtime.log confirms exact app path); title A+B visually confirmed, load fixture37662 running. No second game or Simulator. Continue same handles, verify Observatory input/native pause-resume then clean close/save hash and fallback counters. Not performance/audio/soak acceptance.

## 2026-09-07 — R368 cleanup verification and selected-header regression repair

- Completion: suite53489 exited0 including repository safety (generated/check-r368.log). All handles terminal/no game or Simulator. Installed-package runtime smoke remains next; not run during this test-only repair.

- User-authorized other-project Simulator erasure was already completed R283. Live inventory confirms all devices shutdown, Devices492MiB, DerivedData2.1GiB and66GiB available. No additional deletion; installed runtimes/device definitions and source/input/local saves retained. Previously erased simulator-installed apps/saves are not recoverable from those containers.
- Read first causal failure in generated/check-r367-selected.log: suite24309 exited1 at test-huffman-tail.py's old-only header hash, not game code. Current selected canonical header differs only in original-dispatch lookup, outside the memory-helper contract exercised by this parked experiment regression. Explicitly allow the two exact audited identities, retaining fail-closed behavior for any unknown header. No generated/product code or app changed.
- Focused65038 exits0:128000 full-state/memory/event comparisons across16entries and8modes in each ASan/UBSan and optimized build (generated/check-huffman-r368.log). Full repository suite53489 running generated/check-r368.log; poll same handle. Root289a87a remains dirty/preserved; canonical module selection unchanged. No game/Simulator. Next suite completion then fresh installed normal-package real-save smoke, not a new performance claim or mobile promotion.

## 2026-09-07 — R367 canonical audit, cache hit and module-only packaging

- Previous turn progress: canonical generation/header/chunk parity verified; link active. Revalidated SAME52431/live93238, waited without restart.52431 exited0; canonical module audits and cmp matches isolated runtime-tested candidate byte-for-byte, SHA5499889a558e44197bb740b3b3c5cc96187bcd80e3a8df097b814cb2703692e1. Manifest exact two_range_policy=rmge01-two-range-v1, sourceFNV28d765f74af57637, existingFPRF/THP policies and savedPGO unchanged. Repeated same separate-output command83592 exits0 with cache hit, no recompilation.
- Strengthened audit-module.sh: legacy/none preserved, declared two-range policy requires1024/indexed, exact tested header SHAda2def...9336 and matching normalized generated.h; unknown/duplicate declarations refuse. Actual audit block exercised by nine synthetic fixture cases in test-two-range-module-audit.py; focused and suite71019 pass (generated/check-r367.log). This supplements moduleABI/architecture/SMC/chunk audit, not a claim that hashes alone prove semantics.
- Saved previous marker to generated/two-range-integration-r365/active-module-before-promotion.txt, then selected canonical module via apply_patch. Module-only package71372 exits0/private app audit passes; previous app retained generated/macos/GalaxyPad.app.previous.20260907T182801Z. New signed module75db109bdfb1a1b096eed361c20e0e36443b0e31d5296600a305ce25a2e2323c. Builder cmp confirms launcher/runner/frontend byte-identical; normal runner remains e86f2e...2b689, frontend61de93...1750. No runtime rebuild or dependency revision change.
- Post-selection suite24309 running generated/check-r367-selected.log; poll same handle. Next fresh normal-package real-save gameplay/save-lifecycle smoke; previous runtime evidence used diagnostic runner, so do not claim installed package runtime acceptance yet. No game/Simulator. Small dispatcher headroom is integrated, but movie56Hz, full G6/performance/audio/soak/story/mobile/PRD goals remain open. No publication/deletion.

## 2026-09-07 — R366 canonical module build and generated-source parity

- Checkpoint: SAME52431 live after1330/1332 compile output; verified linker93238 at18s/~664%CPU. Poll52431 through link/audit; no restart or promotion. Tests terminal/no game/Simulator.

- Previous turn progress: pinned policy/cache/bootstrap wiring and port build passed. Revalidated current status/dirty checkout/no game/Simulator, port69d416...5d7f/profilef39a...60d, normal marker still accepted118044...fc631,68GiB free. Started env GALAXYPAD_MODULE_OUTPUT=$PWD/generated/modules-two-range-r366 GALAXYPAD_PGO_PROFILE=$PWD/generated/pgo/rmge01.profdata bash scripts/build-module.sh, handle52431/log generated/build-module-r366.log. This output owns its own marker only; no normal selection/app change.
- Preflight RMGE01/Broadway/MEM2 passed. Canonical generation selected distinct cacheede0bf4e2d66a29f. cmp verifies both RMGE01.h and normalized generated.h equal runtime-tested R358 candidate, SHAda2def2ce566f57929afe0a8a2f7de5539b17624eb37452d6928790ac6999336. Recursive chunk-source comparison to accepted tree passes unchanged. Actual Ninja flags retain savedPGO/ThinLTO/O2/strictFP/macOS14. Expected one-function profile mismatch plus unprofiled integer-TU warning recorded; no hidden flag relaxation.
- Fullsuite89152 exited0/repository safety passes generated/check-r366.log. Build52431 still live; poll same handle through remaining compilation/link, no restart. Next manifest/header/module audit and comparison with isolated candidate, then package/runtime validation. No runtime smoke before exact canonical artifact exists. Full original PRD/G6/performance/audio/mobile hold remains, no publication/deletion.

## 2026-09-07 — R365 pinned canonical dispatcher wiring

- Completion: port build45774 exit0, SHA69d41668bf3a234f0380c1f530c325f49e01435b965f7209bbbcc211d5e85d7f; suite64927 exit0/repository safety passes generated/check-r365.log. All handles terminal/no game/Simulator. New port ready for separate-output canonical module build; no installed app or selected module change.

- Previous turn progress: compiled policy parity/guards passed. Revalidated status, no game/Simulator, ModernGekko0514d9...97/Dolphin13e492...19. Preserved dirty root and baseline port copy generated/two-range-integration-r365/moderngekko_port.before.cpp. Added tested policy include to port, exact eligibility before cache lookup, two_range_policy key+manifest, and Apply before normalized generated.h copy. Generated scoped0025 patch via baseline diff plus exact policy source, reverse-check passes; SHA1a8378b059fc3ef9ff00285c23d491200ed404a2245ac789e90d8413c4d169bc.
- Bootstrap patch pin/peel/apply/recovery wired around existingDCBZ overlay. Initial90207 exited1 on missing allowed-change scope entry (not compilation/runtime failure). Fixed scope registration and added regression covering it; corrected42385 and repeat15649 both exit0, pinned references/patches verified. No unchanged failure retry. C++ policy parity and wiring test cover cache lookup ordering, manifest, eligibility, header-copy ordering and bootstrap pin/order/scope.
- Port-only build45774 running generated/build-port-r365.log; bootstrap source timestamps trigger199 dependency steps, no game/module/app launched. Log contains existing git ^master revision diagnostic before continued compilation; do not infer terminal failure without process result. Fullsuite64927 running generated/check-r365.log. Poll same handles. Next separate-output canonical module build with savedPGO and exact generated-header verification, then module/package/runtime checks before selection. No canonical-module or app promotion yet; original full PRD/G6/performance/audio/mobile hold intact.

## 2026-09-07 — R364 compiled canonical lookup policy parity

- Completion: suite15683 exit0/repository safety passes generated/check-r364.log. All handles terminal/no game/Simulator. Proposed canonical policy parity proven, wiring next; no build/app promotion.

- Previous turn progress: candidate new-save and fresh-process reload passed. Current authoritative status/dirty worktree preserved, no game runtime. Inspected canonical module-source/FPRF/THP/DCBZ policy and bootstrap patterns; build-module.sh delegates generation/cache to moderngekko_port.cpp. Need exact policy identity in cache and manifest before any cache hit, transform before normalized generated.h copy, plus pinned bootstrap peel/apply/recovery ordering.
- Added patches/experiments/two-range-policy.inc, proposed C++ policy for exact RMGE01 DOL/C/1024/indexed only. Apply refuses unexpected SHA before output open, transforms only lookup body with unchanged prefix/callback suffix, refuses duplicate/missing boundaries/reapply and checks write close. This is newly generated disposable output only. No vendor code or bootstrap changed yet.
- tests/test-two-range-policy.py compiles actual policy under ASan/UBSan and compares complete emitted header byte-for-byte with runtime-tested R358 candidate. Hash-provider seam accepts only independently SHA-verified original input; missing/changed input refuses without mutation, success output matches and second application refuses. Wrong disc/DOL/backend/chunk/lookup return none.48682 exited0. Added to host suite running generated/check-r364.log; poll returned handle. Existing exhaustive pointer and full callback oracles remain unchanged.
- Next implement pinned port/bootstrap overlay and source wiring regression, then rebuild only port tool before canonical module/cache work. Installed app, accepted module marker, vendor source and saved inputs unchanged. No runtime smoke this offline policy-only step; latest exact candidate runtime evidence R363. Full original PRD/G6/performance/audio/mobile hold intact, no publication/deletion.

## 2026-09-07 — R363 candidate new-save and fresh-process reload

- Completion: suite54374 exit0/repository safety passes generated/check-r363.log. All handles terminal/no game/Simulator; reproducible integration next, not full goal promotion.

- Previous turn progress: movie pair shows no meaningful regression, still below60Hz. Revalidated current dirty checkout/no game/Simulator, candidate549988...692e1 and source save5040...64a6. Created disposable dispatch-save-r363 with Config/Wii/config.ini only, neutral pipe, no states. Same diagnostic runner/Metal1x/Core6/CPUThreadTrue/Cubeb120ms; no VI recorder because functional save test, not timing.
- First runtime86776 verified actual module, title, then fixture51556 loaded real one-star Observatory. New Plus0.15s fixture did not open pause. Read exact reference PauseButtonCheckerInGame.cpp:12held frames required. Changed completed fixture to0.4s;69938 opens in-game pause visibly. No product input remapping. g6-observatory-quit-select targets Quit; g6-save-quit-confirm chooses Yes. Game explicitly confirmed saved; A dismissed, final Yes returned to title.
- Save changed to106e8248bd8081404e8258a7ca33c54e0d476d651f139fde5ee2e4aad7302c90. First process closed exit0/native4808982602/fallback0/smc_failed0. No orphan before launch72217 using same profile and candidate; title→file fixture61347 loaded one-star Observatory, control75696 visibly changed position/camera. Clean close72217 exit0/native2682444921/fallback0/smc_failed0, new hash unchanged. No states used; original saves/checkpoints elsewhere untouched.
- This bounded new-save/reload regression passes, not crash-corruption recovery or all-save/soak/audio acceptance. All input/runtime handles terminal; no game/Simulator. Evidence dispatch-save-r363 runtime.log/reload-runtime.log and named input logs, task screenshots. Suite started generated/check-r363.log; poll returned handle.
- Candidate now has semantic oracle, small ABBA gameplay benefit, real-save/Terrace/Pull Star/lifecycle coverage, no meaningful movie regression and new-save/reload proof. Next integrate the source-pinned two-range specialization into canonical reproducible build/cache identity and package/audit/runtime validation. Installed app/marker still unchanged. Full original PRD/G6/performance/audio/mobile hold intact; no new deletion/publication.

## 2026-09-07 — R362 candidate movie regression pair

- Completion: suite21713 exited0/repository safety passes generated/check-r362.log. All handles terminal, no game/Simulator. New in-game save/relaunch regression next; no emulator-checkpoint substitution for persistence proof.

- Previous turn progress: candidate real-save/controls/lifecycle/Terrace/Pull Star path worked, still unselected. Revalidated status/dirty checkout/no game/Simulator and exact candidate549988...692e1, movie slot3aea2...b7ab8/savea574...afa6. Fresh dispatch-movie-{candidate,control}-r362 copies of Config/Wii/StateSaves/config.ini with independent neutral pipes. Same runner/Metal1x/Core6/CPUThreadTrue/Cubeb120ms; explicit module logs verified. CUA loaded slot3 and verified castle checkpoint before established g6-r85-trigger-attack sequence.
- Candidate44944/trigger-top35214 first:3384VI56.4Hz, CPU17.550668613ms,p9919.409917. Control21506/trigger-top1746 second:3380VI56.333333Hz,CPU17.572339819ms,p9919.604875. Exact60s intervals/evidence paths in PERF.md. Both scenes show running movie after window; clean window close exits0. All handles terminal/save unchanged/fallback0/smc_failed0/dropped0/invalid-reset0/no orphan or Simulator.
- Essentially unchanged movie cost (~0.12%CPU difference), no meaningful regression observed in this pair; not reproducible speedup or60Hz/audio proof. Candidate's small gameplay headroom does not solve heavy movie decoder shortfall. No more unchanged movie pair needed. Next new in-game save/relaunch regression before canonical integration/selection. Retain original full PRD/G6/performance/audio/mobile hold. Suite started generated/check-r362.log; poll returned handle. No app/marker/dependency changes, deletion, publication or new goal claim.

## 2026-09-07 — R361 candidate real-save and Terrace interaction regression

- Completion: suite27830 exited0/repository safety passes generated/check-r361.log. All handles terminal; no game or booted Simulator. Candidate unselected; movie and in-game save/relaunch regression next.

- Previous turn progress: reverse-order pair retained small CPU benefit, no promotion. Revalidated status/dirty checkout/no game/Simulator, candidate549988...692e1 and prepared real save5040...64a6; profile initially has no savestates. Runtime62035 explicit candidate with runner856b2e...2b0e, Metal1x/CPUThreadTrue/Core6/Cubeb120ms. Waited past wrist-strap screen and visually confirmed title A+B before fixture30462; it exited0 and real one-star Observatory loaded visibly.
- Control fixture28250 exits0 with visible position/camera change. Native Misc pause displays Paused subtitle, resume clears it; subsequent movement responds. Added bounded g6-observatory-terrace-approach.json (directional segment, not a guaranteed whole route) and g6-terrace-pull-star-aim.json (verified from this interior camera). Existing forward/jump/A fixtures plus screenshot-checked short approach segments reached Terrace entrance, Luma dialogue, interior tutorial, Pull Star transformation, hand-cursor targeting and activation to galaxy-selection tutorial. This is fresh visible interaction/transition evidence; no story-completion or every-control claim.
- Created private StateSaves directory (known fresh-profile initialization gap remains) and saved slot5 at galaxy-selection tutorial: f3368150bfc401ed2758ebf39a86995b93d055aac6bbcfb8fc2c6fcbef1e244d,38934611bytes. File created/hash verified, reload NOT tested. Not an in-game save substitute. Original game save5040...64a6 unchanged after clean exit62035; native9079459951/fallback0/smc_failed0,82792depth peeks,22input transitions. All input handles terminal, no orphan/game/Simulator.
- Functional session~651s includes deliberate~16s pause; VI recorder dropped21667. No timing comparison from partial buffer or pause-contaminated average. Whole-run4underruns/10backlog corrections are not perceptual audio acceptance. Evidence generated/runtime/dispatch-gameplay-r361 runtime.log/vi.csv and named input logs; visible screenshots in task. New fixtures dry-run parse/encode pass. Full suite started generated/check-r361.log; poll returned handle.
- Next movie regression and new in-game save/relaunch before candidate integration/selection. Terrace slot5 can continue interaction/progression checks but cannot satisfy persistence evidence. Normal app/marker unchanged, no publication/deletion; full original PRD/G6/performance/audio/mobile hold remains.

## 2026-09-07 — R360 reverse-order fixed gameplay comparison

- Completion: suite7723 exit0/repository safety passes generated/check-r360.log. All handles terminal. Prepared generated/runtime/dispatch-gameplay-r361 from R352 Config/Wii/config.ini, fresh neutral pipe and no savestates; real save5040...64a6 verified. Not launched yet. Next real-save candidate regression, no repeated fixed pair.

- Previous turn progress: module audit and first runtime pair passed with small CPU difference. Revalidated current dirty checkout, no game/booted Simulator and exact candidate549988...692e1/control118044...fc631. Fresh dispatch-{candidate,control}-r360 copies from R352 verified slot406a...0e09/save5040...64a6, separate neutral pipes. Same runner/Metal1x/CPUThreadTrue/Core6/Cubeb120ms. Explicit module identity verified in each runtime log.
- Candidate37107/top68645 first:3596VI59.933333Hz,CPU15.617004961ms,p9918.887875. Control33168/top19034 second:3597VI59.95Hz,CPU15.718986385ms,p9919.043083. Exact60s intervals and evidence paths in PERF.md. CUA verified same hanging railing scene before/after; no input/profiler during measurement. All handles exited0, save unchanged both, fallback0/smc_failed0/dropped0/invalid-reset0. No game/Simulator remains.
- Both orders favor candidate meanCPU, ~0.68%combined, with slightly lower tails. This is small headroom, not large FPS gain, full stability/audio proof or promotion. No third unchanged pair. Next candidate real-save gameplay, transition/save-relaunch and movie regression before canonical integration/selection. Full original PRD/macOS-performance-first mobile hold unchanged. Regression suite started generated/check-r360.log; poll returned handle. No app/marker changes, publication or deletion.

## 2026-09-07 — R359 build audit and first matched gameplay dispatcher pair

- Completion: suite38398 exited0/repository safety passes generated/check-r359.log, both VI recorders dropped0. All handles terminal/no game or booted Simulator. Reverse-order fixed gameplay pair next; no promotion.

- Previous turn progress: isolated module build launched and regressions passed. Revalidated SAME3365/live linker78672; waited without restart. Build3365 exited0, auditpasses candidate5499889a558e44197bb740b3b3c5cc96187bcd80e3a8df097b814cb2703692e1 (114404424bytes). Normal app and active marker unchanged. One modified-function PGO mismatch remains explicit.
- Prepared fresh dispatch-control-r359/dispatch-candidate-r359 with copied R352 Config/Wii/StateSaves/config.ini and independently configured neutral pipes. Exact slot4/save/runner hashes verified. Both explicit modules verified in runtime logs, same Metal1x/CPUThreadTrue/Core6/Cubeb120ms, no overlapping game or Simulator. CUA loaded State4 and visually verified the same railing scene before/after each60s window; no measurement input/profiler.
- Control30107/top86930 exited0:3595VI59.916667Hz, CPU15.738210119ms, wallp9918.612458. Candidate18058/top19671 exited0:3596VI59.933333Hz, CPU15.624909539ms, wallp9917.786167. Exact intervals/evidence in PERF.md. Save5040...64a6 unchanged both; fallback0/smc_failed0/invalid/reset0, genuine EFB peeks, no orphan. Whole-run underruns9/5 include boot/load; not audio acceptance.
- First-pair CPU change only~0.72%, no causal/reproducible claim or promotion. Next one fresh reverse-order pair to bound host/run-order variation, then broader gameplay/transition/save regression if supported, otherwise park. Host suite started generated/check-r359.log; poll returned handle. Full original PRD/G6/performance/audio/mobile hold intact. Cleanup already complete, no new deletion/publication.

## 2026-09-07 — R358 isolated complete module dispatcher build

- Checkpoint: suite31239 exited0/repository safety passes generated/check-r358.log. Build3365 remains live (verified linker78672 at2m28s,~670%CPU); poll same handle, do not restart. Candidate compilation reports anticipated one-function PGO mismatch, link/audit not yet complete. No runtime smoke until candidate audit; installed app untouched.

- R357 suite54084 completed exit0, repository safety passes. Added --two-range to existing verified single-object builder; source-pins actual module_export.c and accepted header, supplies private transformed header via first include directory, preserves original export source path for PGO names and all original compile flags. Only export object replaced; guest objects/callbacks/host cycle accounting unchanged. Existing exact control cache requires matching compiler, graph, all1329object hashes, link command and response before reuse.
- Parser regression passes. Build3365 running generated/build-two-range-r358.log, candidate under generated/two-range-r358; never selects installed app/marker. Poll same handle through terminal link and audit. Full suite started generated/check-r358.log. Next actual fixed-gameplay comparison if audit passes, not promotion from synthetic dispatch gains. No game/Simulator, cleanup already complete, full PRD/performance-first mobile hold intact.

## 2026-09-07 — R357 cleanup verification and dispatch benchmark recovery

- User-authorized other-project simulator erasure was already completed in R283. Live checks confirm Devices492MiB, all28devices shutdown,69GiB available. No new deletion: installed runtimes/device definitions retained, source/reference/game inputs and local saves preserved. Previously erased simulator apps/saves are not recoverable from those containers.
- Recovered generated/two-range-bench-r357.log after lost tool output. No benchmark/compiler/game process remains; log contains all four ABBA results and expected hit counts. Saved PGO/ThinLTO and actual module wrapper were retained, with guest stubs compiled separately without LTO. One candidate function has incompatible profile data ignored by clang; record this limitation rather than claiming identical profile coverage.
- Six synthetic mode means recorded in PERF.md. Most dispatch paths improve; host-handled remains approximately unchanged. Synthetic guest bodies do not establish frame rate, gameplay, or full-module acceptance. No active-module marker or packaged app changed.
- Host regression suite54084 started via bash scripts/check-repository.sh, generated/check-r357.log; poll same handle. Next isolated module-export-object build using verified accepted graph/control cache, followed by exact fixed railing gameplay comparison. No iOS/iPadOS promotion, publication, or goal completion.

## 2026-09-07 — R356 full dispatch wrapper callback/alias oracle

- Completion: suite90885 exit0/repository safety passes generated/check-r356.log. All handles terminal; faithful dispatch benchmark next.
- Previous turn progress: exact two-range pointer lookup oracle passed. Rechecked current authoritative status/dirty checkout/no runtime. Added tests/test-two-range-dispatch.py extracting complete actual generated dolrecomp_find_original/call_original/physical_pc_alias/call bodies from pinned accepted header and transformed copy. Uses actual CPUState and pinned cpu.c ppc_host_call, shared original lookup tables,1322distinct observable chunk stubs. No guest instruction bodies claimed tested by these stubs.
- Matrix27648cases/build covers18virtual/physical/gap/unaligned/end/wrap addresses,6RAMsizes,host pointer absent/present,32host behaviors,4replacement behaviors. Callbacks mutate PC/RAMsize/exception/registers and remove/replace host pointer; replacement handler can intercept original or alias stage. Compares complete finalCPU, return result, and ordered fullCPU/address/kind snapshots at all callbacks/chunk entry, using the same original CPU pointer. Host28266/virtual-or-alias12906/chunk5560events across reference/candidate runs; all nonzero.
- Focused52074 exit0 generated/two-range-dispatch-r356.log:ASanUBSan/O2 both pass. Tests prove wrapper equivalence under these synthetic callback/chunk contracts, not actual game behavior or speed. Added actualcpu.h pin and test to full suite running generated/check-r356.log; poll returned handle.
- Next faithful savedPGO/ThinLTO complete-dispatch benchmark with opaque runtime-selected addresses and actual module wrapper identity, avoiding compile-time folding or lookup-only speed claims. No app/module/runtime changes; offline-only no gameplay smoke, latest accepted profileR354. Full original PRD/G6/performance/audio/mobile hold unchanged.

## 2026-09-07 — R355 exact two-range lookup prototype

- Completion: suite89877 terminal0/repository safety passes generated/check-r355.log. All handles terminal; full-dispatch oracle/timing next.
- Previous turn progress: gameplay profile identified host/module dispatch, not THP. Current checkout/state preserved; verified accepted generated header2cf2a7...83b9. Added scripts/two_range_lookup.py: source-pin mandatory, replaces ONLY dolrecomp_find_original with alignment reject and unsigned offset/range tests, hot second run first. Uses same immutable1322function-pointer table. No CPU state/callback/alias/charge/dispatch-policy change.
- tests/test-two-range-lookup.py extracts actual accepted lookup tables and reference body; emits distinct identity stubs for all1322chunks. Compares every byte [7ffff000,8052e000), including inter-run hole/unaligned/end addresses, explicit wrap/physical-alias/MMIO boundaries and1M full-u32 random addresses. Calls every chunk entry to check identity. Reversed boundary assertions prove header prefix and complete suffix beginning dolrecomp_call_original byte-identical; altered source pin rejected.
-55645 exit0 generated/two-range-r355.log: bothASanUBSan/O2 pass6435854comparisons,1352457hits,1322chunk identities. This proves lookup pointer equivalence on tested addresses, not callback execution, whole-dispatch timing or game acceptance. No speedup inferred from fewer source loads.
- Added test to suite running generated/check-r355.log; poll returned handle. Next full-wrapper callback/physical-alias ordering comparison and faithful savedPGO/ThinLTO dispatcher benchmark; preserve invalid-address semantics and all host cycle charges. No module/app/runtime change, no gameplay smoke this offline-only step; latest accepted gameplay profileR354. Full original PRD/G6/performance/audio/mobile hold unchanged.

## 2026-09-07 — R354 accepted fixed-gameplay CPU profile

- Completion: suite65661 exit0/repository safety passes generated/check-r354.log. All handles terminal/no runtime/Simulator. Dispatch lookup oracle next.
- Previous turn progress: parked signed16 after gameplay warning persisted. Existing detailed R310 capture is movie-specific, not this scene. Fresh gameplay-profile-r354 copied verified slot4/Config/Wii; accepted118044...fc631 explicit module and406a...0e09fixture verified. Runtime72375/PID75354 loaded fixed railing scene. CPU Profiler78034 bounded10s exit0,24MiB trace; no SystemTrace. Postcapture same scene visible, cleanclose72375 exit0,save5040...64a6 unchanged,native1595522256/fallback0/smc_failed0.
- Export83466 exit0, summarize-cpu-profile.py:29459CPU-thread samples,29351P-core/108E-core. Run3391 +chassis_dispatch2184 =18.9246%leaf samples. Next generated routines805170A0671,804B60A0628,804330A0606,804B50A0545. THP kernels/804530A0 absent among leaves; convert_to_double2. This is sampled attribution, not exact costs or unprofiled FPS. Evidence generated/runtime/gameplay-profile-r354/{cpu.trace,cpu.xml,toc.xml,summary.json,capture.log,runtime.log,vi.csv}.
- Exact accepted unsigned module disassembly dispatch.asm joins all2184chassis samples via dispatch-sites.json:1724loads,343branches,78stores,39other. Top sites function-pointer load0xec0400,host_call-field0xd54388,stackrestore0xe00317,run-start/end/page loads206/210/190. Initial symbol name without Mach-O underscore matched nothing; corrected _chassis_dispatch. No source/module edits.
- Actual generated header uses two ranges [80004000,800064E0),[800070A0,8052D280),both stride4096,bases0/3; current lookup page→run→bounds→function table. Next isolated source-pinned two-range lookup hypothesis with exhaustive address/alignment/hole/boundary tests, preserving entire dolrecomp_call callback/replacement/physical-alias contract. Do not change guest cycle flush/host timing or assume sampled loads equal removable cost. Host Run remains separately substantial.
- Suite running generated/check-r354.log; poll65661. No game/Simulator. Full original PRD/G6 performance/audio/mobile hold remain; parked candidate not revived.

## 2026-09-07 — R353 fixed-state gameplay comparison; signed16 candidate parked

- Completion: suite79144 exit0/repository safety passes generated/check-r353.log. All handles terminal/no game/Simulator. Candidate remains parked; accepted gameplay profiling next.
- Previous turn progress: verified fixed railing state. Fresh fixed-{control,candidate}-r353 profiles copy identical slot4SHA406a1c...30e09,Config/Wii/config.ini; independent pipes. No input issued during either window. Explicit module overrides and identical hanging position/camera verified before/after. Control63122/top12900 then candidate96200/top83772 all exit0.
- Control383685711350625..383745711350625:3501VI58.35Hz,CPU16.21918581ms,p9518.366958,p9919.753916. Candidate383884673806458..383944673806458:3304VI55.066667Hz,CPU17.34333067ms,p9518.881875,p9920.945666. Candidate invalid/reset0; both dropped0/save5040...64a6 unchanged/fallback0/smc_failed0. Evidence generated/runtime/fixed-{control,candidate}-r353 summary/VI/runtime/host-top/window-start.
- Same fixed scene removes movement-route drift but does not eliminate host/order effects. Nonetheless gameplay benefit remains unproven and warnings persist alongside movie gains. PARK signed16 d379ff...16a6; no canonical integration, installed-app change or more unchanged comparison loops. Exact helper/oracle/source fixtures retained. No claim that code change alone caused all difference.
- Next profile accepted-module gameplay at fixed railing state to identify its substantial CPU cost, reusing existing gameplay evidence first. Do not continue movie-helper micro-optimization as proxy for whole-game stability. Full original PRD/G6/audio/60Hz/mobile hold remain. Suite running generated/check-r353.log; poll returned handle. No game/Simulator.

## 2026-09-07 — R352 fixed Observatory fixture created and reloaded

- Completion: suite87711 exit0/repository safety passes generated/check-r352.log. All handles terminal; no runtime/Simulator. Fixed-state measurement pair next.
- Previous turn progress: fresh moving-route control faster raised candidate regression warning. No verified recent fixed Observatory fixture found in save inventory; created ignored observatory-fixed-r352 from same real-save source, no inherited savestates. Accepted118044...fc631 module explicitly loaded; real title/file select/one-star Observatory verified. Load65765 and eight input cycles58063 exit0.
- After input settled Mario hangs at same railing seen in R351, camera includes Luma/Observatory/reactor; not the easier initial spawn scene. Saved fresh slot4. Initial save produced no file because fresh profile lacked StateSaves; created that exact directory and retried successfully. This preparation issue is distinct from guest NAND save integrity; native state-save directory initialization remains to harden before shell acceptance.
- Fixture generated/runtime/observatory-fixed-r352/StateSaves/RMGE01.s04,38949741bytes,SHA406a1cd20209e00852ad3d9a3450e6869c33cdd9a9b8e9dda2abb3d8a1f30e09. Reloaded slot4 through UI, visible load confirmation/same hanging position/camera. No movement issued after checkpoint. Closed33432 exit0,real-save5040...64a6 unchanged,native4966448769/fallback0/smc_failed0. Fixture is measurement setup, not new gameplay progression or 60Hz acceptance.
- Next copy exact slot4 and Config/Wii/config.ini to fresh control/candidate profiles with independent neutral pipes; load same slot4, no movement, fixed60s VI/CPU/host-top comparison. No timing claim this preparation turn. Suite running generated/check-r352.log; poll returned handle. No game/Simulator; candidate remains unselected, full PRD/G6/mobile hold.

## 2026-09-07 — R351 fresh real-save control raises regression warning

- Completion: suite93992 terminal0/repository safety passes generated/check-r351.log. All handles terminal. Fixed-state comparison next; installed baseline unchanged.
- Previous turn progress: candidate real-save gameplay56.1Hz, clean persistence/no fallback. Fresh s16-gameplay-control-r351 copied same original gameplay-r289 Config/Wii/config.ini, independent pipe, no savestates; accepted118044...fc631/save5040...64a6 verified. Runtime68105 and load26891 reached real title/file-select/one-star Observatory; eight same input cycles84044/top88676, all exit0.
- Fixed382830873951833..382890873951833:3515VI58.583333Hz,CPU15.48811081ms,p9518.579375,p9920.934166,invalid/reset/dropped0. Visible movement ended hanging from railing; candidate ended standing near same railing. Thus wall-clock input cycles do not guarantee identical guest route/frame workload. Control save5040...64a6 unchanged,native2908375225/fallback0/smc_failed0; whole-run65audio underruns/8backlogdrops not acceptance.
- Control exceeds candidate56.1Hz/CPU16.60129596ms. This is a gameplay regression warning, not a clean causal estimate given route/host variability. Do NOT promote from movie gain. Next fixed gameplay savestate with no movement and equal measurement window to remove input-route drift; one new matched comparison, not repeated unchanged moving-route loops. Keep candidate unselected pending this classification. Main app untouched; no game/Simulator.
- Evidence generated/runtime/s16-gameplay-control-r351/{summary.json,vi.csv,runtime.log,host-top.txt,window-start.txt,load-input.log,control-input.log}. Suite running generated/check-r351.log; poll returned handle. Full PRD/G6 performance/audio/mobile hold unchanged; save-write/transition/relaunch candidate acceptance still open.

## 2026-09-07 — R350 signed16 real-save gameplay regression check

- Completion: suite42059 exit0/repository safety passes generated/check-r350.log. VI dropped0. All handles terminal; fresh real-save control next.
- Previous turn progress: both movie orders favor candidate. Rechecked status/no game/Simulator; copied gameplay-r289 Config/Wii/config.ini only into s16-gameplay-r350 (no savestate), configured pipe, save5040...64a6 verified, CPUThreadTrue/Metal1x. Explicit candidate d379ff...16a6 module confirmed in runtime87270 log. Waited for real title before load47798; visible one-star Observatory reached through file select.
- Eight movement/jump/Spin fixture cycles84856 and host-top58993 complete0. Window382447904163875..382507904163875:3366VI56.1Hz,CPU16.60129596ms,p9519.785667,p9922.181292; wallp9923.069166,invalid/reset0. Postwindow Mario visibly moved to railing near Luma speech bubble. Inputs commanded, not individual continuous visual proof of every jump/Spin. No new save-write or transition/relaunch acceptance.
- Clean close87270 exit0; save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged,native2878932049/fallback0/smc_failed0. Whole-run101underruns/9backlogdrops includes startup/load; audio remains unmet. Host end runner144.3%,kernel52.7%,logi40.4%,WindowServer32.1%. Evidence generated/runtime/s16-gameplay-r350/{summary.json,vi.csv,runtime.log,host-top.txt,window-start.txt,control-input.log,load-input.log}.
- Gameplay performance is not acceptable. R28959.9Hz is historical, not matched evidence of a candidate regression. Next fresh accepted-control real-save run with same fixture/8cycles/60s to classify this gap before promotion, then persistence/transition coverage. Installed app/marker unchanged; no game/Simulator. Suite running generated/check-r350.log. Full original PRD/G6/mobile hold remain.

## 2026-09-07 — R349 reverse signed16 pair supports retaining candidate

- Completion: suite92313 terminal0/repository safety passes generated/check-r349.log. No active handles/game/Simulator; real-save candidate regression next.
- Previous turn progress: first pair favored signed16 candidate. Verified no game/linker/Simulator, both module identities and slot3 hashes; created fresh s16-{candidate,control}-r349 from original disposable fixture, independent pipes. Candidate first73080/top50819, then control98518/top30995; all exit0. Loaded identical castle scene and observed movie progression, explicit module overrides confirmed.
- Candidate window381879787438458..381939787438458:3182VI53.033333Hz,CPU18.65572827ms,p9520.319792,p9921.140792. Control382061392930708..382121392930708:3024VI50.4Hz,CPU19.65344951ms,p9521.447917,p9923.011583. Both savea574...afa6 unchanged, fallback0/smc_failed0/dropped0. Candidate native786203918/control703647059. Evidence generated/runtime/s16-{candidate,control}-r349 summary/VI/runtime/host-top/window-start. Whole-run audio107/150underruns includes load/startup and is not acceptance.
- Both orders favor candidate: combined6300VI/120s52.5Hz vs5997/120s49.975Hz (~5.05%higher). Retain unselected candidate d379ff...16a6 for broader real-save gameplay regression, not another movie timing pair. Does not establish hardware-independent speedup, sustained60Hz, soak/audio or G6 acceptance. Host concurrent load remains recorded, unchanged.
- Next candidate real-save Observatory movement/jump/Spin and transition/save/relaunch validation with exact identity; preserve accepted app/marker until regression and canonical build integration pass. Suite running generated/check-r349.log; poll returned handle. All game/recorder handles terminal, no Simulator. Full original PRD and macOS-first/mobile hold remain.

## 2026-09-07 — R348 signed16 candidate first runtime pair

- Completion: suite79459 exit0/repository safety passes generated/check-r348.log. Candidate VI dropped0. All handles terminal; fresh reverse-order pair next.
- Previous turn progress: candidate module audited and fresh control measured49.55Hz. Verified no runtime/linker/Simulator, candidate d379ff...16a6/savea574...afa6. Explicit candidate33722 launched; waited for module-loaded log before CUA (no extra frontend). Loaded slot3, visible castle/yellowflowers; same trigger/top67538.
- Fixed381552109822625..381612109822625:3118VI=51.966667Hz,CPU19.05844075ms,p9520.670333,p9922.347708,invalid/reset0. Visible Peach/Bowser movie advanced. Both handles terminal0; savea574...afa6 unchanged,native856125621/fallback0/smc_failed0. Whole-run119audio underruns/2backlogdrops includes startup/load; not audio acceptance. Evidence generated/runtime/s16-candidate-r346/{summary.json,vi.csv,runtime.log,host-top.txt,window-start.txt}.
- First pair favors candidate vs control49.55Hz/19.9890582CPUms (~4.88%higher VI rate); NOT promotion evidence yet. Both remain below60Hz. End host sample kernel_task54.4%,logioptions33.4%,WindowServer33.3%; no host-process changes. Need reverse-order candidate/control on fresh identical profiles before retain/park, given prior order/host variability. No unchanged third pair.
- Suite running generated/check-r348.log; poll returned handle. Accepted app/module selection unchanged, no game/Simulator. Full original PRD/G6 performance/audio/mobile hold remain.

## 2026-09-07 — R347 signed16 module audited; control runtime

- Control completed:22962 and4964 exit0, visible castle→Peach/Bowser movie, native890053254/fallback0/smc_failed0, savea574...afa6 unchanged. Window381311756569208..381371756569208:2973VI=49.55Hz, CPUmean19.9890582ms,p9521.757416,p9923.381083; invalid/reset0. Evidence summary.json/vi.csv/runtime.log/host-top.txt under s16-control-r346. This is control only, not evidence of candidate gain. Whole-run audio162underruns/4backlogdrops includes startup/load and does not meet audio acceptance. No game remains. Next explicit candidate run with prepared s16-candidate-r346 profile and same trigger/fixed60s, not reuse an older faster control.
- Same71954 verified live through link, then terminal0/module audit passes. CandidateSHA d379ff0b40dffdce5e4df0b3345641838cacfbbeaf2307da655960e431e916a6,114404600bytes (accepted114404568). Fixture text reduction is not a whole-module size reduction. Accepted selection unchanged; no linker remains.
- Diagnostic runner856b2e...d2b0e and bundledbaseline5c210...e91d verified. Control22962 launched explicit generated/s16-load-r346/control module, log confirms override. Early CUA attachment before UI ready timed out and opened frontend69428; terminated that exact frontend before measurement. Reattached existing runner69396, loaded slot3, visually confirmed castle/lake/yellowflowers.
- Trigger/top4964 running; generated/runtime/s16-control-r346/window-start.txt defines fixed60s window. Continue same runtime through complete window, capture visible movie and close normally, summarize timing/save/fallback checks. Candidate not yet run. No Simulator. Full PRD/G6/mobile hold unchanged.

## 2026-09-07 — R346 isolated signed16 module build

- Previous turn progress: faithful offline ABBA2.1553%lower CPU and16KiBsmaller fixture. Current checkout preserved; no game/booted Simulator,71GiB free. Extended existing build-dc-column-experiment.py with mutually exclusive --s16-load; same verified chunk/header normalization and flags, no broad runtime/helper patch.
- Build71954 active, generated/build-s16-r346.log. Control byte-identical118044...fc631 reused from R316 after compiler/graph/all1329objects/link-command/response checks. Candidate sourceSHAadf04041e97467f45da18613fe5b38d0e0ecc2d069adfc7f67d76509a9409797. Provenance confirms original/candidate flags identical; replaces one chunk object only. At latest check Python68504→clang68618→ld68620 active, linker~669%CPU/1m56s. Poll SAME71954, no restart. Candidate audit/hash not yet available.
- Suite43483 terminal0/repository safety passes generated/check-r346.log. Prepared generated/runtime/s16-{control,candidate}-r346 with copied Config/Wii/StateSaves/config.ini and configured independent pipes. Both slot3SHAaea2b7...b7ab8 and GameDataSHAa57431...afa6 match; CPUThreadTrue,Metal,InternalResolution1. No profile launched or main save touched.
- Next wait same71954 through link/audit, then inspect exact candidate identity and conduct sequential matched60s movie runtime measurements with host-load evidence and clean close/save checks. Accepted app/module marker unchanged. Full PRD/G6 performance/audio/mobile hold remain; this is a verified build wait, not a performance acceptance.

## 2026-09-07 — R345 complete-kernel signed16 load validation

- Final: benchmark29219 exit0. CPU-time ABBA aggregate control25064.4065/candidate24524.1995ns (sum12pattern means), candidate2.1553%lower. __TEXT229376→212992 (-16KiB). This is offline evidence only. Next extend existing verified single-object experiment builder with an isolated s16-load mode, reuse byte-verified control, preserve compiler/link flags and1328other objects, then audit and matched visible runtime comparison. No installed app promotion. All handles terminal.
- Previous turn progress: exhaustive helper oracle passed. Rechecked dirty checkout, no game/booted Simulator, pinned extracted source98d986... and savedPGOf39a3c...; accepted module selection unchanged.
- Added source-pinned transform for exactly16GQR5 coefficient-load calls in both kernels. Regression reverses only those names and proves remaining decoder byte-identical; mismatched input rejected. Correctness-only fast-hit counter omitted from benchmark. Added --s16-load mutually exclusive oracle variant, same extracted control, savedPGO/ThinLTO timing retained.
- Complete oracle74507 exit0 generated/s16-kernels-r345.log: all four builds1440cases/digest882be09b1f38e9ad, candidate4840actual hits/build. One mismatched profile function warning per build retained; no claim of universal profile coverage. Full suite7236 exit0 generated/check-r345.log/repository safety passes.
- Benchmark29219 running generated/s16-bench-r345.log; poll same handle. No module/app/runtime changes or speed claim. Offline-only; full original PRD/G6 performance/audio/mobile hold unchanged.

## 2026-09-07 — R344 signed16 scale-zero load specialization

- Completion: suite79933 terminal0/repository safety passes. All handles terminal; no runtime/Simulator. Next complete-kernel integration and timing, not deployment from this focused proof.
- Previous turn progress: full-kernel local-copy benchmark refuted that candidate. Current dirty state preserved, no game/booted Simulator. Source inspection cpu.c psq_load_value/psq_dequant and cpu.h inline path identifies type7/scale0 still using generic dispatch; signed16 values are exactly representable in float/double. This is not the parked map-cache or local-copy change.
- Added isolated scripts/s16_psq_load.py helper: mask only meaningful GQR load type/scale bits, preserve indexed/LSQE eligibility, two sequential mem_read16 calls and original CPU pointer/register assignment order; fallback to original helper otherwise. No direct-memory assumption, merged read, CPU copy, FP approximation or product edit.
- tests/test-s16-psq-load.py pins cpu.c and compares278528cases/build: all65536signed16 lane values with complemented second lane, four host rounding modes, every type/scale and indexed/W/LSQE combination through external callbacks. Callback mutates GQR/HID2/FPSCR/FPRs; compares wholeCPU, result, callback state digest/count and host flags. Both ASanUBSan/O2 pass in generated/s16-load-r344-retry.log (42746 exit0). Initial link failure lacked cpu_exception.c; added actual implementation, retained failed log.
- Added test to full suite79933 running generated/check-r344.log; poll same handle. Next source-pinned full-kernel integration plus actual-hit proof, then faithful savedPGO/ThinLTO CPU-time comparison. Helper-only tests do not justify module build or performance claims. Accepted app/module unchanged; offline-only, no runtime smoke. Full PRD/G6 performance/audio/mobile hold remain.

## 2026-09-07 — R343 complete-kernel local-state validation

- Final: benchmark29485 exit0. ABBA aggregate control25048.9935/candidate25818.91 threadCPU ns (sum of12pattern means), candidate3.074%slower. Fixture __TEXT229376→245760 (+16KiB). Park local-state copy candidate; no module build or unchanged repeat. Next inspect the exact first-pass helper/memory operations for work that can be eliminated under the already tested mapped/disjoint guard, rather than another state-copy/layout variant. All handles terminal; accepted runtime unchanged.
- Rechecked authorized cleanup: R283 already erased other-project Simulator apps/saves; current Devices492MiB, disk71GiB available, none booted. No additional deletion needed. Source/game inputs preserved.
- Initial complete-kernel and suite logs failed the transform assertion: the outer dispatcher repeats label80452750. Corrected targeting to the unique charged first-pass entry; regression verifies outer dispatcher and kernel1 remain byte-identical. Original failed logs retained.
- Retry27861 exit0, generated/local-columns-kernels-r343-retry.log: all four sanitized/optimized reference/candidate builds1440 cases, digest882be09b1f38e9ad. Candidate actually used24times/build. Pinned saved PGO retained; compiler reports one mismatched profile function on both variants, so this is profile-use with that warning, not complete profile coverage. No game or performance acceptance.
- Suite88787 exit0, generated/check-r343-retry.log, repository safety passes. CPU-time benchmark29485 now running generated/local-columns-bench-r343.log with saved PGO and ThinLTO; poll same handle. No app/module/selection changes or runtime smoke in this offline-only step. Full PRD/G6/mobile hold unchanged.

## 2026-09-07 — R342 guarded first-pass local-state prototype

- Completion: suite48775 terminal0/repository safety passes, including18rejection checks. All handles terminal; no game/Simulator. Full-kernel integration and faithful timing remain next.

- Previous turn progress: exact first-pass oracle found read-ahead and overwritten-store observations. Rechecked status/dirty tree/no runtime. Added scripts/local_thp_columns.py, pinned98d986... input; emits exact first-pass instruction body and a private local CPUState wrapper. Before copying, requires PC80452750/CTR8, valid FP/LSQE/GQR0type0/GQR5signed16scale0, no exception/journal, conservative downcount and RAM sizes; preflights mapped144coeff/264quant/256workspace bytes, rejecting pairwise overlap and CPU/global-journal-field aliasing. No memory snapshot, output-store omission, math change or collapsed charge. Copies state back after exact yield/completion. No copied CPU pointer may reach a callback on eligible memory routes.
- Extended tests/test-thp-first-pass.py: all prior32resume comparisons retained;128reference/candidate comparisons across4paths,2layouts,2budgets,2journal modes,4rounding modes. Candidate32actual hits/build; boundary/journal paths fall back with original CPU callback identity. Initial20939 terminal0 in generated/local-columns-r342.log: ASanUBSan/O2 both pass CPU/memory/normalized event digest/hostflags. Not whole-kernel, arbitrary-operand or game acceptance.
- Added18explicit rejection checks: wrongPC/CTR, exception/FP/LSQE/GQR changes, journal, extreme budget, null/undersized memory, missing read-ahead spans, overlapping workspace/coefficients, aliased MEM1/MEM2 and CPU-backed RAM. They require unchanged CPU/memory/completion output. Added source-pin rejection. Full suite48775 started generated/check-r342.log; poll same handle.
- No app/ref/module/selection/runtime change. Next integrate this isolated wrapper into offline complete-kernel oracle at first-pass entry with explicit hit proof and original rejection/yield path, retain pinned module PGO/ThinLTO for timing. Do not infer gain from local copying or add unrelated helper changes. Full original PRD/G6 performance/audio/mobile hold remains.

## 2026-09-07 — R341 emitted first-pass read-ahead/yield oracle

- Final: corrected full suite75082 terminal0/repository safety passes, generated/check-r341-fixed.log. Both first-pass sanitized/O2 comparisons pass with exact per-path journal counts. All handles terminal; no runtime/Simulator. Next guarded candidate plus eligibility/hit/rejection tests, not a speed claim from this reference oracle.

- Correction: suite91688 terminal1 exposed incorrect new fixed64journal assertion (initial40765 only requirednonzero and passed). Preserved generated/check-r341.log. Count probe24860 terminal0 in generated/thp-first-pass-counts-r341.log: DC64,quarter112,half80,regular64. Earlier stores are overwritten on quarter/half paths, but remain journal-visible. Corrected test to exact per-path expected counts; prior blanket64 statements below are superseded. No runtime bug or candidate change. Rerun full suite next; do not erase intermediate writes unless non-observability is separately proven.

- Previous turn progress: actual source audit established144/264span contract and separated first-pass from observable output. Rechecked status/no runtime. Added tests/test-thp-first-pass.py extracting exact accepted80452750..80452970 body and original internal labels, asserting4yield checks/no unexpected outside targets. Chunk98d986... and cpu/float/header identities pinned; no input/source/module mutation.
- Harness runs four column patterns (DC, quarter, half, regular), direct versus logical-end-at-RAM-boundary inputs, four rounding modes. Uninterrupted budget100000 compared against budget-256 that yields at all7taken column backedges. Normalizes initial credit only when comparing finalCPU and callback/journal snapshot digest; original charges/resumePCs preserved. Checks fullCPU/memory, hostFP flags,workspace journal preimages/writes, reservation invalidation, and callback original-CPU identity. Intermediate yield PC limited to50/70 loop entries; phase completion explicitly joins80452970 without executing second pass.
- Boundary variant places logical coefficient128bytes at end ofMEM1 and quant256 at end ofMEM2; deterministic external-read callbacks supply read-ahead bytes. Non-DC paths prove accesses through16extra coefficient bytes and8extra quant bytes, including final-column register effects. DC/direct variants require zero external reads. This is exact emitted reference consistency, not an independent mathematical IDCT proof or candidate acceptance. Aliasing and journal-disabled candidate eligibility/hit tests remain to add alongside implementation.
- Initial40765 terminal0, generated/thp-first-pass-r341.log:32comparisons in each ASanUBSan/O2 build pass. Added stronger exact64journal assertion and helper pins to suite91688, generated/check-r341.log, live at entry. No timing claim or game smoke this offline-test-only turn. Next guarded local-state first-pass prototype with no-journal/direct-mapped/disjoint preflight, unchanged fallback and copyback at every original yield; actual-hit/rejection tests before timing. Full PRD/G6 performance/audio/mobile hold remains.

## 2026-09-07 — R340 first-pass transform contract audit

- Completion: suite44061 terminal0/repository safety passes. No live handles/game/Simulator; first-pass oracle next.

- Previous turn progress: thermal-aligned accepted capture restored56.4Hz but left CPU17.55ms and no category transition; no new thermal-only run. Rechecked current status/dirty tree/no runtime/Simulator; accepted extracted source98d986...a68991 and Petari THPDec e93f9299...65ee8 verified. Read existing boundary audit, exact generated phase/prologue/backedges/epilogue and Petari column/row reference. Re-ran pinned exact-DOL scan to generated/thp-boundaries-r340.json, pass.
- New correctness finding changes the proposed guard: non-DC column paths prefetch the next column BEFORE final CTR test. Exact804528D4 reads r3+28; final normal iteration input+112 means bytes through143. Quant prefetch80452930 reaches bytes through263. A128/256-byte logical-block preflight is insufficient; baseline spans144/264 are needed and only with CTR8/normal GQRs. Do not skip extra reads/register effects, especially callbacks at RAM boundaries.
- Narrow candidate boundary to first column phase80452750..80452970, keeping original prologue/second-pass output/epilogue and all external entries. Four data-dependent backedges retain exact charge/yield semantics. Updated THP-BOUNDARY-AUDIT.md with guard/alias/journal/reservation/CPU-pointer requirements. No implementation or claimed speedup; next exact first-pass boundary oracle before local-state candidate. This is a larger structural target, not another normal-entry clone or DC-only shortcut.
- Full suite running generated/check-r340.log; poll returned handle. No app/ref/module changes or runtime smoke this source-only audit; R339 latest runtime remains. Full original PRD/G6 performance/audio/mobile hold unchanged.

## 2026-09-07 — R339 thermal-aligned accepted movie

- Completion: suite92934 terminal0/repository safety passes. All handles terminal; next exact transform contract audit, not another thermal-only capture.

- Previous turn progress: public thermal API recorder implemented/tested and idlefair observed. Rechecked status/dirty tree/no game/linker/Simulator. Fresh thermal-r339 profile from exact r282fixture; accepted control118044... and savea574... hashed. Same diagnostic runner/native1/Metal/Core6/Cubeb; explicit module log, UI slot3 castle/lake/flowers, one scripted attack trigger. No product or power changes.
- Runtime98806/probe17885 terminal0. Window378510067948250..378570067948250:3384VI=56.4Hz,3383intervals,CPUmean17.5539093ms,p9518.52425,p9918.925209; wallmean17.7284952ms. Invalid/reset/dropped0. Native892992589/fallback0/smc_failed0, savea574... unchanged. UI movie/Bowser advance and clean close observed. Evidence generated/runtime/thermal-r339/{runtime.log,vi.csv,summary.json,thermal.csv,thermal-summary.json}.
-61thermal query snapshots,60contained in window, allfair1/lowPower0. First starts3.904ms after window start, final starts60.008934s after; max query0.340625ms. Summary counts actual contained queries, does not infer continuous-state durations or frequencies. No state transition to correlate. Accepted performance recovered from R33746.6833Hz to56.4Hz while current reported category remainedfair; R337 thermal was not concurrently measured. This neither proves nor excludes throttling/host-frequency changes.
- Park unchanged thermal-only repeats; privileged frequency remains unavailable without user action, but safe source work remains. Sustained CPU budget17.55ms is still over16.667ms. Next inspect exact THP transform/reference for a guarded whole-kernel fast-path contract retaining original external entries, callbacks and interruption exits; do not re-run parked helper-inline/NaN/widening/DC candidates. Full original PRD/G6 performance/audio/mobile hold remains.
- No game/frontend/Simulator left. Full suite92934 running generated/check-r339.log after runtime close; poll same handle. No new game-completion or stability acceptance inferred from this60s movie.

## 2026-09-07 — R338 public thermal-state observability

- Completion: suite32074 terminal0/repository safety passes. No live test/runtime/Simulator; next new thermal-aligned accepted run.

- Previous turn progress: reverse pair rejected candidate promotion and exposed drifting unchanged control. Read status/no game or Simulator; no power/process changes. powermetrics local help identifies cpu_power frequency and thermal samplers. Direct bounded1s invocation refuses non-root. sudo -n bounded same read-only command refuses because password required; no password prompt, credential request, security change or further privileged retry. Earlier pmset missing CPU status does not rule out throttling.
- Local SDK Foundation/NSProcessInfo.h documents thermal0nominal/1fair/2serious/3critical and nominal fallback for unknown/unsupported systems; lowPower false can also mean unsupported. Added scripts/thermal-state-probe.mm: public API only,1Hz for1..120s, steady-clock query start/end, raw thermal/low-power fields and explicit limitations; no QoS/activity assertion, process suspension or power mutation. Unknown enum fails explicitly. CPU frequency remains unavailable.
- tests/test-thermal-state-probe.py passes20316: ASanUBSan, argument bounds/schema/timestamp checks and two actual API observations. Added to suite32074 (generated/check-r338.log), live at entry. Built generated/thermal-state-probe-r338;2s idle sample generated/thermal-state-r338.csv returns1(fair),lowPower0 in all3rows, first query~0.458ms. Fair is not the unsupported nominal fallback, but idle snapshots cannot establish prior-run state or causal throttling.
- Next new hypothesis: correlate accepted-module VI/CPU slowdown with concurrent public thermal-state transitions in one bounded run, using new1Hz probe and existing exact fixtures. No further NaN-outline tests; candidate remains parked. No runtime smoke this turn (read-only system probe/test only), normal app unchanged. Full original PRD/G6 performance/audio/mobile hold remains.

## 2026-09-07 — R337 reverse pair rejects promotion; baseline drift needs explanation

- Completion: suite24978 terminal0/repository safety passes. All runtime/test handles terminal; next frequency/thermal observability check, not another NaN-outline run.

- Previous turn progress: audited module and first pair yielded no gain; reverse order changes the comparison hypothesis. Verified candidate56628.../control118044... module hashes, no game/linker/Simulator, fresh matching disposable r337profiles and savea574.... Same runner/native1/Metal/Core6/Cubeb, exact slot3 UI load and attack sequence; candidate first then control, no concurrent games/builds.
- Candidate46538/window44004 terminal0. Window377814066810375..377874066810375:3020VI=50.333333Hz, meanCPU19.6809234ms,p9521.507542,p9923.31875. Control69054/window76581 terminal0. Window378026013364916..378086013364916:2801VI=46.683333Hz, meanCPU21.2240178ms,p9523.291459,p9924.818625. Both invalid/reset/dropped0; native754972500/902261744,fallback0/smc_failed0; savea574... unchanged. UI movie advance and clean closes observed. Reports/host-top/logs in generated/runtime/add-nan-{candidate,control}-r337.
- Opposite direction to R336 (control54.1333 versus candidate48.7667). Both second runs slower. PARK NaN-outline candidate, no third unchanged pair or promotion. Aggregate or single-pair numbers cannot distinguish frequency/host drift from candidate effects. This does not prove candidate intrinsically faster or slower.
- Post-run read-only pmset -g therm: no recorded thermal/performance warnings and no CPU power status; pmset -g batt: AC power/100%. Missing status is NOT thermal clearance. Final top candidate/control kernel_task50.9/47.9%,logioptions37.4/35.5%,WindowServer32.9/30.5%; no third-party processes changed. Next inspect available read-only CPU frequency/thermal telemetry for a bounded accepted-baseline run, rather than more compiler micro-variants while baseline drifts. No game/frontend/Simulator remains.
- Full regression24978 running generated/check-r337.log after game close; poll same handle. Full original PRD/G6 performance/audio/mobile hold remains; no physical/mobile/performance acceptance claim.

## 2026-09-07 — R336 candidate audited; matched movie comparison

- Pair completion: control84433/76559 and candidate42218/98926 all terminal0. Control window377360291650166..377420291650166:3248VI=54.133333Hz, CPUmean18.2741946ms,p9520.926041,p9921.85625. Candidate window377555940128625..377615940128625:2926VI=48.766667Hz, CPUmean20.2858167ms,p9523.077625,p9925.374542. Both invalid/reset/dropped0, savea574... unchanged. Native737884723/794422231, fallback0/smc_failed0. UI showed movie advance and clean closes; no new full-game/audio acceptance.
- Host-top final snapshots: control kernel_task53.1/logioptions33.6/WindowServer32.8%; candidate49.0/36.6/30.6%, plus CodexRenderer9.8%. CPU totals/scene progress/frequency may vary; control already below recent56Hz. No thermal/QoS causal claim from these counters. Candidate is ~9.9%lower VI rate in this pair, contrary to offline gain. Keep unselected. Next one fresh reverse-order pair (candidate then control) to distinguish code regression from run-order/host drift; no third unchanged pair or promotion without reproducible gain. No game/frontend/linker/Simulator remains; last full suite30810 passed, no source edits in this runtime turn.

- Previous turn verified wait: resumed79138, exact linker59059 active4m15s then6m33s, no restart. Build subsequently terminal0; audit passes RMGE01/arm64/macOS14/1322chunks/19SMCranges. CandidateSHA56628cf5acefc6057106b152f1c0c8d08e2aa0fa51a694498ce508d3a9fcf3b5,114883432bytes. Final provenance and generated/build-add-nan-r334-fixed.log retained. No normal app/marker mutation.
- llvm-nm shows no standalone _ni_add in candidate, but _galaxypad_add_nan at69ec8f0 and _ppc_ps_add_op at44bc. Attempted exact-symbol disassembly warns missing _ni_add; retained ni-add.asm, not a disassembly failure treated as performance proof. Final LTO changes more call-site code than the offline fixture; final module grows478864bytes versus accepted114404568. No speed inference.
- After linker terminal/no other game, launched accepted cached control with wakeup-r302 runner and explicit STATICRECOMP_MODULE, disposable add-nan-control-r335. Runtime84433 live. Confirmed module-loaded log, UI slot3 castle/lake/flowers, then deterministic g6-r85-trigger-attack sequence. Window/top76559 live, window-start.txt uses steady clock; do not close before start+60s. Next screenshot/cleanclose, verify exit/save/fallback/VI completeness; candidate sequential afterward. No Simulator/mobile or product changes.

## 2026-09-07 — R335 verified link wait and comparison preflight

- Previous turn progress: isolated candidate compiled with exact accepted flags and verified control graph; linker wait continues, not a failed build. Resumed SAME handle79138, still live. Exact ld59059 observed at3m06s/~668%CPU/RSS~1.1GiB. No restart or mutation of executing builder. Candidate float object55KiB exists; final module/audit/hash pending. generated/build-add-nan-r334-fixed.log remains authoritative.
- Prepared comparison fixtures verified now: both slot3SHAaea2b7a3fe295b77e409753a216abf8fe6f2f1ec23ee7a8832fc9ac1a27b7ab8 and both GameDataSHAa5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6. GFX configs byte-identical, internal1, CPUThreadTrue/Core6/Metal/Cubeb. Diagnostic runnerSHA856b2e698373ab4427fd62cf037bc8a8f849d4547ee4265a0afd58cc255d2b0e verified. No game or Simulator launched during link load.
- No source changes or new tests this wait; R334 suite30810 terminal0 remains latest regression evidence. Next poll79138 to terminal and inspect candidate audit/hash, then sequential fixed60s control/candidate movie runs using already prepared add-nan-{control,candidate}-r335 profiles. Full original PRD/performance-first/mobile hold unchanged; no acceptance inferred from live linker or preflight.

## 2026-09-07 — R334 isolated float-TU module link

- Checkpoint: suite30810 terminal0/repository safety passes. Build79138 remains live; exact ld59059 at1m34s/~663%CPU, no terminal output expected until link finishes. Verified wait, not a hang/restart condition. Next poll same handle, then audit/hash and run prepared sequential game pair with linker no longer active.

- Previous turn progress: complete-kernel correctness and ~2.69%offline CPU reduction with PGO/ThinLTO retained. Rechecked status/dirty tree/no booted Simulator,72GiB available. Actual build.ninja float TU uses pinned saved profile, finalO2, ThinLTO, strictFP, arm64/macOS14. Added mutually exclusive --outline-add-nan to existing isolated builder; no source overlay, normal module marker or app edits.
- Builder reused control118044...fc631 after verifying graph hash, all1329object hashes, exact compiler version, normalized link command and response bytes against R316 cached byte-identical control. Replaces only cpu_interpreter_float object; all flags unchanged. Private candidate sourceSHA9f47c2207ca7ba8dfd975246574805709490921b0b7e98ae3e51bd8ecb09e224, recorded in generated/add-nan-r334/build-provenance.json.
- First build terminal1: relocating source lost relative cpu_interpreter_private.h lookup. No linker output or original edits. Preserved directory as generated/add-nan-r334-first-include-failure and log generated/build-add-nan-r334.log. Fixed only the relocated source's single quote include to the original absolute header (unchanged flags); reran in fresh required directory. Build79138 now links, ld59059 verified active; generated/build-add-nan-r334-fixed.log. Poll SAME handle; do not edit running builder or restart a quiet linker. Final candidate hash/audit pending.
- Suite30810 running generated/check-r334.log. Prepared separate add-nan-control-r335/add-nan-candidate-r335 profiles from dcbz-control-r282 Config/Wii/StateSaves/config.ini plus forced Wii pipe setup; no game launched. After link/audit completes, verify save/state/module hashes, run sequential fixed60s slot3/attack comparisons with wakeup-r302 runner, explicit module overrides, native1x Metal; no overlapping games or build load in measurements. No offline-to-game speed inference, no mobile promotion. Full original PRD/G6 performance/audio hold remains.

## 2026-09-07 — R333 complete-kernel NaN-outline correctness and timing

- Completion: benchmark68797 terminal0. CPU ABBA control24914.432/25047.276, candidate24347.027/24271.408 aggregate ns; means24980.854/24309.2175 (~2.69% lower). Both candidate repetitions below both controls; fixture __TEXT229376bytes on both sides. No whole-game gain inferred or NaN dynamic-frequency claim. Next inspect exact module object graph for isolated float-TU replacement, require accepted byte-identical control relink and pinned flags before runtime comparison. No unchanged timing repeat or combination with other candidates. All handles terminal.

- Previous turn progress: source-pinned scalar candidate passed240000cases/build; actual linked sample attribution motivated a distinct NaN-path outline. Rechecked status/dirty tree/no game/Simulator. Extended probe-thp-oracle --outline-add-nan, mutually exclusive with other kernel variants. Both sides use accepted extracted chunk98d986...a68991, only candidate float TU transformed. --module-pgo now permits this variant; pinned saved profile/arm64/macOS14 retained on both sides and ThinLTO retained for both timed executables. Benchmark repeats200000 per12patterns with thread CPU clock, no candidate counters.
- Correctness65326 terminal0, generated/add-nan-kernel-oracle-r333.log. All4reference/candidate sanitized/optimized builds1440cases digest882be09b1f38e9ad:624yields,480FPfaults,480illegal,192interruptions,96callerreturns,18816callbacks,34688journals,1440reservations. Profile mismatch warnings retained, not assumed proof of which function differed. Full suite1918 terminal0/repository safety passes, generated/check-r333.log.
- Benchmark68797 launched using --kernels --outline-add-nan --module-pgo --benchmark --cpu-time; generated/add-nan-kernel-bench-r333.log. Poll SAME handle, don't edit executing driver. No app/module/ref mutation or runtime smoke this turn; accepted R331 app evidence unchanged. No speed claim yet; full G6/performance/audio/mobile hold remains.

## 2026-09-07 — R332 exact FP-helper sample attribution and NaN outline prototype

- Completion: suite51065 terminal exit0/repository safety passes. All handles terminal; no booted Simulator. Next complete-kernel differential and faithful-pipeline timing, no promotion from scalar correctness alone.

- Previous turn progress: fresh policy evidence, no root-cause assumption or product priority change. Rechecked current status/dirty tree/no runtime. Accepted module SHA1180447313459c4c153ca74e5215811e88a9b69a00d15feff940f062148fc631 verified. Used llvm-objdump --disassemble-symbols for convert_to_double/ppc_ps_add_op/ppc_ps_sub_op/ni_add/ni_sub, then classify-cpu-instructions.py --details against R310 summary. Evidence generated/runtime/decoder-profile-r310/{fp-helpers-r332.asm,fp-sites-r332.json}. No new profile capture.
- Samples: convert_to_double1500 (558branch/942integer); ps_add1341 (602load/162store); ps_sub1197 (430load); ni_add875 (222store/221load), ni_sub483. ni_add top sites include frame stp222 and restore ldp221, unlike frame-free ni_sub. Counts are sampled attribution, NOT cycle costs/dynamic frequencies. R111/R274 widening and R285 blanket-inline experiments remain parked.
- Hypothesis: outlining only ni_add's NaN branch may let the normal path avoid stack/cold-path overhead without blanket inlining or arithmetic changes. scripts/outline_add_nan.py pins float-source554149...934f and copies exact NaN branch into noinline,cold helper, passing precomputed result; original addition, isnan gate, infinity handling and remaining TU untouched. No ref/generated module/app edits. This is distinct from R309 FMA tie outline and R285 inline-add/sub.
- tests/test-add-nan-outline.py compares original and candidate including every12x12special pair (signed zeros, subnormal limits, infinities, signed signaling/quiet NaNs) plus deterministic random encodings, randomized FPSCR,4host rounding modes. Scalar88274 exit0:240000cases each sanitized/O2,560NaN results; exact result bits, FPRes exception, wholeCPUState and host flags agree. Evidence generated/add-nan-oracle-r332.log. Added to regression suite51065, generated/check-r332.log, still live at entry.
- No performance inference or full-kernel acceptance. Next add isolated complete-kernel oracle variant; retain original reference/candidate source identity, saved module PGO and ThinLTO in timing. No module build until correctness and credible offline gain. No gameplay smoke this turn because only offline prototype/tests changed; accepted R331 runtime unchanged. Full original PRD/G6 performance/audio/mobile hold remain.

## 2026-09-07 — R331 scheduling-policy capture without product mutation

- Completion: runtime45828 and observer25505 terminal0. Fresh complete window376071318530875..376131318530875:3378VI=56.3Hz,3377intervals, meanCPU17.5757479ms/wall17.7607123ms, CPUp9518.556458/p9919.409167; invalid/reset0. All23776valid snapshots policy1/current31/base31/max63. No observed scheduling-policy transition; not QoS or App Nap classification, and no proof priority changes would help. Observer startup leaves419ms boundary gap; internal max gap2.634ms. Do not treat boundary gap as a hitch.
- UI visibly progressed from castle/flowers into airship movie and Bowser dialogue, then close button terminated exact runtime. Native941972548/fallback0/smc_failed0; savea574... unchanged. Whole-run58audio underruns/3backlog drops include load/startup and are not window acceptance. Reports generated/runtime/scheduling-r331/{summary,aligned}.json; original CSV/log retained. No game/frontend/Simulator remains. Park further policy-only capture; return to established CPU execution budget and inspect R310 sampled FP-helper/native-call costs before selecting another semantics-preserving candidate. No performance fix claimed or mobile promotion.

- Previous turn progress: retained existing priority evidence and verified cleanup. Current worktree preserved; no booted Simulator. Local SDK sys/proc_info.h exposes pth_policy, pth_priority and pth_maxpriority in the SAME proc_pidinfo response already collected. mach/policy.h defines timeshare1/RR2/FIFO4. These are not QoS class or App Nap fields; Apple reference https://github.com/apple-oss-distributions/xnu/blob/main/osfmk/mach/thread_policy.h distinguishes scheduling policies from QoS policy APIs.
- Extended CSV output with policy/base_priority/max_priority, no extra query or runtime mutation. Alignment retains raw histograms, old captures remain readable; parser/invalid/zero/missing tests and ASanUBSan worker probe pass12593. Full suite7168 exit0, generated/check-r331.log/repository safety passes.
- Fresh disposable scheduling-r331 profile from dcbz-control-r282. Runner856b2e...d2b0e verified, accepted module explicitly loaded from modules-thp-r205; no normal app/marker change. UI slot3 loaded and visibly showed castle/lake/flowers. Scripted attack trigger then fixed60s read-only thread sampling at2ms. Runtime45828 and observer25505 live at this entry, PID56915. Poll same handles. Savea574.../stateaea2... verified before window completion. Next clean UI close, verify terminal exit and saved-data hashes, align complete VI/policy interval. No scheduling change authorized by this evidence yet; full performance/mobile hold remains.

## 2026-09-07 — R330 cleanup verification and existing scheduling evidence

- Completion: suite37870 terminal exit0/repository safety passes. R307 longest87.510/83.377/79.304ms intervals have12/9/11 contained valid snapshots, all priority31; large unobserved gaps remain. No priority-drop evidence in those samples, no causal exclusion. No runner/frontend/Simulator/linker observed. Next resolve effective scheduling policy separately from numeric priority; do not change priority on this result alone.

- User-authorized other-project simulator erasure was completed R283. Verified current CoreSimulator492MiB, DerivedData2.1GiB, generated45GiB and filesystem72GiB available; no booted Simulator. No further erase or deletion: preserved installed runtimes, project data, game inputs and local saves. Simulator-installed apps/saves previously erased permanently.
- Hypothesis: existing R305/R307 captures may already answer the current-priority question without another runtime capture. Observer already emits pth_curpri; alignment discarded it. Added per-window/per-hitch priority counts with explicit missing fields, invalid-sample exclusion and no QoS/App Nap inference. Focused containment/parser regression passes.
- Reprocessed original120s windows into generated/runtime/{thread-state-r305,host-pressure-r307}/priority-r330.json. R305 valid47331: priority31=46995,0=335,46=1. R307 valid47265:31=47021,0=244. These are snapshots, not scheduling durations; neither priority values nor absence of explicit source policy prove a scheduler defect. Preserve original captures. Next inspect longest-interval counts and determine a specific missing policy measurement before any scheduling experiment.
- Full suite37870 started, generated/check-r330.log; poll same handle. No product edit/build or game smoke (only offline evidence processing changed). G6/performance/audio and original full PRD remain open; mobile held.

## 2026-09-07 — R329 normal-entry candidate rejected with module profile retained

- Added --module-pgo to normal-entry oracle, pinning saved profile f39a3cedeb6c49f35c411356893c619dc347eb1b0bb5986687f25cf81e7d460d and retaining ThinLTO/arm64/macOS14 flags. Correctness17574 exit0: all4reference/candidate sanitized/optimized builds1440cases digest882be09b1f38e9ad; candidate hits816/720. Evidence generated/normal-kernel-pgo-oracle-r329.log.
- Benchmark96548 exit0, CPU-time ABBA200k transforms per12patterns: control sums24930.152/25125.969, candidate25095.715/25137.230. Means25028.0605/25116.4725 (~0.35% slower). Fixture __TEXT229376→360448 (+128KiB); PAGEZERO is virtual, not resident memory. Evidence generated/normal-kernel-pgo-bench-r329.log. PARK candidate; no full module build or promotion.
- Suite33676 exit0/repository safety passes, generated/check-r329.log. All benchmark handles terminal. No app/module/runtime change or performance acceptance. Next examine independent hitching evidence without assuming absent explicit QoS implies low priority.

## 2026-09-07 — R328 transform normal-entry copies pass oracle

- Previous turn progress: corrected fused-Huffman timing rejected another full link. Rechecked status/journal/no game/linker and no memory hit. New scripts/normal_kernel_entry.py pins accepted extracted chunk98d986...a68991. For each thp_kernel0/1, adds noinline+flatten normal copy of exactly original instruction body with entry switch removed, and routes ONLY pc804526E8/80452B74 there. Every other entry retains original helper; original outer dispatch/charges/trampolines and return semantics unchanged. Reverse-removal assertion reconstructs source exactly. No instruction math/memory guard/callback/backedge changes; no generated module or ref edit.
- tests/test-normal-kernel-entry.py validates exact original resume bodies, normal bodies, outer text, unchanged external case count and source-pin rejection; added to suite. Existing probe-thp-oracle --kernels --normal-kernel-entry compares accepted extracted source on BOTH sides, instrumenting candidate-only normal hit counts only for correctness builds, not benchmarks. Future benchmark uses200k repeats/CPU-time option and ThinLTO; source size grows355161→497346bytes (not native size or predicted speed).
- First5431 failed at candidate coverage wrapper compile: missing stdio include for fprintf/stderr. Waited for terminal then fixed harness include, preserving first log. Rerun69598 terminal0 in generated/normal-kernel-oracle-r328-fixed.log. Reference sanitized/optimized and candidate sanitized/optimized ALL1440cases digest882be09b1f38e9ad;624yields,480FPfaults,480illegal,192interruptions,96callerreturns,18816callbacks,34688journals,1440reservations. Candidate hits816/720 in both builds. No whole-game or arbitrary-state acceptance inferred; original interior fallback text preserved separately.
- Suite86651 terminal0/repository safety passes generated/check-r328.log. No product change/game smoke this turn; latest normal runtime R325 unchanged. Next timing must retain ThinLTO AND pinned saved module PGO before interpreting gain. Source review found current THP oracle command has ThinLTO but no saved-PGO flag, so add/prove that mode before benchmark rather than repeating R323 pipeline mismatch. No other experiments combined. Full original PRD/G6 performance/audio/mobile hold remain.

## 2026-09-07 — R327 fused Huffman candidate parked; larger transform boundary next

- Previous turn progress: corrected ThinLTO pipeline explained nontransfer of profile-free gain. Rechecked status/journal/dirty tree/no game/linker/booted Simulator; memory registry no GalaxyPad hit. No code changes this turn.
- Existing driver --thinlto --fused-tail --tail-coverage --long-codes --runtime-charge-reset28744 terminal0:32sanitized expected coefficients/fullstate/memory cases pass and1008candidate hits. Same linker handle polled until terminal; no restart. Evidence generated/huffman-thin-fused-coverage-r327.log.
- Uninstrumented corrected pipeline --thinlto --fused-tail --long-codes --runtime-charge-reset --benchmark21850 terminal0. CPU-time32pattern ABBA aggregate control1857.544953ns/candidate1832.816656ns (~1.33%edge);zeroAC204.710094/191.662719,dense3510.379813/3473.970594. Final fixture __TEXT196608 (not resident-memory claim; size's4GiB PAGEZERO is virtual). Evidence generated/huffman-thin-fused-bench-r327.log. Unlike earlier native-object benchmark there is no large independent fusion gain. PARK fused Huffman tail; no full module build/promotion/additional unchanged run.
- Read actual extract-thp-kernels.py and R310 instruction-families.json. Two accepted noinline+flatten kernels retain switches for every instruction, even though normal invocation starts at region entry; original outer already supplies external nonleader charges. R310 kernel0 alone4147/5239sampledPCsloads, includingMSR481 and memory-map metadata1150. These weights are not exact costs. Prior R187 split targeted whole Huffman chunk1103, not these closed transform helpers. Next bounded hypothesis: separate normal-entry copy without external switch predecessors for each extracted transform, exact original helper fallback for all interior PCs. Keep original instruction statements/helper callbacks, outer charges/trampolines, backedge budget/yield PCs and true guest-return versus false early-exit distinction; no hoisted memory assumptions or FP relaxation. Require existing1440-case kernel oracle plus path coverage and corrected matching ThinLTO benchmark before link. Code-size/PGO costs may still reject it.
- Latest full suite94173/R326 remains passed; no source change requiring new suite this turn. Actual sanitized/runtime-model benchmark checks above passed; no new game smoke or performance acceptance inferred. Normal app/saves/marker unchanged; no processes remain. Full original PRD/G6 audio/performance and macOS-before-mobile hold persist.

## 2026-09-07 — R326 benchmark pipeline discrepancy reproduced

- Suite94173 polled terminal exit0/repository safety passes. No runtime smoke because only offline driver changed and candidate remains unselected; latest actual module comparison is R325, not superseded by synthetic acceptance.

- Previous turn progress: actual paired movie disproved transfer of large pre-link gain. Rechecked current status/journal/no game/linker/booted Simulator, no GalaxyPad memory registry hit. No product runtime/build/selection edits this turn.
- Existing driver now supports --thinlto: emit optimized LLVM IR with original flags (including ThinLTO and frontend PGO), rename decoder symbol after frontend processing, assemble IR with clang -cc1 -emit-llvm-bc -flto=thin -disable-llvm-passes, assert bitcode magic. Native harness separately compiled, final link exports reference/candidate entry symbols to prevent fixture specialization across the boundary. llvm-as unavailable in installed Xcode; verified cc1 flags via local help. This preserves link optimization but remains a reduced graph, not exact full-module equivalence. Existing default native-assembly path retained for historical reproducibility.
- Sanitized identity58397 and profile-free40420 both terminal0/32long-code expected-coefficient/fullstate/memory cases pass; logs generated/huffman-thin-{identity,no-profile}-r326.log. No optimizer pass rerun during IR assembly. No runtime during testing.
- Uninstrumented CPU-thread ABBA73290 (--thinlto --drop-chunk-profile --long-codes --runtime-charge-reset --benchmark) exit0/32cases: aggregate control1851.371078ns/candidate1831.442188ns (~1.08%lower);zeroAC201.596094/188.980531,dense3501.146063/3473.903844. Evidence generated/huffman-thin-bench-r326.log. Compared R323 native-object control4249.01ns, restoring ThinLTO materially improves control itself; prior~57% profile-free edge is not representative of final pipeline. Consistent with R325 effectively unchanged game, not proof of exact microarchitectural cause.
- Decision: park flags-only candidate577d42c9...9c41 unselected; no more unchanged full builds/movie repetitions. Correct benchmark before considering another source optimization. Next test existing fused-tail in this ThinLTO-preserving pipeline, with same sanitizer/coverage gates, before deciding whether it has an independent benefit beyond profile invalidation. Full suite94173 running generated/check-r326.log after timing ended; poll SAME handle. Normal app, saves, full PRD/G6 performance/audio and mobile hold unchanged.

## 2026-09-07 — R325 final-module test does not reproduce offline gain

- Prior turn progress/verified wait: flags-only build48846 confirmed live, polled SAME handle through linker51811 activity (2:44,4:42,6:06) to terminal0, never restarted. Module audit passed RMGE01/arm64/macOS14/1322chunks/indexed1024/19SMC. Unselected candidate SHA577d42c9e3713a882b733ac222a50c964a9cae08a97460290d313b461fff9c41,114404072bytes. Builder rechecks accepted objects/marker. Normal runnere86f2...b689 and diagnostic856b2...0e rehashed unchanged, profiles/save/settings verified; no game or Simulator during link.
- Control52527 then candidate94369 used explicit respective huffman-no-profile-r324 modules with same wakeup-r302 runner, prepared huffman-{control,candidate}-r325 profiles, Metal/1x640x528/Core6/CPUThreadTrue/Cubeb, completion/phase disabled. Both slot3 visually restored, R85trigger and movie progression observed. Trigger/top67270 and79339 terminal0. Control window373533377450875..373593377450875:3367VI/56.116667Hz,CPUmean17.620464ms,p9518.754541,p9919.579875. Candidate373776515012625..373836515012625:3371VI/56.183333Hz,CPUmean17.617940ms,p9518.698625,p9919.587750. Both dropped0/CPUinvalid0/reset0. CSV/summary/log/top under profiles.
- Both runtime exit0; control native1121799692/candidate1338793262, fallback0/smc_failed0. Both GameData unchanged a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6. Full-window result effectively unchanged (~0.12%VI/~0.014%CPU), not promotion evidence; no sustained60Hz. Whole-run audio remains startup/load-contaminated, not quality acceptance.
- After closing both, llvm-objdump --disassemble-symbols=_func_804530A0 on exact linked control/candidate saved chunk1103.asm beside each. Both function start0x579ca1c but21466/22426disassembly lines: code IS different, so do not claim compile flag was silently ignored or bodies identical. Final module effect differs substantially from R323 pre-link native-object benchmark (which explicitly strips ThinLTO). No reverse-order unchanged repeat warranted for this negligible signal.
- Next isolate final pipeline: preserve ThinLTO through the offline paired fixture (rename symbols after frontend PGO without discarding IR) and compare original/profile-free variants before another costly link. Alternatively one-chunk native-object link requires explicit justification from that experiment; do not drop LTO/profile globally. R32357% number remains synthetic/prelink only and did not transfer to game. No source/runtime edits this turn; suite68691/R324 remains latest passed and exact movie supplies current runtime evidence. No game/linker/Simulator remains. Normal app/marker/full PRD and mobile hold unchanged.

## 2026-09-07 — R324 flags-only isolated module linking

- Prior turn progress: one-variable unchanged-source offline control isolated saved-PGO penalty. Rechecked status/journal/no game/Simulator/no GalaxyPad memory hit. Sanitized wholefixture64950: python3 tests/benchmark-huffman-dcbz.py --drop-chunk-profile --long-codes --runtime-charge-reset; terminal0/32expected coefficient/state/memory cases passed, generated/huffman-no-profile-sanitized-r324.log.
- Extended existing verified builder with --huffman-no-profile. Pins original chunk1103e6aa...6dcc/header/core/profile/accepted module; compiles original source at original path, removes exactly -fprofile-instr-use argument from this one object's compile only, retains finalO2/ThinLTO/strictFP/arm64/macOS14 and original link flags. All1328other objects reused unchanged. No source transformation, fusion, globalPGO removal or active selection change.
- Reuses R316 unchanged control only after graph SHA, all1329object hashes, compiler version, accepted/control binary SHA, normalized exact link argv and response-file bytes match. Current control symlinks cached accepted1180447313459c4c153ca74e5215811e88a9b69a00d15feff940f062148fc631. Provenance records original/candidate flags and sourcee6aa915816ee2a89534f75c4d59dd0c145f7a5d0162c3e1a15b2ebe33b996dcc. Verified difference: one removed profile flag, no added flags. Avoided another redundant unchanged6minute link.
- Build48846 started python3 scripts/build-dc-column-experiment.py --huffman-no-profile >generated/build-huffman-no-profile-r324.log. Control proof/changed-object compile completed; linker51811 verified at1:16elapsed/725.8%CPU/~976MiB RSS. This is LIVE, not a stall; poll SAME handle. Final candidate hash/audit not available yet. Do not edit executing builder. Candidate directory generated/huffman-no-profile-r324/candidate; no runtime until link/audit terminal.
- Prepared fresh generated/runtime/huffman-{control,candidate}-r325 from dcbz-control-r282 Config/Wii/StateSaves/config.ini; configured separatepipes. Slot3aea2b7a3fe295b77e409753a216abf8fe6f2f1ec23ee7a8832fc9ac1a27b7ab8 and GameDataa5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 verified. Neither launched. Use same wakeup-r302 runner/explicit module overrides and prompt slot3/trigger-end60s window to avoid16384sample startup overflow. One runtime only, no compile overlap with timing.
- Focused graph test passes; full suite68691 exit0/repository safety passes generated/check-r324.log. No app/module promotion or gameplay claim; normal marker preserved. G6 performance/audio/full original PRD/mobile hold remains. Next build48846 terminal/audit then paired real movie and no-fallback/save/visual validation; offline57%gain is not game FPS evidence.

## 2026-09-07 — R323 saved-profile effect isolated in remaining Huffman chunk

- Suite79779 polled terminal exit0/repository safety passes. No game test this turn: benchmark/compiler experiment only, normal binary unchanged. Runtime benefit remains unproven.

- Prior turn progress: tail correctness/fullfixture/suite passed. Rechecked status/journal/dirty tree/no game/booted Simulator; no memory registry hit. Added correctness-only --tail-coverage to existing driver, rejects benchmark combination.70178 exit0:1008fused-tail hits and32long-code cases still pass expected coefficients/fullstate/memory (generated/huffman-tail-coverage-r323.log).
- Uninstrumented --fused-tail --long-codes --runtime-charge-reset --benchmark86079 exit0.32patterns xABBA, CPU-thread clock,1000warmup/10000timed calls perstep, no build/runtime overlap during timed execution. Aggregate means control4245.485469ns/candidate2102.897063 (~50.47%lower). Short-code counterpart16513 exit0:1818.967641/1238.178984 (~31.93%lower). Unexpected gains also on zero-AC cases mean do NOT attribute all benefit to fused path. Code object __TEXT60096→92680, profile mismatch1of31 observed. These are pre-link objects (ThinLTO stripped for assembly renaming), not final-module or game measurements.
- Added --identity-control: original source and same flags both variants.67459 exit0/32cases;means4253.292641/4243.483359 (~0.23%difference), both __TEXT60096. This does not support harness variant bias as explanation for the much larger candidate result.
- Added --drop-chunk-profile: original source BOTH variants, remove exactly one -fprofile-instr-use argument ONLY from candidate compile, other flags unchanged; modes mutually exclusive.97448 exit0/32cases, generated/huffman-no-profile-bench-r323.log. Means4249.011281/1844.750031 (~56.58%lower);zeroAC618.031031/196.635844,dense7879.991531/3492.864219. Candidate __TEXT92952 (control60096). Strong offline evidence that saved PGO changes code generation unfavorably for these workloads; fusion's profile invalidation/layout effects confound its apparent gain. No implication that all PGO is bad or whole game is57%faster.
- Decision: prefer UNCHANGED chunk1103 source/one compiler-profile flag removal over adding fused-tail complexity. Retain prototype/tests as evidence, do not integrate it. Next sanitized no-profile wholefixture, then isolated exact1329-object graph replacement of just chunk1103 with candidate flags; require unchanged-control reproduction and fresh paired real movie evidence. Do not regenerate whole module, drop profile globally, or promote from offline numbers. No app/marker change. Full suite79779 now running generated/check-r323.log; poll SAME handle. Full original PRD/G6 performance/audio/mobile hold unchanged.

## 2026-09-07 — R322 isolated normal Huffman tail fusion

- Suite37340 terminal exit0/repository safety passes. No game smoke this turn because experiment is offline/unselected and product binary unchanged; full runtime validation remains prerequisite to promotion.

- Full fixture56416 subsequently terminal exit0:32 long-code cases pass independently expected coefficients and reference/candidate state/memory with runtime charge reset. Explicit whole-fixture helper-hit coverage still required before timing; no speed result inferred.

- Prior turn progress: exact hotspot attribution plus tested reporting. Rechecked status/journal/no game/booted Simulator and no GalaxyPad memory hit. Selected chunk pinned e6aa915816ee2a89534f75c4d59dd0c145f7a5d0162c3e1a15b2ebe33b996dcc; generated header2cf2a7c3752cd6ab93f7c41140aaa2f3c1ae477cca6587925d6a5b51c3d683b9. Original full objective/performance-first/mobile hold unchanged.
- scripts/fuse_huffman_tail.py copies exact emitted80453AAC..53AEC body into static inline helper; only normal leader calls it, original external/interior entries and original backedge remain. Uses original memory helpers and observable statements, rather than speculative direct-memory batching/guards. This simpler scope naturally retains callbacks/journal/remap ordering; no instruction math/charge change. Removal assertion reconstructs original source exactly. No ref/generated module/app mutation.
- tests/test-huffman-tail.py compares actual emitted reference switch/suffix charges against candidate for16entries x8modes x1000cases=128000 perbuild. Both O1ASanUBSan andO2 pass fullCPU, all3memory regions, branch result and ordered callback state digests/counts. Covers MEM1/MEM2 mirrors/external reads/writes, callback register/exception/map changes, journal mutation, reservations, table/output overlaps, CLZ specials and small positive/negative budgets. Initial harness compile lacked generated rotl32 declaration; included and pinned actual generated header, then passed. Test added to suite. This is not full-loop or runtime acceptance.
- Existing whole-chunk Huffman fixture supports --fused-tail (mutually exclusive cached-reads), preserving accepted graph compile/PGO flags. Running56416: python3 tests/benchmark-huffman-dcbz.py --fused-tail --long-codes --runtime-charge-reset >generated/full-huffman-tail-r322.log. Reference compile produced1of31profile mismatch warning and advanced to candidate clang49840/49841; not a stall. Full suite37340 running generated/check-r322.log in parallel, correctness only/no timing. Poll SAME handles. Next whole fixture terminal and explicit candidate-hit coverage before offline timed comparison; no module link or promotion yet.

## 2026-09-07 — R321 remaining decoder instruction attribution

- Suite12050 polled terminal exit0, final repository safety checks pass. No runtime smoke this turn: only analysis/reporting tests changed, no app/module/runtime change; R320 exact-artifact movie evidence remains latest, not a new gameplay claim.

- Prior turn progress: reverse pair completed and DC candidate parked. Rechecked authoritative status/journal/dirty tree, no game/booted Simulator, no memory registry hit. Traced signed module rehashed5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d, matching R310 identity. Used existing summary/hot-functions.asm, no new trace or runtime.
- scripts/classify-cpu-instructions.py now optionally emits aligned weighted sites (--details); tests prove raw marker bits aggregate to one instruction, unrelated symbols excluded, weighted totals agree and missing disassembly fails closed. Default report unchanged. Existing compiled callback/alias test now asserts traced ARM64 CPUState offsets ramd80/ram_sized88/downcountd98/exramda0/exram_sizeda8. Both focused tests pass, full suite12050 running generated/check-r321.log.
- Evidence generated/runtime/decoder-profile-r310/chunk1103-sites-r321.json: all5048 func804530A0 samples join;2324loads/759stores/1864integer-or-other/101branches. x19 mapping metadata offsets account1170samples (465+286+247+172); downcount335. These are sampled-PC weights, NOT instruction costs or dynamic frequency. Prologue preserves CPUState* x0 in x19.
- Hottest native57b0028 (283samples) subtracts0x60c from w23=804540d4 and next stores PC80453AC8. Exact generated label is lbzx r9,r10,r5, followed by doubled index and sthx coefficient store at80453AD0. Native57aff90 (261samples) writes gpr12 after CNTLZW at80453AAC. Thus hottest pair belongs Huffman-Y coefficient tail, not row/IDCT arithmetic. Actual source follows with index increment/CR comparison and original cycle-budget backedge at80453AEC. No unsupported mapping from chunk name alone.
- Reviewed parked CLZ-onlyR86 and wholechunk entry-splitR187 (small unproven gains), plus read-loop cacheR286 and kernel map cacheR311. Do not repeat them unchanged. Next bounded offline prototype fuses normal coefficient tail80453AAC..53AEC, retaining every original interior-entry/fallback path and original backedge budget handling. Guard unsupported callback/journal/CPUState alias/wrapping states before mutation; compare full CPU/memory/reservation/PC/downcount and both branch outcomes to actual emitted sequence. This is a new larger fusion hypothesis, not an approved runtime shortcut or promised gain. No product source/package change, no new gameplay claim; full PRD/mobile hold persists.

## 2026-09-07 — R320 reverse-order DC confirmation; candidate parked

- Suite60548 subsequently polled terminal exit0; final log confirms repository safety passes. No runtime remains. No product source change this turn; completed movie comparison supplies runtime evidence, not full gameplay/performance acceptance.

- Previous turn progress: valid first pair changed promotion decision. Rechecked status/journal, original goal loop/PRD, dirty tree, root289a87a1499c3d0da23c21c472fd16525742208a, ModernGekko0514d9f03f8602809f66fc92fdca87d30e752997, no game/booted Simulator. Memory registry has no GalaxyPad hit. Control118044...fc631/candidate7e5d25...1eff/runner856b2e...0e rehashed unchanged.
- Fresh disposable generated/runtime/dc-{candidate,control}-r320 from dcbz-control-r282 Config/Wii/StateSaves/config.ini; pipe separately configured. Slot3aea2...7ab8 and GameDataa574...afa6 verified, Metal/1x/640x528/Core6/CPUThreadTrue/Cubeb. Candidate-first runtime20003 and control-second27960, exact explicit module override confirmed; both visually load preattack and advance through movie. Trigger/top2317 and73585 exit0. No build/profiler during timing.
- Candidate window371269032341000..371329032341000:3440VI/57.333333Hz, meanCPU17.243299ms,p9518.258250,p9918.945416. Control371481969144500..371541969144500:3369VI/56.150000Hz, meanCPU17.610817ms,p9518.607291,p9919.485125. Both no dropped/invalidCPU/reset samples. Evidence vi.csv/summary.json/host-top.txt/runtime.log beside profiles. Both runtime exit0; candidate native884165797/control1057295988, fallback0/smc_failed0. Both GameData remains a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6.
- Reverse pair favors candidate~2.11%VI, unlike first pair~-1.22%. Equal-duration combined mean VI candidate56.4333/control56.1833 (~0.45%edge), smaller than candidate run variation and not repeatable pair direction. No sustained60Hz; PARK candidate without promotion or more unchanged repeats. Host top reports no interval swap increases; background activity and unequal guest-frame progress remain caveats, not a diagnosed cause. Normal app/module selection untouched.
- Full host suite60548 now running generated/check-r320.log after game termination; poll SAME handle. Fresh R310 profile still identifies func804530A0 5048/31256 samples. Read THP boundary audit: that generated chunk spans row/Huffman routines, so do not equate chunk name to one logical function. Next map sampled PCs to exact emitted operations in this remaining hotspot, using existing trace/disassembly before new captures/builds. Full G6 performance/audio and original PRD/mobile gates remain open.

## 2026-09-07 — R319 movie comparison started

- Valid retry control78192 and candidate51910 both exit0; trigger/top62001 and58623 exit0. Explicit module paths confirmed, slot3 preattack and advancing movie visually verified. Control window370762782271458..370822782271458:3373VI/56.216667Hz, meanCPU17.614763ms,p9518.582208,p9919.017083. Candidate370976571423208..371036571423208:3332VI/55.533333Hz, meanCPU17.786801ms,p9519.603875,p9921.127209. Both dropped0/no invalid CPU/reset intervals. Summaries beside each runtime profile. Candidate roughly1.22%lower VI rate/0.98%higher mean CPU; this first pair does NOT support promotion despite offline gain.
- Both GameData SHAa5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 unchanged. Control native789422390/candidate974412975, both fallback0/smc_failed0. Whole-run audio includes startup/state load and is not window-specific acceptance. top shows background kernel_task/WindowServer/Logi activity on both, no interval swap increases; candidate additionally contacts/cloud work. These observations do not identify slowdown causality. No game or booted Simulator after close; disk73GiB available. Next reverse order with fresh profiles, prompt state load and exact trigger-end anchor, then park if no reproducible game gain. Do not rerun builder or promote normal app.

- Control43692 exit0/save unchanged/native3416814113/fallback0/smc_failed0, but requested window INVALID: recorder first16384 samples ended370514078126583 before window start; dropped5967. Startup/UI delay exhausted bounded recorder. Preserved original CSV/log as vi-startup-overflow.csv/runtime-startup-overflow.log. No timing summary or gain claim. Retry78192 uses same module/profile with prompt state loading to stay within capacity; no recorder code change or unsupported delay setting.

- Suite49623 completed exit0/repository safety passed before launch. One control runner43692/PID46928, explicit generated/dc-column-r316/control module confirmed in log. CUA reset resolved stale screenshot binding without starting another process; selected exact app path, loaded slot3 and visually confirmed flowers/lake, then fixture g6-r85-trigger-attack entered ship movie. Trigger76706 exit0.
- Control fixed60s window starts370550409557708; host top60616. No build/profiler/Simulator. Finish and close before separately launching prepared candidate. Initial wall-window comparison is not identical guest-frame workload or promotion evidence; normal app remains unchanged.

## 2026-09-07 — R318 candidate audit complete

- Previous goal turn was a verified wait on74720, not a stall. Polled SAME handle through terminal0. Candidate audit passes RMGE01/arm64/macOS14/1322indexed1024chunks/19SMC. Candidate generated/dc-column-r316/candidate/gRMGE01_recomp.dylib SHA7e5d25a73014892aa2ae52a5c6bea0a6d834d1f5aeff8ae95330a73611ea1eff,114404568bytes (same byte size as control). Control SHA118044...fc631 exactly reproduced accepted binary; builder rechecked all reused object hashes and original active marker before audit. No normal promotion.
- Executing builder is now terminal, so added deferred exact RMGE01.h pin2cf2a7...683b9 and provenance field for future runs. Current run's header had already been manually verified before candidate compile; do NOT rerun completed builder or imply its earlier JSON contained this later field. Full suite49623 running generated/check-candidate-ready-r318.log after source change; poll SAME handle before game launch.
- Normal runner e86f2e09d27f69a41d1f10c2513cde14898557bc379fa787be013b5f0122b689 unchanged. No runner/frontend/booted Simulator. Next existing wakeup-r302 runner plus explicit unsigned control/candidate overrides, prepared dc-control-r317/dc-candidate-r317 profiles and same movie sequence. Initial A/B is not promotion proof; inspect CPU/VI/save/native results and confirm ordering effects before acceptance. No profiler or build during game timing; full performance/mobile/PRD gates remain open.

## 2026-09-07 — R317 control reproduction and comparison preparation

- Previous turn progress plus verified wait: isolated builder and regression suite completed their preparation; original74720 remained active, not stalled. Polled SAME handle and verified ld45160 active through control link. Control completed and SHA1180447313459c4c153ca74e5215811e88a9b69a00d15feff940f062148fc631 exactly reproduces accepted module. This proves the reused object graph, not merely mtimes.
- Builder advanced to one changed-chunk compile and candidate link. Log has one of37functions profile-data mismatch; accepted generated/module-thp-r205.log likewise had one of33functions mismatch. Equal warning counts do not identify the same affected function. No new compiler error observed. Candidate ld45973 (clang45972) confirmed live,1:17elapsed/632.6%CPU. Keep polling SAME74720; candidate final hash/audit not yet available. Do not edit executing builder; generated-header explicit pin still deferred until terminal.
- Prepared ONLY disposable dc-control-r317/dc-candidate-r317 profiles from dcbz-control-r282 Config/Wii/StateSaves/config.ini; Pipe paths separately configured. Both slot3 SHAaea2b7a3fe295b77e409753a216abf8fe6f2f1ec23ee7a8832fc9ac1a27b7ab8 and GameData SHAa5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 match. Native640x528/scale1/Metal/CPUThreadTrue/Core6/Cubeb verified. Neither profile launched; no game during compilation.
- Read-only journal-hook check: global defaults NULL in GXRuntime cpu.c; setter is acquired by StaticRecompLockstep. This does not prove live candidate guard frequency. After build/audit, compare both unsigned modules with same existing runner, same movie route/VI timing and no build/profiler overlap; preserve originals and normal active marker. No whole-game improvement/mobile/full PRD acceptance yet.

## 2026-09-07 — R316 isolated candidate build and cache proof

- Suite34276 COMPLETED exit0/repository safety passes. Build74720 still live: ld45160 observed elapsed3:16/655.8%CPU/~1GiB RSS, log remains unchanged-control gate. Quiet output is not a stall; poll same handle. Header-pin follow-up remains deferred until executing builder stops.

- Previous turn progress: both-kernel correctness/hit coverage and4.03% mixed offline gain justify real-module testing. Rechecked status/journal/active marker/no runner/74GiB free. Accepted and module-build dylibs both SHA1180447313459c4c153ca74e5215811e88a9b69a00d15feff940f062148fc631. Actual target chunk fa455a7...31bae is byte-identical to tested fixture after include-path normalization. Current/tested RMGE01.h both2cf2a7c3752cd6ab93f7c41140aaa2f3c1ae477cca6587925d6a5b51c3d683b9. No GalaxyPad memory hit.
- Added scripts/build-dc-column-experiment.py using actual accepted Ninja graph,1329objects/1322chunks, original O2/ThinLTO/strictFP/arm64/macOS14/PGO flags and pinned compiler/profile/core header/float/chunk identities. Fresh generated/dc-column-r316/{control,candidate}; records every object hash, graph/source hashes and exact commands. First relinks UNCHANGED objects and refuses candidate work unless control SHA exactly matches accepted. Only then compiles one modified chunk/links candidate, rechecks all original hashes/marker, writes explicit unselected overlay manifest and audits candidate. Never invokes Ninja, edits ref, or selects/replaces normal app/module.
- Build74720 live in unchanged control link; confirmed clang45159/ld45160 while log generated/build-dc-column-r316.log says control gate. Poll SAME handle, do not restart if output quiet. Candidate compile/link/audit not yet reached or claimed. Current generated-header identity manually verified as above; add its explicit builder pin after active work stops, without modifying an executing script.
- New focused graph-parser regression passes, preserving compile flags/order dependency/linker input fields; added to suite. Full suite34276 running generated/check-dc-build-r316.log (no benchmark/game concurrently). Poll both existing handles. No Simulator/mobile/publication or full PRD/performance acceptance.

## 2026-09-07 — R315 both signed-DC column kernels

- COMPLETED suite2521 exit0/repository safety passes; all step handles terminal. Product remains unchanged.

- Previous turn progress: first-kernel extension passed exhaustive signed16/FP tests and~2.25% mixed offline gain. Read current status/source and second emitted column. Added explicit both_kernels/nonzero_dc mode; second helper uses multiply PC80452BEC/finalPC80452C24 and rejoins original80452C30 CTR/yield branch. Default earlier modes preserved. Outer chunk/entry trampolines unchanged. No generic arithmetic, callback, journal or timing bypass.
- Direct second-kernel test passes262144 wholeCPU/memory/hostFP comparisons (all signed16 values×4rounding modes), overlaps/reservations/13guards, using its actual emitted instruction order. Combined check/oracle38913 exit0. Full oracle1440cases/digest882be09b1f38e9ad and original counters match across4sanitized/O2 variants; separate counters prove96hits in EACH kernel for EACH candidate build. Counter-free benchmarks remain separate.
- Benchmark32272 exit0:200k-per-case CPU-time ABBA control24472.642/24651.839ns summed means, candidate23634.761/23510.023; aggregate0.9597004 (~4.03% faster), kernel0 ratio0.9513120, kernel1 0.9681504. This justifies an isolated candidate build, not an accepted in-game gain. Source review found existing build-fprf-experiment.py precedent for reusing hash-verified unchanged accepted objects without invoking Ninja or selecting the candidate; current accepted graph/objects still need verification before reuse.
- Next build reproducible isolated candidate from exact current accepted graph/profile with only target chunk replaced; preserve normal module/app and active marker, validate identities/flags/source guard, record object reuse/commands and audit output. If cache identity cannot be proved, use pinned canonical opt-in build, not guessed flags or stale objects. Then a real movie comparison without concurrent builds/profilers. No mobile/full PRD gate promotion.
- Full suite2521 running generated/check-both-dc-suite-r315.log; poll SAME handle. Evidence generated/check-second-dc-r315.log, oracle-both-dc-r315.log, bench-both-dc-r315.log. Live checks: no runner/frontend/booted Simulator; no GalaxyPad memory hit; no publication.

## 2026-09-07 — R314 signed DC with zero AC

- COMPLETED full suite26160 exit0/repository safety passes; all step handles terminal. No product build/runtime/Simulator/publication this step.

- Benchmark37493 exit0:200k-per-case ABBA mixed candidate/control0.9775334 (~2.25% improvement). Kernel0 per-pattern ratios zero0.8866484/DC0.8644319/sparse0.8816263/dense1.0073102/varied0.9955470/extreme0.9946771. Qualifying fixtures improve~11–14%; former sparse regression removed. Text candidate245760bytes. This is offline kernel evidence, not real-frame weighting or game performance. Next test corresponding second-kernel segment with its actual instruction order and exact final PC/cycles before considering canonical integration. Full suite started generated/check-dc-column-suite-r314.log; record/poll current handle.

- Previous turn progress: zero-column fixtures showed10–11% targeted benefit but sparse regression and tiny aggregate effect. Rechecked current status/helper/source. Extended the offline helper with explicit nonzero_dc option; default R313 behavior remains available. The predicate now permits the first signed16 coefficient only; guarded scale0 dequantization is exact signed16→double, while original paired multiply/FPSCR/store/yield handling stays unchanged.
- tests/test-zero-column.py --nonzero-dc passes262144 comparisons: all65536 signed16 values under4rounding modes, random/special quant bits, wholeCPU/RAM/EXRAM/hostFPflags/reservations and output/source overlaps.13reject guards remain (first rejection moved from DC byte to first AC byte because DC is now supported). Added mode to suite. Combined focused/oracle handle57143 exit0; log separately confirms focused completion, not merely shell final status.
- Full oracle1440cases/digest882be09b1f38e9ad and all existing callback/journal/yield/exception counters match across4sanitized/O2 reference/candidate variants.96fast hits in each candidate, up from60 for all-zero support. No runtime/module claim. Evidence generated/check-dc-column-r314.log and oracle-dc-column-r314.log.
- Read-only comparison of the second kernel's80452BDC..80452C30 column after subtracting0x48c shows only lhz r7/or r0 instruction order and intermediate CR/PC differences; final zero-AC contract may transfer but has NOT been tested/applied there. Keep this distinction before broadening scope.
- Started200k-per-case uninstrumented CPU-time ABBA, generated/bench-dc-column-r314.log; record/poll current handle before further build. No game/Simulator/module/app mutation or publication; full PRD/performance/mobile gates open.

## 2026-09-07 — R313 guarded whole zero-coefficient column

- COMPLETED suite19297 exit0/repository safety passes. All step handles terminal. No runtime/app change or publication.

- Benchmark56268 exit0:200k-per-case ABBA control totals24600.528/24468.489ns; candidate24310.271/24432.641ns, aggregate0.9933542 (~0.66%). Kernel0 ratios by pattern: zero0.8902563, DC0.9031650, sparse1.0376199, dense1.0001624, varied1.0052256, extreme1.0027747. Concrete~10–11% improvement in qualifying zero/DC fixtures, not whole-game acceptance; sparse regression and unknown real-column distribution prevent promotion. No module build or unchanged repeat. Next extend predicate/state handling to nonzero DC with zero AC, which is excluded now; retain exact paired multiply/FPSCR and original loop/yield control. This is a changed mechanism/input domain, not a claimed fix.
- Full suite19297 running generated/check-zero-column-suite-r313.log after benchmark; poll SAME handle. No game/Simulator or concurrent measurement.

- Previous turn progress: DC-store segment correctness/coverage passed but its~0.44% aggregate signal was insufficient for module work. Rechecked current status and exact generated column from80452750 through804527A0, retaining original804527A4 CTR/decrement/yield/backedge. Audited CPU psq_dequant/type7 and actual paired multiply/FPSCR routines; no runtime/source mutations in ref.
- New offline scripts/zero_dc_column.py uses direct16-byte all-zero coefficient/8-byte quant/32-byte output guards, exact GQR5, GQR0type0/FP/LSQE, no exception/journal, bounded nonoverlapping mappings, CPUState/source/output and journal-pointer alias exclusions. All source reads before stores; exact ppc_ps_mul_op retained for arbitrary quant values/FPSCR. Reproduces f0/f10/f11 lanes, r0/r6/r7/r8, CR/SO, r3+16/r5+32/r10+32, PC52798 and22charged cycles. Eight stores/reservation clears stay ordered. Rejoins original loop control, never batches across yields. Unsupported input retains unchanged emitted labels/outer entries.
- tests/test-zero-column.py passes40000 exact emitted-column wholeCPU/RAM/EXRAM/hostFP comparisons across4rounding modes, random plus special quant values, four mirrors, varied budgets/reservations, and output overlapping coefficient or quant memory.13no-mutation rejection guards pass. Uses actual cpu/float/exception implementations under ASan/UBSan, no math stubs. Added to suite (pending full run).
- Full kernel oracle76047 exit0: all4sanitized/O2 reference/candidate variants1440cases/digest882be09b1f38e9ad with identical624yields/480FPfaults/480illegal/192interruptions/96callerreturns/18816callbacks/34688journals/1440reservations. Candidate-only correctness counters prove60fast hits in both builds; counters absent in benchmark. This is fixture evidence, not full game/coverage acceptance.
-200k-per-case CPU-time ABBA56268 running generated/bench-zero-column-r313.log; poll SAME handle before any build. Focused evidence generated/check-zero-column-r313.log and oracle-zero-column-r313.log. No normal app/module changes, no game/Simulator/publication/mobile; original PRD/performance gates open.

## 2026-09-07 — R312 guarded DC-only repeated-store segment

- COMPLETED full suite40939 exit0/repository safety passes. All step handles terminal; no product edit/runtime/Simulator/publication this step.

- Benchmark70589 exit0:200k-per-case ABBA aggregate candidate/control0.9955605 (~0.44%); changed kernel0 ratio0.9914648 (~0.85%), unchanged kernel1 ratio0.9996722. Too small for an expensive module/runtime promotion; no speed claim or unchanged rerun. Retain the exact guarded segment as offline groundwork, not accepted product code. Next investigate a whole zero-coefficient column, requiring exact registers/FPSCR/stores/branch/downcount behavior and direct-memory/alias/callback guards; do not assume all-zero output permits skipping guest state or yield boundaries. Full suite40939 running generated/check-dc-store-suite-r312.log; poll SAME handle.

- Coverage8792 exit0: candidate sanitized and O2 each192 fast-path hits; full1440-case digest/counters match. Started uninstrumented200k-per-case CPU-time ABBA70589, generated/bench-dc-store-r312.log; poll SAME handle. No concurrent suite/build/game during timing; performance outcome still pending.

- Prior turn progress: callback-bounded mapping cache rejected on measured3.51% regression. Rechecked current source/status; no GalaxyPad memory hit or active runner. New hypothesis is work elimination in the existing DC shortcut, not another metadata-cache variant.
- Source-pinned offline scripts/fuse_dc_stores.py adds one guarded fast path at kernel0 label80452788, rejoining8045279C. Original three paired type0 stores reuse f0/ps1[0]; guard requires r7zero, no exception, FP/LSQE enabled, type0 GQR0, no journal, sane nonoverlapping mappings, full24-byte direct span disjoint from CPUState/journal pointer, and safe downcount subtraction. Converts two lanes once, keeps six32-bit stores/reservation clearing in order, reproduces CR/SO, PC52798, r10+32, downcount-8. Other labels/outer trampoline and unsupported states retain original code. No callback skipped on supported path because its guards exclude those routes; no output contents or guest scheduling fabricated.
- Direct emitted-segment test100000 cases matches wholeCPUState/RAM/EXRAM/reservations over random lane bits, four mirrors, offsets/CR/XER/budgets;14 rejection cases include journaling, exceptional/disabled states, unsupported quantization, bounds, CPUState/journal-pointer alias and underflow. Initial harness incorrectly declared copied boolean-return segment void; corrected harness return type, final ASan/UBSan test passes. No product error inferred.
- Initial full-kernel oracle17696 exit0: four sanitized/O2 reference/candidate variants1440cases/digest882be09b1f38e9ad, matching all observable fixture counters. Added candidate-only correctness-run hit assertion/destructor report (not benchmark instrumentation) to prevent fallback-only coverage. Coverage oracle8792 running generated/oracle-dc-store-coverage-r312.log; poll SAME handle. Next verify hits then200k CPU-time ABBA without counters; no speed/module/runtime claim yet. Focused test added to suite, fullsuite pending. Normal app unchanged/mobile/full PRD open.

## 2026-09-07 — R311 callback-bounded kernel read-map prototype

- COMPLETED suite13112 exit0/repository safety passes. All step handles terminal. No product change, runtime launch, publication or mobile work this step.

- Benchmark73475 exit0:200k repetitions/case, ABBA control totals24595.800/24618.835ns, candidate25625.517/25315.953ns (~3.51% slower). Text229376→245760bytes. Reject/park this cache design; metadata-load attribution did not predict benefit. No module build/repeated unchanged cache experiment. Full suite13112 running generated/check-cached-kernel-suite-r311.log; poll SAME handle.
- Next move toward a closed algorithm-level decoder fast path with explicit supported-state guards and original fallback, not further generic metadata/helper layout variants. Exact guest state, callback effects where applicable, scheduling/yields and output parity still required before adoption; no permission inferred to bypass them. Current proposal remains unimplemented, not a new success criterion.

- Previous turn progress: current movie profile and exact instruction join confirmed the structural target, with clean runtime/save and passing suite. Inspected actual get_ram_ptr/read helpers, kernel generated calls, prior rejected global-hoist/lookup-order/Huffman cache work. Current experiment is scoped to the two transform kernels, not another unchanged Huffman or lookup-order run.
- Added offline scripts/cache_kernel_reads.py pinned to extracted kernel SHA98d986...a68991. Lazy local RAM/EXRAM pointer/size snapshot, original EXRAM-first/unsigned lookup arithmetic. Scalar reads and type0 paired-load fast reads use it; external read invalidates before callback; quantized/slow psq_load invalidates before original helper; all94 memory/paired stores invalidate before original call. Pure arithmetic/control retained, unsupported call names fail closed, outer chunk byte-identical. No memory contents cached, no store batching, no skipped callback/PC/cycle checks, no ref/module/app mutation.
- tests/test-kernel-read-cache.py passes84000 width/boundary/mirror/read-callback pointer+size remap comparisons under ASan/UBSan. Additional CPUState-alias mapping-field store proves next read resnapshots; source guard and exact94 adjacent store invalidations checked. Added regression to suite (not yet run this step).
- Full kernel oracle69385 exit0: all4 sanitized/O2 reference/candidate variants1440cases/digest882be09b1f38e9ad; yields624/fp_faults480/illegal480/interrupted192/caller_returns96/callbacks18816/journals34688/reservations1440. This is fixture correctness, not game acceptance or complete external-callback proof.
-200k-repetitions-per-case CPU-time ABBA benchmark73475 running generated/bench-cached-kernel-r311.log; poll SAME handle. No simultaneous build/game/benchmark. Next inspect effect before any canonical integration/module build; no speed claim yet. Focused evidence generated/check-kernel-read-cache-r311.log and oracle-cached-kernel-r311.log. Normal app unchanged/mobile/full PRD/performance gates remain open.

## 2026-09-07 — R310 current movie CPU profile

- COMPLETED full suite36507 exit0/repository safety passes; all handles terminal. No game/frontend/Simulator remains.

- Previous turn progress: rejected exact tie-body outlining after longer timing removed the apparent gain; no reason to repeat it. Inspected existing kernel/caller assembly and R214 profile; refreshed older hotspot evidence before another structural candidate. No product/source changes.
- Verified no runner/frontend/booted Simulator,74GiB free. Prepared decoder-profile-r310 from dcbz-control-r282 Config/Wii/config.ini/slot3. Save a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6; slot3 aea2b7a3fe295b77e409753a216abf8fe6f2f1ec23ee7a8832fc9ac1a27b7ab8. Launched existing wakeup-r302 app23163/PID39628, signed module5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d verified. Early CUA selection timed out before initialization and opened frontend39680; verified its exact path and terminated that frontend only. Wait for module/window initialization before selecting app; do not repeat this early-selection mistake.
- Loaded slot3 through States menu, screenshot verifies pre-attack scene. Trigger37024 exit0, film visible. CPU Profiler88533 bounded10s exit0; trace22MiB, not SystemTrace. Subsequent screenshot shows film advanced, clean UI close/runtime23163 exit0; no runner/frontend left. Original copied save unchanged, native582941929/fallback0/smc_failed0. Whole~100s45underruns/3backlogs includes startup/state load/profiler and does not prove fixed-window audio or unprofiled performance.
- Export90108 exit0;31256 CPU samples,31198 P-core/58 E-core. Leaves: kernel0 5239,kernel1 2839,func804530A0 5048 (42.00% combined); convert_to_double1500,ps_add1341,ps_sub1197,psq_store1080,ps_madd975,func804540A0 971,StaticRecompCore::Run880,ni_add875,TryGetLockedCachePair769. Fresh exact-binary disassembly joins all selected PCs. Kernels6312/8078 samples on load instructions (78.14%),316store,217call,278branch,948integer/other,7scalarFP. Sampling skid/inlining apply: percentages are attribution, not per-instruction cost or optimization gain.
- Source/mapping-field rereads remain a concrete structural target. Next inspect a narrowly guarded kernel read segment and its callback/alias invalidation boundaries, distinct from parked Huffman-loop snapshots and global hoisting. No assumption that metadata remains constant across callbacks, no guest timing change. Full PRD/performance/mobile gates remain open. Artifacts generated/runtime/decoder-profile-r310/{cpu.trace,cpu.xml,summary.json,hot-functions.asm,instruction-families.json,runtime.log,vi.csv}; anchor is command-start, not exact profiler sample-window proof.
- Focused CPU-summary/classifier tests pass. Full suite36507 running generated/check-decoder-profile-r310.log; poll SAME handle. No other live step handles; no publication.

## 2026-09-07 — R309 exact FMA tie-correction outline experiment

- COMPLETED suite72215 exit0, repository safety checks pass. All step handles terminal. No runtime smoke because product unchanged; offline scalar/full-kernel correctness and bounded timing are the evidence, not gameplay acceptance.

- Full repository suite72215 running generated/check-madd-tie-suite-r309.log after benchmarks completed; poll same handle. Next select structural decoder work with greater possible impact, not further helper layout variations. Full G6/performance/audio and original PRD remain unaccepted.

- Benchmark73528 exit0: short ABBA control totals24793.527/24711.497ns, candidate24286.202/24265.093; apparent1.93% improvement with visible within-batch variation. Changed sampling length from20k to200k repetitions per case to test that weak signal. Longer46295 exit0: control24590.892/24479.402ns, candidate24590.770/24706.204; aggregate ratio1.0046195 (~0.46% slower). Text size unchanged229376bytes. Reject performance promotion/park exact tie outlining; no module build or repeat unchanged. No product/runtime change or gameplay acceptance.

- Previous turn was progress: actual target/thread source audit ruled out assuming an explicit priority defect and redirected work to measured CPU execution. Rechecked goal loop, latest status, existing THP oracle/hotspot/parked results and current exact float source; no GalaxyPad memory hit. Normal runner hash remains e86f2e09d27f69a41d1f10c2513cde14898557bc379fa787be013b5f0122b689; no game/frontend launched.
- Hypothesis: move only ni_madd_msub's conditional single-precision tie-correction body into a noinline cold helper to reduce hot code footprint. This differs from parked add/sub inlining and NaN outlining, but is not assumed beneficial. scripts/outline_madd_tie.py refuses any source except SHA554149...934f; original arithmetic/order/constants/guard retained. All changes offline, no vendor/product patch or module selection.
- tests/test-madd-tie-outline.py (41931 exit0) passes960000 scalar result/exception/whole-state/host-flags comparisons in both ASan/UBSan and O2, all4rounding modes, special triples and random bits. Constructed inputs reach50114 ties and50000 actually corrected results; stale-source rejection passes. Added focused regression to repository suite (not yet run this step).
- Full generated-kernel oracle23499 exit0: reference and candidate, sanitized and O2, each1440cases/digest882be09b1f38e9ad, matching yields624/fp_faults480/illegal480/interrupted192/caller_returns96/callbacks18816/journals34688/reservations1440. Same extracted kernel source on both sides, only float implementation differs. Correctness within this fixture is not full-game acceptance.
- CPU-time ABBA benchmark73528 now running, log generated/bench-madd-tie-r309.log; poll SAME handle. No other benchmark/build/game during measurement. Next inspect actual outcome before module work; no speed claim yet. Evidence check-madd-tie-r309.log/oracle-madd-tie-r309.log. No publication/mobile work.

## 2026-09-07 — R308 scheduling-policy source audit

- Read-only audit followed R307's inconclusive shared-delay evidence. ModernGekko CMakeLists.txt macOS target selects DolphinNoGUI/PlatformMacos.mm (not the unused legacy tree). Core/Core.cpp:231 creates EmuThread using std::thread; :476/:493 establishes it as CPU thread, :637 calls the selected CPU function. Video thread is separately created at :436. Common/Thread.cpp naming/Apple affinity code does not establish QoS. No explicit QoS/activity policy found in inspected runtime/platform paths.
- This is not evidence that effective QoS is low, that App Nap occurred, or that raising priority would fix the observed joint gaps. No scheduling, power, queue or synchronization behavior changed. Avoid turning absence of an explicit policy into a speculative fix. Host-counter freshness limitation and runnable-versus-on-core distinction remain unresolved; do not repeat unchanged observer captures.
- Next return to the established CPU-execution deficit: use existing decoded/paired-single hotspot evidence to select a materially different semantics-preserving candidate, require focused differential correctness and an isolated throughput gain before a module build. Parked candidates remain parked; do not rerun them unchanged. Sustained movie CPU budget and gameplay hitch acceptance are distinct, both still open.
- No product edits, rebuild or gameplay claim this step, so no new runtime/full-suite result. Live checks: no GalaxyPadRunner/GalaxyPadFrontend or booted Simulator;74GiB available. Other-project Simulator erasure already completed in R283 (59GiB device storage to492MiB, eventual free-space gain about57GiB); no repeated erasure or deletion of regenerated data. Normal app and full PRD/mobile hold unchanged. No publication.

## 2026-09-07 — R307 combined host/thread/VI capture

- Final suite60870 exit0/repository safety checks pass. All handles terminal; no game/frontend remains. No publication/mobile work.

- COMPLETED input2281/thread33226/host93921/runtime24200 exit0. End screenshot shows visible movement; save5040...64a6 unchanged/native4641449698/fallback0/smc_failed0. Whole279s5underruns/13backlogs not fixed-window audio acceptance. VI7177=59.808333Hz,7176valid,CPUmean15.503245ms,p9917.911792.
- Worst87.510041ms at101.048993s:22.198125CPU/2.507749EFB/.171878idle/.001208throttle/0DVD/0gather/.017833wakeup.83.377292ms at3.776572s:23.191166CPU/.334709EFB/.129172idle/.033749wakeup. State samples12(11running/1waiting),9(allrunning); observer gaps46.991083/57.352458ms respectively.79.303541ms hitch has34.367292ms query duration, so not every observer gap is between calls. No scheduler/wait causal proof.
- Host bracketing snapshots show zero swap deltas, but repeated complete CPU counters at100ms exposed freshness limitation. Verified published Apple XNU host.c (https://github.com/apple-oss-distributions/xnu/blob/main/osfmk/kern/host.c, retrieved2026-09-07): HOST_STATISTICS_TIME_WINDOW1s, randomized2..10-request quota and cached return for third-party apps. Published source is not exact running-kernel version proof. Do not label query time as counter refresh time or use short-bracket zero deltas to exclude pressure. align-host-pressure now explicitly marks snapshot_freshness unknown_kernel_cache; artifacts regenerated.
- Stop unchanged combined-probe repeats: valid new evidence is repeated joint delay and API resolution limit, not host-only diagnosis. Next select suitable scheduling evidence without bypassing restrictions or return to a materially different CPU-work optimization. No queue/priority/timeouts altered. Full suite60870 running generated/check-host-pressure-r307.log after runtime exit; poll same handle. Artifacts runtime/host-pressure-r307/{vi.csv,states.csv,host.csv,summary.json,aligned.json,pressure.json}.

- Load51593 exit0/Observatory verified. Input2281/thread33226/host93921 live, runtime24200. VI window364920000334375..365040000334375. Observers begin shortly after anchor; report edge coverage accurately. No builds/top/stack sampler during capture; poll same handles and close only after inputs and observers finish.

- Previous turn was progress: host idle audit and bounded recorder tests. No preexisting runner/frontend/Simulator; runner856b...d2b0e verified. Prepared host-pressure-r307 with same original one-star save5040...64a6/config. Existing wakeup app launched runtime24200/PID37222, module/title verified. No rebuild or system setting changes.
- Added align-host-pressure.py + synthetic tests for host snapshot bracketing, missing coverage, invalid endpoints and counter reset; pass. Aggregate CPU ticks do not resolve per-core scheduling; brackets include neighboring work. Added test to suite, not executing suite during runtime. Next real-save load then120s VI +2ms thread +100ms host capture, clean close/analysis.

## 2026-09-07 — R306 idle host audit and pressure recorder

- Previous turn was progress: independent observer52.13ms delay spans game hitch pair, not causation proof. Live vm_stat/pmset/sysctl/three-sample top checks: AC100%,lowpowermode0 both power profiles; no recorded thermal/performance warning,74GiB available. Swap used605.56MiB; swapins511166/swapouts826164 unchanged over check, compressions76973044 unchanged. Lifetime counters are not current pressure. WindowServer42-43%/logi21-30%/kernel28-49% while total CPU78-81% idle; does not prove per-core availability. Did not change settings or stop unrelated processes.
- Added scripts/host-pressure-probe.cpp using read-only Mach host_statistics CPU_LOAD and host_statistics64 VM_INFO64. Bounded<=120s,100ms polling, preallocated buffer, query start/end timestamps, raw CPU ticks/pageins/pageouts/swap/compression counters, export after capture. Invalid/short replies fail with empty data fields. No memory stress, process suspension, privileged access or runtime rebuild.
- ASan/UBSan CSV/order/bounds tests pass,54628 exit0; generated/host-pressure-probe compiled, one-second actual capture generated/host-pressure-r306.csv. Ten snapshots, CPU deltas user42/system39/idle414, swap/compression unchanged,19pageins. No gameplay inference from idle fixture. Added test to suite; full suite not run this step.
- Next existing audited wakeup app120s real-save with thread-state2ms and host-pressure100ms; align query bounds/counter deltas around reproduced hitch and report resolution/observer overhead. No claim that aggregate idle excludes scheduling contention; no thermal-frequency proof from absence of recorded warning. Full PRD/performance/mobile gates remain open.

## 2026-09-07 — R305 cross-process thread-state capture

- Final validation64064 exit0, repository safety checks pass. All handles terminal; no game/frontend/Simulator launched after capture.

- COMPLETED input48488/observer16451/runtime6948 exit0. Post-window screenshot shows visible Mario movement; clean close. Save5040...64a6 unchanged/native4283020223/fallback0/smc_failed0. Whole263s6underruns/13backlogs not fixed-window audio acceptance. No runner/frontend remains.
- VI7187=59.891667Hz,7186valid,CPUmean15.416324ms,p9917.566417.58.091209ms hitch ends60.837214834s,CPU18.659875/EFB.456708/idle.26009/throttle.001248/DVD0/gather0/wakeup.032583. Next53.341291ms ends60.890556125s,CPU21.69925/EFB14.454875/idle2.122129/wakeup.023543. Do not subtract overlapping counters.
- State window47331valid snapshots (44272state1,3059state3), no invalid. Largest58ms interval contains7snapshots allstate1; next53ms contains10 allstate1. Critically observer inter-query gap364311568819833..364311620950250 =52.130417ms spans these intervals; next14.570875ms gap follows. First-window edgegap1.164s is observer launch offset, not interior stall. Query maximum.302125ms. Extended alignment output to show whole inter-query gaps rather than only clipped per-frame gaps.
- Interpretation: state1 is running OR runnable; gaps preclude sustained-state claims. Shared scheduling pressure/observer sleep overshoot now a concrete lead, not proof of host-only lag. Next collect bounded host pressure/scheduling context, do not repeat identical state-only capture or remove game synchronization. Artifacts runtime/thread-state-r305/{states.csv,vi.csv,summary.json,aligned.json}. Alignment tests pass; full suite64064 started generated/check-thread-state-r305.log after runtime exit, poll same handle.

- Load44995 exit0/one-star Observatory verified. Input48488 and observer16451 live, runtime6948. Window364250769603916..364370769603916. Observer120s/2ms starts just after window anchor; report edge gaps rather than assuming coverage. No top/stack sampler/build during this changed-variable capture. Poll same handles before closing/export/analysis.

- Previous turn was progress implementing/testing read-only libproc observer. No runner/frontend/Simulator initially; existing audited wakeup app runner856b...d2b0e verified. Prepared thread-state-r305 original one-star save5040...64a6/config. Runtime6948/PID36074 launched, module/title verified. One-second2ms cross-process access probe exit0, access.csv. No rebuild/suspension/priority changes.
- Added scripts/align-thread-state.py and synthetic tests: query containment/straddles/invalid snapshots/max gaps pass. No state duration interpolation; running includes runnable. Added test to suite, not run full suite during runtime. Next real-save load then120s state+VI window and active inputs, clean close, aligned hitch analysis.

## 2026-09-07 — R304 bounded thread-state observer

- Previous turn was progress excluding measured wakeup during87ms hitch. Inspected installed macOS SDK libproc/proc_info/thread_info headers: PROC_PIDLISTTHREADS/PROC_PIDTHREADINFO expose name, run state and raw user/system counters without suspending threads. Implemented scripts/thread-state-probe.cpp, compiled generated/thread-state-probe. Exact GalaxyPadRunner leaf and unique CPU thread required, max120s/period2..100ms, bounded preallocated vector, no capture-time file writes. Invalid reads exported invalid/empty and stop; no reacquisition. Query start/end use same C++steady epoch as VI.
- Self-test83737 exit0 observed waiting61/running80 snapshots of controlled named worker; missing name rejected. ASan/UBSan test24944 checks sleeping/busy distinction plus malformed/bounds/non-runner rejection. Added to repository suite; full suite not rerun this step. No game/source patch/build/app change.
- Interpretation: TH_STATE_RUNNING includes normal runnable state, not proof of on-core execution; WAITING snapshots don't name the wait. Raw CPU-time units deliberately unlabeled pending verification. Sample gaps and query latency must be shown, not interpolated into continuous states. Next existing audited wakeup-r302 app120s real-save plus2ms observer; verify cross-process access early, align query-contained observations to VI hitches. No repeated full-stack sampler/SystemTrace; no priority/sync changes. Normal app unchanged/mobile/full PRD gates open.

## 2026-09-07 — R303 wakeup diagnostic build/audit and capture

- COMPLETED input4441/top83567/runtime98201 exit0. End screenshot visibly moved Mario onto ledge. Clean close once, terminal verified, no post-close app query. Window363679754984208..363799754984208:7189VI=59.908333Hz,7188valid/noreset. CPUmean15.477628ms,p9917.813375,407over16.667ms.
- Largest87.0895ms at78.109318208s:23.011CPU/.307458EFB/.174345idle/.001498throttle/0DVD/0gather/.03421wakeup. Next34.407292ms at116.383449s with17.070375CPU/1.993999EFB/.278585idle/.016417wakeup; adjacent29.016459ms at78.138335s with22.098875CPU. Wakeup135.974745ms total,p99.039667ms; valid nonzero counter refutes notification wait for this captured87ms incident. No generalized exclusion for all past incidents.
- Save5040...64a6 unchanged; native4032114677/fallback0/smc_failed0. Whole245s8underruns/13backlogs not fixed-window audio acceptance. Evidence generated/runtime/wakeup-r303/{vi.csv,summary.json,host-top.txt,runtime.log}. Stop wakeup-only repeats; next bounded host thread-state evidence to discriminate scheduler delay from unmeasured waits, preserving synchronization. Normal app unchanged/full PRD/mobile/performance open.

- Runtime98201/PID35355 title verified; load68126 exit0, screenshot shows one-star Observatory. Input4441/top83567 live. Fixed window363679754984208..363799754984208,16cycles/120s, no sampler. Poll same handles; clean close only after target end and inputs complete, then inspect wakeup/VI deltas.

- Previous turn was progress plus verified build wait. Polled SAME52718 through199steps/exit0; audit9808 exit0. Runner856b2e698373ab4427fd62cf037bc8a8f849d4547ee4265a0afd58cc255d2b0e, accepted signed module5c2101...e91d unchanged.74GiB free; no preexisting game/frontend/booted Simulator.
- Prepared wakeup-r303 from accepted-r238 Config/config.ini and original midblock-r145 Wii, save5040...64a6 verified; no savestates. Launched isolated wakeup-r302 app with VI-only environment, no stack sampler. Next title/load then120s active-control capture; no normal-app promotion/mobile work.

## 2026-09-07 — R302 wakeup VI wiring and isolated build

- Build52718 confirmed live; poll same handle before any rebuild/bootstrap. No game or Simulator launched.

- Previous turn was progress installing notification-only core observer. Added ModernGekko0024-wakeup-recorder.patch SHA2fab078afc255d0f278c476a3b86c67bef901aadc97962737c34f51558916350. Runtime configures CPU-owned counter from VI Enabled, reads cumulative notification elapsed ns; bounded sample/CSV append wakeup field. Missing old field stays null; reset stays null. Pin/peel-before-gather/restore/scope wired.
- Two bootstrap passes. Recorder70ns export/parser13nsdelta/reset/missing tests pass. Added actual installed-header threaded hook regression using existing completion fixture with WakeupWithNotification; ASan/UBSan passes. First suite25815 failed because this turn edited check-repository.sh while its bash reader was active (truncated command ore-order.py); inspected intact file/bash-n, stopped editing, full rerun67594 exit0 generated/check-wakeup-r302-repeat.log. Do not modify executing shell scripts.
- Normal runner e86f2e09d27f69a41d1f10c2513cde14898557bc379fa787be013b5f0122b689 unchanged; no game/frontend. Started isolated four-job existing-cache build, output generated/macos/wakeup-r302/GalaxyPad.app, log generated/build-wakeup-r302.log. Next poll same handle, audit, then bounded real-save VI capture without sample overhead. No runtime result/causation/promotion/mobile acceptance yet.

## 2026-09-07 — R301 notification-only core hook

- Previous turn was progress: exact instruction mapping and state contract. Added pinned Dolphin0021-wakeup-notification-timing.patch SHA a5478ac7b988642fe57a7f38d7ab242dacdfe6f66a9fe21806b589faf42c8bdd. BlockingLoop default Wakeup delegates WakeupWithNotification with original Event::Set. Existing state atomics/guards unchanged. RunGpu supplies observer only invoked when notification needed; Core::IsCPUThread gates non-atomic metric, other threads call original Set. No per-burst clocks; Event lock/notify untouched. Separate default-disabled PerformanceMetrics wakeup member.
- Bootstrap peel before gather/core and restore last; scope/hash pinned. Bootstrap6785 exit0 and repeat pass. Installed-source state test passes. Compiled tests/completion-observer.cpp against actual installed headers with ASan/UBSan; threaded completion/wait/stop regression passes. Combined17649 exit0/full suite passes generated/check-wakeup-r301.log. Newly added wiring test passes separately and is now in suite; it verifies CPU guard/default notification/unchanged Event/pin order. No direct vendor edits.
- Incomplete: wakeup timer is not yet configured/read by runtime or VI CSV. Next ModernGekko runtime patch mirrors separate cumulative gather-field plumbing, plus parser missing/reset/export checks. Build only isolated app after verification. No runtime/build/mobile/app promotion this step; normal app unchanged/full goal open.

## 2026-09-07 — R300 wakeup instruction mapping and observer contract

- Previous turn was progress: stack capture identified RunGpu mutex wait. Exact runner symbol0x10054bb84 +0x5c =0x10054bbe0, matching sampled runtime0x104a0bbe0 after0x44c0000 slide. Preceding branch at0x10054bbdc targets0x100cf6f40; Mach-O indirect table resolves std::mutex::lock. Following stubs resolve unlock and condition_variable::notify_one. Source sequence matches sleeping-state Wakeup -> Event::Set flag transition/lock/unlock/notify. Evidence runtime/wait-sample-r299/{wakeup.asm,wakeup-stubs.txt}. Still no timestamp proof that this mutex caused a specific hitch.
- Added tests/test-wakeup-notification.py extracting actual Wakeup body and substituting only notification call with an observer callback. Eight state/enable combinations plus repeat-pending suppression pass ASan/UBSan,60065 exit0. Disabled zero clocks; enabled clocks only sleeping-state notification. Added to suite; full suite not rerun this step. Fake state/event test is not real concurrency proof.
- Next canonical Wakeup notification hook retaining original default Event::Set and exact atomics/guards; in RunGpu, measure only notification slow path with CPU-owned VI-enabled timer. Avoid per-burst clocks and non-CPU counter writers; retain Event's lost-notification handshake. No vendor edits/build/app promotion/runtime/mobile in this step.

## 2026-09-07 — R299 remaining-request audit and bounded stack sample

- COMPLETED input98791/sample61329/runtime62202 exit0. End screenshot visibly shows Mario on ledge. Closed once and verified via terminal (no post-close app query/relaunch); no frontend remains. Save5040...64a6 unchanged/native4193595925/fallback0/smc_failed0. Whole263s12underruns/16backlogs not audio acceptance.
- VI7106/120s=59.216667Hz,7105valid,CPUmean15.159666ms,p9917.533042. Largest249.867ms at12.671753s has15.051458CPU/.576708EFB/.084871idle/.00075throttle/0DVD/0gather; next128.235ms at26.858116s. Sampling can perturb timing; not a baseline regression or exact scheduler diagnosis.
- CPU-only stacks.txt lines477..2175:10679root samples,418 float-future move subtree (EFB-compatible),214 BlockingLoop::Wait entry +2 other offset,4 RunGpu+92 -> mutex lock -> __psynch_mutexwait. Do not sum nested frames or convert counts to exact milliseconds. No timestamp alignment. RunGpu wakeup mutex is newly observed outside gather/idle elapsed boundaries; next verify instruction/source mapping and add CPU-only wakeup timing, not speculative query counter or Event handshake removal. Event::Set deliberately locks to avoid lost notification; preserve it. Artifacts generated/runtime/wait-sample-r299.

- Live capture: runner62202/PID28629; load82755 exit0 and Observatory screenshot verified. Found leftover frontend28311 from prior close observation (same diagnostic path, start05:27:54); terminated that exact process before measurement, no second game. Current input98791 and sample61329 (sample28629,120s,10ms) live. Window362695208716666..362815208716666. Sample log confirms running. No build/top/screenshot during window; next poll SAME handles, clean close then summarize VI and CPU-only stacks.

- Previous turn was progress: reproduced75.7ms hitch with zero gather/DVD counters. Source audit confirms AsyncRequests::PushBlockingEvent future.get at header48, query guarded by ShouldEmulate and bounding-box path still blocking. QueueEvent also allocates SPSC nodes; nonblocking submission is not allocation-free. No causation claim. CoreTiming's CPU SleepUntil is covered by throttle metric; its other sleep branch belongs to GPU/non-CPU callers.
- Existing R237 sample has only77 CPU samples and no per-stack timestamps; cannot attribute R298. Choose a changed-variable bounded /usr/bin/sample capture (120s,10ms interval) on the existing audited runner rather than another counter/rebuild. Align VI120s and active inputs; examine CPU-thread stacks only, avoiding whole-process idle-thread totals. Aggregate sample cannot prove exact hitch correlation or scheduler run-queue latency; sampler overhead is a caveat, not baseline acceptance. No heavy SystemTrace.
- Prepared wait-sample-r299 from original one-star fixture/config, save5040...64a6 verified, no preexisting runner or booted Simulator. Runtime62202 started with VI-only env on existing gather-wait-r297 app. Next wait for module/title, load real save, then start bounded sample/control window. No normal promotion/mobile work.

## 2026-09-07 — R298 gather diagnostic build/audit and real-save capture

- COMPLETED: input26389/top98848 exit0; end screenshot shows Mario moved onto Observatory ledge. Close UI observation timed out but runtime29572 exited0, no runner remains and CSV exported. All handles terminal; no restart needed.
- Window362200639600958..362320639600958:7186VI=59.883333Hz,7185valid/noreset. CPUmean15.4097995ms,p9917.86625,355over16.667ms. At9.093723s:75.698208wall/17.926334CPU/0.395417EFB/0.386586idle/0.001374throttle/0DVD/0gather ms. Next37.820583wall/19.1115CPU/3.857376EFB/3.850411idle/0DVD/0gather. Gather total0ms, DVD28.789291ms. No disjoint blocked-state inference from overlapping counters.
- Save5040...64a6 unchanged; native3917997288/fallback0/smc_failed0. Whole248s6underruns/10backlogs are not fixed-window audio acceptance. Evidence generated/runtime/gather-wait-r298/{vi.csv,summary.json,host-top.txt,runtime.log}. Stop gather-only repetitions; next other async waits or bounded scheduling attribution. Normal app unchanged/full goal open.

- Load23169 exit0; screenshot shows real one-star Observatory. Started 16-cycle input26389 and 12x10s host sampler98848. VI window start362200639600958, target end362320639600958. Runtime29572 still live; no capture result until clean close/post-join export. Poll same handles; avoid screenshot/build during fixed window.

- Previous turn was progress plus a verified build wait. Polled same19571 through199steps/exit0. Audit42088 exit0 (generated/audit-gather-r298.log). Runner SHA f4873728609cf9809db29d4ffc7185d55b929884cbcd3474acff54ec7d9418a1; signed module unchanged5c2101...e91d.
- Prepared generated/runtime/gather-wait-r298 from accepted-r238 Config/config.ini and original midblock-r145 Wii; GameData5040...64a6 verified. No savestate. No preexisting runner/booted Simulator. Runtime29572/PID27729 launched with VI-only environment/accepted module. Title visibly verified after module load; load sequence23169 started. Next verify Observatory then bounded120s active capture. No normal promotion/mobile work.

## 2026-09-07 — R297 gather wait VI recording and isolated build

- Build handle19571 confirmed live after launch; continue polling this handle, do not restart on observation timeout.

- Previous turn made progress installing the disabled boundary. Added ModernGekko0023-gather-wait-recorder.patch SHA c8e58096449a7cb60843d21d38946d486ca484c35b63f060f4cdd189b93ca3e0: configure separate CPU counter using VI Enabled before startup; append cumulative gather ns to VI samples/post-join CSV. Canonical header copies match installed source. Bootstrap peel/restore/pin/scope updated.
- Initial generated diff had no context lines and failed read-only apply-check; corrected to contextual hunks before bootstrap. No manual vendor editing. Two bootstrap passes and full suite25660 exit0 (generated/check-gather-r297.log). Nonzero CSV60ns, old-schema null, 11ns delta/reset-null and installed wiring checks pass.
- Normal runner SHA e86f2e09d27f69a41d1f10c2513cde14898557bc379fa787be013b5f0122b689 unchanged; no game or booted Simulator. Started isolated four-job build using existing cache, output generated/macos/gather-wait-r297/GalaxyPad.app and generated/build-gather-r297.log. Poll same handle before any build/bootstrap. No runtime attribution, performance promotion or mobile acceptance yet.

## 2026-09-07 — R296 canonical gather wait boundary installed

- Previous step was progress: sanitized source-derived guard contract added. Added Dolphin0020-gather-wait-timing.patch, SHA e17a99eb10881d6a68713806b4cdf1f1b38a49d483d65026b1bede8d4c6eadc9. Separate default-disabled PerformanceMetrics member wraps only GatherPipeBursted's existing FlushGpu, retaining guard/order; no global FlushGpu timing or synchronization changes.
- Bootstrap pins/scope/recovery wired, gather peeled before DVD and restored afterward. Bootstrap56342 exit0 and repeat52637 exit0; installed-source 128-case ASan/UBSan boundary test and new wiring test pass. DVD wiring test passes. No direct vendor edit or build/app change.
- Clarification to R295 wording: upstream IsOnThread here returns IsDualCoreMode, not an actual thread-identity check. Test toggles that predicate; CPU ownership must follow the caller path, not the predicate name.
- Work incomplete: runtime does not configure/read this counter yet. Next new runtime patch extends VI sample/export/parser with gather elapsed ns and configures from VI Enabled before CPU startup. Update test parity/schema expectations, bootstrap peel ordering and pins before building the isolated app. Full suite not run this step; no game/Simulator launched.

## 2026-09-07 — R295 gather wait observer contract

- Previous step was progress: source audit identified the exact non-idle caller and its synchronization guards. Current status still keeps performance/mobile acceptance open.
- Added tests/test-gather-wait-boundary.py extracting the actual GatherPipeBursted prefix. Wraps only FlushGpu with the existing timer and compares against the original branch across all 128 combinations of link mode, CPU-thread predicate, deterministic mode, FIFO end/base match, pending distance and timer enablement.
- ASan/UBSan passes expected status/flush/run ordering, unchanged guards and disabled zero-clock behavior (session66816 exit0). Linked path is represented by a continuation marker, not a test of its implementation. Fake FIFO testing does not establish concurrency correctness or hitch causation. Added test to repository suite; full suite not rerun this step.
- Next canonical separate CPU-owned counter/VI export wiring, preserving synchronization; no vendor edit, module change, runtime launch or mobile work in this step.

## 2026-09-07 — R294 remaining wait boundary audit

- R293 repository suite log ends `Repository safety checks passed`; session55889 is no longer live. Rechecked 71GiB available, no booted Simulator and no GalaxyPadRunner. No additional deletion needed.
- Read actual pinned AsyncRequests/Fifo/CommandProcessor/VideoBackendBase source. Blocking future completion is AsyncRequests.h:48, with non-EFB callers for performance queries and bounding boxes; state serialization is another caller but R293 did not load a savestate. Non-idle GatherPipeBursted calls FlushGpu at CommandProcessor.cpp:356. Fifo.cpp:556 has a separate sync-distance event wait, gated by sync-GPU configuration. Absence of an explicit profile override alone does not prove its effective value.
- Next smallest instrumentation target is the CPU-side non-idle GatherPipeBursted FlushGpu call, using a separate opt-in cumulative timer without changing synchronization. Avoid globally timing FlushGpu across shutdown/other threads with a single non-atomic counter. Async future waits remain a subsequent candidate, not a diagnosed cause. No source/runner/module changes or new runtime launch this step.

## 2026-09-07 — R293 planned final longer DVD-wait capture

- Previous turn progress: diagnostic app built/audited, first live DVD counter capture passed but did not reproduce large hitch. Rechecked status/no runner/Simulator; same diagnostic runnerab221c...9c480 verified, no rebuild. Prepared dvd-wait-r293 from same original one-star fixture/config, save5040...64a6 verified, pipe configured.
- Started99963 with same isolated app/VI-only environment and accepted module. Planned one120s/16cycle active route, host sampling; then clean close/aligned DVD/VI analysis. This is the bounded final repeat for this hypothesis unless new evidence justifies a changed next step. No normal-app promotion/mobile work/fullgoal acceptance.
- PID21479 focused after module load, title/real-save Observatory verified. Load7870,16cycleinput12538,12sampletop3226 all exit0. Compiled steady window360981276399250..361101276399250; end screenshot visibly different Mario position. Clean close99963 exit0; no runner remains.
-7189VI=59.908333Hz,7188valid/noreset, CPUmean15.339435ms. Reproduced88.335125ms hitch at82.174217s,23.071750CPU/0.334EFB/0.308541idle/0.001582throttle/0DVD. DVDtotal31.995288ms,p990.09825. This falsifies DVD-result wait as cause of THIS captured hitch. Stop DVD-only repeats; next non-idle FIFO/async wait or scheduling attribution, no synchronization/timing policy changes. Save5040...64a6 unchanged/native4105704564/fallback0/smc_failed0; whole245s3underruns/8backlogs not audio acceptance. Evidence runtime/dvd-wait-r293. Suite55889 active generated/check-dvd-long-r293.log.

## 2026-09-07 — R292 diagnostic DVD-wait package and gameplay capture

- Final suite21808 exit0; repository safety checks pass. All handles terminal, no live runtime/build/Simulator, no staging/publication.

- Previous turn progress: canonical patches/repeated bootstrap/suite passed, build5551 live. Polled SAME5551 through199steps/exit0. Isolated app audit52476 exit0 generated/audit-dvd-r292.log. Diagnostic runner SHAab221c1a1787e20aa1098f89e9abeead67851207c60d2a6eb6e97af01339c480; bundled module unchanged5c21...e91d. Normal runner remains e86f...22b689.
- Prepared dvd-wait-r292 disposable profile from accepted-r238Config/config.ini and original midblock-r145Wii, pipe configured; save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6. No game/Simulator before launch. Runtime98894 started isolated wrapper, VI-only env, module/completion/phase overrides unset; normal app not replaced.
- Hypothesis: a repeated gameplay hitch will show whether measured DVD result-queue waiting accounts for significant time. No claimed culprit or performance fix. Next verify existing runtime window, load real save, fixed60s active route/host sampling, clean close and aligned DVD/CPU/VI analysis. No mobile work/fullgoal acceptance.
- RuntimePID20674 confirmed; first PID window query was before UI registration, not process failure. Same runtime then focused/attached, title and real-save Observatory verified. Load70053 exit0;8cycleinput77951/top8883 exit0. Compiled anchor360596065858166..360656065858166. End screenshot Mario hanging ledge, not identicalR289route end. Clean close98894 exit0; no game/frontend/Simulator.
- Summary3588VI=59.8Hz,CPUmean15.353678/p9918.4255ms,3587valid/noreset. DVDtotal14.477747ms,p950.029208,p990.090457. Largest25.556834msVI has22.237125CPU/1.203542EFB/0.366717idle/0.000209DVD. Earlier55–59ms hitch absent; counter working but cannot exclude disc waits in that prior incident. Save5040...64a6 unchanged/native3302073185/fallback0/smc_failed0. Whole209s34underruns/7backlogdrops not audio acceptance. Last top runner142.1/WindowServer45.7/kernel42.5/logi31.6%. Evidence runtime/dvd-wait-r292/{runtime.log,vi.csv,summary.json,window-start.txt,host-top.txt}.
- Next one bounded120s same-diagnostic capture to seek larger incident, then park unchanged no-hitch capture loops. No rebuild/normal promotion/mobile work. Suite21808 live generated/check-dvd-runtime-r292.log at entry.

## 2026-09-07 — R291 canonical DVD wait recording and isolated build

- Previous turn progress: wait-coverage audit and queue-wrapper regression. Added separate PerformanceMetrics DVD IdleWaitTiming member, configured by existing VI recorder opt-in before CPU start; wraps exactly existing FinishRead WaitForData with no observer, preserves Pop/map behavior. Appended dvd_wait_elapsed_ns to bounded sample/export; summary supports missing old-schema DVD counters as null plus reset handling. Recorder export test uses nonzero50ns; queue source test normalizes installed wrapper and tests unchanged contract. Added pinned wiring test.
- Canonical patches ModernGekko/0022-dvd-wait-recorder.patch SHA9ce037d4df807985ad626afa2fc6b967b07cc079998b3867c76d2b118c33bcd6 and ModernGekko-dolphin/0019-dvd-wait-timing.patch SHA22f1385f2d2ca5e667354b08daa8e33e3bb3e5ca85a6346fac617eec3a7caeba. Bootstrap pin/peel/restore/scope integration added. Initial72600 failed scope audit because generated unified patches lacked diff--git headers for newly touched DVD file; corrected headers/hash pins, no direct vendor edit. Fixed/repeat bootstrap69027 both pass, then full suite passes (generated/bootstrap-dvd-{fixed,repeat}-r291.log and generated/check-dvd-r291.log).
- Started5551 `GALAXYPAD_MACOS_OUTPUT=$PWD/generated/macos/dvd-wait-r291/GalaxyPad.app GALAXYPAD_JOBS=4 ./scripts/build-macos-app.sh` log generated/build-dvd-r291.log. Uses accepted module and existing build cache; last23/199, LIVE/no compile errors. Next poll SAME handle (no restart/bootstrap during build), audit isolated package and prepare new real-save gameplay run with DVD/VI recording. Normal app not replaced;71GiB free/no runner/Simulator. No runtime DVD result, performance fix, mobile work, staging/publication or fullgoal acceptance yet.

## 2026-09-07 — R290 CPU wait coverage and DVD-boundary regression

- Final suite18252 exit0, repository safety checks pass. No live process/build/Simulator, staging or publication; runtime smoke not rerun because product unchanged.

- Prior turn progress: fresh accepted gameplay59.9Hz with55–59ms hitches, clean-save/exit evidence. Rechecked status/no runner, read VI-WAIT-AUDIT, Fifo wait routes, AsyncRequests and VideoBackendBase. Current presentation submission nonblocking; query result path separately blocking. DVDThread::FinishRead CPU result-queue wait remains outside measured EFB/idle/throttle. No attribution claim.
- Read actual phase logger: mutex and line-buffered fprintf per event. Reject that path for lightweight wait timing. Reuse existing IdleWaitTiming cumulative counter (CPU-thread-only, opt-in configure before start), eventually sampled into VI record. Correct PerformanceMetrics path is VideoCommon/PerformanceMetrics.h, not Core/PerfMetrics.h.
- Added tests/test-dvd-wait-boundary.py extracting exact current result selection/map/queue loop and replacing only WaitForData with measured invocation. ASanUBSan passes cached-result/no-wait, out-of-order3/1/2 results, equal pop/wait counts, disabled0clockcalls, enabled6clockcalls/30ns sum. Fake queue does not prove real scheduling; no product source changed. Added to suite,18252 active generated/check-dvd-boundary-r290.log.
- Next canonical runtime wiring for DVD cumulative VI field, backward-compatible CSV parser/tests and isolated diagnostic runner. Do not change synchronization, timeouts, disc service policy or normal app; no mobile work or fullgoal acceptance.

## 2026-09-07 — R289 current accepted gameplay CPU-budget measurement

- Final check: repository suite18346 exit0; no running game/frontend/Simulator or build. All input/runtime/top handles terminal. No staging/publication, normal artifact unchanged.

- Previous turn progress: corrected offline charge model and rejected snapshot on measured evidence. Pivot from movie-only experiments to fresh post-cleanup real-save gameplay budget measurement, not a repeat title/Observatory acceptance claim. Hypothesis: current sustained gameplay CPU budget differs from the THP movie deficit; measure complete60s active movement/Jump/Spin plus host load using accepted app only.
- Prepared disposable gameplay-r289 Config/config.ini from accepted-r238 and original midblock-r145 Wii (no savestates); native1x/Metal/Cubeb/CPUThreadTrue/Core6. Save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6, runner e86f...22b689/module5c21...e91d verified. Runtime41750 PID15034, VI-only recorder, module override/completion/phase envs unset. No Simulator or build.
- CUA first attach timed out, second selected an unintended frontend15070. Verified existing runner window by System Events PID query (initial AppleScript selector syntax corrected); terminated only unintended frontend, focused verified runtime15034 and CUA then attached correctly. No second game launched. Ready title screenshot verified. Started real-menu load10480; next poll same handle, verify Observatory, then fixed-window active controls/host-top. No product change, mobile hold/fullPRD/performance/audio open.
-10480 exit0; visible one-star Observatory verified. Input29267 runs8 control-smoke cycles; top57646 samples6times/10s, both exit0. Compiled steady anchor359517108479958;60s end359577108479958. Postwindow screenshot shows changed Mario position/camera in normal Observatory. Native close41750 exit0; no frontend/runner remains.
- Summary3594VI=59.9Hz, CPUmean15.075371ms/p9917.498791,188/3593CPUintervals exceed16.667ms, zero invalid/reset. Two hitches58.928667/54.534458ms at31.786/31.841s, CPU24.665792/22.915458, EFB0.183624/4.719875,idle4.340656/0.295166. Save5040...64a6 unchanged;native3803902777/fallback0/smc_failed0. Whole258s audio2underruns/6backlogdrops not audio acceptance. Final host sample runner137.1/WindowServer46/kernel43.6/logi28.5%. Evidence runtime/gameplay-r289/{runtime.log,vi.csv,summary.json,host-top.txt,window-start.txt}. This supports separate sustained movie CPU deficit and intermittent gameplay stall work; no fullG6/soak claim. Next hook-coverage/wait attribution, not repeated unchanged scene test. Suite18346 running generated/check-gameplay-r289.log.

## 2026-09-07 — R288 align offline dispatch charge reset with host

- Final verification: suite91588 exit0, repository safety checks pass. All handles terminal; app unchanged, no runtime/Simulator/build, staging or publication.

- Prior turn progress: long-code coverage/expected outputs passed, snapshot near-tie parked. Rechecked status/no runner and exact StaticRecompCore_Run.cpp:155-158 resets m_guest.downcount after every dispatch; module header loop budget256. Earlier exhausted fixture accumulates charges across re-entry;1M budget is the opposite stress extreme. Neither matches the host accumulator-reset policy. R275 already rules against repeating host dispatch-condition reordering as a major performance fix.
- Added explicit --runtime-charge-reset mode to decoder fixtures. Reset before each dispatch, accumulate exact native charge separately, restore aggregate at final return for CPU-state oracle comparison. Does not model Dolphin timers, interrupt delivery, minimum effective host charge or timebase updates; these routines are measured as isolated completed decoders, not a runtime substitute. Legacy stress modes retained and incompatible positive-budget combination rejected.
- New tests/test-fixture-charge-reset.py extracts/compiles actual wrapper; UBSan regression proves each entry sees zero, total charge remains-600 across two-300 calls, and legacy mode retains accumulating behavior. Added to repository suite.
- Started89805 sanitized long-code/coverage then conditional noninstrumented ABBA timing, generated/huffman-reset-{check,bench}-r288.log. Sanitized32cases and1646cached reads passed; timing live at last check. Next poll SAME handle and classify result before any module decision. No product/vendor/app edits/mobile work; fullPRD/performance/audio remain open.
-89805 exit0:128timing rows ratio1.003645; length6/8/12/16 ratios1.004736/1.002325/1.007242/1.000928. No benefit under host-reset mode; snapshot remains parked. Do not repeat unchanged. Next materially different generated-code work with explicit alias/callback contracts. Repository suite91588 live generated/check-charge-reset-r288.log at this entry; no runtime/Simulator or module build.

## 2026-09-07 — R287 long-code Huffman fixture coverage

- Prior turn progress: callback-safe loop snapshot oracle passed; instrumentation proved old timing fixtures hit zero changed-loop reads. Rechecked status/no game/Simulator. Read exact RMGE01 generated loads and Petari THPHuffmanTab/table-generation source as semantic reference, not region-address replacement.
- Added source-only tests/fixtures/huffman-long-throughput.c. Synthetic singleton canonical zero codes at lengths6/8/12/16, symbols0(EOB/DCzero) or1(dense+1), start counts1/27/29/32. Exact table layout:quick symbols0,lengths32,Vij64,maxCode68,valPtr140. Incomplete prefix table with only code0 is valid. Bitstream construction is independent of generated execution; expected DC7+dense and remaining63coefficients=dense checked in addition to fullCPU/6MBmemory parity.
- Extended benchmark driver --long-codes; non-timed --loop-coverage asserts changed-loop reads >0. Started sanitized coverage67440, generated/huffman-long-coverage-r287.log. Next poll SAME handle then fix any causal fixture failure or run non-instrumented timing if coverage/outputs pass. No module/product/vendor changes or mobile work; originalPRD/G6/performance/audio remain open.
- Initial67440 failed harness256-call cap (no product assertion). Raised bounded cap4096 and added per-case logging;48235 exit0, all32 expected-coefficient/fullstate/memory cases pass,1646 changed-loop reads. Long exhausted-cycle-budget timing20196 finished0. Identified harness-induced per-iteration yielding: add separate --long-run-budget (1M starting guest cycles) to measure uninterrupted decoder work; do not mix the two budget regimes. Started41631 sanitized positive-budget coverage then conditional noninstrumented benchmark: generated/huffman-long-budget-{check,bench}-r287.log. Poll SAME handle, no restart. Suite25260 passed before this final test-mode addition; product unchanged.
-41631 exit0: positive-budget32cases/1646reads pass;128timing samples aggregate ratio1.00312855, perlength6:1.0098013/8:0.9988424/12:1.0082190/16:0.9984212. Exhausted-budget20196 ratio1.00202893. Neither shows convincing gain: park mapping snapshot without module build. New fixtures retained for materially different decoder work. Final suite81469 exit0 (generated/check-long-huffman-final-r287.log). All handles terminal; no game/Simulator/build, staging/publication or mobile work.

## 2026-09-07 — R286 callback-aware Huffman loop mapping snapshot

- Previous goal turn made progress by rejecting exact add/sub inlining on whole-kernel evidence. Rechecked current status/no runner. Audited exact decoder prefix:15 static search loops,15 mem_read32 calls, no other memory calls; only gpr/pc/downcount/cr/xer writes. Mapping cannot change on ordinary reads/register updates, but external callbacks can mutate it.
- Added isolated scripts/cache-huffman-loop-reads.py, exact source SHA e6aa...6dcc. Snapshots RAM/EXRAM pointers/sizes at loop entry, preserves original EXRAM-first mapping/unsigned boundary arithmetic, and refreshes all four fields immediately after every slow mem_read32. No caching across callbacks without refresh, no memory writes/journal changes, no outer entry/guest cycle/PC changes. Test driver --cached-reads and --remap-reads; no vendor/module selection changes.
- Baseline full-chunk ASan/UBSan76582 exit0:65536 cases,0capped,505936journals,634reservation clears. Stronger callback-remap version11753 sanitized and83344 O2ThinLTO both exit0:65536cases,0capped,483932journals,634clears,205570mapping changes. Callback modifies RAM pointer/size, EXRAM pointer/size and next address; full state/memory/callback comparisons remain equal. Logs generated/cached-loop-{reads,remap,remap-o2}-r286.log.
- Whole decoder existing32 synthetic tables benchmark13862 exit0, accepted PGO flags/no new mismatch warnings, aggregate candidate/control0.9947955; pattern ratios1.00165/0.99110/1.00787/0.98684. Near tie is NOT a speed claim. Existing one-bit tables may bypass changed loops; added non-timed --loop-coverage counter run9487 (generated/cached-loop-coverage-r286.log). Next poll SAME handle before interpreting performance; if coverage absent, extend synthetic valid long-code fixtures rather than building a module or repeating unrepresentative timing. Suite77540 running generated/check-cached-loop-r286.log. Normal app unchanged/mobile hold/fullPRD/G6/performance/audio open.
- Coverage9487 exit0: cached_loop_reads=0, all32 state/memory cases pass. This invalidates using that benchmark to assess loop-cache speed (not a correctness failure). Next valid longer-code tables with asserted nonzero loop coverage. Repository suite77540 exit0; all handles terminal, no runtime/Simulator/build active. No product smoke because app unchanged; no staging/publication.

## 2026-09-07 — R285 exact scalar add/sub inlining experiment

- Previous turn made progress by establishing sustained CPU-thread saturation from existing complete windows. Rechecked status/processes (no game/Simulator), then inspected exact GXRuntime cpu_interpreter_float.c, not the separate DolRecomp implementation. Paired add/sub call ni_add/ni_sub twice; these helpers preserve NaN priority, exception flags and FI/FR semantics.
- Hypothesis: forcing only these two exact bodies inline within their float translation unit removes call/temporary overhead without a finite-only arithmetic fast path or global chunk flattening. Added isolated --inline-add-sub mode to tests/probe-thp-oracle.py; it adds always_inline attributes only, verifies removing attributes restores byte-identical source, uses the same extracted accepted kernel for both sides, and preserves existing source hashes. No vendor/product/module change.
- Whole-kernel oracle90500 exit0: all four O1ASanUBSan/O2/ThinLTO variants agree on1440 cases,624 yields,480FPfaults,480illegal,192interruptions,96callerreturns,18816callbacks,34688journals,1440reservations, digest882be09b1f38e9ad. Evidence generated/thp-inline-add-sub-r285.log. This is existing oracle coverage, not full host-FENV/gameplay acceptance.
- Started paired whole-kernel thread-CPU ABBA benchmark11278: generated/thp-inline-add-sub-bench-r285.log. Next poll SAME handle; inspect runtime ratio/code size before any additional build. Candidate unselected, normal app unchanged, mobile hold/fullPRD/performance/audio still open.
- Benchmark11278 finished0. Control totals24705.057/23759.817ns; candidate24784.256/24750.473ns, ABBA means24232.437/24767.3645 (~2.2% slower). Text229376→212992bytes. Short-batch variation prevents treating slowdown as precise, but there is no benefit supporting a module build. Park helper-only inlining; next structural decoder work, not repeating finite/widening/inlining helper variants. Product/vendor untouched.
- Repository suite98579 exit0 (generated/check-inline-add-sub-r285.log). No runtime smoke because no product change; offline full-kernel oracle and timing complete. No live process/build/Simulator, staging or publication.

## 2026-09-07 — R284 distinguish sustained CPU budget from isolated hitches

- Previous goal turn: progress (authorized cleanup, verified erasure/reclaimed71GiB free, clean-close candidate evidence, passing suite). Rechecked status/journal, dirty worktree, empty runner/booted Simulator inventory; no GalaxyPad memory hit. No builds or external mutations.
- Hypothesis: worst-20 summaries obscure whether CPU execution itself consumes the frame budget. Extended scripts/summarize-vi-timing.py with full-interval nearest-rank distributions/totals/strict60Hz-budget counts, preserving null validity/reset semantics. Focused tests pass missing/empty/single/boundary/quantile and complete-window cases. Read recorder CLOCK_THREAD_CPUTIME_ID and vi_end_field_event wiring to establish clock provenance.
- Reprocessed five existing dcbz windows into budget-r284.json beside original summaries. R282 accepted:59.246212375CPU/59.994651125wall seconds, mean17.65381775msCPU, p9518.657542ms,2911/3356=86.7402% exceed16.667ms. R283 accepted mean17.611376ms supports sustained CPU pressure. Contended R281 meanCPU32.817492/wall54.580026ms shows additional slowdown is not purely off-CPU. Counters include all CPU-thread host work, not only guest arithmetic; differing scene progression/frequency/core placement prevent stronger attribution.
- This changes next-action priority: isolated90ms hitch removal cannot cure the persistent56Hz movie plateau. Target significant generated THP/paired-single execution work using exact GXRuntime helpers and complete-kernel oracle, not the unrelated DolRecomp cpu implementation encountered during source search. R214 sampled profile is supporting older evidence, not a fresh capture. DCBZ candidate remains parked/default-off; mobile hold/full PRD scope unchanged.
- Full repository suite passed (`generated/check-budget-r284.log`,49703 exit0). No runtime smoke rerun: product artifacts unchanged, this step only reanalyzed existing recordings. No process left running; no staging/publication.

## 2026-09-07 — R283 authorized Simulator cleanup and reverse-order comparison

- Chris explicitly authorized deleting other projects' Simulator data, then continuing performance work. Read-only inventory found 28 shutdown devices across iOS 18.5 and 26.5. `xcrun simctl erase all` completed successfully; device storage fell from 59 GiB to 492 MiB. Filesystem available space rose from approximately 14 GiB to 20 GiB, not 59 GiB of physically reclaimed space. Installed runtimes and device definitions retained. Simulator-installed apps and saves permanently erased; repository inputs, local NAND/saves, source and active build products untouched. No Simulator booted.
- Verified all 37 paths from prior `generated/cleanup-r281-targets.txt` are absent. Prior obsolete-product cleanup remains complete.
- Existing control runtime closed through its UI before returning to measurement. Its pre-cleanup fixed window 356709246758458..356769246758458 contains 3364 VI / 60 seconds = 56.066667 Hz, no invalid CPU/reset intervals; worst interval 89.013041 ms wall, 22.383333 ms CPU, zero EFB, 0.285833 ms idle wait. Save hash remains a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6. Native4333453946/fallback0/smc_failed0 on clean shutdown. Full-run counters include later cleanup activity and are not an uncontended benchmark.
- Next: finish the prepared reverse-order candidate using the same fixture, module override, VI recorder and host sampling. Candidate remains default-off and normal app unchanged. Mobile development remains on hold for performance/stability.
- Completed candidate26258 and input85849/top81392, all exit0. Exact candidate module load confirmed; visible fixture restore and advancing film verified. Window357149007820833..357209007820833:3384VI=56.4Hz, zero invalid/reset, worst87.184959ms wall/25.607833CPU/0EFB/0.275961idle. Native829614096/fallback0/smc_failed0; save remainsa574...afa6. However CoreSimulator reclamation still consumes84.9%CPU in final host sample. This is not clean reverse-order confirmation and must not be aggregated as promotion evidence. Park candidate/default-off; avoid unchanged repeated A/B tests. Control92595/top99516 also verified terminal0; no game/Simulator remains.
- Asynchronous disk reclamation continued after simctl returned: final observed free71GiB versus14GiB before, roughly57GiB additional physical space reclaimed. Initial20GiB reading was intermediate. Next use existing decoder and VI profiles to distinguish substantial CPU cost from non-CPU hitch time; full PRD and mobile hold unchanged.
- Post-cleanup repository suite passed (`generated/check-cleanup-r283.log`,96398 exit0). Final process/Simulator check empty; available space remains71GiB. No product source changes, build, promotion, staging or publication.

## 2026-09-07 — R282 first candidate/control runtime pair; small unaccepted edge

- Previous turn progress: cleanup freed~12GiB and preserved current artifacts; suite passed. No permission received to erase other projects' Simulator devices; those remain untouched. Host snapshot now JumpConnect~8% versus R281315%; no game/build active. Read current launch environment/source discovery; use same accepted runner with explicit unsigned module overrides on both sides, no app replacement.
- Candidate profile dcbz-candidate-r281 (preparedR281) launched with candidate1b78e759...2cc2c0 and VI-only recorder, other diagnostics unset. Runtime82281, trigger17920 FINISHED0. CUA visibly restored correct pre-attack slot3 and movie progressed. Compiled steady anchor356150923986791,60s end356210923986791. Added low-frequency host sampling: top -l6 -s10 -o cpu -n8 -stats pid,command,cpu,threads,state after trigger (55941 FINISHED0); same sampling on control. Close after complete window, native1014524093/blockfallback0/smcfailed0, savea574...afa6 unchanged. Summary3380VI=56.333333Hz, zero invalid/reset; worst93.11625ms wall/21.962083CPU/0EFB/0.25416idle. Whole152.9s audio62underruns/3backlog drops is not window/audio acceptance.
- Fresh dcbz-control-r282 copied sameR85Config/Wii/config.ini/slot3; same native1x/Metal/Cubeb/CPUThread/Core6 and pipe setup. Runtime13565, trigger93131 FINISHED0; same accepted unsigned module11804473...fc631 and runner, same diagnostics/top (81463 FINISHED0). Visible restored scene and advancing movie; clean close. Anchor356375019157083..356435019157083:3357VI=55.95Hz, zero invalid/reset; worst89.34325ms wall/22.150834CPU/0EFB/0.239573idle. Native927846912/blockfallback0/smcfailed0, savea574...afa6 unchanged.
- Logs/screens observed in tool conversation; persistent evidence under generated/runtime/{dcbz-candidate-r281,dcbz-control-r282}/{runtime.log,vi.csv,summary.json,window-start.txt,host-top.txt}. Final shown top samples both runner114.8%, JumpConnect7.6/7.7%, WindowServer42.5/41.6%. Snapshots are not scheduling/thermal equivalence proof. No full-movie completion, gameplay-loop, audio or soak acceptance; both closed during film.
- Candidate edge23VI/3357=~0.685% is small, not sufficient to promote. R28118.33Hz baseline is not part of this comparison. Both quieter runs still contain long non-CPU intervals outside measured EFB/idle waits; do not declare host contention the sole cause. Next reverse-order control/candidate to complete BAAB under unchanged sampling, then accept/park based on evidence. No new builds, source edits to product, app promotion or mobile work. All runtime/input/top handles terminal; full PRD/G6/performance/audio open.

## 2026-09-07 — R281 candidate audited, slow control, user-directed cleanup

- Resumed live62691 link without restart; FINISHED0. Candidate SHA2561b78e7596d49f79dce045a876e5b48f966ebef6f467a38e745d4ebfcfd2cc2c0,114404648bytes. Explicit audit-module.sh passed RMGE01/arm64/macOS14/1322chunks/indexed1024/19SMC (`generated/audit-module-dcbz-r281.log`,77826 FINISHED0). Manifest differs from accepted only by dcbz_policy=rmge01-dcbz-loop-8-v1. Linked symbols include prepare57b17ec/store57b18f0. No promotion.
- Prepared dcbz-control-r281/candidate-r281 disposable profiles from pre-attack-r85, copied only slot3 (aea2b7a3...ab8), configured separate Wii pipes; Config diff empty, native1x/Metal/Cubeb/CPUThreadTrue/Core6/WaitForShaders. Both GameData hashes a5743199...afa6. Started same normal runner with explicit accepted unsigned module and VI-only recorder; runtime65408. Visible slot3 restore, trigger76245 FINISHED0, compiled steady anchor355479641258458. Movie visibly advanced but window24.1FPS; host snapshot JumpConnect315.1%CPU, runner72.9%, WindowServer50.9%; later CodexRenderer179.3%, runner81.1%. AC100%, pmset no recorded thermal warning (not proof no throttling). No unrelated process stopped.
- Chris requested clearing old Simulator/folder temp data, overriding measurement work. Closed test through refreshed UI;65408 FINISHED0 and no runner remains. Fixed60s ends355539641258458:1100VI=18.3333Hz; largest interval463.043ms wall/63.474ms CPU/0EFB/0.553ms idle wait. Heavy host contention is a lead, not proof of all lag; do not compare this to prior57Hz as a clean candidate A/B. Save remainsa574...afa6; normal signed module remains5c21...e91d. Candidate profile still unlaunched. Evidence generated/runtime/dcbz-control-r281/{runtime.log,vi.csv,summary.json,window-start.txt}.
- Cleanup inventory: generated55GiB (runtime25GiB, macos4.4GiB), CoreSimulator59GiB, DerivedData2.1GiB. Simulator devices are other named projects/generic devices, all shutdown; no GalaxyPad-named app directories found. No device erase or other project cleanup authorized by inference. Original inputs/extraction, reference repos, all Wii/NAND/savestates, current build cache, accepted/candidate module trees, normal app, latest rollback and diagnostic summaries preserved.
- Validated37 exact obsolete targets in generated/cleanup-r281-targets.txt:8 retired module trees,3 PGO compile trees,20 old app backups,6 obsolete diagnostic app copies. No current scripts/tests/config refer to retired module trees; archived decoder-PGO script can rebuild its deleted compile output. Non-force rm -r completed50396 FINISHED0; initial rm -rf rejected before executing. Permanently removed~12GiB (rebuildable, not Trash recoverable). Disk free rose~3GiB→15GiB; generated55→43GiB. No source/git data removed. The37-target local manifest records exact paths.
- Started repository safety/regression check after deletion (`generated/check-cleanup-r281.log`,41042 LIVE at entry) and Simulator metadata scan for GalaxyPad-owned containers. Next poll these exact handles and report cleanup; do not erase unrelated Simulator devices without explicit scope. Full goal remains active; mobile builds held.
- Completion:41042 FINISHED0, Repository safety checks passed. Simulator container-metadata scan20101 finished with no GalaxyPad matches (rg no-match exit1, no diagnostic errors); generated/galaxypad-simulator-containers-r281.txt empty. Other-project Simulator59GiB untouched. Final available~15GiB. No active cleanup/test/game/build.

## 2026-09-07 — R280 source parity audit; live final link

- Previous turn progress: canonical opt-in integration/tests/port build passed and isolated module build started. Resumed SAME live62691 handle, never restarted/bootstraped. Build progressed278→718→1052→1330/1332; compiler finished modified chunk1103 at1111/1332. Final linker observed PID7898 at~729%CPU, with live handle confirming continued execution. Unchanged log during ThinLTO is not a hang or failure.
- Added read-only `scripts/audit-dcbz-candidate.py`. Ran against generated/modules-dcbz-r279/RMGE01/2c680585a8f58e1cc9c5521b579057f12b124ff0ef409e470a57606c50a93c09-6d25f3ee43c19ac3;1322 chunk filenames match accepted,1321 byte-identical, only chunk1103 changed. Its full output equals the tested eight-site transformation, SHA256783117cac193bb2319e155174b73e3362f6ed722b9864cfa098e3c2a79a7be9b. Evidence generated/audit-dcbz-source-r280.json,34160 FINISHED0. This does not verify the final binary, headers, linking, gameplay or timing.
- Checked PGO warning context: candidate's sole mismatch is unchanged chunk1102 (33functions/1mismatch), also present in accepted generated/module-thp-r205.log. Modified1103 finishes without a new mismatch warning. Existing cpu_interpreter_integer unprofiled and nested-comment warnings remain unrelated. Do not claim the entire build has no warnings.
- Accepted manifest confirms indexed/1024, Appleclang21, O2/ThinLTO/strictFP and profilef39a3ced...460d matching requested candidate configuration. Candidate manifest is written at completion, so absence while linking is not an error. Space3.1GiB available late link. No game/Simulator/mobile build.
- Next: poll62691 until terminal; on success run audit-module.sh with explicit absolute candidate dylib, compare final manifest and binary identity, then prepare matched runtime measurement using the same accepted runner. A conditional audit command was queued only if the last wait completed; it did NOT run because handle stayed live. Do not infer an audit log exists. No app promotion or normal marker changes; full PRD/G6/performance/audio remain open.

## 2026-09-07 — R279 canonical opt-in integration; isolated module build started

- Previous turn progress: completed synthetic decoder timing supported a runtime candidate. Preserve macOS-first/G6/fullPRD scope. Added canonical `patches/experiments/dcbz-loop-policy.inc`, default-off except exact GALAXYPAD_DCBZ_LOOP=1, exact RMGE01 DOL/C/1024 identity. Policy rmge01-dcbz-loop-8-v1 includes the exact tested loop helper, hashes chunk1103 before writing, rejects altered site count/signature/reapplication. No runtime memory-helper changes.
- `python3 tests/test-dcbz-loop-policy.py` compiles C++ policy under ASan/UBSan and proves byte-exact output against R277/R278 tested transformation. Tests disabled/invalid env values, wrong disc/DOL/backend/chunk count, missing/extra sites, duplicate function, reapplication, wrong-hash refusal. Pass `generated/dcbz-policy-r279.log`,75678 FINISHED0. This is policy/source proof, not module gameplay.
- Prepared ModernGekko0021-dcbz-loop-policy.patch SHA256ef5ce417866cd67f6fd314f8585a6ea7716e50bcf824a044c4b8d4020c3c0e3b. Standard git apply --check passes. Adds policy include/selection before cache lookup, enabled-only cache suffix (disabled preserves old key), manifest field and application after THP extraction. Bootstrap pins it, peels it before dependencies, restores afterward, applies after THP and includes it in reviewed scope.
- Exact root/vendor/DolRecomp pins reconfirmed0514d9f0/13e49209/fa0cf619; no game/compiler/booted Simulator before bootstrap. First and repeat bootstrap pass (`generated/bootstrap-dcbz{,-repeat}-r279.log`,74513/43559 FINISHED0). New `tests/test-dcbz-policy-wiring.py` checks applied-policy parity/cache order/manifest/bootstrap recovery and is in repository suite. Existing THP wiring still passes unchanged.
- `./scripts/check-repository.sh` completes Repository safety checks passed (`generated/check-dcbz-r279.log`), then `cmake --build generated/build/moderngekko-desktop --target moderngekko-port --parallel 8` completes199 steps (`generated/build-port-dcbz-r279.log`,combined46708 FINISHED0; benign duplicate-library warnings). Port SHA256c5c54f412ef115d52553f518d88bae53b4d9c642bf8fabd47ef9cf3e0249709f. Normal runner e86f2e09...22b689/module5c210107...e91d hashes remain unchanged.
- Space preflight:3.6GiB available versus accepted module tree839MiB. Started `GALAXYPAD_DCBZ_LOOP=1 GALAXYPAD_MODULE_OUTPUT="$PWD/generated/modules-dcbz-r279" GALAXYPAD_PGO_PROFILE="$PWD/generated/pgo/rmge01.profdata" ./scripts/build-module.sh > generated/build-module-dcbz-r279.log 2>&1`. LIVE session62691; last poll confirms running, log has Wii preflight RMGE01/Broadway/MEM2 pass. No module completion or speed gain claimed. Next poll same handle; no bootstrap/restart/concurrent game. Inspect candidate cache/manifest/profile and audit when finished, then isolated runtime comparison before any promotion. Original PRD, G6, audio and performance remain open; no mobile build/publication.

## 2026-09-07 — R278 completed synthetic decoder benchmark supports runtime candidate

- Previous turn progress: loop-preserving form passed whole-chunk checks and retained PGO. Hypothesis: reduced repeated mapping work improves completed Huffman blocks, not merely a store microbenchmark. Added source-only synthetic fixture `tests/fixtures/huffman-throughput.c` and `tests/benchmark-huffman-dcbz.py`; exact accepted module/source/helper hashes checked. No game data, vendor mutation, or app/module build.
- Fixture uses canonical two-code lookup tables (0=zero/EOB,1=category1/2), four entropy word patterns (zero/ones/alternating), four starting bit counts1/27/29/32, ordinary RAM coefficients and an identity placement table. Runs the exact Y routine through guest LR return, allowing bounded internal yields; verifies stack restoration, reservation clearing and whole CPU/memory equivalence. Zero/dense blocks also have independent expected coefficients. Identity placement is synthetic, not Nintendo's natural-order table; no claim of complete legal stream coverage or measured movie input distribution.
- First compile failed before execution because an asm name declaration followed prior uses. Fixed by compiling each unchanged function name to native assembly with accepted O2/strict-FP/PGO flags, then renaming its symbol in assembly before assembling/linking. ThinLTO is deliberately omitted for decoder objects to preserve PGO lookup identity across two co-linked variants; this is pre-link benchmark evidence, not a final app comparison. Both bodies are ASan/UBSan-instrumented for correctness mode, not just the harness. No profile mismatch warning in the successful runs.
- `python3 tests/benchmark-huffman-dcbz.py` passes32 complete-state/memory cases under ASan/UBSan (`generated/huffman-throughput-check-r278.log`,5201 FINISHED0). Independent dense-coefficient checks were added afterward and pass in both benchmark runs. First short20k ABBA run ratio0.900974 had warm-up variation; raw retained `generated/huffman-throughput-benchmark-r278.log`,35203 FINISHED0, not the decision baseline.
- Longer `python3 tests/benchmark-huffman-dcbz.py --benchmark` uses1,000 warm-up calls before each100,000-call batch, ABBA per32 fixtures, CLOCK_THREAD_CPUTIME_ID. Output/whole-state checks occur outside measured loop; identical fixture reset, LR/stack/reservation assertions and CPU-copy costs remain inside both sides. All32 cases improve. Sum of64 per-call means: control118743ns (rounded), candidate107063ns, ratio0.901635 (~9.84% less time); pattern ratios zero0.570677,ones0.939855,alternating0.905678/0.908739. Evidence `generated/huffman-throughput-long-r278.log`,90934 FINISHED0. This is not9.84% game speed or a weighted movie estimate.
- Current fixture SHA2565cdaef2c250776803d80b74cbfbde587db3db35a14177050d9e02fd79797c3a6; driver cdd473f39f94620f19aa83fb9752e6e19fc1e7d6535251837cffff1f8be9d12b. ExistingR277 whole-chunk sanitized/O2ThinLTO correctness covers both Y/chroma sites; completed fixture benchmarks only Y. Full repository suite not run this turn.
- Next justified step: canonical pinned integration of the eight-site loop-preserving policy and cache identity, followed by an isolated module candidate, not replacement of the normal app. Require exact compile/profile checks and matched movie runtime evidence before promotion. No active process or mobile build; original PRD/G6/performance/audio remain open.

## 2026-09-07 — R277 preserve decoder PGO while specializing line stores

- Previous turn progress: new guarded line-clear candidate passed isolated and complete-chunk correctness. This turn tested whether compiled shape justifies a runtime build. Extended existing `scripts/inspect-huffman-y.py --dcbz` to generate only private candidate sources from pinned accepted decoder/module, use actual O2/strict-FP/profile flags, and inspect pre-link ARM64 assembly (ThinLTO omitted solely for assembly output).
- `python3 scripts/inspect-huffman-y.py --dcbz` shows original decoder12,233 instructions versus candidate body21,854; compiler explicitly rejects one mismatched profile. `--dcbz --no-profile` separates profile effects: original22,098 versus candidate body21,408 (helper not included in these initial body counts). Logs `generated/dcbz-assembly{,-no-profile}-r277.log`; sessions22288/66353 FINISHED0. Do not build/promote the direct-loop-removal form on these results.
- Changed hypothesis/shape: retain the original eight-iteration loop and move only mapping/reservation preparation and individual store selection into helpers. Added `patches/experiments/dcbz-ram-loop.inc`, SHA256 ed743fdeb1b32af89dfd1fbfefa671a63b2a36ed7c2c59b684f105275783a421. Guards match R276; a failed preparation leaves all original per-word callbacks intact. Prepared regular-RAM writes cannot modify CPUState or the journal function pointer. No global host-pointer/restrict assumptions.
- `python3 scripts/inspect-huffman-y.py --dcbz-loop` retains the original profile without a mismatch warning. Original body12,233 instructions/1,441 loads/790 stores; candidate body12,104/1,557/770 plus prepare64/8/1 and store59/15/5. Total instruction count12,227, but additional calls/loads mean this is NOT speed evidence. Source and assembly retained under `generated/dcbz-loop-assembly-r277/`; log `generated/dcbz-loop-assembly-r277.log`, session47591 FINISHED0. Inspector now includes emitted dcbz helpers in counts and pins both experiment snippets.
- Extended focused helper/whole-chunk tests with explicit `--loop` / `--dcbz-loop` modes; historical forms retained. `python3 tests/test-dcbz-ram-line.py --loop` passes18,740 cases each ASan/UBSan and O2. `python3 tests/test-chunk-entry.py --dcbz-loop` and `... --optimized` each pass65,536 complete-state/memory/callback comparisons, capped0/journal_events505936/reservation_clears634. Logs `generated/dcbz-loop-{helper,oracle,optimized}-r277.log`; sessions32279/96416 FINISHED0. No full repository suite this turn.
- Read Petari THPDec.c buffer setup and exact generated callsite: reference coefficient blocks derive from ordinary work area, while Gbase uses separate locked-cache output. This supports investigating ordinary-RAM clears, but is not a runtime address capture or region substitution. Next construct completed-decoder synthetic entropy benchmark with output/state verification and time that whole path under matched flags, before any module build. App/vendor unchanged; no runtime/build/booted Simulator/mobile build or acceptance. Full original PRD remains active.

## 2026-09-07 — R276 guarded decoder line clearing passes differential checks

- Previous turn was progress: execution-loop evidence ruled out a minor dispatch-order experiment. Goal remains G6/macOS performance first. Read actual generated decoder body and pinned GXRuntime memory helpers: eight dcbz sites each perform eight separately mapped mem_write32 calls. No prior dcbz experiment found in the journal. Hypothesis: folding only callback-free full mapped lines can remove repeated mappings/reservation checks without changing observable guest behavior.
- Added unapplied `patches/experiments/dcbz-ram-line.inc`, SHA256 6f576f6719cb6a60a4a9fc1363b5da1be701b16c712ec85a85da2514f7b49711. Requires valid nonoverlapping MEM1/MEM2 window sizes, a full32-byte mapping, no active write journal, and target disjoint from CPUState and the journal function pointer. Clears the matching reservation before memset; otherwise retains all eight original stores. No memory callbacks or journals are batched, no CPU field is cached across callbacks, no guest PC/cycle/entry changes. Vendor/app untouched.
- Added `tests/test-dcbz-ram-line.py`, pinned to actual cpu.h SHA7fc14481...d4657. `python3 tests/test-dcbz-ram-line.py` passes18,740 whole-state/memory/callback comparisons each at O1ASanUBSan and O2. Covers MEM1/MEM2/cache aliases, full/partial/missing mappings, address wrap, journal and external callbacks that remap RAM/rearm reservations, and guest RAM overlapping CPU state. Evidence `generated/dcbz-ram-line-r276.log`; session30023 FINISHED0. These are bounded cases, not arbitrary-memory proof.
- Extended existing `tests/test-chunk-entry.py --dcbz` to start both variants from hash-verified unchanged R263 reference; only candidate replaces exactly8 zero loops. No parked extraction is reused in candidate body. Both `python3 tests/test-chunk-entry.py --dcbz` and `... --dcbz --optimized` pass65,536 full-chunk comparisons over all1,024 entries/eight budgets/eight memory/callback modes; capped0, journal_events505936, reservation_clears634. Logs `generated/dcbz-chunk-{oracle,optimized}-r276.log`; sessions66321/59657 FINISHED0. This checks interior entries, yields, CPU state and observed memory/callback order, not gameplay or performance.
- Next: measure offline whole-decoder work/code shape before deciding whether a module build is justified. Do not infer speed from fewer source operations or passing tests. Full repository suite not rerun this turn. Normal runner e86f2e09...22b689 and signed module5c210107...e91d hashes reconfirmed unchanged; no game, compiler, or booted Simulator remains. No iOS/iPadOS build, publication or goal acceptance.

## 2026-09-07 — R275 execution-loop triage; retain performance-first scope

- Goal remains G6 with macOS performance/stability first, as explicitly reiterated by Chris; no iOS/iPadOS build. Hypothesis: dispatch termination checks or repeated state synchronization might be a larger recurring cost than the parked arithmetic probes.
- Read Run.cpp, IsHostCallAddress, FastDispatchableAt/ChunkIndexOf and HookInstructionFallback in the pinned vendor source; reviewed R214 profile and R270 candidate shutdown counters. No source or app mutation. Commands: bounded `sed`/`rg` reads of these sources and `generated/runtime/thp-profile-r214/summary.json`, `tail` of `generated/runtime/empty-rel-candidate-r270/runtime.log`.
- Evidence: 1,025,894,946 native dispatches and 11,101,990 bursts (~1.08% ratio) in R270. Moving downcount ahead of dispatchability only avoids the final check on budget-exhausted bursts, not checks on all billion dispatches. Host-call recognition calls an external callback; the REL slow path resolves mappings. No blanket purity assumption or untested reordering.
- R214 leaf attribution: Run 813/31,295 (~2.60%); SyncIn 80 and SyncOut 6 (~0.27% combined); THP kernels 5,112+2,801 and decoder chunk 5,015 (~41.31% combined). These historical samples prioritize investigation, not a rigorous speedup bound, current whole-app profile, or evidence that the entire decoder chunk is Huffman. Reject dispatch-order work as the primary response to lag; no build justified.
- `hook_fb=87053387` is instruction fallback-hook traffic, NOT host callback count and NOT the block-level `fallback` counter. Existing cache-operation fast path still increments it, so this count does not establish full interpreter synchronization cost.
- Separate unresolved symptoms: repeatable movie CPU deficit (~56.92Hz after R271 promotion) versus R247's 53.444ms idle graphics-completion wait within a 74.400ms interval. Neither average cadence nor CPU leaf samples explain every hitch. Keep rolling capture parked until recurrence; next inspect decoder body/dataflow for a materially different semantics-preserving opportunity, not repeat extraction or helper experiments.
- Process check: `pgrep -x moderngekko-run` finds none; `xcrun simctl list devices booted` lists no devices. Disk 3.5GiB available. No build, launch, Simulator, publication, acceptance, or runtime performance gain this step. STATUS updated; original PRD remains intact.

## 2026-09-07 — R274 hardware widening whole-kernel checks; parked

- Prior turn was progress: isolated result/status checks and timings selected a kernel comparison. Added `--hardware-widen` to existing oracle: same accepted extracted chunk on both sides, candidate alone gets pinned private types.h insertion and unchanged copied cpu.h so relative types include resolves correctly. No vendor/module/app change.
- Oracle69114 FINISHED0: all four reference/candidate sanitized/optimized variants match digest882be09b1f38e9ad, each1440cases/624yields/480FPfaults/480illegal/192interruptions/96callerreturns/18816callbacks/34688journals/1440reservations. Log generated/hardware-widen-kernel-oracle-r274.log. Earlier isolated FP status tests remain separate coverage, not arbitrary-state proof.
- 200,000-repeat wall-time ABBA77134 FINISHED0: totals24908.315/24896.225/26144.435/26501.995ns (sum12pattern means), sizable run variation. Added explicit `--cpu-time` benchmark mode using CLOCK_THREAD_CPUTIME_ID to distinguish scheduling delay from execution cost;73477 FINISHED0 totals24790.804/25084.706/24488.996/24751.438ns. CPU-time control mean24771.121 versus candidate24786.851 (~0.064% slower), variation exceeds difference. Text229376→245760bytes (+16KiB). All rows retained in generated/hardware-widen-kernel-{bench,cpu-bench}-r274.log.
- PARK hardware widening: isolated conversion benefit does not translate to useful complete-kernel work reduction. No module build or normal-package change warranted. Do not repeat this helper candidate unchanged. Next reconsider a larger structural cost rather than another small conversion/lookup/entry reshaping experiment; retain accepted relocation guard. No runtime/build/booted Simulator/mobile build; original PRD/G6/performance/audio remain open.

## 2026-09-07 — R273 normal-finite hardware widening prototype

- Prior turn was progress: promoted normal-path save smoke passed. Re-read actual conversion and earlier integer-only widening probe. New unapplied f32-hardware-widen.inc uses a hardware float-to-double cast only for exponent1–254; all exceptional classes remain on original convert_to_double. Snippet SHA256 bb191ac81e691ea661d5af075ec4800863d3f019cca51bdb60a3cd582ad7bb4f. Actual types.h reverified824490a35e968e645067711189a67156536ddd127ade063418ddcb59787f58bf. No vendor/module/app edit.
- Added isolated tests/probe-f32-hardware-widen.c with FENV_ACCESS ON, noinline reference/candidate, strict FP flags. Initial29972 FINISHED0:424576 exact result/status checks across all four rounding modes. Read actual Common/ArmFPURoundMode.cpp before adding its FZ/AH/FIZ mode settings. Expanded55337 FINISHED0:849152 comparisons pass O2 ASan/UBSan and unsanitized O2 across both runtime flush modes, all exponent/sign classes with fraction boundaries, random encodings, clean/preexisting exception flags; raw ARM FPSR/FPCR also unchanged. Unsupported controls may read back cleared; evidence is for actual host, not all ARM implementations. Not exhaustive 2^32 input coverage or enabled-trap testing.
- ARM64 assembly includes fcvt d0,s0 in candidate normal path. Six alternating5M-call trials: final normal finite ratios .7341–.7710 candidate/reference, random encodings .7499–.8368; zero category retained in full log. Timing excludes check harness and uses default restored environment. Evidence generated/f32-hardware-r273{,-final}.log, f32-hardware-flush-r273.log, f32-hardware-r273.s. Microbenchmarks are not game performance; do not promote on these alone.
- Next add source-pinned private types.h override to existing complete-kernel oracle, comparing identical accepted extracted chunk and actual helpers with only conversion changed. Then longer whole-kernel ABBA timing before considering a module candidate. No runtime/build/booted Simulator/mobile build; normal runner/module unchanged and original PRD/G6/performance/audio remain open.

## 2026-09-07 — R272 normal-path real-save smoke passed

- Prior turn was progress: tested runner promoted with rollback package. Fresh promoted-r272 profile uses accepted-r238 Config/config.ini plus copied original midblock-r145 Wii; no savestates. Normal wrapper launched with STATICRECOMP_MODULE and all timing envs unset. Runtime18666 discovers bundled normal-path module5c21...e91d without --module. Title visibly ready before input.
- Real-menu save-load sequence78839 FINISHED0; Observatory visibly loads one-star save. Control smoke59585 FINISHED0 with changed Mario position/camera; displayed59.9–60.0FPS only, not fixed-window timing. Native close18666 FINISHED0/native2848134382/fallback0/SMCfailed0. Save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged. Whole182s1underrun2backlogs is not long-run audio acceptance. Evidence generated/runtime/promoted-r272/runtime.log and conversation captures.
- Normal-path discovery/save reload check complete. Do not repeat unchanged Observatory or empty-rel comparisons. Remaining repeatable movie deficit and intermittent hitch remain open; no mobile work yet.
- Read current core/types.h conversion after shutdown: normal binary32-to-binary64 path uses integer bit assembly; earlier R131 probe changed integer assembly only, not a hardware conversion. Next investigate an isolated normal-finite hardware-widening alternative, with exceptional inputs on exact old path and CPU floating-point control/status equivalence explicitly tested before timing. No implementation or performance claim for that idea yet. No runtime/build/booted Simulator remains; full original PRD/G6/performance/audio open.

## 2026-09-07 — R271 uninstrumented movie completion and runner promotion

- Prior turn was progress: ABBA supports small guard gain. Fresh empty-rel-smoke-r271 profile from original R85; all completion/arming/phase/VI envs unset. Candidate runtime93173/module5c21...e91d, state3 visibly restored, trigger8844 FINISHED0. Movie visibly progressed and completed into playable plaza; no load used to exit the movie.
- Movement90715 visibly changed Mario's position after completion. Jump tap51215 and back/jump sequence29052 completed; screenshots did not clearly isolate airborne Mario, so no new standalone jump visual acceptance. Backward movement put Mario behind foreground geometry; forward sequence35702 returned him visibly into plaza. Final59.9FPS label is not a fixed-window cadence measurement. Clean native close93173 FINISHED0; native5907487252/fallback0/SMCfailed0,16button transitions. NAND a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 unchanged. No VI/completion files. Whole439.4s62underruns23backlogs not audio acceptance. Evidence generated/runtime/empty-rel-smoke-r271/runtime.log and conversation captures.
- Staged normal app clone in runner-promotion-r271; replaced only runner with tested candidate. Signing and staged private app/module audit24639 FINISHED0. Byte comparisons verify original wrapper/frontend/signed module unchanged and runner identical to tested candidate. Moved old normal app to explicit recoverable backup `generated/macos/GalaxyPad.app.previous.runner-r271`, moved audited staged app into normal path. Final audit16627 FINISHED0/`generated/audit-promoted-r271.log` passes.
- New normal runner SHA256 e86f2e09d27f69a41d1f10c2513cde14898557bc379fa787be013b5f0122b689; module unchanged5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d. Wrapper/frontend unchanged. No publication or user save/config edit.
- Next normal-path launch/discovery and real-save reload smoke for promoted package, then continue unresolved macOS performance/stability work rather than more unchanged guard comparisons. The ~1.83% movie gain is incremental; still below60Hz and G6/audio/full original PRD open. No runtime/build/booted Simulator/mobile build remains.

## 2026-09-07 — R270 reverse-order runtime comparison supports small guard gain

- Prior turn was progress: first matched pair favored candidate. Created fresh original-R85 profiles empty-rel-candidate-r270 and empty-rel-control-r270; same module/VI-only env/native1x/Metal/trigger and compiled steady-clock anchors. Candidate first, closed before control launch. Both restored flowers/lake visibly and showed advancing movie before/after each full interval. No build/Simulator overlap.
- Candidate runtime54565/trigger18957 FINISHED0: window[350492340263958,350552340263958),3406VI=56.766667Hz,3405 complete intervals,zero invalid CPU/reset intervals; worst21.515ms wall/20.281375CPU/.314829idle. Control runtime10586/trigger43251 FINISHED0: window[350709963254416,350769963254416),3342VI=55.7Hz,3341 complete intervals,zero invalid CPU/reset intervals; worst23.654125ms wall/22.135CPU/.252424idle. Raw logs, timestamps, traces and summaries in respective runtime directories.
- Completed ABBA counts3365/3424/3406/3342, mean control55.891667Hz versus candidate56.916667Hz, +1.8339% throughput. Both candidate measurements exceed both controls. Supports retaining this small targeted optimization, not universal speedup/60Hz/stability/audio acceptance. No more unchanged micro-optimization repeats for this guard.
- Both clean native exits/fallback0/SMCfailed0. Candidate native1025894946,control1087222724. Both NAND hashes unchanged a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6. Whole candidate156.1s54under5backlog/control166.6s72under5backlog are unequal-duration diagnostics, not audio acceptance. Closed during films; not movie-completion evidence.
- Next diagnostics-disabled candidate run through movie completion and gameplay/input/save smoke, then audited normal runner promotion if no regression. Normal package remains unchanged until then. No game/build/booted Simulator/mobile build remains; full original PRD/G6/performance/audio open.

## 2026-09-07 — R269 candidate movie comparison, first pair positive

- Prior turn was progress: corrected steady-clock control window. Reverified runner e86f2e09...22b689 and R85 slot hash; no prior game or booted Simulator. Candidate runtime42537 uses empty-rel-r267 app, prepared empty-rel-candidate-r267 profile, VI-only diagnostics (completion/arming/phase unset), native1x/Metal, same module5c21...e91d. Module-loaded confirmed before CUA attachment. State3 visibly restores flowers/lake; trigger25487 FINISHED0; immediate compiled steady-clock anchor.
- Full window [350196769050000,350256769050000):3424VI=57.066667Hz,3423 complete intervals,zero invalid CPU/reset intervals. Longest22.1835ms wall/20.954791CPU/0EFB/.000834throttle/.214784idle. Movie visibly advancing before/after; native close/runtime42537 FINISHED0. Evidence generated/runtime/empty-rel-candidate-r267/{runtime.log,window-start.txt,vi.csv,summary.json}.
- Compared corrected control3365VI=56.083333Hz, candidate +59VI/+1.7533% in this pair; worst control interval35.416792ms versus candidate22.1835ms. A single pair does not establish causal gain or stability. Do not compare raw whole-run audio counts with unequal durations: candidate179.4s55underruns6backlogs, not audio acceptance.
- Native1256340498/fallback0/SMCfailed0; NAND a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 unchanged. Closed during movie; no movie-completion or G6 claim. Normal app untouched.
- Next reverse-order candidate/control with fresh original R85 profiles, same VI-only instrumentation and compiled steady-clock anchors, to finish an ABBA comparison before promotion. No game/build/booted Simulator/mobile build remains. Full original PRD/performance/audio/G6 open.

## 2026-09-07 — R268 control movie captured; clock origin corrected

- Prior turn was progress: isolated guard candidate and matching profiles built. First control used flight-r260 runner with only GALAXYPAD_VI_TIMING enabled; completion/arming/phase unset. Restored R85 state3 visibly, ran exact trigger96321, observed advancing movie, cleanclosed99737. Its requested window used Python time.monotonic_ns rather than the recorder's C++ steady_clock: analyzer correctly rejected coverage. Do not use `empty-rel-control-r267` for a numerical comparison or infer a retrospective offset. Native-only/save unchanged; raw trace retained.
- Added `tests/steady-clock-now.cpp`, compiled `generated/steady-clock-now`, using identical std::chrono::steady_clock to recorder. Live values confirmed C++349793081791583 versus Python monotonic348743371283458 and RAW349793110305041: different origins, not recorder buffer loss. Use compiled helper immediately after trigger for both comparison windows.
- Prepared fresh `empty-rel-control-r268` from original R85 files; runner/module unchanged. Runtime65649 and trigger30544 FINISHED0. Fresh state restore screenshot matches flowers/lake, movie visibly advancing before/after interval; closed natively only after interval ended. VI recorder only, no game/build overlap. Exact fixed interval [349905919863333,349965919863333): 3365 VI events,56.083333Hz,3364 complete intervals,zero invalid CPU/reset intervals. Longest interval35.416792ms wall/20.714167CPU/0EFB/.001168throttle/.236168idle. Summary and trace under generated/runtime/empty-rel-control-r268.
- Native898556703/fallback0/SMCfailed0; NAND a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 unchanged. Whole141.6s60underruns4backlogs not audio acceptance. No movie completion or full G6 claim; this run closed during the film.
- Next launch the already prepared empty-rel-candidate-r267 profile with the empty-rel-r267 app, same VI-only env and native1x/Metal settings, restore state3, same trigger, then `generated/steady-clock-now` and full60s interval. Do not reuse a stale closed CUA app binding before shell module-load confirmation. Control is closed/no game/build/booted Simulator; mobile builds held/full PRD open. No candidate speedup measured yet.

## 2026-09-07 — R267 relocation-guard candidate built and isolated

- Prior turn was progress: actual-resolver and host tests prepared a valid guard. Added SHA-pinned empty-rel overlay to bootstrap peel/restore/apply/scope checks. Original bootstrap88973 and repeat76183 FINISHED0. Host test now recognizes the already-integrated size-zero path without inserting it twice; experimental guard mode also accepts the applied guard. Added guard and actual-resolver tests to the repository suite.
- Sequential suite/build58326 FINISHED0. `generated/check-r267.log` ends Repository safety checks passed; `generated/build-empty-rel-r267.log` links moderngekko-run successfully. No AOT/module regeneration. Duplicate-library linker warning retained; not a build failure. Normal app untouched.
- Isolated `generated/macos/empty-rel-r267/GalaxyPad.app` copied from flight-r260 and only runner replaced. Signing/strict verification41638 FINISHED0. Candidate runner SHA256 e86f2e09d27f69a41d1f10c2513cde14898557bc379fa787be013b5f0122b689; control flight-r260 runner c460fd846c68c1dd8f56706da8e7a77767e71f6083f35a541b7e6b2bf437f7fe. Module unchanged 5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d. Normal runner affbcb51...fe295 independently reverified before build.
- Prepared disposable `generated/runtime/empty-rel-{control,candidate}-r267` from pre-attack-r85 Config/Wii/config.ini/slot3; configured Wii pipes. Both slot hashes aea2b7a3fe295b77e409753a216abf8fe6f2f1ec23ee7a8832fc9ac1a27b7ab8. Control NAND a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6. Config verifies native1x, CPUThread True, CPUCore6, Metal/Cubeb. Neither launched yet.
- Next matched R85 movie comparison, same diagnostics/settings and unchanged module, using flight-r260 as control (not historical accepted-runner timings). Verify visible state restore/trigger, full measurement window, native-only execution and save preservation; close each before next. No current runtime/build/booted Simulator or mobile build. This is build readiness, not speedup/G6/audio/full PRD acceptance.

## 2026-09-07 — R266 empty relocation-list guard prepared

- Prior turn was progress: longer whole-kernel timing ruled out paired loads. Audited current HookExternalPointer, MMU::TryGetLockedCachePair and actual ResolveRuntimeAddress/TranslateRelAddress. Existing pointer hook calls relocation lookup twice per pair; actual resolver scans only m_active_rel_sections and returns the input on an empty list, with no refresh or other side effects. Keep every journal/MSR/MMU/BAT/debug/cache guard intact; no cached eligibility or assumption based on module type.
- Prepared unapplied `patches/experiments/lc-empty-rel.patch` SHA256 94a970ed151b50fc1820f2850fcfce7f6164386240fa2fc68862a2dc312eb067. It guards only the two relocation calls with the live vector's empty check. `git apply --check` against current pinned/overlaid vendor passes. Vendor and normal app unchanged.
- Extended `tests/test-lc-pair-host.py --empty-rel`; original and experimental modes pass O2 ASan/UBSan (4717 FINISHED0). Verifies zero resolver calls for empty list, both calls for nonmatching nonempty list, relocation rejection for either byte, and unchanged opt-in/tracing/MSR/journal/MMU/legacy behavior. Added `tests/test-empty-rel-resolution.py`, compiling actual resolver methods: 5,138 comparisons pass for empty/nonempty/zero-sized/overlapping/boundary/wrap and clear/repopulate transitions. Host scaffolding is not an integrated runtime test.
- Next measure this small host-side change in an isolated runner against the same current runner/module before any normal-package promotion. No speedup claimed from eliminating calls; do not bypass other eligibility checks. No runtime/build/booted Simulator, no mobile build. Full original PRD/G6/performance/audio remain open.

## 2026-09-07 — R265 paired-load whole-kernel comparison; parked

- Prior turn was progress: isolated load prototype and payload-dependent microbenchmarks selected a whole-kernel check. Extended existing `tests/probe-thp-oracle.py --kernels --pair-load`: both variants compile the identical accepted extracted chunk; only candidate gets a temporary overridden CPU header with exact snippet SHA256 4e41c47f814bf6b79783f37bb98b1a1bea7e568f6790f4285d89d7559c58de3d. Source header remains pinned by existing oracle checks; insertion reversal is exact. No vendor/app modification.
- Oracle19833 FINISHED0, `generated/pair-load-kernel-oracle-r265.log`: all four reference/candidate sanitized/optimized variants produce digest 882be09b1f38e9ad. Each executes 1,440 cases, 624 yields, 480 FP faults, 480 illegal instructions, 192 interruptions, 96 caller returns, 18,816 callbacks, 34,688 journals, 1,440 reservation clears. Covers six coefficient patterns and actual runtime arithmetic helpers; not exhaustive arbitrary states.
- Initial 20,000-repeat ABBA total times 24734.75/24469.9/23675.9/24809.05 ns (sum of 12 pattern means) appeared promising but variable. Increased only sample length to 200,000 repeats per pattern. Longer ABBA totals 24620.390/24647.075/24468.350/24528.500 ns: mean control 24574.445, candidate 24557.7125, approximately 0.068% lower. Individual pattern variation exceeds aggregate difference. Candidate text 245760 versus control 229376 bytes (+16 KiB). Both timing sessions14322/96827 FINISHED0; logs `generated/pair-load-kernel-bench{,-long}-r265.log` retain every row.
- PARK paired direct-RAM load optimization: no meaningful whole-kernel benefit despite isolated finite-load gains. No module build or runtime comparison warranted for this shape. Next performance work must target a different substantial cost rather than replay paired-load/store, extraction, FP routing, or decoder PGO experiments. Accepted app unchanged; no game/build/booted Simulator/mobile build. Full original PRD/G6/performance/audio remain open.

## 2026-09-07 — R264 paired direct-RAM load prototype

- Prior turn was progress: extraction correctness and compiler evidence ruled out that shape. Read current memory helpers and R155 store-pair rejection before selecting a different path. Direct type-zero paired loads can reuse a checked eight-byte mapping without store journals/reservation bookkeeping. Added unapplied `patches/experiments/psq-ram-pair-load.inc`; valid bounded mapping sizes, two lanes only, lane-ordered conversion/assignment/read, otherwise original helper body. No external callback is skipped on the fallback path. No vendor or accepted app change.
- `tests/test-psq-ram-pair-load.py` pins actual cpu.h, extracts its inline load body and inserts the snippet only in a temporary test. O2 ASan/UBSan passes 100,000 random-bit cases across MEM1/MEM2/mirrors/boundaries/wrap, callback remapping/state mutation, w/type/LSQE routing plus explicit destination-CPU alias. Quantized slow helper is a test double: routing compared, not quantization revalidated. Initial run50861 FINISHED0, then finite-input run76497 FINISHED0.
- Five-million-call alternating trials, six per memory type: zero-filled inputs show no benefit (MEM1 candidate/reference .996–1.047; MEM2 1.076–1.120). Corrected benchmark payload to mixed positive/negative finite floats: MEM1 .785–.793; MEM2 .714–.753. Both logs retained: `generated/psq-ram-pair-load-r264.log` and `generated/psq-ram-pair-load-finite-r264.log`. This is a payload-dependent isolated microbenchmark, not runtime performance or representative decoder input proof.
- Next test this exact header insertion with the existing complete transform-kernel execution oracle, then a bounded whole-kernel benchmark with representative input categories. Do not promote from microbenchmarks alone. No module build or runtime started; booted Simulator list empty; full original PRD/G6/performance/audio open, mobile builds held.

## 2026-09-07 — R263 luminance decoder extraction tested and parked

- Prior turn was progress: exact-DOL boundary evidence selected an independently testable region. Added `scripts/extract-huffman-y.py`, source-pinned to e6aa9158...996dcc. All 407 instruction labels and switch entries checked; generated goto closure verified; reversing the trampoline replacement restores the outer function byte-for-byte. Outer entry cycle charges and distinct early-return/guest-return paths preserved. Private candidate `generated/huffman-y-r263/candidate.c` SHA256 76fdb33cad574c96f4aa2affd392e892f6a2a6e9837e851fe0aeb0fcd5b1405b; reference f1be7297...423ca. No module selected or built.
- Extended existing `tests/test-chunk-entry.py` with source-pinned `--huffman-y`, leaving historical mode unchanged. Initial sanitized 32,768 cases passed (10107 FINISHED0). Expanded budgets -257/0/1/16/64/256/1024/4096 across all 1,024 chunk entries, direct/external memory, journals and mutating callbacks: O1 ASan/UBSan and O2 ThinLTO each pass 65,536 whole CPU-state/memory/callback comparisons, capped=0, journal_events=505936, reservation_clears=634 (88179 and 20937 FINISHED0). Evidence `generated/huffman-y-oracle-{expanded,optimized}-r263.log`. This is differential coverage, not arbitrary-state or movie acceptance.
- Added `scripts/inspect-huffman-y.py` using accepted compile flags and module identity, omitting ThinLTO solely to obtain pre-link ARM64 assembly. Current-profile original function 12,233 static instructions versus candidate outer 16,091 plus helper 6,993; compiler explicitly ignores one mismatched function profile. No-profile control distinguishes that consequence: original 22,098 versus candidate 15,656+7,003 (22,659); loads 5,302 versus 3,639+1,688; stores 2,440 versus 1,808+677. Not dynamic execution costs and not a runtime speed comparison. Sessions 40997/33849 FINISHED0; assembly logs under generated/huffman-y-assembly*-r263.log.
- Park this extraction: correctness passed but no convincing compiler-work reduction, with substantial current-profile code growth. Do not link a movie candidate for this shape, and do not repeat rejected decoder PGO retraining to rescue it. Next reconsider a different substantive cost from the existing current-module profile; keep the intermittent wait recorder parked unless the hitch recurs. Accepted runner affbcb51...fe295 reverified unchanged. Found and terminated only stale diagnostic frontend PID78873 at vi-counters-r241; subsequent exact-name check confirms exit. No game, frontend, build, or booted Simulator remains. No mobile build; G6/performance/audio/full PRD remain open.

## 2026-09-07 — R262 decoder investigation resumed; bounded extraction target

- Re-read R214–R221 profiling and rejected experiments, current status, goal ordering, and PRD mobile/shell gates. No new performance measurement or accepted app change. Simulator list empty; disk has 3.3 GiB available. Do not repeat intermittent captures or start mobile builds.
- Ran `python3 scripts/audit-thp-boundaries.py` against its pinned exact DOL. Luminance Huffman region 0x804534B4–0x80453B10 is 1,628 bytes (407 guest instructions), SHA256 dd607137a05d79686e36d7276e3984521f82b84910c7b902bd0dcec112c7133e. No direct calls or branches outside the region; sole indirect exit is unconditional LR return at 0x80453B0C. The complete region lies in accepted chunk_1103_text1_804530A0.c, whose current profile has 5,015 samples across the entire chunk, not exclusively this region.
- Next falsifiable experiment: extract this region into a bounded native helper, preserving original guest operations, outer interior-entry cycle charges, memory callbacks, and return-dispatch behavior. This differs from the parked entry-switch duplication and CLZ experiments. First audit generated control-flow closure and byte-exact restoration, then differential execution including interior entries/yields/callbacks. Only build a runtime candidate if offline compiler/benchmark evidence warrants it; no assumed speedup from the boundary audit.
- G6 and full original PRD remain open. Existing repeatable movie result is approximately 55.3 Hz; the isolated 53 ms idle FlushGpu hitch is a separate unresolved observation. No runtime, build, or Simulator started in this step.

## 2026-09-07 — R261 overhead probe and armed rolling capture; no trigger

- Prior turn progress: rolling candidate and analyzer ready. Added noinline O3 diagnostic microbenchmark tests/bench-completion-flight.cpp;62960 FINISHED0, generated/bench-completion-flight-r261.csv. Six alternating trials2M iterations: enabled CPU pair3.282–3.424ns, graphics pair36.186–37.542ns; disabled CPU1.642–1.747ns/graphics1.308–1.361ns, baseline0.975–1.553ns. Allocation outside timing, no export/file creation. Indicates modest isolated event-path cost, NOT end-to-end or scheduling-overhead proof; existing idle timer clock cost not included in supplied-timestamp CPU probe.
- Isolated flight-r260 app runtime94513/PID92551; phase and VI recorders unset, completion path enabled, explicit delay180000ms. Logged valid1 deadline347524403199000/threshold20000000. Readytitle/focus/load66013 FINISHED0; Observatory visible at~347465470524125, BEFORE arm. Input37271 FINISHED0,21cycles from347499179972708 through347650451847333; armed window347524403199000..347644403199000. Before/end visuals distinct Mario positions/no warning; no midwindow screenshots. No file while live.
- CleancloseAX1/runtime94513 FINISHED0/export1. Flight metadata triggered0/graphics_frozen0/invalid_waits0,30.87M CPU and31.23M graphics old events overwritten as intended. Analyzer status not_triggered; no qualified20ms CPU idle wait after arm, NOT proof all hitches or sustained slowdown solved. No VI trace requested, so no measured120sVI claim. Native5844654744/fallback0/SMCfailed0/save5040...64a6 unchanged. Whole349.9s32under8backlog not audioacceptance. Evidence flight-r260/{runtime.log,window.log,completion.csv,summary.json}.
- Park intermittent idle-hitch capture now; no further unchanged near60FPS loops. Return to reproducible CPU-heavy movie path and R214 current-module kernel/decoder profile, respecting all parked/rejected experiments. R24753ms idlewait remains localized but unresolved; retain opt-in rolling probe for recurrence. No accepted app change or mobilebuild. Fullsuite check-r261.log launched after runtime close; follow returned handle. G6/fullPRD open.

## 2026-09-07 — R260 rolling candidate and flight analyzer

- Prior turn progress: canonical integration/repeatbootstrap/fullsuite passed, build active. Revalidated51036 at152/196 then FINISHED0. Isolated generated/macos/flight-r260/GalaxyPad.app copied accepted, runner replaced, signed/strict verified6430 FINISHED0. Runner c460fd846c68c1dd8f56706da8e7a77767e71f6083f35a541b7e6b2bf437f7fe; module remains5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d. Accepted app untouched.
- Analyzer --flight added to summarize-completion-timing.py. Requires versioned exact metadata, valid flags/threshold, arming and trigger-duration consistency, frozen CPU endpoints matching metadata, complete paired retained streams covering trigger. Distinguishes not_triggered and graphics_response_missing; neither yields attribution. Does not infer exact DONE publication or one-to-one notification/wait cause. Focused metadata/coverage/endpoint tests pass; fullsuite34555 active/check-r260.log.
- Disposable flight-r260 profile prepared from accepted-r238 Config/config.ini and original midblock-r145 Wii, pipe configured/save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 verified. No runtime launched.
- Next after suite: bounded enabled/disabled diagnostic overhead check, then isolated flight capture with explicit GALAXYPAD_COMPLETION_ARM_DELAY_MS and verify logged deadline relative to visible gameplay. Choose sufficient post-boot delay, not assume a time gate proves scene. Trigger freezes recent context; use --flight analyzer, not old full120s completion analysis. No runtime/build/Simulator/mobilebuild; fullPRD/G6 open.

## 2026-09-07 — R259 rolling capture canonical integration

- Prior turn progress: timestamp reuse/arming/error tests passed. Canonical Dolphin0018 adds rolling ring/trigger/flight/options headers, updates completion session to rolling recorder and idle timer to tested observer variant. CPU no longer reads extra completion clocks; observer receives existing idle start/end. Graphics completion retains optional callback, now wall-only samples. Runtime0020 requires explicit GALAXYPAD_COMPLETION_ARM_DELAY_MS when completion path enabled, logs valid/deadline/20ms threshold, reports invalid config without arming, enables idle timing if VI OR completion capture is enabled. Original FlushGpu/state/notification order intact.
- Pins: ModernGekko0020 SHA c72c94b29bb419f423218deb4a8d3fcb2762b911273dc0d64df20c187d6d3172; Dolphin0018 SHA c40a8223d07fa85f0dccb33f13ed886f9c53b15ca60af5f1813598b31968466b. Initial generated diff lacked real context and failed apply-check; regenerated context before applying. Bootstrap14867 first/repeat FINISHED0; extended hash/peel/scope/header/opt-in/reused-endpoint checks pass. No direct vendor edits.
- Sequential fullsuite/build51036 active: generated/check-r259.log then generated/build-flight-r259.log. Follow SAME handle, no bootstrap or parallel build/runtime. Accepted app/module untouched, no mobile build or runtime launched.
- Next isolated sign/verify, diagnostic overhead checks and triggered capture; validate logged deadline against actual gameplay start and analyze trigger metadata/ring coverage instead of full120s stream coverage. Prior first-N analyzer does not yet interpret flight metadata and must not silently be reused. G6/fullPRD open/mobileheld.

## 2026-09-07 — R258 arming validation and timestamp reuse

- Prior turn progress: rolling recorder/export tested, runtime unchanged. Added isolated completion-flight-options.h parser requiring explicit delay milliseconds, strict unsigned whole input, maximum3600000ms and checked nanosecond deadline addition. Missing/malformed/overflow leaves output unchanged; explicit0 is allowed but must not be mistaken for gameplay arming. Integration must log chosen deadline and verify scene/window relative to it.
- Added rolling/idle-wait-timing.h candidate variant preserving existing wait implementation plus optional observer receiving already-read start/end. Disabled mode executes wait once with zero clocks/observer calls; enabled observer adds no clocks. Separate variant preserves canonical current header parity until overlay integration. Tests injected exact10/20 timestamps and production clock path pass.
- Added flight-specific RLIMIT_FSIZE8byte buffered-write/fclose failure test with limits/SIGXFSZ restored; result WriteFailed verified. Initial test initializer-list nullptr type deduction error fixed using explicit constchar* array before successful rerun. Focused/fullsuite78209 FINISHED0/check-r258.log ends Repository safety checks passed.
- Next canonical rolling integration: replace completion session type, install recent/trigger/flight/options headers and observed idle variant; Runtime Configure with parsed/logged delay/deadline and20ms threshold; enable idle clocks if either VI or flight recorder enabled; replace extra CPU Record calls with reused timestamp observer. Update hash/bootstrap/order tests and old exact wiring assertions, build isolated runner only after repeat bootstrap/fullsuite. No runtime/build/Simulator or accepted app/vendor change; G6/fullPRD open/mobileheld.

## 2026-09-07 — R257 rolling recorder export

- Prior turn progress: isolated trigger protocol passed. Added completion-flight-recorder.h wrapping tested rolling trigger, still not vendor/runtime wired. Configure takes explicit absolute steady-clock arming boundary and positive threshold; invalid zero threshold reports InvalidConfiguration instead of silently acting enabled. CPUWait accepts existing timestamps; graphics takes wall-only samples and explicitly marks CPU clock invalid, avoiding per-notification threadCPU syscalls.
- Exclusive post-both-join CSV export includes version, triggered, graphics_frozen, arming/threshold, trigger endpoints, invalid-wait and perstream overwritten metadata; chronological ring export. No-trigger and missing-graphics-response outputs are distinct and must not be treated as successful hitch attribution. Idempotent/no-overwrite behavior retained.
- Focused O2ASanUBSan35701 FINISHED0 tests two joined writers, startup exclusion, trigger endpoints, paired graphics freeze and clock bypass, no file before shutdown export, restart/nooverwrite, invalid config, no-trigger and trigger-with-no-graphics metadata. Fullsuite78620 active/check-r257.log; follow same handle. New flight-specific forced write-error test still to add before canonical integration (old recorder failure tests remain passing).
- Next runtime integration via canonical patches: explicit validated arming delay/absolute deadline logged, reuse IdleWaitTiming timestamps, replace first-N completion session with rolling recorder, and update analysis for trigger-scoped metadata/retention. No new capture until disabled/enabled overhead and export checks. Accepted app/vendor untouched; no build/game/Simulator, fullPRD/G6 open/mobileheld.

## 2026-09-07 — R256 rolling-capture trigger protocol

- Prior turn progress: isolated recent-event ring and fullsuite passed. Revalidated checkout R255/no game, preserved dirty root. Added isolated completion-trigger.h, not vendor/runtime wired. CPU supplies existing wait start/end timestamps (no extra clocks) into bounded paired ring; first eligible wait at/above threshold freezes CPU storage and release-publishes a request. Graphics owner records its final AfterNotify before freezing; no cross-writer array read/mutation. If no later graphics boundary arrives, Frozen remains false and export must report that explicitly.
- Explicit monotonic not_before boundary excludes startup and waits crossing arming time; this is a time gate, NOT proof gameplay began. Invalid backwards wait increments metadata; zero threshold disables trigger. First trigger remains immutable. Canonical runtime arming/clock reuse/export still pending.
- Focused O2ASanUBSan plus fullsuite74876 FINISHED0/check-r256.log ends Repository safety checks passed. Tests disabled bypass, below/equal threshold, pre/cross arming waits, invalid time, first-trigger retention, ring order, missing graphics response and100 joined CPU/graphics request-between-notification-boundaries cases. Tests do not prove arbitrary scheduling/observer overhead.
- Next integrate trigger into recorder/export with explicit trigger/arming/graphics-response/overwritten metadata and runtime arming configuration. Reuse IdleWaitTiming's start/end instead of the old extra CPU clock samples; wall-only graphics observations suffice for notification-to-resume timing, with CPU validity false unless actually sampled. Preserve normal disabled behavior; no unchanged first-N captures. No build/runtime/Simulator or accepted app change; fullPRD/G6 open/mobileheld.

## 2026-09-07 — R255 bounded recent-event buffer

- Prior turn progress: R254 proved first-N completion recording fills before gameplay; rejected rather than treating missing events as absence of delay. Added isolated recent-completion-events.h (not vendor/runtime wired), fixed32768 ring retaining latest observations with overwritten count and chronological At access. Owner-thread Freeze stops writes and clocks; Configure resets only before writers/after joins. No live cross-thread array reads/copies.
- O2ASanUBSan tests cover repeated wrap/order/kinds, overwrite counts, freeze immutability, disabled/frozen clock bypass and restart. Threaded test publishes an atomic freeze request; writer records its final paired boundary and freezes itself; reader waits for join. This tests storage/ownership, not full production trigger protocol or sampling overhead. Focused/fullsuite69855 FINISHED0/check-r255.log ends Repository safety checks passed.
- Next recorder-level trigger protocol: CPU records wait endpoint then requests graphics-owner freeze; preserve final paired graphics boundary and metadata when no post-trigger event occurs. Provide explicit gameplay arming scope so startup waits cannot freeze the capture early. Reuse existing idle timestamps/minimize per-event clock overhead; test races/wrap/coverage before canonical integration. Accepted app and existing vendor headers unchanged, no build/game/Simulator; G6/fullPRD open/mobileheld.

## 2026-09-07 — R254 completion capture rejected: event rate exceeds first-N buffer

- Prior turn progress: candidate/analyzer prepared. Runtime76991/PID87374, readytitle focus/load91137 FINISHED0; fixed120s input2118 FINISHED0,345659166192000..345779166192000. Before/end screenshots normal Observatory/distinct Mario position/no warning. No midwindow screenshots/phase trace/heavy profiler. Timing files absent live. CloseAX1/runtime76991 FINISHED0, both exports1/native5364183575/fallback0/SMCfailed0/save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged.
- Completion capture INVALID for gameplay window: CPU first32768 events span345515057022750..345515716158875, only0.659136125s; graphics first32768 span345514719281541..345519184583458,4.465301917s. CPU dropped28608766/graphics27943040 later events. Analyzer correctly rejects retained stream coverage; empty completion-summary.json is not a result. Observer records completion far more frequently than VI, so no unchanged repeat or merely modest buffer increase.
- VI independently retains full120s7191VI=59.925Hz, max27.719292ms wall/22.515125CPU/1.111667EFB/0.001291throttle/0.316904idle; no53ms idlehitch in this window.2801 VI drops occur later. Whole324.2s46underruns11backlogs not audioacceptance. Instrumentation overhead not established; cannot infer improved behavior from absent hitch.
- Next redesign capture scope as bounded recent-event flight recording frozen on a long CPU idle wait, with separate writer ownership and explicit publication/retention boundaries. Reuse existing idle wall timestamps where possible, avoid duplicating expensive per-event clock calls, and test wrap/freeze/races before integration. Do not allocate enough for tens of millions of events or repeat first-N boot capture. Accepted app untouched, no runtime/build/Simulator. G6/fullPRD open/mobileheld.

## 2026-09-07 — R253 completion candidate and analyzer ready

- Prior turn progress, integration and regression passed. Original69922 revalidated live137/196 then FINISHED0. Isolated completion-r253/GalaxyPad.app copied accepted, replaced runner, deep signed/strict verified56660 FINISHED0. Runner feddd3538b426c1c7a35584c3fa582ab0e544470a8b37e21cd18df033234589d; module unchanged5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d. Accepted runner independently verifiedaffbcb51f27f853b0b5ee403b5033c3bd5a6f7ea3e514b969c2ef4272af9e295.
- Added read-only summarize-completion-timing.py: requires both retained streams span window, per-stream nondecreasing wall times and alternating boundary kinds; reports longest wholly-contained CPU waits with surrounding notification observations and valid CPU deltas. Does not infer causal one-to-one pairing or exact DONE publication. Focused coverage/boundary/invalid-clock tests pass and added suite. Fullsuite57844 active/check-r253.log; follow same handle.
- Disposable completion-r253 profile prepared from accepted-r238 Config/config.ini and original midblock-r145 Wii, pipe configured/save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 verified. No runtime launched yet.
- Next after suite terminal: launch isolated candidate with GALAXYPAD_VI_TIMING=profile/vi.csv and GALAXYPAD_COMPLETION_TIMING=profile/completion.csv, phase trace unset. Ready title/focus/real save/active fixed120s/endvisual/cleanclose. Audit BOTH event stream capacities/window before analyzing notification vs CPU resume. If event rate exhausts buffer before gameplay, fix bounded capture scope/capacity, not claim absence of waits. No mobilebuild; G6/fullPRD open.

## 2026-09-07 — R252 completion runtime integration

- Prior turn progress: recorder/export tested and both-join boundary verified. Canonical Dolphin0017 embeds exact completion-events/recorder/session headers plus tested BlockingLoop observer; CoreTiming Idle records CPU begin/end around exactly one FlushGpu; Fifo installs graphics notification callback only if recorder enabled. Runtime0019 configures independent GALAXYPAD_COMPLETION_TIMING before boot and exports/reports in existing post-Shutdown helper. No changes to synchronization state/order, module code or stable settings.
- Patch pins: ModernGekko0019 SHA01c931eb6103d5e0c0437231ee386a6ed5802dfca5c5c574dcd5006178e71186; Dolphin0017 SHAcba14f51f95284ab097a1b0d5aef6f99bb8dd6571bd7b7e8b39efd56bc5933c4. Bootstrap peel/restore order extends existing overlays; first/repeat36795 FINISHED0. Completion header parity/hash/opt-in/endpoints/export checks and VI wiring pass. No direct vendor edits.
- Fullsuite then isolated macOS runner build sequential69922 active: generated/check-r252.log and generated/build-completion-r252.log. Follow same handle, no bootstrap/second build/runtime until terminal. Accepted app/module untouched, no mobile build.
- Next after build: isolated sign/verify and bounded capture with both VI+completion opt-ins; check per-stream event rates, dropped counts and retained window before attributing notification-to-resumption gap.32768 events perstream may fill sooner than VI; incomplete coverage is not negative evidence. DONE publication precedes notification observations; compare boundaries without assuming exact one-to-one wait/notify mapping. FullPRD/G6 open/mobileheld.

## 2026-09-07 — R251 completion recorder and export

- Prior turn progress: real BlockingLoop observer seam passed enabled/disabled tests. Added isolated completion-recorder.h, not yet vendor-wired. Heap allocation only at opt-in Configure; separate CPU/graphics append storage and immutable shared configuration while writers run. Production steady/current-thread CPU clock records validity. Exclusive-create one CSV with separately ordered streams and per-stream drops, idempotent post-both-join export and explicit file errors. No clock/file activity when disabled.
- Focused35696 FINISHED0 O2ASanUBSan includes two real joined writers, no file before export, invalid injected CPU clock, boundary names, production clock paths, post-finish suppression, restart/no overwrite, missing-directory open error and artificial8byte RLIMIT_FSIZE buffered write/fclose failure with signal/limits restored. Fullsuite5454 launched/check-r251.log; follow same handle.
- Join provenance rechecked Core.cpp: EmuThread owns video_guard at568; CPU function runs on same emulation thread at642; scope guard stops GPU loop and joins graphics at457 (failure path462); Core::Shutdown joins emulation at836. Thus existing Runtime post-Shutdown helper can export both buffers. Do not export merely on CPU idle return or hook removal.
- Next canonical integration: Common headers for recorder/events, explicit GALAXYPAD_COMPLETION_TIMING Configure before boot, CPU idle lambda WaitBegin/WaitEnd, optional Fifo completion observer BeforeNotify/AfterNotify, export/report from existing post-Shutdown helper. Preserve old accepted package and separate VI opt-in. No runtime/build/Simulator; fullPRD/G6 open/mobileheld.

## 2026-09-07 — R250 notification observer seam regression

- Prior turn progress: bounded per-writer buffers tested. Added isolated completion-observer.patch against pinned Dolphin Common/BlockingLoop.h. Optional noexcept function pointer/context observes before/after existing normal completion m_done_event.Set; no calls on busy payload iterations and null default for all existing callers. Original state decrement and event Set order preserved; DONE may become visible before observation. Shutdown notification intentionally not instrumented.
- test-completion-observer.py reconstructs headers from pinned13e492094902644b0d113c586300d358640f9e19 in temporary directory, applies candidate and compiles O2ASanUBSan.100 enabled+100 disabled threaded runs exercise payload gate, Wait/Wakeup, paired observer ordering and Stop/join,20s harness timeout.86687 FINISHED0. Initial patch missing trailing context failed apply-check; corrected before successful test, vendor unmodified.
- Fullsuite2771 active/generated/check-r250.log; follow same handle. No runtime/build/Simulator; accepted app/module unchanged. Next wire isolated observer to bounded graphics events and CPU idle endpoints, opt-in only, exclusive export after both threads join; canonical patch scope/provenance and actual runtime remain unverified. G6/fullPRD open/mobileheld.

## 2026-09-07 — R249 bounded two-writer event storage

- Prior turn progress: completion source audit ruled out the100ms CPU-timeout interpretation. Added isolated completion-events.h and sanitizer tests, not wired to vendor/runtime. Distinct CPU/graphics buffer instances, per-writer append only, no cross-thread reads until both join. Fixed32768 events each, explicit dropped count, disabled/full paths skip clocks. Records preserve wall/threadCPU/validity and WaitBegin/WaitEnd/BeforeNotify/AfterNotify boundary kinds.
- Important measurement limit: BlockingLoop publishes DONE before signaling event, so CPU may return before observer timestamp. Record notification boundaries as observations, not exact proof of state publication time or a one-to-one wait/notify pairing. Multiple Wakeup calls can require repeated wait iterations. Preserve original event order; no new lock/shared append/cross-thread mutable read.
- Tests exercise distinct joined writers, boundary kinds, CPU validity, capacity retention/drop, no clocks disabled/full, and reset. Focused tests plus fullsuite launched sequentially on98179 (check-r249.log); follow same handle. No product/source-vendor change/build/runtime/Simulator; accepted app untouched.
- Next add opt-in observer at actual completion notification boundary (not every busy payload), CPU wait begin/end, and exclusive post-both-join export. Determine event rate/drop completeness before interpreting; late notification vs late CPU resume then narrows53ms wait. G6/fullPRD open/mobileheld.

## 2026-09-07 — R248 graphics completion source audit

- Prior turn progress: R24753.4ms idle wait reproduced. Read Fifo worker payload, BlockingLoop state/completion transitions, Event flag/mutex protocol and async EFB cache refresh. Updated VI-WAIT-AUDIT.md with exact implications and next discriminating measurement.
- Ruled out misreading `Run(...,100)` as CPU completion timeout: it is the graphics worker's idle new-work wait. Event Set uses flag plus predicate mutex before notify, so no obvious source-level lost-notify defect found; not proof against scheduling delay. Worker returns only after queued events/FIFO decode/draw Flush/RefreshPeekCache; CPU EFB counter does not cover that whole path. Async cache population deliberately skips staging completion wait, so no blanket synchronous-readback claim.
- Next bounded worker-completion and threadCPU/wall correlation to split late worker completion from delayed waiter resumption. Review observer overhead and thread join/read ownership before integration; do not instrument each busy-loop iteration without a bound. No timeout/priority/event/graphics policy changes, no new runtime. R245 fullsuite remains current; accepted app unchanged, no build/game/Simulator, G6/fullPRD open/mobileheld.

## 2026-09-07 — R247 hitch reproduced inside idle graphics completion wait

- Prior turn progress, first instrumented run did not reproduce large hitch. One planned samecandidate repeat now does. Fresh idle-repeat-r247 Config/config.ini from accepted-r238 and original one-star Wii; runner remains0b6bd1bf...36e209/module5c21...e91d. Runtime26644/PID82910, focus/visible readytitle/load73524 FINISHED0. Active16cycle input17236 FINISHED0; before/end screenshots normal Observatory/distinct Mario position, end hanging ledge. No midwindow screenshots/phase trace/heavy profiler.
- Complete120s344202943468583..344322943468583 retained,7188VI=59.9Hz/7187intervals, zero invalid CPU/reset.344 drops only AFTER window (lastretained344362924946750). Largest interval at56.101226958s:74.399875ms wall,16.720458ms CPU,0.325333ms EFB,0.001208ms throttle, **53.444174ms idle FlushGpu elapsed**. This directly localizes most of this hitch to the idle-to-graphics-completion call, unlike the previous uninstrumented95.9ms event. Elapsed includes scheduling delay; not proof GPU compute took53ms or a lost wakeup. Following interval34.714584wall/24.02875CPU/0.465212idle remains distinct.
- FreshAXclose1/runtime26644 FINISHED0/export1/native4729596192/fallback0/SMCfailed0/save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged. Whole279.4s3under8backlog not fixedwindow audioacceptance. Evidence idle-repeat-r247/{runtime.log,window.log,vi.csv,summary.json}. No source change; R245fullsuite latest. No runtime/build/Simulator remains.
- Next inspect graphics BlockingLoop completion and worker payload path: determine long GPU work versus worker/waiter scheduling/wakeup latency. Preserve SyncOnSkipIdle and required FIFO synchronization. This is causal boundary localization for one reproduced hitch, not a fix for sustained CPU-heavy slowdown or historical22FPS. No more identical attribution runs needed at this boundary. FullPRD/G6 open/mobileheld.

## 2026-09-07 — R246 idle-wait candidate capture, large hitch not reproduced

- Prior turn progress. Same82837 revalidated live68/188 then FINISHED0; fullsuite check-r245.log passes. Candidate generated/macos/idle-wait-r246/GalaxyPad.app copied accepted, replaced runner, signed/strict-verified32143 FINISHED0. Runner0b6bd1bfbe4d56a4be7b45d4936d82cea8a7b2ab58cadbdf768760407036e209; module5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d unchanged.
- Disposable profile idle-wait-r246 uses accepted-r238 Config/config.ini and original one-star NAND. Runtime95167/PID82187, focused wrist-strap then verified title before real-save load59888 FINISHED0. Before/end screenshots show normal Observatory and distinct Mario location, no remote warning. Active input14477 FINISHED0; fixed120s343790135879458..343910135879458, finished343910138509250. No midwindow screenshots/phase trace/heavy profiler; CSV absent live.
- Closed AX1, runtime95167 FINISHED0; export1,16384 retained and940 drops AFTER requested window (last retained343942728162250), so selected120s complete. Analyzer7186VI=59.883333Hz/7185intervals, zero invalid CPU/reset. Largest22.661041ms wall/20.827708CPU/0.283334EFB/0.001292throttle/0.331872idle. No80–96ms hitch reproduced. Max idle elapsed among all selected intervals1.174752ms (wall17.914042/CPU15.338166), total1937.679004ms; therefore idle counter is active, not stuck at zero. These results do not rule out intermittent idle waits in a different run or prove instrumentation improves performance.
- Native4879941916/fallback0/SMCfailed0, disposable save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged. Whole292.4s34underruns9backlogs not audio acceptance. Evidence idle-wait-r246/{runtime.log,window.log,vi.csv,summary.json}; accepted app untouched. No runtime/build/Simulator remains.
- Next one bounded repeat with SAME counter-equipped candidate to seek the missing large-hitch event; distinguish reproduction failure from excluded cause. If still absent, park isolated hitch attribution and return to reproducible sustained CPU-heavy movie path, not endless normal-FPS loops. G6/fullPRD open/mobileheld.

## 2026-09-07 — R245 idle wait recorder integration

- Prior turn progress: isolated timing primitive and fullsuite passed. Wired CPU-owned IdleWaitTiming in PerformanceMetrics, configured before boot from VI recorder opt-in, wrapping only existing CoreTiming::Idle FlushGpu. Exactly one wait retained; no synchronization policy or module changes. Ordinary metric Reset does not erase instrumentation session configuration.
- ModernGekko0018 SHAfd268ed85ae8ad14624ac402cc399b9a38f440b116a2bfa44322e30490c75967; Dolphin0016 SHA63970dcc657af8239d2ccf64da74cf88395721e5aaec945fedf88f9388e776c1. Bootstrap pins/scope-audits both and peels/restores in dependency order. First/repeat2339 FINISHED0; focused O2ASanUBSan, header parity, opt-in/boundary/hash tests pass. Initial patch path typo caught by apply-check and corrected before bootstrap; no vendor direct edit.
- VI buffer/export gains idle_wait_elapsed_ns sixth column. Tests cover retention/reset/CSV value; analyzer reports elapsed delta or null for reset/missing historical column, not zero. Existing five-column R242 evidence remains readable. No inference that these elapsed counters are exclusive off-CPU states.
- Fullsuite then isolated macOS runner build launched as one sequential command: generated/check-r245.log and generated/build-idle-wait-r245.log. Follow returned handle; no bootstrap or other build while active. Accepted app/module untouched, no game/Simulator/mobile build. Next verify build terminal, isolate/sign candidate, repeat active120s route and test whether idle wait tracks the long gap. FullPRD/G6 open.

## 2026-09-07 — R244 idle-wait timing primitive

- Prior turn progress: identified active idle-to-FIFO path and documented gates, not runtime attribution. Added isolated patches/experiments/idle-wait-timing.h, not wired to vendor or packaged. Single CPU-thread elapsed accumulator; Configure resets/enables before writer start. Disabled executes original callback once without clock reads; enabled measures around exactly one callback, saturates overflow, ignores backwards injected clocks, propagates callback errors. No locks, file I/O or allocations.
- tests/idle-wait-timing.cpp runs O2ASanUBSan via existing timing harness;50700 FINISHED0. Tests default-disabled clock bypass, enabled repeated accumulation, joined writer, reset, backwards clock, saturation, production clock and exception propagation. Fullsuite launched generated/check-r244.log; follow returned handle.
- Wiring decision: own IdleWaitTiming in PerformanceMetrics CPU-only section, configure from Runtime::Run before boot using same VI opt-in, wrap only CoreTiming::Idle's existing FlushGpu call. Actual PerformanceMetrics::Reset call found Core.cpp299 on CPU-thread startup, so keep instrumentation session configuration separate from ordinary performance metric reset. Extend VI sample/export/analyzer with idle elapsed, then canonical patch/pin/bootstrap twice before isolated build. Do not time any-thread Fifo::ExitGpuLoop/shutdown calls.
- Accepted app/source vendor unchanged, no runtime/build/Simulator. This helper is not hitch attribution or a performance fix. FullPRD/G6 remains open, mobileheld.

## 2026-09-07 — R243 active idle-to-FIFO wait route identified

- Prior turn progress: R242 complete capture narrowed95.9ms hitch beyond measured EFB/throttle. Read-only source/config audit now distinguishes SyncGPU=false default from independent FlushGpu wait. docs/VI-WAIT-AUDIT.md records exact gates/callers and evidence limits.
- StaticRecompCore_Run165 calls CoreTiming::Idle for configured0x804ab358. SyncOnSkipIdle defaults true/no captured override; Idle574 calls FlushGpu, which waits in ordinary dual-core non-deterministic mode. Therefore SyncGPU=false does NOT rule out CPU waiting for graphics completion. Other GatherPipeBursted caller and DVD result wait remain possible. R237 CPU sample has3 BlockingLoop::Wait samples separately from EFB futures; not aligned causal proof.
- Next opt-in cumulative elapsed around CPU idle FlushGpu only, sampled by VI recorder; preserve exactly one FlushGpu call and disabled clock bypass. Avoid any-thread/shutdown instrumentation and do not disable synchronization. Candidate test/build required before runtime; no product change this turn, R241 suite remains current. No game/build/Simulator; fullPRD/G6 open/mobileheld.

## 2026-09-07 — R242 hitch attribution excludes EFB and throttle in largest gaps

- Prior turn: progress, build/test completed and isolated candidate prepared. Runtime15251/PID78579, focused ready title then real-save load83500 FINISHED0. Active input85354 runs16 movement/jump/Spin cycles then waits fixed120s, start342746776607875/end342866776607875. Before/end screenshots show Observatory and distinct Mario location (end hanging from ledge); no guest remote warning. No midwindow screenshots, phase trace or heavyweight profiler. Timing file absent while live.
- Closed via fresh AXclose1; UI read timed out after close, but original runtime15251 FINISHED0 confirms terminal. Export_result1, dropped0. New analyzer summary.json:7173VI/120s=59.775Hz,7172 complete intervals, zero invalid CPU/reset intervals. Largest gap at77.225383s:95.934541ms wall,20.374625ms threadCPU,0.401625ms EFB,0.001374ms CPUthrottle. Second at68.328449s:78.746042ms wall,23.397083ms CPU,0.255291ms EFB,0.002376ms throttle. These measured EFB/pacing paths cannot account for the large nonexecuting spans. This does not distinguish other blocking waits from descheduling or prove sustained historical22FPS solved.
- Native4079424824/fallback0/SMCfailed0; save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged. Whole246.5s49 audio underruns/10backlogs is not audio acceptance. Candidate runner77d595cd...e0e73f/module5c21...e91d; accepted package unchanged. Evidence generated/runtime/vi-counters-r241/{runtime.log,window.log,vi.csv,summary.json}. No source changes this turn; R241 fullsuite current.
- Next inspect active CPU-side FIFO/other wait gates and source call graph before extending attribution; do not disable synchronization. Specific candidates FifoManager::WaitForGpuThread m_sync_wakeup_event and deterministic-only SyncGPU m_gpu_mainloop.Wait must be checked against actual configuration. No more identical FPS-only loops. No runtime/build/Simulator remains; iOS/iPadOS held until performance great, fullPRD/G6 open.

## 2026-09-06 — R241 attribution candidate ready

- Prior turn: progress. Original build54832 revalidated live76/188 then FINISHED0; no restart. Isolated generated/macos/vi-counters-r241/GalaxyPad.app copied accepted app and replaced only runner, deep ad-hoc signing/strict verification58815 FINISHED0. Runner SHA77d595cd498f2ecab2533bfb4abd291744804718e648b9720837b62748e0e73f; module remains5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d. Accepted runner independently rechecked affbcb51f27f853b0b5ee403b5033c3bd5a6f7ea3e514b969c2ef4272af9e295, unchanged.
- Added read-only summarize-vi-timing.py: requires complete retained window and increasing wall timestamps; reports VI frequency and longest wholly-contained intervals with CPU/EFB/throttle durations. Invalid CPU clocks and counter decreases yield null instead of false attribution. No summing elapsed counters as exclusive states. Focused boundary/order/incomplete-window/reset tests pass and added to repository suite.
- Disposable generated/runtime/vi-counters-r241 uses accepted-r238 Config/config.ini and original midblock-r145 one-star Wii NAND, pipe configured; save SHA5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6. No runtime launched yet. Full suite79768 active/check-r241.log; poll same handle before runtime work.
- Next launch this candidate with GALAXYPAD_VI_TIMING pointing to profile/vi.csv and GALAXYPAD_PHASE_TRACE unset; wait loaded title, focus, load real save, capture fixed active120s before/after visuals without midwindow screenshots, close, then attribute longest intervals using the new analyzer. Keep one game/zero Simulators. No iOS/iPadOS build or performance claim; full PRD/G6 open.

## 2026-09-06 — R240 bounded wait attribution integration

- Goal remains macOS performance/stability before any iOS/iPadOS build; full original PRD retained. Hypothesis: VI wall/thread-CPU gaps can be narrowed using existing cumulative EFB elapsed and CPU throttle elapsed without a heavyweight trace or changes to pacing/depth semantics.
- Added both counter snapshots to the opt-in memory-only recorder; disabled guard precedes reads. Export remains exclusively after emulation-thread join. EFB/throttle elapsed are not disjoint pure off-CPU categories; do not blindly subtract them from CPU time. Counter decreases after reset must invalidate that interval in analysis.
- Canonical ModernGekko0017 SHA c5a2da36a4de45e3a6c440ccd03593971649f9109f0cafa64cdae81536a02b6e; Dolphin0015 CPU-thread-only getter SHA 15ae34e6ad58c5d3995aa1b8591b041f5bc405ddc7ab901366bcac3d8a47f68f. Bootstrap includes hash/scope audit; first and repeat pass. Focused recorder sanitizer tests and wiring/parity pass. Full suite17291 FINISHED0, generated/check-r240.log ends Repository safety checks passed.
- Accepted app/module unchanged; no mobile build, game or Simulator launched. Next isolated macOS runner build and visible active-play capture with the new counters. This is diagnostic integration, not a performance fix or acceptance claim.

## 2026-09-06 — R239 accepted/candidate hot-path binary audit

- Prior turn: progress, accepted sequentialcontrol59.9–60 vs priorcandidate48–52, no causalregressionclaim. No runtime launched. CurrentbuildRelease/-O3 confirmed; signedmodulesboth5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d. Accepted/candidaterunners remainisolated.
- Targeted llvm-objdump -d --disassemble-symbols comparison: StaticRecompCore::Run both540instructions/sameopcodeorder; HookExternalRead+HookExternalPointer both198/sameopcodeorder. Differences primarilyPC-relativeaddresses/annotations. Remaining5Run+3hookoperanddifferences are address-add lowimmediates. Read Mach-Osection mapping and precedingADRP pagevalues:7nonempty NUL-terminated referencedbyte sequences match,1emptysequence inconclusive. No claimed fullsemantic equivalence, call-target/dataflow audit, cachelayout equivalence, or performanceproof.
- Tool correction: initial --macho --disassemble ignoredsymbolselection, emitted broad149MiB/42MiB outputs. Verified and terminated ownedllvmobjdumpPID75284; oldsession15905 terminal. Replaced only these ownoutputs using bounded generic-d mode; finalRun546lines each and smallhook outputs. No unrelated files/processes touched.
- Evidence generated/runner-compare-r239/{accepted,candidate,accepted-hooks,candidate-hooks}.asm. No obvioushotexecutioncode-work change found; this does not explain intermittentCPUthroughputdifference. Stop inferring candidatebug from sequentialFPSsnapshots alone.
- Next low-cost attribution: add cumulativeexistingEFB elapsed and actualCPUthrottle elapsed snapshots to boundedVI recorder, disabledguard before extra reads. ExistingEFBtimer wrapsblockingdepth path; throttle alreadymeasuresSleepUntil. These categories include elapsedtime, not pureoffCPU sums; avoid double-counting withCPUtime. Preserve allrequiredsynchronization anddepthsemantics. No new heavyweightprofiler/unchangedFPS trial. No game/build/Simulator; R235fullsuite10681 latest/no productsourcechange, fullPRD/macOSfirstgoal active.

## 2026-09-06 — R238 accepted-runner control

- Prior turn: progress, sustained48–52FPS captured with candidate recorderdisabled and smallCPUsample. Fresh accepted-r238 copiesR237Config/config.ini and originalone-starWii; runs acceptedapp with both diagnosticenvvars explicitlyunset. Runtime13244/PID74905, focusedtitle then load72123/movement52711 FINISHED0. Before/afterObservatory screenshots59.9–60FPS/distinctMario position. No candidatepromotion.
- This control did not reproduce sustainedslowdown, but is shorter/sequential and no matchedthermal/corefrequency record exists; R236samecandidate also59.9Hz. Therefore cannot yet attribute difference to code or hardware. Hostps accepted133.4%CPU/WindowServer42.4/Logitech30.7/JumpConnect8.0 broadly similar to prior snapshot but not equivalent workload proof. Do not blame or stop unrelated apps.
- Live configuration checks: GFXfiles byteidentical, currentdesktop CMakeRelease/-O3 -DNDEBUG/compiler/usr/bin/c++; CoreCPUThreadTrue/CPUCore6. pmset currentAC100%charged, lowpowermode0 forbothAC/battery. This rules out currentlowpowermode setting, not transienthistoricalpower/thermal effects or compileridentity equivalence.
- CleancloseAX1/runtime13244 FINISHED0/fallback0/SMCfailed0/NAND5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged. Whole225.53s2underruns6backlogs not fixedwindowaudio acceptance. No productsourcechange; R235fullsuite10681 remainslatest. No game/build/Simulator.
- Next recorded build/runtime identity comparison plus bounded matched cadence withlivehost evidence before additional diagnostics. Acceptedrunner staysaffb...e295, candidate02b0...cb51 remainsisolated. Keep sustainedCPUwork and isolatedoffCPUhitch as distinct problems; iOS/iPadOSexplicitlyheld until macOSperformancegreat, originalPRD/fullgoal open.

## 2026-09-06 — R237 disabled smoke and sustained slowdown sample

- Prior turns: progress interrupted by user status/priority messages; same runtime53929/PID73449 confirmed live, no restart. It was launched with env-uGALAXYPAD_VI_TIMING/env-uGALAXYPAD_PHASE_TRACE using candidate02b0...cb51 app/profilevi-disabled-r237. Load72962 completed while window unfocused but title remained; focusedAX0 and retried86250 successfully, movement63896 completes with distinct visible position. No claim focus is all historical load failures' cause.
- During disabled smoke screenshots show Observatory52.1 then48.2FPS and laterAX49.2; no streamingdiagnostics. Short sample73449 1 10 saved slow-sample.txt (2228FINISHED0):77CPUthread samples,60underchassis_dispatch including3floatfuturewait; video46/77conditionwait,2Metaldepthflushwait. Small statistical snapshot supportsCPU-side sustained bottleneck, not exacttimepercentages or isolatedhitchattribution. Hostps game130.8%CPU,WindowServer40.2,Logitechupdater31.8,JumpConnect7.8; pmset reports no recorded thermal/power status, NOT no-throttleproof. Other processes not stopped or blamed.
- Wait-path audit: CoreTiming::SleepUntil measures actualCPU sleep via CountThrottleSleep; Throttle calls it aftertarget/drift logic. EFBInterface PeekDepthInternal uses AsyncRequests::PushBlockingEvent future.get; existing s_efb_peek_ns records totalduration. Fifo SyncGPU wait is deterministicmode-only; WaitForGpuThread is governed by configured syncGPU path. Do not instrument/disable every namedwait as though allactive; preserve requiredpointerdepth.
- Closed freshAX1; runtime53929 FINISHED0/fallback0/SMCfailed0/NAND5040...64a6 unchanged. No timingfile/export message (disabledpath validated). Whole353.5s412underruns14backlogs not audioacceptance. Source unchanged; R235suite10681 latest. No game/build/Simulator.
- Next compare acceptedaffb...e295 runner in sameprofile/scene conditions before excluding diagnosticrebuildregression; candidate not promoted. This is a distinct requiredcontrol for new sustained48FPS observation, not anotherunchangedtimingloop. Keep isolatedhitch/nonexecutingtime separate from sustainedCPUwork. User explicitly reiterated no iOS/iPadOS build until macOSperformancegreat; original fullPRD remainsactive.

## 2026-09-06 — R236 lightweight recorder runtime and hitch evidence

- Prior turn: progress, canonicalpatch/build launch. Same14159 polled live then FINISHED0, no restart/bootstrap. Created separate generated/macos/vi-timing-r236/GalaxyPad.app from acceptedapp, replaced only runner with newlybuilt artifact and adhocdeep signed/verified. Signedrunner02b03bcc176bd84a9e6d98aaf0b71e4b74afeaccacb91bb4bfd0c11d0b31cb51; acceptedpackagedrunner remainsaffb...e295. This is separate runner build, not proven one-variable performance comparison with old app.
- Fresh vi-timing-r236 configs/disposableone-starNAND, no savestate. Runtime4197/PID72984 with GALAXYPAD_VI_TIMING=.../vi.csv only; phase/SystemTrace off. Ordinaryload25984 then16 smoke sequences28373 FINISHED0. Before/end screenshots show Observatory/distinctMario positions/no interruption; input started340472883492916 rawns. Verified file nonexistent twice while running. Endclose freshAX1; runtime4197 FINISHED0, export_result1/fallback0/SMCfailed0/save5040...64a6 unchanged.
- CSV16384records, allCPUclocksvalid; capacitydropped2055 laterrecords. Selected120s end340592883492916 precedes last retainedsample, so selectedwindow complete despite laterdrops.7188VI=59.9Hz. Largest interval wall61.630834ms/CPU25.207917ms atelapsed4.671605959s; next37.638333/24.615583ms at4.709244292s. Next27.067/24.223583ms, then25.490291/21.278916ms. Longest includes~36.4ms nonexecuting CPU-thread time, not purely gamecompute; may be wait/descheduling, no specific cause assigned.
- Whole311.6s50underruns8backlogs includesstartup/inputload and is not fixedwindowaudio acceptance. No runtime source/test change thisturn; R235suite10681 remainslatest. Next disabledcandidate smoke (stillunverified) and audit named throttle/GPU/EFB wait boundaries for offCPU part; no profilerretry/unnecessaryunchangedFPS loop. Do not promote yet. No game/build/Simulator, G6/fullPRD open.

## 2026-09-06 — R235 canonical recorder integration and runner build

- Build handle14159 verified live39/182. Poll same; accepted app remains unchanged.

- Prior turn: progress, tested recorder clock/export layer. Added canonical patches/ModernGekko/0017-vi-timing-recorder.patch embeds exact two tested headers and modifies only runtimeowner/config/VIcallback/postjoin exporter. SHA650db467b1839e43dc1fb0522f0fc8fd6f8ee648241db96a28aebdc0e05b6f46. Initial callback patch hunk lacked trailingcontext; corrected before applying, gitapplycheck passes.
- GALAXYPAD_VI_TIMING absent/empty remains disabled; Configure beforeboot, Record inside existingVI callback. Old phase trace remains independently opt-in. Normal/destructor activeboot path exports afterCore::Shutdown joinswriter and reports Saved/OpenFailed/WriteFailed. No game timing/renderer/input behavior intentionally changed.
- Bootstrap peels0017 first, restores/applies after dependencies, pins hash and audits scope including headers.27236 initial/69492 repeat FINISHED0. Newtest-vi-timing-wiring verifies exactheaderparity/pin/optin/two postjoinflush sites. Fullsuite10681 FINISHED0/generated/check-r235.log ends Repository safety checks passed.
- Packaged runner rehashedaffbcb51f27f853b0b5ee403b5033c3bd5a6f7ea3e514b969c2ef4272af9e295 unchanged. Started cmake --build generated/build/moderngekko-desktop --target moderngekko-run --parallel4, loggenerated/build-vi-timing-r235.log. Poll returned handle/no bootstrap/restart. This rebuild is not yet packaged or runtimeverified; next isolated enabled/disabled diagnostic smoke and exactartifact audit. No game/Simulator, fullgoal active.

## 2026-09-06 — R234 opt-in timing recorder and I/O failure tests

- Fullsuite28209 FINISHED0, generated/check-r234.log ends Repository safety checks passed; no active processes.

- Prior turn: progress, bounded buffer and thread contract. Inspected normal Run and destructor shutdown paths: both call Core::Shutdown after Stop; Configure belongs before emulation starts, flush after join, not callback removal. No runtime/vendor modifications yet.
- Added isolated patches/experiments/vi-timing-recorder.h. Empty/null path disables allocation and clock calls. Configure resets session, allocation occurs before writer. Record uses steady_clock and CLOCK_THREAD_CPUTIME_ID when available, invalid CPU clock explicitly flagged. Only single writer while active; FlushAfterJoin caller contract remains required. Exclusive fopen wx refuses overwrite, captures fprintf/fclose failures, repeated flush is idempotent, finished recorder rejects further samples until Configure.
- Added tests/vi-timing-recorder.cpp to existing O2ASanUBSan harness. Joined writer samples injected failed clock plus real clocks. Tests check disabledclock bypass, no file beforeflush, CSVvalidity/order, idempotence, failed-path/nooverwrite and restart. Added8byteRLIMIT_FSIZE with temporary SIGXFSZignore/restoration to trigger actual bufferedwrite/fclose failure without consuming disk.24905 initialpass,98367 expandedpass. Fullsuite launched check-r234.log; poll returned handle.
- Next canonical ModernGekko host integration/patch wiring, copy both local headers via bootstrap and verify provenance; add runtime owner/config/callback/postjoin export with diagnostics errors. Build separate runner and test disabled/enabled paths before accepted app promotion. No game/Simulator/build/product change, G6/fullPRD active.

## 2026-09-06 — R233 bounded diagnostic buffer and thread contract

- Fullsuite completion:31335 FINISHED0, generated/check-r233.log ends Repository safety checks passed. No active processes from this turn.

- Prior turn: progress despite profiler failure, scoped cleanup restored space and eliminated heavyweight capture route. Inspected current runtime/source, no game launched. VideoInterface end-field emits event adjacent to Core::OnFrameEnd (Core.cpp786 documents CPU thread); Core::Shutdown827–836 joins emuthread. Runtime normal exit calls Stop/Shutdown at709–710. Removing hook alone is not a thread publication fence; future output must occur afterjoin.
- Added patches/experiments/vi-timing-buffer.h: standalone fixed16384sample template, wall/threadCPU integers with explicit validity, first-window saturation with dropped counter. No timestamp/syscall/allocation/mutex/I/O inside recorder. One writer; reads/reset only outside running interval. No deltas inferred across invalid clocks. Default capacity ~384KiB depending struct padding, heap-owner needed for runtime; no callback-local large stack object.
- tests/vi-timing-buffer.cpp/test-vi-timing-buffer.py compile O2ASanUBSan and execute real joined-writer lifecycle/order/invalidclock/capacity/drop/reset cases;44399 FINISHED0. Added to suite,31335/check-r233.log active. This is buffer proof only, not timing accuracy, thread-affinity or runtime/performance evidence.
- Next canonical host patch: opt-in buffer allocated before emulation, VI callback reads steady wall and current-thread CPU clocks, callback records only when enabled, post-Core::Shutdown file export/error reporting. Cover disabled path/clock failure/output failure and destructor/restart paths before separate runner build. Preserve accepted runner/app. No game/build/Simulator; full PRD/G6 remain open.

## 2026-09-06 — R232 full-window profiler failure and scoped recovery

- Prior turn: progress, retention metadata audit. Initial4.2GiB free/no game/Simulator. Fresh scheduler-r232 normal-derivedconfigs/disposableone-star save/accepted app. Runtime31536/PID69177, readytitle then load90070 passes. Host86828 captures120s clockpairs/pressure; input2663 runs9 smoke cycles; both FINISHED0.
- Capture54174: xctrace System Trace --attach69177 --time-limit60s --window60s. Reachedlimit but finalization FAILED134 with uncaught system_error Could not expand file to add new page: No space left on device. No valid window/thread-state output or performance claim. Endgame screenshot normal Observatory/Marioledge60FPS; closedAX1/runtime31536 FINISHED0/fallback0/SMCfailed0/NAND5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged.
- Cleanup: initial failed trace scheduler-r232/system.trace verified4.0GiB. Removed exact new directory permanently (first force-style command rejected, safer rm-r succeeded). Space initially still114MiB. Read-only inspection found no snapshots/open trace handles/cores; older Instruments caches untouched. Found /var/folders/b1/t1t38j255k53b4j97j63gfkh0000gp/T/instrumentsTorrXu.ktrace, Apple Trace File4297646024bytes created23:01:28/modified23:02:47 matching this capture, no process holding it. Removed exact orphan file permanently; free space restored4.1GiB. No other temporary/cache files removed. Capture failure/runtime/phase/host logs retained; crashreport xctrace-2026-09-06-230325.ips retained.
- Heavyweight full System Trace PARKED; do not repeat with larger disk budget or another timing-only run. Next lower-overhead diagnostic: bounded in-memory wall/thread-CPU samples on VI CPU callback, no per-event filesystem writes, flush after execution stops. Inspect threading/clock contract and test buffer/ordering/failure behavior before accepted-runner change. This is diagnostic, not a speculative scheduling or renderer fix. R231fullsuite remains latest/no product source change. No runtime/build/Simulator left; G6/fullPRD active.

## 2026-09-06 — R231 retained-window discrepancy resolved

- Completion: fullsuite45123 FINISHED0/generated/check-r231.log ends Repository safety checks passed. No processes left by this turn.

- Prior turn: progress, scheduler evidence with timing limitation. Read saved metadata only; no new runtime or product changes. form.template NSKeyedArchive rawstart1788752866.391601/rawend1788752927.342171/rawWindowStart1788752917.342171/rawDuration10.0 proves60.95057s recording with last10s retained, not an unexplained early stop. TOC startdate must not anchor retained relative timestamps.
- Added scripts/audit-trace-window.py reads only timing fields, resolves archived UID references, requires finite ordered consistent endpoints/duration and refuses missing metadata. tests/test-trace-window.py covers references, discarded prefix, malformed ordering/duration/NaN/missing metadata; focusedpass, suitewired. Saved scheduler-r230/window.json. Avoid exposing captured environment.
- Exploratory alignment using earlier active-r229 paired wall/raw clocks predicts599VI/max23.735ms in retained10s. Its offset spans3.509083ms even during earlier120s and no R230 contemporaneous pair exists: extrapolation only, cannot prove exact event/state overlap. Strongly insufficient to explain70–90ms hitches. Do not turn mostlyRunning aggregate into causal claim.
- Next distinct corrected capture: explicit --window60s with60s limit, paired wall/raw clock anchors before/after, exact retained-window audit before interpreting scheduler versus phase hitches. Bound disk use and no midwindow screenshots; don't repeat default-window command. Fullsuite launched generated/check-r231.log after focusedpass; poll returned session. No game/Simulator/build, G6/fullPRD open.

## 2026-09-06 — R230 scheduler capture and observer boundary

- Prior turn: progress, hitches reproduced without midwindow screenshots. Current state no game/Simulator,5.5GiB available (not a disk blocker). Fresh scheduler-r230 copies same configs/disposableone-starNAND and accepted app. Runtime21544/PID68146 loads bundledmodule; ready title screenshot then ordinaryload78066 passes. No product change.
- xctrace record --template 'System Trace' --attach68146 --time-limit60s --output generated/runtime/scheduler-r230/system.trace; capture33451 FINISHED0. Input44620 executes9 control-smoke cycles, FINISHED0. Capture log says reached requested limit/saved, but authoritative TOC records only10seconds, start2026-09-06T22:47:46.392-05:00/end22:47:56.392. Do not describe as60seconds of scheduler evidence.
- End screenshot during save shows27.8FPS/normalObservatory; after capture exited, same scene screenshot60FPS with Mario idle animation. Transient profiler/host disturbance observed; ps snapshot includes game137.8%CPU,WindowServer33.3,spindump31,otherbackground activity. Not causally assigning each process or explaining historical22FPS.
- Closed freshAXclose1; runtime21544 FINISHED0/fallback0/SMCfailed0/NAND5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged. Whole275.19s84underruns15backlogs under profiler not normal performance/audio evidence.
- TOCexport70765/thread-stateexport97341 FINISHED0. system.trace344MiB,threads.xml84MiB; private data retained ignored. Read-only streaming parser12315 resolves XMLid/ref and selects CPU thread: Running8.973146960s,Blocked.793207052,Runnable.018488121,Interrupted.139760168,Preempted.075397699, total10s. Longest>=20ms state interval .990026166s Running atstart0. This alone neither locates a hitch nor establishes its source.
- Next align exact10second timeline with phase events and examine initial interval/completeness before interpreting wait/work or recapturing. No unchanged FPS loops, no new product fix, no remaining runtime/build/test/Simulator. R228 fullsuite remains latest (no source changes). G6/full PRD open.

## 2026-09-06 — R229 screenshot-free active comparison

- Prior turn: progress, fresh active hitch evidence. Preserved dirty tree, no initial game/booted Simulator. Fresh active-r229 copies same normal-derivedConfig/disposableone-starWii; module5c21...e91d/save5040...64a6 rehashed unchanged. Runtime1685/PID67592 automatic bundledmodule. Verified ready title before load83218, ordinary Observatory visible. No product/config changes beyond same diagnostic Pipe/phase setup.
- Input4432 executes same16 control-smoke sequences; host72776 records120seconds/5seconds. Both FINISHED0. No CUA/screenshot during fixed window337919247128041..338039247128041. Before/end screenshots show distinct Observatory positions, end Mario hanging from ledge with normal world/speech bubble, not remote warning. No assertion about every intervening animation.
- Allthree VI/begin/present7193events=59.941667Hz. Minute presentation59.95/59.933333Hz, p9918.4015/18.701375ms,max64.875875/78.475291ms,27gaps>=20ms. VI>=40ms atelapsed8.482/89.659/111.036s (68.832042/69.482875/58.334ms).24hostintervals zeroSwapins/Swapouts/Pageouts/Compressions. Compared R22829presentationgaps/max89.996ms: hitches persist without screenshots; no statistical speedup or zero-observer-effect claim.
- Closed fresh AXclose1; runtime1685 FINISHED0/fallback0/SMCfailed0/saveunchanged. Whole247.7s2underruns7backlogs not fixedwindowaudio. Evidence active-r229/{phase.csv,host.jsonl,cadences.json,runtime.log}. R228 suite57433 remains latest, no code changes this turn. No remaining game/Simulator/build/test.
- Next distinct question: CPU busy versus descheduled/blocked during a hitch. Local xctrace lists System Trace; inspect/perform bounded scheduler capture aligned to phase events, retaining real input and before/after scene proof. Phase trace is mutex/line-buffered diagnostic instrumentation, so consider its I/O waits rather than assuming every hitch is product cost. Do not repeat screenshot-free FPS run unchanged or declare historical22FPS solved. Heavy movie55FPS and G6/fullPRD remain open.

## 2026-09-06 — R228 active-play cadence and hitch evidence

- Regression completion:57433 FINISHED0, generated/check-r228.log ends Repository safety checks passed. No remaining runtime/test/build/Simulator.

- Prior turn: progress, distinct presentation/VI analysis and guest idle contract evidence. Current repo dirty state preserved, no initial runtimes/booted Simulators. App module5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d; disposable save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 copied with discovery-r222 normal-derived configs into active-r228. No app/source/runtime setting changes apart phase trace and disposable Pipe profile.
- Runtime81976/PID66727, automatic bundled discovery. Initial load53922 completed but title remained (sent too early); screenshot verified title then load95604 completed ordinary one-star Observatory entry. This is a test readiness issue, not save-load failure evidence.
- Input99312 ran16 existing g6-observatory-control-smoke sequences; recorder62588 ran120seconds/5second intervals. Both FINISHED0. Before/mid/mid/end screenshots show Mario at distinct Observatory positions, movement/dust, scene active and no remote interruption. Not hands-on full mechanics acceptance or full soak.
- Fixed window337515665936416..337635665936416: VI/frame_begin/present_done each7186events=59.883333Hz. Minute1/2=59.816667/59.95Hz. Presentation p99=19.240709/18.779458ms, max89.996/66.040542ms,29total gaps>=20ms. VI max92.502292ms; occasional hitches remain despite average near60.24hostintervals zeroSwapins/Swapouts/Pageouts/Compressions. Evidence active-r228/{cadences.json,host.jsonl,phase.csv,runtime.log}.
- Largest VI gaps at elapsed41.149691s and82.008559s overlap DMA gaps64.055/45.808ms and input70.0845/30.940125ms while audio callbacks stay~10.7ms. No sole-cause attribution; live screenshot observation overhead was not independently timestamped. Next matched active route with only before/after visual checks isolates midwindow observation as a variable, not unchanged idle retesting. Does not yet explain historical22FPS or solve heavy movie55FPS.
- Closed via fresh AX close1; runtime81976 FINISHED0, fallback0/SMCfailed0/NAND unchanged. Whole256.25s5underruns9backlogs includes startup/load, not fixedwindow audio acceptance. No game/Simulator. Full regression launched generated/check-r228.log after closure; poll returned session, do not restart. G6/full PRD remain active.

## 2026-09-06 — R227 idle contract and presentation-counter audit

- Petari WPAD.c640–677 resets lastUpdateTime when controller data changes and disconnects after unchanged input exceeds60*sleepTime; default5 at1440, named game helper chooses5/15minutes. Wrong-region symbols remain clues only. Exact accepted RMGE01 generated chunk1235 sets byte[r13-6966]=5 at804D767C/804D7680; chunk1234 loads/multiplies by60/compares elapsed time at804D6AF8–804D6B04 before disconnect-state logic. Consistent with ten-minute no-input test, not direct event/onset proof. Dolphin guest HCI CommandDisconnect calls EventDisconnect, distinct from host Activate(false). No product fix, artificial input, or disabled auto-sleep.
- Prior fullsuite32643 FINISHED0, generated/check-r227.log ends Repository safety checks passed. No runtime or booted Simulator. Accepted R212 package remains unchanged. No new gameplay smoke: this turn inspected existing source/evidence and changed analysis only; R226 recovery was last visible runtime boundary.
- Window FPS route: dolphin_runtime.cpp uses PerformanceMetrics::GetFPS; Core Callback_FramePresented counts after_present events. VI CountVBlank is separate. Updated summarize-scene-trace.py to expose VI/frame_begin/present_done independently, naming counts/Hz instead of calling VI ticks frames/FPS. Focused boundary/event-separation tests pass; fullsuite above predates this small analyzer change.
- Reanalyzed existing evidence, no new run: R223 cadences-r227.json allthree35919events/600s=59.865Hz; maxgaps VI107.013334ms/begin105.842083ms/present103.749ms. Interrupted-scene warning still invalidates gameplay claim. R210 movie allthree3319/60s=55.3167Hz. Neither trace shows sustained loss of presentation counts relative to VI, but neither explains historical22FPS or proves actual display scanout cadence.
- Next: bounded active-play route with meaningful movement/jump/Spin and periodic scene screenshots, no unattended stationary soak or fake keepalive. Compare presentation and VI rates in same labeled window alongside host pressure; preserve existing app and disposable saves. G6/full PRD remain open.

## 2026-09-06 — R226 sustained test reveals remote interruption

- Prior turn: verified wait. SAME75477 remained live through bounded waits then FINISHED0 at600s. Ten complete bins sum35919VI=59.865FPS,range59.4333–59.95,max107.013334ms,133gaps>=20ms.120hostintervals zeroSwapins/Swapouts/Pageouts/Compressions. Saved minutes.json; no inferred thermal pass.
- Crucial end screenshot shows guest Wii Remote communications interrupted overlay. No midrun screenshots, so onset unknown and later near60FPS cannot be interpreted as uninterrupted Observatory workload. This invalidates sustained-gameplay acceptance regardless of counters. Earlier perminute commentary reports raw cadence only, not continuing-gameplay proof.
- Added g6-remote-reconnect.json neutralNunchuk+Apress/release. Initial draft used wrong fixture schema; corrected from existing action fixtures before any execution.67503FINISHED0 clearswarning visibly, Mario/game resumes. Closed runtime69826FINISHED0/fallback0/SMCfailed0/NAND5040...64a6 unchanged. Whole879.6s16underruns14backlogs includes interruption/recovery, not audio acceptance.
- Next read emulated remote idle/disconnect and guest report contracts, identify cause and bounded fix or faithful test procedure; do not mask with fake input. Run deferred fullsuite after closure. No game/Simulator/build left, fullG6/PRD active.

## 2026-09-06 — R225 verified sustained-run wait

- Prior turn: progress, live minute analyzer. SAME recorder75477 andgame69826/actualPIDs64358/64499 live; bounded45swait nonterminal. No input/pause/restart/build.
- Minute3=59.7333FPS,p9919.804541ms,maxgap107.013334ms. First40hostintervals sumzeroSwapins/Swapouts/Pageouts/Compressions. VIgaps>=50ms atelapsed1.266/52.682/163.847/164.491s; sparse gaps do not establish cause or historical22FPS explanation. No premature10min claim.
- Continue to fixedend336533513367500 and recorderterminal, then fullanalysis/movement/close/NANDcheck. Fullgoal active, no blocker; single timedruntime is expected live.

## 2026-09-06 — R224 live sustained-window analysis

- Prior turn: progress, ten-minute test setup. SAME75477 active; actualgame64358/recorder64499 verified live, no restart. Scene remains untouched.
- Added summarize-scene-trace.py for fixedstart/complete60sbins only, VI frames/FPS/p99/maxgap/longgaps. Focused test-scene-trace.py checks half-open minute boundary, incompletewindow exclusion and trailingrow handling; passes. Added suite entry; fullsuite deferred to avoid concurrentheavy work.
- Firstcomplete minute3566VI=59.4333FPS,p9920.928959ms,maxgap94.993667ms,55gaps>=20ms. Not a10minresult or diagnostic cause. Read-only analyzer is lightweight and does not mutate runtime.
- Continue SAME runtime69826/recorder75477 until fixedend336533513367500. Final perminute/hostdelta analysis then movement/cleanclose/NANDcheck. No secondgame/Simulator/build. G6/fullgoal open.

## 2026-09-06 — R223 sustained scene test started

- Prior turn: progress, automaticdiscovery/normalsettings smoke. New sustained-r223 copies normalconfig-derived profile and disposableone-starWii; no savestates. Runtime69826 phase trace, automatic signedmodule discovery, native1x/Metal. Load16332 FINISHED0, Observatory visually verified.
- Started read-only hostrecorder75477 for600seconds/5secondinterval after scene ready. Firsthost monotonic_raw_ns335933513367500 defines fixed10min start; intendedend336533513367500. Keep game stationary/unpaused through fixedwindow; bounded movement and screenshot afterwards. Evidence runtime/sustained-r223/{phase.csv,host.jsonl,runtime.log}.
- Runtime69826/recorder75477 ACTIVE, onegame/noSimulator/build. Poll same handles; do not restart due observation timeout. Next minute-by-minuteVI/frametime correlation with paging/hostpressure, then movement/cleanclose/savehash. Fixedscene test does not prove fullgameplay or60minsoak and cannot by itself close G6.

## 2026-09-06 — R222 automatic module discovery

- Prior turn: progress, parkedFP experiment and normalconfig reconciliation. New discovery-r222 copies currentAppSupport Config/config.ini and disposableknownone-starWii; pipe adapted for repeatableinput. No inheritedSTATICRECOMP_MODULE, no explicit--module. No realuserNAND/config writes.
- Runtime54227 logs bundled gRMGE01_recomp.dylib; ordinaryload58445 reaches visible one-star Observatory. Native close exits0, NAND5040...64a6 unchanged. Whole115.2s2underruns3backlogs not sustainedperformance/audio pass. Evidence generated/runtime/discovery-r222/runtime.log. This verifies runner discovery, not a hands-on frontend flow audit.
- Next fresh ten-minuteObservatory run with phase+hostpressure5ssamples and bounded movement/snapshots; determine whether timing degrades after warmup or correlates with paging/contention. No builds/profilers concurrently. Stationary scene data cannot substitute for fullgameplay/60minsoak. G6/fullPRD open; no game/Simulator/build remains.

## 2026-09-06 — R221 FP routing parked; normal settings reconciliation

- Prior turn: progress, full oracle and benchmark launch. SAME14067 FINISHED0. ABBA aggregate24590.3/25868.7/24508.7/24697.2ns; firstcandidate noisy (firstrow2993ns vs~1500), no selective exclusion, secondcandidate only~0.55% belowcontrolmean. Text229376->245760. No robust benefit: parked without fullmodule build or unchanged repeat.
- Read-only normal ApplicationSupport/GalaxyPad configs: GFX.ini identical to R210; Dolphin Core/DSP fields equal exceptordering, generatedWirelessMac/AnalyticsID differ. config.ini Metal640x528, native1x set inGFX, CPUThreadTrue. Current settings do not explain historical22FPS, nor prove its cause absent earlier.
- Runner moderngekko_run.cpp299–312 chooses STATICRECOMP_MODULE ifset, else siblingbundled module beforeusercache. Explicit--module tests have not verified normaldiscovery at this promotion. No actual user save/config edited. Missing guessed GalaxyPad.log not treated as evidence.
- Next normal discovery with copied current configs/disposable knownsave and no explicit--module, followed by sustained gameplay cadence investigation rather than more tiny decoder tweaks. Preserve full goal and G6 performance/audio boundary. No game/build/Simulator remains.

## 2026-09-06 — R220 FP routing execution oracle

- Prior turn: progress, isolated source prototype. Extended existing oracle with opt-in --kernels --fp-routing, preserving earlier modes. Pins both candidate/reference hashes, normalizes private header include, tests actual complete chunks against runtime helpers. Added357directFPdisabled entry cases over119affectedPCs/3budgets; verifies exceptionvector andsavedCIA, hashes wholeCPU.
- Oracle85294 FINISHED0: all4reference/candidate O1ASanUBSan/O2 variants agree digestee02615b68d66d1e. Whole1440cases/624yields/480FPfaults/480illegal/192interruptions/96callerreturns/18816callbacks/34688journals/1440reservationclears, plus357directfaults. Evidence generated/fp-routing-oracle-r220.log. Not exhaustive arbitrary-state proof.
- Started separate --benchmark ABBA optimized/ThinLTO comparison, generated/fp-routing-bench-r220.log. No module/product edit or game/Simulator. Next poll this benchmark, assess timing and textsize before isolated candidate link. G6/full PRD open.

## 2026-09-06 — R219 checked-entry FP routing prototype

- Prior turn: progress, guarded memory lookup parked. Actual fp_available_inline tests global lazy enable andMSR_FP; raise takes exception atCIA and returnsfalse. Audited paired add/sub/mul/madd arithmetic bodies operate onFPR/FPSCR, not memoryhooks/MSR; no blind entry-check deletion.
- New source-pinned probe-fp-check-routing.py selects119 adjacent arithmetic-only edges within extracted kernels. Adds normalfallthrough nextPC assignment/goto fp_ready before originalnextlabel, and fp_ready immediately after originalnextcheck. All directlabel entries retain originalPC/check/fault path. Original generatedsource recovered byte-for-byte by reversing additions. No memory/branch/callback regions crossed.
- Private generated/fp-check-routing-r219/{reference.c,candidate.c,edges.txt}; candidateSHAad83b55033d700b1ce5f57db07dfb66bda2e66bc8bdb839ca3cf407004a28918. Structural preparation only, not execution proof or measured improvement.
- Next actual-helper whole-kernel and direct-interior-entry FP-disabled/faultCIA/cycle equivalence, then optimizedassembly/offline timing if correct. Do not promote source based on restoration alone. Accepted app unchanged, no runtime/build/Simulator; G6/full PRD open.

## 2026-09-06 — R218 guarded MEM1 lookup feasibility parked

- Prior turn: progress, callback ordering proof. Added source-pinned offline probe-mem1-lookup.py. Candidate skips EXRAM predicate only when masked address below90000000 and size<=exram_size<=10000000 guarantee it cannot match; otherwise original helper runs. Does not hoist metadata across callbacks.
- Actual initialized24MiB/64MiB mappings, EXRAM present/absent, widths1/2/4/8, randomaddresses plus boundaries/mirrors:1.6M pointer/offset/null-output comparisons pass O2ASanUBSan. This fixture is not arbitrary malformed-size validation. Log generated/mem1-lookup-r218.log retains exact wrapper assembly.
- Assembly trades EXRAMpointer load for address/size comparison sequence and adds fallback overhead. Not a clear lower-work route; park without runtime/module build or broad claims. No production edit.
- Next inspect adjacent arithmetic FP-availability checks: identify whether state cannot change between checks, preserve firstfaultPC and callbacks/memory boundaries; separate from existingFPRF classification elision. No game/build/Simulator, accepted app unchanged, G6/fullPRD open.

## 2026-09-06 — R217 memory callback contract

- Prior turn: progress, instruction grouping. Read actual core/cpu.h and StaticRecompCore_Hooks.cpp (initial guessed memory.h/StaticRecomp.cpp paths absent; located actual files). get_ram_ptr checksEXRAM first; direct stores acquire pointer, clear reservation, journal, then write. External hook receives mutableCPUState; host pointer path deliberately rejects non-LC effective addresses because live MMU/MSR/BAT translation matters.
- Added test-memory-mapping-callback.py executing actual headers O2ASanUBSan: external read remapsRAM/changesMSR, next read observes changes; journal remapsRAM yet currentstore uses pre-callback pointer and nextstore uses new mapping; reservation clears. OffsetsMSR298/exception320/reserve_valid344 confirmed. Tests pass. This disproves unconditional cross-callback metadata hoisting, not a current runtime bug.
- Fullsuite80741 FINISHED0/generated/check-r217.log before this new test; new test separately passes and added to suite. No product/runtime modification.
- Next evaluate bounded lookup-order shape: EXRAM-first costs are repeated on THP MEM1 accesses. Require mapping-size/overlap/alias/boundary/callback parity and offline assembly/benchmark before any reordering or special path. No host pointer bypass, no blanket restrict assumptions. G6/fullgoal open.

## 2026-09-06 — R216 whole-kernel sampled instruction grouping

- Prior turn: progress, exact-PC/disassembly audit. Added classify-cpu-instructions.py joining summary offsets (low markers aligned) to exact objdump; missingPC fails closed, mechanical mnemonic/x19-offset grouping. Focused tests verify marker alignment, weights, families and missingPC; added suite entry, fullsuite not rerun.
- All kernel/decoder samples resolve. Kernels7913 samples:6069load,334store,216call,289branch,1003integer/other,2scalarFP. Load-PC share76.7% is attribution, not isolated load cost; external helper arithmetic lies outside kernel leaf counts and sampling skid remains. x19 prologues at577eeb0/578dd38/579ca54 copyCPUState x0. Major offset groups includeMSR298 andRAM/exRAM pointers/sizesd80/d88/da0/da8;320/344 require exact field-offset verification before inference.
- Evidence generated/runtime/thp-profile-r214/instruction-families.json. Accepted app unchanged; no runtime/build. Next callback/alias/state-mutation contract audit for repeated metadata reads before attempting guarded reuse; cannot simply hoist across callbacks or fault boundaries. G6/full PRD open.

## 2026-09-06 — R215 current hot-instruction audit

- Prior turn: progress, current CPU capture. Read summary and disassembled exact signed5c21...e91d (hash reverified), aligning raw profiler PCs to4bytes. No runtime/build or source mutation.
- Kernels total7913samples across2731sites; top10 only313 (~4%). Kernel0 top5789b4c updatesPC/downcount then lazyFP/MSR guard;5784738 cycle updates. Kernel1 578fd00 PC/downcount thenFP/quantization guard. Decoder57aff90 is result store afterCLZ ladder (previously parked optimization);57b0028 PC/cycle then memoryread setup. Sample skid and block-entry concentration prevent attributing whole costs to the sampled load/store.
- Saved exact symbol disassembly generated/runtime/thp-profile-r214/hot-functions.asm. Initial guessed include/gekko/cpu.h nonexistent; actual core/cpu.h shows ordinary nonvolatile downcount, so do not blame volatility. No unsupported optimization applied.
- Next aggregate instruction families/call boundaries across complete kernels to quantify repeated state/memory work and select a distinct structural experiment; do not repeat CLZ or chase one hotPC. Accepted package unchanged, G6/full PRD open.

## 2026-09-06 — R214 residual CPU attribution after promotion

- Prior turn: progress, signed-package smoke. Fresh R85 profile thp-profile-r214, signed module5c21...e91d/accepted runner/native1x/Metal; no phase recorder or concurrent build. Trigger36277 completes, movie visually advancing. CPU Profiler attaches exactPID62089 for10s (5912FINISHED0), cpu-profile export63997 and summarizer pass. Runtime93799 cleanclose/native-only/SMCfailed0/NANDa574...afa6 unchanged. Closed during movie after capture; not a new movie-completion test.
- 31295CPU-thread samples,31248Pcore/47Ecore. Leaf kernel0=5112,kernel1=2801,func804530A0=5015,convert_to_double1517,ps_add1372,ps_sub1203,psq_store1118. Kernels~25.3% and decoderchunk~16%; inlining changes leaf attribution, so not a direct old-profile speed comparison. Raw instruction offsets/cycle weights available in summary.json.
- Evidence generated/runtime/thp-profile-r214/{cpu.trace,cpu.xml,summary.json,capture.log,runtime.log}; capture/export0. Initial xctrace record --help prints usage despite parser error, corrected actual command uses documented flags. Whole126.8s51underruns3backlogs under profiler not acceptance. No game/Simulator/build left.
- Next exact current binary instruction-range analysis inside kernels/decoder; choose a distinct larger cost with falsifiable correctness test, not rejected helper repeats or speculative QoS/renderer changes. G6/full PRD open.

## 2026-09-06 — R213 signed-package runtime acceptance for promotion

- Prior turn: progress, module-only promotion and fullsuite pass. Prepared fresh thp-package-r213 from real one-star Config/Wii/config.ini, no savestates. Explicit module path inside signed app, unchanged runner, native1x/Metal, no phase trace.
- Runtime3367 confirms bundled module loaded. Title visible, load11508 ordinary menu sequence reaches one-star Observatory. Move49481 visibly changes Mario/camera; native close exits0. fallback0/smc_failed0/NAND5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged. Whole153.7s3underruns5backlogs not sustained audio/soak acceptance. Evidence generated/runtime/thp-package-r213/runtime.log.
- This closes the bounded module promotion, not G6. No game/build/test/Simulator remains. Next targeted CPU profile of current movie to locate residual cost after extraction; do not repeat unchanged timing, reintroduce parked helper experiments, or transfer old profile weights blindly. Full original PRD remains active.

## 2026-09-06 — R212 local package promotion

- Completion update: fullsuite89902 FINISHED0, Repository safety checks passed. Next signed packaged-module runtime smoke; no active test/build/runtime/Simulator.

- Prior turn: progress, fresh comparison supports canonical promotion. Existing app audit passes and no runtime/build/Simulator. Packaging script default would bootstrap/rebuild runtime; added explicit --module-only route to stage a copy, replace audited selected module, sign, compare wrapper/runner/frontend, audit before swap and retain prior app. No bootstrap invoked.
- Normal module marker updated to canonical modules-thp-r205/...-daa33a86eff05b70. Package26460 FINISHED0, staged and final audits/signatures pass. Signed module5c21010733a5cb3cf8231853f96963e3d433796c062106fdaf6b4261fe0be91d; wrapperdbc8...0124/runneraffb...e295/frontend61de931aafa359c9263d5faecc43a78cff5fee96cac246b00d423804dcbe1750 unchanged. Backup generated/macos/GalaxyPad.app.previous.20260907T023102Z retained. Log generated/package-thp-r212.log. No publication.
- Full repository suite ACTIVE89902, generated/check-r212.log; poll same. Next signed packaged-module save-load smoke after tests, not simultaneous runtime. This promotes a measured incremental gain, not G6/60Hz/audio acceptance. Full PRD remains open.

## 2026-09-06 — R211 fresh canonical comparison supports promotion

- Prior turn: progress, canonical movie evidence. Fresh thp-control-r211 copied R85 fixtures, exact hashes verified. Accepted494c...3fbe plus unchanged runner/native1x/Metal/phase-only; runtime55214 and trigger25369 FINISHED0.
- Fixedminute3226frames=53.7666667FPS,p9920.397166ms,80VIgaps>=20ms,DMA28.6997333kHz. CanonicalR2103319frames55.3166667FPS gains2.883%,17longgaps. Supports retaining/promoting this bounded optimization; no unchanged third run or60Hz claim.
- Baseline movie visibly completes to plaza, clean close/fallback0/SMCfailed0/NANDa574...afa6 unchanged. Whole218.4s116underruns3backlogs not fixed-window audio comparison. Private runtime/thp-control-r211/{runtime.log,phase.csv}.
- Next canonical selection/package promotion with accepted runner/frontend retained: build-macos-app.sh defaults to desktop binaries that bootstrap rebuilt, so inspect and supply verified inputs rather than silently changing runtime. Normal app/selection unchanged at checkpoint; no game/build/Simulator. G6/full PRD open.

## 2026-09-06 — R210 canonical fixed movie performance

- Prior turn: progress, canonical audit and ordinary save/lifecycle smoke. Created fresh thp-movie-r210 from exact R85 Config/Wii/config.ini/slot3; slot aea2...7ab8 and NANDa574...afa6 verified. No other runtime/build/booted Simulator.
- Explicit canonical1180...c631 with accepted runner, native1x/Metal, phase-only trace. Loaded state3 visibly, trigger69378 exits0. measure-attack-trace.py reports fixed60s3319VIframes=55.3166667FPS,p9919.602916ms,17gaps>=20ms,DMA29.5338667kHz. No host profiler or recorder.
- Movie visibly advances and completes to plaza; move1757 changes Mario/camera. Native close57735 exits0, fallback0/smc_failed0; NAND unchanged. Whole241.5s86underruns7backlogs are not fixed-window audio acceptance. Evidence generated/runtime/thp-movie-r210/{phase.csv,runtime.log}.
- Interpretation: source-exact full rebuild retains broadly similar speed to experimental55.7833 and exceeds older accepted53.7833 by~2.85%, but current paired control is missing. Next one fresh accepted control before promotion, not another unchanged candidate run. No game/build/Simulator at end, normal app unchanged, G6/full scope open.

## 2026-09-06 — R209 canonical artifact and real-save smoke

- Prior turn: verified wait. Same28332 FINISHED0 without restart. Canonical module SHA1180447313459c4c153ca74e5215811e88a9b69a00d15feff940f062148fc631,114404568bytes; audit ARM64/macOS14/1322chunks/19SMC passes. Manifest source28d765f74af57637, FPRF119, THPkernels2, originalPGOf39a...460d correct; symbols thp_kernel_0/1 present. Binary differs from one-chunk experimental candidate, so retain separate runtime/performance evidence.
- Launched explicit canonical module with unchanged packaged runner and prepared thp-canonical-r206, no phase trace. CUA title verified; ordinary load65336 reaches one-star Observatory. Native pause indicator and resume clear verified. Move76429 visibly changes Mario/camera. Closed native window, runtime38114 FINISHED0; no fallback/SMC failure, NAND5040...64a6 unchanged.
- Private evidence generated/module-thp-r205.log and generated/runtime/thp-canonical-r206/runtime.log. Whole137s8underruns9backlogs includes deliberate12.45s pause; not sustained audio/performance acceptance. No game/Simulator/build remains, normal app/selection unchanged.
- Next canonical fixed R85 movie test before package promotion, because exact source reproduction does not guarantee identical full-link performance. G6/full PRD remain open.

## 2026-09-06 — R208 verified link wait and fixture identity

- Prior turn classified verified wait. Re-polled SAME28332; linker59544 observed4:07 then5:22, ~626–691%CPU, nonterminal. Continued bounded waits without restart.
- Re-hashed accepted wrapper dbc8d3a11e8e9caf40f91ca32d6a2314de441f7aa9446c720d3877f333660124, runner affbcb51f27f853b0b5ee403b5033c3bd5a6f7ea3e514b969c2ef4272af9e295, PGO f39a3cedeb6c49f35c411356893c619dc347eb1b0bb5986687f25cf81e7d460d and prepared NAND5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6; unchanged. These preserve the single-variable runtime comparison.
- No bootstrap/package/selection/runtime change. Next poll28332 to terminal, audit compiled canonical module, then use thp-canonical-r206. Full goal remains open; active compilation is not a blocker.

## 2026-09-06 — R207 verified canonical link wait

- Prior turn: progress, whole-source scope comparison and disposable save setup. Revalidated same session28332 repeatedly without restarting. Compilation advanced944 to1330/1332, then actual linker PID59544 observed44s to3:19 elapsed, ~667–683%CPU and ~1GiB RSS. Build is live, not terminal or blocked. Disk6.8GiB available.
- Expected warning: one of33 functions has mismatched profile data ignored, matching the experimental candidate. No suppression or new product edit. No game/Simulator/package change.
- Next poll SAME28332 through terminal; audit module architecture/SMC/chunk count and manifest THP/FPRF/source/PGO identities, then explicit canonical module launch with accepted runner and prepared thp-canonical-r206 real-save profile. G6 and full PRD remain unmet.

## 2026-09-06 — R206 source-scope verification and runtime preparation

- Prior turn: progress, canonical source reproduction and fresh module build. Re-polled SAME28332: live build advances473 to701/1332; no restart. Current ps confirms ninja/clang activity; disk6.8GiB available.
- Whole generated-directory diff against accepted FPRF output reports only chunk1102. Headers, SMC and every other generated file are identical. This narrows the canonical change to the already-oracle-tested source; it is not binary/runtime acceptance.
- Prepared new ignored runtime/thp-canonical-r206 by copying only Config/Wii/config.ini from midblock-r145; no savestates. Configured pipe. NAND SHA5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6; Metal/native1x/CPUThreadTrue. No game or booted Simulator; app and normal module marker unchanged.
- Next poll28332 to terminal, audit canonical artifact/manifest/symbols, then explicitly launch candidate with accepted packaged runner and isolated profile for ordinary title/file-select/save-load and lifecycle smoke. Do not build/package a changed runner into this one-variable comparison.

## 2026-09-06 — R205 canonical module reproduction

- Hypothesis: fresh generation through the compiled port reproduces the oracle-tested source and records the new policy in a separate artifact identity.
- Started GALAXYPAD_PGO_PROFILE=generated/pgo/rmge01.profdata GALAXYPAD_MODULE_OUTPUT=generated/modules-thp-r205 ./scripts/build-module.sh; session28332, private log generated/module-thp-r205.log. Preflight confirms exact DOL/Broadway/64MiB MEM2; cache key suffix daa33a86eff05b70. No existing matching artifact.
- Fresh chunk1102 exactly matches tested candidate after normalizing its header include; cmp exits0. Initial shell comparison replaced comment line1, then corrected the include match. No generated source edited. Full compile is active, no runtime/Simulator. Normal module marker/app unchanged.
- Next poll SAME28332; do not bootstrap or launch while compiling. On success audit module, manifest policy/source identity and symbols, then isolated gameplay validation. No FPS or stability claim from source parity.

## 2026-09-06 — R204 canonical bootstrap integration

- Hypothesis: the tested two-kernel extraction can be reproduced through the pinned toolchain without changing existing FPRF behavior or reusing an incorrectly keyed cache.
- Added SHA-pinned bootstrap reverse/restore/apply ordering for patch0016, applied after FPRF. Bootstrap and repeat succeed; source-only test-thp-policy-wiring.py checks policy equality, ordering, key/manifest presence and patch pin. Added to repository suite.
- Evidence: generated/bootstrap-r204.log, generated/bootstrap-r204-repeat.log, generated/check-r204.log (Repository safety checks passed), generated/port-r204-build.log (port linked). Old session handles expired; fresh incremental cmake build exits0. Nonfatal upstream ScmRevGen bad revision ^master message retained.
- State: no game or booted Simulator, 7.5GiB available, normal app/selection unchanged. Next separately cached canonical module build, source parity and audit before package promotion. G6 remains open.

## 2026-09-06 — R203 canonical policy and patch preparation

- Previous turn: progress, real-save/lifecycle regression. Current status and existing policy/cache hooks inspected.
- Added thp-kernel-policy.inc with exactidentity/postFPRFhash/584entry/branch/restore guards and test-thp-kernel-policy.py.79006FINISHED0 ASanUBSan: output equals testedcandidate exactly after include normalization, negative guards pass. generated/thp-policy-r203.log.
- Prepared0016 patch adds localpolicyinclude, separatecache/manifest field beforelookup, extraction after existingFPRF. Initial zero-context patch failed normalapplycheck; added3linecontext, now standardcheckpasses. FinalSHAef25620e8ec254b1c5eecf2bfa386ad60c06f085ac3cca0a1996cba611cf8f6f. No vendor files changed.
- Next bootstrap hashpin/peelrestore/applyorder/idempotency, compiledtool validation then canonicalmodule reproduction before selection. No runtime/Simulator/build, normalapp unchanged, G6/fullgoal open.

## 2026-09-06 — R202 real-save and lifecycle regression

- Previous turn: progress, fresh control supports3.7% candidate gain. Checked candidate/save identity and no existing runtime/Simulator; used prepared fresh thp-save-r201 without state files or phase recorder.
- Runtime50119readytitle visually verified; normal load fixture91852FINISHED0 reaches one-star Observatory. Native pause indication verified, resume clears, movement18112FINISHED0 visibly changes position/camera. Clean close50119FINISHED0/native-only/SMCfailed0/NAND5040...64a6 unchanged.
- Whole326.6s18underruns17backlogs/56.37smaxgap includes deliberate pause, not freeze or audio acceptance. Private runtime/thp-save-r201/runtime.log. No runtime/Simulator/build remains, normalappunchanged.
- Next separately keyed canonical kernel policy applied after existingFPRF transformation, exact source parity to testedcandidate before bootstrap/build. Do not substitute experimentalbinary for reproducible cache. G6/fullgoal open.

## 2026-09-06 — R201 accepted reverse control

- Previous turn: progress, candidate linked/visible55.7833FPS. Fresh R85 profile/slot/NAND and accepted494c...3fbe verified; no previous game/build/Simulator before launch.
- Runtime91529/trigger20958bothFINISHED0. Same phase-only/native1x/Metal minute53.7833FPS,p9920.2855ms,DMA28.7168kHz. Candidate+3.7186%, fewer>=20ms gaps29vs65. Retain for further regression, not promotion or60Hz acceptance; no unchanged third run now.
- Movie visibly completes/plaza, cleanclose/fallback0/SMCfailed0/NANDunchanged. Whole382s108underruns5backlogs not fixedwindowaudio. Evidence runtime/thp-control-r201/{measurement.json,phase.csv,runtime.log}.
- Prepared fresh thp-save-r201 from real-one-star midblock-r145 Config/Wii/config.ini, no savestates; NAND5040...64a6 verified and pipe configured. Next candidate untraced title/file-select/save load/pause-resume/movement/close before canonical integration. No game/build/Simulator remains; accepted app unchanged, G6/fullgoal open.

## 2026-09-06 — R200 linked kernel candidate gameplay

- Previous turn progress/link started. Revalidated same73103 and linker52188 at2:11/4:04/5:16, then FINISHED0/auditpass without restart. Module2c29e27f...bbad8,114421048bytes; both kernel symbols verified. No runtime overlapped linking.
- Runtime26619 with acceptedrunner and explicitcandidate; waited for ready module/title before CUA bind, visibleLoad3 then trigger5906once. Fixedminute55.7833FPS,p9919.84825ms,DMA29.7813333kHz. Movie completes/plaza; movement72734visibly changes position/camera, allhandlesexit0.
- Cleanclose/fallback0/SMCfailed0/NANDa574...afa6 unchanged; whole379.8s75underruns7backlogs not audio pass. Private runtime/thp-kernels-r199/{measurement.json,phase.csv,runtime.log}. App unmodified/unselected; no runtime/Simulator/build remains.
- Next fresh acceptedR85phase-only control matching candidate, not historical54.5 or host-recorder54.0167, then retain/park decision. G6/fullgoal open.

## 2026-09-06 — R199 caller boundary tests and isolated module link

- Previous turn: progress, private kernel extraction and initial oracle. Rechecked status/source.
- Added caller-return and callback-PC cases. First5421 failed because fixture treated internalLR as final sentinel; corrected main stopping rule.18609FINISHED0 all4variants1440cases/digest882be09b1f38e9ad,96callerreturns. See PERF.md for counters, generated/thp-kernels-r199-fixed.log.
- Wrapper ThinLTO benchmark45069FINISHED0 ABBA26793.75/24823.60/24719.30/25894.15ns,5.97% mean reduction,96KiB text increase. No extrapolated FPS gain.
- Extended existing source-pinned onechunk builder with --thp-kernels, preserving historical default. New isolated build73103ACTIVE, compile succeeds with1of33PGO mismatch warning, linker52188 verified38s/~671%CPU.1328unchanged objects reused, originalPGO retained. Do not restart; pollsame then audit.
- Prepared fresh isolated runtime/thp-kernels-r199 R85Config/Wii/slot3/Pipe; hashes aea2...7ab8/a574...afa6 verified. No runtime/Simulator launched, accepted app untouched. Next same-link completion/audit then visible matched movie test; G6/fullgoal open.

## 2026-09-06 — R198 extracted kernel chunk

- Previous turn: progress, separate-TU feasibility and dispatcher audit. Current source/status rechecked.
- Added pinned extract-thp-kernels.py, preserving original outer cases/charges/return dispatcher and all584labels, kernel bodies/loop charges unchanged. Private initial void-return candidate superseded before compilation by bool early-exit/guest-return distinction; no destructive overwrite.
- Final candidate98d98621d4f744ea76977a63e44f3e2e3e731f50051e4a3a800a772e05a68991 in generated/thp-kernels-r198-exits. --kernels83114FINISHED0 uses complete original/candidate chunks: all4variants864cases/digestaa72b761a9c075fd. Unrelated callees abort and were not reached. Log generated/thp-kernels-r198-test.log.
- Boundary: sentinelLR fixture does not prove caller-continuation or all584arbitrary entries. Next bounded caller-return/fault-PC tests and actual-wrapper timing before candidate link. No accepted app/module change or game/Simulator/build remains; G6/fullgoal open.

## 2026-09-06 — R197 separate-TU transform feasibility

- Previous turn: progress, whole-transform prototype11.47% offline gain with code-size risk. Checked current status/source before separating compile inputs.
- Added --flatten-lto: actual helper sources separate, ThinLTO for control/candidate. Benchmark68614FINISHED0 A26997.55/B24434.70/B23704.95/A26947.00,10.76% mean reduction,96KiB text increase. Correctness48937FINISHED0 all4variants864cases/digestaa72b761a9c075fd. Logs named in PERF.md.
- Dispatch audit: original80452554 internalgoto bypasses chunk entry switch. A kernel cannot blindly retain another suffix-charge switch; preserve original external charges and block/loop charges, set PC on trampolines, retain outer return dispatcher and584labels. Documented exact integration contract, no source/module mutation.
- Next source-pinned kernel extraction and wholechunk entry/return/cycle comparison before candidate linking. No duplicated CPU globals, blanket inlining, FPS promise or app change. No game/Simulator/build left; G6/fullgoal open.

## 2026-09-06 — R196 transform-local compilation prototype

- Previous turn: progress, exact operand shortcut parked for weak timing. Checked source/status/no game/build before new scope.
- Hypothesis: expose complete transform/helper dataflow to compiler, retaining actual arithmetic and state effects. --flatten uses same-TU helpers in both control/candidate and adds attribute only to two transforms.
- Correctness76534FINISHED0: all4O1sanitized/O2 reference/candidate864cases/digestaa72b761a9c075fd unchanged. Existing compile observed live, no restart.
- Timing93793FINISHED0: ABBA28781.95/25495.70/25567.95/28896.65ns totals;11.47% lower transform time. Entire benchmark text81920->180224bytes,96KiB/+120% increase. Logs generated/thp-flatten-r196.log and thp-flatten-bench-r196.log.
- Next: separate-TU ThinLTO attribute feasibility matching module build before any isolated candidate; preserve caller dispatch/interior entries/shared globals. No blanket inline rollout, FPS claim or app change. No runtime/Simulator/build remains; fullgoal/G6 open.

## 2026-09-06 — R195 paired operand identity experiment

- Previous turn: progress, loaded host correlation excludes active paging for54FPS run. Current source/status rechecked.
- Hypothesis: duplicate25bit C-operand rounding may be skipped exactly for already-rounded bit patterns. Added isolated --identity-c float copy, no ref/module changes; low28zero guard returns input else original function.
- Correctness98564FINISHED0: all4reference/candidate sanitized/O2 transform variants864cases/digestaa72b761a9c075fd. Standalone probe initially missed leading_zeroes dependency; extracted actual helper then12,582,912comparisons/6,094,848guarded identities pass ASanUBSan.
- Timing23931FINISHED0: A29832.7/B30058.95/B29499.5/A29805.2;~0.133% average reduction, disturbed first candidate pattern. Park, do not cherry-pick outlier or spend full link on weak result. Evidence logs named in PERF.md.
- Next: larger transform-level specialization/evidence rather than another scalar-helper tweak. No game/Simulator/build or appchange; fullgoal/G6 remain open.

## 2026-09-06 — R194 loaded host correlation

- Previous turn: progress, host recorder and idle evidence. Checked one-game/Simulator state, accepted module494c...3fbe and copied exact R85 slot/NAND/config to fresh isolated profile.
- Hypothesis: paging might coincide with movie shortfall. Added absolute raw/monotonic/wall timestamps, launched accepted game88867 and host recorder43772; waited for module ready before CUA bind, loaded3 visibly, trigger81216once.
- Evidence: fixedminute54.0167FPS/p9920.416708ms/DMA28.8405333kHz.13overlapping host intervals:zero swapins/out/pageouts/compressions, pressure78–79%, swap701.62MiB stable. Raw clock aligns phase; ordinary monotonic differs. Output runtime/host-correlation-r194/{measurement.json,correlation.json,host.jsonl,phase.csv}.
- Visible movie advances and completes to plaza. Nativeclose88867FINISHED0; trigger81216/recorder43772FINISHED0. Fallback0/SMCfailed0/NANDa574...afa6 unchanged; whole199.1s104underruns5backlogs not acceptance. No game/Simulator/build remains.
- Interpretation/next: active paging does not explain this54FPS run; historical22FPS/thermal/background contribution remains limited evidence. No paging workaround or unrelated app changes warranted. Return to larger decoder optimization;12site candidate still offline, app unchanged, G6/fullgoal open.

## 2026-09-06 — R193 host resource attribution preparation

- Previous turn: progress, candidate microbenchmark and correctness regression. Current status and host state rechecked.
- Hypothesis: current paging/background pressure could confound further decoder micro-optimizations; inspect without changing unrelated apps. Host16GiB,8.0GiBdiskfree,701.62MiBswap allocated.
- Added read-only scripts/record-host-pressure.py and tests/test-host-pressure.py. Parser passes;6second recorder15844FINISHED0 with3samples: zero swapins/swapouts/pageouts/compressions, reported pressure79%. Private generated/host-pressure-r193.jsonl. Background CPU observed but no game was running, so no causality claim.
- Boundary: idle-host sample cannot explain prior22FPS or prove no thermal throttling. No game/app/settings/process mutation, no Simulator/build. Fullsuite not rerun; gitdiffcheck passes.
- Next: fresh accepted R85 phase run paired with timestamped host readings; classify loaded contention before another optimization build. Recorder is diagnostic overhead, not uninstrumented acceptance. G6/fullgoal open.

## 2026-09-06 — R192 offline candidate timing

- Previous turn: progress,12site candidate oracle parity. Checked status/source; no module changes.
- Hypothesis: candidate must improve actual transform execution rather than harness hashing. Added --benchmark strict O2 ABBA with20000repetitions per12cases, setup/hash excluded, CPU resets/yields/output callbacks retained.
- Result77709FINISHED0: A30031.95/B29036.65/B28943.70/A29899.30 total ns across12means; average3.255% lower candidate transform time. Hot-memory synthetic equal weights, no final PGO/host costs; not FPS evidence. generated/thp-merge-bench-r192.log.
- Regression11797FINISHED0: all4reference/candidate sanitized/O2 variants864cases, unchanged digestaa72b761a9c075fd after timing refactor; generated/thp-oracle-r192-regression.log. gitdiffcheck passes. No game/bootedSimulator.
- Decision: retain offline candidate unpromoted; insufficient broader-impact proof to justify full link alone. Next larger decoder boundary work or weighted relevance evidence, not unchanged third benchmark. G6/full original scope open, accepted app unchanged.

## 2026-09-06 — R191 isolated merge-crossing classification candidate

- Previous turn: progress, expanded observable-state oracle. Rechecked current status/source before editing.
- Hypothesis: FPRF overwritten after non-observing paired merges can be deferred without changing arithmetic or observable state. Added strict emitted-shape scan and optional --merge-fprf candidate in the offline probe,12sites only. No memory/control boundary crossed.
- Test: `python3 tests/probe-thp-oracle.py --merge-fprf`,68000FINISHED0. Reference and candidate each O1 ASan/UBSan and O2 pass864cases, identical digestaa72b761a9c075fd including callbacks/exception/yield/journal/reservation effects. Evidence generated/thp-merge-fprf-r191.log.
- Boundary: no game/module/app edit, no measured gain or fullsuite rerun. Full goal and G6 performance/audio remain open.
- Next: offline matched timing excluding initialization/hash overhead, then reject or consider canonical scope tests/integration only if useful. Avoid a costly link for a negligible12site improvement.

## 2026-09-06 — R190 observable transform state

- Previous turn: progress, first executable oracle; current status/source rechecked.
- Hypothesis: a usable candidate oracle must observe memory/callback/exception state, not only final pixels. Extended the same source-pinned probe with LSQE disabled, alternate GQR5/6, write journals/preimages, reservations, full normalized state at each callback/exit, and synthetic output interruption.
- Tests: initial576case variant92888FINISHED0; final864case variant52883FINISHED0. Both O1 ASan/UBSan and O2 agree:432yields/288FPfaults/288illegal/96interrupted/12480callbacks/17152journals/864reservationclears/digestaa72b761a9c075fd. Logs generated/thp-oracle-r190.log and thp-oracle-r190-interrupt.log. No runtime/build overlap.
- Boundary: synthetic callback exception-field mutation is not MMU fidelity; no candidate speedup, no fullsuite rerun or module/app change. G6/full scope remains open.7.9Gi free, no game/Simulator launched.
- Next: bounded transform specialization with oracle comparison and offline timing before expensive link; retain unsupported-state fallback and exact scheduling. Do not repeat parked entry-split or unchanged generic finite-helper changes.

## 2026-09-06 — R189 first executable transform oracle

- Previous turn: progress, exact-input boundary closure/contract evidence.
- Hypothesis/step: accepted transform bodies can be isolated with exact entry charges and resumed yields for offline state comparisons. Added source-pinned tests/probe-thp-oracle.py, no game/generated module mutation.
- Initial failures: generated helper header missing at compile; fixed actual header include. Runtime then correctly raised program exception because fixture omitted HID2 LSQE; diagnostic identified pc700/output0, fixture corrected. Not a product defect and no unchanged retry.
- Test: `python3 tests/probe-thp-oracle.py`, final63083 FINISHED0; generated/thp-oracle-r189-lsqe.log. O1 ASan/UBSan and O2 both96cases/84yields/48FPfaults/3072callbacks/digest4a471989ef100135.48successful blocks restore stack/f25–31; zero-block64pixels all128. Existing nested-comment compiler warning retained.
- Boundary: optimization consistency, not independent decoder accuracy or candidate speedup. No fullsuite rerun or runtime launch; accepted app unchanged, G6/fullgoal open.
- Next: extend LSQE/GQR/journal/reservation/callback-exit coverage before local-state/math specialization; preserve all early exits and guest timing.

## 2026-09-06 — R188 exact THP boundary audit

- Goal/hypothesis: larger closed transform boundary may permit useful specialization after small entry-layout candidate was parked.
- Step: inspect Petari reference and exact RMGE01 generated instructions; add raw-DOL hash/branch scanner including conditional and indirect edges. Confirm width/base/quantization relative locations from caller and transform.
- Test: `python3 scripts/audit-thp-boundaries.py > generated/thp-boundaries-r188.json` passes exact DOL identity, final returns and both transform closure assertions. No runtime launched; no booted Simulator or build.
- Result: two bounded regions verified, but state/memory/GQR/FP/cycle contracts prohibit an assumed pure-math drop-in. Detailed verified/inferred boundaries in THP-BOUNDARY-AUDIT.md. No performance claim or app change.
- Next: offline oracle exercising actual generated transforms and complete state/memory exits before proposing a specialization. G6/full original goal remains open.

## 2026-09-04 — G0/G1 initial audit

- **Goal:** establish the private/safety boundary and begin exact input identity.
- **Hypothesis:** the supplied image is a readable retail Galaxy Wii image and the host can safely enter Phase 0 without changing protected input.
- **Bounded step:** inspect git/reference state, host tools, processes, Simulators, disk capacity, hash the image, and use WIT read-only metadata commands.
- **Commands:** `git status --short --branch`; `git rev-parse HEAD`; `xcrun simctl list devices booted`; host version probes; `df -H .`; `shasum -a 1/-a 256`; WIT `ID6`, `FILES`, and `DUMP -l` via its x86_64 slice.
- **Result:** confirmed an `RMGE01` NTSC/USA scrubbed WBFS with one signed encrypted DATA partition and IOS 33. The image hash is stable. No Simulator or game instance was active. The supplied SunPad checkout is clean but newer than the reviewed pin. WIT's arm64 slice is not usable on this host; Rosetta x86_64 is usable.
- **Evidence:** this journal, `STATUS.md`, `DISC-IDENTITY.md`, and `DEPENDENCIES.md`; protected raw logs remain local and ignored.
- **Interpretation:** the image passes the family/format precondition, but G1 remains open until exact header fields, deterministic extraction, DOL hashes, and executable inventory exist.
- **Next step:** install the repository safety gate, reproduce public dependency pins with push disabled, then extract twice into unique ignored staging directories and compare deterministic manifests.

## 2026-09-04 — G0/G1 completion and G2 guard

- **Goal:** finish G0/G1 and prove the Wii/Broadway selection contract for G2.
- **Hypothesis:** the reviewed graph can identify this Wii input and expose 64 MiB MEM2, but DolRecomp's title database fallback needs a GalaxyPad-owned fail-closed guard.
- **Bounded step:** reproduce all reviewed source pins; disable push; extract twice; compare manifests; build/test DolRecomp; inspect platform/CPU/MEM2 selection; add a guarded AOT entrypoint.
- **Commands:** `./scripts/bootstrap-dependencies.sh`; two WIT `EXTRACT` runs; sorted per-file SHA-256 manifests and `cmp`; CMake/Ninja DolRecomp build; `ctest`; focused source inspection.
- **Result:** G0 and G1 pass. Both extraction manifests match. DolRecomp built on arm64 and all 16 tests passed. ModernGekko identifies Wii via disc magic, and its address model provides 24 MiB MEM1 plus 64 MiB MEM2. The missing-title database path can degrade to GameCube mode, so GalaxyPad now requires an exact local title entry, explicit Broadway, Wii-named output, and emitted macro verification.
- **Evidence:** `config/galaxypad-disc.json`, `DISC-IDENTITY.md`, `EXECUTABLE-COVERAGE.md`, `WII-SUBSYSTEMS.md`, and ignored manifests/build output.
- **Interpretation:** the static contract is credible and fail-closed; G2 still needs a successful exact-DOL generation run.
- **Next step:** run the guarded C-backend generation and interpret its SMC/warning output before starting the ModernGekko module build.

## 2026-09-04 — G2/G3 exact AOT and module

- **Goal:** complete the Wii/Broadway substrate and produce an auditable exact-input ARM64 module.
- **Hypothesis:** RMGE01's main DOL can use DolRecomp's portable C backend without unknown instructions, while conservative SMC and interpreter-capable sites can remain explicit and measurable.
- **Bounded step:** generate guarded Broadway C, inventory warnings/fallback/SMC sites, build the ModernGekko desktop tools and module, and inspect its Mach-O and module descriptor.
- **Commands:** `./scripts/generate-aot.sh`; `./scripts/audit-aot.sh`; `./scripts/build-desktop-tools.sh`; `./scripts/build-module.sh`; `./scripts/audit-module.sh`.
- **Result:** generation produced 331 chunks and zero unknown instructions. All 419 interpreter-capable PCs have named mnemonics. The 19 SMC candidates are retained; four are explicit `icbi` sites and 15 are conservative store sites. The exact arm64 module is 100,762,840 bytes, targets macOS 14.0, has ABI 3/state size 3528, and hashes to `91efc5269d76feb9e6b5f31dc07bd4cbfe5720a238a84d1d6954d7a5617f2f9f`.
- **Evidence:** `EXECUTABLE-COVERAGE.md`, `SMC-ANALYSIS.md`, `HOME-MENU-ADAPTER.md`, the audit scripts, and ignored outputs under `generated/aot/` and `generated/modules/`.
- **Interpretation:** G2 and G3 are met. Legal ARM64 linkage is not gameplay proof, so G4 still required actual Wii boot and entry execution.
- **Next step:** run one bounded headless boot to expose initialization faults before the required Metal boot.

## 2026-09-04 — G4 headless initialization defect and fix

- **Goal:** boot the exact module through the Wii runtime with a bounded diagnostic surface.
- **Hypothesis:** the Null renderer can exercise boot/AOT without involving visible Metal rendering.
- **Bounded step:** run the exact module headless in a fresh isolated user directory, trace the first fatal failure, patch one identified cause, and repeat with a clean bounded harness.
- **Commands:** `moderngekko-run ... --headless --allow-interpreter --no-mods`; targeted LLDB/stack probes; `./scripts/bootstrap-dependencies.sh`; `./scripts/smoke-macos-runtime.sh headless 55`.
- **Result:** the first run loaded the module, then asserted in `ImGui::GetIO()`. The call came from shader-wait progress during shared renderer initialization: headless selected Null (and therefore created no ImGui context) while ModernGekko forced wait-for-shaders. The root-owned overlay now disables that UI wait only for headless mode. The reproduced smoke shut down cleanly with `native=116469500`, `fallback=0`, and `smc_failed=0`.
- **Evidence:** `patches/ModernGekko/0002-headless-no-shader-wait.patch`, `scripts/smoke-macos-runtime.sh`, and ignored log `generated/runtime/smoke-headless-20260904T072904Z.log`.
- **Interpretation:** the defect is fixed without changing the Metal path or guest fallback semantics. Headless evidence alone does not satisfy G4.
- **Next step:** repeat with a real Metal surface and Cubeb audio, then stop cleanly and inspect counters/NAND state.

## 2026-09-04 — G4 Metal Wii boot

- **Goal:** satisfy the macOS Metal Wii-runtime boot gate without inflating it into a title/file-select claim.
- **Hypothesis:** the exact module will pass visible renderer, audio, Wii service, and AOT initialization with the same zero fallback/SMC-failure result.
- **Bounded step:** launch `moderngekko-run` with `--graphics Metal`, the exact extracted root/module, a fresh user directory, and no mods; allow sustained execution; interrupt once and inspect the shutdown and Wii state.
- **Result:** the process created the Cocoa/Metal runtime, loaded RMGE01 at entry `0x8000403c`, initialized Cubeb, sustained `native=436246556`, and exited status 0 with `fallback=0` and `smc_failed=0`. The isolated Wii directory contains `uid.sys`, `SYSCONF`, WC24 files, and `fst.bin`. No game/runtime process or Simulator remained afterward.
- **Evidence:** `WII-SUBSYSTEMS.md`, `EXECUTABLE-COVERAGE.md`, and ignored `generated/runtime/g4-metal-user/` state.
- **Interpretation:** G4 is met. No visual title, file-select navigation, pointer response, audio correctness, or save acceptance is claimed.
- **Next step:** G5—add/capture the required frame, pointer/EFB, input, and audio breadcrumbs, then prove title and file-select with new-file creation on macOS.

## 2026-09-04 — G5 packaged title, pointer, and fresh file

- **Goal:** prove more than a title frame by driving the packaged macOS app through pointer-based fresh-file creation.
- **Hypothesis:** the existing Wii Remote/Nunchuk substrate can provide independent title-gate buttons and deterministic absolute IR without altering guest behavior.
- **Bounded step:** add a Pipe controller harness; package the exact arm64 runtime/module as a private ad-hoc-signed app; run one isolated NAND; drive title and file-select; create one file; capture one protected native screenshot; stop cleanly.
- **Commands:** `./scripts/wii-pipe.py configure/run`; `./scripts/build-macos-app.sh`; LaunchServices/CUA observation; exact-process SIGINT; hashes and package audit.
- **Result:** the app rendered the Wii safety screen, title, six-slot file select, create-file prompts, icon selection, and the final file-select state. Absolute IR visibly moved and highlighted intended targets. The independent Pipe A+X mapping passed the held-A+B title gate; Pipe B itself did not reach the emulated Wii B bit and is now used only for semantic Nunchuk C. Slot 1 contains a newly created Mario file. The protected 2501×1368 screenshot and NAND hashes correlate the visible state. A prior long Metal run ended with 5,231 frames, nonzero render/resource counters, `native=905716466`, `fallback=0`, and `smc_failed=0`.
- **Evidence:** `INPUT-AND-POINTER.md`, `SAVE-AND-NAND.md`, `AUDIO.md`, `scripts/wii-pipe.py`, the package scripts/tests, and ignored `generated/runtime/g5-app-user/` evidence.
- **Interpretation:** the video, absolute-pointer, independent-button, and fresh-file portions of G5 are proven. A single screenshot is supporting evidence only. Audio correctness and credible per-frame pointer/EFB/input breadcrumbs remain unproven, so G5 remains open.
- **Next step:** expose bounded audio queue/underrun and input/pointer/EFB diagnostics, rerun the same title/file-select sequence, and accept G5 only if the evidence remains stable.

## 2026-09-04 — G5 bounded I/O telemetry

- **Goal:** correlate the visible file-select proof with non-capturing audio, EFB, and input breadcrumbs.
- **Hypothesis:** passive counters at existing runtime boundaries can prove signal and command flow without changing emulation or retaining protected media/history.
- **Bounded step:** count Cubeb callbacks/nonzero frames/states, EFB peek coordinates/depth/wait, and P1 button/IR samples; rebuild; run one same-save packaged sequence; stop while the Pipe and pointer remain active.
- **Result:** 8,313 frames and 1,556,836,850 native dispatches completed with zero fallback or failed SMC. Cubeb delivered 16,864 callbacks/8,634,368 frames, including 5,810,776 nonzero frames at peak 21,011 and zero backend error states. Input recorded 25,967 samples, 403 button-active samples, four transitions, and last IR `(655,529)`. File select recorded zero CPU EFB peeks; separate instrumented scenes produced depth-peek counts and stalls, proving the counter path.
- **Evidence:** hash-pinned ModernGekko/Dolphin diagnostics overlays, `PERF.md`, `INPUT-AND-POINTER.md`, and private ignored runtime output.
- **Interpretation:** G5's pointer/EFB/input breadcrumb requirement is credible and the host audio signal path is active. The current counters cannot distinguish intentional silence from DSP queue starvation or validate pitch, so G5 remains open rather than converting signal presence into an audio-correctness claim.
- **Next step:** count main-DSP queue-empty and overrun events plus producer cadence, rerun title/file select, then decide G5 and proceed to the G6 opening/Gateway route.

## 2026-09-04 — G5 main-DSP starvation isolated

- **Goal:** distinguish intentional silence from host/DSP starvation and close or reject G5 audio with measured evidence.
- **Bounded experiments:** add main-DSP FIFO counters; compare the 80 ms default, 120 ms, and 200 ms reserves; enable the existing Apple ±2% adaptive prebuffer/resampler on macOS; correct the fixture startup delay; hold the proven file-select signature for 60 seconds.
- **Result:** the correct file-select graphics signature was `0x14564ea8ed5a5f20`. Its run produced 26,812 DMA enqueues and 196 running-state underruns extending through enqueue 26,179, with one startup backlog correction, zero hard queue-full drops, and a 142.7 ms worst producer gap. Larger reserves and the bounded adaptive path improved intermediate samples but did not eliminate sustained starvation.
- **Interpretation:** G5 audio is not stable. Increasing the resampling range would trade the defect for unverified pitch change and was rejected. Release and module builds are already optimized, with the module using O2-last plus thin LTO.
- **Next step:** build a separate exact-input O3 AOT module and compare the identical file-select path. Keep it only if hashes/ABI/coverage remain valid and runtime cadence materially reduces starvation without semantic changes.

## 2026-09-04 — G5 O3 candidate rejected

- **Goal:** determine whether more aggressive static optimization removes the measured main-DSP producer deficit without changing audio policy or guest semantics.
- **Bounded experiments:** build a separate exact-input O3/Thin-LTO module; reject an initial dylib that accidentally inherited macOS 26.0; rebuild every object and link with an explicit macOS 14.0 target; audit architecture, ABI, entry, chunk and SMC tables; rebuild the restored 80 ms/no-adaptive runner; compare O2 and O3 sequentially from identical copied NAND/controller snapshots with the same 126-second route.
- **Result:** the corrected O3 module hashes to `1258238b213ba2d2dc1aea2df1526f6d5a75ba4aa827fd3694422b6e312a1755`, is 100,250,888 bytes, and passes the RMGE01 ABI/entry/331-chunk/19-SMC/macOS-14.0 audit. Both runs reached projection `0x14564ea8ed5a5f20` with `fallback=0` and `smc_failed=0`. O2 recorded 6,169 frames, 26,119 enqueues, 268 underruns through enqueue 26,037, and a 151.7 ms worst producer gap. O3 recorded 5,828 frames, 24,734 enqueues, 270 underruns through enqueue 24,530, and a 131.4 ms worst gap.
- **Interpretation:** O3 improves only the worst isolated gap while reducing overall frame/DMA throughput and leaving sustained starvation unchanged. It is rejected; the accepted O2 module/package identity remains unchanged. A first O2 route completed but its PTY pipeline swallowed shutdown output on Ctrl-C, so it was discarded and both valid samples were rerun from new identical snapshots with exact-PID SIGTERM and normal exit.
- **Next step:** run the accepted O2 module under the same scripted route with Null/headless rendering and compare it with the valid Metal baseline. If starvation disappears, focus on render/EFB scheduling; if it persists, focus on core/DSP pacing and guest timebase.

## 2026-09-04 — G5 renderer discriminator remains starved

- **Goal:** determine whether Metal presentation is the dominant cause of the main-DSP producer deficit.
- **Bounded experiments:** run accepted O2 with ordinary headless/Null; observe that Null audio prevents mixer/DMA measurement; add a one-line default-preserving override that honors an explicit valid audio backend in headless mode; smoke `--headless --audio Cubeb`; rerun the identical 126-second route from a fresh copied snapshot.
- **Result:** the ordinary headless run reached the same projection signature but correctly reported `No Audio Output` and zero callbacks/DMA, so it was inconclusive. With explicit Cubeb, Null reached projection `0x14564ea8ed5a5f20`, completed 6,436 frames and 27,104 DMA enqueues, and recorded 216 underruns through enqueue 26,762. Metal completed 6,169 frames and 26,119 enqueues with 268 underruns through enqueue 26,037. Null's maximum EFB/depth-peek and producer gap was approximately 1.80 seconds, a separate backend-specific stall.
- **Interpretation:** removing Metal improves aggregate throughput and underrun count, but starvation remains sustained; Metal is a contributor, not the sole cause. The Null stall prevents treating this as a clean renderer performance ranking. The explicit-audio behavior is retained as a hash-pinned diagnostic overlay while ordinary headless and packaged defaults remain unchanged.
- **Next step:** add bounded per-frame VI/core/DSP/audio cadence buckets correlated with EFB wait and host elapsed time. Use them to locate whether the persistent deficit starts in guest scheduling, runtime pacing, or mixer pull before changing any production timing policy.

## 2026-09-04 — G5 cadence root cause and continuity fix

- **Goal:** identify and remove sustained file-select audio starvation without wider resampling, fake audio, or a game-speed change.
- **Bounded experiments:** add aggregate-only frame/audio/DMA active spans and fixed gap buckets; run the accepted O2 route; take early and file-select CPU samples; resolve the hot guest PC against the RMGE01 US symbol map and pinned Petari source; enable the existing exact-PC idle skipper; retest first without and then with the already-reviewed ±2% Apple queue servo; raise only its retained-reserve target from 50% to 80%.
- **Result:** the pre-fix route ran at 50.590 Hz and generated DMA at 27.282 kHz while Cubeb requested 48.004 kHz. `loop_804AB358` consumed 215/1,000 late main-thread samples and lies in RMGE01 `SelectThread`'s idle wait. The exact-DOL idle hint restored 59.884 Hz and 31.946 kHz, reducing 186 underruns to three. The unmodified 50%-reserve servo still produced three late underruns and was rejected. At 80% of the existing 200 ms reserve, the identical route produced 7,096 frames and 30,059 enqueues across 120.433 seconds with zero underruns, one startup backlog correction, zero queue-full drops, and a 156.6 ms worst producer gap. Projection remained `0x14564ea8ed5a5f20`; `fallback=0` and `smc_failed=0`.
- **Evidence:** `patches/ModernGekko/0006-runner-cadence-diagnostics.patch`, `0007-rmge01-staticrecomp-idle.patch`, `patches/ModernGekko-dolphin/0004-apple-staticrecomp-audio-reserve.patch`, `AUDIO.md`, `PERF.md`, and ignored `generated/runtime/g5-*-r4..r7` logs/samples.
- **Interpretation:** G5 is met. The change restores guest idle semantics through Dolphin's existing core-timing path and retains a bounded queue reserve; it does not speed up game time or widen the ±2% servo. Correct pitch across gameplay, Wii Remote speaker routing, interruption recovery, and soak remain G9 work rather than being inferred from file select.
- **Next step:** begin G6 from the proven fresh file: route Star Festival/opening to Gateway Galaxy, add dedicated movement/jump/Spin/pointer controls as needed, and preserve continuous-audio/native-only telemetry through the first Grand Star/save/relaunch loop.

## 2026-09-04 — G6 opening playable; gameplay cadence defect exposed

- **Goal:** advance the fresh RMGE01 file from the opening through Gateway Galaxy while retaining visible input, native-only AOT, graphics, and audio evidence.
- **Bounded step:** start from the isolated G6 NAND, activate the selected file with corrected lower IR coordinates, advance the opening story and Peach letter, exercise movement/jump/pointer actions in Star Festival, capture screenshots at state changes, and cleanly stop the long exploration for aggregate telemetry. An experimental 90% reserve was present only to test whether additional headroom affected the result.
- **Result:** screenshots prove the opening book, Peach letter, first visible Star Festival gameplay, position changes after stick input, repeated jumps, and pointer Star Bit collection from 0 to 16. Gateway Galaxy was not reached. Clean shutdown after approximately 53 minutes reported `fallback=0`, `smc_failed=0`, 156,955 frames/3,197.898 seconds (49.081 Hz), 655,035 DMA enqueues/3,198.984 seconds (26.210 kHz), a correct 48.000 kHz host callback, 3,109 underruns from enqueue 20,923 through 654,765, 31 backlog corrections, zero queue-full drops, and a 178.0 ms maximum producer gap.
- **Evidence:** ignored screenshots under `generated/runtime/g6-opening-user-r2/ScreenShots/RMGE01/`, aggregate clean-shutdown telemetry, and the reusable input fixtures under `tests/fixtures/g6-*.json`.
- **Interpretation:** the actual game is visibly controllable, but G6 remains unmet. The nearly identical video/DMA deficits show that active gameplay is running at about 81.9% reference cadence; the mixer is consuming correctly and cannot synthesize missing guest production. Raising the retained queue target from 80% to 90% did not help and was reverted, preserving the smaller proven G5 policy.
- **Next step:** reproduce visible Star Festival gameplay in a fresh bounded run, capture a CPU sample there, resolve the dominant guest PC against the exact RMGE01 symbols, and test the narrowest semantic idle hint before continuing the Gateway route.

## 2026-09-04 — G6 true-1× AOT chunk-size discriminator

- **Goal:** recover reference gameplay cadence without changing guest timing, EFB semantics, audio pitch, or runtime fallback policy.
- **Hypothesis:** the 331-function/4,096-instruction C output is large enough to create an instruction-cache bottleneck during active play; smaller generated functions may improve host compilation and runtime locality.
- **State correction:** the earlier active-play profiles were not native 1×. The frontend `config.ini` value `resolution=1920x1080` overrides `Config/GFX.ini` and maps to Dolphin EFB scale 3. The matched runs below use frontend `resolution=640x528`, which maps to EFB scale 1. Prior 3× evidence remains valid for visible behavior, input, AOT coverage, and the existence of the cadence defect, but not as a native-resolution performance comparison.
- **Bounded experiments:** compare accepted O2/4,096 with O2/1,024 and O2/256 C chunks from the same copied Star Festival state at true 1× and dual core; audit each module before launch; capture 60-second active-gameplay samples; stop by exact PID for aggregate telemetry. Build an O1/4,096 candidate only far enough to reject its accidental macOS 26.0 target and 2.8% size increase. Finally, test O2/1,024 generated sources compiled with `-Oz` after a temporary local template validation change, then revert that change after the measured regression.
- **Result:** accepted O2/4,096 produced 15,706 frames/306.750 seconds (51.20 Hz) and 65,692 DMA granules/307.567 seconds (27.34 kHz), with 271 underruns. O2/1,024 reduced the module from 100,762,840 bytes to 93,190,792 bytes and produced 19,097 frames/359.711 seconds (53.09 Hz) and 79,973 granules/361.515 seconds (28.31 kHz), with 254 underruns. O2/256 produced 16,313 frames/308.545 seconds (52.87 Hz) and 68,591 granules/311.381 seconds (28.19 kHz), with 225 underruns. All three retained `fallback=0`, `smc_failed=0`, ABI 3, CPU state size 3,528, entry `0x8000403c`, 2 code ranges, and 19 SMC ranges. The 1,024 split is the best of this bounded curve but remains about 11% below the 59.94 Hz/32 kHz target.
- **Rejected size build:** the 1,024-chunk `-Oz` module was 61,174,312 bytes and passed the same ABI/range/macOS-14.0 audit, but visible Star Festival play fell to about 21–22 FPS. Its clean full-route counters were 10,480 frames/310.506 seconds (33.75 Hz), 44,029 DMA granules/312.476 seconds (18.04 kHz), and 651 underruns. The sample was dominated by uninlined floating-point/runtime helpers. `-Oz` is rejected and its temporary template edit was removed.
- **Cache finding:** `moderngekko-port` currently hashes the DOL, DolRecomp binary, backend, compiler, and optimization level, but not the effective `DOLRECOMP_C_CHUNK_INSTRUCTIONS` or dispatch-mode environment. The 4,096, 1,024, and 256 outputs can therefore receive the same cache suffix. Separate output roots prevented overwrite in these experiments, but the 1,024 candidate cannot replace the accepted module until cache identity is made reproducible and a rebuilt artifact passes regression.
- **Evidence:** ignored modules under `generated/modules-c1024/`, `generated/modules-c256/`, and `generated/modules-c1024-oz/`; ignored copied runtime states and samples `generated/runtime/g6-*-native-r8..r11`; protected Star Festival screenshots; clean-shutdown telemetry above.
- **Interpretation:** finer C chunking yields a real but insufficient 3.7% cadence improvement over the matched 4,096 baseline. Very small chunks slightly regress, and optimizing for minimum code size severely regresses because hot helper calls stop inlining. G6 remains unmet; no Gateway, Spin, Grand Star, Observatory save, or relaunch/load claim is made.
- **Next step:** make C-generation options part of the verified cache identity, reproduce the 1,024 artifact through the normal script, and use the active-gameplay samples to test one narrow O2 helper/inlining hypothesis before resuming the Gateway route.

## 2026-09-04 — cache identity fixed; G5 continuity reopened

- **Goal:** make the best bounded C1024 result reproducible, then prove it does not regress the accepted G5 file-select/audio route.
- **Hypothesis:** explicit codegen options in the cache identity will reproduce the isolated artifact byte-for-byte; C1024 will preserve the zero-underrun G5 route, with dual core available as one bounded discriminator if needed.
- **Bounded step:** add validated `--c-chunk-instructions` and `--dispatch-lookup` port options; set both in DolRecomp's controlled environment, cache identity, and manifest; pin the overlay patch; rebuild via `scripts/build-module.sh`; strengthen module audit to compare descriptor ranges with generated source count; run matched true-1× G5 routes for C1024 single core, accepted 4,096 single core, and C1024 dual core.
- **Result:** the normal build created cache suffix `fd7022cf46adb6b9`, recorded 1,024/linear in its manifest, and reproduced the isolated 93,190,792-byte module byte-for-byte at SHA-256 `34488efb37e30502f1e40e047d91bc69e78c709d129ece39c8b7089e6095a59c`. ABI 3, state size 3,528, entry `0x8000403c`, 2 code ranges, 19 SMC ranges, 1,322 chunk ranges, zero unknowns, and macOS 14.0 all pass. Bootstrap, repository safety, Wii-mode, controller, app-config, and module audits pass.
- **Regression result:** all three routes reached projection `0x14564ea8ed5a5f20`, four button transitions, final IR `(655,529)`, nonzero output, `fallback=0`, and `smc_failed=0`. C1024 single core produced 58.34 Hz/31.46 kHz with 13 underruns; accepted 4,096 single core produced 56.77 Hz/30.52 kHz with 30; C1024 dual core produced 57.73 Hz/30.74 kHz with 26. Dual core is rejected. The current comparison favors C1024, but none reproduced the prior zero-underrun acceptance.
- **Environment:** the fanless MacBookAir10,1 had just completed the full 1,322-object ThinLTO build. Load averages were still declining; Low Power Mode was off and `pmset -g therm` reported no thermal or performance warning. This may contaminate the absolute rates but does not prove the cause.
- **Evidence:** hash-pinned `patches/ModernGekko/0008-module-cache-codegen-options.patch`; ignored `generated/modules/...-fd7022cf46adb6b9/`; ignored G5 profiles `g5-*-r12..r14`; clean-shutdown telemetry; protected file-select screenshot.
- **Interpretation:** cache integrity and byte reproducibility are proven. G5's visual/input/AOT evidence remains intact, but its continuity stability is reopened under the goal-loop regression rule. C1024 remains a working candidate, not an accepted/package identity.
- **Next step:** allow the host to return to a low-load state, rerun C1024 single core at true 1× with a file-select CPU sample, and restore G5 only if continuous delivery reproduces without a new timing or buffer policy.

## 2026-09-04 — G5 restored; C1024 packaged with identity enforcement

- **Goal:** resolve the reopened continuity gate and promote only a package proven to contain the accepted module.
- **Hypothesis:** the post-link results were load-sensitive; a cooled, unsampled C1024 single-core control should recover reference file-select cadence without policy changes. Packaging must follow the audited active-module marker rather than a cache-specific path.
- **Bounded step:** run one cooled sampled diagnostic, then one cooled unsampled control from the same G5 source snapshot at true 1×; rebuild the private app; compare source and embedded identities; fix the discovered hardcoded package paths; first prove the strengthened audit rejects the stale package, then rebuild and pass it.
- **Result:** the sampled run produced 31.70 kHz with 10 underruns and showed no hot `SelectThread`; exact floating-point conversion/multiply and paired-single helpers dominated. The decisive unsampled run produced 7,466 frames/125.242 seconds (59.61 Hz), 31,591 DMA granules/126.470 seconds (31.97 kHz), zero underruns, one startup-only backlog correction, zero hard drops, accepted projection `0x14564ea8ed5a5f20`, four input transitions, final IR `(655,529)`, `fallback=0`, and `smc_failed=0`.
- **Package finding:** both package scripts hardcoded the old `ea42...` module path. The old signed dylib therefore survived the first rebuild and the old hardcoded-hash audit falsely passed. Both scripts now consume `active-module.txt`; the audit signs a same-named temporary copy of the audited active source and compares that deterministic signed hash with the package. It rejected the stale app (expected `e4953c...`, found old `6e90bf...`) before the corrected rebuild passed.
- **Accepted identity:** unsigned module SHA-256 `34488efb37e30502f1e40e047d91bc69e78c709d129ece39c8b7089e6095a59c`; packaged signed module SHA-256 `e4953cc272310fd20469e6b3430858dba7e38f637e70521324baf6e39916ac13`.
- **Evidence:** ignored `g5-c1024-cooled-r15` sample, ignored `g5-c1024-cooled-control-r16` state, exact clean-shutdown telemetry, strengthened package scripts, and passing module/private-app audits.
- **Interpretation:** G5 is met again and C1024 is the accepted working/package identity. The load-sensitive failures remain useful performance evidence, but the cooled control satisfies the existing route without a buffer, pitch, timing, or semantic change. G6 remains unmet because active Star Festival play is still only 53.09 Hz/28.31 kHz and the Gateway/Grand-Star/save loop is incomplete.
- **Next step:** resume G6 on C1024. The pinned LLVM backend is not an Apple route—it explicitly accepts only x86-64 Linux/Windows—so do not install LLVM 19/20 for this blocker. Continue with either a semantics-preserving C-helper/profile optimization or progression evidence that does not conceal the cadence defect.

## 2026-09-04 — Bowser transition reached; visible 19 FPS stutter confirmed

- **Goal:** resume the exact G6 route on the accepted true-1x C1024 identity while preserving honest visible-play and cadence evidence.
- **Bounded step:** route the existing fresh file through the Star Festival village using short deterministic stick sequences, capture every navigation state, trigger the Bowser attack transition, then stop progression when the user reported obvious stuttering and approximately 18 FPS. Sample the live title, CPU stacks, and process load concurrently; terminate only the exact runtime PID for aggregate shutdown telemetry.
- **Result:** visible evidence now reaches the village road, Toad dialogue, plaza, Bowser fleet cinematic, and post-attack plaza. Movement, repeated jump input, camera input, and pointer-driven Star Bit collection remained responsive before the slowdown. The title sample independently reproduced 19.4 FPS and then 29.1–38.6 FPS. Across the full run, graphics averaged 42.047 Hz and DMA production 22.458 kHz, with 3,620 underruns, 67.63% of frames at least 20 ms apart, and a 542.5 ms maximum frame gap. Native-only execution remained intact (`fallback=0`, `smc_failed=0`).
- **Profile:** the single CPU-GPU thread saturated one host core. Correct synchronous Metal EFB depth reads consumed 2.28% of graphics active time in aggregate and peaked at 12.871 ms; the sample caught 273 waits in `Metal::StagingTexture::Flush`. Generated paired-single/float helpers and guest display-list/vertex work account for much of the remaining CPU cost. No thermal/performance warning was recorded and Low Power Mode was off.
- **Source/build review:** the selected module already compiles Release C with `-O2`, ThinLTO, `-ffp-contract=off`, and `-fno-fast-math`. Bit conversion and the common unquantised paired-single load path are already source-inline, while `ppc_fmuls` retains required 25-bit rounding, non-IEEE arithmetic, exception gating, and FPSCR/result updates. The linked image still materialises hot helper bodies. This rules out “turn on LTO” as the next experiment and makes blanket force-inlining/fast-math unsafe.
- **Interpretation:** the user's visible report is confirmed; title-bar snapshots near 49 FPS were misleading and are not playability evidence. G6 remains unmet. EFB readback is an actionable hitch contributor, but disabling or returning stale depth would violate pointer correctness and cannot be used as a performance fix. The larger deficit is CPU throughput on the portable C AOT path.
- **Evidence:** ignored `generated/runtime/g6-c1024-accepted-r17/checkpoint-*.png`, `sample-user-visible-stutter.txt`, exact clean-shutdown telemetry, and reusable short diagonal controller fixtures.
- **Next step:** add per-frame EFB depth-read aggregation/correlation and test a semantics-preserving same-frame readback reduction only if exact coordinates/results prove equivalence; in parallel, isolate one generated paired-single helper optimization with an exact active-scene comparator. Do not resume the acceptance route until the visible cadence improves materially.

## 2026-09-04 — indexed dispatch accepted; blanket force-inline rejected

- **Goal:** materially reduce the confirmed active-gameplay stutter without changing guest timing, floating-point semantics, EFB correctness, or fallback policy.
- **Bounded experiments:** first force-inline only the hot paired-single load helper and compare an exact scripted Star Festival route; restore it after regression. Then enable DolRecomp's existing indexed dispatch, repair the module-template coverage parser for indexed headers, compare the same pristine-state route, and require exact projection/input/native-only signatures. Finally reproduce the winner through the normal cache-aware build, audit and package it, and rerun the established G5 route.
- **Rejected result:** forced paired-single-load inlining grew code and reduced graphics/DMA throughput by 6.63%/6.75%, increased underruns from 280 to 337, and visibly fell to 15.7 FPS. The header change is reverted.
- **Accepted result:** indexed dispatch improved matched graphics from 51.528 to 55.104 Hz (+6.94%), DMA from 27.515 to 29.434 kHz (+6.97%), and reduced underruns from 280 to 155. It retained projection `0x190cf1d3666f1e56`, all 44 input transitions, `fallback=0`, and `smc_failed=0`; the active screenshot read 45.3 FPS. The canonical build reproduced SHA-256 `80411bfa49266f23c44657f03e79d53f6ed1b660a406c0a0b1b051e3f08f97d1` byte-for-byte and passed module/package audits.
- **Regression:** the accepted indexed artifact completed G5 at 59.744 Hz/31.977 kHz with zero underruns, one startup-only backlog correction, the exact file-select projection, four input transitions, and native-only execution.
- **Evidence:** pinned `patches/ModernGekko-dolphin/0005-indexed-module-tables.patch`; explicit indexed default in `scripts/build-module.sh`; exact fixture `tests/fixtures/g6-perf-route.json`; ignored runtime evidence `g6-perf-accepted-r19`, `g6-perf-psq-inline-r20`, `g6-perf-indexed-r22`, and `g5-indexed-regression-r23`; signed private app audit.
- **Interpretation:** the dispatch cost was a high-leverage component of the portable C bottleneck. The improvement is genuine and reproducible, but visible gameplay remains below target and G6 progression/save criteria remain unmet.
- **Next step:** profile the indexed build in the same active scene, then isolate the next semantics-preserving hot path or exact same-frame EFB readback redundancy. Do not trade correctness or pitch for the remaining cadence gap.

## 2026-09-04 — indexed hot paths profiled; two narrow candidates rejected

- **Goal:** identify the next semantics-preserving improvement after indexed dispatch, using the exact active Star Festival comparator.
- **Profile:** an active-scene sample kept `StaticRecompCore::Run`, GPU FIFO work, chassis dispatch, paired-single load/store, exact multiply, depth-peek refresh, and bit conversion at the top. The accepted image contained 197 materialised paired-single inline helper bodies.
- **Paired-single experiment:** route the common non-indexed GQR0 W0/W1 loads and stores through four shared helpers while leaving the remaining generic cases unchanged. The valid rebuilt candidate was 93,035,096 bytes, had 14 remaining inline bodies and 12,419 specialized calls, and retained the exact ABI/state/entry/code/SMC metadata. It produced 55.716 Hz graphics, 29.733 kHz DMA, and 129 underruns; the immediate indexed control produced 55.901 Hz, 29.830 kHz, and 124 underruns. The 0.33%/0.32% regression rejects it; source, tests, emitter, and the root recompiler were restored, and focused GXRuntime tests passed.
- **Chassis experiment:** because `StaticRecompCore` already excludes registered host-call PCs before module entry, remove only the duplicate host callback from the outer chassis path while preserving the full hook-aware path for generated cross-chunk calls. The candidate passed arm64/macOS-14.0 and descriptor checks, retained projection `0x190cf1d3666f1e56`, 44 input transitions, `fallback=0`, and `smc_failed=0`, and visibly reached the correct Star Festival overlook. It produced 17,965 frames/319.492 seconds (56.230 Hz), 75,393 DMA granules/321.470 seconds (30.019 kHz), and 120 underruns. The 0.59%/0.64% edge over the immediate control is not material enough to accept. The initially watched `hook_fb` counter is instruction fallback, not host-callback traffic, so it cannot validate this hypothesis. The template was restored.
- **Evidence:** ignored `g6-profile-indexed-r24`, `g6-perf-psq-specialized-r25`, `g6-perf-indexed-control-r26`, and `g6-perf-fast-entry-r27`; clean shutdown telemetry and an active-scene screenshot; passing restored-source checks.
- **Interpretation:** code-size reduction and removal of a logically redundant boundary check are not automatically throughput wins on this workload. Neither candidate clears measurement noise or materially changes visible cadence. The accepted indexed module and package remain unchanged; G6 remains unmet.
- **Next step:** correlate same-frame depth peeks by coordinates and returned values before considering any readback coalescing. Preserve exact EFB semantics unless equivalence is proven.

## 2026-09-04 — same-frame EFB correlation measured; duplicate shortcut rejected

- **Goal:** determine whether repeated Star Pointer depth reads cause enough redundant GPU synchronization to justify a correctness-gated coalescing experiment.
- **Bounded step:** add default-off per-read CSV tracing and aggregate per-presented-frame counters; run the unchanged accepted indexed module through the exact 44-transition, 1× Star Festival fixture; group reads by frame, channel, coordinate, and returned depth.
- **Result:** 31,349 depth reads occurred across 12,377 frames with peeks, with a maximum of three per frame. Median/p95/p99/max latency was 0.079/0.847/0.976/7.644 ms. All 1,473 repeated same-frame coordinates returned the same depth, but their combined 17.799 ms was only 0.260% of the 6.840-second EFB total. Projection remained `0x190cf1d3666f1e56`, all 44 input transitions arrived, and `fallback=0`/`smc_failed=0`.
- **Evidence:** hash-pinned root and Dolphin trace overlays; ignored `generated/runtime/g6-efb-trace-r28/efb.csv`; aggregate clean-shutdown telemetry; new `docs/EFB-READBACK.md`.
- **Interpretation:** Dolphin's deferred tile cache already absorbs the repeated-read cost after the first synchronized population. Removing the duplicates would create stale-value risk without a material performance gain, so no EFB behavior is changed.
- **Next step:** resume G6 progression on the accepted indexed module. Capture caller-resolved traces when Gateway/Pull Star interaction becomes available; do not spend another iteration on generic same-frame caching.

## 2026-09-04 — post-attack CPU review and phase tracing

- **Goal:** review the reported 18 FPS stutter after the accepted indexed-dispatch improvement and identify the next falsifiable measurement without changing guest semantics.
- **Bounded step:** preserve the live post-attack progression; capture effects-heavy and sparse views; take one 10-second process sample; compare CPU and video-thread residency; add default-off cross-thread timestamps for VI, frame begin/end, present queue/completion, input, DMA production, and audio callbacks; compile and smoke the trace using Null/headless.
- **Result:** sparse views reached 59.9 FPS, while Bowser ship/fire/fountain/plaza views commonly ranged from about 40 to 50 FPS. The CPU thread spent 5,932/7,000 samples in `StaticRecompCore::Run`, with 5,336 below `chassis_dispatch`. The video thread waited in 4,166/7,011 samples; synchronous EFB-depth wait appeared in only 48. No single generated guest function dominated. The isolated smoke emitted 31,408 well-formed event rows across every instrumented boundary.
- **Interpretation:** the earlier approximately 18 FPS transition remains a genuine failure, but there is no fixed 18 FPS cap. Portable-AOT guest execution is the dominant remaining cost and rendering contributes intermittent hitches. Blanket fast math, semantic batching, stale EFB reads, or another tiny chassis shortcut are not evidence-backed fixes.
- **Evidence:** ignored `generated/runtime/g6-progression-r29/sample-heavy-plaza.txt`, visible progression checkpoints, ignored `generated/runtime/phase-trace-smoke.csv`, hash-pinned phase-trace overlays, and `PHASE-TRACE.md`.
- **Next step:** capture the exact copied Star Festival route with Metal and phase tracing, repeat uninstrumented to quantify observer cost, and use interval distributions to choose one narrow generated-code optimization. G6 remains unmet.

## 2026-09-04 — phase-localized PGO improvement accepted

- **Goal:** materially reduce the confirmed G6 active-gameplay stutter without altering guest timing, floating-point semantics, EFB behavior, or dispatch correctness.
- **Hypothesis:** because matched tracing localizes the dominant delay before presentation and sampling shows distributed generated work rather than one safe helper, route-trained Clang PGO can improve layout/inlining across that workload while preserving exact execution signatures.
- **Bounded step:** run sequential traced/untraced Metal routes from twin state; instrument the unchanged O2/ThinLTO/indexed module; train on the same 44-transition route; merge the private local profile; build one profile-use candidate; compare fresh control/candidate states; regress G5; add hash-keyed `--profile-use` support; reproduce through the normal module/package path.
- **Result:** traced versus control throughput was 54.397/54.649 Hz graphics and 29.091/29.279 kHz DMA, limiting observer cost to 0.46%/0.64%. Presentation itself was sub-millisecond at p99 while pre-present frame work exceeded 20 ms on 29.21% of intervals. PGO then improved the matched route from 54.961 to 57.444 Hz (+4.52%) and from 29.455 to 30.784 kHz DMA (+4.51%); underruns fell from 175 to 79. Projection `0x190cf1d3666f1e56`, 44 transitions, `fallback=0`, and `smc_failed=0` matched. The fresh G5 confirmation produced 59.77 Hz/31.98 kHz, zero underruns, exact projection, four transitions, and native-only execution.
- **Reproducibility:** `moderngekko-port --profile-use` now requires Clang+C, hashes the profile into cache identity, records it in the manifest, and supplies it to compile/link. `GALAXYPAD_PGO_PROFILE` is explicit in `scripts/build-module.sh`. The normal build reproduced the isolated 99,510,616-byte module byte-for-byte at SHA-256 `6fba4629eb07e79132a344bc2d68bf9f62627478d5a7dc8a19e1af291942c916` (cache suffix `1a7fde44f42859a5`, profile SHA-256 `f39a3cedeb6c49f35c411356893c619dc347eb1b0bb5986687f25cf81e7d460d`). Module and private-package audits pass; packaged signed dylib SHA-256 is `25e17d0e730b512f20083db26d3a7f58bc60da126161fe34c227357372c65393`. The smoke test now follows the audited active-module marker and passed against this exact module.
- **Evidence:** ignored `generated/runtime/g6-phase-trace-r30`, `g6-phase-control-r31`, `g6-pgo-train-r32`, `g6-pgo-control-r33`, `g6-pgo-candidate-r34`, `g5-pgo-regression-r35`, and `g5-pgo-regression-r36`; ignored `generated/pgo/`; hash-pinned profile-build overlay; `PHASE-TRACE.md`, `PERF.md`, and clean-shutdown telemetry.
- **Interpretation:** PGO is a genuine semantics-preserving improvement and is now the selected local/package identity. It substantially reduces but does not eliminate the reported stutter; the route remains short of reference cadence, and no Gateway, Spin, Grand Star, Observatory save, or relaunch/load acceptance claim is made.
- **Next step:** resume fresh G6 progression on the accepted PGO identity and capture transition/active-scene cadence plus Grand Star/save/relaunch evidence. If dense effects still fall materially below reference, use the PGO counts to isolate one exact generated helper or hot guest loop; do not alter EFB depth, timing, or strict FP semantics.

## 2026-09-04 — long visible PGO progression review

- **Goal:** directly review the reported obvious stutter and 18 FPS behavior on the newly accepted PGO identity, without treating a short benchmark or title-bar snapshot as product acceptance.
- **Bounded step:** keep one exact Metal/Cubeb process alive; run the established route, then navigate the Star Festival with short deterministic Pipe inputs and fresh screenshots; inspect sparse, lake, road, and dense town compositions; terminate only the exact PID and collect aggregate telemetry.
- **Result:** no inspected PGO frame reproduced 18 FPS before the Bowser transition. Sparse views held 59.9–60.0 FPS, the densest observed town/lake frame read 47.9 FPS, and other inspected views ranged from about 50 to 62.5 FPS. Across 5,208.246 seconds the run produced 295,583 frames (56.753 Hz); DMA produced 1,233,012 granules across 5,208.612 seconds (30.301 kHz). It retained native-only execution, 64 input transitions, and the exact active module, but accumulated 1,678 underruns and 51 backlog drops.
- **Evidence:** ignored `generated/runtime/g6-pgo-progression-r37/checkpoint-*.png`, exact clean-shutdown telemetry, and reusable short axis/diagonal fixtures.
- **Interpretation:** PGO materially removes the old 19.4–38.6 FPS failure in the covered pre-attack route, but it does not close sustained cadence/audio continuity. Fixed stick vectors also drift relative to screen direction as the game camera rotates, so the route remains screenshot-gated rather than blindly scripted. No Bowser re-trigger, Gateway, Spin, Grand Star, save, or relaunch/load claim is made.
- **Next step:** continue G6 from a fresh PGO state with shorter camera-aware route segments, reach the transition and Gateway, then compare post-attack dense effects and verify Grand Star/save/relaunch before changing another hot path.

## 2026-09-04 — PGO Bowser and Gateway reproduction

- **Goal:** reproduce the user's reported stutter in the effects-heavy opening transition on the exact accepted PGO identity and continue the real G6 progression route rather than extrapolating from the pre-attack benchmark.
- **Bounded step:** launch one Metal/Cubeb process from a copied disposable fresh-file state; use screenshot-gated short Pipe inputs to trigger Bowser, traverse the attacked plaza, enter the castle transition, and reach Gateway without restarting or changing the module. A later live configuration audit found that this run was actually at EFB scale 3, so it is not a 1x baseline.
- **Result:** the checkpoint immediately before Bowser read 60.4 FPS. The ship cinematic read 43.0 and 42.5 FPS; post-attack traversal generally read about 44–53 FPS and reached 39.9 FPS under the ship. The castle transition read 37.0, 39.4, 38.1, and 39.1 FPS at four separated points. Gateway was reached on the same uninterrupted run, where directional movement and jumping visibly worked at roughly 45–53 FPS.
- **Evidence:** exact module SHA-256 `6fba4629eb07e79132a344bc2d68bf9f62627478d5a7dc8a19e1af291942c916`; ignored `generated/runtime/g6-pgo-bowser-r38/checkpoint-bowser-*.png`, `checkpoint-progress-*.png`, `checkpoint-castle-cinematic-*.png`, and `checkpoint-gateway-*.png`; reusable short jump-turn fixtures.
- **Interpretation:** the old 18 FPS floor did not recur, but the complaint is confirmed: dense effects and the castle cinematic remain conspicuously below reference cadence after PGO. Gateway reachability and basic control are now proven; Spin, the first Grand Star, Observatory save, clean relaunch/load, aggregate telemetry, and broader audio acceptance remain open.
- **Next step:** preserve the live Gateway state, complete its tutorial and first Grand Star, then cleanly terminate for aggregate telemetry. Use the PGO profile to select a narrow semantic-preserving CPU optimization only after the progression/save evidence is secured.

## 2026-09-04 — Gateway slowdown review

- **Goal:** explain the user's visible approximately 20 FPS observation without dismissing it or corrupting the live first-star progression state.
- **Bounded step:** compare foreground, covered-window, stationary, and moving title-rate observations; collect two 10-second CPU samples in the same Gateway state; move the pointer to a corner as a one-variable discriminator; audit the effective renderer configuration.
- **Result:** the covered/resized window briefly read 22.3 FPS, the fully foregrounded stationary state recovered to 59.9 FPS, and active traversal read 50.3 FPS. The two profiles placed 1,450/7,401 and 1,629/7,873 CPU-thread samples in synchronous Metal depth readback waits. Pointer-at-corner did not improve that share. The effective `GFX.ini` records `InternalResolution = 3`, invalidating any claim that this run is the required 1x baseline.
- **Interpretation:** there is no fixed 20 FPS cap, but the stutter is real. Host-window/tool contention explains the lowest transient observation; accidental 3x rendering adds avoidable work; Gateway's correct Star Pointer depth path creates a separate material synchronization cost. None justifies stale/disabled EFB reads.
- **Evidence:** ignored `generated/runtime/g6-pgo-bowser-r38/sample-gateway-foreground.txt`, `sample-gateway-pointer-corner.txt`, live title queries, screenshots, and the effective runtime `Config/GFX.ini`.
- **Next step:** finish the Grand Star and in-game save on the uninterrupted run, then relaunch the exact PGO module at explicit scale 1 with EFB tracing and repeat the Gateway route. Compare depth values/targets, read count, stall, frame distribution, and audio cadence before selecting any default-off optimization.

## 2026-09-05 — native-1× cause isolated and default fixed

- **Goal:** explain and remove the reproduced approximately-20-FPS floor without weakening EFB, timing, floating-point, or native-execution correctness.
- **Bounded step:** relaunch the exact accepted PGO module from a copied fresh state with both frontend `resolution=640x528` and Dolphin `InternalResolution = 1`; replay the opening, Bowser attack, castle transition, and Gateway; sample Gateway; cleanly stop for aggregate telemetry; then audit how missing and packaged frontend profiles choose scale.
- **Result:** static title/story/Gateway views held 59.8–60.0 FPS, ordinary Star Festival traversal generally read 49–54 FPS, Bowser arrival read 41.7–44.9 FPS, and castle abduction read 38.8–39.9 FPS. Gateway movement returned to 59.9–60.0 FPS. The five-second Gateway sample put only 78/3,599 CPU-thread samples in synchronous EFB-depth waits. Clean shutdown after 10,032.273 seconds produced 572,694 frames (57.085 Hz), 30.479 kHz DMA, 2,692 underruns, 48 backlog drops, 302 input transitions, and native-only execution; 10.123% of frames exceeded 20 ms.
- **Cause and fix:** missing profiles were created at frontend `1920x1080`, which maps to Dolphin EFB scale 3, while the package seed used unsupported `640x456`. Hash-pinned `patches/ModernGekko/0012-native-resolution-default.patch` changes missing-profile creation to `640x528`/scale 1; the package seed and its test now match. Existing explicit settings are not migrated. The frontend regression test, fresh-profile smoke, rebuilt ad-hoc-signed app, and private-app audit pass.
- **Interpretation:** accidental 3× rendering caused the catastrophic approximately-20-FPS behavior. Native 1× removes that floor but does not remove all stutter: the effects-heavy castle transition remains around 39 FPS and aggregate audio still underruns. Correct EFB/deferred-invalidation behavior is retained. The prior scale-3 run was user-terminated during an apparent tool-induced freeze, not cleanly completed.
- **Next step:** keep G6 open. Complete the rabbit/Grand Star/save/relaunch route at native 1×, then train or compare a transition-inclusive PGO profile before accepting another CPU-path change.

## 2026-09-05 — native-1× freeze review and Gateway-trained PGO rejection

- **Goal:** distinguish the user-reported freeze from the remaining low-frame-rate hotspot, then test whether adding the newly reached Gateway workload to PGO materially improves native-1× play.
- **Hypothesis:** if the apparent freeze is a guest deadlock, native/graphics counters will stop; otherwise the surviving process will continue advancing. If missing Gateway profile coverage limits the accepted module, a merged opening-plus-Gateway profile will improve a state-matched route without changing input, projection, fallback, or SMC signatures.
- **Bounded step:** inspect the killed-window process; continue the preserved native-1× state through Bowser's attack, castle abduction, and Gateway; capture separated FPS checkpoints and a 10-second active-scene sample; train an instrumented module on eight Gateway sweeps; merge the private profiles; build one candidate; compare accepted and candidate modules from the same state with the same eight sweeps and graceful shutdown.
- **Commands:** exact `moderngekko-run --graphics Metal` launches with isolated copied user directories; menu-driven state load/quit; `wii-pipe.py` reconnect plus eight `g6-rabbit-sweep.json` repetitions; `sample`; LLVM profile merge; `GALAXYPAD_PGO_PROFILE=generated/pgo/rmge01-opening-gateway.profdata GALAXYPAD_MODULE_OUTPUT=generated/modules-pgo-gateway ./scripts/build-module.sh`.
- **Result:** the window-close incident left an orphan runtime still advancing near 60 Hz, so it was not a guest freeze. The continued heavy scenes nevertheless reproduced the real problem: about 31–44 FPS during the attack and 23–36 FPS during castle abduction, followed by recovery to about 60 FPS in Gateway. The active Gateway sample put 4,583/7,038 CPU-thread samples below generated dispatch and only 236/7,038 (3.35%) in synchronous Metal depth readback. In the valid matched comparison, accepted produced 58.059 Hz/13.081% gaps at least 20 ms; the candidate produced 57.825 Hz/13.392%. Both recorded exactly 130 input transitions, 47 underruns, projection `0xf8f6f1fa3d9e4c07`, `fallback=0`, and `smc_failed=0`.
- **Evidence:** ignored `generated/runtime/g6-pgo-gateway-train-r44`, `g6-pgo-bench-accepted-r46`, and `g6-pgo-bench-candidate-r47`; private merged profile SHA-256 `a8f42ab70fab0b0da57643ed9b0499645d4218df02c15b6fc1f56ead69ac434`; rejected candidate SHA-256 `dad82fd6a1a15121d398cff3abe016df51200fa9e7bfdb727b654c50b5dff729`; Gateway state slot 2 SHA-256 `618410501798a7e6cd86eff67d002b3dfd0268b24838793705981bae26a2729e`.
- **Interpretation:** the catastrophic scale-3 floor is fixed, but the native-1× castle transition is still plainly too slow and is CPU/AOT dominated. Additional broad PGO training does not improve it. Keep the accepted module unchanged and avoid EFB shortcuts or compiler churn unsupported by a measured win.
- **Next step:** resume the preserved Gateway state on the accepted module, complete the rabbit/Grand Star/save/relaunch G6 route, and use a transition-specific trace only if progression remains stable enough to isolate one exact hot guest path.

## 2026-09-05 — first Star Bunny caught; hide-and-seek checkpointed

- **Goal:** advance the lowest unmet G6 progression gate on the unchanged accepted native-1× module.
- **Hypothesis:** the preserved Gateway state is live rather than frozen, and calibrated Star Pointer plus short camera-aware movement segments can complete the first chase and expose the three-rabbit tutorial without changing runtime semantics.
- **Bounded step:** relaunch one isolated Metal/Cubeb runtime, load preserved slot 2, reconnect the Pipe controller, locate the taunting rabbit by screenshot-gated traversal, calibrate the pointer to its screen position, stun it with Star Bits, catch it with a directional jump, advance the two dialogue pages, then exercise the crater and pipe hiding places. Save a post-catch continuation state and shut down through the application menu.
- **Result:** the first rabbit was visibly caught and the game advanced to the three-rabbit hide-and-seek sequence. Crater traversal produced the expected planet-scale launch/coin path, and entering the green pipe visibly released its hidden rabbit. The released-rabbit routes still need controlled catches. Clean shutdown retained `fallback=0` and `smc_failed=0` after 174 input transitions; the long manual/state-load session is not a performance comparator and still recorded 1,475 audio underruns.
- **Evidence:** ignored `generated/runtime/g6-gateway-continue-r48/ScreenShots/RMGE01/`; post-catch state slot 3 SHA-256 `79d2af10bf4a4056dd2be81af327a3aff30288a80df0e15e698ef862111271ea`; clean `run-r48.log`; focused ground-pound, pipe-entry, and calibrated rabbit-pointer fixtures under `tests/fixtures/`.
- **Interpretation:** G6 progression is healthy past the first chase, and the apparent freeze remains disproven. The current obstacle is deterministic route control for the three released rabbits, not a renderer, input, or AOT correctness failure. G6 remains open.
- **Next step:** resume slot 3, save immediately when each hidden rabbit is released, catch each from a reproducible state, then verify Spin, the first Grand Star, Observatory save, and clean relaunch/load on the accepted module.

## 2026-09-05 — pipe-hidden rabbit caught and relaunch-checked

- **Goal:** turn the released pipe rabbit into a reproducible G6 progression result while continuing to characterize the user's freeze and frame-pacing reports.
- **Bounded step:** preserve an exact mid-pipe slot, replay one short back-right route, capture the rabbit crossing frame-by-frame, save an interception slot, calibrate a 50 ms lead shot, verify the catch dialogue, shut down cleanly, relaunch the accepted module in a fresh isolated runtime, load the caught state, and continue its dialogue with a minimal A-tap fixture.
- **Result:** slot 5 deterministically recreates the pipe exit; slot 6 places Mario at the interception. Pointer `(-0.47, 0.42)` with a 50 ms delay hit the rabbit and produced the visible `You caught me!` dialogue. The settled continuation was saved as slot 7 SHA-256 `09a665490c42a65f4a424754ce85f4fcfbbdea82637a7fec31c391eebb356b07`; a fresh process loaded it and displayed the caught rabbit's `Aw, I knew I should've hidden in the crater.` dialogue. The earlier apparent post-load stall was a test-authoring mistake: `g6-confirm-play.json` intentionally waits 30 seconds. Telemetry continued at about 60 Hz, so it was not a guest freeze. A new focused `g6-tap-a.json` avoids that ambiguity.
- **Performance:** R49 shut down at 59.906 Hz/31.973 kHz with six underruns and 5.244% gaps at least 20 ms. R51's much longer exploratory route averaged 57.690 Hz/30.796 kHz with 523 underruns and 15.517% gaps at least 20 ms. Both retained `fallback=0` and `smc_failed=0`. Native 1x prevents the old 20 FPS floor but does not yet provide uniformly smooth long-session pacing.
- **Interpretation:** one of the three hidden rabbits is now caught and survives emulator-state relaunch/load. This is not the required in-game save proof, and G6 remains open. Crater traversal and the dark grass ringed by green bulbs were reverified, but neither remaining rabbit was caught in this step.
- **Next step:** resume slot 7, use short screenshot-gated inputs to trigger and checkpoint the grass rabbit before it leaves the tuft, then repeat for the crater rabbit. Only after all three catches should the route proceed to Spin, Grand Star, Observatory in-game save, and clean relaunch/load.

## 2026-09-05 — grass rabbit caught; final crater escape path isolated

- **Goal:** continue the native-1x G6 progression from the pipe-rabbit checkpoint while preventing another tool pause from being mistaken for a game freeze.
- **Bounded step:** locate the dark grass ringed by green bulbs, preserve exact pre-release and rabbit-visible states, replace long blind inputs with screenshot-gated pulses, catch the released rabbit on a downhill/right route, checkpoint the result, navigate to the final crater, and sample the crater release frame-by-frame. Recheck the catch against the repeated rabbit dialogue and the reference `TrickRabbit` attack path before claiming progress.
- **Result:** the grass rabbit was released visibly, followed around the lower hemisphere, and stopped by the downhill/right intercept. Its `Talk A` prompt and `Aw, I knew I should've hidden in the crater.` continuation were captured. A text cross-check confirmed that this line is also used for the grass catch; it was not evidence of accidentally retalking only the earlier pipe rabbit. The two-caught continuation was routed to the last crater and preserved immediately before entry as R52 slot 8 SHA-256 `234719b8175f85c945ab73ad6d100efc72c5ac266b20ebe785901576b62fa0aa`. Eight subsecond screenshots show the final rabbit leaving the crater overview toward the far-right spike/rock branch. The first direct Spin intercept missed, so no third-catch, Spin unlock, Grand Star, or in-game save claim is made.
- **Reference check:** `TrickRabbit::receiveMsgPlayerAttack` accepts `MR::isMsgPlayerSpinAttack(msg)` by calling `requestCaught()`, so the next route should use Spin contact rather than depend on scarce Star Bits. The actor also returns to `Wait` when it becomes more than 1,500 units from Mario, giving a bounded stationary-target recovery path after a missed release.
- **Performance:** clean R52 shutdown produced 192,276 frames over 3,233.909 seconds (59.456 Hz), 31.735 kHz DMA, 170 underruns, 49 backlog drops, 12.186% frame gaps at least 20 ms, `fallback=0`, and `smc_failed=0`. This is long exploratory evidence, not a matched optimization result; the old native-scale 18–20 FPS floor stayed absent, while tail pacing/audio remain open.
- **Evidence:** ignored `generated/runtime/g6-gateway-rabbit-servo-r52/`, grass release/catch frames at `07-49-13`, `07-57-12`, and `07-57-45`, crater release sequence at `08-16-30` through `08-16-32`, exact pre-crater state, focused chase/Spin fixtures under `tests/fixtures/`, and clean `run-r52.log`.
- **Next step:** relaunch the accepted module at native 1x, load R52 slot 8, enter the crater, wait out the forced overview, then route from the exit to the captured right-side spike/rock branch and Spin through the stationary or tiring rabbit. After the third dialogue, continue immediately to the Spin tutorial, first Grand Star, Observatory in-game save, and clean relaunch/load proof.

## 2026-09-05 — crater rabbit caught; pipe handoff corrected

- **Goal:** complete the captured crater chase without blind planet sweeps, then reconcile why the three-rabbit completion event did not fire.
- **Bounded step:** extract the Gateway area/child placement tables with pinned local `dtk` and `pyjmap`; identify the four cooperating `RunawayRabbit` placements and their switches; read the live Mario/rabbit positions from the emulator's own MEM1/MEM2 mappings under short debugger stops; use that read-only distance signal to select ordinary Pipe-controller movement; preserve the closest state and finish the collision chase at full game speed.
- **Result:** the crater actor (`Obj_arg0=2`) was reduced from 3,739 to 877 world units, then caught by six uninterrupted left-jump/Spin pulses. Its `Talk A` prompt and full `Aw, I knew I should've hidden in the crater.` dialogue are visible in R53 frames `09-30-08` and `09-30-32`. Live object/nerve inspection also proved the grass actor had reached the same caught path, while the supposed pipe catch had not: the lower-pipe actor transfers to a separate upper-pipe actor controlled by switch 1112. That upper actor remained at `(16820.5, -10518.7, 6419.6)` in its hidden state. This corrects the earlier interpretation rather than silently carrying it forward.
- **Continuation:** R53 slot 8 SHA-256 `b24f828bb5fc29656ada26719a6c55b7837ccd304be7dc75ec65044e6a1ad415` preserves the two genuine catches and places Mario about 760 units from the upper-pipe actor. Backup `RMGE01.s08.caught-crater-grass` SHA-256 `19ecbaf8b7f5e83ceee3c0b87b1617664521c537031491bf3d74fb2e9e637a6e` preserves the same two catches near the lower pipe.
- **Performance:** clean R53 shutdown produced 312,648 frames over 5,682.418 seconds (55.020 Hz), 29.370 kHz DMA, 522 underruns, 107 backlog drops, and 13.537% frame gaps at least 20 ms, with `fallback=0` and `smc_failed=0`. Repeated debugger stops account for the 8.209-second maximum gap, so this navigation run is failure/continuation evidence rather than an optimization comparator.
- **Interpretation:** G6 remains open. The game did not freeze; several apparent stalls were Mario colliding with spike or pipe lips, and position/frame counters continued. The remaining progression task is now exact: enter the lower pipe in the forward direction, catch the released upper continuation, then prove Spin, Grand Star, Observatory save, and clean relaunch/load.
- **Next step:** resume the R53 slot, use the backed-up lower-pipe state if helpful, enter from the lower mouth toward the upper mouth, preserve the released upper actor immediately, and complete its chase before continuing the tutorial.

## 2026-09-05 — catch evidence corrected; grass chase re-checkpointed

- **Goal:** audit the disputed R52/R53 catch claims against live guest state, preserve only genuine progression, and continue the final grass chase without mistaking repeated dialogue or dust frames for a catch.
- **Correction:** R53 slot 8, its named `caught-crater-grass` backup, R52 slot 8, and R52's named `caught-grass` file all loaded with collector count 1 and the grass actor in `Hide`. Their names and screenshots were therefore insufficient and the earlier two-catch claims are retracted. The current lineage was separately verified at collector count 2/3 with the crater and upper-pipe continuations stopped and the grass actor hidden before release.
- **Bounded step:** trigger the grass actor from the genuine 2/3 state; verify its root nerve changes from `Hide` to `Runaway`; preserve the release and near-contact states; derive Mario/rabbit world positions from the runtime's live MEM1 mapping; and use the actor's `WalkerStateRunaway` hysteresis to create far/stationary and near/stationary checkpoints. Every attempted hit was rejected unless the root nerve and collector changed.
- **Result:** the released grass actor remains in `Runaway` and the collector remains exactly 2/3. Slot 8 preserves the released chase, slot 5 preserves a repeatable near-contact route, slot 4 preserves a far stationary actor, slot 3 preserves a grounded 1,208-unit stationary approach, and slot 2 preserves a later stationary view. No screenshot, repeated crater-Luma dialogue, Star Bit impact, or state filename is treated as a catch. G6 remains open.
- **Performance check:** a five-second active native-1x sample of the unchanged accepted PGO module put approximately 82.7% of the 3,580 main-thread samples in `StaticRecompCore::Run`; synchronous `PeekEFBDepth`/Metal flush wait accounted for 150 samples (4.2%). This agrees with the existing conclusion that portable-AOT guest execution is primary and EFB synchronization secondary. The sample followed many state loads/debugger stops and is diagnostic only, not a matched cadence comparator.
- **Next step:** complete real Spin/contact on the grass actor and require collector 3/3 before advancing to the tower, Spin tutorial, first Grand Star, Observatory in-game save, and clean relaunch/load. Keep macOS stability and effects-scene pacing ahead of iPadOS/iOS shell, full three-dot menu, and touch-control promotion.

## 2026-09-06 — grass Star Bit stun proven; near-contact route reduced

- **Goal:** replace ambiguous Star Bit and dust-frame observations with a state-proven grass-rabbit stun, then reduce the remaining physical catch to a short reproducible intercept.
- **Bounded step:** inspect the live P1 pointer, WPad B edge, rabbit Star Pointer target, view/projection matrices, and collector; follow the moving target only while acquiring a valid depth pixel; run the B edge and projectile flight uninterrupted; require the root nerve to leave `Runaway`; then replay ordinary jump and stick sectors from protected stationary checkpoints and retain only measured distance improvements.
- **Result:** the shot consumed input normally and changed the grass actor root from `Runaway` (`806a158c`) to `BlowDamage` (`806a15a4`) while the collector correctly remained 2/3. Slot 6 SHA-256 `53bd057b1b07b1aadb86f7e886b23d3b7aca61068793ad9f5fee186e06770006` preserves that exact verified stun. A separate repeatable route from the stationary slot reduced Mario/rabbit separation from 1,356 to about 250 world units; protected slot backups preserve the 255-unit and pre-profile states. Spin attempts at stale phases did not change the root or collector and are rejected.
- **Interpretation:** pointer delivery and Star Bit damage are now proven on the unchanged accepted native-1x module. The open G6 blocker is narrowly the rabbit's immediate wall-jump reversal and physical Spin overlap, not a frozen runtime or an unresponsive B mapping. No 3/3, tower, Spin unlock, Grand Star, or save claim is made.
- **Next step:** resume the protected 250-unit approach, checkpoint the wall-jump reversal by phase, and use a short direction-adjusted Spin intercept. Require collector 3/3 before continuing to the tower and first Grand Star.

## 2026-09-06 — Gateway tutorial completed; small-planet key encounter checkpointed

- **Goal:** convert the corrected 2/3 rabbit state into uninterrupted visible progress toward the first Grand Star while retaining a safe continuation after every newly proven mechanic.
- **Bounded step:** complete the final grass-rabbit Spin/contact, advance the three-rabbit collector and tower dialogue, climb the tower, acquire Spin, activate and use the Launch Star, collect all five Black Hole planet Star Chips, launch to `HeavensDoorSmallZone`, and apply the Luma's stated Spin-then-contact mechanic to its enemy group.
- **Result:** collector 3/3, tower activation, Spin, the first Launch Star route, all five Star Chips, Launch Star materialization/use, and landing on the small gold planet are visibly proven. Four roaming `KuriboMini` actors were defeated. The remaining key is held by a distinct larger `ChildKuribo` actor associated with `ExterminationKuriboKeySwitch`; a first imprecise contact attempt caused a death, and the protected state was successfully reloaded.
- **Checkpoint:** ignored R55 slot 3 SHA-256 prefix `256b5788` preserves all four mini-Goombas cleared, two life wedges, and the still-live key carrier. The runtime remains intentionally paused and responsive after more than six hours; this is meaningful stability evidence but not the required in-game save/relaunch proof.
- **Performance:** native 1× has removed the earlier scale-3 18–20 FPS floor. Sparse and ordinary Gateway scenes can reach approximately 50–60 FPS, while effects-heavy scenes still fall into roughly the 23–44 FPS range and long-session audio/pacing defects remain open.
- **Next step:** approach the key carrier to confirmed Spin range, stun and contact it, checkpoint the cage unlock, then continue through the first Grand Star and Observatory in-game save/relaunch/load gate before promoting work to the iPadOS/iOS shell, touch controls, and complete three-dot menu.

## 2026-09-06 — resume audit and macOS window-close stability fix

- **Goal:** advance G6 stability after reviewing the original PRD, goal loop, current status, and continuation record.
- **Hypothesis:** the reported hidden runtime after closing the window is caused by `WindowDelegate` failing to stop the manually pumped platform loop. Routing close requests through the existing Quit path should terminate normally from both running and paused states.
- **Audit:** old PID 99094 is gone; no Simulator is booted. The exact DOL/module audit passes. Protected R55 slot 3 hashes to `256b57886f9575b20cd58764c865674b19b4496c7a6c2fad9d709439720a88df`. Free disk space is approximately 22 GB; no regeneration is needed. R55's final telemetry contains hundreds of deliberate pause/debugger gaps and cannot serve as a clean performance measurement.
- **Change:** root-owned vendor overlay `0008-macos-window-close-shutdown.patch` adds `windowShouldClose:`, invokes `Application::shutdown`, and defers surface destruction to the runtime teardown. Bootstrap checks its exact hash and accepts only its declared source path. Two successive bootstrap runs pass.
- **Commands:** `scripts/bootstrap-dependencies.sh`; `cmake --build generated/build/moderngekko-desktop --target moderngekko-run --parallel 4`; `scripts/audit-module.sh`; `scripts/check-repository.sh`; normal Metal runner invocation into isolated R56 and R57 directories; System Events red-close-button clicks while running and paused.
- **Result:** R56 closes with exit 0 and zero fallback/failed SMC; R57 copies the protected NAND/config/slot, visibly reloads small-planet gameplay, pauses, and closes with the same successful shutdown signature. No orphan runtime remains. R57 recorded one underrun around the load/transition; this is not an audio acceptance pass.
- **Evidence:** ignored `generated/runtime/window-close-r56.log`, `generated/runtime/g6-resume-r57.log`, and R57 screenshot `RMGE01_2026-09-06_04-39-24.png`. Original R55 remains intact.
- **Interpretation:** a concrete macOS lifecycle defect is fixed. G6 stays open because the first Grand Star and in-game save/relaunch/load are still unverified. Existing gameplay progress is restored successfully under the changed host.
- **Packaging/regressions:** the private app rebuild and deep-signature/package audit pass. Runner SHA-256 is `9f53e1919309097d0bb92b261ae23d6d634416fd10271f0fac79ce085f420448`; signed packaged runner is `13164fdf3598812469792838bc118620a7d137676e596f6bd8cee5d981710c60`. Repository safety, ignore policy, Wii fail-closed guard, Pipe input, and macOS configuration checks pass. Final process/Simulator inventory is empty.
- **Next step:** package/audit the fixed runner, resume protected slot 3, complete the key/cage route, and obtain the first Grand Star and in-game save/relaunch evidence. Use the goal loop's park-and-pivot rung if automated encounter navigation again consumes a session without advancing; preserve the open gate and proceed with an independently testable host/control task.

## 2026-09-06 — first key/cage cleared and Sling Star progression

- **Goal/hypothesis:** advance G6 from protected slot 3 using ordinary foreground controller input. Short inputs can be lost when the runtime is not foregrounded; explicit focus should restore movement and make the Spin/contact encounter controllable.
- **Step:** relaunch the fixed runner into R57; load slot 3; reach the Luma; compare down-stick input before/after foregrounding; approach the key carrier; Spin, allow the stun to settle, and follow with directional contact. Collect the dropped key, return to the freed Luma, complete its dialogue, jump into its Sling Star, and Spin to launch.
- **Result:** key carrier defeated; golden key visible at `04-47-59`; cage visibly open at `04-49-02`; Luma transformed into Sling Star at `04-53-25`; Mario landed on the next planet at `04-54-02`. The next caged Luma instructs Spin/contact against its enemy group. The first approach took one damage; protected arrival slot 4 was restored and the runtime shut down through the fixed red close button with exit 0, `fallback=0`, and `smc_failed=0`.
- **Evidence:** ignored R57 screenshots under `generated/runtime/g6-resume-r57/ScreenShots/RMGE01/`; `generated/runtime/g6-resume-r57-cont.log`. Key-drop slot 2 SHA-256 `8e7405c5b1bda70672575662aec4d6005c256c600058f9d259e953595f6fc575`; cage-open slot 1 `9e383902c3a530c0389c9285620cec3c88492d66728297c93a32a9a776f9609a`; next-planet slot 4 `0781718e46961735f601e2bebb41e0ee0ef6be696599857bbd35ce9caa90ba62`.
- **Input finding:** explicitly set the runtime process frontmost before a controller sequence. Merely clicking a menu item did not consistently establish gameplay focus. The unchanged down-stick input produced visible movement once foregrounded. Petari's `WalkerStateStagger` reference also describes a delayed kick-enabled state; it was used as behavioral guidance, with no game-memory writes or region-specific patch.
- **Performance interpretation:** visible window title at final close was 59.8 FPS. Eight underruns occurred across this pause-heavy exploratory run. Aggregate active spans include tool pauses up to 40 seconds, so dividing frame count by wall time would produce a false low FPS result. No sustained performance acceptance is claimed.
- **Next step:** resume R57 slot 4 on the second gold planet, explicitly foreground controls, clear its enemy/cage route, enter the green pipe, then complete the Grand Star and Observatory save/relaunch gate. G6 remains open. No runtime or Simulator is left active.

## 2026-09-06 — bounded second-planet attempt; explicit native pause indication

- **Goal/hypothesis:** continue G6 using foreground Pipe controls, then address the ambiguity between an intentional pause and the reported freeze if navigation fails to advance.
- **Gameplay result:** a regular second-planet Goomba was defeated and dropped Star Bits, not a key. The giant encounter and second cage are not cleared. An apparent flipped-enemy image did not prove a stun or defeat; subsequent contact caused a death. No key or progression claim is made from that animation. The goal loop's park-and-pivot rung is used instead of repeating unchanged navigation.
- **Protected continuation:** R57 slot 4 remains the two-wedge second-planet arrival (`0781718e46961735f601e2bebb41e0ee0ef6be696599857bbd35ce9caa90ba62`). Slot 5 is a one-wedge regular-enemy-clear checkpoint (`6509a7fa690a775ce96b7f2c0e41c7078cb8ab03a7ffd55c21705bbb81d4dbe0`); slot 6 is a one-wedge giant-approach checkpoint, not a stun proof (`00e72af10929166d06a73aabec8e9c61533a40b9e0ee3e1050daf5630816b86c`). Original R55 is unchanged.
- **Shutdown evidence:** `generated/runtime/g6-resume-r57-cont2.log` records `native=864931602`, `fallback=0`, and `smc_failed=0`; red close terminated PID 40228. Manual pauses invalidate whole-run wall-time FPS calculations.
- **Host hypothesis/change:** the last FPS title remains frozen during an intentional core pause. Hash-pinned `0009-macos-pause-indicator.patch` adds a native window subtitle based on actual `Core::State::Paused`, refreshed by the existing event loop, including pauses outside the menu. No input binding, core timing, guest state, or module change is involved. Incremental runner build and Pipe/config/repository regressions pass; runtime validation follows.
- **R58 verification:** System Events reads the pause/resume subtitle transitions from the actual window. Loaded slot 4, resumed visibly (`RMGE01_2026-09-06_05-12-55.png`), paused, resumed (subtitle absent), and re-paused (subtitle present). Red close then exited with status 0. `generated/runtime/pause-indicator-r58.log` records `native=212316924`, `fallback=0`, `smc_failed=0`, zero underruns, and zero backlog drops. This short pause-heavy test is lifecycle/UI evidence, not a sustained performance pass.
- **Delivery/checks:** rebuilt private app passes its audit; repeated bootstrap is idempotent; module audit, Wii mode, Pipe/config, and repository safety checks pass. Unsigned runner SHA-256 `8509cf8b472785564b1272eab5bb8fab6a4794e4ca7f6c353cb05f31fccca2a8`; signed packaged runner `9842e3f6b8b14e5d149cd81dcf86eee650c7f6001b09b4ffcf04a4af9c8cd8c6`; accepted module unchanged. No runtime or Simulator remains. Nothing published.
- **Next:** G6 still requires second key/cage, pipe interior, first Grand Star, Observatory in-game save, and clean save/relaunch/load. Before another encounter attempt, resolve its actual Spin/kick timing and safe approach from source plus the two-wedge arrival checkpoint; do not repeat the failed pole chase. Independently testable native control/lifecycle work may continue under the documented park-and-pivot rule, without claiming mobile or G6 acceptance.

## 2026-09-06 — R59 second key, cage, interior, and central switches proven

- **Previous turn classification:** progress: R58 changed and verified native pause indication. G6 remains the lowest unmet gate.
- **Hypothesis:** the giant's stagger contract differs from the regular enemy. Petari `KuriboChiefParam` sets kick enable to 70 frames and stagger duration to 240; `receiveMsgPlayerAttack` accepts a second PlayerHitAll when kickable. `ShockWaveGenerator` can also send the stun. These are behavioral clues, not wrong-region patch addresses.
- **Ordinary-input experiment:** from the protected giant approach, move closer with short right/down steps, Spin, hold neutral 1.5 seconds, then approach and Spin again. At `05-16-47` the enemy is visibly inverted with stun sparkles; at `05-17-11` the giant is gone and its golden key is visible. No memory writes or guest patches were used.
- **Progression:** collected the key (`05-17-29`), cage-open camera (`05-17-41`), reached freed Luma (`05-20-44`), entered green pipe (`05-21-49`), viewed the trapped Grand Star (`05-22-29`), and turned the central flipswitch ring blue (`05-30-12`). One outer panel was activated first (`05-25-52`). The machine still runs: remaining outer panels and the Grand Star pickup are not claimed complete.
- **Gameplay learning:** sustained directional control plus jump/Spin crossed the moving exterior electrical band; short jumps that landed at the moving gap edge caused damage. A normal interior death respawned at the interior Luma, demonstrating the game's checkpoint. On the outer switches, landing on top of a moving arm does not activate the floor tile beneath it. Route along the floor when the arm clears rather than repeatedly jumping onto it. A later death was restored from the protected central-ring state.
- **Current protected R57 states:** slot 4 unchanged two-wedge planet arrival; slot 5 interior arrival SHA-256 `747ad1b5c4de5943bda5fa3c29223a260413b246023466078f1af548ceec76f9`; slot 6 central-ring blue/two wedges `618a40597704f1234619bd7d20549fc6cf2f1ce21d5026d3807f44f85a11d23a`; slot 7 second key drop `33a881025fa3e7ffc7f5dc3e577420ea7185dc0aaeb20bcb3bcaa531a1d0121f`; slot 8 second cage open `4e27a6dce1cca4f20590b011363f12286fea7d2f4977d9c6c690928468b4cce5`. Slots 5/6 intentionally replace the earlier, less advanced checkpoints; slots 1–4 and original R55 are intact.
- **Runtime evidence:** ignored `generated/runtime/g6-second-key-r59.log` and timestamped screenshots in R57. Clean red-close shutdown, exit 0, native dispatch 2,935,053,611, fallback 0, failed SMC 0, 14,198 frames, 17 underruns, one backlog drop. Many deliberate pauses (up to 31 seconds) invalidate a wall-time FPS average. Final title read 59.9 FPS; this is not sustained performance acceptance. Runner/module unchanged from R58. No runtime or Simulator remains; nothing published.
- **Next:** resume slot 6, activate the remaining outer floor switches without revisiting blue panels, collect Grand Star, finish Observatory dialogue, perform an in-game save, cleanly exit, relaunch without loading an emulator state, and visibly load the saved progression. G6 remains open until that evidence exists.

## 2026-09-06 — R60 Grand Star and Observatory in-game save

- **Previous turn:** verified progression. R60 resumed the hashed central-ring checkpoint on the unchanged R58 runner/accepted module, with no other runtime or Simulator.
- **Experiment/result:** floor-level traversal activated the remaining outer panels; jump/Spin returned to the central area. Revisiting blue panels toggles them yellow, so two accidental central toggles were corrected. All switches turned green and the machine shutdown cutscene triggered at `05-47-09`. Grand Star pickup followed at `05-48-13`; Observatory arrival and power restoration rendered and advanced. The result screen explicitly confirmed `Grand Star Rescue`, Gateway Galaxy, and one star at `05-50-58`.
- **Save evidence:** advanced Rosalina's introduction and Terrace unlock, selected Yes at the real save prompt (`05-57-17`), received `The game has been saved` (`05-57-38`), and returned to playable Observatory with one star (`05-58-02`). `GameData.bin` SHA-256 after clean shutdown is `5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6`. This supersedes fresh-file-only persistence evidence, but relaunch/load validation follows separately.
- **Runtime:** `generated/runtime/g6-panels-r60.log` exits 0, native dispatch 4,943,994,601, fallback 0, failed SMC 0; 21,162 frames, 11 underruns, two backlog drops. The pause-heavy total is not an FPS benchmark. The final Observatory title read 51.3 FPS; residual scene performance remains open. All screenshots/save data remain ignored/private. No product source or module changes.
- **Next:** R61 starts a fresh process with the same NAND and no savestate load. Require file-select one-star progression and playable Observatory after loading before recording the persistence loop as passed.
- **R61 result:** fresh title at `05-59-37`; A+B to file select; Mario slot 1 shows one star/two Star Bits at `06-00-26`; Play This File restores Observatory at `06-01-01`; movement/jump visible at `06-01-23`. No savestate load occurred. Red close returns exit 0, native dispatch 564,983,515, fallback 0, failed SMC 0. GameData hash remains unchanged after shutdown. Normal save/relaunch persistence is passed.
- **Acceptance boundary:** G6's progression/persistence portion is now proven, but PRD D3 additionally requires the packaged entry, music/sound-effects evidence, and stable timing. Final Observatory title is 53.3 FPS; R61 logs 11 underruns/one backlog drop. These are not waived by the progression success. Keep G6 open while resolving those technical acceptance items; do not replay the full tutorial unnecessarily now that a genuine one-star save exists.
- **Checks/next:** module audit and repository/Pipe/config checks pass; no runtime remains. Next use the in-game save for an uninterrupted Observatory performance baseline and packaged-entry validation. Full story, completion mechanics, mobile controls/menu/simulators, lifecycle/soak, device and release gates remain in scope and unaccepted.

## 2026-09-06 — R62 repeatable packaged Observatory diagnostic

- **Previous turn classification:** progress: actual Grand Star save/relaunch proved. **Hypothesis:** the saved Observatory can be restored through the packaged argument entry with a fixed input sequence, giving a useful performance comparator without pauses or tutorial navigation.
- **Change:** added `g6-observatory-save-load.json` and exact Pipe encoding regression. Copied only NAND/config to ignored R62, created its Pipe controller, verified native 1× and the exact saved GameData hash. Package audit passed. No source/module optimization was made.
- **Run:** launched packaged GalaxyPad bootstrap with explicit game/module/user-dir arguments and default-off phase tracing; set runtime frontmost; ran the fixture; observed playable one-star Observatory. Left it unpaused, sampled CPU for ten seconds, and measured the final 30-second trace window. Result: 55.135 Hz VI/frame/present, frame p95 21.809 ms, zero EFB reads, and dominant native-execution CPU work. Full details and exact trace timestamps are in PERF.md.
- **Outcome:** packaged argument entry and deterministic load fixture pass; stable timing/audio do not. Final title 54.5 FPS; clean exit 0, fallback 0, failed SMC 0, 228 underruns/three backlog drops. Trace/sample overhead prevents acceptance claims. Private evidence is under `generated/runtime/observatory-r62*`. No runtime remains.
- **Next:** matching untraced/unsampled packaged control, then a focused runtime/dispatch experiment supported by the CPU sample. Keep native timing and all original PRD semantics intact. Default frontend/import flow and full mobile/product scope are still open.

## 2026-09-06 — R63 uninstrumented control confirms user-visible lag

- **Experiment:** same packaged runner/module, isolated copy of the real save, and regression-covered Observatory load fixture; no phase trace, CPU sample, savestate load, or deliberate pause. Screenshot `observatory-r63/ScreenShots/RMGE01/RMGE01_2026-09-06_06-16-17.png` confirms playable one-star Observatory.
- **User evidence:** user reported 38.5 FPS with obvious lag during this run. Later AppKit title reads were 53.0 and 52.2 FPS, not evidence that the reported dip was resolved.
- **Confound:** early process inventory showed unrelated JumpConnect at 145.6% CPU and Logitech updater at 30.2%; later JumpConnect was absent from the top consumers, updater was 39.6%, and the game 101.3%. No unrelated process was altered. These snapshots do not prove causality or provide a clean instrumentation-overhead comparison with R62.
- **Result:** normal red-close exit 0; native 4,257,797,466, fallback 0, failed SMC 0. Whole-run graphics 15,113 frames / 299.998439916 seconds = 50.377 Hz, 4,385 gaps >=20 ms, 292 >=33 ms; DMA 63,103 enqueues / 300.312011417 seconds, 837 underruns, 12 backlog corrections. Zero EFB color/depth reads. Whole-run timing includes title/load and is not a stationary-scene percentile. Log stays private at `generated/runtime/observatory-r63.log`.
- **Decision:** reject stable-performance acceptance. Removing diagnostic instrumentation did not establish sustained 60 FPS. Keep G6 open; native execution remains the supported profiling direction, with host load controlled/recorded in future comparisons. Do not disable EFB/audio/timing semantics. No runtime remains.

## 2026-09-06 — R64 isolated dual-core Observatory candidate

- **Previous turn classification:** progress: R63 established uninstrumented lag and excluded EFB reads for the saved scene. Source inspection confirms the desktop default is CPUThread=False and the prior sample combined CPU/GPU work. Earlier G5 dual-core rejection was route-specific; no default change is authorized by this experiment alone.
- **Change/hypothesis:** copied R63 NAND/config into private R64, configured its Pipe, and added only `CPUThread = True` under Core. Separate graphics work may free enough guest-execution time in Observatory. Same packaged executable/module and load fixture; no tracing, sampling, savestate load, or pause. Unrelated host apps left untouched.
- **Evidence:** screenshot `generated/runtime/observatory-r64/ScreenShots/RMGE01/RMGE01_2026-09-06_06-22-32.png` shows the one-star Observatory. Title samples 60.2 and 59.9 FPS. Whole-run graphics 9,405 frames / 156.985107583 seconds = 59.910 Hz; 1,208 gaps >=20 ms, seven >=33 ms, five >=50 ms. DMA 39,295 blocks / 157.280725875 seconds = 31.980 kHz, six underruns/six backlog corrections. Zero EFB reads. Final projection `0x1d78cebeb09305ae`, six input transitions, fallback 0, failed SMC 0. Red close exits 0. Save SHA-256 remains `5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6`.
- **Boundary/next:** this is a promising candidate, not a validated A/B or stability pass. R63 had transient JumpConnect contention and a longer interval; R64 still had Logitech updater around 28–29% CPU. Repeat single-core with comparable conditions/duration; then test longer dual-core movement and G5 if the advantage survives. Keep default config/package unchanged and G6 open. No runtime remains; nothing published.

## 2026-09-06 — R65 single-core follow-up and readiness correction

- **Previous turn:** progress: R64 established a near-target isolated dual-core candidate. Copied its NAND/config into R65 and changed only CPUThread to False; same package/module/1× and save hash.
- **Invalid comparison caught visually:** first fixture invocation did not leave title. Screenshot `RMGE01_2026-09-06_06-26-56.png` proves the reported 59.9 FPS was title, not Observatory. Retried after visible readiness; `RMGE01_2026-09-06_06-28-13.png` confirms the same one-star Observatory. No pause or savestate load. Startup delay/window existence is not a sufficient readiness gate.
- **Scene evidence:** Observatory title readings 55.7, 55.9, and 54.9 FPS; runtime around 102% CPU, updater around 20% and transient launchd 25%. This supports the dual-core direction without proving a controlled speedup. All unrelated processes left untouched.
- **Runtime:** exit 0; native 3,240,065,580, fallback 0, failed SMC 0, zero EFB reads, projection `0x1d78cebeb09305ae`. Whole-run 13,688 frames/236.717 seconds and 147 underruns are archived, but excluded from matched averages because of extra title time/input (12 transitions). Private log/screenshots under `generated/runtime/observatory-r65*`.
- **Correction/next:** PERF.md now requires inspected input-ready title before the fixture and inspected Observatory before the measurement interval. Command-encoding tests are not navigation acceptance. Keep G6/defaults unchanged; run fixed-duration comparison with those gates, then movement/G5 validation. Clean close leaves no runtime.

## 2026-09-06 — R66 corrected fixed-window single-core baseline

- **Previous turn classification:** progress: R65 caught an invalid startup comparator and established readiness requirements. R66 copied its unchanged save/config/package and enabled only diagnostic phase tracing; CPUThread=False, native 1× remain explicit.
- **Readiness:** initial screenshot showed title animation without the prompt; waited for inspected `Press both A and B` screenshot `06-32-08` before the single fixture invocation. Inspected one-star Observatory screenshot `06-33-16`. Six input transitions, no retries, pauses, or savestates. This resolves the R65 test setup defect.
- **Measurement:** exact 60-second trace window `279871552632333` inclusive to `279931552632333` exclusive. VI/frame/present 55.500 Hz, DMA 29.628 kHz; frame-start p95 18.877 ms/p99 20.490 ms. PERF.md records event counts and percentile method. Whole-run counters are not substituted for this window.
- **Safety:** clean close exit 0, native 2,817,894,712, fallback 0, failed SMC 0, zero EFB reads, final projection `0x1d78cebeb09305ae`. Whole-run 163 underruns remain a failure indicator but are not window-local. Host load recorded without altering unrelated apps. No runtime remains.
- **Next:** equivalent trace-on dual-core 60-second window from inspected Observatory, then uninstrumented movement/G5 tests if the advantage survives. No product defaults changed; G6 and remaining original scope stay open.

## 2026-09-06 — R67 matched dual-core diagnostic and movement smoke

- **Previous turn:** progress: R66 established a visually gated 60-second single-core control. R67 copied identical save/config/package and changed only CPUThread=True, retaining phase tracing and native 1×. Ready title screenshot `06-37-51`; one successful load fixture; Observatory screenshot `06-38-54`.
- **Matched diagnostic:** `generated/runtime/observatory-r67/phase.csv`, interval `280203455943375 <= steady_ns < 280263455943375`. Frame/present 3,597 events = 59.950 Hz, VI 3,596 = 59.933 Hz; DMA 15,002 blocks = 32.004 kHz. Frame-start median/p95/p99 16.680/17.148/17.672 ms; 14 of 3,596 gaps >=20 ms. Present p95/p99 17.241/17.838 ms. Same method as R66: frame throughput +8.02%, DMA +8.02%. Host contention is still uncontrolled: Codex renderer briefly 97%, later updater 48.1%/NordVPN 27.9%; none altered. Evidence favors dual-core, not general acceptance.
- **Controls:** after the measured interval, added/ran `g6-observatory-control-smoke.json` (short lateral movement, jump, Spin pulse, opposite lateral movement, neutral). Screenshot `06-40-56` shows Mario airborne away from the initial pad; `06-41-07` shows safe landing with three life wedges. Title 59.9 FPS. Spin command was sent but its effect is not independently isolated by these screenshots. Exact command encoding is regression-covered.
- **Shutdown:** exit 0; native 4,243,424,481, fallback 0, failed SMC 0, zero EFB reads. Whole-run 15,990 frames/266.844 seconds, 11 underruns/10 backlog corrections; no audible-quality acceptance inferred from counters. No runtime remains, no defaults changed, nothing published.
- **Decision:** stop repeating stationary tests. Next run G5 dual-core regression plus longer untraced gameplay/control/lifecycle coverage; only promote the setting if those pass. G6/D3 and the original full scope remain open.

## 2026-09-06 — R68 untraced dual-core G5 functional regression

- **Previous turn:** progress: matched Observatory diagnostic favored dual-core and short movement/jump worked. R68 copied the older R36 zero-star NAND with current R67 config/package, CPUThread=True and 1×. No tracing, sampling, savestate loading, or pause; original G5 fixture ran once after AppKit startup with its built-in title wait.
- **Visible/input result:** `generated/runtime/g5-dual-r68/ScreenShots/RMGE01/RMGE01_2026-09-06_06-45-52.png` shows file-select Mario slot 1, zero stars/bits, pointer hovering the slot and its details visible. Final title 59.9 FPS. Four input transitions, final IR 655/529 match the established input signature.
- **Runtime:** 11,017 frames / 183.871781708 seconds = 59.917 Hz; 46,019 DMA blocks / 184.192416 seconds = 31.980 kHz. One underrun at enqueue 30,954; two backlog corrections, one startup and one near that underrun. Clean exit 0, native 1,895,920,783, fallback 0, failed SMC 0. No runtime remains.
- **Evidence boundary:** final projection `0xa838adff8d9a2b08` differs from historical R36 `0x14564ea8ed5a5f20`; final draw counts also differ. Older screenshot was unhovered file select while current pointer visibly selects Mario. Do not claim an exact historical render-signature pass or infer causality from aggregate hashes. Current run records zero EFB reads, whereas old R36 recorded reads; package RMG.ini still explicitly preserves CPU EFB access/deferred invalidation/arbitrary mipmaps. No EFB setting changed for this test. Fresh visible functionality/cadence passes, zero-underrun audio acceptance does not.
- **Next:** longer untraced Observatory controls, explicit Spin observation, pause/resume/close and audio assessment before default promotion. Preserve current configuration until then; G6 remains open.

## 2026-09-06 — R69 resource-layout correction and unresolved EFB configuration

- **Previous turn:** progress: R68 provided fresh file-select behavior/cadence, but its zero EFB reads differed from older runs. Before further performance promotion, inspected the actual resource lookup and defaults.
- **Source finding:** macOS `Common/FileUtil.cpp` resolves `GetBundleDirectory()/Contents/Resources/Sys/`; the builder copied Sys to Contents/MacOS instead. `GraphicsSettings.cpp` defaults GFX_HACK_EFB_ACCESS_ENABLE to False. Checking RMG.ini at the wrong path did not establish active semantics. Earlier R62–R68 packaged-EFB acceptance implications are withdrawn; those timings are provisional.
- **Fix/check:** builder now copies Sys under Resources. App audit explicitly requires RMG.ini there and checks all three required EFB/mipmap settings. Negative test: old package fails with `missing runtime-resolved Galaxy settings`. Rebuilt app passes audit/signature/repository checks. Packaged runner hash remains `9842e3f6b8b14e5d149cd81dcf86eee650c7f6001b09b4ffcf04a4af9c8cd8c6`; module unchanged. Previous package preserved by normal builder backup.
- **Runtime limitation:** R69 copied the R68 G5 profile and ran the same fixture untraced. Screenshot `generated/runtime/g5-bundle-sys-r69/ScreenShots/RMGE01/RMGE01_2026-09-06_06-52-17.png` confirms zero-star file-select and pointer hover. Exit 0, native 1,824,326,754, fallback 0, failed SMC 0; 10,526 frames/175.727 seconds, two underruns/three backlog corrections. EFB reads are STILL ZERO. Therefore the layout fix alone does not prove effective game settings or explain telemetry. Do not call it a resolved runtime EFB fix.
- **Next:** inspect/log actual resolved Sys path and Config::Get/g_ActiveConfig EFB flags after boot; inspect where depth telemetry is incremented. Distinguish disabled reads from uncalled reads or an instrumentation gap before another optimization/default promotion. No runtime remains. G6/full original scope open.

## 2026-09-06 — R70 compiled platform-path cause confirmed and patched

- **Previous turn:** progress: resource-layout audit improved but zero reads remained unresolved. Inspected EFBInterface::PeekDepth: telemetry increments only after ShouldSkipAccess; rejected reads were invisible.
- **Bounded diagnostic:** temporarily logged first depth call with File::GetSysDirectory, game ID, Config::Get(EFB access), active graphics flag, and coordinates. Built an isolated ignored diagnostic app; normal app was not replaced by this diagnostic. R70 ran the G5 fixture with copied R69 profile. Log reports `sys=.../EFB-Diagnostic-r70.app/Sys/ game=RMGE01 configured=0 active=0 x=0 y=0`. Valid reads are being disabled, not merely absent. No game-semantic override was used.
- **Cause/fix:** FileUtil.cpp selected the iOS branch using `defined(__IPHONE_OS_VERSION_MIN_REQUIRED)`, which selected the wrong path in this desktop build. Added TargetConditionals.h and changed that branch to TARGET_OS_IPHONE. Canonical overlay `patches/ModernGekko-dolphin/0010-apple-sys-platform.patch`, SHA-256 `6da482f7393a68809a33c2898a08aac2bd121d7472f1f4a2e5a6f5aefae69279`. Bootstrap peels/reapplies it around the lower Apple patch and verifies its scope/hash. Keeps real iOS root-Sys behavior and standard macOS Resources/Sys behavior.
- **Validation:** bootstrap applied, repeated through normal package build successfully; package audit and repository/Pipe/config checks pass. Temporary diagnostic edits were restored before the normal rebuild. R70 closed cleanly; no runtime remains. Isolated diagnostic app/log remain ignored for evidence only. Runtime validation of the corrected path/active EFB reads is still required next; no performance/default acceptance claimed.
- **Correction:** R69 source reading named the intended macOS branch but did not establish the compiled branch. R70 supplies that missing direct evidence. R62–R69 packaged timings remain unsuitable for acceptance under the required EFB semantics. Next launch corrected package, confirm depth reads, then reassess dual-core performance and controls with real depth enabled.

## 2026-09-06 — R71 actual compiler-flag precedence fixed with regression

- **Previous turn:** progress: R70 proved disabled valid EFB requests but misattributed the selected Sys path solely to the iPhone macro. R71 launched that first patch; compile-command inspection then found `-DLINUX_LOCAL_DEV` in FileUtil.cpp's actual desktop flags. ModernGekko CMake explicitly forces it on. That earlier branch takes precedence regardless of TARGET_OS_IPHONE. The first fix was insufficient.
- **Correction:** updated canonical 0010 overlay so the relocatable Linux branch excludes Apple. TargetConditionals still distinguishes real Apple platforms. New overlay SHA-256 `9c065043c15f941a73705d7809ab5e0a90a145ad7bd304346507aad80150b6d9` supersedes `6da482...`; bootstrap hash updated after reversing the old overlay.
- **Regression:** `tests/test-sys-platform.sh` extracts the real Sys selector from FileUtil.cpp and preprocesses macOS with/without LINUX_LOCAL_DEV, iOS with that flag, Linux, and Windows. It explicitly reproduces the old failure by restoring the old first condition. Integrated into repository checks. This tests actual preprocessor behavior, not file presence or a textual assertion alone.
- **Runtime boundary:** stopped R71 normally after confirming the compiler issue, before calling the regression accepted. Exit 0, fallback/failed SMC 0, zero EFB reads. Fixture's final hold is not a performance comparator. Log stays private at `generated/runtime/g5-sys-fixed-r71.log`.
- **Build/checks:** revised bootstrap applied and repeated successfully through normal app build; Sys selection, Pipe/config/repository checks and package audit pass. No runtime remains. Next runtime validation must show real depth reads; R62–R71 disabled-EFB packaged timings remain ineligible for acceptance. No dual-core default promotion.

## 2026-09-06 — R72 packaged depth access and G5 continuity verified

- **Previous turn:** progress: corrected actual preprocessor precedence with a failing-old/passing-new source-derived test. R72 used the normal rebuilt app, copied zero-star NAND/config, dual-core/native 1×, no tracing/sampling, and the established G5 fixture once.
- **Visible evidence:** `generated/runtime/g5-efb-restored-r72/ScreenShots/RMGE01/RMGE01_2026-09-06_07-09-49.png` shows selected Mario slot 1, zero stars/bits, pointer hover and details. Title reads 59.9 FPS. No savestate load or pause.
- **Decisive runtime result:** depth_peeks=24,966, frames_with_peeks=10,600, total_ns=3,175,222,512, max_ns=11,638,125, max_peeks_per_frame=3. The previous zero-read/disabled-setting defect is resolved on the normal package. 12,149 frames/202.752240625 seconds, DMA 50,740 blocks/203.108046625 seconds, zero underruns, one startup-only backlog correction at enqueue 51. Four input transitions, final projection `0xa838adff8d9a2b08`.
- **Lifecycle:** red close returns exit 0; native 2,135,220,654, fallback 0, failed SMC 0. No runtime remains. Runner remains packaged SHA-256 `1a0ac2236c7827661e2b2869c2e0b9fecc2fab584d63f97f6161851b4b4c0120`; accepted module unchanged. Private evidence under `generated/runtime/g5-efb-restored-r72*`.
- **Next:** stop revisiting the resolved path diagnosis. Use the genuine Observatory save in this corrected package for longer movement/explicit Spin/lifecycle and audio/timing validation. G6 remains open; no default promotion or mobile acceptance from G5 alone. Earlier disabled-EFB package comparisons remain withdrawn.

## 2026-09-06 — R73 corrected-package Observatory controls and pause lifecycle

- **Previous turn:** progress: R72 proved real packaged depth reads and G5 continuity. R73 uses the same corrected package/accepted module, dual-core/native 1×, copied real one-star NAND, no trace/sample/savestate. Inspected ready title `07-13-53`; one load fixture; inspected Observatory `07-15-15`.
- **Controls:** ran the regression-covered short control fixture. `07-15-31` visibly shows Mario airborne with the blue Spin streak/sparkles after the dedicated shake pulse. Lateral displacement and subsequent safe return are observed. Unlike R67, the Spin effect is directly visible.
- **Lifecycle:** Toggle pause produced AX static text `Paused — Misc > Toggle pause to resume`; resume removed it. Repeated control fixture after resume; `07-16-10` shows airborne Mario, demonstrating responding controls. Title samples 59.3/59.9 FPS, 58.8 immediately after resume. Re-paused, verified subtitle again, then red close exited 0. No orphan runtime.
- **Runtime:** native 3,083,169,706, fallback 0, failed SMC 0; 26,667 depth reads across 10,504 frames, total 4.429 seconds/max 11.139 ms. Nine underruns, 12 backlog corrections. The 16.29-second deliberate pause is visible in maximum frame gap and INVALIDATES whole-run FPS; this is controls/lifecycle evidence, not a sustained-performance or audible-quality pass.
- **Persistence:** GameData SHA-256 remains `5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6`. Screenshots/log are private under `generated/runtime/observatory-r73*`. No defaults changed or data published.
- **Next:** uninterrupted longer corrected-package gameplay with controls and audio evaluation; decide dual-core default only from applicable evidence. Avoid replaying tutorial or repeating the now-passed pause test. G6 remains open for full stable timing/audio/default packaged-entry coverage; original later-platform/full-story scope unchanged.

## 2026-09-06 — R74 five-minute unpaused controls/stability check

- **Previous turn:** progress: corrected-package controls/Spin/pause lifecycle verified. R74 retained corrected runner/module, real save, CPUThread=True/native 1×, default audio reserve, no instrumentation or deliberate pause. Ready title inspected `07-19-34`; Observatory inspected `07-21-05`. Five-minute wall-clock interval began at 12:21:22 UTC after scene verification; final check at elapsed 298.467 seconds, then normal close after the interval completed.
- **Gameplay:** three short movement/jump/Spin control passes, spaced through the interval. Screenshots `07-22-58` and `07-24-54` demonstrate continued movement/airborne Spin. No death, freeze, savestate, or progression rewrite. Repeated title samples read 59.9 FPS. This is a bounded Observatory check, not full-story or release soak coverage.
- **Counters:** whole-process graphics 28,166 frames/470.032923792 seconds; DMA 117,545 blocks/470.379200125 seconds. About 59.92 FPS and 31.99 kHz. 4,009 frame gaps >=20 ms, 31 >=33 ms, nine >=50 ms, two >=100 ms (includes startup/loading). 17 underruns and 22 backlog corrections remain; they are not waived by near-reference averages. EFB 73,107 depth reads, 26,492 frames with reads, total 11.166 seconds/max 37.951 ms. Whole-process counters are not misrepresented as isolated five-minute statistics.
- **Safety/evidence:** exit 0, native 8,240,372,060, fallback 0, failed SMC 0, 12 input transitions. GameData SHA-256 remains `5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6`. No runtime remains. Evidence stays private under `generated/runtime/observatory-r74*`. Asked the user for an optional listening check; no answer received during this run, so music/effects quality remains unverified.
- **Next:** investigate remaining queue underruns/backlog corrections with near-reference producer cadence. Source currently defaults AudioBufferSize=80 ms and adaptive queue reserve; a bounded reserve comparison may be justified by observed frame/readback gaps, but do not use it to excuse actual sub-reference production. No default changes yet; G6 open.

## 2026-09-06 — R75 audio reserve improvement and conservative profile seeding

- **Previous turn:** progress: R74 established near-reference sustained production but intermittent underruns. Mixer source targets 80 ms by default, and backlog correction jumps to roughly half that reserve. Hypothesis: 120 ms gives transient readback/host gaps more margin without altering game timing.
- **Experiment:** isolated R75 copied R74 and changed only AudioBufferSize=120. Same corrected package, dual-core/1×, real save, no tracing/sampling/pause. Ready title `07-30-10`; Observatory `07-31-38`; post-load five-minute clock began 12:31:52 UTC, last check 276.179 seconds then another 25-second wait before normal close. Three identical control passes, with visible airborne Spin at `07-33-38`/`07-35-14`.
- **Result:** 29,044 frames/484.727661458 seconds, DMA 121,207 blocks/485.099025125 seconds, about 59.92 FPS/31.98 kHz. Four underruns/nine backlog corrections versus R74's 17/22. Durations/host scheduling are not identical; this is a useful sequential improvement, not an isolated laboratory result. Twelve input transitions, 75,021 depth reads, save hash unchanged, exit 0/native 8,479,894,544/fallback 0/failed SMC 0. No audible-quality confirmation received. No runtime remains.
- **Delivery:** added default-Dolphin.ini with CPUThread=True and AudioBufferSize=120, copied into package, seeded ONLY if a user's Dolphin.ini does not exist. Existing profiles are not migrated or overwritten. Executable regression extracts/runs the actual launcher guard against isolated new and pre-existing profiles and verifies byte-for-byte preservation. Build/audit/repository checks pass. This promotes an incremental default, not G6 acceptance or a claim that four underruns are harmless. The latency tradeoff is explicit.
- **Next:** validate the default frontend/import entry and remaining sound/scene timing under the seeded configuration. Existing user profiles require an explicit settings choice rather than silent overwrite. Avoid another title-only or unchanged stationary benchmark; full G6 and later story/mobile/product scope remain open.

## 2026-09-06 — R76 normal frontend entry wiring and identity restrictions

- **Previous turn:** progress: audio reserve reduced underruns and new-profile defaults were shipped. Inspected normal no-argument launcher instead of another direct runner experiment.
- **Findings/fix:** frontend was compiled for profile directory moderngekko while wrapper seeded GalaxyPad. It also seeks sibling moderngekko-run, absent after package renaming. Both build paths now configure GalaxyPad branding/profile; the app provides a relative moderngekko-run symlink to GalaxyPadRunner, verified by package audit. Runner's existing sibling module discovery resolves the bundled exact module; actual launch still needs UI validation.
- **Legacy local profile:** normal launch initially rejected old `resolution=640x456`. Preserved `config.ini.before-native-resolution-r76` beside the local GalaxyPad profile and changed only that line to 640x528. Controls and other values retained. New Dolphin.ini was seeded with CPUThread=True/AudioBufferSize=120. No existing Dolphin.ini was overwritten.
- **Visible result:** normal launcher now displays GalaxyPad, Metal, 640x528, the existing Quartz keyboard/mouse profile, and no configured extracted game. It suggested an unrelated discovered GoldenEye disc and exposed a sideways-profile replacement action. Neither was activated. These are unresolved generic-frontend paths, not import/play acceptance.
- **Identity hardening:** both build scripts set required disc ID RMGE01 and exact DOL SHA-256 `2c680585a8f58e1cc9c5521b579057f12b124ff0ef409e470a57606c50a93c09`, using existing frontend/runner validation. Regression cross-checks build definitions against config/galaxypad-disc.json. Package rebuilt/audited, all repository tests pass. New signed runner SHA-256 `e346573ae3613a9c591de1bb2939c745abdedc4f4fc4be940fd851facbcfad58`; module unchanged.
- **Cleanup/next:** UI tool refreshes left extra owned launcher instances; after normal UI close and repeated UI-tool timeouts, exact verified test PIDs were terminated. No launcher/game remains. Next remove/replace the sideways-profile path with safe Wii Remote+Nunchuk generation, constrain initial disc selection, then test exact import/default frontend launch. No unrelated disc was read for gameplay, imported, or modified. G6/full scope remain open.

## 2026-09-06 — R77 Nunchuk generation and explicit disc selection

- **Previous turn:** progress: normal frontend wiring/profile/identity restrictions fixed, with generic sideways generation and arbitrary disc selection identified as remaining hazards.
- **Change:** Wii generator now emits independent positional A/B, Button X on all shake axes, left stick to Nunchuk, right stick to IR, shoulder L to C, trigger L to Z, Extension=Nunchuk/Sideways=False. Left stick no longer also drives D-pad. SDL backend source confirms positional A/B/X aliases and upward-positive stick naming. GameCube generation unchanged. Existing user profile was not regenerated; no connected-gamepad playability claim.
- **Selection:** branded builds return an empty discovered-disc list, avoiding recursive Documents scanning and arbitrary initial disc selection. Existing exact disc/DOL validation remains. User must explicitly Browse; no unrelated disc is imported automatically.
- **Reproducibility/tests:** canonical overlay `patches/ModernGekko/0013-galaxypad-frontend-controls.patch`, SHA-256 `64d397f7dd284d94f02a8377bfee1e9453a4007cd901c43ae6ca525ddcba421b`. Bootstrap peels/reapplies around lower overlays and verifies scope/hash. Compiled moderngekko.frontend_config and frontend_config_gamecube both pass. Normal app build/audit/repository checks and subsequent bootstrap pass.
- **UI evidence/boundary:** rebuilt normal launcher screenshot shows `Replace with generated Wii Remote + Nunchuk profile`, `No ISO, WBFS, or RVZ selected`, and `Using existing WiimoteNew.ini`. Repeated automated Browse clicks did not open a picker; raising the window and keyboard focus observation did not resolve it. No source conclusion about the cause yet. This is not import/launch acceptance. Close-button action terminated the launcher; UI tool returned connectionInvalid afterward, and process inventory confirms no launcher/game remains.
- **Next:** diagnose Browse input/dialog activation using the actual normal app, then select the verified ref/supermariogalaxy.wbfs and validate extraction/default launch. Do not replace the unresolved UI acceptance with a direct-runner-only claim. G6/full PRD scope open.

## 2026-09-06 — R78 actual frontend import and runner handoff

- **Previous turn:** progress on safe profile generation; Browse automation unresolved. Temporary local diagnostics showed SDL mouse down/up at 110,412 while ImGui saw -296,-69. This supports an automation/global-pointer mismatch, not a proven product rendering defect. Ctrl+Tab then Tab navigation activated Browse; native picker accepted the exact supplied ref/supermariogalaxy.wbfs. After raising the launcher again, Tab/Return activated Extract and Play and visible extraction progress appeared.
- **Handoff proof:** normal profile now records games/RMGE01 in default-game.txt. Frontend PID 61872 launched sibling moderngekko-run PID 62126 with normal --game/--user-dir arguments; log proves automatic bundled module loading. Existing keyboard profile retained, normal Dolphin.ini has CPUThread=True/AudioBufferSize=120. No direct runner substituted for the import test.
- **Limits/performance:** UI tool remained attached to the hidden frontend blocked in SDL_WaitProcess, so child gameplay was not visually verified. Shutdown reports 3,839 frames/76.867 seconds (~49.94 FPS), 16,334 DMA blocks/78.486 seconds (~26.64 kHz), 144 underruns/five backlog corrections, 2,258 real depth reads, fallback=0/smc_failed=0. Startup and a concurrent frontend rebuild confound comparison with R75; these poor counters are not waived or called a stability pass. Private copied log: generated/runtime/frontend-r78.log.
- **Cleanup:** repeated UI attachment/close checks timed out; exact verified child PID received TERM and both child/frontend exited. Temporary source diagnostics removed with apply_patch and source matches pre-R78 backup byte-for-byte. Clean frontend rebuilt and copied into package, re-signed. No Simulator running; user explicitly reminded us to keep at most one open at a time.
- **Next:** verify visible child gameplay from normal entry without simultaneous compilation, then isolate slow-scene/default-profile differences. G6 remains open, with original full-story/mobile/touch/menu scope unchanged.

## 2026-09-06 — R79 normal profile and transient host contention

- **Previous turn:** progress: actual import/handoff confirmed, startup counters poor. Hypothesis: normal settings differ from R75 or startup/host contention explains the deficit. GFX.ini is byte-identical; Dolphin.ini differs only in ordering, window position, MAC and analytics identity. CPUThread=True, AudioBufferSize=120, Metal/native 1x remain effective. No settings changed.
- **Process boundary:** closed leftover frontend 62289, verified no booted Simulators, launched one direct packaged runtime with normal imported game/user paths, without rebuilding. Early CUA attachment spawned frontend 62685 while runner 62655 was still starting; verified exact PID and terminated only that extra frontend. Once runtime registered, full-path getApp attached successfully. Future attachment must wait for runtime startup rather than repeatedly launching the app. Direct launch here diagnoses the normal profile; it does not replace R78 frontend UX acceptance.
- **Visible/timing:** ready A+B title screenshot read 43.9 FPS immediately after an AX reading of 59.9; subsequent AX readings recovered to 59.9. Concurrent host snapshot showed JumpConnect PID 62197 at 326.7% CPU, runner 113.6%, Logitech updater 39.3%. Later JumpConnect no longer appeared among top consumers. Unrelated applications untouched. This correlation is not proof every slowdown is external.
- **Sample:** generated/runtime/frontend-profile-r79.sample.txt, five seconds beginning 08:14:02 local. Main thread waits normally in AppKit event dispatch; CPU thread has 809/3,831 samples in PrecisionTimer SleepUntil and 2,513 beneath StaticRecompCore::Run's principal dispatch branch. Sample catches recovered timing, not a scheduler trace of the earlier dip; no deadlock or single new causal bottleneck established.
- **Shutdown:** normal window close, exit 0. Private log generated/runtime/frontend-profile-r79.log: 5,617 frames/103.311 s (~54.37 FPS), 23,496 DMA blocks/103.473 s (~29.06 kHz), 104 underruns/five backlog corrections. Last underrun at enqueue 12,811, leaving 10,685 later enqueues with none. EFB reads 4,068, fallback=0/failed SMC=0, no gameplay input. No runtime/launcher/Simulator remains. Repository tests pass; no product code change justified by this diagnostic.
- **Next:** use the solved ready-runtime UI attachment for file-select/control verification, then slow opening-scene timing without compilation or frequent captures. Do not repeat stationary title runs or call recovered title FPS G6 acceptance. Preserve original story/mobile/menu goal and one-Simulator limit.

## 2026-09-06 — R80 nonblocking frontend child monitoring

- **Previous turn:** progress, normal profile mismatch excluded and runtime UI attachment learned. Normal launcher visibly remembered RMGE01; keyboard Play launched the exact sibling runner. Waiting for ready child still left CUA bound to hidden frontend, timing out. Source confirms SDL_WaitProcess(process,true) blocks its AppKit main thread throughout gameplay. This is a launcher responsiveness defect, not an emulation FPS diagnosis.
- **Change:** same child monitor now polls SDL_WaitProcess(false), clearing stale SDL errors before each poll and breaking on exit or an actual error; pending state pumps SDL events and sleeps 50 ms. Existing exit-code/error handling remains. Pinned POSIX SDL source distinguishes pending waitpid=0 from errors, confirming semantics. No game timing/input/render settings changed.
- **Regression/provenance:** tests/test-frontend-wait.sh extracts the actual main-thread wait body and compiles it against deterministic SDL stubs. Tests pending polls/event pumping/delay, zero exit, nonzero exit, monitor failure, stale-error clearing, and rejects any blocking wait. Wired into repository checks. Extended canonical 0013 frontend overlay SHA-256 8304632c1cc9dbb81e1c56dbc87f328a91c2bef824737127a6cfa1bf78653891; bootstrap pin updated, reverse-check and repeated bootstrap pass. Frontend rebuilt/copied/re-signed; package audit and repository tests pass. Packaged runner/module unchanged.
- **Runtime verification:** rebuilt Play launched child 64608 from frontend 64574. UI query returned noWindowsAvailable for the intentionally hidden frontend rather than observation timeout. One-second parent sample shows 851 samples in nanosleep and one in SDL_PumpEvents; process snapshot 0% CPU, child alive and bundled module loaded. Same-bundle tool attachment still does not select child; no visible gameplay claim. Exact owned child received TERM for cleanup; parent exited afterward. Private evidence generated/runtime/frontend-r80-parent.sample.txt and frontend-r80-after.log.
- **Before-change timing:** generated/runtime/frontend-r80-before.log contains 5,519 frames/92.139 s (~59.9 FPS), two underruns at enqueue 23,184/23,307 of 23,437, 3,939 depth reads. This run had no concurrent compile/sample and is materially better than R78/R79, but title-only timing is not G6 acceptance. Do not credit these pre-change counters to the launcher fix.
- **Artifact qualification:** final signed runner SHA-256 is 6b354a3cfcf11f36dc0a023d347ea94d8ef96a9e113cf7b5ccea2a1508562539. The launcher build's dependency graph also rebuilt the runner, and bundle signing refreshed signatures; “unchanged” above means no intended runner-source/module policy change, not byte identity with R76. Use this exact candidate hash for subsequent runtime comparisons. After-change log records 2,328 frames/38.929 s, one underrun near shutdown, fallback=0/failed SMC=0; no controlled speedup claim.
- **Next:** stop repeating normal-entry attachment diagnostics. Import/relaunch handoff and responsive monitor are now evidenced; use the direct-runtime binding for actual controls and a named slow opening-scene interval. Preserve full G6/story/mobile/touch/menu scope and one-Simulator rule.

## 2026-09-06 — R81 corrected-package opening and attack bottleneck

- **Previous turn:** progress: frontend monitoring corrected. Hypothesis: the corrected resource/dual-core/120 ms package may fix the previously slow opening; test the real route rather than title/Observatory only. No game or booted Simulator existed at start.
- **Identity/setup:** direct packaged entry, game generated/extracted/run1, isolated user generated/runtime/opening-r81, --allow-interpreter --no-mods --graphics Metal. Wii NAND copied from fresh-file g6-pgo-control-r33, Config/config.ini from observatory-r75. Pipe configuration initially refused to replace copied WiimoteNew.ini; --force used only on this disposable copy before core initialization. Runner current signed hash 6b354a3cfcf11f36dc0a023d347ea94d8ef96a9e113cf7b5ccea2a1508562539; module verified 25e17d0e730b512f20083db26d3a7f58bc60da126161fe34c227357372c65393. GameData before/after a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6. Normal user save untouched.
- **Visible route:** ready A+B title inspected before save-load fixture (same slot/Play navigation; this NAND enters story, not Observatory), then opening-pages fixture. Star Festival appears 59.9 FPS. Forward jumps plus short screenshot-gated diagonal corrections traverse lake/road/town; Star Bits 3→14. Lake sample 58.8, most later readings 59.9, occasional counter overshoot. Navigation required corrections and is not a deterministic benchmark route. Left-jump from plaza approach triggered attack cinematic, with observed 42.6, 44.4, 43.8, 42.0 FPS; attacked plaza later 59.9. No single FPS snapshot claimed full-scene acceptance.
- **Causal sample:** generated/runtime/opening-r81/attack.sample.txt, five seconds during cinematic. CPU thread 3,988 samples: 3,812 in principal StaticRecomp Run branch, 3,705 under chassis_dispatch. Top generated chunk self groups include func_804530A0 (523) and func_804520A0 (397 plus additional callsites); paired-single helpers also visible. Video thread 3,685/3,988 in condition-variable wait, not GPU saturation. Host snapshot runner 102.1%, updater 40.3%, WindowServer 33.2%; JumpConnect not a top consumer. This supports CPU-side generated work as the next target, not resource/EFB disablement or a blanket external-load explanation.
- **Checkpoint:** first Save State action produced no file because StateSaves directory was absent. Created the isolated directory and retried; visible OSD confirms save. Actual checkpoint is POST-attack plaza, not the earlier slow cinematic. File StateSaves/RMGE01.s01 SHA-256 e837b70798a2eaf411d61ebf146366e7c7a6b4f44bd963e7a5009a6ec0a0e90d. It is diagnostic continuation only; cannot prove real save/load or reproduce earlier cinematic. Missing directory is a separate native diagnostic-state setup rough edge.
- **Shutdown:** UI close, exit 0. generated/runtime/opening-r81.log: 53,328 frames/926.175 s (~57.58 FPS), 222,491 DMA blocks/926.519 s (~30.74 kHz), 371 underruns/12 backlog corrections, 110,410 real depth reads (34.55 s cumulative/max 14.47 ms), 54 input transitions, fallback=0/failed SMC=0. Mixed navigation/story/cinematic/sample/state-save interval is not a controlled whole-route comparison. No runtime/Simulator remains; G6 still open.
- **Next:** map exact generated hot chunks to guest operations and inspect a semantics-preserving optimization hypothesis. Petari checkout has RMGK01 symbols only; do not apply its addresses directly to RMGE01. Reuse post-attack checkpoint for castle continuation, but obtain a pre-cinematic checkpoint if matched attack timing is needed. No more title-only or launcher-attachment loops.

## 2026-09-06 — R82 native count-leading-zeros candidate

- **Previous turn:** progress: real attack slowdown captured and CPU-side bottleneck localized. Disassembled exact packaged module, not a regional symbol guess. func_804530A0 symbol offset 0x4be360c plus sampled 74304 gives 0x4bf584c; preceding instructions contain the CNTLZW branch ladder, and following PC materialization resolves guest 0x80453AC8. Generated chunk_1103 shows guest 0x80453AAC CNTLZW r12,r31 emitted as a 32-iteration bit-search loop. This identifies a concrete inefficient operation within the hot region; the whole 523-sample group must not be attributed to CNTLZW alone.
- **Candidate:** patches/DolRecomp/0001-cntlzw-intrinsic.patch, SHA-256 93a3d55a6a3a28380abe351d7528fb059a2d2fee2f0d4b96aac9e5ce7fd3b551. GCC/Clang emission uses v ? (u32)__builtin_clz(v) : 32u; other compilers retain original loop. Destination, Rc update call, guest PC/downcount and other instructions unchanged. Zero guard avoids builtin undefined behavior. Bootstrap now permits only this pinned DolRecomp delta and validates nested scope; accepted application module is not regenerated in place.
- **Tests:** tests/test-cntlzw.sh reconstructs emitted code, compares intrinsic and portable branches against an independent reference over zero, all-ones, every one-bit boundary/complement and one million deterministic random words under UBSan. It verifies source record call remains and ARM64 fast function contains native clz. Passed. Actual toolchain test_codegen_emit/test_c_execute rebuilt; ctest codegen_compile and c_execute pass. Repository suite including new test passes.
- **Build:** moderngekko-desktop dolrecomp and port rebuilt. Candidate command GALAXYPAD_MODULE_OUTPUT=$PWD/generated/modules-cntlzw-r82 GALAXYPAD_PGO_PROFILE=generated/pgo/rmge01.profdata ./scripts/build-module.sh, redirected to generated/runtime/cntlzw-r82-build.log. Exec session 80440 remains live; exact-DOL/Broadway preflight passed and no candidate cache existed. ~15 GiB available versus ~756 MiB accepted artifact size. No game/Simulator running. Packaged runner hash still 6b354a3cfcf11f36dc0a023d347ea94d8ef96a9e113cf7b5ccea2a1508562539 and accepted module untouched.
- **Next:** poll existing build, not restart. Verify changed generator hash/cache identity and actual ARM64 lowering in candidate; then test comparable gameplay with strict semantics/no fallback. Do not promote from micro-tests or pretend this one instruction fixes the whole 42–44 FPS deficit. Original G6/full story/mobile/menu goal stays active.

## 2026-09-06 — R83 candidate link wait and THP decoding attribution

- **Previous turn:** progress: emitter optimization/regressions and isolated build started. Re-polled existing exec session 80440 and inspected live compiler/linker processes, never restarted. Compilation advanced 405→1,330 of 1,332 steps. Linker PID 70140 still live after two minutes at ~694% CPU; ThinLTO is active. Candidate cache suffix 1fd29a768e1f21dc, log generated/runtime/cntlzw-r82-build.log. No artifact completion claimed.
- **Profile caveat:** compiler reports mismatched old profile data ignored for changed functions and unprofiled files. Replacing a loop changes instrumentation CFG hashes. The resulting candidate may lose some PGO benefit; measure net result, and retrain if necessary rather than claiming the intrinsic guarantees faster gameplay.
- **THP lead:** Petari THPDec.c contains the same signed Huffman coefficient extension followed by natural-order lookup and 16-bit coefficient store seen at exact RMGE01 0x80453AAC/0x80453AC8/0x80453AD0. This is a semantic correspondence, not regional address substitution. Supplied MovieData has PrologueA/PrologueB and other THP files. PrologueA header read directly declares 59.9399986 FPS, 5,591 frames. Runtime movie identity not yet directly captured, but CPU video-decoder work is now a stronger specific hypothesis than generic rendered fire complexity.
- **Audit improvement:** audit-module.sh accepts one optional absolute candidate module path, otherwise retains accepted-marker behavior. Default accepted module passes; relative candidate rejected; repository tests all pass. This avoids changing the accepted marker just to inspect an experimental module. PERF.md records the decoding lead and PGO caveat.
- **Next:** poll 80440; while ld remains live do not launch game or rebuild. On completion audit exact candidate, inspect hot ARM64 lowering, record hash/manifest and proceed to gameplay comparison. Accepted app/module untouched; no game/Simulator running. G6/full objective remains active.

## 2026-09-06 — R84 candidate completion and native lowering proof

- **Previous turn:** progress plus verified wait: THP lead/audit support added, linker live. Re-polled existing session and process; ld remained active at 682–698% CPU, then session 80440 completed exit 0. No restart or concurrent game.
- **Candidate identity:** generated/modules-cntlzw-r82/RMGE01/2c680585a8f58e1cc9c5521b579057f12b124ff0ef409e470a57606c50a93c09-1fd29a768e1f21dc/gRMGE01_recomp.dylib. SHA-256 4e1464f9bbf00b1f5466fecca0d03ca47ebbbd082c230507bec066420d30ff89, 98,949,544 bytes. Manifest generator binary hash 2c1440e12ab16a2256b51485b6c8b1cbf8d8bfaa57afe27d5b83a35a5898eb81; same O2/ThinLTO/strict FP flags, chunk size/indexed dispatch and original PGO identity. Old-profile mismatch caveat remains.
- **Verification:** audit-module.sh with explicit candidate passes RMGE01/entry/ARM64/macOS14/1,322 chunks/19 SMC ranges. Actual candidate disassembly of func_804530A0 contains clz; hot r31→r12 sequence loads offset0x7c, clz at native0x4b99cec, stores offset0x30. This verifies intended compiled lowering, not runtime gain. Accepted packaged module SHA-256 still 25e17d0e730b512f20083db26d3a7f58bc60da126161fe34c227357372c65393.
- **Reproduction preparation:** tests/fixtures/g6-r81-pre-attack-route.json concatenates the exact 15 movement-fixture invocations from R81, stopping before its final left-jump trigger. 82 actions; dry-run validates encoding. Preconditions: fresh-file Star Festival entry reached through story, matching input setup and settled camera. External screenshot delays are not encoded, so this is NOT a proven deterministic route; inspect endpoint and correct if needed before creating a pre-cinematic checkpoint. Existing R81 state is post-attack and cannot replay its earlier movie.
- **Next:** confirm pre-attack checkpoint on baseline, then compare candidate from the same state with no concurrent build or frequent screenshots. Run gameplay/native dispatch/audio checks before any promotion. No current build/game/Simulator; full G6/story/mobile/menu scope remains open.

## 2026-09-06 — R85 pre-cinematic checkpoint and replay verified

- **Previous turn:** progress: candidate built/audited with intended native lowering. Started baseline packaged runner using isolated pre-attack-r85, copied fresh-file NAND/config from opening-r81, explicit Pipe setup, StateSaves directory created before launch. No other game/build/Simulator. Ready title and opening story inspected, then standard pages fixture reaches Star Festival.
- **Failed route/adjustment:** combined g6-r81-pre-attack-route.json landed near the dock, not R81's endpoint. It must NOT be treated as deterministic replay. Missing observation delays/continuous connection may contribute; no definitive cause assigned. Short manual inputs navigated out from dock railing and down the road. Three local diagnostic states saved at successively closer positions; only slot 3 is the selected comparison fixture.
- **Selected checkpoint:** generated/runtime/pre-attack-r85/StateSaves/RMGE01.s03, SHA-256 aea2b7a3fe295b77e409753a216abf8fe6f2f1ec23ee7a8832fc9ac1a27b7ab8. Visible position is yellow flowers beside the fountain/lake railing, before the attack. Left-jump then diagonal-forward-right starts the cinematic. Saved states 1/2 remain private exploratory checkpoints, not the selected target.
- **Replay proof:** loaded slot 3 through native States menu; OSD and screenshot show restored pre-attack position. New g6-r85-trigger-attack.json concatenates the two short movement fixtures and triggered the same cinematic on checked replay, title ~39.1 FPS. Initial trigger showed 38.5 FPS. This establishes a reusable diagnostic precondition, not a real-save acceptance claim or candidate result.
- **Cleanup/evidence:** normal close, exit 0, log generated/runtime/pre-attack-r85.log. 84,168 frames/1,459.753 s, 562 underruns, 197,159 real depth reads; state saves/reload and long navigation invalidate this as a matched performance run. No runtime/build/Simulator remains. Accepted package and candidate identities unchanged; candidate still untested in gameplay.
- **Next:** copy slot 3/NAND/config into separate baseline/candidate user directories; load after ready title and run the exact short trigger. Capture matched movie interval telemetry and check actual movie/render/audio behavior. Do not repeat the long navigation route. Keep full G6/story/mobile/menu objective active.

## 2026-09-06 — R86 G6 matched attack candidate comparison

- **Hypothesis:** native CNTLZW lowering reduces measured movie decode cost enough to justify candidate promotion.
- **Step:** sequential isolated control/candidate runs from R85 slot 3 with identical trigger and phase tracing; visually check restoration and progressing attack; normal native close after complete timing window. No Simulator booted.
- **Result:** fixed trigger-relative 60-second window measures 43.667 vs 44.300 FPS, 23.311 vs 23.648 kHz DMA, frame-start p99 24.332 vs 24.133 ms. Only +1.45%; no material fix or promotion. Both clean exit/native-only and unchanged NAND. Full details/identity/intervals in PERF.md; ignored evidence under generated/runtime/cntlzw-{control,candidate}-r86 and sibling logs.
- **Implementation:** reusable scripts/measure-attack-trace.py rejects incomplete intervals and reports event rates/nearest-rank gaps. Accepted app and active module marker unchanged; no game/Simulator remains.
- **Next:** inspect larger exact-DOL THP decode costs or PGO loss before another candidate. G6 still open; full story/mobile/menu goal retained.

## 2026-09-06 — R87 G6 decoder pixel-store localization

- **Prior-turn classification:** progress: R86 produced measured evidence rejecting candidate promotion.
- **Hypothesis:** repeated quantized pixel stores, not just Huffman CLZ, account for meaningful movie CPU cost.
- **Evidence:** inspected R81 sample and accepted disassembly; func_804520A0+51244 is return from ppc_psq_store_inline at exact guest 0x80452A2C (psq_st f9,0(r6),0,6). Profile includes repeated external byte-write/MMU stacks. Pinned THP source uses GQR6 unsigned-byte scale -3 and locked-cache work tiles, while GXRuntime only directly maps MEM1/MEM2. Details in PERF.md.
- **Interpretation:** locked-cache pixel-store fast path is a concrete hypothesis, not yet a measured address or safe implementation. MMU translation, memchecks and lockstep cache journals must survive optimization. Next bounded exact-PC EA/GQR/MSR diagnostic before another build candidate; do not blindly reuse raw HookExternalPointer.
- **Validation:** added boundary/incomplete-window regression for R86 analyzer, wired repository suite, all pass. No runtime/build/Simulator launched; accepted package/marker unchanged. G6 remains open.

## 2026-09-06 — R88 G6 live pixel-store diagnostic

- **Prior-turn classification:** progress: R87 mapped exact hot instruction and selected the next falsifiable runtime question.
- **Hypothesis/step:** locked-cache tiles cause per-byte MMU overhead. Added bounded opt-in TraceGalaxyPixelStore at guest 0x80452A2C; canonical pinned overlay, source-derived compiled tests, bootstrap, and runner build pass. Built separate ignored diagnostic app without replacing accepted package.
- **Runtime:** loaded copied R85 slot 3, visually verified flowers/lake, ran exact trigger, visibly reached advancing attack movie. Sixteen capped records confirm size 1, GQR6=3d043d04, MSR=a032, EAs in e0000000/e0000020/e0002a00 ranges. No game behavior changed. Normal close exit 0, fallback/failed SMC zero; no Simulator booted.
- **Evidence/identity:** generated/runtime/pixel-r88.log and Pixel-Diagnostic-r88.app. Signed diagnostic runner 3a62a5b8218ca6814e9d2e8aa6974971df0ebf7aaa9129d98bdf0997a1df0a94; accepted runner/module unchanged. PERF.md has patch identity and interpretation.
- **Next:** translation-safe locked-cache byte-store optimization, default-off and differential-tested with fallback for memchecks/shadow journaling. MSR.DR is on; do not skip address translation based only on effective 0xE address. G6 remains open; no runtime left.

## 2026-09-06 — R89 G6 guarded byte-store candidate

- **Prior-turn classification:** progress: R88 live diagnostic confirmed the exact locked-cache address/GQR/MSR precondition.
- **Hypothesis:** avoid generic per-byte MMU dispatch while preserving live BAT translation, L1 bounds and debug/shadow semantics to reduce THP pixel-store cost.
- **Implementation:** opt-in GALAXYPAD_LC_BYTE_FAST runner path and MMU::TryWriteLockedCacheByte; canonical hash-pinned overlay 0012. Guards/semantics detailed in PERF.md. Normal package and module unchanged.
- **Tests:** source-derived compiled sanitizer test for values/bounds/guards/remapping/DR-off passes; full repository suite passes. These are stubbed method tests, not a full MMU equivalence or speedup claim.
- **Build:** session 38409 live, log generated/runtime/lc-byte-r89-build.log; MMU header triggers dependent rebuild. No simultaneous game/Simulator. Next poll same handle, verify bootstrap once complete, build a separate candidate package, then off/on matched R85 trigger test with activation breadcrumb. G6 remains open.
- **Completion addendum:** build exited 0, separate LC-Byte-Candidate-r89.app signed/verified, runner SHA-256 d4d0c3c89c7f4534efc90d0fb2b280b62c0d3a0a4256b11a470c212878d8ff53. Initial bootstrap scope check failed because generated unified diff lacked diff --git metadata; corrected metadata updates overlay hash to d980680c30fda3e54593930d6815d78ead4c78cd26a9b074f77fbac77394f126 without changing code. Repeated bootstrap then passes. No build/runtime remains; next candidate-app off/on matched measurement, not another build.

## 2026-09-06 — R90 G6 locked-cache byte off/on comparison

- **Prior turn:** progress: guarded candidate built, sanitizer tests/overlay/package checks completed.
- **Hypothesis:** measured decoder byte writes benefit materially from guarded L1 stores. Ran same candidate app/module with opt-in 0 then 1 in isolated copies of R85 state/NAND/config; same trigger and phase tracing, one game at a time.
- **Result:** exact 60-second trigger-relative window 42.50→47.50 FPS (+11.76%), DMA 22.688→25.359 kHz, p99 frame start 26.868→22.803 ms. Activation breadcrumb confirms exact hotspot. Candidate movie visibly advances/completes, plaza returns, lateral input moves Mario. No promotion: trace-on diagnostic gain, still below 60 FPS.
- **Cleanup:** both normal exit 0/native-only, unchanged NAND. No game/Simulator remains. Evidence generated/runtime/lc-{off,on}-r90/phase.csv and sibling logs, detailed in PERF.md. Accepted package untouched.
- **Next:** uninstrumented movie/gameplay/G5 regression plus memory-equivalence strengthening before promotion; G6 remains open.

## 2026-09-06 — R91 untraced movie/control regression

- **Prior turn:** progress: R90 measured an activated candidate's +11.76% trace-on gain.
- **Step:** expanded byte-store test to 4,194,304 canonical-expression comparisons with BAT_WI and alias/remap cases; sanitizer/repository suite pass. Then ran unchanged R89 candidate/accepted module with opt-in on, no trace/profiler, isolated R85 state/config/NAND.
- **Visible result:** restore/trigger correct, attack progresses at 46.2 FPS, movie finishes to plaza 59.9 FPS, lateral input relocates Mario. Normal close exit 0/native-only, NAND unchanged; 255 whole-run underruns remain, so no audio/stability pass. Details and limitations in PERF.md.
- **Next:** G5 candidate file-select/audio regression without savestate, then promotion decision. Default package unchanged; no runtime/Simulator remains. Full G6/story/mobile/menu goal remains active.

## 2026-09-06 — R92 candidate normal-boot file-select regression

- **Prior turn:** progress: expanded byte tests and untraced movie/gameplay validation completed.
- **Step:** isolated copy of R72 config/NAND, no emulator state, same R89 candidate and accepted module with LC opt-in on. Ready title inspected then established G5 fixture once, including sustained file-select interval.
- **Result:** A+B and pointer targeting work visibly at 59.9 FPS; ~59.92 Hz/31.98 kHz whole-run cadence, 3 underruns and 3 backlog corrections, real depth reads. Native close exit 0, fallback/failed SMC zero, NAND unchanged. No LC activation on this route; do not attribute sparse underruns to optimized writes or claim zero-starvation acceptance.
- **Evidence:** generated/runtime/g5-lc-r92.log; PERF.md records exact counts/limitations. Repository checks pass, no runtime/Simulator remains. Default package unchanged.
- **Next:** same candidate disabled baseline to separate audio variance, then incremental-promotion decision. G6 remains open; do not equate file-select regression with full stability.

## 2026-09-06 — R93 disabled G5 baseline / incremental package default

- **Prior turn:** progress: G5 functional candidate regression, three underruns observed.
- **Control:** same runner/profile/fixture, LC disabled, no state/trace. Visible title/file select/pointer pass; ~59.92 Hz/31.98 kHz, three underruns/three backlogs, normal native-only exit and unchanged NAND. Timing/duration differ from enabled run; no identical-cause claim. PERF.md records limits.
- **Decision/action:** package guarded LC optimization as incremental default given matched movie improvement and passed bounded checks; not full stability acceptance. Wrapper defaults env to 1 and preserves explicit 0; compiled runtime outside wrapper stays opt-in. Added inheritance/opt-out tests and wrapper-identity package audit.
- **Verification:** repository tests, build (session 61752 completed 0) and private app audit pass. Packaged runner d4d0c3c...d8ff53 matches tested candidate; module unchanged. No game/Simulator remains; earlier package recoverable in packaging backup.
- **Next:** verify default package activates at attack without explicit LC env, then address remaining ~47-FPS decoder cost. G6/full original goal still open.

## 2026-09-06 — R94 packaged default proof / remaining CPU profile

- **Prior turn:** progress: baseline comparison and guarded incremental default packaged/audited.
- **Step:** current package launched with LC env unset; R85 checkpoint restored visibly, trigger once, exact activation breadcrumb confirms wrapper default. Five-second sample during visible movie, then normal close exit 0/native-only. No game/Simulator remains.
- **Evidence:** generated/runtime/default-lc-r94.log and default-lc-r94/attack.sample.txt. 3619/3940 CPU samples under generated dispatch, remaining paired-single arithmetic/conversion costs quantified in PERF.md. Sampled runtime still has underruns; no new performance or full stability claim.
- **Next:** bit-exact paired-single arithmetic/conversion optimization, preserving NaN/subnormal/FPSCR rules; no repeat default activation or EFB changes. G6/full goal remains open.

## 2026-09-06 — R95 finite paired-add/sub isolated probe

- **Prior turn:** progress: verified packaged default activation and refreshed decoder profile.
- **Hypothesis:** four finite input lanes can skip ni_add/ni_sub exceptional-input handling while preserving exact casts, rounding and guest status behavior.
- **Implementation/evidence:** tests/probe-ps-finite.c and shell runner compare standalone candidate with directly included pinned arithmetic code; not linked into game. One million whole-CPUState cases pass including four rounding modes, random FPSCR, special-value cross-product and aliases. Ordinary-add microbenchmark alternating candidate/reference ratios 0.703–0.726; not a game speedup claim. Full repository suite passes.
- **Next:** pinned isolated module add/sub candidate with actual-source regression, keeping exceptional inputs on old path. Use accepted generator, not rejected CLZ generator, and preserve strict FP/PGO controls. No default changes or live game/Simulator; G6/full goal remains active.

## 2026-09-06 — R96 finite add/sub candidate implementation/build

- **Prior turn:** progress: isolated arithmetic hypothesis passed state tests and microbenchmark.
- **Implementation:** two actual helper finite guards in ignored copied runtime; canonical experimental patch and hash-checked build script, no accepted-source/default mutation. Actual-body test passes one million comparisons. Full repository suite passes.
- **Build evidence:** reused accepted generated chunks (excludes rejected CLZ), original PGO/O2/strict FP/ThinLTO, arm64/macOS14. Session 39974 live, last 461/1332, logs generated/ps-finite-r96/{configure,build}.log. Two changed helper functions have old PGO data ignored; runtime benefit remains unproven.
- **Next:** poll same build, audit output, compare matched R85 route using current default LC runner. No game/Simulator running; app/module selection unchanged. G6/full original goal remains active.

## 2026-09-06 — R97 verified build wait / host FP parity

- **Prior turn:** progress: actual helper candidate tested and isolated build started.
- **Verified wait:** session 39974 remains live, advancing 746→1247/1332 in inspected logs; no restart, no runtime overlap. Link still pending.
- **Additional progress:** host FE_ALL_EXCEPT parity added to million-case actual-candidate test and passes. Disregard concurrent-build microbenchmark output. Explicit unselected experiment manifest and PGO hash guard added; module audit permits explicit generated-directory symlink, accepted module audit and repository suite pass.
- **Next:** continue same build handle through link, audit completed candidate, then matched gameplay; no app/module default change or live game/Simulator. G6/full goal active.

## 2026-09-06 — R98 verified ThinLTO wait / matched profiles prepared

- **Classification:** verified wait on session 39974. Source compilation reached 1330/1332; linker PID 84814 observed ~702%, then ~678% CPU at 5:03 elapsed. No errors or terminal result; do not restart or infer hang from quiet Ninja output.
- **Preparation:** isolated ps-baseline-r98/ps-candidate-r98 profiles copied from R85 Config/Wii/config.ini and only slot 3; Pipe configured independently. Both checkpoint hashes aea2b7a3fe295b77e409753a216abf8fe6f2f1ec23ee7a8832fc9ac1a27b7ab8. No game/Simulator launched during link.
- **Next:** same build handle through completion, audit candidate dylib, then use prepared profiles for matching traced R85 trigger comparison. Accepted app/module unchanged; full goal active.

## 2026-09-06 — R99 module completion and add/sub rejection

- **Prior turn:** verified wait on live ThinLTO with matched profiles prepared.
- **Completion:** original session 39974 exited 0, candidate SHA 89a47ced8d7304c326ea8dc25fdf2a12c5fa8a995697ba3f3fbdc6cbc7a01417, module audit passes.
- **Experiment:** sequential prepared baseline/candidate profiles, same default LC runner and trigger/trace policy. Both restore pre-attack and render advancing movie. Fixed minute 46.283 vs 45.517 FPS (-1.66%); candidate p99 worse. Reject promotion despite passing microtests. Full interval/identity/evidence in PERF.md.
- **Cleanup/next:** both native-only clean exit; no game/build/Simulator remains. Accepted module and LC default unchanged. Do not rerun unchanged candidate; inspect bigger decoder/profile hypothesis and prevent rejected CLZ from normal future generation. Full G6 goal remains open.

## 2026-09-06 — R100 default generator hygiene

- **Goal/hypothesis:** G6 remains open. A rejected emitter experiment still applied by bootstrap could leak into a future ordinary module build. Remove it without changing accepted gameplay artifacts.
- **Step:** bootstrap reverses exact hash-checked CLZ overlay, then requires clean pinned DolRecomp. Retained differential regression applies patch to a temporary copy from exact revision. Rebuilt both generator binaries, not game modules.
- **Commands/evidence:** bootstrap twice; cmake --build generated/build/moderngekko-desktop --target moderngekko-port-recompiler; cmake --build generated/build/dolrecomp --target dolrecomp; check-repository.sh; ctest --test-dir generated/build/dolrecomp -R 'codegen_compile|c_execute' --output-on-failure. All pass. Initial ctest against desktop cache lacked test executables, not a semantic regression; corrected cache has both tests passing. Hashes in PERF.md; accepted runner/module verified unchanged.
- **Next:** inspect larger decoder/profile opportunity. No runtime/build/booted Simulator; retain one-Simulator rule. This is build hygiene, not a new FPS gain or G6 acceptance.

## 2026-09-06 — R101 decoder training gap verified

- **Prior turn:** progress, default generator hygiene complete.
- **Goal/hypothesis:** G6 performance. Inspect helper lowering and accepted PGO before another code rewrite. Found both hottest decoder chunks have no training coverage, despite live R94 samples; exact counts/CFG hashes in PERF.md. This is a concrete next experiment, not a speedup claim.
- **Checks:** llvm-profdata show --counts --function for both chunks and helper; llvm-objdump of accepted add helper; recursive generated-source diff between trainer and accepted tree (identical); module-info and trainer/checkpoint/helper hashes. No rebuild, active package untouched.
- **Preparation:** existing instrumented module reusable; new decoder-training-r101 profile copied from R85, slot3 verified, Pipe configured. New pgo/decoder-profiles-r101 output directory. No game or booted Simulator.
- **Next:** launch existing trainer with separate LLVM_PROFILE_FILE, restore state3 and visually trigger attack, normal close to flush profile; prove nonzero decoder counts before building an isolated profile-only candidate. Training runtime is not performance evidence. Full G6/audio and original goal remain open.

## 2026-09-06 — R102 decoder training / candidate build

- **Prior turn:** progress, missing training coverage verified and isolated run prepared.
- **Execution:** existing trainer, current runner/default LC, R85 state3 restored visibly; exact trigger executed once (input session72522 exit0). Movie advances; native close flushes raw profile, runtime70563 exit0/native-only. NAND unchanged. No simulator.
- **Result:** both target decoder counters now populated with matching CFG identities. Actual counts/hashes/log in PERF.md. Merged with accepted profile without replacing original. This supports a profile-only experiment, not a performance claim.
- **Implementation/checks:** hash-checked isolated build script/config, repository suite passes. Build1268 active last74/1332, generated/pgo/decoder-r102-build.log; accepted package/module untouched. No game running.
- **Next:** poll same build, audit completed output, then matched R85 comparison. Do not restart due quiet ThinLTO, launch during compilation, or treat trainer FPS as candidate speed. Full goal remains active.

## 2026-09-06 — R103 verified build wait / test preparation

- **Prior turn:** progress, actual decoder training captured and profile-only build started.
- **Verified wait:** build1268 polled live, advancing 311→626/1332 with compiler processes; not restarted. No game or booted Simulator.
- **Preparation:** decoder-baseline-r103 and decoder-candidate-r103 copied independently from R85 Config/Wii/config.ini and state3; both state hashes aea2b7a3...7ab8, Pipes configured. Do not launch during build.
- **Safety correction:** discovered 31MiB default.profraw at repository root timestamp11:17:20, likely instrumented module-info inspection output. Moved exact file to ignored generated/pgo/module-info-default-r103.profraw, preserved contents. Added profraw/profdata to ignore and tracked-file rejection; git check-ignore and repository suite pass. No private output staged or published.
- **Next:** same build handle through link/audit, then matched prepared profiles; accepted package untouched, full G6 goal active.

## 2026-09-06 — R104 verified compiler wait

- **Classification:** verified wait on live build1268. Repeated bounded polls and log/process checks show 766→989→1155/1332; active compiler processes, no restart.
- **Checks:** no errors/profile-CFG mismatches observed in current log. Existing nested-comment warning and unprofiled cpu_interpreter_integer.c warning recorded; neither establishes a decoder mismatch. Accepted signed module25e17d0...65393 and runnerd4d0c3c...d8ff53 rehashed unchanged. No game/Simulator; diff check passes.
- **Next:** same handle through link/audit, then prepared decoder-baseline-r103/candidate-r103 comparison. No FPS or goal-completion claim.

## 2026-09-06 — R105 verified ThinLTO wait

- **Classification:** verified wait. Original build1268 advanced 1266→1330/1332; source compile complete and linker91424 active at ~699%, then ~688% CPU, elapsed1:35. Quiet Ninja output is not a hang; no restart.
- **State:** 15GiB available, no game or booted Simulator. Candidate not yet linked/audited; accepted package untouched.
- **Next:** same handle to terminal result and audit, then prepared R103 A/B. Full goal remains active.

## 2026-09-06 — R106 verified link wait

- **Classification:** verified wait, same build1268 and linker91424. Bounded polls remain live; linker inspected at ~689% CPU/3:26 elapsed/24:17 accumulated CPU, with no reported errors/profile mismatches. Subsequent 45-second poll still live. No restart and no game launch.
- **Next:** preserve same handle to success/audit; then prepared matched R103 profiles. Accepted package unchanged, G6/full goal active.

## 2026-09-06 — R107 link completion / baseline measurement

- **Prior turn:** verified wait. Build1268 completes0 and module audit passes; identity in PERF.md. No regeneration/restart.
- **Runtime:** accepted package with prepared decoder-baseline-r103/trace. R85 restoration visibly correct, trigger once, advancing attack, normal close11545 exit0/native-only. Fixed-minute38.733FPS, DMA20.6784kHz,p9928.933ms. Candidate not yet launched.
- **Interpretation:** baseline below historical result; follows heavy linker load, possible thermal/host confound. Candidate comparison needs follow-up baseline if apparent gain. No concurrent build/game now, no Simulator, accepted package unchanged.
- **Next:** prepared decoder-candidate-r103 with audited candidate, same trace/restore/trigger, then necessary control. Full G6 goal active; no improvement claim yet.

## 2026-09-06 — R108 candidate measured, reverse control prepared

- **Prior turn:** progress, candidate built/audited and first baseline measured.
- **Run:** candidate2580, exact R85 restore/trigger78102, visible advancing movie, same trace/default LC. Analyzer first refused incomplete window; same runtime continued, complete window40.733FPS/21.745kHz,p9930.451ms. Normal close0/native-only, NAND unchanged.
- **Interpretation:** +5.16% vs first38.733 baseline but worse tail; host/thermal confound remains. Do not promote. Exact data in PERF.md.
- **Next:** fresh prepared decoder-control-r108 accepted-module baseline, same trigger/trace, to test order/host effect. No game/Simulator/build running; full G6 goal active.

## 2026-09-06 — R109 reverse control completed

- **Prior turn:** progress, first candidate measured with unresolved host/order confound.
- **Control:** accepted module31325 restores R85 visibly and advances attack, input70078 exits0, complete trace47.283FPS/p9922.911ms. Native close0/fallback0/smc_failed0/NAND unchanged. Initial polling call lacked session id, corrected to returned31325 without any relaunch.
- **Interpretation:** accepted result now far exceeds initial38.733 baseline and candidate40.733; initial apparent+5.16% is not causal gain. Exact interval in PERF.md. No promotion.
- **Next:** one candidate confirmation after control using prepared decoder-confirm-r109, no recent build; then conclude comparison, no indefinite unchanged reruns. No game/Simulator running; G6/full goal active.

## 2026-09-06 — R110 decoder-PGO experiment concluded

- **Prior turn:** progress, reverse control exposed host/order variation.
- **Confirmation:** candidate44966 visibly restores R85 and advances attack, input52789 exits0, fixed43.933FPS/p9924.679ms; clean exit0/native-only/NAND unchanged. Exact counters and interval in PERF.md.
- **Decision:** reject promotion; final candidate below accepted47.283 and worse tails. No exact causal regression claim given host variance, but no useful demonstrated gain. Marked experiment manifest rejected. No more unchanged profile runs/rebuilds.
- **Next:** inspect hot decoder generated/native structure for higher-impact, falsifiable optimization before full compilation. Accepted package unchanged; no game/Simulator/build active. Full G6/audio/original goal remains open.

## 2026-09-06 — R111 exhaustive widening probe

- **Prior turn:** progress, decoder-PGO experiment rejected on gameplay evidence.
- **Inspection:** generated hot chunk uses repeated paired load/arithmetic helpers and state updates. Checked historical inlining/O3/GQR failures to avoid repetition. Integer convert_to_double normal path offered a small isolated simplification with no FP semantics change.
- **Experiment:** new standalone probe compares candidate to actual pinned header for every 32-bit encoding, passes all2^32. Four alternating mixed-bit microbenchmarks show6.9–8.1% slowdown; process17260 exit0, command/log in PERF.md.
- **Decision/next:** reject before costly module build, no runtime edits. Inspect memory-hook/batching semantics for a larger opportunity rather than another unchanged helper/codegen trial. No game/build active; original G6 goal remains open.

## 2026-09-06 — R112 paired-store contract regression

- **Prior turn:** progress, exhaustive widening candidate rejected cheaply.
- **Inspection:** exact paired-store/helper/memory-hook/MMU path. Found two separate byte callbacks with observable lane-read sequencing; naive batching not equivalent. Dormant external_pointer ABI exists but current implementation lacks live BAT/debug eligibility for safe reuse.
- **Test:** new source-derived test-psq-store-order.py validates callback/lane ordering, GQR snapshot,32bit wrap,w/disabled/invalid behavior under UBSan; wired into full suite, all pass. No runtime edits or FPS claim.
- **Next:** independently test guarded two-byte locked-cache pointer eligibility and fallback contract before implementation; never blindly expose raw pointer. No game/build/Simulator used; accepted package unchanged, full goal active.

## 2026-09-06 — R113 isolated pair-pointer eligibility

- **Prior turn:** progress, paired-store contract captured in regression.
- **Implementation:** unselected/unapplied experimental MMU pair-pointer method, same-page/full-range live translation and debug/cache/journal guards. Caller exclusions explicitly required; runtime untouched.
- **Tests:** test-lc-pair-pointer.py passes ASan/UBSan, allcacheoffsets/all65536pairs at four offsets, byte-method parity, remap/DR/flags/guards/bounds/null/smallcache. Initial stub declaration match corrected, then full repository suite passes.
- **Next:** optional caller integration with explicit MSR/journal/quantization and unchanged fallback contract, then evidence before promotion. No game/build active; accepted package unchanged and full G6 goal open.

## 2026-09-06 — R114 caller capability/fallback prototype

- **Prior turn:** progress, isolated pair-pointer eligibility tests complete.
- **Prototype:** unapplied type4 pair snippet uses size0 capability request rejected by existing host, avoiding unchecked old-pointer path. Host contract requires side-effect-free null on unsyncedMSR or other failed guards. Positive-size legacy pointer path not changed.
- **Tests:** actual reference store/quantizer extraction;1M operand/64scale comparisons including hostFPflags, null/journal fallback order/wrap/GQR/type/lane tests; sanitizers and full suite pass. No live runtime changes or performance claim.
- **Next:** isolated opt-in host callback integration and coherent verification/build only after guards pass. Accepted package unchanged, no game/build/Simulator, full G6 goal active.

## 2026-09-06 — R115 host request guard prototype

- **Prior turn:** progress, caller quantization/fallback prototype tested.
- **Prototype:** size0 callback exact opt-in; excludes pixel trace, unsyncedMSR, lockstepjournal, non-E and relocated either byte. No MSR propagation ahead of quantization. Delegates live MMU/debug/cache/bounds eligibility; legacy positive-size behavior preserved.
- **Checks:** actual callback with unapplied snippet compiled against host stubs under sanitizers; environment/guard/legacy cases pass, full repository suite green. No native runtime mutation/build or speed claim.
- **Next:** assemble isolated runtime+module candidate with unchanged accepted generated code/PGO; validate integrated source before compilation, preserve normal bootstrap/app. Full G6 goal active, no game/build running.

## 2026-09-06 — R116 experimental host built / caller integrated

- **Prior turn:** progress, host guard prototype verified.
- **Assembly:** canonical experimental host/runtime patches SHA87d3d595...950a1 / ba071642...ecd1dd. Host script temporarily applies only experiment, builds182steps, copies runner, reverses delta via EXIT trap. Build16456 exit0, candidate runner6dde0e4dea8621dc1064666e0da6bfb6258535b72e7121def485752521519ed6. Forward apply check after completion proves reversal; original packaged runnerd4d0c3c...d8ff53 unchanged. Desktop build cache still experimental until normal rebuild.
- **Caller:** copied GXRuntime under generated/lc-pair-r116. Initial git apply silently skipped path because copy inherited outer repository; detected unchanged hash, initialized local ignored Git metadata in copy, applied patch and verified source1350d1196b38b147477890ef0dc59d5d9e389a55a169d51913fc033be34edb69. No commit. Actual caller byte/FP/fallback test and integrated host test pass; full suite passes after source restoration.
- **Next:** isolated module using accepted generated chunks/original PGO and copied runtime; then coherent candidate app and matched opt-in runtime comparison. No game/build active. Full G6 goal remains open; no speed claim.

## 2026-09-06 — R117 paired-store module build started

- **Prior turn:** progress, experimental host built and actual copied caller tested.
- **State:** runner/source hashes reverified,14GiB available, no game or booted Simulator. No existing module build found.
- **Build:** scripts/build-lc-pair-module-experiment.sh verifies runner/cpu/float/accepted-module/profile identities, tests actual caller, uses accepted generated chunks and originalPGO/O2/ThinLTO/strictFP. Config explicitly unselected. Exec4915 active, module-build.log observed114/1332. Existing comment/unprofiled-integer warnings, no errors observed. Full repository suite passes.
- **Next:** same build handle through link/audit, no restart or runtime overlap; then separate candidate app and verify activation before matched performance. Original package unchanged; desktop host cache remains experimental until normal rebuild. Full G6 goal open.

## 2026-09-06 — R118 verified build wait / profiles prepared

- **Prior turn:** progress, matching module build initiated with checks.
- **Verified wait:** module4915 still live,352→558/1332 during inspection and another bounded poll; no restart/error observed. No game or booted Simulator.
- **Preparation:** three independent R85 profiles pair-off-r118/pair-on-r118/pair-activation-r118, Pipes configured and all slot3 hashes verified. Activation diagnostics separate from timed runs. Runner symbol/native code confirms pair method exists; actual successful pointer acquisition still requires runtime proof. Native success add at symbol+0xd4, shared return+0xdc.
- **Next:** same build to link/audit, then isolated coherent app and activation proof before on/off performance. Accepted package unchanged, full G6 goal open.

## 2026-09-06 — R119 verified compilation/link transition

- **Classification:** verified wait on original4915. Bounded polls/log checks show941→1125→1330/1332, then active linker99100 ~724%CPU. No errors observed; no restart.
- **State/next:** no game or booted Simulator; package untouched. Same handle through link/audit, then isolated candidate app and separate activation proof before same-binary on/off measurement. Full G6 goal remains active.

## 2026-09-06 — R120 verified ThinLTO wait

- **Classification:** verified wait on same4915/linker99100, active ~713%CPU at3:14elapsed after bounded polls. No reported errors;14GiB available. No restart/runtime launch/package change.
- **Next:** same handle through completion/audit, isolated app, activation proof and prepared on/off comparison. Full G6 goal remains active.

## 2026-09-06 — R121 build completion / private candidate app

- **Prior turn:** verified wait. Same4915 finishes0 after active linker99100; module audit passes, no restart.
- **Package:** separate Pair-Candidate-r121.app copied from accepted app, candidate runner/module inserted, ad-hoc signature/deep-strict verification and resource diff pass. Candidate signed identities and descriptor in PERF.md. Normal app hashes unchanged.
- **Next:** prepared activation-r118 profile, explicit opt-in and actual successful pointer proof; then same-binary on/off timing without diagnostic overhead. No game/build active, full G6 goal open.

## 2026-09-06 — R122 successful runtime capability proof

- **Prior turn:** progress, candidate linked/packaged/audited.
- **Runtime:** isolated signed candidate opt-in1 restores R85 visibly, trigger80389 exits0. Debugger53292 attaches99616, breaks pair method, steps out to ppc_psq_store+152 with nonnull pointer; exact caller func_804520A0+51244. Detach exits0 and movie visibly resumes. Native close55420 exits0.
- **Evidence/interpretation:** logs in PERF.md, actual successful fast-path acquisition proven. Debugger run is not speed/audio acceptance. No game/debugger remains, normal app untouched.
- **Next:** same candidate opt-in0/1 prepared R118 profiles with phase tracing, no debugger overhead; no promotion before evidence. Full G6 goal active.

## 2026-09-06 — R123 paired-store OFF control

- **Classification:** progress, first matched timing control. Same signed candidate, pair opt-in0, existing byte path1, prepared R85 slot3 and trigger once. No debugger/build overlap.
- **Result:** phase analyzer accepts complete fixed minute: 2794 frames,46.5667 FPS,frame p99 23.214125ms,DMA24.861867kHz. Movie completes visibly into plaza; title sample59.9 there is not movie acceptance. Native close41298 exit0,fallback0,smc_failed0. Whole296.7s run has283underruns/6backlogs, not directly comparable to unequal-duration runs.
- **Evidence:** generated/runtime/pair-off-r118/phase.csv and pair-off-r123.log. Next unused pair-on-r118 with identical candidate/checkpoint/trace policy. G6 open, no promotion.

## 2026-09-06 — R124 paired-store ON comparison

- **Hypothesis:** guarded pair acquisition reduces decoder callback cost without altered quantization. Same candidate/R85 lineage, explicit pair1 instead of0, byte1, phase tracing, no debugger/build. Runtime76922/input28808 exit0.
- **Result:** complete fixed minute3112frames=51.8667FPS (+11.38%), frame p95/p99 20.386542/21.058750ms, present p99 21.027292ms, DMA27.6864kHz. Wall-time aligned, not exact guest-frame aligned; host variability remains a limitation.
- **Visible/safety:** yellow-flower checkpoint restored, movie advances and returns to plaza59.9title sample, native close0, fallback0/smc_failed0. NAND unchanged a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6. Whole187.6s has152underruns/3backlogs; unequal duration prohibits raw-total audio comparison. No game/Simulator remains.
- **Evidence/next:** generated/runtime/pair-on-r118/phase.csv and pair-on-r124.log. Candidate unselected. Next uninstrumented movement/G5 regressions and bounded confirmation before promotion. Full goal/G6 performance open.

## 2026-09-06 — R125 uninstrumented pair gameplay regression

- **Prior turn:** progress, first matched comparison showed11.38% gain. Verified no game/Simulator, candidate runner/module and R85 state hashes before launch.
- **Hypothesis/step:** improvement must survive without tracing and preserve movie-to-gameplay input. New disposable pair-untraced-r125 from R85, pair1/byte1, no debugger/phase trace/build. Restore slot3 visibly, trigger17059 once, wait for movie completion, right-one-second96081 once; both input sessions exit0.
- **Evidence/result:** movie50.6FPS snapshot, plaza60.0, then moved Mario/camera58.2. Visible movement proves responsive return, not merely an advancing movie. Native close93900 exit0, fallback0/smc_failed0. Log generated/runtime/pair-untraced-r125.log; whole219.3s141underruns/4backlogs, no audio acceptance. NAND a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 unchanged.
- **Next:** G5 pointer/file-select candidate regression and bounded matched confirmation before default promotion. Normal package unchanged; experimental desktop cache warning remains. No runtime/Simulator active. Full goal/G6 open.

## 2026-09-06 — R126 candidate normal-boot G5 regression

- **Prior turn:** progress, uninstrumented movie-to-movement continuation passed its limited scope. No game/Simulator verified; candidate hashes rechecked.
- **Hypothesis/step:** paired-store candidate must preserve title/file-select behavior outside decoder route. New g5-pair-r126 copies R72 Config/Wii/config.ini, no states, same G5 fixture73111 once after ready title inspected. Pair1/byte1, no tracing/debugger/build.
- **Evidence:** title59.9FPS, file1 Mario highlighted by pointer, final60.0. Fixture exits0; close35088 exits0,fallback0/smc_failed0. Private g5-pair-r126.log:12195frames/203.525s,50932DMAblocks/203.825s,1underrun/2backlogs,zero backend errors,24018depth reads,4button transitions. No LC breadcrumb expected on this route. NAND unchanged a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6.
- **Interpretation/next:** bounded input/display regression passes; not zero-starvation, new-file-creation reproof, or broad audio acceptance. Candidate unselected. Next reverse-order attack confirmation to bound host/order uncertainty before promotion decision. No runtime/Simulator remains; full goal/G6 open.

## 2026-09-06 — R127 reverse-order ON confirmation

- **Prior turn:** progress, bounded G5 regression. Verified no game/Simulator and same signed candidate identities before launch.
- **Hypothesis/step:** test repeatability/order sensitivity of prior gain; ON first this pair. Fresh R85 profile pair-confirm-on-r127, exact state hash, pair1/byte1, same trace window, no debugger/build. Restored visibly, trigger30323 once exits0.
- **Result:** complete fixed minute3120VI/frame events=52.000Hz, frame p95/p99 20.302292/20.703125ms;3121present events=52.0167Hz (boundary difference),13014DMAblocks=27.7632kHz. Close to prior ON51.867, not yet completed reverse-order A/B. Movie visibly returns to plaza60.0snapshot.
- **Safety/evidence:** native close22819 exit0,fallback0/smc_failed0,NAND unchanged a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6. Whole229.3s155underruns/3backlogs not audio acceptance. Private pair-confirm-on-r127.log and pair-confirm-on-r127/phase.csv.
- **Next:** fresh OFF half, then promotion decision without more unchanged repetitions. Candidate unselected, no runtime remains, no Simulator used; full G6 goal open.

## 2026-09-06 — R128 confirmation complete / integration decision

- **Prior turn:** progress, reverse-order ON measured52.0. Verified no game/Simulator and same candidate hashes. Fresh R85 pair-confirm-off-r128, pair0/byte1, same phase policy, trigger40348 once exits0; no debugger/build.
- **Result:** fixed minute2796frames=46.600FPS, frame p95/p99 22.572417/23.111875ms, present p99 23.161250ms, DMA24.881067kHz. Reverse pair ON52.0 is11.59% faster; first pair11.38%. Both orders support material gain despite wall-time alignment/host limitations.
- **Safety:** movie visibly completes into plaza, native close28766 exit0,fallback0/smc_failed0,NAND unchanged a5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6. Whole246.9s280underruns/4backlogs not matched audio acceptance. Private pair-confirm-off-r128.log and phase.csv.
- **Decision/next:** accept for incremental integration based on source-derived safety tests, actual activation, both timing orders, untraced gameplay and G5 regression. selected=false until normal bootstrap/cache/package integration audits. No further unchanged timing repetitions. Wire canonical patches/cache identity/default with explicit0 opt-out, verify normal build and package; preserve old accepted artifact until ready. G6/audio/full goal remain open. No runtime/Simulator remains.

## 2026-09-06 — R129 host bootstrap integration

- **Prior turn:** progress, repeatable timing gain accepted for integration. Current status/worktree inspected; package hashes verified unchanged after work.
- **Change:** bootstrap hash-pins existing canonical lc-pair-host.patch, peels it ahead of lower overlapping patch idempotency checks, restores it last on failure, reapplies after LC byte patch, includes paths in allowed dependency scope. EXIT restoration remains installed until complete. Runtime opt-in unchanged; no wrapper/default/package mutation.
- **Tests:** bootstrap75997 exit0 then repeat79170 exit0; reverse-check proves host patch integrated. Full repository suite17322 exit0, including actual integrated callback guards/legacy path. Logs generated/lc-pair-r116/bootstrap-r129.log, bootstrap-repeat-r129.log, check-r129.log.
- **Finding/next:** moderngekko_port.cpp cache identity includes emitter binary/codegen/profile but not GXRuntime source content. Fix that before caller integration/selection to avoid stale module reuse. Caller source remains original; host vendor source now integrated. Normal package d4d0c3c...d8ff53/25e17d0...65393 unchanged. Full goal/G6 open; no game/build active.

## 2026-09-06 — R130 source identity collector

- **Prior turn:** progress, host bootstrap integrated. Inspected actual port key and module CMake dependencies: runtime cpu sources/headers and ABI/template content absent from existing cache identity.
- **Change/test:** unapplied module-source-identity.inc collects sorted relative paths and HashFileSha256 results from runtime src/core/include, template and StaticRecompABI.h; fails closed on absent/empty trees, missing hash, filesystem errors or symlinks. Compiled test passes ASan/UBSan for per-input content change, creation order, checkout relocation, addition/rename, symlink/missing rejection. Hash provider is a deterministic content double; does not test cryptography or actual port wiring. Added to repository suite.
- **Next:** wire into actual port identity before cache hit and manifest; test cache-key invalidation and add canonical bootstrap overlay. Not yet cache-safe production selection; runtime caller/package unchanged. Full goal/G6 open, no runtime/Simulator launched.

## 2026-09-06 — R131 cache fingerprint integrated

- **Prior turn:** progress, collector tested. Added canonical0014 overlay (SHA d3640ee79866a4a48482d90b44410a83d538c4b8be7fa72cc30b192e33278431), collector called before cache lookup; existing key includes source fingerprint and manifest records module_sources_fnv1a. Per-fileSHA256 content list feeds existingFNV key scheme; no cryptographic collision claim for FNV.
- **Bootstrap/tests:** hash pin, peel before lower port overlays, restore last on failure, scope allowlist. Initial35985/repeat57752 exit0. Collector test requires exact integrated helper, extracts actualFNV, verifies derived-key changes per source category plus previous fail-closed/path tests. Content hash remains test double. Full suite89191 exit0.
- **Build:** normal moderngekko-port target74137 ACTIVE, observed45/185 including shared core dependencies, log generated/lc-pair-r116/port-build-r131.log. No module generation/selection/package mutation. Poll same build next; do not restart.
- **Next:** complete port build, integrate runtime caller while retaining independent sequential oracle, then module/cache/package audits. Full G6 goal active; no game/Simulator launched.

## 2026-09-06 — R132 caller integrated / independent oracle retained

- **Prior turn:** progress, cache key integration. Original74137 finishes0, portSHAcb8f514c0faf0f575f8157bd654ec91b67cba3dbe89791127bfed9445e167ff3.
- **Change:** runtime patch canonicalized under vendor paths as0013-lc-pair-runtime.patch SHAd05dda85e73b20f41f7042b1a6df2476fc30c971664c922ccfd7bd6f66483975. Bootstrap applies/hash-checks actual cpu.c1350d1196b38b147477890ef0dc59d5d9e389a55a169d51913fc033be34edb69 and allows scope. Independent oracle from git HEAD cpu.c verifiedec16e3a5...b69c7; pair test requires actual optimized body equals expected candidate, rather than injecting into optimized source.
- **Tests:** bootstrap33971/repeat94855 pass. Concurrent source-reading suite94441 failed while bootstrap peeled collector; setup race, not accepted evidence. Sequential settled-source rerun39396 passes full suite; log check-stable-r132.log. Run bootstrap/tests sequentially henceforth.
- **Next:** normal originalPGO module build with new cache fingerprint; audit module/cache before default/package promotion. Active marker remains old1a7fde44f42859a5, app unchanged. No runtime/Simulator launched; full G6 goal open.

## 2026-09-06 — R133 normal module build / output-path correction

- **Prior turn:** progress, caller integrated. Verified no game/compiler/Simulator,13GiB free, originalPGO f39a3ced...460d and actualcpu1350d119...edb69 before build.
- **Build:** normal build-module.sh with originalPGO and isolated output override;44651 active, new key44290ae60c4cad14, compilation30→164/1332. No restart. Log generated/lc-pair-r116/normal-module-r133.log.
- **Path defect:** relative output override interpreted under build_dir during build but caller directory during inspect. Actual valid cache under generated/build/moderngekko-desktop/generated/modules-pair-integrated/RMGE01/<DOL>-44290ae60c4cad14; preserve it. Fixed script normalization for future calls; actual-block test covers default/relative/absolute/spaces after cwd change and passes. Existing running script retains old variable; final inspect may target empty outer cache, so audit exact built path explicitly.
- **Next:** poll same44651 through link; compare/audit module and manifest before normal active selection/package. No game/Simulator, original app and marker unchanged. Full G6 goal active.

## 2026-09-06 — R134 verified build progression / generated parity

- **Classification:** verified wait on original44651; bounded polls and log advance796→1330/1332. No restart or new module build.
- **Evidence:** recursive generated RMGE01 tree comparison versus accepted is empty; CMakeCache confirms Release/O2/originalPGO/integrated GXRuntime path. Full repository suite13470 exit0, generated/lc-pair-r116/check-r134.log.12GiB available at check.
- **Next:** same nested44290ae60c4cad14 cache/build handle through link and audit, compare measured module before normal marker/package update. Original app/active marker unchanged. No runtime/Simulator; full G6 goal open.

## 2026-09-06 — R135 verified ThinLTO wait

- **Classification:** verified wait. Original44651 remains live; exact linker10867 advances1:51→3:22→4:59 elapsed at~697–707%CPU, no error/completion reported. No restart or competing runtime.
- **Next:** same handle to completion, then explicit nested-cache audit/hash comparison. No app/active-marker mutation; no Simulator launched. Full G6 goal remains open.

## 2026-09-06 — R136 reproducible artifact parity / package started

- **Prior turn:** verified wait.44651 exits1 after successful link/publication, attributable to known relative-output workflow; no linker process remains. Explicit module audit passes arm64/macOS14/1322chunks/19SMC.
- **Parity:** normal unsigned modulee8da217b3594c69a0b02546964e07feeef0fab49a0fb369bce3c6d2a88982d00 matches measured candidate exactly; normal runner6dde0e4dea8621dc1064666e0da6bfb6258535b72e7121def485752521519ed6 also identical. Manifest sources fingerprintcabf2f59c72309c2,originalPGO/O2/indexed1024.
- **Selection:** copied audited module/manifest into normal44290ae60c4cad14 cache with generated-tree symlink to retained nested build. Standard originalPGO build82303 exits0, confirmed cache hit/new absolute active marker; no regeneration. Wrapper pair default1 plus byte1, both explicit0 inherited opt-outs tested.
- **Package:** normal build-macos-app34200 ACTIVE, log generated/lc-pair-r116/package-r136.log. Script retains prior app before replacement. Poll same handle, then audit/signature/hash and visible wrapper smoke. No runtime/Simulator. Full G6/audio goal open.

## 2026-09-06 — R137 normal package promotion verified

- **Prior turn:** progress, exact runner/module reproduction and selection.34200 completes0; audit-macos-app and module/signature checks pass. Full repository80042 exit0.
- **Identity/default:** signed runneraffbcb51f27f853b0b5ee403b5033c3bd5a6f7ea3e514b969c2ef4272af9e295,module315d08dbe765d55aeaedf3dd55738c0e7b6a7720a5e863c2692dbdffdaed63b0 exactly match tested candidate. Previous app preserved at generated/macos/GalaxyPad.app.previous.20260906T183531Z. Pair default1/explicit0 opt-out tested; config now selected=true.
- **Visible smoke:** normal app14639 launched with pair/byte/phase env unset, fresh R85-derived package-pair-r137. Restore slot3, right-one-second68704 once, both screenshots visibly show Mario/camera displacement at59.9–60.0snapshot. Native close0,fallback0,smc_failed0,NAND unchangeda5743199...afa6.124.9s4underruns/6backlogs, not audio/soak acceptance. Private package-pair-r137.log.
- **Next:** use promoted package to investigate remaining representative G6 gameplay compute cost (Observatory/heavier views); no more unchanged pair A/B. Movie gain~11.5% accepted but52FPS/audio deficits keep G6 open. No game/Simulator active; full original platform/menu/story goal unchanged.

## 2026-09-06 — R138 promoted Observatory diagnostic

- **Prior turn:** progress, package promotion verified. No game/Simulator; R64 Config/Wii/config.ini copied into isolated observatory-r138 (CPUThreadTrue/native640x528), NAND5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6.
- **Runtime:** ready title visually checked; save-load60310 once exits0, one-star Observatory visible. sample13337 10s/1ms60380 exit0, cpu-sample.txt under profile. Final58.9FPSsnapshot versus initial60.3; not sustained measurement. Native close31766 exit0,fallback0/smc_failed0,NAND unchanged.
- **Evidence:**6174/7298 CPU-thread samples under StaticRecomp Run,5442 under dispatch;199 float-future wait samples inside dispatch. Physical footprint732.6M/peak1.8G. Whole192.5s88underruns/18backlogs,24388depth peeks,6button transitions. Private observatory-r138.log. This sampled run is not matched to older controls; unlike historical zero-peek Observatory data it includes active depth queries.
- **Next:** inspect collapsed leaves and actual hot chunk structure for remaining compute cost, explicitly account for current pointer/readback state. No repeat pair A/B or broad performance claim. No runtime/Simulator remains; full G6 goal open.

## 2026-09-06 — R139 host-loop cost attribution

- **Prior turn:** progress, fresh promoted Observatory sample. Inspected actual Run.cpp, generated805170A0 chunk, signed Run disassembly and all counter consumers.
- **Evidence:**751/7298CPU-thread leaf samples in Run (~10.3%); collapsed offsets include+988 (sample-enable guard) and+1224 (idle-PC load), but individual instruction weights unavailable.514 aggregate dispatch leaves span threads; do not present global collapsed table as CPU-only. Chunks are address-range dispatchers, not semantic single functions.
- **Rejected shortcuts:** native-dispatch count drives lockstep start/limit as well as trace/sample cadence, so removing/batching it is not purely diagnostic. Per-block cycle/timebase/interrupt checks preserve timing; native division by12 already lowers to umulh/shift/msub. Only charged-cycle total is diagnostic, insufficient evidence for a worthwhile isolated change. No code/runtime policy mutation.
- **Next:** existing default-off dispatch-site sampling on ready-title→real-save Observatory, interpret PC frequency separately from CPU-time sample; map concrete entry PCs before candidate. Preserve EFB/timebase/interrupt semantics. No game/build active, package unchanged; full G6 goal open.

## 2026-09-06 — R140 guest-PC frequency diagnostic

- **Prior turn:** progress, source attribution ruled out blind counter/timebase removal. No runtime/Simulator before launch. Promoted package/R64-derived real-save profile, STATICRECOMP_DISPATCH_SAMPLES=1, title inspected, fixture14161 once exits0, one-star Observatory visibly active.
- **Result:** native3408206674,fallback0,smc_failed0; top every4096dispatch samples804a2f14:19744,80517538:13971,80517584:13789,8001cf64:10488,8051e830:8569. Whole-run/startup and periodic-sampling bias remain; frequency is not CPU cost. Generated804a2f14 follows cache-instruction fallback at804a2f10;805175xx is register save/restore code.
- **Safety:** native close84370 exit0,NAND5040acdd...64a6 unchanged,220.15s9underruns/11backlogs; not audio acceptance/comparison. Private dispatch-r140.log. No runtime remains.
- **Next:** inspect exact save/restore helper and cache-op boundaries for unnecessary dispatcher exits with unchanged timing/memory semantics; no speculative cache suppression. Full G6 goal open, package unchanged.

## 2026-09-06 — R141 cache invalidation bounds prototype

- **Prior turn:** progress, dispatch-PC frequencies. Actual HookInstructionFallback already has dcache-off cache-op path preserving privilege fallback, invalidation and PPCTables cycle charges. Save/restore805175xx is straight-line memory operations through blr, not per-register dispatcher exits. No generic suppression warranted.
- **Hypothesis/change:** OnICacheInvalidate still binary-searches code ranges for out-of-code data invalidations. Unapplied smc-outside-bounds.inc checks envelope after existing fallback-JIT notification and linked-address resolution; does not suppress either or alter guest timing/cache instruction.
- **Test:** actual method compiled alongside injected candidate;200k sorted nonoverlapping range/state/input comparisons including empty/inactive, relocation, wrapping/zero lengths, fallback callbacks. ASan/UBSan23387 passes. Test added to full suite; no runtime patch/build/launch. Stubbed test is not lockstep/hardware proof.
- **Next:** quantify eligibility/cost before host candidate; current sample's OnICacheInvalidate leaf contribution alone is modest. No speed claim. Full goal/G6 open, promoted package unchanged.

## 2026-09-06 — R175 canonical identity audit/preparation

### R176 follow-up — canonical artifact completed

### R177 follow-up — actual package compatibility

### R178 follow-up — residual CPU attribution

### R179 follow-up — raw instruction capture

### R180 follow-up — range analysis

### R181 follow-up — external-entry hypothesis reduced test

### R182 follow-up — whole hot-chunk assembly

### R183 follow-up — whole-chunk correctness

### R184 follow-up — remaining synthetic gates and link

### R185 follow-up — verified link wait and test preparation

### R186 follow-up — linked candidate gameplay

### R187 follow-up — current control and candidate parking

- Prior turn progress: candidate measurement/visible compatibility. Fresh accepted control98930/trigger57920, sameR85config/window/policy:54.5FPS,p9920.070333ms,DMA29.0986667kHz. Candidate55.25 difference1.376%, insufficient for promotion; parkunselected, no unchanged third timingrun.
- Baseline movievisibly completes, clean0/nativeonly/NANDunchanged; whole299.8s105underruns/5backlogsnotacceptance. No game/build/Simulator. App/normalmarker unchanged.
- Next larger algorithm-level THP boundary feasibility audit: exactRMGE01 function/ABI/register-memory/timing sideeffects before native replacement. Do not shrinkgoal or erase correctness work; G6/audio/performance and fullPRD remain open.

- Prior turn progress/verifiedwait. Same1613 confirmed linker4:18→5:50 then FINISHED0/auditpass. CandidateSHAaff3f3ec52d7e6cdb9f138fed2b056e77054b1340c5b7ad725cdee970da4a289,114404792bytes,+82608. No restart.
- Runtime54348 with acceptedrunner/candidate/isolated R185R85profile; readytitle/restore inspected then trigger11390once. Fixedminute55.25FPS,p9919.627375ms,17gaps>=20ms,DMA29.4954667kHz. Visible moviecomplete/plaza and movement81147 pass; clean0/nativeonly/NANDunchanged.282.7swhole93underruns/10backlogsnotacceptance.
- Historical53.6 not pairedproof. Next fresh acceptedFPRF control samepolicy/window beforegain/promotion claim. App/normalmarker unchanged/no runtime/build/Simulator; G6fullgoal open.

- Prior turn progress: expanded tests green and isolated link started. Same1613 confirmed live linker46787 (~680–706%CPU,1:18→2:22), no restart. Fullsuite15883 FINISHED0, generated/check-r185.log. No game/Simulator.
- Prepared runtime/chunk-entry-r185 exactR85Config/Wii/slot3/Pipe; hashes slotaea2...7ab8/NANDa574...afa6 verified. No runtime launched during build. Added explicit reference/candidate sourceSHA assertions to private wholechunk harness; checks and py_compile pass, sourcevariants identical to prior successful32768tests.8.3Gi available.
- Next same1613 finish/linkaudit/binarysize+SHA, then exact matchedmovie test with accepted runner and fresh profile. App/normalmarker untouched/G6fullgoal open.

- Prior turn progress: wholeentrysanitizer parity. Added journal callbacks/reservation state to records,8modes32768cases.82961O2ThinLTO and83417ASanUBSan both0, capped0/170262journalevents/283reservationclears, fullstate/memory/orderedcallbacks match.
- Added isolated onechunk builder with exact module/source/profilepins, actual Ninja flags and allobjecthash provenance; only1of1329objects replaced. Build1613 ACTIVE, generated/chunk-entry-r184-build.log, do not restart. Expected missingprofile/size risk stays explicit. Next samehandle link/audit/size then realmoviecandidate if valid. App/normalmarker untouched; no game/Simulator, G6/fullgoal open.

- Prior turn progress: actual whole-chunk assembly. Added full1024entry differential harness, separate TUs/actual CPU memory helpers. First76847 sanitizer failure from unchanged reference on uninitialized zero-sized RAM; test setup corrected to valid4096MEM1 for everymode, not product code.
- Same corrected85607 observed live through compile then FINISHED0:16384 comparisons, capped0. FullCPUState/PC/downcount/memory/ordered callbacks agree including callback state mutations. generated/chunk-entry-r183-fixed-test.log. Synthetic cases do not prove gameplay or hardware exception behavior; optimized/LTO, active journal/reservation coverage remain.
- Next expand those narrow gaps before isolated singlechunk link using accepted objects/flags. Do not ignore R182 candidate profile/code-size costs or promote based on tests. No runtime/build/Simulator/app change; fullgoal/G6 open.

- Prior turn progress: actual four-entry block correctness. New isolated chunk probe retains original function fallback and every instruction; normal dispatch195leaders vs829suffixroutes. Byte-exact body assertions pass, no execution proof yet. Actual build flags read from Ninja, no-LTO only for assembly.
- Compile85259 exits0: profile-guided reference12233instructions, candidate normal17934+fallback21512, candidate profile missing warning. Profile-free control61548 exits0: reference22098 vsnormal17987+fallback21562. Normal smaller, combined79%larger; static counts not FPS/linked-size gain. Detailed loads/stores and artifacts in PERF.
- No module build/promotion. Next full1024entry differential harness with callbacks/loops/PC/cycles before linking isolatedcandidate; retain profile penalty and code-size costs. App/vendor unchanged, no runtime/build/Simulator; fullgoal/G6 open.

- Prior turn progress: offline range analysis and fullsuite pass. Added private-source-pinned block probe extracting actual80453AAC–AB8/body/four cases. Candidate separates leader into label-free helper; interior PCs retain reference.400000 full-state/branch-target/cycle comparisons pass ASanUBSan. Corrected unsigned arithmetic in independent test expectation after initial assertion; parity itself passed.
- O2 static65/13loads/8stores reference vs37/5/5 normal plus6/1/0 routing. Not dynamic improvement: original includes all suffixes; clear local benefit only demonstrated gpr12 reload elimination. Actual linked/PGO/whole-CFG effects unproven; selected block has no callback/exception operations.
- Next isolated complete hot-chunk route/entry experiment with all cycle/callback/CFG semantics retained, actual assembly/code-size gate before module build. No product-source/package changes/game/build/Simulator; G6/fullscope open.

- Prior turn progress: instruction/core/thermal evidence. New offline parser plus focused synthetic regression pass; suite wiring added, fullsuite not rerun. Exact trace range results saved summary-r180.json/decoder-r180.json. Pair address regions875/31081=2.815%; specific CLZ827=2.661%; no repeated rejected candidate. Skid/zero-count limitations recorded in PERF.
- Inspected actual hot emitted code and ARM64: per-instruction external switch predecessors plus CPUState traffic suggest a larger control-flow/register-propagation issue. Next isolated normal-block versus interior-suffix entry experiment, preserving all state/cycles/exception/CFG behavior. Assembly benefit must precede broad generator changes. No game/product-source mutation/build/Simulator; G6/full scope open.

- Prior turn progress: post-change CPU attribution. Existing text capture cannot recover grouped offsets; checked local sample docs and Xcode templates/help before new runtime. CPU Profiler preflight14617 exported valid cpu-profile (exit54 due time-limited launched sleep); no permission workaround. Actual attach46155 10s exits0; cpu/thermal exports65846/47377 succeed.
- Fresh movie-instructions-r179 R85profile/one trigger80136, exact package, runtime6707 native-only clean0, visible advancing movie, NANDa574...afa6 unchanged.162.8s79underruns/3backlogs diagnostic-only. No game/Simulator/build remains.
- CPU31081 samples,31054 P/27 E. Thermal Fair entire10.875s (not clock/throttle proof). Raw leaf addresses and cycle weights available now; top paired-store sites map GQR/MEM2-pointer loads, not prior epilogue. PERF records histogram. Next aggregate hot instruction ranges before a distinct guarded-code candidate; avoid repeating rejected CLZ/type0RAM/PGO/globalinline. App unchanged/G6 open.

- Prior turn progress: packaged exact optimization and visible smoke. Fresh R85 isolated movie-profile-r178, exact current signedmodule/runner and hashes, no trace/build overlap. Runtime57312, trigger30876 once, sample20989 10s on PID44316 all exit0. Visible movie advances; intentionally closed after sample, no completion claim. NANDa574...afa6 unchanged/fallback0/smc_failed0. Whole145.2s81underruns/4backlogs not acceptance.
- CPU7509 samples; inclusive Run7283/dispatch6921; exclusive leaves sum7509, top decoder804530A0=1128/804520A0=896. Full breakdown in PERF. Binary base0x133f04000, sampled first804530A0+79220 resolves0x579cf3c at CNTLZW cascade end, flags/guarded coefficient memory access follows. Historical notes already cover this region and rejected CLZ/PGO; no recycled candidate.
- Symbol-collapsed offsets do not establish per-instruction weight. Next bounded raw-address CPU histogram to resolve remaining generated decoder overhead before another semantic-preserving candidate. No product-source/package changes this turn; no runtime/build/Simulator remains. G6/performance/audio/full scope open.

- Prior turn classified progress: canonical reproduction, test-baseline fix and normal selection. Resumed same package64332; it finished0. Audit72877 passes; runner hash unchanged affbcb51...9e295, signed module14ea8867e2b0a47caf761b2ba24ff608cd39a27b1e9290dbc2302f6d4651f184. Preserved previous app232128Z.
- Exact packaged wrapper37922, explicit in-app module, fprf-canonical-r175 real-save profile, no phase trace/savestate/build overlap. Ready title inspected before fixture53317; visibly loaded one-star Observatory. Native pause state appeared, resume cleared and animation advanced; right45847 changed Mario/camera. Native close exit0/fallback0/smc_failed0/saveSHA5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged.156.4s whole-run9underruns/8backlogs includes deliberate12.5s pause; not audio/performance proof.
- Optimization packaged and compatibility-validated, G6 still open. No game/Simulator/build remains. Next fresh post-change CPU sample during same R85 heavy movie to locate residual cost; no more identical timing comparisons. Read-only Petari THP search confirmed available source but symbols are Korean-region clues, not exact-address evidence.

- Final update: selection82587 and suite36660 both exit0. Normal marker now canonical/cache hit, no regeneration. Package64332 ACTIVE, generated/package-r176.log. Poll same handle; then audit package/signatures/hashes and run prepared real-save profile through actual packaged wrapper. No game/Simulator launched.
- Same build2122 finished0; audit5001 passes. Canonical SHA494c2a71d16963cf720af228ff8b9323a1d5b2967776cb759aa11f161beb3fbe exactly equals measured experiment. Manifest source28d765f74af57637/policy rmge01-fprf-119-v1/cache27fc425ac63117e7/originalPGO verified. This permits reusing R169/R171 exact-module evidence with accepted runner; no redundant timing or explicit-module launch. New package still requires wrapper/runner smoke.
- Fixed two private experiment tests to use historical midblock-r144 source and original chunkSHA2dc3b915db7bd33c84a0481499fefd0ee91a201bc20cb6540248caf06729d539, not mutable selection. Test39259 exit0: canonical policy and64800 full-state/host-flag chain comparisons.
- Copied canonical module+manifest into new normal27fc cache and linked generated source, preserving all old artifacts. Normal selection82587 active (module-select-r176.log); fullsuite36660 active (check-r176.log). No bootstrap until suite/build terminal. Next package/audit/exact-wrapper real-save smoke; standalone runner relinked since last package so do not assume its identity. App unchanged/no runtime/no Simulator; G6 performance/audio and full PRD scope open.

- **Prior turn:** progress, canonicalgeneration/1321unchangedchunks/build. Same2122 authoritatively running,1275/1332 latest. Do not restart or testgame alongside link.
- **Correction:** source fingerprint is cache/manifest only; actual module export contains ABI/game/guest tables. Fixed inaccurate embeddedfingerprint wording in status/perf/journal. CanonicalchunkSHAf2d6911016e8e1c9f46caf5ee97dece58689f5b5f51397860bb868383c34bd61 recorded.
- **Preparation:** fresh fprf-canonical-r175 from real one-star save, no savestate, Pipe configured/NAND5040...64a6 verified. Next same2122link/manifest+binaryaudit then canonicalmodule/acceptedpackagedrunner smoke. App/normalmarker unchanged, no game/Simulator; G6/fullgoal open.

## 2026-09-06 — R174 canonical module build

- **Prior turn:** progress, bootstrapintegration/fullsuite/compileinclude correction. Port17259 finishes0; c83d7fed4abba224a0b9e5e53fbc65cfa3f1e925a3dfc98e54b1f4016dbb3dd1. Dependency graph relinks standalone runner too; normal packaged runner/app remain unchanged.
- **Active:** canonical build2122 with GALAXYPAD_MODULE_OUTPUT=generated/modules-fprf-r174 and originalPGO. Newcache27fc425ac63117e7; exactDOL/Broadway preflight passes. Generatedchunks diff: onlyhot804520A0 differs,119deferredcalls,1321others byteidentical. Compilation underway, generated/module-fprf-r174.log. No normal selection mutation.
- **Next:** pollSAME2122 throughlink/audit, verify policy manifest/source identity, run accepted packaged runner with explicitcanonicalmodule and isolatedprofile. No gameplay/build overlap or Simulator. Dependency/upstream ledgers updated. Fullgoal/G6 remains open.

## 2026-09-06 — R173 bootstrap integration and compile correction

- **Prior turn:** progress, canonicalpatches/applicability. Wired policy/helpers/hashpins/peel-restore/scope in bootstrap.94019/97618 both0; runtimefloat matches tested554149a2...ab934f. Added region/chain/emitted source-only regressions to fullsuite;62132 exits0, generated/check-r173.log. Canonical policy+exactcandidate64800 tests also pass after adapting candidate audit to integrated helperprefix.
- **Causal build error:** port38342 fails missing<regex> (standalone driver had supplied it). Waited fullsuite terminal before reverting ONLY policy overlay; canonicalpatch now includesheader, SHA881996463a31728375b651b5cdf96f165f5a087ccc5c85d94851fc4a21421488 supersedesdf640. Fixedbootstrap51517/repeat15819 both0. No bootstrap/source-test overlap.
- **Active:** port17259 rebuild, generated/port-r173-fixed-build.log; pollsame. CMake may rebuild dependencies whose files were peeled/reapplied; app/runner/package remain untouched. Next toolcompletion/cachepolicyverification and canonicalmodulebuild then exactartifactsmoke. No game/Simulator; fullgoal/G6 open.

## 2026-09-06 — R172 canonical policy and patch preparation

- **Prior turn:** progress, real-save/pause/input compatibility. Added standalone C++ fprf-module-policy.inc, exactidentity/backend/chunk policy key, originalchunkhash guard and strict119-site transform. test-fprf-policy.py passes ASanUBSan/exactcandidate comparison and negative identity/reapply/boundary checks. Expected wronghash stderr in test is intentional.
- **Patches prepared, not applied:** ModernGekko0015 SHAdf640115b80156cb9e46208e6e53bc49d350a07c1f23921419b4ec49293a548c adds helper/policy cache+manifest/application beforecompile. Dolphin0014 SHA661f2a2452e2240018140fedae6baaeea3b5c3e723d9266e3644f42581d1d019 appends exact4tested runtimehelpers. Both applicability checks pass. Initial zero-context portpatch rejected, regenerated with context; no vendor mutation occurred.
- **Next:** bootstrap variables/hashpins/peel-restore/order/idempotency plus canonical compiled integration tests, then port/module rebuild with correctfingerprint. App untouched, no runtime/Simulator/build; fullgoal/G6 open.

## 2026-09-06 — R171 real-save and native lifecycle smoke

- **Prior turn:** progress, reverse control supports incrementalgain. Exact candidate untraced with copied one-star save/no savestates. Fixture84490 loads Observatory, pause indication/resume animation verified, movement80933 changes view. Close4989 exit0/fallback0/SMCfailed0/NAND5040...64a6 unchanged.181.4s10underruns/10backlogs includes21.37s deliberatepause, not timing claim.
- **Next integration boundary:** preserve119sites via exactDOL/backend/chunk/source guards and explicit policy cache identity. Module source collector already covers runtime edits but experimental manifest refers to baseline sources; cannot promote by copying into oldcache. Canonical preparation/rebuild/audit+smoke required. No source/app selection change yet, no game/Simulator/build, G6/fullgoal open.

## 2026-09-06 — R170 reverse accepted control

- **Prior turn:** progress, candidate53.6FPS/visiblecompletion/movement. New accepted reversecontrol91740, fresh R85profile, trigger85181once. Fixed51.6833FPS/p9920.864ms/DMA27.5904kHz brackets candidate with prior51.5. Supports~3.7–4.1%gain; no more unchanged pairs now.
- **Evidence:** movie completes/plaza, nativeclose0/fallback0/SMCfailed0/NAND unchanged;234s152underruns/4backlogs. No extra game/Simulator/build/profiler. powermetrics non-root read refused; no privileged retry, thermal attribution unproven.
- **Next:** candidate G5/pause/real-save smoke and reproducible canonical119-site integration/source identity before package promotion. App unchanged/candidateunselected, no running processes. Fullgoal/G6 still open because baseline60Hz/audio unresolved.

## 2026-09-06 — R169 linked candidate and first movie result

- **Prior turn:** progress, exact source-chain tests plus verified link wait. Same67896 completes0/audit passes; moduleSHA494c2a71d16963cf720af228ff8b9323a1d5b2967776cb759aa11f161beb3fbe, +304bytes. Linked119deferredcalls verified.
- **Runtime evidence:**42140, isolated fprf-r168, title/restore visible then trigger87012once; fixed53.6FPS/p99 20.540ms/DMA28.6144kHz versus accepted51.5 (~4.08%promising, not final attribution). No extra launcher/build/profiler/Simulator. Movie completes/plaza, right29759 visibly moves Mario/camera, native close0/fallback0/SMCfailed0/NAND unchanged.
- **Boundary/next:** whole234s121underruns/12backlogs is failed audio/60Hz acceptance. Candidate stays unselected. Next fresh accepted reverse control then bounded integrate-or-park decision; app unchanged, no runtime/Simulator/build, G6/fullgoal open.

## 2026-09-06 — R168 actual candidate-chain audit

- **Prior turn:** progress, isolated candidate compilation/link. Same67896 is authoritatively live, ld33004 observed3:42/~702%CPU; do not restart.
- **New evidence:** test-fprf-candidate-sources.py audits exact source delta and tests all43real chains/162suffixes, including longer-than-two-operation chains and actual guest register assignments.64800 full-state/hostflag comparisons pass ASanUBSan;26587 exit0, generated/fprf-r168-source-check.log. No timing benchmark alongside link.
- **Preparation:** fresh runtime/fprf-r168 copies R85Config/Wii/config/slot3, Pipe configured. SlotSHAaea2...7ab8/NANDa574...afa6 verified. Not launched; next same67896 link/audit, actual linked shape then movie measurement. App/normal selection unchanged, no game/Simulator; G6/fullgoal open.

## 2026-09-06 — R167 isolated object-reuse candidate

- **Prior turn:** progress, emitted-path correctness. New build-fprf-experiment.py source-pins acceptedmodule909889...cc9de/chunk2dc3...d539/float3026...4806c/originalPGO, obtains exact flags/object graph from accepted build.ninja, records all accepted object hashes and commands. Only119hotchunk calls change; original float source retained with4separate deferred helpers appended. No cold/classifier prototype combined.
- **Build:** first preparation assertion assumed1331objects, actual graph1329 (1322chunks+7runtime/export); corrected before compile. Build67896 now ACTIVE, both units compiled and linker33004 CPU-active. Reuses1327accepted objects. generated/fprf-r167-build.log/provenance; no app or normal selection mutation. Chunk SHA6b20f948c1b52b6779eebec409ba9bd2b9d1e3b73c904338a40b4e0b28b5b929; float554149a2e7d08783402af38e5a2b898aff476370b0e3a6af0ed4bd411aab934f.
- **Next:** poll SAME67896 to terminal/audit, inspect linked shape and isolated movie test, not another full rebuild. No game/Simulator. Additional reverse-transform assertions added to build script (syntax-check passes); current compiled candidate scope already follows same deterministic transform. Fullgoal/G6 remain open.

## 2026-09-06 — R166 emitted execution proof

- **Prior turn:** progress,283static candidate sites and negative boundary tests. New test-fprf-emitted.py uses actual emitter and runtime exception/arithmetic paths;80000 comparisons pass allentrysuffixes/FPavailability/rounding/downcount/MFFSobserver, ASanUBSan. Full focused run66811 exit0 with256kchain/region tests too.
- **Scope:** hotchunk119/224eligible supported arithmetic writes is static coverage only. No generated/reference/app mutation, no game-speed claim.10Gi free. Next isolated source-pinned hotchunk/deferredhelper candidate with unchanged accepted objects/PGO/strictFP where possible; no whole-module regeneration merely to change one chunk. Actual link audit and game test required. No runtime/Simulator/build; fullgoal/G6 open.

## 2026-09-06 — R165 emitted-region audit

- **Prior turn:** progress,256000 chain-state differentials. Current bounded decision is coverage and barriers, not full compilation.
- **Change/evidence:** read-only audit-fprf-regions.py plus test-fprf-regions.py. Negative tests pass for observation/exit/exception/CFG/unknown boundaries; exact active-module generatedC gives283sites/69chunks,119 hot804520A0. Run15764 exits0. Counts are static opportunities only.
- **Source audit:** FP-availability gate reads lazy enable/MSR and unavailable path returns before arithmetic; selected arithmetic helpers do not alter these inputs. No actual emitter transformation yet. Next generated-chain execution tests including unavailable FP/external entry/downcount/observation boundaries before isolated candidate. App/reference source unchanged, no runtime/Simulator/build; G6/full goal open.

## 2026-09-06 — R164 observer audit and dataflow proof

- **Prior turn:** progress, accepted control51.5 rejects cold candidate promotion. Rechecked existing untraced R153 profile and traced R162 sample (22/7412CPU RecordPhase) instead of running another tracing-only comparison. No evidence that logging is dominant.
- **New bounded evidence:** tests/test-ps-fprf-chain.py extracts unchanged arithmetic with ONLY first-operation FPRF write omitted.256000 chains across16op pairs/4rounding/aliases/random+special/FPSCR/maddvariants pass final wholeCPUState/hostflags under UBSan.248110 intermediate FPRF differences deliberately prove why this cannot be a global helper change.
- **Source boundary:** actual generated804527E8add→804527ECsub offers consecutive writer case; following store/exception path is a hard barrier. Next conservative emitter analysis and negative boundary tests, quantify eligible coverage before any costly module build. No production change/performance claim, no runtime/Simulator, goal/G6 open.

## 2026-09-06 — R163 accepted control and broader diagnosis

- **Prior turn:** progress, candidate failure/user lag corroborated. Goal is discriminate candidate from host variation, not continue micro-tuning. Fresh accepted-r163 copies R85Config/Wii/slot3; one runtime46172, trigger93056 once after visible checkpoint. No extra launcher/Simulator/build.
- **Evidence:** fixed minute51.5FPS/p99 22.205ms/DMA27.4944kHz versus candidate30.0; accepted reproduces prior51.72. Candidate rejected for promotion, precise causal regression still not isolated. Host-top55857 exit0 collects9samples10s apart; no swap delta, lowpowermode0/no recorded thermal warning, not definitive frequency/scheduling proof.
- **Broader finding:** phase trace line-buffers every event with shared mutex; quantify observer overhead next. SunPad QoS is explicitly experimental, not a stable fix to import blindly. Runtime hardcodes StaticRecomp; no hidden JIT path used.
- **Close:**46172 exit0, fallback0/SMCfailed0/NANDa574...afa6 unchanged. Movie visibly advances but not completed;152underruns/3backlogs over176s. No game/Simulator/build remains. App unchanged, G6/full goal open. Next low-overhead/untraced measurement boundary and structural CPU/decoder cost analysis, not another arithmetic microbenchmark.

## 2026-09-06 — R162 linked candidate and movie startup

- **Terminal update/user direction:** user reports22FPS and requests broader underlying diagnosis. Fixed60s minute30FPS/p99 84.99ms/DMA16.015kHz fails badly; do not blame machine or candidate alone without fresh accepted control. Later sample35677 completes after measurement: CPU6985/7412Run,6719dispatch; video6743wait. Host no recorded thermal warning, no observed swap delta; neither excludes transient host effects. CPU thread entry lacks explicit QoS in inspected source, not yet established defect.
- **Completion:** visible title→R85load→trigger63277 once→advancing movie. Stale CUA snapshot auto-openedfrontend31268, closed before measured sequence. Movie not completed; native close15389 exit0/fallback0/SMCfailed0; NAND unchanged;423underruns/6backlogs over238.7s. No runtime/Simulator remains. Candidate stays isolated. Next accepted control with host-pressure/scheduling evidence then structural CPU/AOT analysis, not another tiny helper experiment.

- **Prior turn:** progress, expanded scalar correctness coverage. Same build21911 polled to terminal0; module audit passes. Candidate SHA54ac7e20643caa5679fdbbe4553c984766d2fd05b6e2f03998c261146ac40a2d.
- **Linked evidence:** generated/cold-fp-r162-reference-{add,sub}.asm and candidate.asm. Add/sub127 instructions each versus196/292; add stack80 versus96bytes; identical cold functions folded to one address. Whole module grows0.98% (115444744 versus114321880bytes); helper reduction does not establish whole-game gain.
- **Runtime:**15389/PID31256 launched with accepted runner and explicit candidate module, cold-fp-r161 profile and phase trace. Initial existing CUA game snapshot timeout; no second launcher started. Next retry existing binding after startup, visibly load protected slot3 and trigger attack once, measure complete fixed minute, verify movie completion/native close/NAND. App runneraffb...9e295/module5f810...097c/signature remain accepted. No Simulator; G6 open.

## 2026-09-06 — R161 scalar exception coverage during candidate link

- **Hypothesis/check:** outlined ni_add/sub are shared by scalar callers that consume FPRes.exception; paired-only state comparison does not directly verify that returned field. Added200k direct result/exception/full-state/host-flags comparisons over4rounding modes and special/random inputs. Existing1M paired and10k actual-register-call-mix comparisons retained.
- **Evidence:** correctness-only mode disables timing loops while build is active. Run84908 exits0, both UBSan and unsanitized checks pass; generated/cold-fp-r161-correctness.log. No full suite repeated because product source/package unchanged and isolated focused test is the changed layer.
- **Active build:** SAME21911 reaches1330/1332; linker30839 observed actively consuming~705%CPU. No runtime measurement alongside build. Prepared disposable generated/runtime/cold-fp-r161 from R85Config/Wii/config.ini/slot3 and configured Pipe, no launch.
- **Next:** poll21911 through link/audit, inspect linked helper shape, then candidate movie measurement with accepted runner and isolated profile. No app/normal-module selection mutation. No booted Simulator or game; full goal/G6 remain open.

## 2026-09-06 — R160 isolated cold-FP module build

- **Prior turn:** progress, call-mix correctness/micro improvement. Added deterministic source-pinned cold_fp_transform.py; original3026eb10...4806c required, original function names/operations preserved, unchanged NaN-result blocks extracted. Candidate floatSHA3094efccd08f31c676df672928accfd6af0da6647e6860604bcf028fbc190f2e. Tests consume the same transformation rather than a separate candidate rewrite.
- **Build contract:** scripts/build-cold-fp-experiment.sh checks originalPGO, verifies isolated runtime file set/content (all other files exact), accepted module9098890e...cc9de and generated source link. Candidate manifest selected=false, original source fingerprint explicitly renamed reference fingerprint. Compile/link both originalPGO, O2/ThinLTO/strictFP/macOS14.
- **Active work:**21911 running, generated/cold-fp-r160-build.log;70/1332 observed. Prebuild1M helper+10k call-mix CPUState/hostflag tests pass UBSan/unsanitized. Keep same handle/cache, no restart. Next link/audit then compare linked helper shape and movie behavior/performance.
- **Safety:** accepted app/normal marker untouched, no runtime/Simulator; G6/full goal open.

## 2026-09-06 — R159 generated register-call mix and assembly boundary

- **Prior turn:** progress, cold-exception helper correctness/micro gain. Hot804530A0 is largely bit-decoding; arithmetic calls live804520A0. Extended test-cold-fp.py with first24 actual add/sub register calls; explicitly a call mix, not contiguous guest execution or complete decoder.
- **Tests:** additional10k full CPUState/hostflags mix comparisons across4rounding modes pass, alongside1M prior helper comparisons. Corrected unsigned negation in benchmark initialization to -(double)r. Final unsanitized mix candidate/reference .7857,.7932,.7880,.7845; add.709,.706,.702,.698. Sanitized timings excluded.
- **Code-size caveat:** standalone assembly instruction lines original add/sub86, candidate132, cold functions34each. Original/candidate have different inlining decisions; not directly comparable to signed ThinLTO/PGO module or proof of code reduction. Do not extrapolate~21% call-mix improvement to FPS.
- **Next:** isolated full-runtime cold-block transformation preserving original symbols, originalPGO and accepted generated source; build candidate, inspect linked helper shape then real movie measurement. No more micro repetitions, no app/vendor edit or runtime/Simulator. G6 open.

## 2026-09-06 — R158 exception-path outlining prototype

- **Prior turn:** progress, classification-only differential; uncertain small helper gain. Prior inlining audit: failed change was forced ppc_psq_load_inline, not arithmetic cold-path extraction. It grew representative assembly19%/module2.3% and hurt runtime. GQR0centralization was separately rejected.
- **New isolated mechanism:** test-cold-fp.py extracts exact current ni_add/ni_sub NaN-result blocks into noinline,cold functions. Same initial add/sub evaluated once; unchanged NaN precedence/exception/FI/FR/infinity handling; unchanged paired helper body uses new internal routine. No classifier change, finite-only shortcut, vendor edit or game patch.
- **Evidence:**1M complete CPUState and host-FP-flags comparisons over4rounding modes/special/random/aliases pass UBSan and unsanitized.4alternating5M normal-add micro ratios .708,.701,.700,.687. Sanitized numbers excluded. Promising only at helper scope; earlier finite-path micro improvement failed whole-game test.
- **Next:** actual generated-callsite code-size/assembly verification and broader helper/mixed coverage before candidate module. No package mutation/runtime/Simulator, G6 open.

## 2026-09-06 — R157 full-helper classification differential

- **Prior turn:** progress, classifier-only parity/micro gain. Added test-classify-helpers.py; extracts actual add/sub/mul/madd bodies, changing only classifier call. Reuses strict test harness, removes rejected finite-arithmetic candidate completely.
- **Correctness:**1M CPUState and host FP exception-flag comparisons,4host rounding modes, arbitrary FPSCR/special/random operands, destination aliases pass UBSan and unsanitized. Shared unchanged internal math helpers; not whole-Dolphin lockstep.
- **Performance boundary:** initial complete-add benchmark ratios1.030,1.010,.996,.998. Expanded uniform-dispatch harness add.930–.956/sub.944–.956/mul.922–.927/madd.953–.969. Sanitized timings explicitly excluded. Add changed with harness/layout; no robust whole-game gain inferred, no module build.
- **Next:** compare actual emitted helper code and audit exact scope of earlier failed inlining before choosing higher-impact candidate. Classification remains unselected. No vendor/app mutation, runtime/Simulator or G6 closure.

## 2026-09-06 — R156 normal-result classification prototype

- **Prior turn:** progress, RAM-pair prototype parked. Signed ps_add_op0x41a0 arithmetic/FPSCR path inspected; classify portion0x42d4 onward computes exceptional alternatives even on normal results. No assumption that sampled offsets are individually weighted.
- **Prototype:** unapplied classify-f32-normal.inc returns0x04<<sign for normal exponent using integer check/branch expectation; existing NaN/infinity/subnormal/signed-zero classification retained. Arithmetic/rounding/exception paths untouched.
- **Evidence:** tests/test-classify-f32.py extracts actual classifier. Exhaustive33,554,432 special-class bit patterns, normal exponent/sign boundaries and1Mrandom parity pass UBSan.6 alternating10M-call normal-result micro ratios .5733,.7157,.6456,.6650,.6720,.6518; final warm~33–35% improvement only in isolated classifier.
- **Next:** full arithmetic-helper state/host-FP-flag differential and representative helper microbench before build. No game performance claim, vendor/app mutation or runtime/Simulator. G6 open.

## 2026-09-06 — R155 paired direct-RAM-store prototype parked

- **Prior turn:** progress, fresh profile and helper attribution. Added unapplied patches/experiments/psq-ram-pair-store.inc: only two-lane type0, no journal, valid8-byte direct RAM region, CPU alias exclusion; preserve sequential conversion/reservation/store per lane. Original path otherwise unchanged. No vendor edit.
- **Correctness:** tests/test-psq-ram-pair.py extracts actual inline body/includes actual memory helpers.100k ASan/UBSan comparisons cover arbitrary FP bits, MEM1/2 mirrors/boundaries, reservation lines, journal/external callback lane mutations, type/w/LSQE fallbacks; explicit CPU-state alias case passes. Slow quantized helper is a test double; unchanged fallback routing tested, not quantization revalidated.
- **Decision:** unsanitized6 alternating5M-call MEM2 trials candidate/reference .7805,1.1076,1.0039,.9993,.9903,.9877; early warmup dominates, final benefit negligible. Park without full module build. Standalone benchmark retained, not automatic green performance gate. Source/app unchanged.
- **Next:** inspect current sampled arithmetic helper instructions for specific avoidable work distinct from rejected finite-FP and blanket-inline candidates. No game/Simulator, G6/full goal open.

## 2026-09-06 — R153–R154 fresh movie profile and helper attribution

- **Prior turns:** progress, current package smoke then fresh diagnostic profile. Real R85slot3 restored from inspected title, trigger47932 once.10s/1ms sample23410 collected70218 exit0; visible movie advances before/after. Close74133 exit0/fallback0/smc_failed0; NANDa5743199...afa6 unchanged.181.3s123underruns/6backlogs sampled mixed run, not performance acceptance.
- **CPU-only evidence:**7982samples,7549Run/7257dispatch. Exclusive totals subtract direct children and sum7982: decoder804530A0=1184,804520A0=1027,add674,madd586,store_inline533,sub470,load_inline369,convert318,store315,mul212,ExternalPointer196,Run194,TryGetPair179. Private movie-profile-r153/cpu-sample.txt.
- **Attribution:** loaded module base134a70000, hot store-inline13a1f8d64 resolves file offset5788d64. Disassembly preserves GQR/LSQE fallback, integer conversion, duplicate per-lane RAM bounds/reservation/journal, full register frame; sampled+632/+640 epilogue restores. Collapsed offset lists do not assign individual weights. No wholesale-inline inference.
- **Next:** isolated type0 paired direct-RAM eligibility/check-sharing contract and differential tests preserving sequential lane values/stores, reservations, journal/external callbacks, aliases/boundaries. Quantized pair path unchanged. Need prove benefit before module build; no rejected FP/PGO repetitions. No game/Simulator, package unchanged, G6 open.

## 2026-09-06 — R152 packaged real-save smoke

- **Prior turn:** progress, correctness package/audits complete. Ran actual normal wrapper/signed in-app module with LC env overrides unset and isolated package-midblock-r152 Config/Wii. Inspected ready title, fixture78409 once finishes0, visible Observatory59.9 and movement47530 visibly relocates Mario60.1 snapshot.
- **Result:** close99899 exit0, fallback0/smc_failed0, save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged.139.7s7underruns/5backlogs not audio acceptance. No game/Simulator/build remains.
- **Next:** R94 movie CPU profile predates pair/timing fixes; refresh a bounded current-package movie CPU sample from exact R85 checkpoint, attribute current leaves before next optimization. This is diagnostic, not another unchanged timing comparison. Preserve strict FP/memory semantics; rejected finite-FP, PGO, widening and blanket-inline experiments stay rejected. G6/full goal open.

## 2026-09-06 — R151 normal correctness package

- **Prior turn:** progress, G5/lifecycle pass and normal cache prepared.35483 completes0 with new-cache hit, no regeneration. Full suite36917 exit0 in generated/check-r151.log after bootstrap source settled.
- **Package:**46291 exit0, generated/package-midblock-r151.log. App/module/signature audit passes. Signed runneraffbcb51f27f853b0b5ee403b5033c3bd5a6f7ea3e514b969c2ef4272af9e295 unchanged; signed module5f810ae0ed1f056c7ea8b2256d124405b0b05c6a68f4fd9d7c35fda3068d097c. Prior app preserved generated/macos/GalaxyPad.app.previous.20260906T194009Z.
- **Boundary/next:** correctness update packaged, no G6/60Hz/audio closure. Fresh packaged-wrapper smoke next, then concrete remaining THP/audio cost investigation; don't repeat rejected finite-FP/global-inline/PGO candidates. No game/Simulator/build remains.

## 2026-09-06 — R150 file-select/pause regression and correctness selection

- **Prior turn:** progress, representative movie shows no material speed gain. Fresh midblock-g5-r150 copies real one-star config/NAND, no states/trace. Inspected ready title, G5 fixture91269 once exits0; pointer visibly highlights Mario file1 at59.9FPS.
- **Lifecycle:** native Misc/Toggle pause displays explicit paused subtitle. Resume clears indicator and scene animation advances. Native close81007 exit0/fallback0/smc_failed0. Save5040acdd...64a6 unchanged.260.1s3underruns/4backlogs,39.4s max frame/DMA gap is deliberate pause, not spontaneous freeze. Not audio acceptance.
- **Decision:** accept proven cycle correction for packaging, not a claimed speedup/G6 pass. Copy audited9098890e...cc9de plus manifest into normal da63c951ce4d7349 cache, generated-source symlink to candidate; old artifact preserved. Normal originalPGO build-module cache-selection35483 ACTIVE, generated/module-select-r150.log. Next same handle completion/audit then normal packaging with prior app backup and signed-artifact check.
- **Safety:** no runtime/Simulator remains; accepted app unchanged pending package step. Remaining movie/audio problem still open.

## 2026-09-06 — R149 representative movie validation

- **Prior turn:** progress, candidate real-save smoke. Fresh midblock-movie-r149 copied R85 Config/Wii/config.ini and only slot3; checkpoint/hash verified. Candidate9098890e...cc9de, accepted wrapper, phase trace, no competing game/build/debugger/Simulator. Existing CUA handle avoids extra launcher.
- **Execution:** title inspected, native LoadState3, checkpoint visibly verified, trigger71167 once exit0. Movie visibly advances and completes to attacked plaza59.9. Native close24464 exit0, fallback0/smc_failed0; NANDa5743199ed343b04d2d02069881d8754310194ed2b3012f4ec54152272a2afa6 unchanged.
- **Measurement:** scripts/measure-attack-trace.py complete fixed minute51.716667FPS, framep99 20.930459ms, DMA27.6096kHz. Near prior51.867–52, no meaningful speedup or exact causal regression claim. Whole223.05s155underruns/4backlogs keeps audio open.
- **Next:** G5/pause-resume regression then select correctness fix if clean; remaining THP bottleneck separate, no more unchanged timing repetition. App/normal marker unchanged, no game/Simulator remains, G6 open.

## 2026-09-06 — R148 module completion and visible candidate smoke

- **Prior turn:** verified wait. Same87320/linker19705 continues then exits0; no restart. Module audit passes RMGE01/ARM64/macOS14/1322chunks/19SMC. SHA9098890e25b36e802ab5f793c9ad7800f991190f41e972996937343d84ecc9de.
- **Runtime:** accepted wrapper/runner with explicit candidate and isolated midblock-r145 profile,53653. CUA getApp during startup opened extra frontend; closed via native window without Play. No second game started. Inspected ready title59.9, ran fixture74977 once to real one-star Observatory59.1; movement47235 finishes0 and visibly relocates Mario,59.6FPS snapshot. Clean close53653 exit0.
- **Evidence boundary:** native1202396346/fallback0/smc_failed0;5691frames over97.074s,43underruns/10backlogs over97.4s. Save5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 unchanged. Short smoke with startup/title/load included, not matched timing or audio acceptance.
- **Next:** controlled representative heavy-scene timing and G5/lifecycle regression before promotion. Candidate separate, normal marker/app unchanged. No game/Simulator remains, G6/full goal open.

## 2026-09-06 — R147 same-linker verified wait

- **Classification:** verified wait. Original87320 repeatedly returns running; linker19705 advances52s→4:36 elapsed with~682–718%CPU, RSS~1.1GB. No reported error. No replacement/restart.
- **Next:** wait on the same handle, then audit module and run prepared midblock-r145 real-save profile. Accepted package/save unchanged; no game/Simulator started. G6 open.

## 2026-09-06 — R146 verified compilation/link wait

- **Prior turn:** progress, generated-source scope proof and isolated profile preparation. Revalidated original87320 live, compilation942→1330/1332. Last observation linker19705 active11s at718%CPU under ninja16673/port16459; no restart.
- **Checks:** generated Broadway/zero unknown instructions; retained executable-memory warning, no suppression. Candidate645M/free12Gi before link. No game/Simulator launch or acceptance claim while CPU busy.
- **Next:** continue same87320 through link/publication, audit candidate module then use prepared midblock-r145 profile for ready-title→real-save gameplay validation. Accepted app/normal marker unchanged; G6 open.

## 2026-09-06 — R145 generated-code scope proof and runtime preparation

- **Prior turn:** progress; new timing module compiling. Same87320 remains live, ninja16673 and active compiler children verified,447→652/1332. No restart or replacement build.
- **Evidence:** compared all1322 generated chunks with accepted module generation. Removing only the candidate external-case downcount statements yields byte-identical source in every chunk;1002284 added entry charges, no instruction-body changes. Actual emitted helper proof remains R144.
- **Preparation:** fresh generated/runtime/midblock-r145 contains copies of R138 Config/Wii/config.ini and newly configured pipe. CPUThread=True, native1x, Metal preserved. Real one-star GameData SHA5040acdd95157448d660fd02f03c16c17e240523bdfa889ccbd253f9fe5364a6 verified. No savestate or runtime launched.
- **Next:** poll existing87320 through link and candidate module audit. Use accepted packaged runner with explicit candidate module and prepared profile, inspect ready title before g6-observatory-save-load fixture; validate visible input/gameplay/audio/timing before promotion. Normal app/marker unchanged; single-Simulator limit maintained, G6 open.

## 2026-09-06 — R144 canonical timing fix and candidate build

- **Prior turn:** progress, isolated fix. Added emitted local-return and embedded-data tests; randomized CFG cases include embedded instructions. Tests always obtain pinned original emitter/CFG before applying candidate so integration cannot double-apply it. Focused ASan/UBSan pass.
- **Integration:** bootstrap pins patch99b2e3b16dd5df387f2fdcaa57250de717657eadd717598b0cf22bc9cbf7e11e and audits scope; first99131/repeat52896 pass. Recompiler-only target refreshes port sibling, SHA e569b97d1dabf68d15099ea025be1c0d6b96bcbabbf50b61d6325e83962668aa. Full suite8718 exit0, generated/check-r144.log. No bootstrap/test overlap.
- **Actual-DOL evidence:** probe now extracts real external switch cases instead of hardcoding old dispatch. Accepted source still reproduces0; freshly generated candidate helper80517538 charges4 and performs3stores+blr under UBSan. This proves emitted-path correction, not end-to-end timing/performance.
- **Active work:** module build87320, generated/module-midblock-r144.log; separate generated/modules-midblock-r144/RMGE01/2c680585a8f58e1cc9c5521b579057f12b124ff0ef409e470a57606c50a93c09-da63c951ce4d7349. OriginalPGO hash unchanged. Last observed70/1332. Poll same handle, do not restart; next link/module audit then isolated visible gameplay/audio tests. Accepted app/normal marker unchanged; no game/booted Simulator; G6 open.

## 2026-09-06 — R143 isolated external-entry timing fix

- **Change:** candidate suffix-cycle calculation at every instruction; external switch charges nonleader suffixes while internal labels/fallthrough and leader charging remain unchanged. Corrected malformed initial patch hunk counts; git apply --check passes. Normal vendor generator untouched.
- **Evidence:** tests/test-midblock-cycles.py compiles actual patched CFG/emitter and executes emitted C under ASan/UBSan.19 straight-line entries, direct branch/fallthrough and counted-loop interior/full iterations pass. Fallback charge remains zero and ends preceding suffix; empty CFG passes.10k randomized32-instruction CFGs compare to pinned original: leaders, leader charges, loop boundaries unchanged and all suffixes match original forward-scan oracle. Test added to suite; full suite not rerun here.
- **Boundary/next:** no package/runtime change or performance claim. Add local-return and embedded-data coverage before canonical integration/regeneration; validate timing and gameplay on candidate before promotion. No game or booted Simulator at end. G6/full goal open.

## 2026-09-06 — R142 timing defect reproduced

- **Prior turn:** progress, SMC bounds correctness prototype. Sample leaf OnICacheInvalidate74/7298=~1.01%; shortcut removes only part, so parked without host build/traffic instrumentation.
- **Finding:** emitter provides switch entry for every instruction but charges only CFG leaders. Actual selected helper full entry805174fc charges19 for18stores+blr; observed hot interior80517538 executes3stores+blr with zero generated charge, then host minimum1. Internal fallthrough legitimately avoids repeat charge, but external entry bypasses leader entirely.
- **Evidence:** tests/probe-midblock-charge.py extracts actual selected helper, compiles with stubbed stores and UBSan; confirms register-store counts/return PC/zero interior charge. It is an explicit defect reproducer, not added as green regression gate. R140 observed13971 samples at this entry; frequency not CPU cost.
- **Next:** source-level external-entry suffix cycle accounting, preserving internal fallthrough and normal leader charges; focused control-flow tests before regenerating module. No speculative timing shortcut or runtime mutation. Full G6 remains open, package unchanged.
