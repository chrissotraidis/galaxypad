# R809: counter-free candidate passes source gate; link running

R808 real plaza eligibility (99.21695%) justifies a clean scene speed experiment.
Added --ni --uninstrumented to the existing private builder rather than another
build pipeline. It uses a distinct directory, retains all1329 object/control/
flags/response identity checks, and changes only the same six-instruction region.
The generated source contains no cross eligibility counters or report calls.
No normal source, app, module selection or save is changed.

Extended the exact-source numerical regression with mutually exclusive
--uninstrumented-source support. It requires the extracted helper to equal the
checked-in helper byte-for-byte, including known include/generated-comment
boundaries, and rejects diagnostic identifiers anywhere in the source. The
first check caught an omitted generated comment in the test expectation, before
linking. Fixed the expectation, not product arithmetic. Retained that failed
pre-link directory as cross-uninstrumented-r809-prelink-check.

Corrected exact-source regression passes262144 complete CPU/FPSR cases,
48258 fast paths,256 ties;24129 fast paths separately in NI0/NI1. Builder rerun
also passes before linking. Source SHA256:
a5aa41a80cecc05d7eb1caf31a099915dac2b8d264bbe560e2754516e47d365d.
Existing one-function PGO mismatch warning retained; no matching-profile claim.

## Exact continuation

Exec53784 is running the private link. Poll SAME handle; do not restart on
timeout. Last linker48934 (parent48933) active655%CPU at10seconds. Build logs:
generated/cross-build-r809b.log/.err. Output directory:
generated/candidates/cross-uninstrumented-r809. Provenance records source and
validated inputs before link. No finished binary or speed result claimed yet.

No Simulator booted;32Gi available before build. After link finishes, verify
platform/signature/hash checks, then run baseline and candidate sequentially
with identical host/settings/save/scene and matched frame/audio windows. Keep
one Simulator/game, no builds during runtime measurement, and separate warm-up
from scene measurements. Capture visible scene/input and preserve save backup.
Reject promotion without repeatable end-to-end benefit; full original PRD,
SunPad UI, performance, audio, stability and device goals remain active.
