# iPad test deployment: build 7166, September 16

After the session-close checkpoint, the owner requested the latest build on the
hardware iPad for hands-on testing. Installed the exact retained 7166 app used on
iPhone, without rebuilding or changing runtime settings. No public IPA was made.

Device: iPad Pro 12.9-inch, sixth generation (iPad14,5), iPadOS 26.6.2.
Previous installed build: 15. New installed build: 7166, same bundle identity.
Live installed-app metadata confirms the new version. The iPhone was untouched.

The installed build-15 app's application identity and entitlements match the
candidate exactly. Signing authority matches, the profile includes this iPad,
and platform/device-family, Game Mode metadata and deep strict signature checks
pass. An initial authority-string check used insufficient codesign verbosity;
repeating with -dvv confirmed the match. Installation succeeded in place.

Stopped the old app, backed up 33 Wii save/configuration/preference files, then
read them back after installation before launch: all 33 byte-identical, none
added or missing. No app uninstall, container reset, save replacement or transfer
of iPhone settings was performed.

Launch succeeded with PID 5259. Logs confirm the bundled game module loaded,
RemoteIO initialized and frame events progressed. An initial 15.9-second window
reported 59.937 frame events/s, nominal thermal state and the preserved 2x render
scale. This is startup activity, not demanding-scene or matched performance proof.
Startup logs also contain redacted runtime warning/error events; a live process
and frame activity do not establish that every runtime path is healthy. Audio
counters include startup underruns/drops; audible sound is not accepted by these
logs. Physical gameplay, touch, controller and audio assessment is now with the
owner. No general iPad performance improvement or release approval is claimed.

Private evidence: generated/ipad-7166-20260916/ contains installed-app identity,
device details, before/after backup hashes, preservation.json, install results,
receipt.json, process snapshots and device-console.log. The exact candidate is
still generated/game-mode-7166-20260916/GalaxyPad.app. The source suite passed at
the preceding documentation checkpoint; this deployment changed no app code.

## Owner feedback after testing

The owner reports build 7166 is working great on the iPad and subsequently
authorized publishing this version. This is a positive overall hands-on report;
no specific full-game route, duration or matched performance gain was established.
